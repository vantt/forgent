# Phase 03 — Single live worker per Run

Wave 2 · Gate: Phase 02 · Findings: C1, H2, H13, M1, M9(a), M15(a), L2. Context: review §C1/H2/H13/M1, Phụ lục 4/6/7.

Mục tiêu duy nhất: **không path nào spawn worker thứ hai cho một Run chưa settle** — qua resume, recover, session retry, hay caller unfenced.

## Status — 2026-09-22

**R1–R9 xong trên nhánh `dispatch-hardening-phase03-single-live-worker`** (worktree cùng tên, rẽ từ `main` HEAD lúc bắt đầu — chờ đúng lúc `session-engine.mjs` được track khác nhả, xác nhận qua `git log`/dirty-check trước khi tạo worktree). Đây là phase phức tạp/rủi ro nhất track (đụng `assignment-runner.mjs`, `herdr-round.mjs`, `cli-spawn-supervisor.mjs`, `session-engine.mjs`, `recover.mjs`, `recovery-planner.mjs`, `reconciliation-planner.mjs`, `run-lock.mjs` cùng lúc). Test đích (không phải full suite): 69+40+64+236 ≈ 400+ test qua các suite `assignment-dispatch`, `herdr-spawn-adapter`, `herdr-spawn-assignment-dispatch`, `herdr-reconciliation`, `cli-spawn-reconciliation`, `run-lock*`, `dispatch-reconciliation*`, `dispatch-recovery`/`verbs/dispatch-recovery`, `recovery`, `coordination-session-engine` — 0 fail sau 2 vòng fix-forward (chi tiết ở dưới). Full `npm test` đang chạy để so baseline trước khi commit.

Chi tiết từng requirement:
- **R1 (C1a)**: `executeAssignment`'s resume block trước đây chỉ check `if (rec.settled && rec.runResult) return rec.runResult;` rồi ÂM THẦM RƠI XUỐNG launch-worker-mới nếu reconcile trả BẤT KỲ status nào khác (`waiting`/`held`/`observed`/`refused`/`stale`/`failed`/`parked` với reason khác) — đây CHÍNH LÀ lỗ hổng double-spawn cốt lõi. Sửa bằng allow-list AN TOÀN (không phải liệt kê enum) — chỉ 2 case được đi tiếp: `settled` (trả kết quả) và `parked`+`command-missing` (chưa dispatch gì); mọi case khác throw `RunnerConfigError{code: rec.status==='waiting' ? 'run-in-flight' : 'run-unreconciled', phase:'post-admission'}`. Cách này tự động phủ cả `stale`/`failed` — 2 status có thật trong `reconcileCliSpawnRun`/`reconcileHerdrSpawnRun` nhưng KHÔNG có trong enum liệt kê của R1 (đọc code trực tiếp để xác nhận vocab thật, không đoán theo spec text).
- **R2 (C1b)**: `runHerdrRound`'s resume-shortcut (`return await reconcileHerdrSpawnRun(...)`) trước đây trả THẲNG raw reconcile object (`{status,settled,runResult}` hoặc `{status,reason,...}`) làm chính adapter result của `runHerdrRound` — sai contract với caller đang expect `driveRound`'s shape hoặc runResult phẳng. Giờ: settled → unwrap trả thẳng `rec.runResult`; không settled → sentinel tường minh `{settled:false, observed:true, reconcileStatus, reconcileReason}`.
- **R3 (C1c)**: 
  - Phần 1 (session-ownership refusal): `recover.mjs` import `findCoordinationSessionOwningAssignment` (từ `runtime-inspection.mjs`, KHÔNG phải `src/runner/coordination/` — không vi phạm module's own "never imports from coordination/" boundary, đã verify bằng cách đọc source). `recoverApplyUseCase` refuse `resume-driver` khi assignment thuộc session, throw trước khi acquire lock.
  - Phần 2 (`liveness.fresh`): tìm ra bug thật — `collectEvidence` (recovery-planner.mjs) ĐÃ tính đúng `{id:'liveness', type:'liveness', fresh}` từ lâu, nhưng CẢ `deriveRecoveryFacts`'s `resume` branch LẪN `isActionLegal`'s `resume-driver` branch (2 nơi độc lập, đúng như doc comment "legal right now can never diverge between the two paths" cảnh báo) đều KHÔNG BAO GIỜ đọc field đó — resume được authorize chỉ vì "chưa settle", bất kể driver còn sống hay không. Sửa cả 2 nơi: strict `fresh !== false` → refuse (`park`/`needs-input`), không suy đoán khi thiếu evidence.
  - Phần 3 (claim-clear proof): **thử route qua `planReconciliation`/`applyReconciliation('clear-assignment-claim')` như spec chữ đúng nói, rồi PHÁT HIỆN VÀ REVERT** — trace hết mọi writer thật của `dispatch.claim` (chỉ 2, cả 2 trong `session-engine.mjs`) xác nhận file LUÔN RỖNG (`fs.openSync(path,'wx')` không ghi gì), không bao giờ có `{pid,startTime}`. `planClearAssignmentClaim`'s dead-holder proof (`holder()`) cần đúng shape đó — dùng như spec chữ sẽ làm `dispatchClaimCleared` LUÔN `false` trong production (regression, không phải fix), và một test có sẵn bắt được chính xác lỗi này (`test/verbs/dispatch-recovery.test.mjs`'s dead-driver fixture). Quay về unlink trực tiếp, nhưng giờ AN TOÀN đúng nghĩa vì được 2 lớp mới ở Phần 1+2 gate trước (session-ownership + `liveness.fresh===false` đã bắt buộc) — proof thật nằm ở surrounding apply, không cần đọc lại từ nội dung file claim.
- **R4 (H2)**: grep xác nhận CẢ HAI nơi (`createAndExecuteSessionTask`'s `dispatch.claim`, `retrySessionTask`'s `retry-N.claim`) unlink claim VÔ ĐIỀU KIỆN khi `err instanceof RunnerConfigError`, không check `phase` — nghĩa là lỗi `post-admission` mới của R1 (Run có dispatch thật đang treo) SẼ bị xử lý y hệt lỗi "chưa spawn gì cả" và claim bị xoá nhầm, mở lại đúng race R1 vừa đóng ở tầng session. Thêm `&& err.phase !== 'post-admission'` ở cả 2 nơi.
- **R5 (H13)**: grep ra 6 điểm `publishMutableProjection(commandPath,...)` ghi thẳng vào `controller/commands/<id>.json` không qua control-token CAS — 2 trong `driveRound` (pre-launch, pre-prompt, interim), 2 terminal commit trong `driveRound` (fail-path, success-path), 2 trong `reconcileHerdrSpawnRun` (prepare-confinement interim, settlement-inference terminal). `commitCommandOutcome` cũ (cli-spawn-supervisor.mjs) chỉ support field `state/outcome/receiptDigest/bindingDigest` — mở rộng thêm `patch` (merge field khác cùng lúc, an toàn ngược vì default `{}`); thêm hàm mới `patchCommandRecord` (CAS giống hệt, không đụng state/outcome) cho 3 write không phải terminal-commit. **Bug tự tạo rồi tự sửa**: cả 2 hàm ban đầu gọi `computeSha256Digest(controlToken)` VÔ ĐIỀU KIỆN — crash khi `controlToken===undefined` (2 test có sẵn dùng `reconcileHerdrSpawnRun(runDir)`/`runHerdrRound({...})` không kèm control context, hợp lệ — không phải scenario "stale controller"). Sửa: `controlToken===undefined` → bỏ qua CAS check hoàn toàn (không có live context để fence); có `controlToken` (đúng hay sai) → check y nguyên. Test "stale controller token observes without settling" chạy lại xác nhận CAS thật vẫn hoạt động đúng.
- **R6 (M1)**: `admitRunAttempt` trước đây tính "next available attempt" hoàn toàn KHÔNG check current generation còn sống hay không — một caller không qua đường resume (fresh `executeAssignment` call) có thể tạo attempt 02/03/... mới liên tục trong khi attempt 01 còn đang chạy thật. Thêm check trong `publishNextGeneration`'s callback: nếu `current` tồn tại, chưa có `result.json`, và `inspectRunControl(priorRunDir).held===true` (hàm mới trong `run-lock.mjs`, đọc-only, tái dùng logic dead-holder y hệt `acquireRunControl`'s reclaim decision nhưng KHÔNG có side-effect ghi generation mới vào ledger CỦA RUN KHÁC) → refuse `run-in-flight` trừ khi `forceNewAttempt:true`. **Cố ý để lại gap**: cơ chế core (an toàn thật) đã xong và test qua, nhưng CLI flag `--force-new-attempt` (argv parsing ở `bin/fgos.mjs`/`command-registry.mjs`) CHƯA wire — `opts.forceNewAttempt` đã sẵn sàng nhận từ bất kỳ caller nào (kể cả CLI sau này), chỉ thiếu đường argv → opts. Không đủ ngân sách phase để khám phá an toàn toàn bộ CLI-parsing stack.
- **R7 (M9a)**: `reconciliation-planner.mjs`'s `withLocalLock` (mutex nội bộ cho mọi `applyReconciliation`/`applyCollectResult`/...) trước đây KHÔNG hề reclaim — crash giữa acquire/release để lại `reconcile.lock` KẸT VĨNH VIỄN, chặn mọi reconcile apply sau đó trên host, dù module ĐÃ CÓ sẵn `startTime(pid)`/`holder()` (dùng cho cwd-lock/assignment-claim khác). Viết lại `withLocalLock`: ghi `{pid, startTime}` JSON thay vì raw pid string; gặp `EEXIST` → đọc + `holder()` → chỉ reclaim khi `state==='dead'` (PID+startTime proof, không phải TTL).
- **R8 (M15a)**: kiểm tra kỹ, xác nhận ĐÃ ĐƯỢC LÀM ĐÚNG từ trước (không phải phase này) — write persist `paneId`/`agentSession`/`resourceIncarnation` (giờ qua `patchCommandRecord` sau R5) nằm ngay sau `startAgent()` và TRƯỚC `deliverBrief()`, đúng thứ tự literal "trước khi deliver prompt", cho cả path confined lẫn unconfined (block dùng chung, ngoài `isConfined`). Không cần sửa code, chỉ note lại.
- **R9 (L2)**: doc-only — thêm deprecation comment ở CẢ HAI điểm tạo `dispatch.claim`/`retry-N.claim` (session-engine.mjs) giải thích redundancy với admission-time check mới của R6 (mạnh hơn: cross-process, không chỉ same-process), giữ 1 release chưa xoá. Thêm CHANGELOG `### Fixed` (tóm tắt toàn bộ single-live-worker fix) + `### Deprecated` (2 file claim).

**Gap đã biết, ghi lại thay vì giấu**: (1) phase doc đề xuất test mới `test/runner/dispatch-resume-live-worker.test.mjs` — CHƯA viết; thay vào đó dựa vào coverage rất rộng của test suite CÓ SẴN (assignment-dispatch/herdr-spawn-adapter/herdr-spawn-assignment-dispatch/herdr-reconciliation/cli-spawn-reconciliation/run-lock*/dispatch-reconciliation*/dispatch-recovery — tổng ~400 test, đã chạy lại toàn bộ và xanh) để xác nhận không regression, nhưng KHÔNG có test mới trực tiếp chứng minh "resume với supervisor/worker pid sống → throw run-in-flight" theo đúng tên gap #1 phase muốn đóng. (2) R6's CLI flag `--force-new-attempt` chưa wire argv, xem chi tiết ở trên. (3) Gộp vocabulary `RECONCILE_STATUS` (Steps mục 3) không cần làm — điều tra trực tiếp cho thấy `waiting` đã nhất quán giữa `reconcileCliSpawnRun`/`reconcileHerdrSpawnRun` (2 hàm THẬT trong assignment-runner.mjs/herdr-round.mjs) từ trước; vocab lệch chỉ tồn tại ở một hàm `reconcileCliSpawnRun` KHÁC, cấp thấp hơn trong cli-spawn-supervisor.mjs, không phải hàm R1/R2 thao tác.

## Requirements

- R1 (C1a) Resume: sau `rec = await reconcileFn(runDir)`, `waiting|held|observed|refused` hoặc `parked` với reason ≠ `command-missing` → throw `RunnerConfigError{code:'run-in-flight'|'run-unreconciled', phase:'post-admission'}`; chỉ `command-missing` được launch.
- R2 (C1b) `reconcileHerdrSpawnRun` trả `runResult` khi `settled` (normalize từ receipt/outbox), hoặc `{settled:false, observed:true}` tường minh; `runHerdrRound:1044-1051` không trả raw reconcile object làm adapter result.
- R3 (C1c) `dispatch recover --action resume-driver`: refuse khi Run thuộc session (`findCoordinationSessionOwningAssignment`), `resume` yêu cầu `liveness.fresh === false`, xoá claim qua `planClearAssignmentClaim/applyClearAssignmentClaim` (một bộ proof).
- R4 (H2) session-engine unlink `dispatch.claim`/`retry-N.claim` chỉ khi `err.phase !== 'post-admission'`.
- R5 (H13) herdr-round ghi `controller/commands/<id>.json` qua `commitCommandOutcome` có control token.
- R6 (M1) Unfenced admission: refuse khi current generation chưa có `result.json` và holder alive/unknown (dùng identity Phase 02); flag `--force-new-attempt` cho operator.
- R7 (M9a) `reconcile apply` lock reclaim dead holder bằng `startTime(pid)` (đã có trong module).
- R8 (M15a) herdr unconfined: persist `paneId` trước khi deliver prompt.
- R9 (L2) Sau R6, session claim file trở thành redundant → giữ một release, ghi deprecation.

## Files

- `src/runner/dispatch/assignment-runner.mjs:1840-1876` (R1), `:955-1023` (R6), `:3079-3377` (reconcile vocab)
- `src/runner/dispatch/herdr-round.mjs:2036-2040, 2120-2125, 2170-2176, 1044-1051` (R2); `:1325-1330, 1503-1515, 1571-1580, 1695-1712, 2113-2118` (R5); `:1497-1515` (R8)
- `src/verbs/dispatch/recover.mjs:205-233`, `src/runner/dispatch/recovery-planner.mjs:91-140, 286-299` (R3)
- `src/runner/coordination/session-engine.mjs:385-397, 436-446, 4810-4825` (R4)
- `src/runner/dispatch/reconciliation-planner.mjs:537-541` (R7)
- Tests mới: `test/runner/dispatch-resume-live-worker.test.mjs` (cli-spawn + herdr: resume với supervisor/worker pid sống → 0 spawn, 0 `controller/commands` thứ hai); `test/verbs/dispatch-recovery.test.mjs` (refuse session-owned, refuse fresh heartbeat); `test/runner/coordination-session-engine.test.mjs` (post-admission error giữ claim); `dispatch-reconciliation-concurrency.test.mjs` (dead-holder reclaim)

## Steps

1. `impact` cho `executeAssignment`, `reconcileHerdrSpawnRun`, `reconcileCliSpawnRun`, `recoverApplyUseCase`, `createAndExecuteSessionTask`, `commitCommandOutcome`. Cảnh báo HIGH dự kiến — báo trước khi sửa.
2. Viết test R1 trước (fail đỏ) → R2 → R1 code → R4 → R3 → R5 → R6 → R7 → R8.
3. Gộp vocabulary reconcile: `waiting`(herdr) = `running`(cli) → một enum `RECONCILE_STATUS` dùng cả hai adapter (không đổi tên function, chỉ giá trị).
4. Docs: recovery-design §9 S1/S2, §10 F-b/F-f (giờ đúng cho `executeAssignment`), control-plane §Observation ("recover không relaunch; relaunch chỉ qua runner với proof").

## Validation

Test gaps #1, #3, #5, #10, #12 xanh; `test/runner/herdr-reconciliation.test.mjs`, `cli-spawn-reconciliation.test.mjs` không regress; `npm test` ngoài session.

## Risk / rollback

R1 có thể chặn resume hợp lệ nếu reconcile trả `parked` với reason lạ → liệt kê reason được phép launch tường minh (allow-list), log reason bị chặn. R6 đổi hành vi pinned `assignment-dispatch.test.mjs:3023` (next-available-attempt) — cần cập nhật test có chủ đích, ghi trong report phase. Rollback từng R.
