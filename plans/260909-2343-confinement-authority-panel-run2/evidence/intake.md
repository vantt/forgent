# Intake — Confinement Authority cho fgOS dispatch (RUN 2, independent)

## Verbatim request (person's own words)

> "Cho tôi một architecture panel để thiết kế Confinement Authority cho fgOS
> dispatch. Hãy xác định component boundary, component parent, canonical
> contracts, backend abstraction, default implementation đơn giản và lộ trình
> mở rộng. Ghi toàn bộ discussion và quyết định cuối vào một file."

Follow-up, same day, after being told an identical panel already ran at 21:07:

> "chạy lại di, sau đó so sanh sau"

## What this run is

A deliberately INDEPENDENT second run of the same case. The person's stated
purpose is a convergence comparison against the 21:07 session
(`aap-260909-fgos-confinement-authority`) AFTER this one lands — not a
resume of it, not a revision of it.

## Hard isolation rule for this session

No role in this session may read, be shown, or be told the content of the
21:07 report or its evidence dir. No prompt embeds its findings. The
comparison is the coordinator's job AFTER this session produces its own
independent answer. A role that is fed the prior answer cannot
independently converge on it — that would destroy the only thing this
run is for.

Prior-run paths that are OFF LIMITS to every dispatched prompt:
- `plans/reports/architecture-advisory-panel-260909-2107-confinement-authority-fgos-dispatch-report.md`
- `plans/260909-2107-confinement-authority-panel/`

## Six questions the person asked, verbatim scope

1. component boundary
2. component parent
3. canonical contracts
4. backend abstraction
5. default implementation (simple)
6. expansion roadmap

## Known context (coordinator, factual, not an answer)

Subject under design is this repository's own dispatch path
(`src/runner/dispatch.mjs` and `src/runner/dispatch/*`). PROJECT_ROOT for
dispatched advisors IS this repo.
