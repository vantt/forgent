# P3-08 & P3-09: Changed-lines coverage & State canary

Status: PROPOSED
Summary:
- P3-08: Changed-lines coverage dự kiến sẽ được thêm vào `compare` job bằng cách đọc `coverage/related` và `git diff` (warn-only, không block). Việc này còn nợ pipeline producer thật trên PR live.
- P3-09: State canary live PR deployment & AC 9 exit criteria (yêu cầu kiểm chứng trên ≥1 PR thật), cùng pipeline producer sinh `wallTimeRatio` trong `selector-plan.json` còn nợ runtime live nên giữ Status: PROPOSED.
- AC 6 đã giải quyết: Đã hoàn thiện logic thu thập evidence và tự động promote per-rule qua `scripts/test-select-promote.mjs` tuân thủ nghiêm ngặt contract §3.1, §3.4, §4.3 và §14 (tập đối chứng độc lập gồm full-shadow + fault set, baseline sạch theo phạm vi test case của rule `file ∈ ruleTests`, N>=3 mutant kill-rate 100% không tính các case inconclusive như timeout/invalid-syntax, K>=10 failure opportunities, 0 miss trên hash hiện tại, bắt buộc đo wall-time ratio, coverage-set là tập con của rule-set, kiểm tra hash mutant chặt chẽ, ghi nhận `quarantinedHash` vào manifest khi cách ly, và cơ chế phục hồi cách ly chỉ kích hoạt khi rule được sửa đổi và có reproduction mutant trong nightly ledger). Đã nghiệm thu qua `test/scripts/test-select-promote.test.mjs`.
- AC 7 đã giải quyết: Tích hợp cảnh báo C4 vào `fgos return` và PR compare job khi `item.verify` tương tác với test selector (dùng chung helper `checkVerifyForC4`, tuân thủ cảnh báo rủi ro C4, đảm bảo selector không thay thế full suite cho authoritative verification). Đã nghiệm thu qua `test/cli/fgos-return.test.mjs` và `test/scripts/test-select-compare.test.mjs`.
Concerns/Blockers: AC 3, 5, 6, 7 đã giải quyết dứt điểm logic và cover bằng test đầy đủ. Riêng P3-08 (changed-lines coverage), P3-09 (state canary exit criteria trên PR thật), và pipeline producer cho `wallTimeRatio`/`coverageSet` tách riêng và giữ PROPOSED đúng khuyến nghị nghiệm thu.
