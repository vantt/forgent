# P3-06 & P3-07: Nightly fault-injection & Coverage-map

Status: DONE
Summary: 
- Đã cài đặt khung chạy mutation testing (`test-select-mutate.mjs`), có hàm phân loại `classifyMutant` 6 nhãn theo chuẩn C2.
- AC 5 đã giải quyết: Đã bổ sung generator `generateMutantPayload`/`generateMutantPayloads` tự động tiêm metadata `ruleHash` (chuẩn hóa SHA-256 không phụ thuộc status hay thứ tự key) và `boundary` cho registry mutant tại `test/test-ownership-mutants.mjs`; loại bỏ các mutant thiếu boundary.
- Đã thêm workflow CI `selector-nightly.yml` tự động chạy hàng ngày.
- Đã cài đặt khung cho `test-select-coverage-map.mjs` (xuất JSON và Markdown).
Concerns/Blockers: AC 5 đã hoàn tất và nghiệm thu. Về promotion (AC 6): logic evidence và promotion per-rule đã hoàn tất và verify chặt chẽ qua unit test; tuy nhiên trên pipeline thật chưa chạy đầu-cuối do chưa có producer sinh `wallTimeRatio` vào `selector-plan.json` và `coverageSet` (kiểm tra `coverage-set ⊆ rule-set` tạm thời bỏ qua khi chưa có producer; mọi rule sẽ tạm dừng an toàn ở `wall-time-ratio-missing` cho đến khi pipeline live tích hợp). Nợ này được theo dõi cùng P3-08/P3-09. 
