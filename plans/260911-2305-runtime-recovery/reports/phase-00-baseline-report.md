# Phase 00 — Baseline Report

**Cell:** P00 (wave 0, read-only-baseline)
**Plan:** [../plan.md](../plan.md)
**Phase brief:** [../phase-00-freeze-existing-behavior.md](../phase-00-freeze-existing-behavior.md)
**Date:** 2026-09-12
**Mode:** observation only — no source changes in this cell (verified: `git status --short` shows no tracked-file diffs from this cell's work, only pre-existing untracked plan/design artifacts from the prior design session).

## Focused suites run (all green, no source touched)

| Suite | Tests | Pass | Fail | Skip |
|---|---|---|---|---|
| `test/runner/dispatch-liveness.test.mjs` | 20 | 20 | 0 | 0 |
| `test/runner/main-checkout-lock.test.mjs` + `lock.test.mjs` + `lock-wait.test.mjs` | 86 | 86 | 0 | 0 |
| `test/runner/coordination-replay.test.mjs` | 28 | 28 | 0 | 0 |
| `test/runner/recovery.test.mjs` (Work-lifecycle `resolveAction`/`resolveStaleDoing`) | 33 | 33 | 0 | 0 |
| `test/runner/herdr-agent.test.mjs` | 32 | 32 | 0 | 0 |
| `test/runner/herdr-spawn-adapter.test.mjs` (fake client) | 36 | 35 | 0 | 1 |
| `test/runner/coordination-recovery-and-quorum.test.mjs` | 31 | 31 | 0 | 0 |

`test/runner/dispatch-recovery.test.mjs` does not exist yet — it is a P03 testLease file to be created by that cell, not an existing baseline suite. No other testLease file named in `code-panel-cells.json` for P01/P02L/P02H/P05/P05S exists yet either (`run-lock.mjs`, `cli-spawn-supervisor.mjs`, `read-evaluators.mjs`, `recovery-planner.mjs` all absent) — confirmed by direct `ls`, recorded here so later cells don't waste a round rediscovering it.

## S0 scenario findings

| ID | Scenario | Frozen observation |
|---|---|---|
| S0-1 | `blocked`/`paused-limit` mapping | Corrected owner: `src/runner/dispatch/herdr-round.mjs:55-61` (`ERROR_CLASS_FOR_OUTCOME`) is the actual mapping, not `liveness.mjs`/`result-ladder.mjs`. The 20/20 `dispatch-liveness.test.mjs` pass is still the frozen baseline for the ladder itself (`evaluateLadder`), but no test currently pins the `blocked`/`paused-limit` → `worker-timeout` mapping specifically. Not touched this cell. |
| S0-2 | Herdr prompt timeout resend | Corrected gate: `herdr-round.mjs:527/548` — resend fires when there is no ack **file** yet (`ackSeen` is set only by `fs.existsSync(paths.ackPath)`, line 492) and `resends < maxResends` and the agent's polled state is a ready state (`isReadyState`, herdr-round.mjs:539); confirmed by `herdr-spawn-adapter.test.mjs`'s "a herdr that stops answering does not turn a live round into an idle timeout" (passes, 2.9s real wait). Dropping the earlier "pre-`working`" framing: a worker that goes `working` → `done` (a ready state) without ever producing an ack file IS re-briefed, because the gate keys on the ack file, not on ever having been `working`. Transport resend is not semantic duplicate delivery — preserved baseline, matches plan's non-goal. |
| S0-3 | `run.mjs` close-after-steps | **Not deferred** (correction to the earlier "out of lease" framing): `closeSessionByQuorum` (`src/runner/coordination/session-engine.mjs:3579-3678`) evaluates quorum and either closes or throws a `CoordinationError` naming the refusal reason (e.g. incomplete actors not covered by `partialPolicy`, below-minimum completions) — it does not silently no-op. **Caveat, verified this cell**: a direct search of `src/` found no file named `run.mjs` and no occurrence of the literal fields `closeRefusalReason` / `closeAttempted` anywhere in the codebase, so the specific claim that a `run.mjs:743-767` caller invokes `closeSessionByQuorum` unconditionally after every step loop (incl. after a fan-out-failure `break`) and captures the refusal into those two named fields could **not** be verified against current source — flagged for the Lead to confirm the citation or source location. Covered behaviorally by the coordination-recovery-and-quorum suite (31/31 green). |
| S0-4 | Lock reclaim, live vs dead PID | **Corrected — this was backwards.** `main-checkout-lock.mjs:276-292` freezes a **heartbeat-mitigated TTL takeover of live holders**, the opposite of a "no TTL-only takeover" guarantee: for a numeric-PID identity, `held = pidLive && withinTtl` — a live PID whose record has aged past `ttlMs` reads as **not held**, so a live holder can be taken over once its TTL lapses; for a string identity there is no PID to probe at all, so `held` is judged by TTL freshness alone (fail-closed to `AMBIGUOUS` only when `ttlMs` itself is missing). This is the **opposite** of traceability row X01 ("live holder past TTL held"). `main-checkout-lock.test.mjs` does prove same-identity self-recognition refresh and that `forceReclaimAmbiguousLock` never reclaims a genuinely live, well-formed record — but that is a narrower guarantee than X01 claims, not X01 itself. **P01 must NOT reuse `main-checkout-lock.mjs`'s held-ness rule for per-Run control** if X01's "live holder past TTL still held" property is actually required — it needs its own rule. |
| S0-5 | Result link after retry declaration | **Not deferred** (correction to the earlier "not independently fixtured" framing): `retrySessionTask` (`session-engine.mjs:4384`) and result-link fencing — `assertValidRunIdForAssignment` (`session-engine.mjs:239`) plus the `dangling-ref` throw right after it (`:244`) — already exist and are tested (`coordination-recovery-and-quorum.test.mjs:625`, "retrySessionTask dispatches a NEW Run for the SAME Assignment ... never deletes/rewrites the original evidence"). The only genuinely missing piece is the schema-2 **exact-retry declaration** — there is no current schema-2 exact-retry code path to freeze, so that narrower gap alone is recorded as open, owned by P01. |
| S0-6 | BL1 premature partial close | **Different track, same kernel mechanism**, tracked as **tsk-5qj**. BL1 (`session-engine.mjs`'s `partialPolicy`-declared auto-close-after-first-required-pass) is documented and reproduced in `docs/architect/agent-coordination/verification/architecture-advisory-panel/P02.1.md` (Table 1/Table 4). Corrected evidence sentence: `git log --all -S partialPolicy -- src/runner/coordination/session-engine.mjs` returns **two introduction commits** (`a9613222`, `35a266b7`), both predating the 2026-09-06 BL1 audit record — the earlier sentence wrongly implied this `git log` came back empty; the conclusion is unchanged, since neither commit is a post-audit fix: **BL1 remains open/unfixed** at the kernel level today. `coordination-recovery-and-quorum.test.mjs:463` ("closeSessionByQuorum closes to 'partial' ... under an explicit partialPolicy naming the missing actor") is the closest existing kernel fixture, though it exercises correct explicit-`partialPolicy` behavior rather than BL1's auto-close defect itself. **This runtime-recovery track is not exposed to it**: none of `fgos-plan-loop`'s own `open.json`/`fix-N.json`/`close.json` templates declare `partialPolicy` (confirmed by reading `.agents/skills/fgos-plan-loop/SKILL.md` section 1-4), and the architecture-advisory-panel's own `test/verbs/coordination-run-live-proof.test.mjs` proves a full produce→review→red-team→revise→recheck→close cycle works in ONE continuous session precisely when `partialPolicy` is omitted. The Lead accepts re-running the named suites above as sufficient S0 fixture evidence for every scenario in this table **except BL1**, which relies on the `:463` citation above instead. Recorded as a known, separately-owned kernel gap — out of this track's fix authority, tracked here only so no runtime-recovery cell rediscovers it as new. |
| S0-7 | Herdr create/bind/locator crash windows | **Corrected — the earlier framing overstated the gap.** `herdr-round.mjs:654` mints the agent name as `normalizeAgentName(\`fgos-${workId ?? 'run'}-${Date.now()...}\`)` — timestamp-derived. `herdr-round.mjs:261` sets `runId: round.agentName`, but that is only `createWorkerHome`'s temp-dir label, not "today's Run identity" for Herdr — dropping that framing. A durable `runId` (`run_<assignmentId>_<attempt>`, minted at `assignment-runner.mjs:852`) already exists in `run.json`, and locators (`agentName`/`paneId`/`agentSession` in `visibility.json`, resolvable via `findAgentBinding` in `visibility-session.mjs:156`) are already persisted before each step. The real gap is narrower than previously stated: there is no production caller of `findAgentBinding` today, and the Herdr agent name is not derived from the durable `runId` — so a crash between `agentStart` and any locator read still has no recovery path keyed off the durable identity, which is the orphan-risk window P02H must close. |

## Exit gate check (phase-00-freeze-existing-behavior.md)

- [x] Baseline fixtures stored (this report + table above; scenario-level JSON fixtures were not separately emitted as files since every scenario is already reproducible by re-running the named suite/line reference — re-running is the fixture, per the existing suites' own determinism).
- [x] Focused tests green — corrected totals: **265 pass / 1 skip / 266 total** across the 7 suites above (`recovery.test.mjs` is 33 tests, not 61 — the earlier count and the resulting 294/295 total were both wrong); the 1 skip in `herdr-spawn-adapter.test.mjs` is pre-existing, not caused by this cell.
- [x] No `blocked`/`paused-limit` remapping shipped.
- [x] No deterministic Herdr naming shipped (S0-7 only *observes* the current timestamp-derived name).
- [x] No terminal-transfer behavior changed — the required "replay post-terminal neutralization" check is `src/runner/coordination/replay.mjs:87-151` (post-terminal `operation-authorized`/aggregation/contribution events are neutralized into `ignored*` lists, never silently dropped), exercised by `coordination-driver-authorization.test.mjs` (67/67 green).
- [x] No lock reclaim policy changed.
- [x] `git diff --check` — no source diff from this cell (see git status note above).

## Known changes explicitly labeled (not shipped here, future slice)

- S0-1 mapping: **preserved baseline**, owned by `herdr-round.mjs`'s `ERROR_CLASS_FOR_OUTCOME` (corrected owner), no change planned this cell.
- S0-4 lock semantics: **preserved baseline for `main-checkout-lock.mjs` itself**, but P01's per-Run control must NOT copy this file's held-ness rule wholesale — it freezes a TTL-can-take-over-a-live-holder behavior, the opposite of X01's stated requirement, so P01 needs its own rule rather than reuse.
- S0-7 Herdr naming: **named future slice**, P02H — corrected scope: wiring the already-durable `runId` into the agent name and giving `findAgentBinding` a production caller, not inventing a durable identity from scratch.
- BL1 (tsk-5qj): **named future slice, but explicitly out of this track's cell scope** (owned by whichever track/backlog item fixes `session-engine.mjs`'s partial-close path; this track only needed to confirm it isn't triggered by its own templates).

## Reconciliation with phase-00-baseline-260911.md

`plans/260911-2305-runtime-recovery/reports/phase-00-baseline-260911.md` (dated 2026-09-11, a day earlier) exists in this same `reports/` directory and covers a different, narrower suite set (113 recovery/coordination/lock tests + 100 herdr/confinement/assignment tests, no `recovery.mjs`/`resolveAction` suite). This corrected report **supersedes** that file's S0-4-equivalent characterization ("Existing lock tests include live-PID TTL reclaim behavior; this remains frozen legacy behavior" — true as a description, but this report additionally makes explicit that it is the *opposite* of X01's stated requirement, which the 2026-09-11 file does not flag). This report **agrees with** that file's S0-7/S0-2 nuance: durable `runId` exists but is not yet threaded into Herdr launch identity, and terminal replay keeps neutralizing post-terminal records — both consistent with the corrected S0-7/exit-gate rows above.

## Unresolved questions

- The S0-3 citation naming a `run.mjs:743-767` caller and the fields `closeRefusalReason`/`closeAttempted: true` could not be matched against current source (`src/` grep for both literal field names returned no matches; no `run.mjs` file was found under the repo). The behavioral claim (close-after-steps is not deferred) is independently verified via `session-engine.mjs:3579-3678`; the specific file/field citation needs the Lead to confirm or correct.
