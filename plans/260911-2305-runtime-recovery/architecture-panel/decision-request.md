Author: coordinator (driver bookkeeping — no operation in the registered
graph carries this artifact at Phase 4; see the skill's own reconciliation
note). Written 2026-09-12, after Phase 1 (interpret-request +
investigate-context) settled.

## Before We Ask

Eight ambiguities were handed up by the lead advisor (interpretation.md
§Ambiguities). Each run through the three-test Scout Before Ask discipline:
scouted, user-exclusive, material now.

| # | Ambiguity | Scouted? | User-exclusive? | Material now (changes Phase 5 output)? | Disposition |
|---|---|---|---|---|---|
| A1 | Four vs five readiness labels | Yes — source prompt itself lists five | No — resolved from text | No, but corrected immediately regardless | **Resolved, not asked.** Five labels, exact strings. Operation-brief typo corrected. |
| A2 | S0-S8 (intake) vs S0-S5+P06+P08 (charter) phase scope | Yes — phase-designs/README.md's own table | No — resolvable from repo | Not for Phase 5 (shapers don't grade phases) | **Defaulted, not asked.** Canonical phase set = the 7 files that actually exist (S1-S4, P06, P08, plus S0/S5 as named in plan.md). S6-S8 named in intake but with no phase-design file: treated as NOT APPLICABLE / out of this package's scope, not silently invented. Carried forward to Phase 7 synthesis. |
| A3 | Threshold for "direct contradicting evidence" against a baseline | Yes — op_004 itself demonstrated the shape | No | Not for Phase 5 | **Defaulted, not asked.** Operating definition (already applied by the context investigator): a real file:line/behavior that a doc claim states differently, cited concretely. Carried forward as the standing bar for every later role. |
| A4 | "Alternatives" = architecture alternatives or scope/sequence slices | Yes — the prompt's own three named categories (baseline conservative / gateway-change / long-horizon) are architecture-shape categories, not scope slices | No — resolvable from the prompt's own text | **Yes — decides how wide Phase 5 goes** | **Resolved, not asked.** Shapers propose genuine, defensible architecture alternatives for the whole runtime-recovery approach, one per named category, each a candidate a person could actually choose. Not three orderings of the same design. |
| A5 | Is "zero decision request" expected or merely permitted | Yes — it's a hope stated once, not a requirement stated as a rule | No | No | **Defaulted, not asked.** Permitted, not mandatory. Report honestly whether achieved (this document is that report — zero decision requests sent for Phase 1-5). |
| A6 | P08 residual-risk acceptance bound | N/A — a Phase 7/8 concern | No | No | **Deferred**, not defaulted — named for the synthesizer to state explicitly when P08 is reached, not invented now. |
| A7 | Must every phase fit `fgos-code-panel` single-change granularity | Yes — plan.md's own "no parallel cells edit same file" / leased-file rules are real repo constraints already visible | No | Weakly — informs constraint-advocate's Phase 5/6 framing | **Defaulted, not asked.** Treat single-code-panel-cell granularity as a real constraint the constraint advocate should weigh, not a hard gate the shapers must satisfy exactly (that grading is Phase 6/7's job). |
| A8 | Are the prior three review-round reports (plans/reports/design-review-*.md) part of the shapers' own reading list, given the independence goal | Yes — design-panel-prompt.md's reading item 15 literally includes them for the SESSION overall | No — this is a sequencing decision, not a fact question | **Yes — directly decides shaper independence** | **Resolved, not asked; a coordinator sequencing decision.** The prior review reports stay in the session-level mandatory reading list (item 15) and were already read by this coordinator + the context investigator (who scoped its own reading to the 16 explicitly-cited items only, correctly NOT reading plans/reports/ per its own prompt). The three Phase 5 shapers are **deliberately not given** the prior review reports or design-audit-final.md/detailed-design-review.md — they get the same 16-item reading list the context investigator used, plus interpretation.md/scout-report.md, so their proposals are genuinely independent of the coordinator's own three prior rounds. The critic (Phase 6) and synthesizer MAY be given the prior reports later, once independent shaping already exists on the record, without contaminating the shaping step itself. |

## Questions

None. All eight ambiguities either resolved directly from repository text
or the prompt's own words, or deferred to the phase where they become
material with an explicit name rather than a silent default.

## What We Are Doing While We Wait

Not waiting — no question was sent. Proceeding directly to Phase 5
(shape-system-proposal, shape-alternative-proposal, shape-constraint-
proposal), three real dispatches under the `framing-shaping-open` window
(open from session start, no authorization needed).

## What We Are Not Asking

- Whether the 15 baseline decisions are correct — declared ratification,
  not reopened (interpretation.md §7).
- Whether "hoàn thiện thiết kế" means this panel may edit
  detailed-design.md/phase-designs/*.md directly — moot regardless: this
  skill has no write access to PROJECT_ROOT (Bounds #2); the deliverable
  is advisory, handed back to the person to apply.
