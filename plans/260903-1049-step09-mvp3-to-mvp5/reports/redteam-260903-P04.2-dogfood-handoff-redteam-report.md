# Red-Team report — P04.2 (MVP6+ dogfood handoff)

Independent adversarial pass against `docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md`
and `P04.2.md`, run in parallel with (not coordinated with) the Reviewer.
Full findings and evidence appended as `## Red-Team (Independent)` in
`docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.2.md`.

## Verdict: APPROVE WITH CONCERNS

**Findings: 1 HIGH, 1 MEDIUM, 0 LOW.**

## HIGH — Still-open limitations omits the crash-bricked-`coordinationId` window

The cell's own `current-cell.md` Bug Taxonomy names three examples of a
still-open limitation that must not be silently treated as solved: "no
driver handoff/multi-writer resume, the session-wide-caps self-heal gap,
**the crash-bricked-coordinationId window**." The handoff's "Still-open
limitations" section covers the first two but omits the third entirely
(`grep -ni "crash|bricked|SIGKILL|permanently unrunnable"` over the handoff
returns zero matches). The gap is real and current: `P00.1.md` Carried-
Forward Gap #15 (12/40 real SIGKILLs reproduced upstream) and `P04.1.md`'s
own Red-Team section independently reproduced the exact shape against the
NEW resume door — a crash between `mkdirSync` and `writeManifestRaw`
permanently bricks that `coordinationId` (fails closed with a generic
"already exists," no recovery). P04.1's own live proof used a fixed
`coordinationId`, exactly the vulnerable shape. A maintainer dogfooding
MVP6+ per this handoff's own resume recipe needs this named up front.
Smallest fix: one more bullet in "Still-open limitations," citing P00.1 Gap
#15 and P04.1's Red-Team section.

## MEDIUM — Plan-Level Acceptance table's "zero diff" claim contradicted by P01.1's own diff

`P04.2.md`'s whole-plan Plan-Level Acceptance re-check table claims "Zero
diff to `session-engine.mjs`/`store.mjs`/.../`group-cognition-framework.yaml`
confirmed... in P01.1..." — but `P01.1.md`'s own Commands section records a
real `store.mjs | 53 ++++++++-` diff (its own `assertDispositionRefOwnedBySession`
deliverable). The underlying compatibility verdict likely still holds (the
change is additive and Reviewer-confirmed backward-compatible in P01.1's own
Review section; `session-engine.mjs`/`replay.mjs`/`schema.mjs` genuinely have
zero diff throughout), so this is a citation-accuracy finding, not a
substantive regression. Smallest fix: reword the evidence cell to exclude
`store.mjs` from the blanket "zero diff" claim and name P01.1's own additive
change explicitly.

## Attacks that held (no finding)

- Overclaim hunt on §4/§5 (Expected artifacts, Resume command) — every claim
  traces to real P04.1 evidence, none stronger than proven.
- Authority-boundary hunt on §6 vs. plan.md Non-Negotiable Boundaries and
  coordination-foundation-baseline.md Accepted Invariants — no implicit or
  explicit expansion of coordination authority into source mutation.
- Command accuracy — live-ran `fgos coordination launch-master-loop --help`
  and cross-checked `src/cli/command-registry.mjs`: `--plan`, `--objective`,
  `--writer-id`, `--file` all genuinely exist, exactly as documented.
- Scope escape — `git status --porcelain -- src/ core/ test/` empty.

## Unresolved questions

None — both findings have a clear, small fix direction; no further
investigation needed before the Coordinator/Fixer round.
