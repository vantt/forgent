# Doer Report — P01.1 (Phase 01, MVP3 recheck lineage and driver disposition)

Role: Doer. Cell: P01.1. Branch: step-09-mvp3-to-mvp5.

## Outcome

R1-R8 fully dispositioned, one at a time, in
`docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P01.1.md`.
No `run-retried`/retry code touched. No Work/git/coding-domain path touched.
No accepted contract file edited.

### Accepted unchanged (cited to P03.1.md, no source touched)

R3, R5, R7, R2's artifact-revision-link half, R6's mechanism, and 3 of R8's
5 named negative-semantics cases (missing authorization, reused invocation
key — already covered at both engine and door level per P03.2's own
tabulation; terminal session — covered at engine level).

### Gap closed

- **R4 / R8's "disposition target must reference a real...target" bullet**:
  `recordDriverDisposition` (`store.mjs`) had zero check that `targetRef`/
  `evidenceRefs` resolve to a ref owned by this session — P00.1.md's
  Carried-Forward Gap #9. Added `assertDispositionRefOwnedBySession`
  (`src/runner/coordination/store.mjs:1012`), mirroring
  `session-engine.mjs`'s existing `assertRefsOwnedBySession` semantics
  exactly (reject a ref naming a different session or a real-but-foreign
  Assignment; leave an unresolvable-looking string alone — matching
  precedent, not a stronger guarantee). 4 new tests in
  `test/runner/coordination-recheck-disposition.test.mjs`, mutation-verified
  (reverting the check fails exactly those 3 refusal tests, nothing else).
- **R8 cases #1 (unknown target) and #2 (stale/nonexistent artifact ref)**:
  proven at the engine level already, but had no door-level (CLI/request)
  test — the phase's own Bug Taxonomy names exactly this trap. 2 new tests
  in `test/verbs/coordination-run-driver-steps.test.mjs`. No source change
  needed (the door has no error-swallowing around the engine calls); this
  closed a proof gap, not a behavior gap.

### Deferred (not fixed, with reasoning)

- **R2's prior-finding/verdict link** stays artifact-revision-scoped/
  best-effort. Already ruled on by P03.1's own Coordinator (MEDIUM-3) with
  the same evidence available today — not re-litigated. A hard predecessor
  edge would be a new contract-schema decision, out of this cell's
  authority.
- **R8 case #4 (terminal session), door-level proof only.** Structurally not
  constructible today: `run.mjs` opens a session once and closes it once,
  after the whole steps loop — no step type transitions to terminal
  mid-loop, and a second call naming the same `coordinationId` is refused
  before any step runs. Reaching this shape needs a resume door
  (`P00.1.md` Gap #17, tagged Phase 04/MVP5) — out of scope here. Engine
  level is fully proven and lock-held-atomic.

## Scope note for Review

Touched `test/verbs/coordination-run-driver-steps.test.mjs`, outside the
phase file's literal Files list (`test/runner/coordination*.test.mjs` only),
to close the two door-level R8 gaps. This mirrors where P03.2 itself put
every door-level test for this exact surface. Flagged explicitly in
P01.1.md §7 for Review to confirm.

## Test Results

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination*.test.mjs'
-> tests 314 / pass 314 / fail 0

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  'test/runner/flow-definition*.test.mjs' 'test/runner/coordination*.test.mjs' \
  'test/verbs/coordination*.test.mjs' 'test/cli/coordination*.test.mjs'
-> tests 431 / pass 431 / fail 0

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/**/*.test.mjs'
-> tests 5165 / pass 5158 / fail 1 / skipped 6
   The 1 failure is exactly the known-unrelated baseline:
   test/cli/fgos-intake-4.test.mjs:318. No other failure, no new failure.
   Baseline was 5159/5152/1; delta (+6 tests) is exactly this cell's new tests.
```

## Files Touched

- `src/runner/coordination/store.mjs` (new helper +
  `recordDriverDisposition` wiring)
- `test/runner/coordination-recheck-disposition.test.mjs` (+4 tests)
- `test/verbs/coordination-run-driver-steps.test.mjs` (+2 tests, +1 import)
- `docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P01.1.md`
  (new)

Status: DONE
Summary: All of R1-R8 dispositioned with citations; one genuine gap closed
(disposition ref-ownership check, source + tests) and two proof-only door-level
gaps closed (tests only); full suite green except the one pre-existing,
unrelated baseline failure.
