# Red-Team (Independent) Report — P03.1 Role Execution Policy Readiness

Cell: P03.1. Track: step-09-mvp3-to-mvp5. Base: 52a1db76.

## Verdict: APPROVE

0 HIGH, 0 MEDIUM, 3 LOW (documentation/completeness notes only, none blocking).

## Summary

Ran all 7 assigned attack vectors independently against the real diff (not
the description), read the actual dispatch/session-engine code paths, ran the
specified test command in the foreground (418/418 pass), and cross-checked
`git diff 52a1db76`/`git status --porcelain` for scope escape. Full findings
appended as a `## Red-Team (Independent)` section at the end of
`docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P03.1.md`.

Key results:

1. **Fail-closed test attacked** — held. Traced every dispatch entry point
   (`dispatchDeclaredOperation`, `dispatchPrimaryTask`, `retrySessionTask`,
   fan-out branch dispatch) and confirmed all four share the exact same
   `runExecutorAttempt` -> `executeAssignment` -> `resolveAssignmentDispatchPolicy`
   -> `resolvePolicyTierModel` chain, enforced structurally by a static
   single-call-site test in `coordination-declared-consult.test.mjs:725`. No
   alternate silent-downgrade path found. LOW note: the R8 test itself only
   directly exercises `dispatchDeclaredOperation`, relying on the shared
   architecture (not a second literal test) for the other paths.

2. **minTier escalation "real caller" claim attacked** — held, and actually
   *understated* by the Doer's own trace. `fgos coordination run --file
   <request.json>` (a live, shipped CLI command, `bin/fgos.mjs:3122-3123` ->
   `src/verbs/coordination/run.mjs`) already accepts a per-actor `tier`
   override in a hand-authored request file and threads it into
   `dispatchDeclaredOperation`'s `cliPolicy` — meaning Red-Team critical
   escalation is usable TODAY via a real CLI invocation, not just a
   theoretical JS API, with zero further code needed. LOW: the trace doesn't
   name this concrete live command, framing the mechanism as only reachable
   via `launch-master-loop.mjs` (Phase 04 territory).

3. **`capabilities[]` inertness re-attacked independently** — held. Full
   `src/` grep confirms `cohort-planner.mjs`'s `planCohort` is the only
   functional consumer; `workflow-adapter.mjs:97` is an unrelated
   Workflow-YAML projector. Fixture declares no `profile.topology`/
   `profile.cohort`/fan-out step, so `capabilities[]` is fully inert here.

4. **Tier monotonicity / over-constraint attacked** — held, by design.
   `minTier` is semantically a floor; permanent non-lowerability at operation
   scope is the PolicyPatch contract's intended safety property, not an
   accidental over-constraint.

5. **Real resolution proof re-verified** — held. Ran the exact test command
   in the foreground; read `dispatch-coordination-role-tiers.test.mjs` in
   full: real `openDeclaredProtocolSession`/`dispatchDeclaredOperation`, real
   shipped fixture via `loadCoordinationProtocol`, genuine child-process fake
   executor (never a JS-level stub over `executeAssignment`).

6. **Scope escape checked** — held. Diff limited to exactly the 3 named
   files; no `src/runner/dispatch/**`/session-engine internals touched; no
   literal provider/model string (verified against the repo's real committed
   `.fgos/config.json` `modelPolicies` table — every literal covered).

7. **R7 re-attacked** — held, with one pre-existing (not new) gap noted:
   no doctor check validates minTier-vs-configured-provider tier coverage,
   but this gap is orthogonal to and pre-dates this cell (applies to any
   `policy.minTier` use anywhere), resolves cleanly under this repo's real
   default config, and fails LOUD (not silently) when it would matter — so it
   doesn't violate R7's actual intent. LOW: worth a future backlog item.

## Commands run

```bash
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/dispatch*.test.mjs' 'test/runner/flow-definition*.test.mjs'
# 418 pass, 0 fail
git status --porcelain
git diff 52a1db76 --stat -- core/coordination-protocols/standalone-master-coordination-loop.yaml test/runner/flow-definition-standalone-master-coordination-loop.test.mjs test/runner/dispatch-coordination-role-tiers.test.mjs
```

## Files touched by this red-team pass

- `docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P03.1.md`
  (appended `## Red-Team (Independent)` section only)
- This report file (new)
