# Doer Report — P03.1 Role Execution Policy Readiness

Track: step-09-mvp3-to-mvp5. Cell: P03.1 (Phase 03, R1-R8). Branch:
step-09-mvp3-to-mvp5.

## Summary

Closed all of R1-R8. R1's audit found every one of
`standalone-master-coordination-loop`'s 6 operations collapses to the SAME
dispatch policy today (tier `standard`, one default executor/model, no
capability differentiation) — confirmed by tracing `dispatchDeclaredOperation`
through `resolveAssignmentDispatchPolicy` with the fixture's real (empty)
policy stack, and confirmed no live caller (`launch-master-loop.mjs`) passes
per-role policy either. Added `policy.minTier` per operation (Doer/Fixer:
`standard`, Reviewer/Red-Team/Recheck: `analytical`) — the only portable field
actually wired into this fixture's non-cohort dispatch resolution path
(`capabilities[]` is confirmed inert for it, per `flow-definition.md`'s own
documented semantics and `cohort-planner.mjs` being its only real consumer).
Red-Team's critical-escalation and Reviewer/Red-Team's read-only-executor
preference are both closed with real, already-existing, already-tested
mechanisms (assignment/cli-scope `PolicyPatch`, the already-registered
`executors.claude-reviewer` config entry) rather than new schema plumbing —
documented as concrete recommendations for Phase 04's live-proof driver, since
`launch-master-loop.mjs` (the current driver) doesn't yet expose per-round/
per-actor overrides and extending it is outside this cell's Files list.

No `P03.2` follow-up recommended — every requirement is closed outright or
closed with a proven, already-legal mechanism.

## Files Changed

- `core/coordination-protocols/standalone-master-coordination-loop.yaml` —
  added `policy.minTier` to all 6 operations, plus an explanatory comment on
  the `operations:` block recording why `capabilities[]`/`preferExecutor` are
  deliberately absent.
- `test/runner/flow-definition-standalone-master-coordination-loop.test.mjs` —
  added 2 tests: intended minTier-per-role mapping, and no literal
  provider/model/`preferExecutor` anywhere (raw-text + validated-IR checks).
- `test/runner/dispatch-coordination-role-tiers.test.mjs` (new) — 4 tests: a
  real dispatch (real fixture, real engine, real Node-subprocess fake
  executor) proves Doer/Fixer resolve `standard`, Reviewer/Red-Team resolve
  `analytical`, a caller-supplied `assignmentPolicy.minTier: 'critical'`
  escalates Red-Team for exactly one round, and a missing-critical-tier
  provider fails closed (`RunnerConfigError`, zero settled runs) instead of
  silently downgrading.
- `docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P03.1.md`
  (new) — full R1-R8 trace with code citations, Final Intended Mapping table,
  Proof Matrix, Gaps.

## Proof

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  'test/runner/dispatch*.test.mjs' 'test/runner/flow-definition*.test.mjs'
# 418 pass, 0 fail

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/**/*.test.mjs'
# 5191 total, 5184 pass, 1 fail (test/cli/fgos-intake-4.test.mjs:318,
# documented pre-existing baseline failure — unrelated to this cell), 6 skip
```

No regression beyond the documented baseline. `src/runner/coordination/
{session-engine,store,replay}.mjs` untouched (no defect found — the
"portable scope cannot pin an executor" behavior is by design). No
`Do Not Touch` file was edited. No Work/git/coding-domain path touched.

## Gaps (see P03.1.md's own Gaps section for full detail)

- R5's `preferExecutor` routing to `executors.claude-reviewer` and R4/R6's
  per-round critical-escalation judgment both need Phase 04's live-proof
  driver to pass `assignmentPolicy`/`actors[].executor` — a concrete,
  already-legal, already-tested mechanism, not yet exercised by
  `launch-master-loop.mjs`'s own request composer (out of this cell's Files
  list). The structural read-only guarantee (every role, every operation)
  already holds today regardless — proven live in the R8 tests.

Status: DONE
Summary: R1-R8 closed; fixture now declares intentional minTier per role, proven by 4 real-dispatch tests (incl. critical escalation + fail-closed); full suite green except the documented pre-existing baseline failure.
