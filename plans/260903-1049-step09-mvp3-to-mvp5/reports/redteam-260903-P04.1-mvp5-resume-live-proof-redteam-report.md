# Red-Team Report — P04.1 (MVP5 resume + live proof)

Independent adversarial pass on `step-09-mvp3-to-mvp5` cell P04.1 (base
`52a1db76`, scoped files: `src/verbs/coordination/run.mjs`,
`test/verbs/coordination-run-driver-steps.test.mjs`,
`test/verbs/coordination-run-live-proof.test.mjs`, one `CHANGELOG.md` line).
Full attack detail and verdict recorded in the `## Red-Team (Independent)`
section appended to
`docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.1.md`.

## Verdict: APPROVE WITH CONCERNS

**1 HIGH, 0 MEDIUM, 0 LOW.**

## Summary

- **HIGH-1 (new finding):** a resumed request naming a DIFFERENT `writerId`
  than the session's own driver can dispatch plain `operation`/`fan-out`
  steps into that session and consume the real driver's already-issued
  `driver-authorized` authorizations — reproduced live end to end (real CLI,
  fake executor, fresh workspace). `authorize`/`disposition` steps ARE
  correctly gated by `assertDriverIdentity` (also confirmed live); only
  ordinary dispatch is exposed. This exact request shape was categorically
  unreachable before this cell (any second call to an existing
  `coordinationId` always died at `openSession`'s guard before any dispatch
  ran) — resume is what makes it reachable, so this is scored as a genuine
  new bypass, not the pre-existing/accepted architectural gap the Doer's own
  Gaps section frames it as. Smallest fix: assert
  `request.writerId === manifest.provenanceRoot.writerId` at the resume
  boundary in `run.mjs`, surgical, no new engine plumbing.
- **Concurrency (Doer's own flagged gap): attempted and held.** Two real,
  independently spawned OS processes raced an identical Call-2 request
  against the same already-resumed `coordinationId`. Exactly one won and
  drove the session to `completed`; the other failed immediately with a
  specific, pre-existing, correctly-scoped guard
  (`createAndExecuteSessionTask`'s per-Assignment in-progress claim) — zero
  corruption, verified by direct on-disk inspection (6 Assignments, correct
  event counts, correct disposition order).
- **Broken/corrupted-session resume (Gap #15 shape + malformed JSON):
  attempted and held**, both fail closed with clear, distinct errors — one
  cosmetic note (a crash-artifact directory still reports "already exists",
  unchanged from pre-cell behavior, not a regression).
- **`aggregateBounds` inertness: attempted and held** — confirmed via the
  Doer's own new test asserting the original cap survives a wider resumed
  declaration.
- Test vacuity: not vacuous (specific counts/ids/regexes throughout). Scope
  escape: none (`git diff --stat` confirms exactly the four scoped files
  changed; zero diff on every Do-Not-Touch path).
- Full targeted suite: 364/364 pass, matching the Doer's own reported count.
