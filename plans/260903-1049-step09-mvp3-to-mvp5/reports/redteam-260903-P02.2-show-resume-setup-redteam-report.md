# Red-Team Report — P02.2 (MVP4 R5-R6, closes Phase 02)

Independent red-team pass over the Doer's `show`/`launch-master-loop`
extensions (R5) and R6 "nothing new" claim. Ran in parallel with a
separate Reviewer; no coordination between the two.

## Attacks run (all held — claim confirmed, no violation found)

1. `show` mutation invariant — `replay.mjs` confirmed pure-read (module
   doc: "no writes, ever"; no write primitive found by grep). `show.mjs`'s
   new code adds only `fs.existsSync` reads and a read-only
   `loadCoordinationProtocol` call.
2. Reimplemented ownership check (`isRefOwnedBySession` vs `store.mjs`'s
   `assertDispositionRefOwnedBySession`) — compared character-by-character;
   logically identical (same segment walk, same cross-session/asgn_
   checks, same inputs). No disagreement found.
3. Recheck-lineage overclaim — `targetArtifactRef` never referenced in
   `show.mjs` (`grep` confirms); rendered fields carry no lineage-joinable
   data; docs explicitly disclaim a hard pointer.
4. Post-terminal disposition marking — `show.mjs`'s local
   `TERMINAL_EVENT_TYPES` matches `replay.mjs`'s own private set and
   `store.mjs`'s `TERMINAL_EVENT_TYPE` map exactly (same 4 types). Live
   test confirms a hand-crafted post-terminal disposition is marked, not
   hidden.
5. `describeNextAction` — `closed: true` branch unit-tested directly,
   produces a sensible non-refusal message; no `--resume` or
   nonexistent-command wording anywhere in either code path or test.
6. R6 "nothing new" claim — grep across the full diff for
   `process.env`/`readFileSync`/`writeFileSync`/`existsSync`/etc. finds
   only the two `fs.existsSync` calls inside `isRefOwnedBySession`, both
   against already-established `.fgos` paths; `loadCoordinationProtocol`
   reuses existing discovery. No new config surface.
7. Scope escape — `git status --porcelain` matches the Doer's own §6 file
   list exactly, minus the two named out-of-scope component-boundary files
   (unrelated concurrent session, confirmed harmless) and
   Coordinator-owned `current-cell.md`/`index.md`. No engine-internal,
   dispatch, or Work/git diff.

Test run: `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test
'test/verbs/coordination*.test.mjs'` → 47 pass, 0 fail (foreground,
matches Doer's reported count).

## Verdict

**APPROVE.** Full attack write-up appended as `## Red-Team (Independent)`
in
`docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P02.2.md`.
