# Fixer report — P04.2 handoff gaps (3 accepted findings)

Cell: P04.2. Track: step-09-mvp3-to-mvp5. Docs-only fix pass.

## 1. HIGH (Red-Team Attack 2) — crash-bricked-coordinationId omission

**File:** `docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md`, "Still-open limitations" section.

**Before:** 4 bullets (driver handoff/multi-writer resume, corrupted-session coverage scope, concurrent-resume-same-writer scope, `aggregateBounds` field coverage, self-heal-branch gap — actually 5 listed).

**After:** Added a 6th bullet naming the crash-bricked-`coordinationId` window: `openSession` (`store.mjs`) creates the session directory before writing `session.json`; a crash in that window leaves a directory with no manifest, unrecoverable (fails closed via `EEXIST`/"already exists", no recovery path). Cites `P00.1.md` Carried-Forward Gap #15 (12/40 real SIGKILLs reproduced in the predecessor track) and `P04.1.md`'s own Red-Team "Broken/corrupted-session resume" section (independently reproduced the same shape against the new resume door). States plainly it is not fixed or owned by any cell in this plan — directly relevant because §5 of the same handoff recommends a fixed `coordinationId` for resume.

## 2. MEDIUM (Red-Team Attack 4) — false "zero diff" claim for `store.mjs` in P01.1

**File:** `docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.2.md`, "Plan-Level Acceptance" table, row "Existing Step 08 and MVP1/MVP2 behavior remains compatible".

**Before:** "Zero diff to `session-engine.mjs`/`store.mjs`/`replay.mjs`/`schema.mjs`/`group-cognition-framework.yaml` confirmed independently by Reviewer and/or Red-Team in P01.1, P02.1, P02.2, P03.1, and P04.1" — factually wrong: P01.1 added `assertDispositionRefOwnedBySession` to `store.mjs` (+53/-1, confirmed by reading `P01.1.md`'s own "## 6. Commands" `git diff --stat -- src test`).

**After:** Reworded to: zero diff to `session-engine.mjs`/`replay.mjs`/`schema.mjs`/`group-cognition-framework.yaml` across all cells; `store.mjs` carries P01.1's own additive `assertDispositionRefOwnedBySession` change (+53/-1, cited to `P01.1.md`), Reviewer-confirmed backward-compatible in that cell's own Review section; zero diff to `store.mjs` in every OTHER cell (P02.1, P02.2, P03.1, P04.1).

The underlying compatibility verdict (Step 08/MVP1/MVP2 remain compatible) still holds — the `store.mjs` change is additive, not a modification of existing dispatch/disposition semantics, and was independently Reviewer-confirmed in P01.1 as backward-compatible. Only the citation was corrected; the row's conclusion is preserved, matching the Red-Team's own recommended fix direction.

## 3. MEDIUM (Reviewer) — "nine" miscount

**File:** `docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.2.md`, sentence immediately below the Plan-Level Acceptance table.

Verified `plan.md:65-92`'s own Plan-Level Acceptance bullets directly: counted 10 (full test suite; MVP3 preserves RunResults; MVP3 disposition; MVP4 launcher; role execution policy; MVP5 live proof; MVP5 resume; MVP5 dogfood handoff; no Work/git mutation; Step 08/MVP1/MVP2 compatibility). The Doer's own table in `P04.2.md` already lists all 10 rows correctly — only the closing summary sentence undercounted.

**Before:** "All nine Plan-Level Acceptance bullets are satisfied, the last one by this cell's own deliverable."

**After:** "All ten Plan-Level Acceptance bullets are satisfied, the last one by this cell's own deliverable."

## Verification

```
git diff --check -- docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.2.md
# exit 0, no output
git status --porcelain -- src/ core/ test/
# empty — docs-only scope held
```

Status: DONE
Summary: Fixed all 3 accepted findings (1 HIGH, 2 MEDIUM), all docs-only edits to the handoff playbook and P04.2.md's self-check table/sentence. `src/`/`core/`/`test/` untouched; both sanity checks pass.
