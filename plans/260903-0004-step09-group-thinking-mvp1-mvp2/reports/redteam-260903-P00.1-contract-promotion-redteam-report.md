# Red-Team Report — P00.1 Contract Promotion

Cell: P00.1 (Phase 00, Step 09 group-thinking MVP1/MVP2 contract promotion)
Scope: adversarial attack on the WRITTEN CONTRACT TEXT (docs-only cell, no
source/runtime code exists yet). Full findings are recorded in the cell's
own trace file, not duplicated here.

Findings written to:
`docs/architect/agent-coordination/verification/step-09-group-thinking-mvp1-mvp2/P00.1.md`
(new `## Red-Team` section, replacing the "Pending" placeholder).

## Summary of findings

| Severity | Finding |
|---|---|
| HIGH | Recheck could silently collapse onto the original Assignment via the existing taskKey/idempotent-claim (`wx`) precedent this same contract cites as authority — nothing requires a recheck's `taskKey` to differ from the original's. |
| MEDIUM | `invocationKey` uniqueness scope (session-scoped vs. binding-scoped vs. global) is never stated in prose; storage layout implies session-scoped but the text doesn't say so. |
| MEDIUM | No atomicity/ordering guarantee for appending `operation-authorized` concurrently with a terminal-status transition — unlike the explicit atomic-ref rule given to `assignment-created` and the explicit restart-safety rule given to `aggregateBounds`. |
| MEDIUM | `activation.maxInvocations` has no stated resume/restart counting guarantee, unlike the parallel `aggregateBounds` guarantee ("recomputed fresh from on-disk state, never bypassable by restart"). |
| LOW | `grantedContextRefs` has no stated same-session scope constraint — no read-side counterpart to the contract's existing write-side foreign-evidence rejection (Phase 06 R6). |
| LOW (folded into angle-1 write-up) | `activation.mode` default-to-`required` is stated only for full omission of the `activation` block, not for the case where `activation` is present but `mode` itself is absent. |

Four attack angles produced nothing (documented in the cell file, not
repeated here): activation-omission-means-no-gate (foreclosed except the LOW
sub-case above), binding-vs-template scoping (already unambiguous),
cross-invariant interaction with `assignment-run-runresult.md` (no
contradiction), `group-cognition-framework.yaml` retroactive effect (zero
`activation:` keys in the fixture, confirmed by direct read — no effect),
and the `flow-definition.md` forward-reference residual (confirmed
cosmetic-only, agreeing with the Reviewer's own Re-Review note).

## Verification performed

- Read `current-cell.md`, all of `P00.1.md` (including Review, Coordinator
  Disposition, Re-Review After Fix Round 1, Fix Round 1).
- Read the full current text of `coordination-session.md` and
  `flow-definition.md`.
- Read `assignment-run-runresult.md` in full (read-only cross-check).
- Read `core/coordination-protocols/group-cognition-framework.yaml` in full
  (read-only isolation-fixture check) — confirmed zero `activation:` keys
  anywhere in the fixture.

No file was edited outside the cell's own `## Red-Team` section. No source,
test, or `core/` file touched. No protected file
(`group-cognition-framework.yaml`, `assignment-run-runresult.md`) modified.

## Overall verdict

DONE_WITH_CONCERNS — one HIGH finding should be fixed before Phases 01-03
implement against this text; the MEDIUM findings share a repeatable pattern
(missing durability/scope qualifiers that this cell's own sibling rules
already model) and are cheap, one-clause fixes.
