# Fixer report — P01.1 test-coverage fix (MEDIUM-1, LOW-1)

## Scope

Fixed exactly the 2 Red-Team findings the Coordinator routed to a Fixer round
in `docs/architect/agent-coordination/verification/step-09-group-thinking-mvp1-mvp2/P01.1.md`
("### Coordinator Disposition"): MEDIUM-1 (no test asserts `actor` field of
node-operation bindings) and LOW-1 (no test asserts `graph.entry`/node
count/node-id set). HIGH-1 (`cohort-planner.mjs` resolver landmine) was
explicitly dispositioned "document, don't fix in this cell" and was left
untouched.

## File changed

`test/runner/flow-definition-standalone-master-coordination-loop.test.mjs` —
only file edited. Both fixes added inside the existing test
`"...declares the required first-pass and declared revision/recheck
operations"` (lines ~50-83 after the edit):

1. Built a second map, `actorByOp` (`Map<operationId, actorId>`), from the
   same `node.operations` loop that already built `nodesByOp`, then asserted
   the expected actor per operation: `produce-candidate` -> `doer`,
   `review-candidate` -> `reviewer`, `red-team-candidate` -> `red-team`,
   `revise-candidate` -> `fixer`, `reviewer-recheck` -> `reviewer`,
   `red-team-recheck` -> `red-team`.
2. Added `assert.equal(def.spec.graph.entry, 'phase-produce')` and
   `assert.deepEqual(def.spec.graph.nodes.map(n => n.id).sort(), [...])`
   against the sorted expected node-id set
   `['phase-first-pass', 'phase-produce', 'phase-recheck', 'phase-revision']`.

No existing assertion was removed, weakened, or reordered — both additions
are purely additive to the same test body.

## Files NOT touched (per instructions)

- Fixture: `core/coordination-protocols/standalone-master-coordination-loop.yaml`
- `src/runner/definitions/schema.mjs`
- `src/runner/definitions/protocol-loader.mjs`
- `src/runner/coordination/cohort-planner.mjs` (HIGH-1 subject — explicitly
  out of scope, left alone entirely)
- `current-cell.md`, `index.md` (not edited by this Fixer round)
- The Review/Red-Team/Gaps/Coordinator-Disposition sections of `P01.1.md`
  (only a new `## Fix Round 1` section was appended at the end)

## Verification

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/flow-definition*.test.mjs'
...
ℹ tests 49
ℹ pass 49
ℹ fail 0
```

49/49 pass — same count as before the fix (both new assertions landed inside
the existing test, no new `test(...)` block was added).

```
$ git status --short test/runner/flow-definition-standalone-master-coordination-loop.test.mjs
?? test/runner/flow-definition-standalone-master-coordination-loop.test.mjs
```

The file was already untracked before this Fixer round (it was created
earlier in this cell), so it does not appear in `git diff --stat`. Confirmed
no other file under `test/runner/` changed by this round, and no changes made
to the fixture or any `src/` file (both were already true before this round
and remain true).

## Documentation

Appended a `## Fix Round 1` section to the end of
`docs/architect/agent-coordination/verification/step-09-group-thinking-mvp1-mvp2/P01.1.md`
with before/after code for both fixes and the verification output.

Status: DONE
Summary: Extended the single existing test with actor-binding and graph-shape assertions closing MEDIUM-1 and LOW-1; 49/49 tests pass; only the one test file was edited; HIGH-1 and all other files left untouched.
Concerns/Blockers: none
