# Fixer report — P02.1 review findings (MED-1, LOW-1, LOW-2)

Branch: `step-09-group-thinking-mvp1-mvp2` (nothing committed).
Scope: exactly the three findings the Coordinator Disposition accepted.
MED-2 not routed here and not touched.

## What changed

| Finding | File | Change |
|---|---|---|
| MED-1 | `test/runner/coordination-driver-authorization.test.mjs` | +2 fixture shapes/tests making `operationId` and `targetActorId` independently load-bearing in `resolveBindingAuthorization`'s match, plus a shared `writeDefinition()` helper |
| LOW-1 | `src/runner/coordination/replay.mjs` | one branch in the consumption loop: `duplicate-ref` throw when a second `assignment-created` claims an already-consumed `authorizationId` (+1 test) |
| LOW-2 | `src/runner/coordination/session-engine.mjs` | R4 gate's refusal now names post-terminal `ignoredAuthorizations` matching the same triple (+1 test) |

No other file modified. `resolveDeclaredOperationActor` untouched. No existing
assertion weakened or removed.

## MED-1 — evidence now supports two of the three triple components

Two fixtures, per the Reviewer's own smallest-fix direction:

- **one node, two driver-authorized operations, SAME actor** — `nodeId` and
  `targetActorId` identical, so only `operationId` separates the bindings.
- **one node, one operation template, TWO actors** (the
  `independent-research-fan-out-fan-in` cohort shape) — `nodeId` and
  `operationId` identical, so only `targetActorId` separates them.

Each asserts an authorization for one binding refuses the other (zero
`assignmentRefs`) and then that the authorized binding dispatches.

Re-ran the Reviewer's mutation sweep in an isolated copy of `src`/`test`/`core`
(deleted afterwards, never against the working files):

| Mutation of the match predicate | Before (16 tests) | After (20 tests) |
|---|---|---|
| drop `record.nodeId === nodeId` | 16/16 pass | 20/20 pass (unchanged) |
| drop `record.operationId === operationId` | 16/16 pass | **19/20, caught** |
| drop `record.targetActorId === targetActorId` | 16/16 pass | **19/20, caught** |
| restored | 16/16 | 20/20 |

`nodeId` deliberately stays non-load-bearing — MED-2 explains why, and it is
out of this round's scope. Recorded honestly in the P02.1.md Fix Round 1
section rather than claimed as closed.

## LOW-1

`duplicate-ref` category, matching this file's existing duplicate-ref
rejections. Message names the authorization and both claiming assignment ids.
Confirmed discriminating: deleting the branch in the isolated copy → 19/20.

## LOW-2

Additive only. Category stays `validation`; the clause is appended AFTER
`refusing to materialize an Assignment`, so the `/no unconsumed
"operation-authorized"/` substring five existing tests match is intact. Grepped
`test`/`src`/`docs`/`core` for both message fragments first — no existing test
needed updating.

## Verification

- `coordination-driver-authorization.test.mjs`: 20/20 (was 16/16).
- Focused glob `flow-definition*` + `coordination*`: **320/320, 0 fail**
  (was 316/316; the +4 are the only delta).
- Full suite `test/**/*.test.mjs`, twice:
  - run 1: 5086 tests / 5072 pass / 9 fail / 5 skipped
  - run 2: 5086 tests / **5074 pass / 7 fail** / 5 skipped

**No NEW failure vs the index.md baseline of 8.** Run 2's seven are baseline
#1-#7 exactly by name; baseline #8 (`herdr-spawn adapter (LIVE)`) passed that
run — a strict subset of the baseline, matching the Reviewer's own run-2
observation.

Run 1's two extras were live-subprocess timing flakes in files this cell does
not touch and that import nothing from `src/runner/coordination/**`:
`dispatch.test.mjs` → "cliSpawnAdapter kills the whole process GROUP on
timeout" (already recorded by the Doer as run-1's extra) and
`herdr-spawn-adapter.test.mjs` → "interactiveMode ignores premature idle signal
until working is observed at least once" (same live-executor family as baseline
#8). Each re-run in isolation → 1/1 pass; both cleared in run 2.

## Diff scope

`git status --porcelain` after the round shows this Fixer touched only:

- `src/runner/coordination/replay.mjs` (modified, pre-existing Doer change +6 lines)
- `src/runner/coordination/session-engine.mjs` (modified, pre-existing Doer change +12 lines)
- `test/runner/coordination-driver-authorization.test.mjs` (untracked, extended)
- `docs/.../step-09-group-thinking-mvp1-mvp2/P02.1.md` (Fix Round 1 section appended at the end)

`current-cell.md` and `index.md` carry only their pre-existing (non-Fixer)
modifications. Nothing committed.
