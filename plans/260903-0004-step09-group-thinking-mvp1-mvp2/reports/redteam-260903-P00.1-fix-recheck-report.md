# Red-Team Recheck Report — P00.1 Fix Round 2

Cell: P00.1 (Phase 00, Step 09 group-thinking MVP1/MVP2 contract promotion)
Scope: independently re-verify the 6 fixes from Fix Round 2 (1 HIGH + 3
MEDIUM + 2 LOW, all accepted) actually close the exploit scenarios this
Red-Team originally described — not just that text was added.

Findings written to:
`docs/architect/agent-coordination/verification/step-09-group-thinking-mvp1-mvp2/P00.1.md`
(new `### Re-Review After Fix Round 2 (Red-Team recheck)` subsection,
appended to the end of the `## Red-Team` section).

## Per-finding verdict

| # | Finding | Verdict |
|---|---|---|
| 1 | HIGH — recheck/taskKey collision | CONFIRMED-RESOLVED — new clause uses "MUST incorporate" (hard requirement, not a recommendation) and names the exact forbidden derivation (`nodeId`+`operationId`+`actorId` alone) verbatim. |
| 2 | MEDIUM — `invocationKey` scope | CONFIRMED-RESOLVED — now explicitly session-scoped ("checked against that session's own `events.jsonl`"), closing the per-binding reading. |
| 3 | MEDIUM — terminal-race atomicity | CONFIRMED-RESOLVED — new Recovery Rule point 5 requires the same atomic/serialized write path as `assignment-created`, AND a replay-side fail-closed rule for any `operation-authorized` after a terminal event, closing both the write-time and retroactive-replay halves of the race. |
| 4 | MEDIUM — `maxInvocations` resume counting | CONFIRMED-RESOLVED — new sentence requires the count to be "counted fresh from the on-disk `operation-authorized` events... never from in-memory state," mirroring `aggregateBounds`'s guarantee. |
| 5 | LOW — `grantedContextRefs` cross-session scope | CONFIRMED-RESOLVED — option (b) chosen: every entry "must resolve to an artifact/ref owned by this same `coordinationId`"; cross-session refs are rejected. |
| 6 | LOW — `mode` default omission | CONFIRMED-RESOLVED — now explicitly covers both full omission of `activation` and `activation` present without `mode`, with a concrete example. |

All 6 fixes use hard requirement language (MUST / must resolve to / counted
fresh from... never from in-memory state) and name the specific exploit
scenario they foreclose, rather than a soft recommendation that would leave
wiggle room.

## Verification performed

- Independently re-read the full current text of `coordination-session.md`
  and `flow-definition.md` (not the Fixer's own before/after quotes).
- Re-attempted the exact exploit scenario described in each of the 6
  original findings against the new text.
- Re-ran `git diff --check` on both contract files — clean, exit 0.
- Re-ran `git diff --stat` on both protected files
  (`core/coordination-protocols/group-cognition-framework.yaml`,
  `assignment-run-runresult.md`) — empty, zero diff on both.
- Re-ran `git status --porcelain` — only the 3 intended files touched
  (`coordination-session.md`, `flow-definition.md`,
  `coordination-foundation-baseline.md`, the last predating this fix round
  from Fix Round 1). No source/test/`core/` file touched.

## Overall verdict

DONE — all 6 findings confirmed resolved, no new ambiguity introduced by
the fixes, no protected file touched, diff hygiene clean. This cell is safe
to close from a Red-Team perspective; no further adversarial round is
needed.
