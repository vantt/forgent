{
  "kind": "declared-protocol",
  "objective": "Continue aap-260909-fgos-confinement-authority: Phase 8 (explain the recommendation for ownership, preserving the panel's own live disagreement).",
  "writerId": "coordinator-driver",
  "coordinationId": "aap-260909-fgos-confinement-authority",
  "protocolRef": { "id": "core.coordination-protocol.architecture-advisory-panel-v1" },
  "actors": [
    { "id": "lead-advisor-actor", "executor": "claude-bwrap", "tier": "critical" }
  ],
  "steps": [
    {
      "type": "authorize",
      "as": "authExplain",
      "operationId": "explain-recommendation",
      "targetActorId": "lead-advisor-actor",
      "authorizationId": "auth_explain_v1",
      "invocationKey": "ik_explain_v1",
      "reason": "Phase 7 synthesis and red-team are both in -- write the document the person actually reads.",
      "grantedContextRefs": ["asgn_coordinator_driver_op_052", "asgn_coordinator_driver_op_054"]
    },
    {
      "type": "operation",
      "as": "explain",
      "operationId": "explain-recommendation",
      "targetActorId": "lead-advisor-actor",
      "contextRefs": ["asgn_coordinator_driver_op_052", "asgn_coordinator_driver_op_054"],
      "objective": "Write the one document meant for the person, about a Confinement Authority component for fgOS dispatch at /home/vantt/projects/forgentX -- consequence first, architecture second, defensible to colleagues who will live in the resulting codebase, not a summary of internal panel process. THE REAL, FINAL SYNTHESIS (op_052, read it directly at /home/vantt/projects/forgentX/.fgos/assignments/asgn_coordinator_driver_op_052/runs/01/agent-report.md for full text -- this objective only summarizes it): the panel's own three proposals (A: runtime interception with a new confinementProfile field; B: compile-time synthesis, fatally falsified because it can't know the per-item worktree path that only exists at dispatch time; C: a runtime value object deriving posture from capability+cwd) were ALL written greenfield, but the synthesizer's decisive correction is that fgOS ALREADY HAS half of a Confinement Authority: `executor.confinement` is a real, validated config field (/home/vantt/projects/forgentX/src/runner/dispatch/config.mjs:696-775, closed vocabulary privateHome/isolatedSession/ownWorktree, unknown keys refused, a real C5 invariant already enforced for permissionMode:'bypass'), and `resolveExecutorCommand` (transport.mjs) already resolves and returns this `confinement` object -- but only `herdr-round.mjs`'s adapter consumes it; the `cli-spawn` adapter that claude-bwrap/agy-bwrap/codex-readonly actually use ignores it completely, and none of those three executors declares a `confinement` object at all today. FINAL RECOMMENDATION (labelled 'A′' by the synthesizer): do not add a new sibling field (rejecting proposal A's own `confinementProfile` design) -- instead extend the EXISTING `executor.confinement` object with a new `filesystem` sub-object (`{backend: 'bwrap-ro'|'provider-native', writable: [...]}`), validated inside the SAME existing validator (config.mjs's `validateExecutionProfileShape`), rendered by a new pure function in a new file `src/runner/dispatch/confinement.mjs`, called from `resolveExecutorCommand` in transport.mjs right after the executor resolves -- zero caller changes anywhere, because that function already returns a `confinement` key today. Two backends ship now: `bwrap-ro` (emits today's exact bwrap prologue, verbatim) and `provider-native` (no-op passthrough for codex-readonly). A found-live asymmetry to name plainly: `provider-native` (codex's `-s read-only`) CANNOT express a writable exception the way `bwrap-ro` can -- and codex-readonly declares NO writable exception today, which is the same class of failure that made claude-bwrap's own writable-assignments-bind necessary in the first place (this session's own earlier investigate-context dispatch to codex-readonly reproduced exactly this: a real, live no-evidence failure from a fully-read-only mount). The backend contract must REFUSE at load time a `writable[]` a backend can't honor, never silently drop it. FIRST IMPLEMENTATION STEP (concrete, do this before any refactor): a golden/snapshot test in test/runner/dispatch.test.mjs asserting resolveExecutorCommand's argv for claude-bwrap/agy-bwrap/codex-readonly is byte-identical before and after the change -- the synthesizer wrote out the exact expected argv arrays already; use those verbatim. RESIDUAL-GAP MITIGATION: the earlier idea (refuse to load a 'bwrap-family' executor lacking a profile) was rejected as a heuristic (sniffs `command==='bwrap'`, which mis-detects codex-readonly, which IS confinement-family but whose command is 'codex') -- the synthesizer's better version: let the CAPABILITY declare `requiresConfinement: true`, checked at the existing prefer-validation cross-check in config.mjs (~line 1045-1053) against whether the preferred executor's `confinement.filesystem` backend exists -- load-time, loud, no heuristic. LIVE, UNRESOLVED DISAGREEMENT -- state this plainly, do not smooth it: the architecture-critic attacked Proposal C's folding of herdr-round.mjs's three session-hygiene booleans into one profile object as an abstraction leak (the cli-spawn adapter ignores those fields silently) -- correct on its face, transport.mjs:295 confirmed. But the synthesizer split this finding rather than accepting it whole: C was right that ONE profile-shaped object is the correct DATA SHAPE (fgOS already has exactly this in `executor.confinement`), while A was right that the CONSUMER boundary must stay separate (a pure argv-renderer must never touch herdr-round.mjs's session/env mutation). The critique was read by the constraint-ranking as vindicating both of A's positions; the synthesizer states it only vindicates one. Tell the person this nuance is real and worth understanding, not just 'the critique landed.' RED-TEAM (op_054): independently reviewed the panel's own conduct (not the architecture) and returned APPROVE, finding the critique of Proposal A was a genuinely argued lifecycle-timing distinction rather than a convenient dismissal, the constraint-advocate's self-downgrade of its own Proposal C was genuine self-correction, and Proposal B was a real, seriously-argued candidate that was fatally falsified on the merits, not a strawman. REVERSIBILITY, state as three tiers, explicitly: GREEN (reversible, do now) = the filesystem sub-object + two backends + golden tests + the capability-side requiresConfinement load gate, all additive, byte-identical argv, zero effect on non-adopting executors. AMBER (reversible with care, do next) = dedup the two copy-pasted bwrap prologues in .fgos/config.json and replace the hardcoded absolute path `/home/vantt/projects/forgentX/.fgos/assignments` with a `{repoRoot}`-relative token -- name this plainly as a real portability defect (these three executors cannot run as configured on any checkout other than this exact machine, which directly undercuts fgOS's own stated mission of serving OTHER projects, not just itself). RED (irreversible, a USER DECISION, never bundled into this work) = renaming or retiring the executor identities `claude-bwrap`/`agy-bwrap`/`codex-readonly` -- verified load-bearing in three live capability bindings (`capabilities.advise`, `capabilities['code:review']`, `capabilities['code:debug']`) and in the tsk-1o4 live-falsification-evidence trail cited by id inside those very config descriptions; renaming would break that audit trail's own anchor. Say explicitly: this recommendation (A′) never needs to touch the red tier at all -- that is a real point in its favor, not incidental. WHAT STAYS THE PERSON'S OWN CALL, name explicitly, not as a closing disclaimer: (a) whether to fold the amber-tier path fix into the same delivery or file it separately; (b) whether `codex-readonly`'s missing writable-exception is worth its own follow-up work item now or later (a real, live-reproducible gap, but a different concern from the architecture decision itself); (c) the backend-abstraction shape at N=2 (a registry-of-functions vs. a plain switch) is a genuine tie in the evidence -- name the one observable fact that would break the tie (a third backend ever needing its own validation rules) and say the person need not decide this now. Use absolute paths for anything you verify yourself. Also write a short human-readable agent-report.md alongside your structured result. Write your structured result as JSON with exactly this shape: {\"status\": one of \"done\"|\"blocked\"|\"failed\"|\"no-evidence\" (lowercase, no other values), \"summary\": a non-empty string}. You may add any other fields you find useful alongside these two required ones.",
      "expectedOutputs": ["agent-result.json (status, summary)", "agent-report.md (human-readable)"]
    }
  ]
}
