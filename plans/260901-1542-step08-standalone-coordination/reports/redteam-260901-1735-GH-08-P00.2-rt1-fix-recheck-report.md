# Red-Team re-check — P00.2 RT1 fix

Cell: P00.2 (Truthful provider/tier/executor resolution, Phase 00 R5-R8)
Track: step-08-standalone-coordination
Branch: step-08-standalone-coordination
Scope: narrow re-verification of the RT1 (HIGH) fix only — not a full re-run
of the original Red-Team attack list.

## Verdict: FIX CONFIRMED

## What was checked

1. **`hasGovernanceOptions` hand-trace** (`assignment-policy.mjs:167-174`) —
   correctly `true` when either `disallowedProviders` or
   `disallowedExecutors` is a non-empty array; combined with
   `!hasExecutorRegistry` it throws a named `RunnerConfigError` before any
   provider resolution happens.
2. **Live re-run of RT1's exact reproduction** — reconstructed the identical
   `runnerConfig`/`cliOverride`/`options` shape from the Red-Team finding via
   a throwaway script calling `resolveAssignmentDispatchPolicy` directly.
   Confirmed it now throws instead of silently resolving with a false
   `providerModel: 'glm-cli'`.
3. **No-regression check** — same registry-absent config, no governance
   options: resolves successfully (`providerModel: 'glm-cli'`), both live
   and via the pre-existing test at `assignment-policy.test.mjs:466-484`.
4. **Boundary check** — `disallowedProviders: []` (empty array) does not
   trigger the throw, live and by code (`.length > 0` guard).
5. **Ordering check** — `primaryExecutor` is defined at lines 127-131,
   before the new throw (170-174) and before the `isImplicitDefaultExecutor`
   branch (200-204). The throw fires strictly before that branch could ever
   run for the governance-options-present case — no gap recombination.
6. **New-test quality** — both new RT1 tests
   (`assignment-policy.test.mjs:423-464`) assert on the actual thrown error
   message, not just "throws". The updated pre-existing "governance gate
   rejects disallowed egress/provider" test now supplies a real registry and
   still asserts the original `disallowed egress` rejection message — still
   testing real governance rejection.
7. **Scope** — `git status --porcelain -- src test domains` shows exactly
   the 5 expected files (`assignment-policy.mjs`, `config.mjs`,
   `resolve.mjs`, `assignment-policy.test.mjs`, `dispatch.test.mjs`);
   `config.mjs`/`resolve.mjs` are unchanged holdovers from the earlier
   F1-F4 pass, not touched by this RT1 round.
8. **Commands** — focused test command: 420/420 green, reproduced
   independently. Smoke: `{"mechanism":"in-process","configured":false}`.
9. **Adjacent sweep** — no similar "no registry to verify against" gap found
   elsewhere in the file for the governance-options-present case; the new
   throw's placement makes the F1 exemption branch unreachable whenever
   governance needs a registry.

## Evidence

Live reproduction script output:

```
PASS: threw as expected -> RunnerConfigError : governance requires "disallowedProviders"/"disallowedExecutors" but runnerConfig.executors is absent -- provider family cannot be verified for executor "glm-cli"
PASS: empty array does not trigger throw. providerModel = glm-cli
PASS: no governance options resolves fine. providerModel = glm-cli model = glm-4.6-literal
```

Test run:

```
ℹ tests 420
ℹ pass 420
ℹ fail 0
```

Smoke:

```
{"mechanism":"in-process","configured":false}
```

## Verification note appended

A "### Red-Team re-check (RT1 fix)" subsection with `Verdict: FIX CONFIRMED`
was appended to
`docs/architect/agent-coordination/verification/step-08-standalone-coordination/P00.2.md`
under the existing "## Fix Evidence" section, after the RT1 entry. No other
section of the file was touched.

Status: DONE
Summary: RT1's fix (`hasGovernanceOptions` fail-closed check) verified correct, complete, and non-regressive by hand-trace, live reproduction, and full focused test/smoke run (420/420 green).
Concerns/Blockers: none
