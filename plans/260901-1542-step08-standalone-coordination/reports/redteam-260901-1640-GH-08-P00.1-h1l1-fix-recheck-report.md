# Red-Team re-check: P00.1 H1/L1 fix (round 2)

Focused verification, not a full red-team pass. Scope: confirm the Fixer's
second-round patch (H1 detect-and-report strike, L1 AdhocTask storage-line
removal) is correct, complete, and introduced no new contradiction.

## What was checked

Read the live current content (not Fix Evidence prose) of the four files
named in the Coordinator disposition, cross-checked against the two primary
sources:

1. `decisions/ADR-008-coordination-session-and-mission-deferral.md` —
   Decision 4 now states plainly "No migration reader, detector, reporter,
   compatibility writer, or stored-data migration is built for
   `.fgos/missions/`" with no exception carve-out. Rejected Alternatives
   entry correctly frames detect-and-report as considered-and-rejected,
   citing `plan.md:68-71` and the checkpoint.
2. `plans/260901-1542-step08-standalone-coordination/plan.md:68-71` and
   `proposals/step-08-standalone-coordination-protocols.md:1493-1499` — both
   read "no migration reader, detector, reporter, compatibility writer, or
   stored-data migration" / "do not add a legacy reader, detector, reporter,
   or stored-data migration path." ADR-008's corrected text matches both
   verbatim in substance.
3. `intent-preservation-ledger.md:238` — Decision Trace row updated to "no
   legacy reader, detector, reporter, or stored-data migration." No
   detect-and-report survivor.
4. `docs/specs/runner.md:1201-1203` — Vietnamese persistence paragraph
   updated to "không có reader/detector/reporter/migration tương thích
   ngược nào." Old "BÁO RÕ" carve-out text confirmed removed via grep.
5. `contracts/coordination-session.md` Storage Layout — `tasks/<task-id>.json`
   AdhocTask line genuinely removed (not commented/hidden); only
   `session.json`, `events.jsonl`, `synthesis.md` remain.
6. Scope: `git status --porcelain -- src test domains` empty. Full
   `git status --porcelain` shows only `AGENTS.md`/`CLAUDE.md`
   (auto-regenerated GitNexus block, pre-existing L2 no-action),
   `docs/architect/agent-coordination/**`, `docs/specs/{reading-map.md,
   runner.md}`, and `plans/260831-1637-step07-inline-assignment-mvp/plan.md`
   — matches the cell's declared footprint.
7. New-contradiction sweep: grepped ADR-009, ADR-010,
   `contracts/flow-definition.md` for detect-and-report/detector/reporter/
   AdhocTask/`tasks/<task-id>` — zero matches. Repo-wide grep for
   `detect-and-report` shows the only remaining canonical-doc hit is
   ADR-008's own Rejected Alternatives entry (correct, naming what was
   rejected); other hits are in stateful report files or an unrelated topic
   in `docs/history/events-jsonl-merge-abort-truncation-gap/RESEARCH.md`.
8. `git diff --check` clean; `git diff -- decisions/ADR-006-*.md
   decisions/ADR-007-*.md` still empty (unaffected by this pass).

## Verdict

FIX CONFIRMED — both H1 and L1 fixes are correct, complete, and introduced
no new contradiction. Full note appended to
`docs/architect/agent-coordination/verification/step-08-standalone-coordination/P00.1.md`
under "### Red-Team re-check (H1/L1 fix)".

Status: DONE
Summary: H1 (ADR-008 detect-and-report carve-out) and L1 (AdhocTask storage
line) are both fully fixed across all four propagated files, with no
remaining contradiction anywhere in the cell's scope; scope is clean.
Concerns/Blockers: none.
