# Red-Team Report — P00.2 Dispatch Policy Fixes

Track: step-08-standalone-coordination | Cell: P00.2 | Branch:
`step-08-standalone-coordination` | Base: `75505c75`

## Verdict: FINDINGS (1 HIGH)

## Summary

Ran the 9 prescribed attacks against `assignment-policy.mjs`/`resolve.mjs`/
`config.mjs` (full current bodies, not diffs) after the Doer → Reviewer (F1
HIGH/F2 HIGH/F3 LOW/F4 LOW) → Fixer pass. 8 of 9 attacks found nothing new —
the Fixer's F1/F2/F4 fixes are correct for the scenarios their own tests
cover, `deriveProviderFamily`'s extraction is genuinely byte-identical,
`config.mjs`/R8 is untouched, scope is clean, and the required focused
command (418/418) and `decide` smoke both reproduce green independently.

Attack 1 (governance bypass sweep) succeeded: **RT1 (HIGH)**.
`options.disallowedProviders`/`disallowedExecutors` — the exact mechanism
F2 added to make governance provider-family-truthful — silently reverts to
H2a's defect (`providerModel = raw executor id`, not the true family)
whenever `runnerConfig.executors` is absent. This is a fully schema-valid
config shape (`executors` is optional per `validateRunnerConfigShape`), not
a malformed edge case, and it is the SAME branch the Fixer's own test at
`assignment-policy.test.mjs:323-339` already exercises for the registration
throw — that test just never asserts `providerModel` or governance in that
state, so the gap was invisible to it.

Live-reproduced two ways (see P00.2.md's Red-Team section for the full
runnable snippets):
1. A `cliOverride.model` literal override combined with `preferExecutor:
   'glm-cli'` and no `executors` registry: `disallowedProviders: ['z-ai']`
   (the operator's honest, family-named block) does not throw;
   `effectivePolicy.providerModel` comes back `'glm-cli'` (wrong — real
   family is `'z-ai'`), and `provenance.provider.value` is wrong too.
2. The same outcome via the ordinary tier-resolution path (no literal model
   override) whenever a project's `modelPolicies` happens to be keyed by
   raw executor id rather than true family.

Mitigating factor: no production call site in this repo sets
`disallowedProviders`/`disallowedExecutors` today (grep confirms zero
non-test, non-comment references), and this repo's own `.fgos/config.json`
does populate a full `executors` registry — so there is no live exposure
through this repo's actual dispatch path right now. The finding is about
the mechanism's trustworthiness for the config shape it is explicitly
supposed to cover (R6 states no carve-out conditioned on registry
presence), not an active incident in this repo today.

## Attacks attempted, no finding

- F1 fix correctness for both `isImplicitDefaultExecutor` sub-cases
  (unset `preferExecutor` vs. explicit `'claude'` while the global command
  differs) — hand-traced, matches stated intent.
- `deriveProviderFamily` extraction at `resolveExecutorConfig`'s call site
  — confirmed byte-identical via `git diff`.
- New Fixer tests (F1/F2/F4) — read in full, none tautological; each pins
  a real resolved value from real inputs.
- R8/`config.mjs` — `git diff` shows only the additive `supportsPolicyTier`
  function; everything else byte-for-byte untouched.
- Scope (`git status --porcelain -- src test domains`) — exactly the 5
  files current-cell.md names.
- Full baseline (`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test
  test/runner/assignment-policy.test.mjs test/runner/dispatch.test.mjs
  test/setup/registrations.test.mjs`) — reproduced 418/418 green, `decide`
  smoke reproduced `{"mechanism":"in-process","configured":false}`.
- Provenance-before-throw ordering — both governance checks throw before
  `governanceSource` is constructed, so no inconsistent recorded state is
  reachable; question is moot.

## Disposition written

Findings appended under `## Red-Team` in
`docs/architect/agent-coordination/verification/step-08-standalone-coordination/P00.2.md`,
verdict line included. No other section of that file, `current-cell.md`, or
`index.md` was touched.
