# Red-Team re-check: P00.3 round-2 fixes (HIGH #1, MEDIUM #2, tracking-entry fix)

Scope: verify the two code-level round-2 fixes to `findDuplicateOverrideFlag()`
in `src/runner/dispatch/cli.mjs`, and verify `index.md`'s P00.4 tracking entry
is genuine. Not a full re-attack of the original cell.

## What was verified

Read the current `findDuplicateOverrideFlag()` (cli.mjs L865-881) — matches
the Fix Evidence's quoted code exactly. Rather than trusting the prose,
reproduced the original attacks live via a standalone driver script
(`/tmp/claude-1000/.../scratchpad/redteam-recheck-probe.mjs`), each building
its own temp `.fgos/config.json` + assignment fixture and calling
`execFileSync` against the real `src/runner/dispatch.mjs execute` subcommand:

1. **HIGH #1 original attack** (`--executor named_executor --executor`,
   trailing bare, `--cwd` placed first so it lands last in `rest`): now
   exits 1 with `duplicate flag "--executor" -- pass it at most once before
   launch`. Fixed.
2. **MEDIUM #2 original attack** (`--tier --executor named_executor`): now
   exits 1 with `invalid override tier "--executor"` — no duplicate-flag
   message. `flagValue()` and `findDuplicateOverrideFlag()` agree that the
   literal string `--executor` is `--tier`'s dangling value, not a real
   `--executor` occurrence. Fixed.
3. **Adversarial sweep (new, not in original findings)**:
   `--executor --executor named_executor` — does the duplicate-checker's
   `i += 2` skip disagree with `flagValue()`'s `indexOf`? Result: both
   agree. `findDuplicateOverrideFlag()` counts exactly one `--executor`
   occurrence (second token consumed as the first's value); `flagValue()`
   independently resolves to the same first occurrence and the same second
   token as its value (`"--executor"` literal), which then correctly fails
   downstream as an unregistered executor id. No new inconsistency.
4. **Boundary — bare `--executor` alone, last token**: exits 0, no throw;
   `flagValue` returns `undefined`, `cliOverride.preferExecutor` is omitted,
   dispatch proceeds with the default executor. An empty `rest` array is
   unreachable in practice (execute always requires `--assignment`/
   `--contract` first) but by inspection the `while` loop with
   `rest.length === 0` trivially returns `null`.

Confirmed the 2 new regression tests exist in
`test/runner/assignment-dispatch.test.mjs` (line 523: trailing bare
duplicate; line 578: dangling-value-slot false-positive) — both real
`execFileSync` subprocess calls against a real temp config/assignment, not
tautological.

Confirmed `index.md`'s "Cell P00.4 (planned, not yet opened)" section
(L88-109) has genuine, specific content — scope (2 known call sites plus the
`transport.mjs:163` third instance), root cause, source citation, and an
explicit "do not close Phase 00" guard. Not a placeholder title.

Ran the full focused suite + smoke myself:

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  test/runner/assignment-dispatch.test.mjs test/runner/dispatch.test.mjs \
  test/runner/assignment-policy.test.mjs test/runner/assignment.test.mjs \
  test/runner/assignment-provenance.test.mjs
```

452/452 pass, 0 fail. Smoke: `{"mechanism":"in-process","configured":false}`.

`git status --porcelain -- src test domains` shows exactly
`domains/coding/workflows/feature.yaml`, `src/runner/dispatch/cli.mjs`, and 4
test files (`assignment-dispatch`, `assignment-policy`,
`assignment-provenance`, `assignment`) — no `resolve.mjs`,
`assignment-policy.mjs` (source), `config.mjs`, or `transport.mjs` in scope.

## Verdict

**FIX CONFIRMED.** Both round-2 findings (HIGH #1 off-by-one, MEDIUM #2
false-positive) are correctly fixed with no regressions and no new gap found
in an adversarial follow-up sweep. The P00.4 tracking entry in `index.md` is
genuine, not a placeholder.

Appended this verdict as a new "### Red-Team re-check (round-2 fixes)"
subsection at the end of `P00.3.md`'s Fix Evidence section (no other section
of that file was touched).

Status: DONE
Summary: Both round-2 fixes (HIGH #1 off-by-one, MEDIUM #2 false-positive) verified genuine via live subprocess reproduction; adversarial sweep found no new gap; P00.4 tracking entry in index.md confirmed real.
Concerns/Blockers: none
