# Red-Team Report — P00.4 (provider-family fix, final cell of Phase 00)

Full findings are in `P00.4.md`'s own `## Red-Team` section (replaces the
placeholder). This report summarizes the attack log and verdict.

## Verdict: FINDINGS (2, both currently latent — no live dispatch mis-routed today)

## Attack log

1. **4-shape re-verification (bare/Claude, bare/non-Claude,
   `invocations[]`/non-Claude, `invocations[]`/`providerModel`)** — re-built
   from scratch, cross-checked against `resolveAssignmentDispatchPolicy`
   AND `resolveExecutorConfig` directly (not just the warning's own logic).
   All 4 resolve as documented. Clean.
2. **Multiple/empty `invocations[]` entries** — found a real divergence for
   `invocations: []`: `resolveAssignmentDispatchPolicy` silently succeeds
   (`providerModel: 'claude'`), `resolveExecutorConfig` throws. But
   `validateExecutorEntryShape` already rejects this shape at config-load
   time (non-empty-array requirement), confirmed via `loadRunnerConfig`
   throwing on it — unreachable via any real config, so not written up as a
   finding.
   Then widened the attack to "`invocations[]` present but no `via:"cli"`
   entry" (e.g. MCP-only) — this shape **does** pass real config validation,
   and this repo's own live `.fgos/config.json` already has one:
   `gitnexus` (`kind: 'tool'`, `invocations: [{via: 'mcp', ...}]`, no cli
   entry). Reproduced the exact same disagreement against this repo's real,
   loaded config (not a synthetic fixture) — **MEDIUM finding**, written up
   in P00.4.md.
3. **`transport.mjs`'s new test genuineness** — read the actual test
   (`dispatch.test.mjs` ~line 1943): calls `resolveExecutorCommand` for
   real, not mocked; reverting the fix flips the assertion. Genuine. Clean.
4. **Full regression sweep** — required 454-test command list: 454/454 pass.
   Full suite: 4678 tests, 4664 pass, 9 fail — 8 exact index.md baseline
   matches (#1-7, #10), the 9th (`spawnWorker: idleTimeoutMs...`) is the
   same test this cell's own Doer/Coordinator already flagged flaky,
   re-verified passing 353/353 in isolation. Zero new/unexplained failures —
   cleaner than every prior round's own full-suite count. Clean.
5. **Live config sanity, independently reproduced** — `node bin/fgos.mjs
   list` against this repo's real `.fgos/config.json`: exit 0, empty
   stderr. Confirmed directly, not trusted from the Fix Evidence claim.
   Clean (as documented — the 12 registered executors are all `via:"cli"`-
   bearing, so the new MEDIUM finding's affected shape (`gitnexus`) doesn't
   currently trigger this check either — see finding for why).
6. **Live gitnexus finding, traced end-to-end**: confirmed
   `compileDispatchPlan` itself resolves `gitnexus` correctly via its
   mcp-handback branch (`governance.providerFamily: null`, mechanism
   `in-process`) through both its direct-`executorId` and `for`/purpose
   doors — so no live crash today. But `resolveAssignmentDispatchPolicy` is
   called *unconditionally*, ungated by mechanism, from
   `assignment-runner.mjs:637` for every assignment — if any
   `policy.preferExecutor` (or workflow op) ever names `gitnexus` (or a
   future MCP-only agent-kind executor), the persisted
   `providerModel`/`provenance.provider.value` would be silently wrong
   (`'claude'`). Grepped `domains/`/`docs/` for any live `preferExecutor:
   gitnexus` — none found, so latent, not active.
7. **Scope** — `git status --porcelain -- src test domains`: exactly the 7
   expected files (3 src, 4 test), nothing under `src/runner/coordination/**`.
   Clean.
8. **Other independent `providerFamily`/`providerModel` call sites** —
   grepped the whole `src/runner/dispatch/` tree per the brief. Found two
   more (`cli.mjs:188-191` `spawnWorker`, `cli.mjs:443-446` `execute`
   handler) that read `executor?.providerModel` directly via `modelForTier`
   (no command-based inference), defaulting to `'claude'` when absent.
   Empirically confirmed: for the same accepted bare-shape gap, this also
   picks a literal Claude model name for a non-Claude-command executor's
   actual spawn — a real functional consequence beyond the documented
   governance/provenance-labeling framing. Same accepted mitigation
   (declare `providerModel`) already fully covers it — **LOW,
   documentation-completeness finding only**, written up in P00.4.md.

## Findings written to P00.4.md

- **MEDIUM** — `invocations[]`-shaped, no-`via:"cli"`-entry executors (live:
  `gitnexus`) reproduce the exact disagreement this cell exists to close;
  the Round-2 warning-narrowing fix's blanket "invocations[]-shaped ->
  already agrees" claim is false for this sub-case, and the warning doesn't
  cover it either.
- **LOW** — the already-accepted bare-shape gap's real blast radius extends
  to live model selection (`spawnWorker`/`execute`'s `modelForTier` calls),
  not only governance/provenance labeling as currently documented in Gaps.

Neither finding changes this cell's prior dispositions or requires
reopening the already-fixed MEDIUM #1/#2; both are additive scope/
documentation gaps for a future pass, same "declare `providerModel`
explicitly" remediation direction already accepted.
