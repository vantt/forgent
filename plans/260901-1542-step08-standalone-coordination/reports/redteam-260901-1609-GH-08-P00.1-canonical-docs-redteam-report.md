# Red-Team Report — Cell P00.1 (Canonical Decisions And Contracts)

Red-Team: independent (this session), 2026-09-01, branch
`step-08-standalone-coordination`, base ref `75505c75812703b355f1291d618244c75a5d7b09`.

## Scope

Documentation-only cell (Phase 00 R1-R4). Read the Doer's and Reviewer's full
trail (`P00.1.md`, including the M1/M2/L1/L2 findings, Coordinator disposition,
and Fixer's "Fix Evidence"), the pre-plan architecture review, `plan.md`'s
Locked Product Decisions/Out Of Scope/Intent Traceability, the Phase 00
requirements file, ADR-006/007 (unedited baseline), and the full content of
every new/modified file under `git status --porcelain` (ADR-008/009/010, both
new contracts, the intent-preservation-ledger.md, vocabulary files, five
architecture files, six README indexes, `docs/specs/runner.md` and
`reading-map.md`, the proposal's extraction pointers, and the Step 07 plan.md
stale-row fix).

Attempted every attack in the dispatch prompt's documentation-authority
playbook. Two succeeded (write-ups below); the rest did not:

- **Silent contract reopening**: no new/modified doc redefines or narrows an
  ADR-006/007 decision. `FORBIDDEN_SESSION_FIELDS` is reused, not restated,
  everywhere checked (ADR-008 §2, both contracts).
- **Proposal laundering**: sampled 4 of the 6 extraction-pointer blockquotes
  (lines 175, 227, 914, 1446/1468 — a different set from the Reviewer's
  already-fixed M1 at ~1441) against their target ADR text; all read as
  genuinely settled decisions, not carried-over discussion prose.
- **Forbidden-field escape hatch**: `missionId` is explicitly enumerated
  "mandatory or optional... must not appear... at any nesting level" in both
  the contract's Forbidden Fields table and a Required Negative Test — not an
  omission.
- **AC-I traceability gaps**: compared AC-I001, AC-I006, AC-I007 wording
  between `plan.md`'s Intent Traceability table and the (wholly new,
  uncommitted) `intent-preservation-ledger.md`; none was softened — AC-I001's
  "must not preclude" is if anything broader than `plan.md`'s phrasing.
- **Dangling/broken cross-links**: sampled 20 links across ADR-008/009/010,
  both contracts, three README indexes, and three architecture files — a set
  disjoint from the Reviewer's ~15 already-checked anchors (script in this
  report's evidence trail). All 20 resolved (file exists, anchor present).
- **Out-of-scope creep**: grepped ADR-008/009/010 and both contracts for
  `AdhocTask`, `telemetry`, `herdr`, and `purpose`; all mentions are properly
  deferred/excluded except the one write-up below (L1).
- **Report/reality mismatch**: the Doer's report matches what's on disk,
  including accurately describing the very "detect-and-report posture" phrase
  that H1 below flags as a substantive problem (the report states the fact
  correctly; it just doesn't catch that the fact conflicts with `plan.md`).
  The Fixer's M1/L1 "Fix Evidence" diffs were independently confirmed against
  the live files (blockquote now trails the full 5-bullet list at
  `proposals/step-08-standalone-coordination-protocols.md:1446-1448`; ADR-010's
  M5c citation reworded as claimed).

## Findings

Full write-ups are appended to `P00.1.md`'s `## Red-Team` section (not
duplicated here in full):

**H1 (HIGH)** — ADR-008 Decision 4's "detect-and-report posture for legacy
`.fgos/missions/`" data directly contradicts `plan.md`'s Locked Product
Decisions text ("There is no migration reader, detector, reporter,
compatibility writer, or stored-data migration," `plan.md:68-71`). The only
source for the detect-and-report requirement anywhere in the diff is the
pre-plan review's finding M4 / Rebuttal Reconciliation row 6 — a reviewer
recommendation, not an explicit human decision — while the actual
maintainer-approved checkpoint text
(`proposals/…:171-173`, "Locked in the 2026-09-01 checkpoint") says only "no
legacy record reader or migration is built," with no detect-and-report
clause. `plan.md`, written after the pre-plan review, appears to have landed
on the stricter position instead. ADR-008 nonetheless self-declares
`Design status: Accepted` over this addition without ever naming the
conflict, and the same claim propagates into
`intent-preservation-ledger.md:238` and the new `docs/specs/runner.md`
section. This is exactly the self-certification pattern the dispatch prompt
flagged as the most important attack for a docs-authority cell, and it
matches `plan.md`'s own Autonomous Execution Contract stop-gate 1 ("an
accepted source above contradicts a requirement and no additive
interpretation exists") — a condition that should have halted the
autonomous track for maintainer clarification, not been resolved
unilaterally.

**L1 (LOW)** — `contracts/coordination-session.md:25`'s Storage Layout names
a `tasks/<task-id>.json` path as "optional session-local AdhocTask records."
`plan.md`'s Locked Product Decisions lists "general AdhocTask storage" as
explicitly out of V1, and the pre-plan review recommended deferring AdhocTax
mentions until Phase 8.4. Naming a canonical (if undefined) storage path for
it inside an Accepted contract pre-authorizes a persistence hook ahead of any
phase actually designing it.

## Verdict

`Verdict: FINDINGS (1 HIGH, 0 MEDIUM, 1 LOW)` — recorded in `P00.1.md`.

Status: DONE
Summary: Two new findings independent of the Doer/Reviewer/Fixer trail: H1
(ADR-008's mission-lite detect-and-report posture contradicts plan.md's
locked "no detector, reporter" text and self-certifies Accepted over an
unreconciled conflict) and L1 (contract names an AdhocTask storage path
plan.md excludes from V1). Every other attempted attack (proposal laundering,
forbidden-field omission, AC-I wording weakening, a disjoint 20-link sample,
report/reality mismatch, further out-of-scope creep) did not succeed.
Concerns/Blockers: 1 HIGH, 1 LOW — H1 is a genuine open product decision the
plan's own stop-gate requires human resolution for, not a doc-quality nit.
