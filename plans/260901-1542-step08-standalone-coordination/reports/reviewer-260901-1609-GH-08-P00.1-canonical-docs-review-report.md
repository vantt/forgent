# Reviewer Report — P00.1 Canonical Contracts And Dispatch Prerequisites (Phase 00 R1-R4)

Reviewer: independent (this session), 2026-09-01
Branch: `step-08-standalone-coordination`, BASE_REF `75505c75812703b355f1291d618244c75a5d7b09`
Scope: doc-only cell — new ADR-008/009/010, `contracts/coordination-session.md`,
`contracts/flow-definition.md`, `intent-preservation-ledger.md`, plus updates
to architecture/vocabulary/contracts/decisions indexes, `docs/specs/{reading-map,runner}.md`,
and the Step 07 plan.md stale row.

## Verdict

**FINDINGS** — 1 MEDIUM, 2 LOW, 0 HIGH. Nothing here blocks acceptance; the
MEDIUM is a one-line Markdown placement fix.

## What Was Verified

- **Scope discipline.** `git status --porcelain -- src test domains` is
  empty — no runtime code touched, matching the Doer's claim. `git diff --
  decisions/ADR-006-*.md decisions/ADR-007-*.md` is empty — both accepted
  ADRs are unedited in place, satisfying the plan's explicit prohibition.
- **Metadata and structure.** All three new ADRs (ADR-008, ADR-009, ADR-010)
  carry the full `documentation-governance.md` Required Metadata block
  (Document type, Design status: Accepted, Implementation: Not started, Last
  reviewed: 2026-09-01, Canonical for) plus Context/Decision/Consequences/
  Rejected Alternatives sections. Both new contracts (`coordination-session.md`,
  `flow-definition.md`) carry the same metadata block plus Scope/schema/
  Required Negative Tests sections.
- **R1 substance.** ADR-008 correctly states CoordinationSession as V1
  recovery root, one-way session-to-Assignment membership (no adoption API,
  no timestamp heuristic — matches pre-plan finding M7's resolution), no
  `missionId` anywhere in V1 schemas (AC-I007), and the `SessionActor`/
  `Persona`/`Stance` split with `Participant` explicitly not reused (H1).
  ADR-009 correctly drops `purpose` from FlowDefinition V1 (M8), derives node
  `kind` from the profile discriminator rather than restating it (M1), and
  states the FlowDefinition/Workflow adapter is additive with zero forced
  consumer migration (H3).
- **R2 substance.** Both new contracts define versions/ids/refs/bounds,
  status vocabulary (`active|completed|partial|failed`), topology, the
  `spec.profile` discriminator, PolicyPatch provenance shape
  `{field:{value,source:{scope,id}}}`, and forbidden-field tables that reuse
  (not redefine) ADR-006 §6's `FORBIDDEN_SESSION_FIELDS`. All architecture
  index files (`README.md`, `architecture/README.md`, `contracts/README.md`,
  `decisions/README.md`) and the portal `README.md` were updated to list the
  three new ADRs and two new contracts. `protocol-model.md`'s previously
  "remains unresolved" Stage-vs-Phase question and `system-context.md`'s
  previously "remain under discussion" CoordinationSession/FlowDefinition
  boundary are both now resolved with ADR-008/ADR-009 pointers; the residual
  "under discussion" text in `system-context.md` is correctly re-scoped to
  AdhocTask, the full runtime, and the broader protocol catalog — content
  that genuinely remains open per the plan's own phase sequencing, not the
  identity/schema boundary this cell promotes.
- **R3 substance.** `canonical-concepts.md` adds `FlowDefinition`, `Phase`,
  `SessionActor`, `Persona`, and `Stance` entries, and the existing `Role`
  entry now explicitly states seat/responsibility-position equivalence.
  `deprecated-and-reserved.md` adds an explicit `Participant` reservation
  entry pointing at the platform-level D0014 concept. `docs/specs/runner.md`
  gained a new "CoordinationSession — điều phối agent Work-độc-lập" section
  summarizing the accepted boundary at spec-grade; `docs/specs/reading-map.md`
  gained a pointer bullet. Both are scoped as pointers to the canonical docs,
  not a second source of truth.
- **R4 substance.** The Step 07 `plan.md` Phase 2 row is fixed to `done` with
  the exact commit/evidence citation, and the diff is scoped to only that one
  table row (verified via full `git diff` of the file — no unrelated
  changes). The proposal file carries exactly 6 `> Extracted into ADR-...`
  blockquotes (grep-counted) and its own `Design status: Discussion` line is
  unchanged — proposal text is not silently promoted to normative. All 9
  `### AC-I00[1-9]` ledger headings are present; none is deleted, narrowed, or
  reinterpreted to mean something weaker — each entry's `deferred-preserved`
  status, Must-not-preclude text, and Revisit-when trigger read the same as
  (or are a strict superset of, via the new "Phase 00 Intent Proof Matrix"
  section) what the pre-plan review and plan.md require. AC-I003's source is
  corrected to the dated human checkpoint decision rather than the proposal
  itself, matching finding M5(a). AC-I006 carries the H2 provenance caveat and
  the M8 `purpose`-revisit trigger, exactly as required.
- **Cross-reference integrity.** Spot-checked roughly 15 links/anchors across
  the changed files by opening the target and grepping for the heading: all
  six Vision `V-00x` anchors cited by the new ADRs, all six new
  `canonical-concepts.md` anchors (`FlowDefinition`, `Phase`, `Role`,
  `SessionActor`, `Persona`, `Stance`), the `Participant` anchor in
  `deprecated-and-reserved.md`, both contracts' `PolicyPatch`(`-provenance`)
  anchors, and file-path references to `ADR-001-work-lifecycle-authority.md`,
  `contracts/workflow-stage-operation.md`, and
  `contracts/assignment-run-runresult.md`. All resolved.
- **A correctly-applied prior finding.** The pre-plan review flagged
  `distinctModelFamilies` (cohort field, "model family" — no derivation
  source in current dispatch code) as premature and recommended dropping it
  unless sourced (H2/non-blocking follow-ups), while noting "provider family"
  *is* sourced (`executor.providerModel`). The accepted `flow-definition.md`
  contract's `cohort` block uses only `distinctProviderFamilies` — the
  sourced concept — and `distinctModelFamilies` does not appear anywhere in
  the accepted docs (only in the still-Discussion proposal, where it was
  already written). This is exactly the correct resolution, not carried over
  by accident.

## Findings

See the "## Review" section appended to
`docs/architect/agent-coordination/verification/step-08-standalone-coordination/P00.1.md`
for the full findings with file/line evidence. Summary:

- **M1 (MEDIUM):** an extraction-pointer blockquote in the proposal
  (`proposals/step-08-standalone-coordination-protocols.md:1441`) is inserted
  in the middle of a 5-bullet list instead of after it, breaking Markdown
  list continuity and understating which bullets were actually promoted into
  ADR-010 (all 5 are covered by ADR-010 Decisions 4-5, not just the first 3).
  One-line fix: move the blockquote after the full list.
- **L1 (LOW):** ADR-010's Context cites pre-plan finding M5c as if it already
  supported "human-confirmed intention," but M5c's own text records parity as
  *withdrawn*/*not confirmed* and lists it as an open question for the
  maintainer; the actual confirmation is a separate, later checkpoint source
  that the ledger itself cites correctly. Not a substantive error — AC-I008
  is correctly `deferred-preserved` and ADR-010 doesn't over-claim — but the
  citation could mislead a reader who checks only M5c.
- **L2 (LOW, informational):** `AGENTS.md`/`CLAUDE.md` show a 2-line diff
  outside `docs/` (Do Not Touch scope), but it is solely the
  auto-regenerated GitNexus index symbol-count comment block, not authored
  content — not treated as a real scope violation.

## Commands Run

```bash
git branch --show-current
git status --porcelain
git status --porcelain -- src test domains
git diff -- docs/architect/agent-coordination/decisions/ADR-006-assignment-provenance-and-contract-snapshot.md \
  docs/architect/agent-coordination/decisions/ADR-007-domain-harness-seam-and-non-driving-inline-evidence.md
git diff --stat -- docs/architect/agent-coordination docs/specs plans/260831-1637-step07-inline-assignment-mvp/plan.md
git diff -- <each modified file individually, listed in the dispatch prompt>
grep -c '^> Extracted into' docs/architect/agent-coordination/proposals/step-08-standalone-coordination-protocols.md
grep -n '^### AC-I00' docs/architect/agent-coordination/intent-preservation-ledger.md
grep -rn 'distinctModelFamilies\|distinctProviderFamilies' docs/architect/agent-coordination/
# plus targeted grep/anchor spot-checks against ~15 cross-referenced files
```

## Unresolved Questions

None requiring maintainer input — both findings above are self-contained
Markdown fixes that a future doc pass (or this cell's own close-out) can make
without further design decisions.
