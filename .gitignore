# Upstream source clones — cursors tracked in docs/distillery/, re-clone as needed
/upstreams/

# forgent state layer — events.jsonl is truth (committed); state.json is a
# derived, rebuildable view. Tried bucketing it into .fgos/cache/ in
# phase-02 (plans/260825-0842-fgos-logs-dir-bucketing) and reverted: 21 test
# files independently hardcode this exact path, causing 51 failures for a
# purely cosmetic move (state.json was already gitignored at its old
# location too -- moving it fixes zero dirty-tree problem). Left at root.
.fgos/state.json
# diagnostic/telemetry logs — local observability, never committed
# (worker-dispatch-log D4; also holds approve-post-success-faults.jsonl,
# main-checkout-guard-warnings.jsonl, changelog-nag-history.jsonl,
# entropy-history.jsonl, invocation-faults.jsonl since phase-01,
# plans/260825-0842-fgos-logs-dir-bucketing)
.fgos/logs/
# per-machine runtime facts — tool-status.local.json and the events.jsonl
# truncation-guard mark, moved here phase-02
# (plans/260825-0842-fgos-logs-dir-bucketing). sessions.json/*.lock stay at
# .fgos/ root for now (deferred, on the git hook's own live import path —
# see plan.md phase-02 "held back" section).
.fgos/runtime/
node_modules/

# test-leak artifacts — belt against auto-commit clobber (backlog STR74/STR65)
output.txt
*-output.txt
.fgos/sessions.json

# main-checkout activity lock + sibling lock files (STR65) — process state, never committed
.fgos/*.lock

# untracked local secret environment variables (tsk-gb3)
.fgos/secrets.local.env

# BEE:START
.bee/state.json
.bee/reservations.json
.bee/workers/
.bee/logs/
.bee/capture-queue.jsonl
.bee/feedback-digest.json
.bee/.inject-cache.json
.bee/HANDOFF.json
.bee/spikes/
.bee/manifest-hash.json
# BEE:END

# Workshop tree — untracked in the product repo (repo divorce)
/.bee/
/.claude/*
!/.claude/skills/
!/.claude/settings.json
!/.claude/agents/
/.claude/skills/*
!/.claude/skills/fgos-*/
!/.claude/skills/distill/
!/.claude/skills/_shared/
/.agents/*
!/.agents/skills/
/.agents/skills/*
!/.agents/skills/fgos-*/
!/.agents/skills/distill/
!/.agents/skills/_shared/
/.codex/
/upstreams/

# Rust build output
/herdr-plugin/target/
# tsk-48w: herdr-plugin/web's bundled output, embedded into the binary at
# compile time (RustEmbed) -- never committed, herdr-plugin/build.rs
# guarantees this directory exists even when npm run bundle hasn't run.
/herdr-plugin/static/
