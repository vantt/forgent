# Porting Log

Cột `Status` đọc/ghi qua state layer (`docs/distillery/state/porting/`) — dựng lại được từ event log; các cột khác (Score, Local, Đích, Commit, Ghi chú) vẫn tay-edit. Tính năng bị từ chối vẫn ghi lại kèm lý do — tránh đánh giá lại.

- Status: `candidate` → `planned` → `in-progress` → `ported` / `adapted` / `rejected`
- **Score** `R# E# F#` chấm MỘT LẦN lúc tạo candidate (rubric: `.agents/skills/distill/references/extract-rules.md`); tổng R×E/F không lưu — xem bảng xếp hạng bằng `distill.mjs rank`. Re-score một dòng chỉ khi delta scan mang evidence mới.
- **Local** = tên khái niệm/artifact trong project ta sau khi port (bắt buộc khi `ported`/`adapted`, `—` khi còn candidate). Cầu nối hai chiều source↔local, tra bằng `distill.mjs map [term]`; khi delta scan thấy nguồn tiến hóa một feature đã port, map chỉ ra local nào cần xem lại.
- Score cao + các nguồn giải khác nhau (matrix ô `hòa`/`~`) = ứng viên **deep-dive** cho human.

| Feature | Nguồn | Status | Score | Local | Đích (path) | Commit | Ghi chú / Lý do |
|---|---|---|---|---|---|---|---|
| policy-vs-ops-split | beehive:policy-vs-ops-split + repository-harness:policy-vs-durable-separation | adapted | — | distillery-layout | docs/distillery/ + upstreams/ | 29c4af3 | Đã thành quy ước sống trong distill init; tổng quát hóa khi forgent có state dir riêng |
| pi-executor-runtime-capacity | pi (`@earendil-works/pi-coding-agent`, live proof-test, tsk-47r) | adapted | — | runner.executors.pi | src/setup/registrations.mjs | — | `pi` đăng ký thành `agent`-kind executor thứ hai (cạnh `agy`), invocation `--provider openai-codex --model gpt-5.5`. D4 live proof-test GREEN: hợp đồng worker (tsk-2uf-2) trung tính với provider, xác nhận qua consumer thứ hai thật, không chỉ khẳng định lý thuyết — xem docs/history/pi-executor-runtime-capacity/RESEARCH.md Round 4 |
| three-way-merge-config-installer | repository-harness:rust-clean-architecture-core-installer | candidate | R3 E2 F3 | — | fgOS's own skill/AGENTS.md distribution (`fgos setup`, `fgos doctor`, config-merge nói ở AGENTS.md §Install/setup/doctor gate) | — | **Từ delta scan repository-harness 2026-08-07 (9cc306d→0a79bbe).** Crate Rust `harness` mới thay hẳn awk marker-replace cũ bằng installer thật: so 3 chiều base(đã cài)/local(workspace)/upstream(bản mới) — giống nhau thì im, consumer sửa mà upstream không đổi thì giữ, cả hai đổi thì `git merge-file --diff3` tự merge, merge lỗi thì stage BASE/LOCAL/UPSTREAM/RESOLVED cho người sửa tay + `--continue`. Kèm transaction journal (atomic apply, rollback-on-error) và test riêng (`clean_architecture.rs`) enforce chiều phụ thuộc bằng grep-cấm-import-ngược. Đúng câu hỏi "có compiler nào sinh AGENTS.md gọn không" — có, và nó là merge-engine chứ không phải template engine. fgOS đã tự nhận có nghĩa vụ tương tự (AGENTS.md: "project config always overwrites global, fgOS stays aware of both without conflict") nhưng chưa rõ có conflict-resolution 3-chiều thật khi consumer tự sửa file harness đã cài; đây là mẫu triển khai cụ thể đã test. R3 chạm toàn bộ tầng phân phối skill/config của fgOS; E2 một nguồn nhưng có test suite thật (clean_architecture.rs + release_update.rs + update_lifecycle.rs), không chỉ prose; F3 subsystem (domain/application/infrastructure/interface + three-way merge + transaction journal) |
| evidence-capsule-deterministic-patch-render | repository-harness:onboard-repository-evidence-capsule | candidate | R2 E1 F2 | — | fgos-coding-implement / bất kỳ skill nào tự tay đề xuất diff cho người duyệt | — | **Từ delta scan repository-harness 2026-08-07.** Thay vì agent tự gõ diff (dễ sai transcription/hash), skill build full after-image trong memory rồi pipe qua script build-sẵn (`render_patch.py`) — đọc before-bytes qua `git show`, tự sinh unified diff + SHA-256 thật; capsule kèm digest tính trên bytes thật, không phải lời agent tự khai. Cấu trúc 5 trường bắt buộc (concrete failure prevented / evidence path / destination / content / unknowns / replay-proof) cho mỗi đề xuất. Đáng cân nhắc khi fgOS có bước nào tự tay generate patch cho human review mà hiện đang tin lời agent. R2 một component hẹp (patch-proposal step); E1 một nguồn, chưa dogfood ở forgent; F2 script + capsule schema, không phải subsystem lớn |
| improve-fresh-rerun-gate | repository-harness:improve-harness-fresh-rerun-gate | candidate | R2 E2 F1 | — | fgos-coding-compounding / retrospective loop's "keep/revise/remove" judgment | — | **Từ delta scan repository-harness 2026-08-07.** Sửa harness (docs/tool/runbook) chỉ khi có baseline lỗi quan sát thật; TRƯỚC khi sửa bắt buộc viết 1 câu tiên đoán dạng "nếu thêm X tại owner Y thì agent mới sẽ Z, vì M" — SAU khi sửa bắt buộc rerun bằng session hoàn toàn mới rồi mới cho Keep/Revise/Remove. Gần với "predicted→actual loop" đã có ở `distill.mjs outcome`/porting-log Outcome note, nhưng harness áp nó cho CHÍNH sửa-đổi-harness (self-improvement), không chỉ cho porting candidate. Dogfood thật: `docs/plans/completed/harness-improvement-engineering-boundary-wisdom.md` — Keep, rerun độc lập tìm thêm 6 rủi ro thật không có trong bản trước. R2 subsystem hẹp (skill self-improvement loop); E2 có outcome đo được thật (không chỉ đề xuất), khớp category "một nguồn đã dogfood/đo được"; F1 doctrine + template nhỏ |
| trigger-only-descriptions | beehive:trigger-only-descriptions | adapted | — | trigger-only-descriptions | .agents/skills/distill/SKILL.md | 29c4af3 | Giữ nguyên tên; nâng thành chuẩn chung khi có skill thứ hai. **Convergence @superpowers scan 2026-07-28**: independent 3rd source arrives at the identical rule under the name "Skill Discovery Optimization" (`superpowers:skill-discovery-optimization`), with a concrete cited failure (a workflow-summarizing description caused an agent to run only 1 of 2 required reviews). Two independent sources now cite a real regression each — E1→E2 for this convention if re-scored |
| skill-conventions | beehive:skill-budgets-conventions | adapted | — | skill-conventions | .agents/skills/distill/ | 29c4af3 | <200 dòng + references/ + headless + red flags + handoff + CREATION-LOG; chưa port pressure-test (xem tdd-for-skills) |
| error-why-fix-refusals | beehive:error-why-fix-refusals | adapted | — | error-why-fix | .agents/skills/distill/scripts/distill.mjs (fail()) | 29c4af3 | Mọi refusal theo ERROR/WHY/FIX; nâng chuẩn chung khi có CLI thứ hai |
| managed-block-markers | beehive:managed-block-markers | adapted | — | distill-gitignore-block | distill.mjs init (.gitignore DISTILL:START/END) | 29c4af3 | Byte ngoài marker giữ nguyên; mở rộng cho AGENTS.md khi có onboarding. **Delta 2026-07-14 (beehive codex-onboarding/ao)**: upstream tiến hóa cùng gene sang **JSON đa-chủ** — `.codex/hooks.json` merge-không-overwrite (giữ entry của repo lẫn của beehive), sau sự cố self-onboard clobber. Local ta hiện chỉ marker text-block; nếu distill về sau ghi file JSON đa-chủ, xem pattern merge này |
| retry-budget-anti-loop | beehive:cell-lifetime-budgets-anti-loop | candidate | R3 E2 F2 | — | anti-loop layer của work-item lifecycle forgent | — | **Ưu tiên số một đợt scan @05a131f.** Ba ngân sách kiểm tại cửa claim (max_claims / max_failed_attempts / max_same_signature) trên nền sổ attempts append-only + chữ ký lỗi chuẩn hoá — chặn cả cạn-quota lẫn "chạy lại y hệt bản vá vừa hỏng". Khớp thẳng ghi chú kiến trúc forgent "anti-loop là precondition của fan-out". Chốt cần giữ khi port: cửa này KHÔNG đọc cờ bypass — an-toàn-cấu-trúc ≠ checkpoint-của-người. E2: một nguồn nhưng đã dogfood thật (sinh ra từ GH #27 vá vòng lặp có thật) |
| semantic-judge-rework-teeth | beehive:semantic-judge-verdict-loop | candidate | R3 E2 F3 | — | quality gate của lifecycle forgent | — | Verdict schema đóng (mỗi check bắt buộc evidence, cross-check verdict tự mâu thuẫn) + răng thật lên vòng đời: NEEDS_REVISION mở lại task về `open` VÀ xoá bằng chứng verify cũ. Bài học phải port kèm: bản trước mở lại về `claimed` mà giữ verify cũ → PASS sau đó cap lại được không cần verify mới. F3 vì cần cả judge dispatch lẫn schema lẫn sửa lifecycle. Cân với `signal-scaled-review-roster` (CE) khi thiết kế — hai nửa khác nhau của cùng bài toán |
| store-lock-named-mutex | beehive:store-lock-named-mutex | candidate | R3 E2 F2 | — | móng concurrency khi forgent chạy đa-agent song song | — | Named mutex O_EXCL + staleness HAI TẦNG (30s = ứng viên; cướp quyền đòi pid chết hoặc quá trần cứng 1h) + cướp bằng atomic rename + release chỉ xoá lock khớp pid+token của chính mình. Hai chốt thiết kế đắt: (a) KHÔNG dùng heartbeat chứng khoá tươi vì chủ khoá hợp lệ có thể chặn event loop nhiều phút do `spawnSync`; (b) CLI đợi, hook `maxAttempts:1` không bao giờ chờ. E2: đã dogfood, spike ghi lại ca unlink vô điều kiện cho 7-8 "người thắng" |
| gate-precondition-vs-human-checkpoint | beehive:advisor-consult-gate-precondition | candidate | R3 E1 F1 | — | mô hình human-gate conditional theo stage của forgent | — | Rẻ nhất mà đúng trọng tâm: tách **checkpoint của người** (autopilot gỡ được) khỏi **tiền-điều-kiện cơ học** (không ai gỡ) — đúng bài toán "mixed-autonomy, human-gates conditional theo stage" của forgent. Kèm định nghĩa staleness bằng SỰ KIỆN kiểm được (hash đổi / có decision mới / lệch scope), không bao giờ bằng TTL — tái dùng được cho mọi cache tri thức. E1: một nguồn, chưa có outcome đo được. **Delta scan beehive 2026-07-28 (v1.3.9→v1.18.3, AO2b-AO13)**: chính cơ chế này ĐÃ SINH RA thật trong beehive — advisor-consult trước Gate 3 giờ enforce bằng THROW (không phải warning) khi `advisor_ref` thiếu/stale, staleness đúng 4 điều-kiện-sự-kiện (feature đổi / decision mới / plan-hash đổi / gate bị revoke) — khớp thẳng đề xuất staleness-bằng-sự-kiện của row này. E1→E2: giờ có outcome thật (beehive production, not just beehive) |
| verify-suite-integrity | beehive:verify-manifest-floor-guard + beehive:hermetic-verify-env-sealing + beehive:honest-windows-ci-subset | candidate | R2 E2 F1 | — | verify pipeline của forgent/repo | — | Ba mảnh rẻ cùng một luật "bộ đo phải tự chứng": sàn số suite + danh sách bắt buộc (checker có self-test chứng nó cắn); xoá session id khỏi env con (trị xanh-giả khi dev chạy verify TỪ TRONG một phiên harness sống — ca này forgent dính được ngay); và CI tự khai chính xác nó KHÔNG chứng cái gì. F1 vì là script + convention. Kèm `parallel-verify-runner-discovery`: bỏ mảng suite viết tay không vì thẩm mỹ mà vì nó là điểm va chạm merge của mọi feature thêm test |
| dispatch-payload-as-authority | beehive:dispatch-prepare-payload-builder + beehive:pinned-tier-agent-types | candidate | R3 E2 F2 | — | tầng dispatch của forgent multi-agent | — | Sinh payload dispatch bằng CODE từ một nơi duy nhất, tự kiểm qua chính hàm guard, ghi audit, và từ chối cấp cho ai chưa cầm claim ("payload LÀ thẩm quyền hành động"). Kèm bài học tier: khai tier trong prompt chưa đủ — phải có agent type gắn model, `general-purpose` bị deny vì không mang danh tính tier. Nối tiếp `cost-tiered-delegation` đã là quy ước sống ở đây: nâng từ quy ước lên khuôn sinh-payload. E2: dogfood, có suite pin hợp đồng |
| worktree-merge-semantic-gate | beehive:worktree-merge-staged-verify-gate | ported | R2 E2 F3 | worktree-merge-staged-verify-gate → merge.mjs | src/runner/merge.mjs | — | **Sửa 2026-08-05 (deep-dive parallel-decomposition-and-merge):** hàng này lỗi thời — tính năng đã tồn tại. `src/runner/merge.mjs` implement đúng giao dịch dàn trận: `git merge --no-commit --no-ff` stage trước, verify chạy trên cây CHƯA commit, chỉ commit khi xanh, đỏ thì `git merge --abort` (comment nguồn: `docs/history/pr-lifecycle/reports/validation-s1-gate.md`, spike "nocommit-probe"). Không rõ port có TRỰC TIẾP từ beehive hay hội tụ độc lập (không có evidence trong code trỏ ngược nguồn) — giữ Score cũ vì chưa xác minh được đường đi, chỉ sửa Status/Local cho khớp thực tế |
| crash-transcript-recovery | beehive:crash-transcript-recovery-mining | candidate | R2 E1 F2 | — | lớp phục hồi phiên của forgent | — | Phát hiện phiên crash bằng "clean-end trio" vắng mặt + heartbeat cũ + còn dấu việc dở; khai quật là ĐỀ NGHỊ, không tự chạy. Kỷ luật context đáng học nhất: CLI chỉ PHÁT RA prompt, không gọi LLM — dòng transcript thô không bao giờ vào context orchestrator; cửa sổ chặn cứng 500 event sau settlement bền gần nhất; kết quả vào hàng đợi `--source mined` (chưa xác nhận), không bao giờ tự thành decision. E1: mới, chưa có outcome |
| maturity-ladder | repository-harness:maturity-ladder-h0-h5 | candidate | R3 E2 F1 | — | roadmap forgent (F0–F5?) | — | Thang tiến hóa đo được thay vì feature list phẳng; E2: harness + benchmark thực chứng |
| request-authority-model | repository-harness:request-authority-model | candidate | R3 E2 F1 | — | phân lớp quyền request trong harness forgent | — | Read-only vs change request; E2: harness E12 + beehive docs-lane cùng hướng |
| fail-open-crash-wrappers | beehive:fail-open-crash-wrappers | candidate | R2 E2 F1 | — | hook runtime forgent | — | Adapter pattern (stdin normalize, root discovery, crash log); E2: beehive kế thừa claudekit, đã dogfood |
| six-questions-acceptance | repository-harness:repo-as-os-six-questions | candidate | R3 E1 F1 | — | acceptance test cho harness forgent | — | Định nghĩa "done" cho platform layer |
| state-vs-log-two-physics | beehive:state-vs-log-two-physics + compound-engineering-plugin:disk-as-source-of-truth | candidate | R3 E2 F2 | — | thiết kế memory/knowledge layer | — | Nguyên lý nền tảng; E2: beehive tường minh + harness ngầm (changesets vs docs). **CE củng cố (scan 2026-07-17)**: "results table in conversation without writing to disk first = a bug" — mọi skill dài (optimize/dogfood/sweep/handoff) write-then-read-back verify + checkpoint resume-được; bản áp-dụng-rộng của cùng luật disk-là-truth, context-là-ephemeral |
| context-rules-matrix | repository-harness:context-rules-matrix | candidate | R3 E2 F2 | — | context budget layer | — | Phase × lane × must/should/skip + token budgets + score-context; E2: harness + NLAHs research |
| verify-enforced-close | beehive:cell-task-unit + repository-harness:story-complete-atomic + compound-engineering-plugin:verification-evidence-gate | candidate | R3 E3 F3 | — | task unit của forgent | — | **E3 hội tụ độc lập ×3**: beehive cap-requires-proof ↔ harness story-complete atomic ↔ **CE ce-work `verification_evidence` khi behavior_change** (scan 2026-07-17) — ba nguồn độc lập cùng luật "không đóng khi chưa có proof"; deep-dive candidate số một. CE thêm: behavior_change là CỜ quyết định cần evidence (docs-only skip), giống lane scaling — củng cố cách beehive đã làm |
| orchestration-protocol-v1 | repository-harness:orchestration-protocol-v1 + symphony:typed-runtime-boundary + beehive:fleet-dispatch-and-merge-loop | candidate | R3 E3 F2 | — | chuẩn giao tiếp CLI forgent ↔ agent apps (agy/opencode) | — | **E3 sau symphony scan (2026-07-13)**: harness ĐỊNH NGHĨA protocol v1, symphony là CONSUMER độc lập thực chứng qua ranh giới product thật — không còn "cùng họ" mà là producer+consumer chạy được; hợp định vị platform. **Củng cố 2026-08-05 (deep-dive parallel-decomposition-and-merge)**: đây là candidate ĐÚNG BÀI TOÁN "đẩy item con ra cho agy/opencode thực thi" mà user đang hỏi — symphony's RUN_CONTRACT.json (required_outputs/forbidden_paths/result_json_schema, validate RESULT.json trước khi tin) + bee-herding's cold-external-process/capped-concurrency/manual-merge-only shape là hai bản triển khai gần nhất với "orchestrator gọi tiến trình agent NGOÀI, không chia sẻ session, tự verify kết quả trước khi tin" — đúng hình dạng forgentX cần hơn beehive's in-chat cell-swarm (worker là subagent Claude cùng phiên, không áp dụng). Ưu tiên deep-dive/port kế tiếp nếu forgentX chuẩn hóa dispatch↔agy/opencode thành schema |
| tdd-for-skills-iron-law | beehive:tdd-for-skills-iron-law | candidate | R2 E2 F2 | — | quy trình viết skill + pressure-test harness | — | E2: beehive kế thừa superpowers; đang là debt của chính distill (CREATION-LOG) |
| tool-registry-capability | repository-harness:tool-registry-capability | candidate | R3 E2 F2 | — | tool/capability layer | — | "Absent capability = clean skip"; E2: harness + AHE research. R2→R3 (2026-07-30, tsk-2br deep-dive): concrete blocking use-case now exists (tsk-1e4 — fgos-coding-planning/validating hardcode GitNexus instead of consulting an `impact-analysis` capability) |
| changeset-event-sourcing | repository-harness:changeset-event-sourcing | planned | R2 E3 F3 | — | durable layer của forgent khi có db | — | **User chốt 2026-07-13 thành LUẬT**: "db chỉ được vào khi là view; truth ở JSONL committed; rebuild từ zero" — engine defer tới ngưỡng friction, chọn engine đảo-ngược-được. **Reconsideration-note sau beads scan (2026-07-14)**: beads @303e263 đã RẼ KHỎI JSONL-truth sang Dolt-as-truth khi multi-agent write thành tải chính (beads:dolt-as-versioned-truth) — E3 lịch sử vẫn đúng nhưng quỹ đạo đổi; luật forgent VẪN hợp vì forgent single-writer + cần git-diffable, song ngưỡng "multi-writer thành tải chính" giờ có tên là điểm phải xem lại luật — human đã thấy note này. **Đối-trọng sau beads-rust scan (2026-07-15)**: beads-rust — sibling độc lập của beads — CỐ Ý fork tại classic SQLite+JSONL rồi ĐỨNG LẠI đó, tôi-luyện lõi (health contract/sync safety) thay vì đổi engine sang Dolt (beads-rust:sqlite-jsonl-classic-truth). Tức cùng một hạt giống bd sinh ra hai quỹ đạo NGƯỢC nhau; luật forgent (DB-là-view, JSONL-committed-là-truth) có bằng chứng ĐỒNG THUẬN từ br, KHÔNG chỉ đối-nghịch từ bd-Dolt. Ngưỡng phải-xem-lại vẫn đúng chỗ cũ (multi-writer thành tải chính). **Củng cố từ worktree-in-out consult 2026-07-28**: repository-harness:changeset-committed-truth-db-rebuild — cùng pattern, không thêm evidence mới, fold vào row này thay vì tạo duplicate |
| silent-bookkeeping | beehive:silent-bookkeeping | candidate | R2 E1 F1 | — | communication doctrine forgent | — | Kèm gate-presentation-contract; UX khác biệt rõ nhất của beehive |
| hook-catalog-projection | beehive:hook-catalog-projection | candidate | R2 E1 F2 | — | hook layer nếu forgent multi-runtime | — | Chỉ đáng khi chốt mục tiêu multi-runtime; đi cặp parity tests |
| proof-before-tag-promotion | repository-harness:proof-before-tag-promotion | candidate | R1 E2 F2 | — | release engineering forgent | — | E2: sinh từ sự cố thật (run 29222332569); frozen baseline vs current contract |
| repo-separation-playbook | repository-harness:repo-separation-playbook | candidate | R1 E1 F1 | — | quy trình tách module→repo sau này | — | YAGNI hiện tại nhưng đáng giữ; khi forgent tách sub-product sẽ cần |
| next-work-derived-from-state | beehive:cell-status-lifecycle + repository-harness:runnable-derived-dispatch | candidate | R2 E3 F2 | — | task/work layer của forgent harness | — | **E3 hội tụ độc lập**: readyCells ↔ runnable predicate — "việc kế tiếp" là derived query từ deps+status, không phải list tay; đi cặp với verify-enforced-close (cùng task unit) |
| cas-expected-status-transitions | repository-harness:story-status-single-door | candidate | R2 E2 F1 | — | state transition guard của forgent harness | — | Từ deep-dive `routing`: CAS chống lost-update; chưa cấp bách (single-writer hôm nay) nhưng rẻ để cài ngay khi dựng state layer, tránh retrofit sau khi có 2nd writer |
| routing-model-per-interface | beehive:hive-first-skill-router + repository-harness:protocol-next-action-table + compound-engineering-plugin:orchestrator-judges-fans-out-fixes | candidate | R3 E3 F1 | — | nguyên tắc thiết kế routing forgent | — | Từ deep-dive `routing` (tổng hợp, không phải port 1 mechanism): prose-handoff cho interface agent↔agent trong chain, data/exit-code cho interface chạm consumer không-chắc-là-agent; chọn theo audience của từng interface. **E2→E3 sau scan CE 2026-07-17**: CE hội tụ độc lập về "judge centrally, fan out only fixes" (ce-resolve-pr-feedback) — cùng luật beehive critical-rule-13 (fan-out-keep-decide) từ hướng PR-review, "being invoked is NOT authorization" (authority không tự nới). Ba nguồn cùng khung routing-theo-audience + decide-altitude-không-delegate |
| typed-memory-consolidation | marketing-cockpit:four-memory-types + marketing-cockpit:procedural-memory-reinforcement | candidate | R3 E2 F3 | — | higher layer (process/framework/skill) của forgent | — | Từ deep-dive `state`: 4 loại memory + consolidation loop + quên có trọng số bằng chứng (±0.1/−0.2). Thứ beehive/harness/symphony đều KHÔNG có. ~~CÓ ĐIỀU KIỆN~~ **Điều kiện resolve 2026-07-13 (user)**: kiến trúc 2 tầng đồng thời — lower layer (state/FSM/cursor/changeset) giữ 2-physics, higher layer dùng 4 mem-type; hai mô hình phân tầng, không phủ định. Port khi dựng higher layer (sau Phase 1-2 của compound stack); retention math tự hiệu chỉnh, không bê nguyên số fgOS |
| durability-tier-ladder | symphony:run-artifact-durability-split | candidate | R2 E2 F1 | — | doc durability của forgent (khi có branch/PR flow) | — | Từ deep-dive `state`: "chạy xong ≠ đã merge ≠ đã bền" thành N mức bền tường minh, chống bug coi run thành công là đã-xong. Rẻ để ghi vào doc từ đầu; chỉ đáng khi forgent có fleet-run / branch-PR flow thật. E2: sinh từ ràng buộc thật của consumer đứng ngoài (symphony) |
| anti-loop-recovery-matrix | marketing-cockpit:failure-recovery-matrix | candidate | R3 E2 F2 | — | reliability layer cho autonomous loop của forgent | — | Từ re-dive `routing` (4-nguồn): anti-loop (max_skill_visits/max_chain_depth) + circuit-breaker per-service + quality-decay escalation + default-fail-block. Chạm đúng `/loop`/autonomous mode forgent đã có — trần cứng chống runaway, rẻ hơn debug loop cháy. **Nâng cấp 2026-07-13: forgent chốt tiến tới multi-agent fan-out → đây thành TIỀN-ĐIỀU-KIỆN của fan-out, không được mở song song trước khi có anti-loop/circuit-breaker.** E2: sinh từ ràng buộc fleet-không-người-trông của fgOS |
| cognitive-tier-model-decoupling | marketing-cockpit:executor-registry-cognitive-tier | candidate | R2 E2 F2 | — | delegation/model policy layer forgent | — | Từ re-dive `routing`: task khai cognitive_tier (lightweight/standard/analytical/critical), một map tier→model đổi model cả hệ + silent downgrade. Bản trưởng thành của cost-tiered-delegation forgent đã dùng (haiku/frontier) — tách policy khỏi task-site, sạch hơn hardcode model mỗi chỗ |
| intent-scoring-agent-dispatch | marketing-cockpit:three-level-intent-routing | candidate | R2 E1 F2 | — | routing layer nếu forgent lên multi-agent | — | Từ re-dive `routing`: dispatch nhiều-agent qua token-scoring (specificity_first) + fewer-active-tasks load-balance, tất định + có test case. **Điều kiện đã resolve 2026-07-13: forgent CHỐT tiến tới multi-agent** → không còn "maybe", là hướng đã định; port khi tầng multi-agent thực sự dựng (vẫn chain tuyến tính hôm nay nên chưa ngay). E1: một nguồn, chưa outcome |
| signal-driven-chaining | marketing-cockpit:signal-driven-chaining | candidate | R2 E1 F3 | — | workflow chaining nếu forgent cần reactive/fan-out | — | Từ re-dive `routing`: pub-sub signal chaining (catalog 14+, ttl/debounce) + loop-detection dispatch_chain max-depth 5. **Điều kiện đã resolve 2026-07-13: forgent CHỐT tiến tới reactive fan-out** → hướng đã định; port khi dựng tầng reactive (F3 subsystem, chưa ngay). E1: một nguồn |
| run-artifact-durability-split | symphony:run-artifact-durability-split | candidate | R2 E2 F1 | — | knowledge/memory layer forgent | — | Phân artifact thành 5 mức bền (branch/PR · commit-retain · local-compactable · rebuildable · local-only) + "successful run ≠ merged change"; E2: symphony dogfood như shipping product, mở rộng beehive:state-vs-log-two-physics; F1 doc/convention |
| standalone-product-boundary | symphony:typed-runtime-boundary + symphony:product-boundary-non-goals | candidate | R3 E2 F2 | — | định vị forgent như platform mà agent app tiêu thụ | — | Product tách khỏi engine chỉ qua versioned protocol (capabilities = behavioral promises) + non-goals tường minh; E2: symphony là ca thực + hợp orchestration-protocol-v1; deep-dive candidate nếu forgent muốn tách sub-product |
| agent-agnostic-adapter-projection | marketing-cockpit:agent-agnostic-adapter-spec + beehive:dual-runtime-contract | candidate | R3 E2 F3 | — | multi-runtime của forgent (nếu chạy ngoài Claude Code) | — | **E2 hội tụ**: fgOS (1 core → N nền qua adapter 4-required+6-optional) + beehive (2 belt) độc lập cùng "core neutral + adapter mỏng"; F3 subsystem. Deep-dive candidate với standalone-product-boundary: hai kiểu tách khác nhau (multi-runtime vs producer/consumer) |
| four-memory-types | marketing-cockpit:four-memory-types | candidate | R2 E2 F2 | — | knowledge/memory layer forgent | — | working/episodic/semantic/procedural + consolidation + importance-forgetting; mở rộng beehive:state-vs-log (2→4 loại); E2: fgOS dogfood, mô hình nhận thức có cơ sở |
| crossfamily-llm-judge | marketing-cockpit:crossfamily-llm-judge | candidate | R2 E2 F2 | — | eval harness forgent | — | Cross-family judge (model không tự chấm) + rubric weighted + baseline regression; E2: fgOS + hợp gene adversarial-verify của beehive; đáng khi forgent cần eval chất lượng output agent |
| default-fail-review-protocol | marketing-cockpit:rigor-scaled-evaluation | candidate | R2 E2 F1 | — | review/quality layer forgent | — | Reviewer giả định 3–5 lỗi tồn tại đến khi chứng minh sạch (Rune) + 3-tier eval theo cost + rigor mapping; E2: fgOS + hội tụ evidence-before-claims của beehive; F1 doc/convention |
| porting-outcome-lifecycle | repository-harness:proposal-lifecycle-explicit + beehive:friction-backlog-outcome-loop | candidate | R2 E3 F1 | — | vòng predicted→actual của chính distill (porting-log + `outcome`/`check`) | — | Từ deep-dive `compound-engineering`: nâng `/distill outcome` thành lifecycle đầy đủ — `check` nhắc row ported chưa có Outcome (như harness backlog-without-outcome), reconsideration khi delta mang evidence mới cho row rejected. **E3 hội tụ độc lập** beehive+harness về explicit-per-item + harness dogfood đo confirmed/ineffective/reverted. F1: convention + nhắc trong `check`, distill đã có mầm `outcome`. BỎ proposal_key SHA-256/SQLite (quá nặng cho markdown) |
| distillery-entropy-trend | beehive:entropy-score-trend | candidate | R2 E2 F1 | — | mở rộng `check` thành health-score cho learning area | — | Từ deep-dive `compound-engineering`: weighted score (unsealed ×8, stale deep-dive ×5, candidate-without-outcome ×2, backfill-pending ×5, broken Where ×10) + trend so audit trước → debt của hệ học đo được. Dogfood beehive lên chính distillery. E2: beehive có công thức + trend, đã dogfood. F1: `check` đã đi qua các invariant này |
| seal-digest-zero-effort | beehive:evolving-loop-two-gates | candidate | R1 E2 F1 | — | `seal` emit digest compounding | — | Từ deep-dive `compound-engineering`: `seal` (bước bắt buộc cuối phiên) emit 1 dòng "compounded: +N entry/±M changed/+K candidate" → chuỗi digest = harness học được gì theo thời gian, zero-effort. Lấy pattern digest-on-close của bee-evolving. E2: beehive dogfood. F1: một dòng append tại seal |
| doctrine-placement-rule | beehive:doctrine-layer-always-loaded | candidate | R3 E2 F1 | — | luật cho always-loaded layer của forgent (CLAUDE.md/AGENTS.md + Phase 0) | — | Placement test một câu ("cần hold khi không stage nào chạy?") + anchor-suite chống doctrine tự rỗng + "transport rides with the order" (lệnh trên standing sheet mang kèm mức tối thiểu để tuân thủ). R3: cross-cutting mọi doc luật của forgent; E2: beehive failure-driven, 2 bài học critical-patterns từ lỗi thật; F1: convention + test nhỏ. Áp thẳng vào Phase 0 khi viết luật nền |
| gate-beads-event-driven | beads:gate-beads-event-driven | candidate | R2 E1 F2 | — | async gate layer của forgent (Phase 2-3 compound stack) | — | Gate/message/event vật hóa thành node đồ thị (gh:pr/gh:run/timer/bead/human) — cùng bài toán async-gate với fgOS signal nhưng bằng graph node query được; E1: một nguồn, dogfood nội bộ |
| discovered-from-lineage | beads:discovered-from-lineage | candidate | R2 E2 F1 | — | capture layer của compound stack forgent | — | "Làm A lòi ra B" là edge có ngữ nghĩa thay vì ghi chú tự do; E2: beads production + cùng họ beehive capture-queue/harness friction-backlog (ba cách cùng một nhu cầu); F1: một dep-type + convention |
| llm-tier-compaction | beads:llm-tier-compaction | candidate | R2 E2 F2 | — | forgetting của higher-layer memory forgent | — | Quên = nén bằng LLM rẻ + archive snapshot trước destructive update (phục hồi được) — thay thế/bổ sung TTL-vứt của fgOS; E2: beads production + cost-tiering hội tụ với memory-decay của fgOS; đi cặp typed-memory-consolidation |
| hash-id-adaptive-length | beads:hash-id-adaptive-length | candidate | R1 E2 F1 | — | ID scheme cho task/work unit forgent khi multi-agent tạo việc song song | — | SHA256→base36 3–8 ký tự adaptive theo ngưỡng collision 25% + namespace per-contributor; E2: beads production + COLLISION_MATH có toán; R1: một component nhưng chặn cả lớp bug trùng ID |
| same-checkout-multi-session-coordination | beehive:cross-session-atomic-claims + beehive:lane-scoped-pipelines | candidate | R3 E2 F3 | — | coordination substrate cho multi-agent fan-out của forgent | — | **Từ delta scan beehive 2026-07-14 (fsh-1..13)**: nhiều phiên tranh cùng một checkout được phân xử bằng primitive cơ học — atomic claim (O_EXCL + TTL/heartbeat, reclaim đòi CẢ TTL-hết LẪN heartbeat-cũ chống cướp-nhầm) + lane per-feature (mỗi feature phase/gate riêng, zero-lane byte-parity) + cross-session hold deny (độc lập phase). Kèm mô hình **pull** (`claim-next`: worker rảnh tự kéo unit approved của lane mình trước, cross-lane chỉ khi gate đã duyệt) — đối lại các row PUSH đã có (`intent-scoring-agent-dispatch`, `signal-driven-chaining`). **Đây là tầng nền forgent CHỐT cần cho multi-agent song song (2026-07-13); đi CẶP `anti-loop-recovery-matrix` (tiền-điều-kiện) — không mở fan-out trước khi có cả hai.** R3 cross-cutting; E2: một nguồn nhưng beehive dogfood thật (chính `.bee/` xưởng này chạy nó); F3 subsystem (claims lib + lane store + hold enforcement interlock). Deep-dive candidate: pull-vs-push và biên "coordination cơ học vs quy ước" |
| chain-integrity-tail-guard | beehive:chain-integrity-guard-tail | candidate | R2 E2 F2 | — | đóng-feature-trung-thực của forgent (nối AGENTS.md critical-rule 12) | — | **Từ delta scan beehive 2026-07-14 (ci-1..3)**: đuôi chain canh tại cửa bằng máy — chốt phải là **producer thật** của trạng thái (chỉ `scribing-run` sinh `compounding`, không set-được-tay), spec-debt tường tại cửa close, waiver loud+logged. Gốc: post-mortem một phiên **giả 7 lần close** bằng hand-edit phase. Tổng quát hóa luật "hook là lưới an toàn, không phải người gác" mà forgent đã ghi (critical-rule 12) thành cơ chế: close phải do máy chứng, không do agent tự khai. E2: sinh từ sự cố thật (dogfood/measured failure); R2 workflow close-path; F2 phase-transition guard |
| explainable-work-ranking | beads-viewer-rust:transparent-triage-scoring | candidate | R3 E2 F2 | — | tầng chọn-việc của work-item lifecycle forgent | — | **Từ scan beads-viewer-rust 2026-07-15**: điểm impact 8-thành-phần MINH BẠCH (graph 42% + vận-hành 58%, trọng-số-tổng-1.0, 5 preset) — TẦNG TRÊN "ready nhị phân" của beads: không chỉ "được làm chưa" mà "nên làm cái nào TRƯỚC + vì sao". R3 cross-cutting cho lifecycle bán-tự-động (cần LÝ DO chọn việc); E2: bvr dogfood trên đồ thị beads thật + là mảnh bổ-khuyết của hội-tụ-×5 next-work-derived-query; F2 scoring component. **Deep-dive candidate**: trọng số nào hợp fgOS + preset gắn ngữ cảnh |
| impact-first-fanout-selection | beads-viewer-rust:greedy-topk-unblock-and-whatif | candidate | R2 E1 F2 | — | bộ chọn việc cho reactive fan-out của forgent | — | Greedy submodular chọn k việc mở-khóa-nhiều-nhất downstream + what-if (mô phỏng nếu-làm-X: unblock trực tiếp/bắc cầu, ngày tiết kiệm, cycle phá) + tách connected-component thành track song song. Đúng hình fan-out reactive fgOS. R2 subsystem chọn-việc; E1 một nguồn chưa có outcome; F2. **Đi cặp** same-checkout-multi-session-coordination + anti-loop-recovery-matrix — chọn-việc-để-fan-ra vô nghĩa nếu chưa có coordination + anti-loop |
| agent-output-envelope-contract | beads-viewer-rust:robot-mode-envelope + beads:agent-first-cli-contract + symphony:typed-runtime-boundary | candidate | R3 E3 F2 | — | hợp đồng output CLI↔agent của forgent | — | **E3 hội tụ độc lập ĐA nguồn**: bvr robot-envelope (`data_hash` + schema-validated bằng test) + beads `--json` bắt-buộc/cấm-interactive + symphony typed boundary + harness protocol-v1 — bốn nguồn cùng đến "agent có đường máy-đọc hạng nhất, KHÔNG chạm UI người". Cái mới bvr mang: `data_hash` (agent biết dữ liệu đổi chưa không cần diff) + envelope đóng-băng-bằng-test (12+ lệnh, ordering-invariant JSON). R3 cross-cutting; F2. **Deep-dive ĐÃ chạy 2026-07-15** (`deep-dives/work-item-schema-and-io-contracts.md`): thêm verbatim RobotEnvelope + compute_data_hash, br exit-code taxonomy 9 mã + tự-mô-tả 3 tầng, hn luật "mutation timeout = unknown outcome → rediscover trước retry" — synthesis fgos.v1 envelope đã đề xuất trong dive |
| agent-mail-coordination | beads-viewer-rust:mcp-agent-mail-coordination | candidate | R3 E1 F3 | — | substrate điều phối multi-agent (trường phái thứ tư) forgent | — | Điều phối bằng MESSAGE-PASSING (MCP Agent Mail + `.agent-mail.yaml`) — trường phái thứ tư cạnh reservation/claim (beehive), store-level (beads Dolt), typed-boundary (symphony). Liên quan trực tiếp hướng multi-agent parallel + reactive fan-out của fgOS. R3 cross-cutting; E1 một nguồn, cơ chế mới, chưa có outcome đo được; F3 subsystem (transport + protocol). **Đối chiếu** same-checkout-multi-session-coordination: mail vs lock — human cân nhắc kiến trúc nào hợp fan-out fgOS |
| reference-oracle-conformance | beads-viewer-rust:conformance-against-reference + beads-viewer-rust:evidence-gated-parity-ledger | candidate | R2 E2 F2 | — | chứng-minh-parity khi domain-extension kế thừa base-workflow forgent | — | Port/rewrite chứng-minh-tương-đương: reference CLI làm oracle, so JSON bất-biến-thứ-tự + typed schema (không diff dòng); parity ledger "complete chỉ khi conformance chống lưng" + gap-doc chống over-claim. R2 testing discipline; E2: bvr production + hội tụ với harness CI-release-contract + beehive infra-contract-tests; F2. Hợp lúc fgOS có base-workflow + domain-extension (coding/marketing/HR/finance) cần chứng minh kế thừa đúng |
| work-graph-drift-gate | beads-viewer-rust:graph-drift-detection | candidate | R2 E1 F2 | — | theo dõi sức-khỏe đồ-thị-việc của forgent | — | So work-graph hiện tại vs baseline snapshot trên 6 chiều ngưỡng-tùy-chỉnh (cycle mới = CRITICAL cứng; density/blocked/actionable/ranking-shift), exit-code-có-nghĩa (`--robot-drift` không baseline → khác 0). R2 subsystem giám sát; E1 một nguồn chưa có outcome; F2. Dùng lại được cho bất kỳ hệ ready-queue/đồ-thị-việc nào |
| predicted-actual-feedback-store | beads-viewer-rust:commit-bead-correlation-with-feedback + beads-viewer-rust:suggestion-feedback-loop | candidate | R2 E2 F2 | — | vòng tự-chỉnh của compound-learning forgent | — | Gợi ý/tương-quan gắn confidence → người confirm/reject → tính accuracy_rate để hiệu chỉnh ngưỡng. Cùng khung predicted→actual với distill outcome-loop + fgOS compound-learning (learnings/predicted-actual). R2; E2 hội tụ với outcome-loop đã có của xưởng; F2. Đáng học: gợi ý luôn CAP + kèm confidence, không xả vô hạn |
| destructive-action-guardrail-as-test | beads-viewer-rust:agent-guardrail-doctrine | candidate | R2 E2 F1 | — | tầng guardrail huỷ-diệt của forgent (nối AGENTS.md guardrails) | — | "NO FILE DELETION" + "never break glass" (thao tác không-hoàn-tác) ở tầng agent-instruction, **biến guardrail thành TEST chạy được** (đi bộ src bắt version-literal hard-code; forbid unsafe). Hội tụ tinh thần với privacy/hard-gate/verify-before-destroy của beehive nhưng nhắm đường HUỶ-DIỆT + kỹ thuật "guard = test" đáng port. R2; E2: bvr sinh từ agent-usage thật + cùng hướng beehive guardrails; F1 convention + guard test |
| bvr-viewer-frontend-stack | beads-viewer-rust:offline-wasm-viewer-with-fallback-ladder + beads-viewer-rust:compile-time-asset-embedding | rejected | — | — | — | — | Toàn bộ tầng viewer web (sql.js/WASM + OPFS cache + force-graph/charts + pages wizard + sqlite chunking + service-worker COEP/COOP) là product-của-một-web-viewer, KHÔNG hợp fgOS (một harness/agent-OS, không phải app xem đồ thị). **Giá trị học đã giữ trong index** dưới dạng *mẫu* (fallback-ladder + reason-field + DIAGNOSTICS state + deterministic-export), không phải bản thân code. Reject để tránh đánh giá lại; nếu fgOS về sau có mặt-web thì mở lại đúng *mẫu*, không port UI |
| sync-safety-blast-radius | beads-rust:sync-safety-blast-radius | candidate | R3 E3 F2 | — | tầng an-toàn thao-tác-huỷ-diệt của forgent (nối AGENTS.md guardrails + repo-divorce) | — | **Từ scan beads-rust 2026-07-15**: sync sinh từ sự cố THẬT (Go bd sync xoá cả cây source) → path allowlist chỉ-ghi-trong-`.beads/` + invariant "không gọi git" biến thành **grep CI test-được** + **strace witness** chứng blast-radius bằng syscall thật + tombstone/conflict-guard KHÔNG-override-được. **E3 hội tụ độc lập falsified-by-data** với beehive:allowlist-not-redaction (cùng bài "chặn bằng allowlist-cho-phép, sinh từ mất-dữ-liệu-thật"); br thêm chiều cơ-học-kiểm-được (grep + strace). R3 cross-cutting safety; F2 (allowlist + witness harness). Áp thẳng thao tác điểm-không-quay-đầu forgent |
| rollout-ladder-design-before-code | beads-rust:rollout-ladder-design-before-code | candidate | R3 E2 F1 | — | kỷ luật thêm tầng adaptive/cache/fan-out của forgent | — | Ship pure types cho tới khi proof gate qua: write-combining là "design artifact only" (zero runtime wiring) + **rollout ladder 5 nấc** (direct-only→shadow→advisory→opt-in→default-on CHỈ sau perf-bundle+parity) + fallback-đường-thẳng bắt buộc + evidence triad (golden+resource+repro). Bản trưởng thành beehive:validate-before-execute cho tối-ưu. **Đi cặp** anti-loop-recovery-matrix + same-checkout-multi-session-coordination — khung "không bật tối ưu/fan-out chưa chứng minh". R3 cross-cutting engineering discipline; E2: br dogfood + hội tụ validate-before-execute của beehive; F1 convention/doc |
| agent-surface-golden-snapshots | beads-rust:agent-baseline-golden-snapshots + beads-rust:snapshot-review-log-gate | candidate | R3 E2 F2 | — | regression cho bề mặt agent-facing của fgOS (platform-cho-agent) | — | Ghim CHÍNH output agent-facing (help/schema/JSON+TOON ví dụ/fresh-agent journey) diff với binary live — output-drift = API-break, fail CI; kèm snapshot-review-log-gate (mỗi delta phải log cause-class, R=regression-block, sinh từ sự cố PR#283 21 snapshot cũ). **Hội tụ** beehive:hook-contract-parity-tests (byte-drift) nhưng đẩy tới contract mà agent NGOÀI dựa vào. R3 cross-cutting cho fgOS-định-vị-platform-agent; E2: br dogfood + gene beehive byte-drift; F2 snapshot capture + test. "Fresh-agent journey" = acceptance test cho agent-UX |
| coordination-evidence-classifier | beads-rust:coordination-evidence-classifier | candidate | R3 E2 F2 | — | tầng advisory-evidence trên coordination cơ học của forgent | — | Phân-loại claim stale/abandoned THUẦN ĐỌC: KHÔNG auto-reclaim, "missing data = evidence, không phải proof", reclaim = audit-comment-TRƯỚC-rồi-claim, flight-recorder content-hashed. **Hội tụ độc lập** với beehive:cross-session-atomic-claims ở "reclaim đòi bằng chứng, không cướp-nhầm" — beehive enforce cơ học (O_EXCL+TTL+heartbeat), br cho tầng ADVISORY thuần-đọc còn thiếu (tách phân-loại pure khỏi hành-động human-gated). **Đi CẶP** same-checkout-multi-session-coordination — mảnh advisory bổ khuyết cho fan-out. R3; E2; F2 classifier + audit append |
| workspace-health-contract | beads-rust:workspace-health-contract | candidate | R2 E2 F2 | — | reliability layer + baseline-gate của forgent | — | Một từ vựng health CHUNG 4 mức (healthy/degraded/recoverable/unsafe, composite=max-severity) mọi surface (startup/doctor/sync-status) dùng chung thay vì mỗi chỗ tự bịa error story; honesty "absence = not evaluated ≠ passed". Cùng gene glossary-driven-vocab (harness) cho HEALTH runtime + diễn đạt được beehive never-build-on-red bằng health-state. R2 subsystem reliability; E2: br dogfood + hội tụ với health-check của xưởng; F2 enum + classifier |
| isomorphism-refactor-ledger | beads-rust:isomorphism-refactor-ledger | candidate | R2 E2 F2 | — | self-modification/grooming discipline của forgent | — | Refactor agent-driven gated proof-đẳng-cấu: score `(LOC×Conf)/Risk` + **equivalence-contract 10 trục** per candidate + **red-baseline-repair-first** (sửa baseline đỏ trước = Order 0) + honor cross-agent reservation (reject ghi lý do, không im lặng bỏ). Cùng luật never-build-on-red của beehive áp cho SELF-MODIFICATION; 10-trục contract chống "refactor đổi hành vi ngầm" đáng bê thẳng. R2; E2: br dogfood + gene beehive:evolving-loop/grooming; F2 card template + ledger convention |
| reliability-gate-suite | beads-rust:reliability-gate-suite | candidate | R2 E2 F1 | — | release engineering của forgent | — | Gate reliability có tên (failure-replay/sync-injection/concurrency-interleave/stress) là DEPENDENCY của release build + emergency-override đòi WRITTEN-REASON (bypass để lại dấu). Cùng gene proof-before-tag (harness) + baseline-gate (beehive) nhưng gate = dependency-của-artifact, không phải khuyến nghị. R2 release path; E2: br dogfood + hội tụ proof-before-tag/baseline-gate; F1 CI wiring + convention |
| default-off-one-time-skill | beads-rust:default-off-migration-skill | candidate | R2 E1 F1 | — | chính sách bundle skill của beehive/forgent | — | Skill dùng-MỘT-LẦN (migration) cố ý LOẠI khỏi install mặc định (opt-in `--with-migration-skill`) vì "một khi đã migrate, skill chỉ pollute context; là công cụ một-lần, không phải bề-mặt-thường-trực". Phán-đoán lifecycle sắc: phân biệt skill-thường-trực vs skill-một-lần, đừng để công cụ đã-xong-việc ăn context mãi — áp thẳng vào beehive skill bundling. R2; E1 một nguồn; F1 install-policy convention |
| agent-context-inherited-governance | beads-rust:agent-first-cli-contract-hardened (field agent_context #297) | candidate | R3 E1 F2 | — | cơ chế context-theo-việc của forgent (đối chiếu read_first của cell) | — | **Từ deep-dive work-item-schema 2026-07-15**: field governance JSON trên bead tổ tiên, PHÁT KÈM lúc descendant được claim/show — constraint đến đúng thời điểm cần, sống trong store nên miễn nhiễm context-compaction/cold-start; không hiện trong list/search (governance ≠ browsable). R3 chạm mọi dispatch; E1 một nguồn (br #297) chưa outcome; F2. Câu hỏi mở: push-lúc-claim (br) vs pull-tự-đọc (beehive read_first) — cần feature thật làm bàn thử |
| two-layer-edge-taxonomy | beads:ready-work-ten-dep-types (enum 19 loại @HEAD, chưng 2 lớp) | candidate | R2 E2 F1 | — | edge schema cho work-graph forgent | — | **Từ deep-dive work-item-schema 2026-07-15**: luật chưng từ bd — lớp blocking ĐÓNG-và-BÉ (blocks/parent-child/waits-for) là lớp duy nhất ready-predicate nhìn; lớp semantic MỞ (discovered-from/caused-by/supersedes/…) chở tri thức, thêm loại mới KHÔNG BAO GIỜ đổi điều phối. E2: bd production (19 loại, Decision 004 dồn cả entity-quan-hệ vào edge) + hn tách bảng dependency cùng tinh thần; F1 convention + enum |
| schema-per-artifact-boundary | fgos-audit:schema-per-artifact-boundary (harness-architecture-audit@2026-07-14) | candidate | R3 E3 F2 | — | schema versioned cho event/view/cell tại cửa store forgent | — | **Từ deep-dive fgos-harness-architecture 2026-07-15**: mỗi artifact chia sẻ = 1 JSON Schema + envelope `v`, validate tại cửa store, TRƯỚC khi có reader thứ hai (runtime khác/viewer/agent ngoài). E3 hội tụ: audit falsified-by-data (fgOS trả P0-2/P0-4/P1-12 vì thiếu đúng thứ này) + trùng decision đã ghi của forgent (envelope versioned). F2 schema files + validate hook. Đòn bẩy #2 của deep-dive |
| doctor-invariant-in-verify | fgos-audit:doctor-enforced-invariants (harness-architecture-audit@2026-07-14) | candidate | R3 E3 F1 | — | verify gate = "doctor" của forgent | — | **Từ deep-dive fgos-harness-architecture 2026-07-15**: mỗi bất biến kiến trúc = một check máy-đọc trong verify gate; nguyên tắc fgOS "ADR chưa accepted khi doctor chưa check nó". Check đầu: chỉ transitionWork ghi status/edge; chỉ store.mjs ghi .fgos/; một-writer-mỗi-field. E3 hội tụ: doctor fgOS (signal bus sạch VÌ SC5) + distill-check/cap-requires-proof của beehive. F1 thêm grep/check vào chuỗi verify. Đòn bẩy #1 — rẻ nhất, lớn nhất |
| add-through-not-alongside | fgos-audit:add-through-not-alongside (harness-architecture-audit@2026-07-14) | candidate | R3 E2 F1 | — | doctrine + red-flag trong AGENTS forgent | — | **Từ deep-dive fgos-harness-architecture 2026-07-15**: bệnh gốc vibe-code của fgOS = "thêm cạnh thay vì đi qua" (2 dispatch stack, 2 model-resolver, 3 signal catalog). Doctrine: capability mới đi XUYÊN store/FSM sẵn có, cấm path song song; thành red-flag tường minh. E2: một nguồn mạnh (audit đo được) + forgent đã dogfood (awaiting-human tái dùng moveWork+FSM, 0 event-path mới). F1 convention/red-flag |
| fsm-single-status-writer | fgos-audit:fsm-single-status-writer (harness-architecture-audit@2026-07-14) | candidate | R2 E3 F1 | — | field-ownership check cho work-state | — | **Từ deep-dive fgos-harness-architecture 2026-07-15**: đúng một writer mỗi field; status/edge chỉ qua FSM, ép bằng grep-check. Thảm họa fgOS: status bị 5 writer đụng + engine bypass FSM (P0-1/P1-1). forgent single-store đã chặn — giữ khi thêm field (outcomes/gates/…) bằng bảng owner + check; fold ở replay là single choke-point. E3 falsified-by-data; F1. Con của doctor-invariant-in-verify |
| multi-target-converter-engine | compound-engineering-plugin:multi-target-converter-engine | candidate | R3 E2 F3 | — | tầng phân phối đa-runtime của forgent (nối dual-runtime hiện tại) | — | **Từ scan compound-engineering-plugin 2026-07-17**: engine parse→convert→write dịch một plugin (skill/agent/command/hook/MCP) ra 7+ platform qua converter/writer registry + tool/permission/hook/model map tường minh mỗi target + install-manifest ownership. Bản CHẠY ĐƯỢC built-out nhất của "one brain, N belts" — beehive mới project 2 belt (.codex/.agents) qua catalog, fgOS mới có spec adapter (marketing-cockpit:agent-agnostic-adapter-spec). R3 cross-cutting nếu forgent đa-platform; E2: CE production + hội tụ với beehive dual-runtime + fgOS adapter-spec (ba nguồn cùng bài); F3 subsystem. YAGNI tới khi forgent thật sự cần belt thứ 3+, nhưng là bản đồ kiến trúc đầy đủ khi tới |
| learning-refresh-gardening | compound-engineering-plugin:learning-refresh-as-gardening + compound-engineering-plugin:grounding-validation-claims | candidate | R3 E2 F1 | — | tầng compound-learning của forgent (bee-compounding + docs/history/learnings) | — | **Từ scan CE 2026-07-17**: audit learning-SET định kỳ Keep/Update/Consolidate/Replace/Delete với retrieval-value test ("two docs on same ground drift apart — prefer consolidation") + grounding-validate claim (path/SHA/link, quote-source) trước khi doc thành trusted. THỨ KHÔNG NGUỒN NÀO KHÁC CÓ: bee-compounding chỉ APPEND critical-patterns; đây là GARDEN chống drift/trùng/lỗi-thời — đúng bệnh mọi memory layer mắc. R3 chạm chính hạ tầng học của xưởng; E2: CE production + đối chiếu bee-compounding thiếu đúng nhánh này; F1 skill + convention. Ưu tiên cao: cải thiện chính cơ chế distillery+beehive dùng để học |
| grounding-validation-claims | compound-engineering-plugin:grounding-validation-claims | candidate | R2 E2 F1 | — | mechanical claim-check cho specs/learnings/distillery-index của forgent | — | **Từ scan CE 2026-07-17**: check cơ học path tồn tại / SHA hợp lệ / link resolve trước khi doc thành trusted knowledge (+ optional semantic validator quote-source). Cùng gene "assertion ≠ evidence" của beehive:cap-requires-proof nhưng cho TRI THỨC. Chạm thẳng lỗi distill `check` đang bắt (Where-path lệch âm thầm) — port nhẹ nhất, giá trị rõ nhất; F1 script + verify hook. E2: CE production + distill check đã chứng cần |
| mode-dispatch-triad | compound-engineering-plugin:mode-dispatch-triad | candidate | R3 E2 F1 | — | contract mode chung cho skill forgent (nâng headless flag hiện tại) | — | **Từ scan CE 2026-07-17**: MỌI skill CE khai 3 mode nhất quán — interactive (hỏi/report) / headless (đánh dấu ambiguous stale, không hang) / pipeline (bounded loop, suppress block) + mode:return-to-caller trả envelope. forgent có headless như MỘT CỜ rời; CE nâng thành CONTRACT ba-mode là điều kiện để skill COMPOSE (lfg gọi ce-work return-to-caller). R3 chạm mọi skill; E2: CE production + beehive/distill đã có headless một phần; F1 convention. Hợp lúc forgent muốn skill gọi skill sạch |
| signal-scaled-review-roster | compound-engineering-plugin:reviewer-persona-selection + compound-engineering-plugin:small-diff-fast-path | candidate | R2 E2 F2 | — | bee-reviewing của forgent (roster động thay panel cố định) | — | **Từ scan CE 2026-07-17**: bật reviewer persona theo SIGNAL trong diff (always-on correctness/testing/maintainability/standards + conditional security-khi-auth, perf-khi-hot-path…) + small-diff-fast-path fail-closed (≤39 exec line → lite roster; mọi uncertainty → full). bee-reviewing dùng panel cố định; CE scale roster theo bề mặt rủi ro thực = "ceremony scales" áp cho review. E2: CE production + hội tụ beehive:risk-lanes ("ceremony scales, memory never"); F2 persona catalog + gate. Personas-as-prompt-asset (không expose Agent) là mô hình đáng đối chiếu |
| install-manifest-ownership | compound-engineering-plugin:install-manifest-ownership + compound-engineering-plugin:managed-root-escape-guard | candidate | R2 E2 F2 | — | installer/onboarding của forgent nếu ship artifact vào project chủ | — | **Từ scan CE 2026-07-17**: track file tool-owned vs user bằng `install-manifest.json` per-plugin + cleanup xóa stale nhưng GIỮ symlink/file user (fork) + ancestor-symlink escape guard (store bị repoint → no-op) + legacy archive không xóa. Nâng beehive:managed-block-markers từ text-block lên file-tracking. E2: CE production + hội tụ beehive managed-markers; F2 manifest + cleanup safety. Đáng khi forgent/beehive ghi nhiều file vào project chủ và cần phân biệt của-mình vs của-user |
| metric-driven-optimize-loop | compound-engineering-plugin:metric-driven-optimize-loop | candidate | R2 E1 F3 | — | tầng eval/tối-ưu output agent của forgent | — | **Từ scan CE 2026-07-17**: vòng tối-ưu experiment hard-metric HOẶC LLM-as-judge + hypothesis backlog + degenerate gate + disk checkpoint CP-0..5 write-then-read-back. THỨ KHÔNG NGUỒN NÀO KHÁC CÓ; LLM-judge mode hợp khi forgent cần chấm chất lượng output agent không có metric cứng (đối chiếu marketing-cockpit:crossfamily-llm-judge nhưng đây là optimize-loop, không chỉ eval điểm). R2 subsystem; E1 một nguồn chưa outcome ở ta; F3 framework. Chưa cấp bách, giữ cho khi forgent có surface cần tune định lượng |
| concept-teaching-explainer | compound-engineering-plugin:concept-teaching-artifact | candidate | R2 E1 F2 | — | nhánh learning hướng-NGƯỜI của forgent (bổ sung critical-patterns hướng-agent) | — | **Từ scan CE 2026-07-17**: ce-explain tạo artifact dạy-học durable (predict-then-reveal cho diff, hard-order check-in) + PR concept-teaching gate archive `docs/explainers/`. Learning layer forgent hiện hướng AGENT (critical-patterns); CE thêm nhánh hướng NGƯỜI — dạy-học-như-side-effect-của-ship. Operational-gate: không phải mọi thứ đáng dạy. R2; E1 một nguồn; F2 skill + template. Đáng khi forgent có người-học ngoài agent (onboarding, teammate) |
| cross-model-peer-review | compound-engineering-plugin:cross-model-peer-pass | candidate | R2 E1 F2 | — | review/eval layer của forgent nếu lên multi-model | — | **Từ scan CE 2026-07-17**: finding rủi-ro-cao (Tier 2/3) được peer review bằng MODEL KHÁC (cross-model adversarial/panel, consent + recipient validation) — một model không tự bắt lỗi mình. Cùng gene chống self-bias với marketing-cockpit:crossfamily-llm-judge nhưng áp cho code/doc/POV review. R2; E1 một nguồn (CE) + họ hàng fgOS cross-family; F2. Điều kiện: forgent phải có ≥2 model family sẵn để làm peer thật |
| feedback-source-sweep | compound-engineering-plugin:feedback-source-sweep | candidate | R2 E1 F2 | — | intake feedback ngoài→work của forgent (nối work-item lifecycle) | — | **Từ scan CE 2026-07-17**: ce-sweep quét source feedback (Slack/GH Issues) → ack tại nguồn → verify fix đã merge → emit plan; state engine single-writer-lease (OS advisory lock, stale reclaim) + fix-verification gate (chỉ đóng khi verify merge tới default branch) + circuit breaker. Mini work-queue có kỷ luật state; cùng họ "durable state, single door" với repository-harness:story-status-single-door. R2; E1 một nguồn; F2. Hợp lifecycle bán-tự-động của fgOS khi feedback ngoài thành nguồn việc |
| native-agent-empty-wait-discipline | beehive:native-codex-wait-discipline | candidate | R3 E2 F1 | — | tầng chống-loop của orchestrator fan-out forgent (nối AGENTS critical rules) | — | **Từ delta scan beehive 2026-07-18 (codex-agent-wait-loop)**: `wait_agent` rỗng của native agent CHỈ là chờ rỗng (im lặng≠thất bại); cấm 2 wait liên tiếp; trước bounded-wait sau phải làm việc thật HOẶC 1 list_agents + 1 commentary; timeout giữ nguyên mọi agent/claim/reservation. **Là TIỀN-ĐIỀU-KIỆN chống-loop của fan-out** — forgent đã ghi memory "anti-loop là precondition của fan-out"; ĐI CẶP `anti-loop-recovery-matrix`. R3 chạm mọi dispatch fan-out; E2 beehive dogfood + RED/GREEN pressure-test 3 kịch bản; F1 doctrine/convention |
| capability-enforced-readonly-fanout | beehive:read-only-analyst-fanout | candidate | R3 E2 F1 | — | an-toàn fan-out của forgent (gather-worker không mutate) | — | **Từ delta scan beehive 2026-07-18 (compounding-fanout-hardening)**: analyst gom-tin spawn với BỀ MẶT NĂNG LỰC read-only (capability LÀ tường, không phải câu dặn — sự cố thật: analyst được dặn "no writes" vẫn commit source), pin Explore; re-dispatch đúng 1 lần; synthesis từ PARTIAL returns không chờ đủ N. Cùng gene "hook là lưới, không phải người gác" áp cho fan-out. ĐI CẶP `same-checkout-multi-session-coordination` + `impact-first-fanout-selection`. R3 fan-out cross-cutting; E2 beehive dogfood + failure đo được; F1 convention + capability wiring |
| computed-wave-schedule-cycle-safe | beehive:computed-parallel-schedule | candidate | R2 E2 F2 | — | bộ tính sóng-việc cho fan-out reactive forgent | — | **Từ delta scan beehive 2026-07-18 (parallel-scheduler)**: `computeSchedule` (Kahn layering + greedy overlap packing, deterministic) + `detectCycles` (Tarjan) + cycle refusal all-or-nothing tại MỌI dep-mutating write (chỉ chặn cycle write này tạo/tham gia), schedule DERIVED-never-stored. Hội tụ với bvr `impact-first-fanout-selection` (chọn-việc) + harness runnable-predicate — beehive cho tầng LỊCH cycle-safe. ĐI CẶP impact-first-fanout-selection (chọn k) + native-agent-empty-wait (chờ). R2 subsystem; E2 beehive dogfood + hội tụ; F2 module thuần + wiring |
| worktree-isolation-axis | beehive:independent-feature-worktrees | candidate | R2 E2 F3 | — | trục điều phối #2 (cô-lập-cây) cho multi-agent forgent | — | **Từ delta scan beehive 2026-07-18 (worktree-parallelism/isolation)**: cô lập git worktree per-feature thay vì khóa-trong-checkout — typed 3-valued resolution + validate 2 chiều git, grant read MAIN-store-only (worktree không tự-cấp), bootstrap store độc-lập gate/phase, merge-back giao dịch verify-xanh + pre/post SHA, log union-merge. ĐỐI CHIẾU `same-checkout-multi-session-coordination` (trục khóa) + symphony isolated-runner — human chọn khóa-cây HAY cô-lập-cây cho fan-out fgOS. R2 subsystem; E2 beehive dogfood + họ worktree symphony/harness; F3 subsystem (store+resolve+grant+merge-back). **Phạm vi hẹp `.fgos/`-trong-`fgw/<id>` đã chốt riêng, KHÔNG chọn 1 trong 2 vế câu hỏi này** — xem `docs/decisions/0020-chan-fgos-khoi-worktree-worker.md` (chặn-cây: không symlink, không copy). Trục rộng (đa-agent tách nhánh tính năng song song) vẫn `candidate`, chưa chốt |
| product-root-repo-divorce-topology | beehive:product-root-repo-divorce | candidate | R2 E2 F1 | — | topology xưởng+repo-lồng của forgent (D4 repo-divorce) | — | **Từ delta scan beehive 2026-07-18 (prt-1)**: config `product_root` cho `.bee/` ngồi TRÊN product repo nested, resolve docs sản-phẩm tách khỏi history/state harness. forgent ĐANG chạy đúng topology này (workshop root + `./repo` độc lập) nhưng bằng quy ước — beehive có config-key + resolution cơ học. R2 subsystem layout; E2 beehive dogfood + forgent tự dùng chính topology; F1 config-key + resolution nhỏ. Áp thẳng để chính-thức-hóa D4 repo-divorce của forgent |
| leveled-autopilot-floors | beehive:gate-bypass-safety-floor | candidate | R2 E2 F1 | — | autopilot-có-mức cho lifecycle bán-tự-động forgent (human-gates conditional theo stage) | — | **Từ delta scan beehive 2026-07-18 (harness-hardening/stop-net)**: bypass 4 mức off/normal/full/total (`normal` giữ floor high-risk; `full`/`total` gỡ có chủ đích) + zero-stops CƠ-HỌC-HÓA tại Stop hook (matrix + loop-guard + fail-open, exploring/Gate1/PreCompact không bao giờ mechanized). Đúng memory forgent "human-gates conditional by stage" — mức bypass = trục điều-chỉnh-autonomy test-được. R2 autonomy subsystem; E2 beehive dogfood + stop-net fixture test; F1 config levels + hook logic |
| cross-project-perf-measurement | beehive:performance-log-cross-project-matrix | candidate | R2 E2 F2 | — | tầng đo cost/hiệu-năng chéo-dự-án của forgent (self-improvement) | — | **Từ delta scan beehive 2026-07-18 (perf-log)**: `performance.jsonl` toàn-máy (XDG) schema `bee-perf/v1` — active-time (không wall-clock), token new/cached/total per-model dedupe-requestId, subagent cost ATTRIBUTED riêng, matrix gom theo folder + cache%, HTML self-contained, fail-open. Cho forgent bằng-chứng-ĐO-ĐƯỢC cho kỷ luật cost-tier fan-out (đối chiếu cost-tiered-delegation memory). R2 subsystem đo lường; E2 beehive dogfood; F2 component perf.mjs |
| single-registry-version-manifest | beehive:release-tuple-manifest-integrity | candidate | R2 E2 F1 | — | packaging/release integrity của forgent nếu ship artifact | — | **Từ delta scan beehive 2026-07-18 (codex-harness-hardening/release)**: version sống ở MỘT registry `COMPONENTS`, `bump_version` một-lệnh + preflight desync refusal; release manifest sha256+mode+role/file ENUMERATE (không hand-list), self-test tự mutate để chứng so-sánh cắn, wired vào verify. Hội tụ repository-harness proof-before-tag + prebuilt-binary-sha256 (dạng Node zero-dep). R2 release path; E2 beehive dogfood + hội tụ harness; F1 scripts + convention |
| skill-instruction-enforcement-doctrine | superpowers:iron-law-discipline-template + superpowers:positive-instruction-doctrine | candidate | R3 E2 F2 | — | how forgent's own skill/hook prose is authored (fgOS skills, CLAUDE.md rules) | — | **From superpowers full scan 2026-07-28.** A measured (not just asserted) doctrine for when prohibition-style enforcement works vs backfires: rule-skipping-under-pressure failures need the 3-part Iron-Law form (imperative + rationalization table + red flags); wrong-shaped-output failures need a positive recipe instead — prohibitions there are measured to score *worse* than no guidance at all (micro-test: 4.4 vs 3.6 baseline vs 3.0 for the recipe). Directly applicable to every fgOS skill/hook that currently uses "don't do X" phrasing without checking which failure mode it targets. R3: cross-cutting, touches how every skill in the repo is worded. E2: single source but backed by real micro-test measurement (not just prose assertion). F2: a review-and-reword pass across existing skills, not a new subsystem |
| model-tier-omission-silent-default | superpowers:model-selection-warning + superpowers:sdd-strict-cost-experiment-ladder | candidate | R2 E2 F2 | — | dispatch layer of any forgent multi-agent orchestration (cost-tiered-delegation) | — | **From superpowers full scan 2026-07-28.** Two converging findings: (1) an omitted `model:` field on a subagent dispatch silently inherits the *session's* model rather than erroring, defeating any cost-tier policy without a signal; (2) measured proof this matters — a cheap-controller config caused a per-task quality gate to "collapse into plan-compliance advocacy" (planted defect shipped 4/5 runs) and cheap reviewers "cleanly flagged 0 of 10 planted defects." Relevant directly to this repo's own `cost-tiered-delegation` memory/convention — worth a hard-fail-on-missing-tier check rather than a silent default. R2: dispatch-layer subsystem; E2: measured with planted-defect eval, not just asserted; F2: a validation check on the dispatch-payload builder |
| tool-description-overrides-skill-instructions | superpowers:tool-description-overrides-skill-instructions | candidate | R3 E1 F1 | — | any fgOS skill/rule that competes with a harness-native tool's own description (e.g. `EnterWorktree`) | — | **From superpowers full scan 2026-07-28.** A documented failure mode with a cited Claude Code issue (#29950): a native tool's own description ("ONLY when user explicitly asks") silently overrides skill/CLAUDE.md instructions telling the agent to prefer that tool proactively — vague prose loses to the tool description every time. Fix pattern: name the exact tool/flag explicitly rather than describing the decision abstractly, turning it into a factual lookup. Cheap, broadly applicable check for any forgent skill that tries to steer usage of a native harness tool. R3: applies to any skill-vs-native-tool conflict, a recurring category; E1: single source, no local outcome measured yet; F1: a wording check, not a subsystem |
| micro-test-harness-for-instruction-wording | superpowers:micro-test-harness-for-instruction-wording | candidate | R2 E2 F1 | — | how forgent iterates on skill/rule wording before committing to it | — | **From superpowers full scan 2026-07-28.** A named, reusable methodology: one API call per phrasing variant (system prompt = the guidance variant, user prompt = a realistic scenario), ~$0.15-0.30/sample, used to decide instruction wording BEFORE paying for a full behavioral eval — explicitly documented catching a case where the cheap test correctly said "don't ship this change." Directly reusable as a script for tuning fgOS skill/rule prose cheaply instead of guessing. R2: a technique applicable wherever forgent authors enforcement prose; E2: source measured it against real wording changes, not just proposed it; F1: a small standalone script + convention |
| worktree-native-tool-detect-and-defer | superpowers:using-git-worktrees-skill + superpowers:tool-description-overrides-skill-instructions | candidate | R2 E2 F2 | — | any fgOS skill that creates/manages git worktrees | — | **From superpowers full scan 2026-07-28.** Detection order: check `GIT_DIR != GIT_COMMON` for existing isolation (with an explicit submodule guard via `git rev-parse --show-superproject-working-tree`, since submodules trip the same check) → prefer the harness's own native worktree tool → fall back to manual `git worktree add` only if no native tool exists, with an explicit warning that the fallback "creates phantom state your harness can't see or manage." Directly relevant since this session's own toolset includes a native `EnterWorktree`/`ExitWorktree` pair — forgent skills that touch worktrees should defer to it the same way, not hand-roll `git worktree` calls. R2: a specific subsystem (worktree handling) shared across multiple fgOS skills; E2: source ran 3 REFACTOR iterations to 50/50 pass rate on this exact detection order, real validated result; F2: a shared detection routine, not trivial but not a subsystem either |
| typed-word-confirmation-for-destructive-actions | superpowers:typed-word-confirmation-for-destructive-actions | candidate | R2 E2 F1 | — | any fgOS skill/verb that deletes state irreversibly (branch discard, worktree removal, backlog item deletion) | — | **From superpowers full scan 2026-07-28.** Requiring the exact typed word (`discard`, not "yes"/"ok"/a paraphrase) before an irreversible action proceeds closes the same wording-workaround loophole this project's own `verification-before-completion`-style checks target — a model (or a human skimming) satisfying the letter via different words. Cheap to add to any fgOS verb that currently accepts a loose confirmation. R2: applies wherever forgent has a destructive verb; E2: paired with a matching convention (`verification-before-completion`'s ban on paraphrased success claims) in the same source, i.e. cross-validated within-source; F1: a wording/gate check, not a subsystem |
| deletion-gated-by-subagent-verification | superpowers:deletion-gated-by-subagent-verification | candidate | R2 E1 F1 | — | test/doc retirement process when new coverage is believed to subsume old (e.g. retiring a superseded fgOS test or doc) | — | **From superpowers full scan 2026-07-28.** Before deleting an old test/doc believed superseded by new coverage, dispatch a subagent with both the old assertions and the new coverage, produce a per-assertion PASS/FAIL table, and quote the verdict in the deletion commit message as an audit trail — makes "new coverage subsumes old" an evidenced claim instead of an assumption. R2: applies to test/doc retirement broadly; E1: single source, not yet dogfooded locally; F1: a review-dispatch step + commit-message convention |
| spy-stub-binary-on-path-testing | superpowers:spy-stub-binary-on-path-testing | candidate | R1 E2 F1 | — | testing wrapper scripts (shell/CLI) that shell out to external tools | — | **From superpowers full scan 2026-07-28.** Put a fake executable on `PATH` that only logs its invocation args (or answers one narrow call and fails loudly otherwise) to test a wrapper script's argument construction/control flow without depending on the real tool's behavior — used for `shellcheck`/`shfmt`/`gh`/`npm` across the source's own test suite. Directly reusable whenever forgent tests a script that shells out (e.g. any CLI wrapper around `git`/`gh`/`node`). R1: narrow, test-infrastructure-only technique; E2: used repeatedly and consistently across the source's own suite, not a one-off; F1: a test-authoring pattern, no subsystem |
| epoch-fence-merge-gate | repository-harness:epoch-fence-merge-gate | rejected | R2 E1 F2 | — | merge-back coordination layer for concurrent worktree sessions in forgent | — | **From worktree-in-out consult 2026-07-28.** CAS state machine for race where 2 sessions approve→commit main at same instant: gate merge-back on epoch/fence check, fail cleanly when stale. **Superseded by `docs/decisions/0021-wire-main-checkout-hook-qua-doctor-setup.md` (2026-07-28)**: tsk-3w8's actual root cause (`.git/index` clobbering) already had a working, tested solution (`.githooks/pre-commit` + `main-checkout-lock.mjs`, str65) just not wired by default — wiring it into `fgos doctor`/`fgos setup` closed the reachability gap without a new subsystem. A mutex-based hook is sufficient for the observed single-writer-at-a-time race; this CAS design stays rejected unless real evidence of a genuine multi-writer-in-parallel need surfaces. R2: merge-back subsystem; E1: harness source, no local outcome yet; F2: state machine + hook wiring |
| changeset-committed-truth-db-rebuild | repository-harness:changeset-committed-truth-db-rebuild | rejected | — | — | — | — | **From worktree-in-out consult 2026-07-28.** Duplicate of existing `changeset-event-sourcing` row (already `planned`). Folded as củng cố note into that row. No new evidence |
| durable-red-marker-no-auto-retry | beehive:staged-verify-gate-durable-red-marker | candidate | R1 E1 F1 | — | merge-back failure handling in forgent worktree workflow | — | **From worktree-in-out consult 2026-07-28 (beehive v1.3.9→v1.18.3).** When `--no-ff --no-commit` verify fails: abort, leave main byte-untouched, write durable marker, DO NOT auto-retry — human/orchestrator decides next step. Anti-loop principle at merge boundary. R1: single boundary (merge-back red path); E1: beehive dogfood, not measured locally; F1: convention + marker file |
| worktree-ownership-cleanup-ordering | superpowers:worktree-ownership-cleanup-ordering | candidate | R1 E2 F1 | — | worktree lifecycle hygiene in forgent multi-agent fan-out | — | **From worktree-in-out consult 2026-07-28.** Path-based ownership rule: `.worktrees/` = self-managed cleanup, `.claude/worktrees/` = harness-managed. Fixed order: merge→verify→remove-worktree→delete-branch (never delete branch before worktree removed). Closes tsk-1os (reclaimOrphanedCheckout force-remove living checkout). R1: narrow (cleanup path); E2: superpowers dogfood + 3-iteration validated; F1: convention + ordering check |
| tool-description-override-consent-bridge | superpowers:tool-description-override-consent-bridge | candidate | R2 E2 F1 | — | skill-vs-native-tool conflict resolution pattern in forgent skill prose | — | **From worktree-in-out consult 2026-07-28.** When native tool description contains "ONLY when user explicitly asks" (e.g. EnterWorktree), skill prose saying "prefer worktree isolation" loses — vague intent < tool description. Fix: name exact tool/flag explicitly in skill, turn decision into factual lookup. Second cause of tsk-424 (EnterWorktree blocked on second call). R2: affects all skills that steer native tool usage; E2: superpowers + cited Claude Code issue #29950; F1: wording review pass |
| decision-schema-rationale-alternatives-source | beehive:context-md-source-of-truth + beehive:structured-decision-recall-surface | candidate | R3 E2 F2 | — | src/state/store.mjs addDecision + gates[id] fold (src/state/replay.mjs:166-172), tsk-63c/tsk-6b6 | — | **From fgos-capture-gaps-vs-bee deep-dive 2026-07-29 (tsk-ma4 audit follow-up).** beehive's live `.bee/decisions.jsonl` (701 real entries, checked directly, not just the curated index) already has `rationale` (required), `alternatives` (populated 64/701 times with real rejected-options-and-why), and free-text `source` (15 distinct real values incl. a person's name) — exactly what fgOS's audit found missing in `decision`/`gates[id]`. Directly ports as a schema extension to `addDecision` + the `gates[id]` fold, superset of tsk-63c's own STR70a `role` ask. R3: touches the one capture surface all 4 gap items reference; E2: beehive dogfood at scale, real production data; F2: extend two existing functions + fold sites, no new subsystem |
| mandatory-context-md-gate-and-unattended-lock-or-park | beehive:phased-chain-with-four-gates + beehive:unattended-triage-front-door + beehive:single-writer-context-md-render | candidate | R3 E2 F3 | — | fgOS decompose stage edge precondition (RUL50-style), tsk-47e | — | **From fgos-capture-gaps-vs-bee deep-dive 2026-07-29.** beehive's Gate 1 makes CONTEXT.md creation structurally mandatory before a phase can advance; `bee-qualifying` guarantees the same outcome even fully unattended ("locks or parks, never neither"), funneled through one single-writer skill (`bee-context-locking`). fgOS has the mechanism (`fgos-coding-exploring`) but no engine-level check that it ran. R3: changes a stage-edge precondition, cross-cutting; E2: beehive dogfoods this across 18 skills in production; F3: subsystem-level — needs its own `fgos-coding-exploring` round first (tsk-47e already filed) before any code |
| judge-verdict-always-decision-logged | beehive:unattended-triage-front-door | candidate | R2 E2 F1 | — | src/intake/plan.mjs judgeDecompose's 4 verdict branches, tsk-6b6 | — | **From fgos-capture-gaps-vs-bee deep-dive 2026-07-29.** No direct beehive field to copy — beehive has no decompose-shaped step — but the principle transfers: `bee-qualifying` never lets an automated judgment advance a phase while writing nothing. `judgeDecompose`'s `pass-through`/`decompose`-with-children branches currently call `moveStage` only (confirmed live: this session's own tsk-ma4 decompose→executing transition wrote zero decision trace). Once `decision-schema-rationale-alternatives-source` lands, each verdict branch should call `addDecision` with `source: 'judgeDecompose'` and a derived `rationale`. R2: one subsystem (decompose.mjs); E2: beehive's principle dogfooded at scale even though no literal field exists to port; F1: small once the schema idea above lands |
| worktree-dispatch-attestation | beehive:worktree-protected-attestation + beehive:independent-feature-worktrees | candidate | R2 E2 F2 | — | chèn quanh `resolveExecutorConfig` (decision 0026 cli/spawn path, `capacities.<id>`) cho nửa (a); `src/runner/frozen-judge.mjs` mở rộng cho nửa (b) | — | **From deep-dive parallel-decomposition-and-merge (2026-08-05).** Hai nửa của cùng một máy trong beehive: (a) chụp `baseCommit`/`headRef`/`declaredPaths` bởi CHÍNH orchestrator TRƯỚC dispatch, không bao giờ nhận từ worker; (b) sau khi worker trả, so diff thực với footprint khai — lệch thì gắn cờ, không tự fail (cùng tinh thần advisory của STR63 frozen-judge). fgOS đã có nửa (b) nhưng THU HẸP chỉ còn file dạng test/CI/lockfile/manifest (`FROZEN_JUDGE_PATTERNS`); mở rộng thành gác diff-toàn-phần quan trọng hơn khi executor là agy/opencode — tiến trình ngoài hoàn toàn (luôn cli/spawn per decision 0026 quy tắc 3), kém tin hơn cả subagent Claude cùng nhà mà chính beehive gọi là "cooperative and fallible, not a security principal". Nửa (a) chưa có — hiện chỉ kiểm danh tính worktree (`isMainWorktree`) TẠI MERGE, không chụp SỚM; điểm chèn tự nhiên là ngay quanh lời gọi `resolveExecutorConfig` (một adapter chuẩn hoá duy nhất, per decision 0026 — không cần biết adapter spawn agy/opencode cụ thể ra sao). R2: một component (dispatch-prep + frozen-judge), không đổi kiến trúc; E2: beehive hardened qua nhiều vòng, dogfood; F2: mở rộng module đã có (`normalizePath`/Set matching tái dùng được), không phải máy mới |
| computed-parallel-wave-schedule | beehive:computed-parallel-schedule | candidate | R2 E2 F2 | — | src/state/graph-metrics.mjs (hàm mới cạnh footprintOverlap) + verb đọc-only (vd `fgos schedule`) | — | **From deep-dive parallel-decomposition-and-merge (2026-08-05).** beehive tính SÓNG chạy song song được bằng Kahn layering trên `open`/`claimed`, cộng Tarjan cycle-detection tại cửa ghi deps — derived-never-stored, "cell chồng file dời sang wave sau thay vì bị từ chối". fgOS đã có phát-hiện-chồng (`footprintOverlap`/`footprintConflicts`) nhưng dừng ở BÁO CẶP xung đột, chưa XẾP THÀNH SÓNG thực thi. Giá trị thật chỉ hiện ra khi có ≥2 executor (agy/opencode) chạy thật song song — xem Open Questions trong deep-dive trước khi ưu tiên, tránh lặp lỗi YAGNI-gắn-nhầm của `worktree-merge-semantic-gate`. R2: một subsystem (state/graph query), không chạm execution; E2: beehive dogfood (94b4a31), một nguồn; F2: hàm thuần mới tái dùng `frontier()`/`footprintOverlap` đã có + một verb đọc-only |
| dispatch-tier-judged-at-dispatch | beehive:three-tier-model-rubric-with-pinned-agent-types + beehive:fan-out-cost-tiering-rubric | candidate | R3 E2 F2 | — | src/runner/dispatch.mjs (chỗ `modelForTier(cfg, work.tier)`) + nơi soạn gói dispatch | — | **From deep-dive parallel-decomposition-and-merge, cập nhật 2026-08-06.** beehive phán model tier TẠI LÚC dispatch bằng chính orchestrator, và *"never fixed at planning — a planning `tier` is at most an overridable hint"* (decision 0016), có ghi lại lựa chọn để đo độ khan hiếm của tier đắt (`cells tier`), cộng feedback ngược từ áp lực tài nguyên (*"if `bee_status` flags ceiling scarcity, re-judge routine cells downward"*). fgOS hôm nay KHÔNG có bước phán này: `src/runner/dispatch.mjs:1053-1054` suy model thẳng từ `work.tier` — mà `work.tier` (`light`/`standard`/`heavy`) là một field mang HAI NGHĨA: `gate-bypass.mjs`'s `isTierCovered` cũng đọc chính nó như lượng nghi thức quy trình. **Đính chính 2026-08-06 (reality check tsk-503):** chiều "field nào là kẻ đến sau" ngược với bản ghi đầu -- `src/state/work.mjs:136-144` khai `work.tier` là *the cost/cognitive weight a work item self-declares; the runner maps tier -> model via a config table at dispatch time*, PROVISIONAL, kèm cảnh báo *Do not let the two drift apart*. Vậy tier->model là nghĩa GỐC và dispatch làm đúng schema; nghi-thức là nghĩa mượn về sau, nên bên cần field riêng nhiều khả năng là gate-bypass. Giá trị thật hiện rõ khi có gói mệnh lệnh soạn động (`tsk-2t6` D3): gói động không có `capacities.<id>` nào giữ model/provider, nên thiếu bước phán tier per-dispatch thì nó buộc rơi về một backend mặc định. R3: chạm tầng dispatch + tách nghĩa một field đang được hai hệ đọc; E2: beehive production 1.18.3, có hook `bee-model-guard` cắn thật; F2: thêm bước phán + chỗ ghi lựa chọn, không phải máy mới |
| decision-citation-and-reversal-sweep | beehive:decision-citation-and-reversal-sweep | candidate | R3 E2 F3 | — | quy ước trích dẫn ADR/RUL/D-local + `docs/decisions/0000-index.md` supersede-trỏ-ngược hiện tại (tsk-37i) | — | **Từ scoped delta scan beehive v2.7.0 (2026-08-17, tsk-37i — người dùng phản ánh trích dẫn D-ID/RUL-ID/ADR trần, không self-contained).** beehive cite quyết định bằng `short8` (hash toàn cục) thay vì số nguyên nhỏ đơn thuần; khi supersede, hệ thống TỰ quét `docs/**` tìm mọi nơi đã trích id cũ, bắt sửa hoặc waive-có-lý-do NGAY trong cùng lượt trước khi ghi supersede — khác hẳn cách fgOS hiện tại (chỉ kỷ luật văn xuôi, `0000-index.md` dòng 30-36, không cơ chế bắt buộc). Index (`docs/decisions/index.md`) máy-regenerate, không tay-sửa, "complete by construction" — so với `0000-index.md` hiện tại của fgOS là tay-viết. R3: chạm toàn bộ tầng trích dẫn quyết định của platform; E2: một nguồn nhưng dogfood thật (store audit tìm ra 70 decide event giấu supersession trong prose, đã fix bằng guard); F3: cần short8 id scheme + sweep logic + relation-required guard — subsystem, không phải script nhỏ |
| one-line-cite-plus-local-delta | beehive:one-line-cite-plus-local-delta | candidate | R3 E2 F2 | — | quy ước trích D-local/RUL ngoài nhà gốc trong skills/specs (vd `.agents/skills/fgos-coding-shaping/SKILL.md` đang trích trần D2/D4/D6 — vi phạm quyết định `0017`) | — | **Từ scoped delta scan beehive v2.7.0 (2026-08-17, tsk-37i).** "One rule, one home": một luật chỉ phát biểu đầy đủ đúng 1 lần ở nhà gốc; mọi nơi khác trích nó BẮT BUỘC kèm 1 dòng tóm tắt + phần khác biệt cục bộ, không bao giờ chỉ id trần — trả lời trực tiếp ví dụ vi phạm người dùng nêu (`0026, 0028-0031, 0033` không tóm tắt). Đi kèm mechanical pointer-integrity check (`pointer_integrity.rs`, chạy mọi lần verify, có negative-control fixture) xác nhận mọi trích dẫn trỏ đúng file+heading thật — bắt được 3 pointer gãy thật ngay lần đầu chạy dù các file đó trước giờ vẫn "pass" mọi check khác. Chia rõ ranh giới: cấu trúc trích dẫn (trỏ đúng chỗ không) máy kiểm được → build check; nội dung gloss (đúng/đủ không) chỉ người phán được → giữ kỷ luật văn xuôi + review. R3: chạm toàn bộ skills/specs/CONTEXT.md; E2: dogfood thật, bắt lỗi thật ngay lần chạy đầu; F2: convention + 1 script kiểm cấu trúc (rẻ hơn hẳn candidate trên vì không cần đổi id scheme) |
| doc-rot-close-gate-bundle | beehive:doc-rot-close-gate-bundle | candidate | R3 E2 F3 | — | `fgos return`/`fgos approve` close path — điểm thêm hard-door tự nhiên trước khi item chuyển `done` | — | **Từ version-by-version trace v1.18.3→v2.7.0 (2026-08-17, tsk-37i round 6, người dùng yêu cầu dò lại từng phiên bản).** Không có ở cursor v1.18.3 round 2 — landed 2026-08-03→2026-08-16, `git log --grep` targeted, không replay hết 1213 commit. Trả lời đúng nửa CÒN THIẾU của `decision-citation-and-reversal-sweep` (round 2): entry đó chặn "quyết định bị SUPERSEDE mà chỗ trích cũ không sửa"; entry này chặn "quyết định LOCKED cục bộ (D-local) rồi KHÔNG BAO GIỜ được route đi đâu cả" — đúng tình huống thật của fgOS đang có (`fgos-coding-shaping/SKILL.md` trích D2/D4/D6 chỉ tồn tại trong CONTEXT.md gốc, không bao giờ route vào spec/ADR nào). 4 cửa cứng: knowledge-freshness (dangling pointer trong touched area), impact (doc còn trích quyết định của feature đang đóng mà chưa reconcile), routing (D-ID locked không có area-spec citation lẫn feature-local record), doc-deferral (prose kiểu "để sau" không kèm trigger đăng ký) — mỗi cửa có escape hatch riêng, logged, không bao giờ silent-skip. R3: chạm điểm đóng item toàn platform; E2: dogfood thật qua 2 feature riêng (doc-impact-synthesis, knowledge-distill-trigger), cell trace thật; F3: subsystem — cần sweep-tại-close + parser D-ID table + trigger registry hai tầng, không phải script nhỏ |
| agents-rule-of-two-plus-injection-defenses | claudekit-engineer:agents-rule-of-two + claudekit-engineer:six-architectural-injection-defenses + claudekit-engineer:untrusted-content-execution-boundary | candidate | R3 E2 F1 | — | skill-authoring checklist (a section in AGENTS.md or a new `docs/specs/` note) + a self-audit of skills that fetch external content (this session's own `distill`/`xia` skills, any skill that fetches URLs) | — | **From claudekit-engineer full-repo distill scan (2026-08-25, ed8a1fa7).** Two research docs (`docs/research/architectural-defense-prompt-injection.md`, `docs/research/preventing-prompt-injection-in-skills.md`) plus one live enforcement point (`xia`'s own untrusted-content rule) converge on the same boundary: fetched/external content is data only, never an instruction, and a skill should satisfy at most 2 of {processes untrusted input, accesses sensitive data, changes state} (Meta's "Agents Rule of Two"). Directly actionable for fgOS: `distill` itself just cloned an external, untrusted repo (this very session) and `xia` (when built) will too — worth an explicit self-check against the 3-clause litmus test and the 6 architectural patterns (Dual LLM, Plan-Then-Execute, etc.) before either skill is treated as hardened. R3: applies platform-wide to any current/future skill that fetches external content; E2: two independent research docs converge with a live enforcement point, and the research itself cites a peer-reviewed multi-org paper (>90% bypass rate on prompting-only defenses); F1: a checklist/doc pass + review of existing fetch-capable skills, no new subsystem |
| hard-gate-anti-rationalization-table | claudekit-engineer:dual-hard-gate-scout-and-requirements | candidate | R2 E1 F1 | — | `fgos-coding-exploring`/`fgos-coding-planning` skill prose — add a named-excuse table alongside the existing scout/requirements gates | — | **From claudekit-engineer full-repo distill scan (2026-08-25, ed8a1fa7).** `cook` and `fix` both pair their scout-first + exact-requirements hard gates with an explicit anti-rationalization table — naming the exact shortcut phrase an agent would use to skip the gate ("this is too simple to plan", "I can see the problem, let me fix it") next to a one-line rebuttal, rather than just stating the gate exists. fgOS already has comparable gates (`fgos-coding-exploring`, `fgos-coding-planning`) but not this specific technique — naming the rationalization in advance may close a self-persuasion loophole a generic gate description doesn't. R2: touches the exploring/planning skill prose, not a new subsystem; E1: one source, not yet dogfooded locally; F1: a documentation/prompt addition to existing skills, no code |
| skill-quality-eval-harness | claudekit-engineer:skill-eval-driven-authoring | candidate | R2 E1 F2 | — | a new tier-1-style static validator for `.claude/skills/*/SKILL.md`-equivalent files in this host's own skill surface, callable from `fgos doctor` or a CI check | — | **From claudekit-engineer full-repo distill scan (2026-08-25, ed8a1fa7).** 3-tier eval harness (`scripts/eval/`): tier1 static frontmatter/syntax validation ($0, CI-mandatory), tier2 e2e "activate and describe, don't execute" probe (~$3.85/run, `--diff` mode), tier3 LLM-judge clarity/specificity/completeness scoring (~$0.15/run, flags <6). fgOS's own skill catalog (`.agents/skills/`, `.claude/skills/`) currently has no equivalent systematic quality check — a skill's description/trigger reliability is unverified until it's actually invoked and misfires. R2: touches the skill-authoring/quality-gate area, not execution-critical paths; E1: one source, no local dogfood yet; F2: needs a real harness component (at minimum tier1), not a doc-only change |
| workflow-artifact-typed-handoff | claudekit-engineer:workflow-artifact-gate-bundle + claudekit-engineer:structured-review-artifact-schema | candidate | R3 E1 F2 | — | fgOS's own verify/gate boundary — e.g. what `fgos-coding-validating`/`fgos return` reads before advancing a stage | — | **From claudekit-engineer full-repo distill scan (2026-08-25, ed8a1fa7).** A 5-file JSON schema (context-snippets/risk-gate/verification/review-decision/adversarial-validation) with shared enums (`DECISIONS: PASS|PASS_WITH_RISK|BLOCKED`, `CONTRACT_STATUSES: OK|CHANGED|BROKEN|UNKNOWN`) that a producing skill and a gating hook both validate against, located via a 24h-recency pointer file. fgOS's own stage-advance gates (`fgos-coding-validating` etc.) already carry similar intent but the *typed enum contract between producer and gate* is more explicit here — worth comparing against fgOS's current verify-evidence shape to see if a shared enum vocabulary would sharpen ambiguous PASS/BLOCKED judgment calls. R3: touches the platform-wide stage-gate boundary; E1: one source, not yet compared against fgOS's actual verify output shape; F2: would extend an existing gate/schema, not build a new subsystem |
