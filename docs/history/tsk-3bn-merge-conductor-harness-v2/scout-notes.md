## Scout 1

**Command:** `rg -n "sync-root|fgos drift|driftDetect|drift-detection" --glob '!node_modules' -l | head -30`

```
This Bash command contains multiple operations. The following part requires approval: rtk rg -n "sync-root|fgos drift|driftDetect|drift-detection" --glob '!node_modules' -l
```

## Scout 2

**Command:** `rg -n "sync-root|drift" --glob '!node_modules' -l`

```
This command requires approval
```
