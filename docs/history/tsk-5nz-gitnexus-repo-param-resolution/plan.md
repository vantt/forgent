# Plan: tsk-5nz — gitnexus repo-param resolution guidance

Mode: tiny

0 of `fgos-routing`'s Mode-gate flags apply (no auth, no authorization, no
data model, no audit/security, no external-system interaction — the
change is prose only, it never calls GitNexus — no public contracts, no
cross-platform surface, no existing covered behavior, no weak proof, no
multi-domain reach). `fgos graph --json`: tsk-5nz is on no `criticalPath`
and not in `topUnblock` — isolated, no ordering to resolve against other
in-flight work.

## Approach

**Chosen path:** add one new paragraph to `CLAUDE.md`'s existing
"## Impact-analysis capability gate" section — the hand-maintained prose
above the "# GitNexus — Code Intelligence" heading, confirmed durable
(git-tracked, survives `gitnexus analyze` regeneration) by RESEARCH.md's
Round 1 evidence. The new paragraph states: on "Multiple repositories
indexed" or "Repository not found" from any gitnexus MCP tool
(`impact`/`context`/`explain`/`trace`/`pdg_query`), call `list_repos` and
match the exact registered `name` by `scanTarget`/absolute project path
before retrying — never reuse the error message's own display string
(confirmed invalid: `git list_repos` — actually the reproduction showed
`repo: "forgent (/home/vantt/projects/forgentX)"` fails with "Repository
not found") and never guess a bare short name (confirmed ambiguous: `repo:
"forgent"` silently resolved to *something* among 3+ repos sharing that
name on this machine, per this session's own reproduction quoted in
tsk-5nz's description).

**Alternatives rejected:**
- Editing `.claude/skills/gitnexus/*/SKILL.md` directly — rejected per
  RESEARCH.md Round 1: those files are untracked/gitignored/regenerated by
  `gitnexus analyze`, so an edit there would not survive the next reindex
  and would never reach another machine or CI.
- Editing the "# GitNexus — Code Intelligence" block inside `CLAUDE.md`
  itself (the `Always Do`/`Never Do`/`Resources`/`CLI` table) — rejected:
  that block's own inline comment states it "regenerates from GitNexus's
  own template on `gitnexus analyze`," same durability problem as above.
- Filing this upstream against the `gitnexus` npm package instead —
  rejected as the *only* action: the ambiguous-resolution behavior of
  `list_repos`/`impact`'s `repo` param may well be worth an upstream
  report too, but that is out of this item's scope (no access to file
  issues against an external package from here) and does not by itself
  stop a forgentX agent from repeating the same wrong guesses today. The
  in-repo prose fix is the piece within this item's control.

**Risk map:** none of the entries below carry medium/high risk, so no
proof point is deferred to `fgos-coding-validating` beyond the pass-through
verify below.

| Component | How risky | What would prove it |
|---|---|---|
| `CLAUDE.md` prose edit | low — text-only, no code path touched, additive (new paragraph inside an existing section, no existing text removed or reworded) | `grep` confirms the new text landed in the file (see Verify) |

**Files touched:** `CLAUDE.md` only, one paragraph inserted inside the
existing "## Impact-analysis capability gate" section (before the "This
gate is prose the agent reads..." closing sentence of that section, so the
new paragraph reads as part of the gate policy, not appended after the
regenerated-block boundary marker).

**Impact-analysis posture:** not invoked — this proof point does not lean
on blast-radius evidence (no symbol, function, or code path is being
changed; the fix target IS the prose describing how to use the tool
correctly, not code that calls it).

## Shape

Single piece, no split. `plan.md` (this file) plus a one-paragraph
`CLAUDE.md` edit is the entire scope — no further design needed for a
`tiny`-lane, single-file prose addition.

Concrete case this addition must cover (already reproduced live in
tsk-5nz's own description, cited above): the exact three-step failure
sequence — ambiguous-repos error → display-string retry fails →
bare-short-name retry "succeeds" ambiguously — must no longer happen to an
agent that reads this paragraph first, because it names the correct first
call (`list_repos`) and the correct matching key (`scanTarget`/absolute
path) instead of leaving the agent to guess.

## Verify (pass-through, syncing onto the item)

```
grep -q "list_repos" CLAUDE.md && grep -q "Multiple repositories indexed" CLAUDE.md
```

Already the item's current `verify` (set by `fgos discover --verdict
clear` at the discovery stage) — not a placeholder, so no sync call is
needed here (the discovery-stage skill already wrote this exact command).

## Outstanding questions

None
