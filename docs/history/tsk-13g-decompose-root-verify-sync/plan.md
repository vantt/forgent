# Plan: tsk-13g — sync a real `verify` onto a split ROOT item itself

Mode: **standard** (2 flags per `fgos-routing`'s own Mode gate: "existing
covered behavior" — this touches the `planning` Step 4 split-decision path
and `fgos sync-root`'s own goal-check, both covered by the existing test
suite — and "weak proof around the area" — event-sourced gate/verify
semantics are subtle and already produced one real incident, tsk-1vc, cited
in this item's own description. No hard-gate flag applies, so not
high-risk.)

No `CONTEXT.md` exists for this item — its discovery verdict came back
`clear` (`docs/history/tsk-13g-decompose-root-verify-sync/RESEARCH.md`),
which skips `exploring` outright (D2). This plan's evidence base is that
RESEARCH.md instead of a locked-decisions table; there is no D-ID to cite.

## Approach

**Root cause (RESEARCH.md Round 1, finding 1), confirmed still current.**
`domains/coding/skills/fgos-coding-planning/references/verify-sync-and-gap.md:22-44`
("Sync a pass-through item's own `verify` field") is scoped explicitly to
"a *pass-through* (non-split ...) item only", and states "Split children
need no such step: the normalizer already forces a real verify onto each
one at creation time." Neither this section nor `SKILL.md`'s own Step 4
("Decide the split, if any") mentions the split **root** item itself. A
decompose root still needs a real `verify` — `fgos sync-root`'s own
`mergeRunnerItem` runs a goal-check against it — but nothing in the
planning flow ever sets one, so it silently keeps whatever placeholder
`discovery` left behind until someone tries to `sync-root` it (RESEARCH.md
finding 3, the tsk-1vc incident).

**Relationship to tsk-14a (RESEARCH.md finding 2), confirmed no overlap.**
tsk-14a (`status: retrospective`, already delivered) fixed the
pass-through half of this same file's gap. Its own plan.md explicitly
scopes split items OUT of its fix: "A **split** item — untouched by this
change; split children already get a real verify forced at creation time
... this item's own description already confirms is not the gap." tsk-13g
is the complementary, still-open third case that plan named but did not
fix: the split **root**, as distinct from its children.

**Canonical file to edit (confirmed, not guessed).**
`test/setup/skill-wrappers.test.mjs:236-273` ("Drift guard: committed
`.agents/skills` vs `core/skills+domains/*/skills`") proves `core/skills/`
+ `domains/<name>/skills/` are the canonical AUTHORING source that
`.agents/skills/` is rendered from (`npm run build:skills`); `.claude/skills/`
is in turn a generated thin wrapper of `.agents/skills/`
(`test/skills/fgos-mirror.test.mjs`), and `plugins/fgOS/skills/` is a
byte-identical mirror of `.agents/skills/` for the 14 `fgos-*` dev-skills
(same test, `PLUGIN_SKILLS_ROOT`). So the one file a person edits by hand
is `domains/coding/skills/fgos-coding-planning/references/verify-sync-and-gap.md`
(plus `SKILL.md`'s Step 4 one-line pointer, same file tree) — every other
copy (`.agents/skills/`, `.claude/skills/`, `plugins/fgOS/skills/`) is
regenerated by `npm run build:skills`, never hand-edited directly.

**Fix direction (this item's own description names two; picking one,
mirroring tsk-14a's own precedent — RESEARCH.md's "still open" note).**
tsk-14a's own rationale for a skill-prose fix over an engine change
(`tsk-14a-plan-verify-sync-gap/plan.md:64-72`) was: the engine's own
fallback (there, `resolvePlan`'s `planApproveVerify`; here,
`mergeRunnerItem`'s goal-check executing `work.verify` literally) is
correct AS a mechanism — the gap is that nothing populates a real value
before it runs. The same reasoning applies here unchanged: `sync-root`
executing whatever `work.verify` says is the existing, correct contract;
the gap is that Step 4 never populates the root's own `verify` before a
decompose write. **Chosen: mirror tsk-14a — a skill-prose fix in
`fgos-coding-planning`'s Step 4, not an engine (`src/`) refusal.** This
also keeps scope minimal (YAGNI): an engine-side placeholder-refusal on
every decompose-write is a broader, cross-cutting defense-in-depth that
would guard against any *future* skill regression too, not just this one
named gap — genuinely useful, but out of this item's own scope; flagged
below as an Outstanding-adjacent note, not silently folded in.

**What command to sync.** A split root has no "one piece" proof surface of
its own — its correctness is "does everything merged still pass". Mirror
`tsk-3ik`'s own informal precedent (`node --test 'test/**/*.test.mjs'`,
RESEARCH.md finding 4) — functionally the same command this repo's own
`npm test` script already runs
(`package.json:27`, plus the `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1` env
prefix already always used for a whole-suite regression). New rule: at
Step 4, if a split is real, check the root's own current `verify` against
the placeholder set the same way the pass-through section already does; if
still a placeholder, sync `npm test` (or the item's own more specific
whole-suite command if one already applies) onto it before finishing the
step — same shape, same call, as the existing pass-through section, one
new subsection down from it.

**Impact-analysis posture: full** — `gitnexus` registered and `present`
(`fgos tool query --capability impact-analysis --status present`). Not
applicable: this change touches only skill-prose files (`domains/coding/
skills/fgos-coding-planning/**`, regenerated downstream copies), no `src/`
symbol — no blast radius for GitNexus to measure, same conclusion tsk-14a
reached for its own analogous prose-only fix.

## Shape

One piece, pass-through (no split — the fix is a single, self-contained
prose addition to one skill file plus a one-line pointer, mirroring
tsk-14a's own shape exactly). File touched by hand:
`domains/coding/skills/fgos-coding-planning/SKILL.md` (Step 4's one-line
pointer, same style as Step 5's existing pointer) and
`domains/coding/skills/fgos-coding-planning/references/verify-sync-and-gap.md`
(new `## Sync a split root item's own `verify` field` subsection,
immediately after the existing pass-through subsection). Regenerated by
`npm run build:skills`: `.agents/skills/fgos-coding-planning/{SKILL.md,
references/verify-sync-and-gap.md}`, `.claude/skills/fgos-coding-planning/
{SKILL.md,references/verify-sync-and-gap.md}`, `plugins/fgOS/skills/
fgos-coding-planning/{SKILL.md,references/verify-sync-and-gap.md}`.

New subsection content (Execute writes this, scaled to match the existing
pass-through section's own shape and tone):

> ## Sync a split root item's own `verify` field
>
> For the **root** item of a real split (Step 4's "several independently
> workable pieces" branch) only: once the split is decided, check the root
> item's own current `verify` (`fgos list --id <item-id> --json`'s
> `data.work[id].verify`) against the discovery-stage placeholder
> constants, the same way the pass-through check above does. If it still
> reads one of those placeholders, sync a whole-suite regression command
> onto the item's own current `verify` field before handing off to
> `fgos-coding-validating`:
>
> ```bash
> fgos edit "<item-id>" --verify "npm test"
> ```
>
> A root item has no single piece-specific proof surface of its own — its
> correctness is "does everything merged still pass" — so a whole-suite
> command is the honest choice here, unlike a pass-through item's own
> narrower, piece-specific command above. If the item already carries a
> real, distinct verify, do nothing — never overwrite a value already set
> deliberately. Without this sync, a decompose root's `verify` stays
> whatever placeholder `discovery` left behind, and `fgos sync-root`'s own
> goal-check later executes that placeholder literally as a shell command
> (the tsk-1vc incident this item's own description cites).

### Cases this needs to hold for

- A split root whose `verify` is still the discovery-stage placeholder
  (the reported failure mode, tsk-1vc) — gets synced to `npm test`.
- A split root whose `verify` is already real (e.g. `tsk-3ik`'s own
  informal `node --test 'test/**/*.test.mjs'`) — untouched, no redundant
  edit.
- A pass-through (non-split) item — untouched by this change; already
  covered by the existing pass-through section above it, which tsk-14a
  already fixed.
- A split **child** — untouched; already covered by the normalizer forcing
  a real verify at creation time (unchanged, not this item's gap).

## Outstanding questions

None. (Adjacent, out-of-scope note carried forward from Approach: an
engine-side refusal of a decompose-write when the root's own verify is
still a known placeholder would be a valuable defense-in-depth against a
*future* skill-prose regression of this same kind — deliberately left out
of this item's own scope per the "why a skill-prose fix, not an engine
change" reasoning above; a candidate for a follow-up item if this gap
recurs after this fix lands.)

## Verify

Per `docs/how-to/write-verify-for-a-skill-prose-change.md` (this item
touches a `domains/coding/skills/**`/`.agents/skills/**`/`.claude/skills/
**`/`plugins/fgOS/skills/**` path). Scope guard restricted to the one
canonical file pair plus its three regenerated copies and this item's own
`docs/history/` evidence:

```bash
npm test && \
grep -q "Sync a split root item" domains/coding/skills/fgos-coding-planning/references/verify-sync-and-gap.md && \
grep -q "Sync a split root item" .agents/skills/fgos-coding-planning/references/verify-sync-and-gap.md && \
! git diff --name-only main...HEAD | grep -qvE '^(domains/coding/skills/fgos-coding-planning/.*|\.agents/skills/fgos-coding-planning/.*|\.claude/skills/fgos-coding-planning/.*|plugins/fgOS/skills/fgos-coding-planning/.*|docs/history/tsk-13g-decompose-root-verify-sync/.*)$'
```

- `npm test` — regression floor for every other item shape, and (via
  `fgos-mirror.test.mjs`/the drift-guard test) proof the canonical file and
  its three regenerated copies are still in sync (i.e. `npm run
  build:skills` was actually run after the hand edit).
- POSITIVE — the new root-sync subsection exists in both the canonical
  (`domains/coding/skills/...`) file and the regenerated `.agents/skills/`
  copy.
- NEGATIVE/scope-guard — nothing outside the four `fgos-coding-planning`
  skill-copy trees and this item's own docs history changed.
