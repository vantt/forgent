Mode: tiny

## Approach

Throwaway `chore` item with `verify: "true"`. No real implementation is
needed — this item exists only to exercise the claim -> discover -> plan
-> validate -> implement -> return -> approve path end to end, as the D5
smoke-test required after editing fgos-routing/approve/pick. Nothing to
build, nothing to touch. One piece is honestly enough — no split.

## Outstanding questions

None
