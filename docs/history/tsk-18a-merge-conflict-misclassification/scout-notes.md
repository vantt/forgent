## Scout 1

**Command:** `rg "merge\.mjs|main-checkout\.lock" --type-list | head -5 && rg -l "merge\.mjs|main-checkout\.lock" --max-count=1`

```
This Bash command contains multiple operations. The following parts require approval: rtk rg "merge\.mjs|main-checkout\.lock" --type-list, head -5 && rtk rg -l "merge\.mjs|main-checkout\.lock" --max-count=1
```

## Scout 2

**Command:** `rg -l "merge\.mjs"`

```
This command requires approval
```
