Mode: tiny

## Approach

Throwaway `docs` item with `verify: "true"`. No real implementation is
needed — this item exists only to give Team Dispatch V1's Cell 6.3 a
real, low-risk `stage: planning` target so one real `planning.validate-plan`
reviewer Assignment can be dispatched through the actual pipeline (not a
fake executor), per
`docs/architect/agent-coordination/step-06-work-attached-team-adoption.md`
§7. Nothing to build, nothing to touch beyond this doc pair.

## Outstanding questions

None
