# Product Backlog

| ID | Story | CoS | Status | Feature |
|----|-------|-----|--------|---------|
| P49 | **Tách prompt worker thành file template riêng + registry agent-type** (thảo luận 2026-07-18): `buildPrompt` (dispatch.mjs) hiện hard-code chuỗi trong code — tune câu chữ phải sửa code; tách ra file template versioned trong repo để review/diff/tune prompt như tài liệu. Chọn template qua **bảng tra cơ học** kind/tier/domain → file (cùng họ bảng tra tier→model, hợp R42 — không LLM trong vòng chọn; cùng hướng P41 per-tier executor + P18 domain registry). Prompt mỏng thành: định danh vai (agent type) + trỏ skill cần dùng + data item — worker chạy trong worktree của repo nên **tự nạp được skill** từ thư mục skill của repo. **Hai điều kiện giữ từ thiết kế hiện tại:** (1) substitution vẫn `{placeholder}` string-replace thuần, KHÔNG template engine có logic — giữ guarantee `shell:false` + prompt đoán-trước-được; (2) template là artifact có golden-file test (item mẫu → prompt kỳ vọng). Log dispatch (P32/P39) ghi kèm tên + hash template đã dùng để trace "worker sai vì prompt version nào" | Đổi nội dung prompt của một agent-type bằng sửa MỘT file template, không đụng code; chọn template là tra bảng có test; golden-file test đỏ khi template đổi hành vi; log dispatch ghi template id + hash; prompt không-feedback giữ byte-identical với test hiện có hoặc test cập nhật có chủ đích | done | [p49-prompt-template-externalization](../../docs/history/p49-prompt-template-externalization/) — `prompt-templates.mjs` (`selectTemplate`/`renderTemplate`/`hashTemplate`) + `prompt-templates/worker-prompt-default.txt`; `buildPrompt` byte-identical (0 existing test edits); dispatch log records template name+hash (R44). "Agent-type + tự nạp skill" trong Story vẫn là hướng tương lai, chưa nằm trong CoS đã chốt vòng này. |
| P50 | **Extract/induct bộ skill-workflow kiểu bee vào fgOS — bee gốc GIỮ NGUYÊN, không modify** (thảo luận 2026-07-18; là P5 mở lại đúng hẹn — D1 bee-fgos-decision: "mở lại khi P35 đóng", P35 đã đóng): fgOS có xương sống cơ học mạnh (event-sourced, single-door, CAS, goal-check không-tin-lời, anti-loop, session worktree) nhưng trí tuệ workflow mỏng (3 điểm biên headless one-shot); bee có workflow trí-tuệ-giàu (exploring/planning/validating/reviewing nhiều vòng, gate) trên nền JSON + kỷ luật. Việc: **trích/chưng cất bộ skill workflow vào fgOS như một layer sản phẩm** (induct vào repo fgOS, KHÔNG sửa bee đang chạy ở xưởng), bản inducted đi qua cửa chính danh: phiên skill-based `take`/`return` (P26 — R42 ghi rõ cửa pull là ngoại lệ chính danh cho trí tuệ vào hệ) làm bề mặt nhận-trả việc; gate-người map vào `awaiting-human` + ask/answer (P19); cell ≈ work-item con mang `parent` (P16); goal-check vẫn tự verify. **Một cửa ghi duy nhất:** bản inducted KHÔNG mang sổ state song song kiểu `.bee/` — work-item state chỉ qua verb C1/C2. **Kèm vòng học liên tục:** đăng ký bee làm **reference source của distillery** (delta scan theo cursor) — theo dõi bee tiến hóa ở xưởng để bản inducted học tiếp từ nó, không phải chép-một-lần-rồi-lệch | Bộ skill inducted nằm trong repo fgOS, versioned cùng sản phẩm; bee tại xưởng không đổi một dòng; bee có mặt trong distillery sources với delta scan chạy được; case study: MỘT feature thật đi trọn vòng bằng phiên skill-based mà mọi state qua verb fgOS (take/return/submit/answer/approve), goal-check xanh; decision record chốt quan hệ với P5/D1 (case study này là bằng chứng D1 chờ) | proposed | — |
| P46 | **Hợp đồng giao tiếp VÀO/RA của fgOS như một bề mặt I/O hạng nhất** — review chặt + hợp nhất các mảnh in/out đang rải rác theo từng stage thành MỘT hợp đồng mạch lạc, để bất kỳ transport nào (terminal, pane, chat, web, thứ chưa nghĩ ra) cắm vào đồng nhất và fgOS **chủ động được cả chiều ra**, không chỉ bị poll (chống "bị động"). Hiện trạng cần soát: chiều VÀO đã có cửa chuẩn (verb/C1, Host Adapter `b2d18cc7`, listener P38) nhưng đường vào rải theo stage (submit P14, answer/async-human-gate P19, pull-door take/return P26); chiều RA đang **bị động** — envelope + poll list/catchup + data_hash (P37), log worker P39, còn **đẩy/khởi-xướng (attention-envelope C8) thì deferred "chờ lực kéo"**. Việc: kiểm toàn bộ điểm in/out, chốt một hợp đồng in-out **tự-mô-tả** (vào=verb/C1 + identity gate; ra=envelope + attention/C8 push + self-description để thêm surface là dịch-transport→verb + đọc-envelope, không đường ghi riêng), và **nâng chiều-ra/khởi-xướng thành hạng nhất** thay vì phần phụ. **Câu hỏi CỬA-CHUẨN (escalate 2026-07-18, trong scope review):** bản thân "cửa chuẩn giao tiếp với người" CHƯA CHẮC là CLI-verb-spawn hôm nay — spawn-rồi-chết về bản chất không giữ kết nối để đẩy, nên "bị động" là hệ quả TẤT YẾU của model này, không phải bug. Ba mô hình phải cân: (1) CLI-spawn = cửa (b2d18cc7 hiện tại); (2) một **daemon nuôi MỘT cửa protocol chuẩn máy-đọc — không phải UI** (socket/JSON-RPC/MCP) để CLI+UI+remote đều là client, push/stream/remote thành bản chất — đây đúng là kiến trúc INTERFACE của herdr (không phải runtime của nó); (3) core-library + adapter mỏng (hợp P41 executor-port), daemon chỉ bật khi cần remote/push. **CHỐT hướng 2026-07-18: model (3), Ở MỨC INTERFACE.** Contract chuẩn = **SCHEMA event-log + giao thức append/read/subscribe** (fgOS đã event-sourced), **KHÔNG phải một lib link được**. **Lib chỉ là CLIENT tham chiếu (Node) của contract đó**, không phải bản thân contract → daemon/UI-backend có thể viết bằng ngôn ngữ khác miễn nói đúng log-format (chống Node-monoculture, hợp mục tiêu substrate đa-app). CLI = adapter local dùng lib; daemon = consumer (xem chốt dưới); CLI standalone chạy đủ. **Chỉ code CÙNG-TIẾN-TRÌNH (CLI, TUI local) mới gọi lib TRỰC TIẾP; mọi UI web/mobile/remote KHÔNG chạm lib — chúng là client của CỬA MẠNG do daemon phơi ra → daemon là điều kiện CẦN cho bất kỳ UI ngoài terminal.** **Prerequisite móng:** tách core (verb-logic) thành lib gọi được độc lập CLI — refactor nội bộ, không đổi hành vi. **Phạm vi quyết định này CHỈ là interface (hình dạng cửa); tầng OWNER — ai ghi, đồng thời, lock C2, nguyên-tử P45 — là layer RIÊNG, CHƯA xét ở đây.** **CHỐT thêm 2026-07-18: daemon RA KHỎI core — là CONSUMER giao tiếp QUA CLI** (`spawn fgos <verb>` cho ghi + poll `fgos list`/`rollup` + data_hash cho chiều-ra), KHÔNG link lib. Hệ quả: daemon thừa hưởng identity-gate + validation + single-door-lock của CLI miễn phí, không chế được đường ghi mới → **b2d18cc7 (Host-Adapter) được GIỮ + FULFILL, KHÔNG bị siêu-việt** (daemon chính là "lực kéo" mà C8 chờ) → **không cần decision-record-phá-luật.** Push nằm ở consumer sống-lâu (poll rẻ + giữ kênh outbound); core fgOS vẫn passive (chỉ CLI+log). Đây là **airemote tổng quát hoá** (consumer spawn-verb + poll + bridge remote). Sub-choice deferrable: poll-qua-CLI (đơn giản, một đường — nghiêng cái này trước) vs tail-event-log (push tức thì nhưng khoá log-format). CHỈ nếu sau muốn daemon **link-lib in-process** (3b) mới đụng b2d18cc7 → khi đó mới cần record. Kèm: **CLI tự nó phải chuẩn hoá lại** (verb surface + envelope + exit-code + self-describing schema) dù chọn model nào. Reference kiến trúc cửa đã có trong distill: `herdr:socket-api-control-surface`, `herdr:self-describing-protocol-schema`, `herdr:session-snapshot-bootstrap-rpc`. **Trước mắt (increment đầu, không chờ contract xong):** giao tiếp trực tiếp qua terminal/agent + vài pane thiết kế riêng show trạng thái work-item; nói với terminal-agent "vào tiến trình trả lời item X" / "vào exploring item X" rồi chat tiếp — chat modal item-scoped là client ĐẦU TIÊN của cổng. Là móng nằm DƯỚI P38/P39/P40 và mọi UI sau; đứng trên P37. Chất liệu distill tham khảo: `docs/distillery/deep-dives/herdr-vs-tmux-observation.md` (xưởng — vì sao surface là client của cổng, không phải runtime), airemote (path-allowlist/send-confirm cho bề mặt chat-untrusted) | Có một spec hợp đồng in/out (vào=verb/C1, ra=envelope+attention/C8, tự-mô-tả) mà một surface MỚI cắm vào chỉ bằng dịch transport→verb + đọc envelope/attention, không đường ghi riêng, caller chưa xác danh không gọi verb mutation; chiều-ra khởi-xướng ("cần bạn") có đường đi hạng nhất chứ không chỉ poll; increment terminal/pane + chat item-scoped ("vào answer/exploring item X") chạy thật trên cổng đó | proposed | — |
| P48 | **Kênh attention/push ("cần bạn") như một subsystem có delivery-semantics** — tách từ P46 vì chiều-ra chủ động KHÔNG phải bolt-on mà là hệ con riêng, đòn bẩy UX cao nhất + rủi ro cao nhất. Phải chốt 5 thuộc tính: **at-least-once** (item chờ lâu chắc chắn người được ping, không rơi), **dedup** (một gate không ping N lần), **routing** (người/thiết bị/kênh nào), **ack** (đã xem chưa), **escalation** (không đáp trong ngưỡng → ping lại / đổi kênh / đổi người). Sống ở **CONSUMER daemon** (poll data_hash P37 / tail log P39 + giữ kênh outbound), **core fgOS vẫn passive** — đây là chỗ hiện thực hoá "không bị động" mà lõi KHÔNG mọc organ push nội tại. Đứng trên P46 (contract in/out), P39 (log substrate), P37 (poll rẻ). Reference pattern: airemote notify (at-least-once, watcher-fed) trên herdr-gateway; herdr `notification.show`. | Một spec kênh attention nêu rõ 5 thuộc tính (at-least-once/dedup/routing/ack/escalation); một "cần bạn" thật của item chờ-người được giao ĐÚNG-MỘT-LẦN tới người, có ack, không đáp trong ngưỡng thì escalate đúng — chứng minh trên item thật, core không thêm trạng thái push nội tại | proposed | — |
| P47 | Audit thống nhất **loại-hình công việc** + **hệ đặt tên/đánh mã** đang chạy song song trong hệ thống — chưa có review nào gộp chung: (a) `kind` work-item (`classify.mjs`: bug/feature/chore/docs, mặc định `task`) — đủ chưa, thiếu loại nào (vd spike, review); (b) nhiều hệ mã số tách biệt cùng sống mà chưa giải thích ranh giới ở một chỗ: P# tuần tự (docs/backlog.md, PBI sản phẩm), D-hex (decision record, per-feature CONTEXT.md + `.bee/decisions.jsonl`), R# (rule trong docs/specs/runner.md), C1-C9 (contract trong architecture-map.md), cell-id `<feature>-N` (bee); (c) tên feature kebab-case tự đặt tay, không qua cửa sinh id nào — dễ trùng/lệch nghĩa khi hai việc đặt tên gần nhau | Một bảng liệt kê đủ mọi loại-hình + hệ mã đang dùng trong hệ thống (nguồn trích từ code/docs thật, không suy đoán); decision record trả lời: giữ đa hệ có chủ đích (mỗi hệ phục vụ đúng một tầng, ghi rõ ranh giới dùng-khi-nào) hay hợp nhất/đổi tên chỗ nào; nếu giữ đa hệ thì ranh giới đó được tài liệu hoá một chỗ (vd phụ lục architecture-map hoặc reading-map) | proposed | [id-systems-audit (draft, chờ chốt)](id-systems-audit.md) |
| P44 | Approve session-nesting guard (P35 Epic 2) chỉ nhận diện worktree đăng ký qua `fgos session start` — một `git worktree add` thủ công, không qua registry, vẫn lọt đúng nguy cơ pull/legacy false-verification (goal-check chạy trên code cũ, báo "verified on main") một cách âm thầm | `approve` xác nhận `repoRoot` LÀ worktree chính (vd `git rev-parse --show-toplevel` so `--git-common-dir`'s parent) thay vì chỉ tra registry — bắt được cả worktree không đăng ký | done | [approve-worktree-guard](../../docs/history/approve-worktree-guard/) — `isMainWorktree` (`src/runner/merge.mjs`) chặn cấu trúc ngay sau chặn sổ đăng ký trong `approve`; red-before-green test (`test/cli/fgos.test.mjs`) tái tạo false-verification rồi xác nhận chặn, 995/995 npm test xanh |
| P45 | `.fgos/events.lock` (P35 Epic 3) chỉ đóng đua trùng-seq tại chính `appendEvent` — không làm các thao tác đọc-sửa-ghi cấp cao ở `store.mjs` (kiểm id-đã-tồn-tại của `addWork`, CAS `expectedStatus` của `moveWork`) nguyên tử liên-tiến-trình; hai lệnh `fgos` tương tác song song trên CÙNG một id vẫn có thể mỗi bên qua tiền-kiểm cũ rồi ghi hai sự kiện hợp-lệ-nhưng-mâu-thuẫn | Một khóa bọc-cả-đọc-sửa-ghi (hoặc vòng CAS-thử-lại) ở tầng `store.mjs` cho các thao tác ghi tương tác cạnh nhau trên cùng id — vòng runner tự hành đã có bảo vệ riêng (runner.lock + write-queue), phần thiếu là hai verb tương tác đua nhau | done | [store-atomic-rmw](../../docs/history/store-atomic-rmw/) — `withEventsLock`/`appendEventLocked` (events.mjs) bọc TRỌN đọc-tiền-kiểm-ghi của `addWork`/`editWork`/`moveWork`/`moveStage` (store.mjs) trong MỘT phiên giữ `.fgos/events.lock` sẵn có; 2 test fork-đa-tiến-trình chứng minh đua đóng (red-before-green qua git stash), 1009/1009 npm test xanh |
| P7 | Trường priority cho frontier ordering. **Chốt qua thảo luận R42 (per P38, 2026-07-17):** khi P38 tồn tại, priority có thể nhận một GỢI Ý từ người qua chat — P7's thiết kế đọc gợi ý đó làm một tín hiệu đầu vào (không phải field cuối áp thẳng) khi tính field priority thật | FIFO-theo-seq lộ giới hạn thật được ghi nhận; nếu tiêu thụ gợi ý người từ P38, field priority cuối vẫn do một bước tính/ghi duy nhất quyết, không phải chat ghi thẳng | proposed | — |
| P8 | Intent-scoring + signal-driven chaining (hướng đã chốt từ consult). **Chốt qua thảo luận R42 (per P38, 2026-07-17):** khi P38 tồn tại, intent có thể nhận một GỢI Ý từ người qua chat — P8's thiết kế đọc gợi ý đó làm một tín hiệu đầu vào (không phải field cuối áp thẳng) khi tính intent-score thật | Phase 2 đóng; có case cần reactive dispatch; nếu tiêu thụ gợi ý người từ P38, field intent cuối vẫn do một bước tính/ghi duy nhất quyết, không phải chat ghi thẳng | proposed | — |
| P36 | Metric riêng cho từng fix của vòng evolving (P13 D4), chỉ mở khi entropy-trend chứng minh không đủ nhạy ở quy mô 1 fix/lần | Có điểm số đo được trước/sau một fix đơn lẻ, độc lập với entropy-trend tổng | proposed | — |
| P27 | Orchestrator service tầng fleet: registry worker + heartbeat + push assignment + lease/liveness + capacity + giao thức/auth cho worker từ xa — đắp TRÊN cùng event log (stage-decompose D1: pull là móng, không đập lại) | Chỉ mở khi có nhu cầu fleet thật (worker từ xa/luôn-bật); thiết kế đứng trên log + claim sẵn có | proposed | — |
| P38 | Human-UI listener (web/chat) trên đất Host Adapter (D b2d18cc7): translator nhận transport (HTTP/chat) → dịch sang verb → spawn `fgos <verb>` → đọc envelope + exit code; kèm identity gate "ai được nói verb nào" trước khi dịch xuống C1 (trust invariant dispatch.mjs: `verify` chạy như shell — intake nguồn chưa kiểm là injection vector); KHÔNG fgos-serve in-process khi C2 một-cửa-ghi còn per-process; chiều ra tạm poll list/catchup + so data_hash, C8 attention envelope chờ lực kéo thật. Đứng trên P37. **Chốt qua thảo luận R42 (per bàn P42, 2026-07-17):** P38 bao gồm một verb chat cho người GỢI Ý priority/intent (phục vụ P7/P8) — verb này ghi một field GỢI Ý, KHÔNG PHẢI field cuối cùng; bộ não thông minh ở giai đoạn planning/scoring (clarify/decompose hôm nay, P7/P8 mai sau) đọc gợi ý này làm một tín hiệu đầu vào rồi TỰ quyết field cuối qua cửa ghi chuẩn — có thể theo đúng gợi ý hoặc đổi khác nếu thấy hợp lý hơn, nhưng gợi ý luôn được cân nhắc, không bao giờ bị bỏ qua và không bao giờ tự nó là giá trị cuối. Giữ đúng R42 (đúng một actor ghi field cuối, picker vẫn cơ học) | Một người dùng qua web/chat submit được vấn đề, trả lời ask, duyệt approve/reject, VÀ gợi ý priority/intent cho một item — tất cả qua verb C1, không đường ghi riêng; caller chưa xác danh không gọi được verb mutation; gợi ý priority/intent của người được planning-stage đọc và cân nhắc (không tự động trở thành field cuối) | proposed | — |
| P39 | Xem live output worker (D 644916a4): (a) tee stdout/stderr của worker TỪNG CHUNK vào `.fgos/logs/<id>.log` ngay khi đến — qua đúng cửa worker-log (một-cửa D3 của worker-dispatch-log), giữ nguyên khối kết-cục cuối (timeout/hỏng-spawn vẫn bắt được, resolve trên exit không đổi) → `tail -f` thành cửa sổ xem live per-item, N worker song song không giẫm dòng nhau; (b) tùy chọn stream-json qua executor args trong config runner — thuần Host Adapter, zero code. File log live là chất nền cho chiều-ra của P38 (UI tail + đẩy) | Trong lúc worker đang chạy, `tail -f` file log của item cho thấy output đến theo thời gian thực; khối kết-cục cuối vẫn ghi đủ như hiện tại; không path committed nào nhận output worker | done | [live-worker-log](history/live-worker-log/) — (a) làm xong: `appendWorkerLogChunk` (worker-log.mjs) + `opts.onChunk` (dispatch.mjs) + wire (loop.mjs), 995/995 test xanh, spec-synced. (b) không cần code riêng — đã sẵn qua `.fgos-runner.json` executor.args (Host Adapter) |
| P40 | Chế độ vận hành qua tmux (D ef6ed305): phiên tmux chuẩn 4 pane — (1) runner loop `while … fgos-runner --once … sleep` (đứng trên runner.lock + idempotent, không cần flag mới), (2) `tail -f` log live per-item (đứng trên P39), (3) cửa người submit/answer/review/approve/reject, (4) dashboard poll `fgos list/ready/rollup/triage` (rẻ hóa bằng data_hash P37); chuông chờ-người bằng tmux monitor-activity/BEL trước khi C8 tồn tại; kèm MỘT trang runbook trong docs sản phẩm. Đa pane chung checkout đòi P35 đóng | Người vận hành dựng được phiên tmux chuẩn từ runbook trong <5 phút: worker đang chạy xem được live, item chờ-người có đánh dấu cửa sổ, duyệt PR không báo động giả khi runner đang merge song song | proposed | — |
| P41 | Per-tier executor + executor port hexagonal-lite (D a4fe4c2b): (a) config runner cho MỖI TIER khai executor riêng `{command, args}` — light/standard/heavy cắm CLI khác nhau trong CÙNG một lượt drain; tier không khai riêng rơi về executor chung (config cũ chạy nguyên, backward-compat); (b) dispatch nâng C9 từ "mẫu spawn" thành interface executor CÓ TÊN (C9 v2) với adapter — hôm nay đúng một adapter `cli-spawn` (argv, shell:false, hành xử giữ nguyên); adapter `rpc/app-server` (vd codex app-server giao tiếp RPC, không phải CLI) chỉ xây khi có nhu cầu cắm thật — mua cái TÊN của bậc trừu tượng, chưa mua bậc | Hai tier trỏ hai executor khác nhau chạy đúng trong cùng một lượt `--once`; config cũ (executor chung) chạy không đổi hành xử; C9 v2 có tên interface + adapter trong spec, RPC adapter ghi là deferred | done | [p41-per-tier-executor](../../docs/history/p41-per-tier-executor/) — `EXECUTOR_ADAPTERS` registry (C9 v2, sole adapter `cli-spawn`) + `cfg.executors.<tier>` override in `dispatch.mjs`; mixed-tier dispatch proven in one config, 0 pre-existing test edits (R41). |
| P43 | Work-graph metrics surface (D f176c18a, dưới stance R42): một verb đọc-thuần trả metrics graph CƠ HỌC fold từ event log — unblock-set transitive, critical path/depth, connected-components (mấy mũi song song độc lập), chuỗi kẹt/stale-blocked — qua envelope; consumer KÉP: khóa sort tương lai của picker (P7, vẫn tra bảng — không phạm R42) + digest cho planner-brain (P8, LLM hết phải đếm cạnh). Chất liệu distillery ĐÃ ĐÀO, tham khảo bắt buộc khi planning: bvr transparent-triage 8 thành phần giải trình được + greedy-submodular top-k-unblock + what-if + connected-components=parallel-tracks (`docs/distillery/sources/beads-viewer-rust.md` xưởng); beads 10-loại dependency + unified cycle detection (`docs/distillery/sources/beads.md`); graph-drift 6 trục làm health gate; five-tier eligibility stack (`docs/distillery/deep-dives/work-item-management.md` §4 — fgOS đang tier 1-2, P43+P7+P8 leo tier 3-4) | Một verb đọc trả qua envelope ít nhất: unblock-set transitive, critical path, connected-components, chuỗi kẹt; số liệu khớp kiểm tay trên fixture; P7/P8 trích dẫn được metric của nó làm input thiết kế | done | [work-graph-intelligence](../../../docs/history/work-graph-intelligence/) — `fgos graph` verb: connected-components + critical-path + stale-blocked + greedy top-k-unblock, deterministic qua C1 envelope (S1–S6). Epic S7–S9 CŨNG XONG: `fgos graph --what-if <id>` + architecture frame (S7), `fgos stale` evidence-classifier advisory (S8), `fgos conflicts` + `footprint` field footprint-intersection planner (S9). Docs-track XONG: platform-foundations L9 (thang hoàn tất việc run≠merge≠durable, DT1) + L10 (add-through-not-alongside doctrine, DT2). Epic đóng hoàn toàn. |
| P37 | Chuẩn hóa entry cho multi-surface (D b2d18cc7): (a) envelope fgos.v1 trên MỌI verb — hoàn tất vế envelope của C1 (hiện chỉ submit/discover có; các verb còn lại in chữ thường, UI nào bám vào cũng là bám regex); (b) verb manifest máy-đọc `fgos --help --json` trả `{verb, params, mô tả, read/mutation}` để listener/UI sinh form-lệnh thay vì hard-code (tiền lệ: bee.mjs --help --json) | Mọi verb trả envelope `{contract, generated_at, data_hash, data}` qua stdout; một manifest máy-đọc liệt kê đủ verb surface; agent/UI render mọi verb bằng một renderer, không regex | done | [entry-standardization](../../../docs/history/entry-standardization/) |
| P4 | Distillery lên state layer làm consumer thứ hai (porting lifecycle thành FSM) | Trạng thái porting đọc/ghi qua cùng một cửa state layer, dựng lại được từ event log | done | [distillery-state-consumer](history/distillery-state-consumer/) |
| P28 | GitHub adapter cho PR lifecycle: transport remote (tạo PR thật, duyệt trên web, auto-merge qua API) đứng trên CÙNG cổng duyệt nội bộ của P17 (pr-lifecycle D1 — local-first, adapter chỉ đổi transport) | Một item proposed tạo được PR GitHub thật, duyệt web → hệ merge remote + item done; cổng duyệt nội bộ không fork luật | done | [github-adapter](history/github-adapter/) |
| P35 | fgos multi-session trên CÙNG checkout (dogfood run-6, 2026-07-17, phiên khác đang chạy P32 song song lần đầu tiên): (a) `isWorkingTreeClean` của `approve`/`return` không phân biệt "bẩn của phiên nào" — một phiên khác merge song song có thể chặn approve của phiên này 2 lần liền; (b) soát diff nhánh runner trước khi hỏi người approve mà diff thẳng với ref `main` đang di chuyển (thay vì `git merge-base`) tạo báo động giả khi có phiên khác merge song song — run-6 tự gây 1 reject oan vì lỗi này | Quy ước rõ multi-session trên chung checkout: (a) mỗi phiên có worktree/checkout riêng (đứng trên hướng fleet P27), hoặc (b) quy tắc ai chịu trách nhiệm commit khi tree dirty do phiên khác gây ra; quy trình soát-diff-trước-approve LUÔN dùng `git merge-base`, không diff thẳng một ref có thể di chuyển | done | [fgos-multi-session-checkout](history/fgos-multi-session-checkout/) — `fgos session start/end/list` (worktree-per-session, `.fgos` symlink dùng chung), `approve` từ chối khi lồng trong session, `events.lock` liên-tiến-trình cho append. (b) diff-trước-approve vốn đã đúng (`git diff trunk...branch` = merge-base). Dư lượng đã ghi (Deferred Ideas): ad-hoc worktree ngoài registry, CAS đua verb-tương-tác |
| P42 | Kiểm định "sự thông minh của giao việc nằm ở đâu": audit ranh giới planner-vs-orchestrator và chốt bằng decision record — hiện loop là cỗ máy CƠ HỌC (FIFO theo seq, tier→model tra bảng, ngân sách visit, ma trận recovery — không gọi LLM nào trong vòng chọn-giao), trí tuệ nằm ở BIÊN stage (clarify/discovery + decompose gọi model, classify intake cơ học); quyết giữ đạo lý "orchestrator ngu — trí tuệ ở biên" hay cho phép thông minh vào vòng chọn việc, và nếu vào thì qua cửa nào | Một decision record trả lời rõ orchestrator được phép thông minh đến đâu, kèm căn cứ đọc code; spec runner ghi stance này; P8 (intent-scoring) và P7 (priority) đọc decision này TRƯỚC khi thiết kế | done | chốt qua decision f69951df (thảo luận + căn cứ đọc code 2026-07-17); stance ghi ở spec runner **R42**: picker cơ học vĩnh viễn, trí tuệ vào qua field-trên-item (dòng chính) + cửa pull (ngoại lệ) |
| P13 | Evolving loop human-gated (F5): tự-sửa 2-gate (Gate A chọn / Gate B duyệt diff), explicit-accept-per-item, Iron Law cho skill sửa hệ, push never automatic | Vòng evolving chạy human-gated với outcome đo được (tiêu chí F5, luật L6); diff được người duyệt trước khi push, không bao giờ tự động | done | [self-improve-loop](history/self-improve-loop/) — **Iron Law's ranh giới thật:** phân loại rủi ro (keyword/module) + chặn cứng đòi `--acknowledge-iron-law`, KHÔNG mechanically verify "test đỏ chạy trước" (không có hạ tầng theo dõi lịch sử đỏ/xanh — xem CONTEXT.md D17) |
| P18 | Mô hình base-workflow + domain-extension: một chu trình chung nền tảng (init→làm rõ→chia việc→thực thi→compound-learn) mà coding/marketing/HR/finance kế thừa và thêm stage riêng (work-item-lifecycle-vision.md §2) | Một domain thứ hai (vd marketing) chạy được trên cùng base FSM, chỉ thêm stage riêng, không fork chu trình | done | [base-workflow-model](history/base-workflow-model/) |
| P21 | Backlog-triage (cửa 2 của triage, phân biệt với P14 intake-triage = cửa 1): forgent cần một cửa quyết "việc nào nổi lên chú ý" (impact/value ranking) tách bạch khỏi "việc này rủi ro/lane gì" — hiện chưa có (per deep-dive work-item-management.md:106, đúc kết từ beads-viewer-rust ranking) | Có một truy vấn/lệnh xếp hạng việc theo tác động, độc lập với phân loại rủi ro của intake | done | [backlog-triage-ranking](history/backlog-triage-ranking/) — `fgos triage` (blocking-fan-out proxy over `deps`) |
| P24 | Rollup view theo "bộ" (stage-clarify D11): human hỏi "việc tôi nộp tới đâu rồi" — xem item gốc + tiến độ hậu duệ (n/m xong), dựa trên field parent (P16 dựng) | Một lệnh đọc cho thấy mọi item gốc kèm tiến độ con; item nội bộ không làm nhiễu tầm nhìn cấp gốc | done | `fgos rollup <id>` (bin/fgos.mjs), dogfooded qua [dogfood-p25-run1](history/dogfood-p25-run1/) run-6 |
| P5 | Xem xét thay bee bằng state layer của forgent — chỉ mở khi forgent đạt F3 (ngưỡng tên, per D2 phase-1-state-layer) | Quyết định có/không kèm case study vận hành song song | done | [bee-fgos-decision](history/bee-fgos-decision/) — D1: CHƯA thay, mở lại khi P35 đóng |
| P22 | Số phận verb `add`: đã hạ cấp khỏi câu chuyện public (stage-clarify D9 — submit là cửa công khai duy nhất; id tự đặt phá nhất quán id tự sinh); quyết xóa hẳn hay giữ làm bề mặt nội bộ cho test/tooling | Spec/docs không kể `add` là cửa vào; quyết định xóa/giữ có decision record; test suite không phụ thuộc đường public nào đã bỏ | done | [work-item-verb-surface](history/work-item-verb-surface/) — D1: giữ nội bộ, không xóa |
| P23 | Verb update/edit field công khai cho work-item (ghi đè kind/risk do classify gợi ý, sửa verify tay...) — "luôn ghi đè được" (stage-intake D1) hiện chưa có bề mặt CLI nào thực hiện được | Người/agent đổi được field của item đã tồn tại qua một verb, đi qua cửa ghi C2, event additive | done | [work-item-verb-surface](history/work-item-verb-surface/) |
| P9 | Merge tự động đề xuất của worker khi review sạch | Vòng đề-xuất-duyệt chạy tay đủ tin cậy, có số liệu | done | gộp vào [pr-lifecycle](history/pr-lifecycle/) (P17 D3) |
| P29 | Packaging/install thật của fgOS (npm/global/copy-bin): câu chuyện phân phối đứng trên nền coexistence P10 (install-coexistence D1 — đợt P10 chỉ chạy-tại-chỗ) | Cài fgos vào một project ngoài bằng một lệnh, init + doctrine + marker detection chạy đúng như canary P10 | done | [distribution-packaging](history/distribution-packaging/) |
| P3 | Compound-learning trên routing (Phase 3): vòng predicted→actual, capture 2 kênh, entropy-trend, evolving human-gated | Vòng predicted→actual chạy thật với bằng chứng output (tiêu chí F4, luật L6) | done | [phase-3-compound-learning](history/phase-3-compound-learning/) |
| P6 | Fan-out song song N worker trên frontier | Anti-loop đã chứng minh trong vòng chạy thật (ngưỡng A1 phase-2-routing); N worker không phá single-writer | done | [fan-out-parallel](history/fan-out-parallel/) |
| P10 | Install/coexistence story của fgOS: cài vào project/global không giao thoa tiến trình với harness khác (doctrine scope lãnh địa, hook gate theo path, một-nhạc-trưởng-mỗi-phiên, phát hiện marker harness khác lúc cài) | Canary chạy trong project cài CẢ bee lẫn fgos: hai bên không chặn nhầm write của nhau, agent không nhận mệnh lệnh điều phối mâu thuẫn. **ĐÓNG PHẦN FGOS per D7:** canary pin 2 deny bee làm known-gap (friction đã file), CoS trọn chờ bee-side fix. | done | [install-coexistence](history/install-coexistence/) |
| P11 | Ly khai bee (xưởng, gốc) ↔ forgent (sản phẩm, ./repo): artifacts hai bên tách tuyệt đối, release sạch không rác | Baseline verify xanh nguyên trạng trong ./repo; một cell bee smoke chạy trọn vòng ghi vào ./repo; fgos ready + runner --dry-run chạy đúng chỗ mới; doctrine hai bên tách đôi | done | [repo-divorce](history/repo-divorce/) |
| P12 | Export chưng cất sử ký sản phẩm: sinh decision-records/changelog vào docs/decisions/ của forgent từ hồ sơ xưởng (per D3 repo-divorce) | Người ngoài đọc docs/decisions/ hiểu các quyết định lớn của sản phẩm mà không cần vào xưởng | done | [decision-records-export](history/decision-records-export/) |
| P14 | Stage Intake: người submit vấn đề, 2 chế độ (a) un-attended rồi bỏ đi / (b) collaborate-now; hệ ghi nhận + auto-classify work-item (per work-item-lifecycle-vision.md §3-4 bước 1-2) | Một vấn đề submit vào tạo đúng một work-item đã phân loại tự động (tier/kind/risk cơ học, id tự sinh); chế độ submit (sync/async) được GHI LẠI trên item — đậu-chờ-người-khi-cần là việc của P15 (chốt tại exploring D6, không phải submit tự đậu vô điều kiện) | done | [stage-intake](history/stage-intake/) |
| P15 | Stage Làm-rõ: runner sơ khởi chạy phân loại + context-discovery; task đơn giản → `ready`, task phức tạp → `need-exploring` rồi kích hoạt exploring trong-sản-phẩm (lifecycle §4 bước 3,5,6) | Item đủ rõ tự chuyển sang stage executing (đủ để `ready` nhặt khi deps xong); item thiếu rõ đậu `awaiting-human` mang câu hỏi cụ thể, lặp tới khi đủ — giải qua chiều `stage` (clarify/executing) + context-discovery gọi model, không phải một status FSM mới (chốt sau khi bàn sâu về mô hình 2 tầng + tham khảo 3 nguồn distill) | done | [stage-clarify](history/stage-clarify/) |
| P16 | Stage Chia-việc: auto làm giàu context + planning, decompose thành n item nhỏ độc lập (dependency rõ) đẩy vào queue (lifecycle §4 bước 7; câu hỏi mở: có cổng-người ở planning không). Ràng buộc đã khóa từ stage-clarify D11: item con mang field `parent` (lineage, không field `origin` riêng); item gốc là mỏ neo chú ý người, không dispatch khi con dang dở; deps giữ nguyên nghĩa chặn, tách khỏi parent; id con dùng chung generateId | Từ một item đã qua stage `executing` (không còn dùng chữ "ready" mơ hồ — xem stage-clarify) sinh ra n item nhỏ có dependency đúng + field parent trỏ đúng gốc, vào queue, runner pick được | done | [stage-decompose](history/stage-decompose/) |
| P17 | Stage PR lifecycle: tạo PR từ nhánh đề xuất, cổng review người, auto-merge khi duyệt sạch (lifecycle §4 bước 9-11; gồm P9). Câu hỏi mở: worktree-mỗi-item vs chung, PR-mỗi-item vs gom-theo-human-submit | PR sinh tự động, người duyệt ở cổng, hệ merge tự động sau duyệt; chiến lược worktree + độ hạt PR đã chốt | done | [pr-lifecycle](history/pr-lifecycle/) |
| P19 | Async human-gate primitive: work-item đậu chờ người quyết (state `awaiting-human`), ghi 'chờ gì', resume khi người trả lời — bất đồng bộ, không chặn; spine dùng lại cho mọi cổng-người của lifecycle (work-item-lifecycle-vision.md). Không làm intake/exploring stage — chỉ primitive | Một work-item vào trạng thái chờ-người, ghi rõ đang chờ gì; người trả lời qua CLI → item rời trạng thái chờ, câu trả lời ghi thành event, item lại actionable | done | [async-human-gate](history/async-human-gate/) |
| P3-S2 | Phase 3 Slice 2 — kênh friction của capture 2 kênh: `work.friction` event tại choke-point park/halt, runner tự quy tội 5 lớp (task-spec/context/environment/verification/state), fold append-theo-id, `fgos check` thêm mục friction (cap 5) + nhắc item thiếu outcome. Settlement dạng đầy đủ dời sau (vision §8, decision 719cbe3a) | Một park/halt thật ghi đúng 1 friction record với layer đúng; `check` in mục friction + nhắc; log không có friction event thì output không đổi | done | [phase-3-compound-learning](history/phase-3-compound-learning/) |
| P25 | Mốc dogfood tự-phát-triển: dùng fgos tại xưởng điều phối phát triển chính fgos sản phẩm (thay vai bee hiện tại — xưởng điều phối, `./repo` là đối tượng). Điều kiện tối thiểu: P16 + P17 khép kín vòng giao việc→nghiệm thu; P10 coexistence trước khi chạy thật trong xưởng đang có bee. Bee chủ động báo người dùng khi đủ điều kiện (standing request 2026-07-16, kiểm tại mỗi feature close) | Một work-item thật của chính fgos đi trọn vòng submit→clarify→chia việc→execute→PR→merge do fgos-runner điều phối trong xưởng, không cần bee đỡ — run-1 2026-07-16: FAIL tại clarify-loop (→P30), bằng chứng docs/history/dogfood-p25-run1/ xưởng; run-2 2026-07-17 SAU P30: clarify HỘI TỤ với answer sẵn có (P30 CoS đạt), đi tiếp decompose→dispatch→goal-check→park verify-miss — biên giới mới: worker execution rỗng (→P31) — **ĐẠT 2026-07-17**: 2 item thật trọn vòng (bo-hardcode 763497d, loai-tru-data-dir 6bb3e01) — submit→clarify→(decompose pass-through)→worker code thật→proposed→reject/park→người hoàn tất qua cửa D2→approve→merge→done; settlement/learning/friction/entropy đủ; bee không đỡ bước nào của vòng fgos | done | [human-rounds](history/human-rounds/) + [dogfood-p25-run1](history/dogfood-p25-run1/) |
| P26 | Cửa pull giao–nhận việc (take/return): tác nhân ngoài runner (người/phiên/runner khác) nhận đúng một item từ queue qua claim CAS, trả kết quả qua cửa event; runner vẫn tự dispatch khi không ai cầm — mô hình hóa attend/unattend (stage-decompose D1) | Một người/phiên take một item, runner không giẫm; return trả kết quả thành event; goal-check giữ luật không-tin-lời-worker | done | [stage-decompose](history/stage-decompose/) |
| P30 | Discovery prompt đủ ngữ cảnh: buildDiscoveryPrompt (src/intake/discovery.mjs) hiện chỉ đưa title-cắt/kind/risk/refs/deps — THIẾU description đầy đủ + toàn bộ ask/answer history (gates) + discovery history → vòng làm-rõ không hội tụ, hỏi lặp rồi fail-safe park (dogfood P25 run-1 phát hiện, 2026-07-16) | Item có answer của người không bị hỏi lại cùng chủ đề; item dogfood bo-hardcode-ten-trunk pass clarify sau 1 answer; description đầy đủ nằm trong prompt phán | done | [discovery-context](history/discovery-context/) |
| P31 | Worker execution thật cho dogfood: run-2 (2026-07-17) worker được dispatch nhưng nhánh fgw rỗng (aheadCount 0, verify-miss ×2 → parked đúng luật) — điều tra executor config/worker prompt của dispatch cho task implementation thật (model nào, prompt đủ chưa, timeout, làm sao thấy log worker) | Item dogfood bo-hardcode-ten-trunk được worker thật implement: nhánh fgw có commit, goal-check xanh, item proposed | done | [worker-execution](history/worker-execution/) |
| P32 | Persist log worker dispatch (observability): hiện output worker chỉ console-tail per luật security-panel (loop.mjs output-discipline — KHÔNG persist vào path committed); muốn log file .fgos/logs cần SUPERSEDE quyết định đó tường minh + gitignore + một cửa ghi (single-door .fgos) + đính stdout vào DispatchError cho timeout/spawn-fail (plan-checker P31 B1/W1/W2 đã map đủ ràng buộc) | Quyết supersede-hay-không có decision record; nếu làm: log đọc được cho MỌI kết cục kể cả timeout, không phá single-door, không vào git | done | [worker-dispatch-log](history/worker-dispatch-log/) |
| P33 | Feedback người tới worker: prompt worker (buildPrompt) thiếu answer từ gates + lý do reject mới nhất → vòng reject không hội tụ (run-3: reject kèm yêu cầu test, worker round-2 no-op vì không thấy lý do); kiểm fold `reason` trên work.move (đường destructure — critical pattern fold-allowlist) | Reject kèm lý do → worker round sau NHÌN THẤY lý do trong prompt và làm đúng; item dogfood có test master-trunk sau một vòng reject — GHI CHÚ: code + unit-proof đã ship (worker-execution-2); live-proof CoS bị anti-loop chặn trước dispatch → gộp vào CoS P34 | done | [worker-execution](history/worker-execution/) |
| P34 | Anti-loop vs vòng người: (a) reject/answer của NGƯỜI hiện ăn cùng ngân sách visit với vòng lỗi → item bị park 3/3 TRƯỚC khi worker kịp thấy lý do reject (run-4); (b) item nhánh-runner bị anti-loop park xong thì KHÔNG còn cửa công khai nào cho người hoàn tất đề xuất (pull-door đo main, không hợp việc trên nhánh; classifier cũng chặn đẩy tay — đúng tinh thần). Thiết kế: can thiệp người (answer/reject) làm mới/mở rộng ngân sách visit? verb người-tiếp-quản-nhánh? | Một vòng reject do người chủ động không bị anti-loop nuốt; item park 3/3 có đường người-hoàn-tất chính danh; live-proof P33 (worker thấy lý do và làm đúng) chạy được | done | [human-rounds](history/human-rounds/) |
| P20 | Registry-manifest máy-đọc cho architecture-map + script 2 phép kiểm trong verify: (a) chiều import một-chiều-xuống theo nhà đã khai, (b) đủ sổ — mọi file src/** bin/** có row (architecture-map §9.3, record 0010; đứng trước S2-friction) | Script đỏ khi có import ngược tầng hoặc file thiếu row; verify chain chạy nó; sổ §6 không drift được nữa | done | [0010](decisions/0010-ban-do-kien-truc-la-ban-chuan.md) + map §9.3 |
| P2 | Routing trên state (Phase 2): frontier sẵn-sàng thành lệnh, chain handoff, recovery matrix + anti-loop, runner tuần tự | Agent lạ tự tìm việc kế tiếp bằng truy vấn derive, không danh sách tay (tiêu chí F3, luật L6) | done | [phase-2-routing](history/phase-2-routing/) |
| P1 | forgent tự quản work-state của chính nó qua tầng state/FSM (Phase 1: store zero-dep, transition có precondition, single-door, CAS, decisions event-sourced) | Một agent lạ, không chat history, trả lời "đang ở đâu / việc gì đang mở" từ state chứ không từ trí nhớ (tiêu chí F2, luật L6) | done | [phase-1-state-layer](history/phase-1-state-layer/) |
