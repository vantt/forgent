# Product Backlog

| ID | Story | CoS | Status | Feature |
|----|-------|-----|--------|---------|
| STR82 | **Auto-detect CLI trợ lý có sẵn trên máy làm executor mặc định cho `.fgos-runner.json` tự sinh, thay vì luôn giả định `claude`** (nêu ra 2026-07-22, deferred idea của str76-runner-bootstrap D1) — D1 khoá `claude` làm executor mặc định vì thiếu bằng chứng thật về người dùng không-Claude; ý tưởng auto-detect (`command -v claude`/`command -v codex`/... rồi điền template tương ứng, rơi về placeholder báo-lỗi-rõ nếu không tìm thấy CLI nào) đã bị loại ở vòng explore đầu vì thêm logic detect cần bảo trì danh sách CLI biết trước, và MVP giả định đối tượng dùng đã có sẵn Claude Code. Chỉ đáng làm khi có bằng chứng dogfood thật cho thấy người dùng thật đang chạy trên CLI khác `claude` và chạm lỗi "command not found" ở bước dispatch | Khi có ≥1 bằng chứng dogfood thật (không phải suy đoán) về người dùng non-Claude, `.fgos-runner.json` tự sinh dùng CLI phát hiện được trên máy thay vì luôn giả định `claude`, rơi về placeholder báo-lỗi-rõ nếu không CLI nào khớp | proposed | (chưa gán — chờ bằng chứng dogfood; ứng viên bee-planning nhỏ khi có nhu cầu thật) |
| STR80 | **Trang trí thêm quy ước OKF cho enduser-docs-index: `index.md` mỗi ngăn (progressive disclosure) + `log.md` lịch sử đổi theo ngày** (nêu ra 2026-07-22 lúc bàn str64-backfill, deferred idea của CONTEXT.md area này) — hiện chỉ mục máy-đọc (`docs/enduser-docs-index.json`) là bảng tra duy nhất; một `index.md` per-thư-mục cho người/agent duyệt nhẹ một ngăn mà không cần load cả manifest. Chưa cần ngay — chỉ đáng làm khi số tài liệu mỗi ngăn tăng thật (hiện `explanation` mới 23 mục, `how-to` 1 mục) | Mỗi thư mục `docs/<quadrant>/` có `index.md` liệt kê + mô tả ngắn các tài liệu trong đó; không đổi hành vi chỉ mục JSON hiện có | proposed | (chưa gán — ứng viên bee-planning nhỏ khi có nhu cầu thật) |
| STR81 | **Kiểm tra lệch giữa đầu-mô-tả (frontmatter) và log cho `enduser-docs-index`** (nêu ra 2026-07-22, deferred idea của str64-backfill D3) — `móc-liên-kết-nguồn` trong đầu-mô-tả mỗi tài liệu là bản sao ghi-lúc-soạn, không phải nguồn thẩm quyền (log qua `fgos doc-sources` mới là nguồn thật); nếu một tài liệu được liên kết lại sau khi đầu-mô-tả đã ghi, hai bên có thể lệch mà không ai biết. Chưa xây — ưu tiên thấp, chỉ cần khi quan sát thấy lệch thật | Một lượt kiểm (khả dĩ trong bee-grooming) phát hiện tài liệu có `móc-liên-kết-nguồn` khác với kết quả `fgos doc-sources` cho cùng đường dẫn, báo cáo rõ ràng — không tự sửa | proposed | (chưa gán — ứng viên bee-grooming) |
| STR73 | **Done-flip của PBI phải KIỂM từng mệnh đề CoS thực-giao, không chỉ "feature tham chiếu đã đóng" — nếu không, PBI có CoS rộng hơn scope feature bị mark `done` NON (premature)** (đào tận gốc 2026-07-21 từ ca STR64). **Bằng chứng cơ chế:** luật done-flip của bee trigger theo FEATURE-đóng + khớp-row, KHÔNG theo CoS-thỏa: `bee-scribing/SKILL.md:94` — *"when this scribing run closes a feature that matches a backlog row, flip that row to `done`"*; `bee-compounding/SKILL.md:81` — *"backlog done-flip specifically remains prose-ruled"*. Cột `CoS` (định nghĩa `scribing-reference.md:297` = "the one-line acceptance signal") **tồn tại nhưng KHÔNG bao giờ được đối chiếu** với artifact thực-giao trước khi flip. **Hệ quả cụ thể:** STR64 CoS đòi 3 mệnh đề (taxonomy + tag capture-time + *"≥1 tài liệu end-user tích luỹ qua ĐÚNG skill export"*); feature `compound-learn-enduser-docs` chỉ giao acceptance HẸP của CONTEXT D8 ("≥1 doc tại capture-time") — một SUBSET; done-flip đánh đồng "feature tham chiếu PBI đã xong" = "PBI thỏa CoS" và mark `done` khi bước 3 (export skill) + bước 4 (backfill) chưa có. **Anh em STR72:** cùng lỗi gốc — tin tín hiệu cục bộ (feature-close) mà không reconcile ngược về nguồn thẩm quyền (PBI-CoS). **Áp fgOS y hệt:** cổng `done` fgOS (FSM compound-learn) ép item qua stage nhưng KHÔNG kiểm acceptance/CoS của chính item; một parent có thể `done` khi mới thỏa một phần con/CoS → cùng bug premature-done. **Đề xuất (bee-exploring shape chi tiết):** (1) done-flip đối chiếu TỪNG mệnh đề CoS với artifact thực-giao (evidence-linked), chỉ flip `done` khi mọi mệnh đề thỏa; thỏa một phần → giữ `in-flight` + liệt kê mệnh đề còn thiếu; (2) khi feature-acceptance (CONTEXT) HẸP hơn PBI-CoS, buộc khai rõ "feature này giao slice X của PBI", cấm auto-equate feature-done = PBI-done; (3) trong fgOS: cổng `done` của item kiểm acceptance/CoS của chính item, không chỉ FSM-stage. | Một PBI chỉ được flip `done` khi mọi mệnh đề CoS có evidence thực-giao; CoS thỏa một phần giữ `in-flight` và liệt kê phần thiếu; feature giao subset của PBI phải khai rõ slice, không đóng cả PBI. Dogfood: STR64 không tự flip `done` khi bước 3/4 chưa có | proposed | (chưa gán — ứng viên bee-exploring; năng lực fgOS + đồng thời vá bee) |
| STR72 | **Decision-supersession phải đồng bộ NGƯỢC về artifact nguồn (ticket/backlog/spec/CONTEXT), không chỉ ghi decision-log — nếu không, framing đã-bị-supersede tái xuất và tái sinh kết luận sai** (đào tận gốc 2026-07-21 từ hiện tượng "mọi phiên đọc STR64 là bee-tooling dù đã chốt fgOS"). **Root cause:** khi một quyết định đổi chiều (supersede), thay đổi rơi vào decision-log NHƯNG không lan về artifact nguồn đã gieo quyết định gốc — dòng STR64 trong `backlog.md` vẫn viết như bee-tooling, nên mọi phiên vào bằng entry-point tự nhiên (ticket ở mặt chữ) tái-suy kết luận cũ; bản sửa (`df90b761`) nằm ở artifact RIÊNG (log) họ không đối chiếu, và ban đầu còn chỉ "narrated in prose", chưa gọi CLI verb → chuỗi decision máy-đọc còn không thấy supersession một thời gian (phải tới `27052dd7` mới formal). Source cũ là attractor mạnh hơn một correction chôn trong log. **Áp cho fgOS y hệt:** một work-item đổi framing mà ticket/`docs/specs` không cập nhật → phiên fgOS tái-suy kết luận cũ; đây là năng lực compound-engineering fgOS phải có, không chỉ vá bee. **Đề xuất (cần bee-exploring shape chi tiết):** (1) supersede kèm bước bắt buộc "reconcile source artifacts" — cập nhật/đánh dấu ticket/backlog/spec/CONTEXT mà decision-bị-supersede đã trích; (2) hoặc backlog-row/spec mang con trỏ tới decision cai-quản, để người đọc thấy inline "framing dòng này đã bị supersede bởi X"; (3) consistency-check (grooming) phát hiện row/spec mâu thuẫn với một active decision đã supersede framing gốc; (4) supersede LUÔN qua CLI verb máy-đọc, không chỉ prose. | Sau khi decision A supersede decision B, artifact nguồn của B được cập nhật hoặc mang con trỏ tới A trong CÙNG đơn vị công việc (không để lệch xuyên-phiên); một check phát hiện được backlog-row/spec có framing lệch active-decision. Dogfood: dòng STR64 được reconcile (text nói rõ fgOS + con trỏ `df90b761`), và một phiên mới đọc STR64 không còn tái-suy bee-tooling | proposed | (chưa gán — ứng viên bee-exploring; năng lực fgOS + đồng thời vá bee) |
| STR71 | **Chất lượng câu hỏi gate (ask self-sufficiency, phòng ngừa — bổ trợ STR70a)** — người trả lời gate thường "không rõ nó đang hỏi gì", phải mở một vòng chat chỉ-để-hiểu-câu-hỏi trước khi bàn nội dung. Giải phòng-ngừa: cell viết `ask` đủ tự-đứng (kèm context để câu hỏi hiểu được ngay). KHÔNG thay STR70a (tư vấn/so sánh vẫn cần chat dù câu hỏi rõ); chỉ cắt phần vòng exploring bị đốt để giải mã câu hỏi. Chưa ưu tiên đợt đầu. | Một gate mới mang câu hỏi người đọc-hiểu-ngay, không phải hỏi lại "đang hỏi gì"; đo bằng giảm số lượt làm-rõ-câu-hỏi trước khi vào nội dung | proposed | (ứng viên — hậu str61-chat-context-continuity; xem `docs/history/gate-dialogue-continuity/CONTEXT.md`) |
| STR70a | **Checkpoint distillate + record chốt 3-phần lên gate (fgOS core, KHÔNG bị chặn)** — nửa không-vướng của STR70 (tách 2026-07-21, `docs/history/gate-dialogue-continuity/CONTEXT.md` D2/D3/D4/D5). Verb non-transitioning ghi distillate {câu hỏi diễn giải lại, options + trục so sánh, leaning + lý do quote nguyên văn người, đã-loại, còn-treo} lên `gates[id]`, LWW, chiếu qua `awaitingContext`; verb DÙNG CHUNG người/agent — nhưng ĐIỀU KIỆN TIÊN QUYẾT là fold `actor` vào gate record (hiện `gates[id]` chỉ giữ `ask`/`answer`/`parentSnapshotAtAsk`, KHÔNG actor — `repo/src/state/replay.mjs:126-134`) để phân biệt note-người vs tóm-tắt-agent. Record bền lúc chốt nở thành CẤU TRÚC 3 phần: vì-sao / mốc-trao-đổi / đánh-đổi (không còn `reason` một dòng). Tiền lệ verb non-transitioning đã có: `decision`/`outcome` (`repo/docs/specs/work-state.md:647-675`). Đều là fact bền hợp core, KHÔNG lật D1/STR46. | `awaitingContext` mang distillate + record 3-phần; `actor` chiếu vào gate phân biệt được nguồn; distillate human-overridable, chỉ `answer` authoritative | proposed | (đích cụm cổng-người, hậu STR69a — chờ STR64 đóng để đụng `repo/src/state/`) |
| STR69a | **Chiếu nguyên văn `ask` vào `awaitingContext` (gần như free, dọn đường STR70a)** — tách từ STR69 (2026-07-21, `docs/history/gate-dialogue-continuity/CONTEXT.md` D1). Dữ liệu ĐÃ lưu sẵn: `gates[id].ask` (`repo/src/state/replay.mjs:130-132`), và `computeAwaitingContext` (`repo/src/state/awaiting-context.mjs:49-73`) đã đọc đúng `view.gates[id]` để tính `changedSinceAsk` nhưng bỏ qua `ask` nằm ngay cạnh. Không dữ liệu mới, không verb/event/contract mới, KHÔNG đụng STR61-D3. Bước đầu tiên của cụm, làm trước STR70a. | `awaitingContext[id]` thêm nguyên văn `ask` gốc; envelope `list` cũ giữ byte-nguyên khi không áp dụng | proposed | (hậu str61; làm trước STR70a — chờ STR64 đóng để đụng `repo/src/state/`) |
| STR69b | **Vệt event gần nhất + diff giàu hơn khi người quay lại gate (cần chốt lại STR61-D3 trước)** — nửa còn lại của STR69. Khác STR69a ở chỗ có thiết kế thật cần chốt: nới `MATERIAL_FIELDS` (`repo/src/state/awaiting-context.mjs:15`, hiện chỉ `title`/`status`) và chọn bao nhiêu event chạm TRỰC TIẾP vào STR61-D3 ("chỉ báo khi thay đổi ảnh hưởng câu hỏi/trả lời gốc, im lặng nếu không") — nới bộ trường = nới định nghĩa "material" mà D3 cố tình giữ hẹp. Cũng là lớp validity-check cho STR70a (changed-since làm lộ leaning stale). Vẫn thuần read-projection, không lật D1/STR46; scope đợt đầu KHÔNG gồm sibling (trần D2 parent-only). | Người mở lại item awaiting-human phiên mới thấy vệt event gần nhất + diff parent giàu hơn; quyết định nới/không-nới `MATERIAL_FIELDS` ghi lại có lý do đối chiếu D3 | proposed | (hậu str69a; cần chốt lại STR61-D3) |
| STR67 | **Goal-directed planning: người dùng khai MVP/milestone, fgOS tự sắp xếp việc hướng tới nó nhanh nhất** (nêu ra + escalate 2026-07-20, trong lúc bàn P50 và soạn câu MVP nháp cho chính fgOS) — KHÔNG phải thói quen roadmap nội bộ của team fgOS, mà là **tính năng sản phẩm cấp cho người dùng cuối**: một người dùng fgOS xây agent app của họ tự khai mục tiêu (MVP/milestone) của dự án HỌ, và fgOS tự sắp xếp/ưu tiên backlog hướng tới mục tiêu đó nhanh nhất — câu MVP nháp của chính fgOS (`docs/history/p50-workflow-induct/reports/...` artifact 2026-07-20) trở thành ca dogfood ĐẦU TIÊN của chính tính năng này, không phải một quyết định nội bộ rời rạc. **Đã có building block liên quan** trong `work-graph-intelligence` (`src/state/graph-metrics.mjs`): `criticalPath(view)` (chuỗi phụ thuộc dài nhất, số bước tối thiểu) và `greedyTopUnblock(view, k)` (greedy xếp hạng việc chưa xong theo mức mở-khoá, thuật toán submodular-cover) — cả hai hiện tính trên TOÀN BỘ graph ("mở khoá mọi thứ"); tính năng mới cần biến thể SCOPED THEO GOAL (chỉ xếp hạng việc nằm trên đường tới một target đã khai, không phải mọi thứ). Cần bee-exploring riêng trước khi shape — còn gray-area thật: "goal" là work-item hay entity mới? liên hệ thế nào với dep-graph/parent hiện có? | Một decision record (CONTEXT.md) chốt rõ goal là gì (data shape), quan hệ với dep-graph hiện có, và biến thể goal-scoped của `criticalPath`/`greedyTopUnblock` chạy được — case-study: câu MVP nháp của chính fgOS được sequencing thật qua tính năng này, không phải xếp tay | proposed | (chưa gán feature — ứng viên bee-exploring) |
| STR66 | **Rename `src/state/domains.mjs` → `workflow-stage-graphs.mjs`** (nêu ra 2026-07-20, lúc bạn đọc thấy naming "domain" dễ nhầm với domain nghiệp vụ) — file thực chất là registry stage+transition-graph theo loại việc (`coding`/`synthetic`), không phải "domain" theo nghĩa DDD. Đã cân nhắc `workflow-shapes.mjs` vs `workflow-stage-graphs.mjs`, chọn vế sau vì D8 nhấn mạnh cạnh chuyển-trạng-thái (transitions) là phần chính, không chỉ danh sách stage — tên phải nói rõ có "graph". Việc rename thật đụng mọi nơi import (`work.mjs`, `frontier.mjs`, `loop.mjs`, `stage.mjs`) — refactor thật, chưa làm, chỉ ghi nhận | Rename xong, mọi import site cập nhật, `resolveDomainName`/`getDomain`/`stageForStep`/`DOMAINS`/`DEFAULT_DOMAIN` export giữ nguyên tên (chỉ đổi tên FILE), suite xanh | proposed | (chưa gán feature) |
| STR65 | **Worktree isolation cho phiên bee song song trong cùng checkout** (điều tra tận gốc 2026-07-20, xuất phát từ 3 lần commit lạ `"worker: output.txt"` đè mất dòng `docsRef` trong `bin/fgos.mjs` giữa lúc p50-workflow-induct Phase 2 thi công) — xác nhận bằng transcript trực tiếp (`~/.claude/projects/.../d728e552-....jsonl`, 2026-07-19 17:41:46): KHÔNG phải bee tự động (bee không có cơ chế tự git-commit), KHÔNG phải bug test suite (`test/runner/loop.test.mjs:29` cô lập đúng qua `fs.mkdtempSync`), KHÔNG phải tiến trình lạ trên máy (ps/cron/systemd đã kiểm, không có gì chạm checkout) — là **3 phiên bee ĐỘC LẬP chạy thật, song song, trong CÙNG một checkout**: `fgos-sample-testbed` (dispatch `fixture-worker-multiply`/`builder-fgos-sample-testbed-1`), `str60-fgos-return-cleanliness-fix`, `p50-workflow-induct`. Cơ chế: race trên `.git/index` dùng chung — đúng critical-pattern đã ghi ngày 20260718 ("git-add-scope": broad add/commit quét nhầm thay đổi chưa commit của phiên khác). AGENTS.md rule 14 đã có paved road (`bee worktree new --feature <slug>`) nhưng không phiên nào trong 3 phiên trên dùng. **Thêm bằng chứng (2026-07-20, grooming p50-workflow-induct):** một occurrence khác cùng nguyên nhân gốc, filed riêng làm review-finding #1 của p50-workflow-induct's review (đóng làm duplicate của STR65 này, không fix riêng) — 2 sự cố cụ thể có SHA: commit `b63c530` âm thầm nuốt một sửa test của cell này vào commit đã amend của phiên khác; commit `d2fd615` xoá mất 7 dòng vừa commit (docsRef wiring trong `bin/fgos.mjs`), gây regression 2 test CLI. Chi tiết đầy đủ: `.bee/backlog.jsonl` (title "worker: output.txt" auto-commits, 2026-07-19T10:40:40Z). **Escalation count (grooming failure-analysis, 2026-07-20):** đây KHÔNG phải sự cố hiếm — đếm được 12 occurrence riêng biệt trong `.bee/backlog.jsonl`, leo thang P3→P2→P1 trải dài 2026-07-17 tới 2026-07-19 (reservation bị giữ bởi phiên khác, `state.json` bị race, `git add -A`/`git commit -- <pathspec>` quét nhầm 487 file của phiên khác, subagent tự commit dù được dặn để nguyên, v.v.). Nghi vấn thêm: test flaky lặp lại ở `test/state/events.test.mjs`'s concurrent-OS-process seq test (thấy lại 2026-07-20 trong groom-cleanup-1, "pass khi chạy riêng/pass lại lần 2" — ĐÃ bị kết luận là flaky nhưng KHÔNG được đối chiếu với STR65) có thể là canary của chính race này, không phải flaky thuần. Cơ chế fix đã tồn tại từ 2026-07-17 (`fgos-multi-session-checkout`, `bee worktree new --feature <slug>`) — vấn đề còn lại là KHÔNG CÓ GÌ ép buộc dùng nó, không phải thiếu cơ chế | 3+ phiên bee đồng thời trong cùng checkout dùng `bee worktree new` thay vì share trực tiếp; qua N phiên song song kế tiếp không còn commit lạ tái diễn | proposed | (bee-core hoặc workshop tooling — chưa gán feature) |
| STR63 | **Frozen-judge tương đương cho fgOS** (nêu ra 2026-07-20, lúc so sánh kiến trúc bee↔fgOS) — fgOS đã có `footprintOverlap`/`footprintConflicts` (work-graph-intelligence S9, `graph-metrics.mjs:371`, `store.mjs:587`) ngừa xung đột GIỮA 2 work-item TRƯỚC dispatch (so `footprint` tự khai của item A với item B, gợi ý sequence/hoist/re-slice). Bee's frozen-judge (`cells.mjs:679-750`) giải bài toán KHÁC, bổ sung chứ không trùng: kiểm SAU khi return xong, MỘT item có tự ý đụng nhóm file nhạy cảm (test/CI/lockfile/manifest) NGOÀI phạm vi `footprint` đã khai của CHÍNH nó không — fgOS hiện chưa có lớp này | Một cơ chế (trong return-gate hoặc goal-check) flag khi files-changed thực tế của 1 work-item đụng nhóm nhạy cảm ngoài footprint đã khai — advisory, không tự fail, giống tinh thần frozen-judge | proposed | (chưa gán feature) |
| STR62 | **Sibling-aware / agent-driven on-demand cross-item context** (deferred idea từ STR61 exploring, 2026-07-19): hai option bị loại ở STR61-D2 — (a) tự động kèm cả sibling (không chỉ parent) khi trả lời gate người, (b) agent tự quyết kéo thêm bối cảnh cha/sibling động thay vì cố định. Cả hai cần khái niệm "sibling" mới trong data model (hiện chỉ có `parent`/`deps`/`blocks`, `work-state.md:864`) — không xây bây giờ, chỉ mở lại nếu baseline "parent-only" của STR61 chứng minh không đủ trong thực tế | Chưa xác định — chờ bằng chứng thiếu-bối-cảnh thật từ STR61 chạy production trước khi chốt CoS | proposed | — |
| STR51 | Skill LLM-assist trước `submit`: nhận text tự do kiểu "thêm vào backlog: làm X", chạy một LLM đơn giản để gợi ý phân loại (tier/kind/domain/risk) rồi mới gọi `fgos submit`/`add` với field đã điền — thay vì chỉ có `classify.mjs` cơ học (đếm từ khoá, D1/D5 `classify.mjs`) và `--domain` thủ công (fold-về-coding nếu bỏ qua, `domains.mjs:81-90`) như hiện nay. Không phạm RUL42 (runner) (stance STR42, decision `f69951df`): đi qua đúng cửa field-trên-item — skill TỰ GHI field đã suy luận qua verb chuẩn, giống hệt một người tự tay truyền `--domain`/`--risk`, không phải orchestrator/picker gọi LLM trong vòng chọn-giao. Cân nhắc khi thiết kế: `submit` hôm nay không có flag override cho tier/kind/risk (chỉ `add` có) — có thể cần thêm override flags cho `submit`, hoặc skill route qua `add`. Khác STR8 (intent-scoring — chấm priority SAU khi item đã tồn tại, dùng graph metrics) và khác D12 của p50-workflow-induct (domain-classifier thông minh, deferred tới khi có domain thứ hai thật) — STR51 rộng hơn D12 (gồm cả tier/kind/risk, không chỉ domain) và là một SKILL client-side, không phải thay đổi trong picker/orchestrator. Nêu ra 2026-07-18 trong lúc bàn p50-workflow-induct; không thuộc scope STR50. **Chốt 2026-07-18 (bàn cùng ngày với STR50): gộp vào Phase 2 (skill authoring) của p50-workflow-induct thay vì tách phase riêng — thiết kế đúng (skill ở tầng gọi trước submit, không sửa lõi) không đụng external-provider vào core code nên không đổi mode `standard` của STR50.** | Một skill nhận text tự do, gợi ý được tier/kind/domain/risk qua LLM, rồi tạo work-item thật qua verb chuẩn (submit hoặc add) với field đã điền — không có đường ghi riêng, không sửa picker/orchestrator; quyết định record trả lời rõ đi qua submit (cần override flags mới) hay qua add | proposed | p50-workflow-induct (Phase 2) |
| STR52 | **Domain thứ hai THẬT: marketing** (nêu ra 2026-07-18 trong lúc bàn STR50/D12) — invalidate tiền đề của D12 ("hiện chỉ có 1 domain thực"): người dùng có sẵn workflow marketing ở MỘT PROJECT KHÁC và muốn điều phối marketing qua fgOS. Hạ tầng kernel đã chứng minh đa-domain chạy được về nguyên tắc (domain `synthetic` sống song song `coding` hôm nay, STR18 base-workflow+domain-extension đã done), nhưng marketing THẬT còn thiếu: (a) registry entry riêng trong `domains.mjs` (stages/stepMap/transitions cho chu trình marketing, khác `clarify/decompose/executing`), (b) retrofit `discovery.mjs`/`decompose.mjs` — hai module này đang hardcode literal stage-name của coding, domain khác vào `clarify`/`decompose` sẽ bị ghi đè sai (cảnh báo đã có sẵn trong comment `domains.mjs:29-34`), (c) bộ skill nội dung riêng cho domain-extension "marketing" (song song bộ STR50 đang trích cho coding, KHÔNG phải `fgos-routing` — module đó domain-agnostic, không cần đổi), (d) STR51 mở rộng thêm field `domain` vào output classify (chỗ nối tự nhiên — cùng cơ chế, chỉ thêm 1 field). **CÂU HỎI SCOPE CHƯA TRẢ LỜI (quan trọng, quyết định đây là 1 feature hay 2):** project marketing kia dùng CHUNG một store/event-log với fgOS hiện tại (khác domain field trên cùng một hệ), hay là một CÀI ĐẶT fgOS RIÊNG cho project đó? Khuyến nghị: không gộp vào STR50 đang chạy (đã khoá scope hẹp cho coding, mode standard, đang thực thi) — mở feature riêng qua bee-hive/bee-exploring khi câu hỏi scope trên có câu trả lời. | Một decision record trả lời rõ câu hỏi scope (chung store hay cài đặt riêng); nếu chung store: registry entry `marketing` thật + retrofit discovery/decompose + bộ skill marketing chạy được một case study thật, domain `coding` không bị ảnh hưởng | proposed | — |
| STR46 | **Hợp đồng giao tiếp VÀO/RA của fgOS như một bề mặt I/O hạng nhất** — review chặt + hợp nhất các mảnh in/out đang rải rác theo từng stage thành MỘT hợp đồng mạch lạc, để bất kỳ transport nào (terminal, pane, chat, web, thứ chưa nghĩ ra) cắm vào đồng nhất và fgOS **chủ động được cả chiều ra**, không chỉ bị poll (chống "bị động"). Hiện trạng cần soát: chiều VÀO đã có cửa chuẩn (verb/CTR001, Host Adapter `b2d18cc7`, listener STR38) nhưng đường vào rải theo stage (submit STR14, answer/async-human-gate STR19, pull-door take/return STR26); chiều RA đang **bị động** — envelope + poll list/catchup + data_hash (STR37), log worker STR39, còn **đẩy/khởi-xướng (attention-envelope CTR008) thì deferred "chờ lực kéo"**. Việc: kiểm toàn bộ điểm in/out, chốt một hợp đồng in-out **tự-mô-tả** (vào=verb/CTR001 + identity gate; ra=envelope + attention/CTR008 push + self-description để thêm surface là dịch-transport→verb + đọc-envelope, không đường ghi riêng), và **nâng chiều-ra/khởi-xướng thành hạng nhất** thay vì phần phụ. **Câu hỏi CỬA-CHUẨN (escalate 2026-07-18, trong scope review):** bản thân "cửa chuẩn giao tiếp với người" CHƯA CHẮC là CLI-verb-spawn hôm nay — spawn-rồi-chết về bản chất không giữ kết nối để đẩy, nên "bị động" là hệ quả TẤT YẾU của model này, không phải bug. Ba mô hình phải cân: (1) CLI-spawn = cửa (b2d18cc7 hiện tại); (2) một **daemon nuôi MỘT cửa protocol chuẩn máy-đọc — không phải UI** (socket/JSON-RPC/MCP) để CLI+UI+remote đều là client, push/stream/remote thành bản chất — đây đúng là kiến trúc INTERFACE của herdr (không phải runtime của nó); (3) core-library + adapter mỏng (hợp STR41 executor-port), daemon chỉ bật khi cần remote/push. **CHỐT hướng 2026-07-18: model (3), Ở MỨC INTERFACE.** Contract chuẩn = **SCHEMA event-log + giao thức append/read/subscribe** (fgOS đã event-sourced), **KHÔNG phải một lib link được**. **Lib chỉ là CLIENT tham chiếu (Node) của contract đó**, không phải bản thân contract → daemon/UI-backend có thể viết bằng ngôn ngữ khác miễn nói đúng log-format (chống Node-monoculture, hợp mục tiêu substrate đa-app). CLI = adapter local dùng lib; daemon = consumer (xem chốt dưới); CLI standalone chạy đủ. **Chỉ code CÙNG-TIẾN-TRÌNH (CLI, TUI local) mới gọi lib TRỰC TIẾP; mọi UI web/mobile/remote KHÔNG chạm lib — chúng là client của CỬA MẠNG do daemon phơi ra → daemon là điều kiện CẦN cho bất kỳ UI ngoài terminal.** **Prerequisite móng:** tách core (verb-logic) thành lib gọi được độc lập CLI — refactor nội bộ, không đổi hành vi. **Phạm vi quyết định này CHỈ là interface (hình dạng cửa); tầng OWNER — ai ghi, đồng thời, lock CTR002, nguyên-tử STR45 — là layer RIÊNG, CHƯA xét ở đây.** **CHỐT thêm 2026-07-18: daemon RA KHỎI core — là CONSUMER giao tiếp QUA CLI** (`spawn fgos <verb>` cho ghi + poll `fgos list`/`rollup` + data_hash cho chiều-ra), KHÔNG link lib. Hệ quả: daemon thừa hưởng identity-gate + validation + single-door-lock của CLI miễn phí, không chế được đường ghi mới → **b2d18cc7 (Host-Adapter) được GIỮ + FULFILL, KHÔNG bị siêu-việt** (daemon chính là "lực kéo" mà CTR008 chờ) → **không cần decision-record-phá-luật.** Push nằm ở consumer sống-lâu (poll rẻ + giữ kênh outbound); core fgOS vẫn passive (chỉ CLI+log). Đây là **airemote tổng quát hoá** (consumer spawn-verb + poll + bridge remote). Sub-choice deferrable: poll-qua-CLI (đơn giản, một đường — nghiêng cái này trước) vs tail-event-log (push tức thì nhưng khoá log-format). CHỈ nếu sau muốn daemon **link-lib in-process** (3b) mới đụng b2d18cc7 → khi đó mới cần record. Kèm: **CLI tự nó phải chuẩn hoá lại** (verb surface + envelope + exit-code + self-describing schema) dù chọn model nào. Reference kiến trúc cửa đã có trong distill: `herdr:socket-api-control-surface`, `herdr:self-describing-protocol-schema`, `herdr:session-snapshot-bootstrap-rpc`. **Trước mắt (increment đầu, không chờ contract xong):** giao tiếp trực tiếp qua terminal/agent + vài pane thiết kế riêng show trạng thái work-item; nói với terminal-agent "vào tiến trình trả lời item X" / "vào exploring item X" rồi chat tiếp — chat modal item-scoped là client ĐẦU TIÊN của cổng. Là móng nằm DƯỚI STR38/STR39/STR40 và mọi UI sau; đứng trên STR37. Chất liệu distill tham khảo: `docs/distillery/deep-dives/herdr-vs-tmux-observation.md` (xưởng — vì sao surface là client của cổng, không phải runtime), airemote (path-allowlist/send-confirm cho bề mặt chat-untrusted) | Có một spec hợp đồng in/out (vào=verb/CTR001, ra=envelope+attention/CTR008, tự-mô-tả) mà một surface MỚI cắm vào chỉ bằng dịch transport→verb + đọc envelope/attention, không đường ghi riêng, caller chưa xác danh không gọi verb mutation; chiều-ra khởi-xướng ("cần bạn") có đường đi hạng nhất chứ không chỉ poll; increment terminal/pane + chat item-scoped ("vào answer/exploring item X") chạy thật trên cổng đó | proposed | — |
| STR48 | **Kênh attention/push ("cần bạn") như một subsystem có delivery-semantics** — tách từ STR46 vì chiều-ra chủ động KHÔNG phải bolt-on mà là hệ con riêng, đòn bẩy UX cao nhất + rủi ro cao nhất. Phải chốt 5 thuộc tính: **at-least-once** (item chờ lâu chắc chắn người được ping, không rơi), **dedup** (một gate không ping N lần), **routing** (người/thiết bị/kênh nào), **ack** (đã xem chưa), **escalation** (không đáp trong ngưỡng → ping lại / đổi kênh / đổi người). Sống ở **CONSUMER daemon** (poll data_hash STR37 / tail log STR39 + giữ kênh outbound), **core fgOS vẫn passive** — đây là chỗ hiện thực hoá "không bị động" mà lõi KHÔNG mọc organ push nội tại. Đứng trên STR46 (contract in/out), STR39 (log substrate), STR37 (poll rẻ). Reference pattern: airemote notify (at-least-once, watcher-fed) trên herdr-gateway; herdr `notification.show`. | Một spec kênh attention nêu rõ 5 thuộc tính (at-least-once/dedup/routing/ack/escalation); một "cần bạn" thật của item chờ-người được giao ĐÚNG-MỘT-LẦN tới người, có ack, không đáp trong ngưỡng thì escalate đúng — chứng minh trên item thật, core không thêm trạng thái push nội tại | proposed | — |
| STR47 | Audit thống nhất **loại-hình công việc** + **hệ đặt tên/đánh mã** đang chạy song song trong hệ thống — chưa có review nào gộp chung: (a) `kind` work-item (`classify.mjs`: bug/feature/chore/docs, mặc định `task`) — đủ chưa, thiếu loại nào (vd spike, review); (b) nhiều hệ mã số tách biệt cùng sống mà chưa giải thích ranh giới ở một chỗ: P# tuần tự (docs/backlog.md, PBI sản phẩm), D-hex (decision record, per-feature CONTEXT.md + `.bee/decisions.jsonl`), R# (rule trong docs/specs/runner.md), C1-C9 (contract trong architecture-map.md), cell-id `<feature>-N` (bee); (c) tên feature kebab-case tự đặt tay, không qua cửa sinh id nào — dễ trùng/lệch nghĩa khi hai việc đặt tên gần nhau | Một bảng liệt kê đủ mọi loại-hình + hệ mã đang dùng trong hệ thống (nguồn trích từ code/docs thật, không suy đoán); decision record trả lời: giữ đa hệ có chủ đích (mỗi hệ phục vụ đúng một tầng, ghi rõ ranh giới dùng-khi-nào) hay hợp nhất/đổi tên chỗ nào; nếu giữ đa hệ thì ranh giới đó được tài liệu hoá một chỗ (vd phụ lục architecture-map hoặc reading-map) | proposed | [id-systems-audit (draft, chờ chốt)](id-systems-audit.md) |
| STR7 | Trường priority cho frontier ordering. **Chốt qua thảo luận RUL42 (runner) (per STR38, 2026-07-17):** khi STR38 tồn tại, priority có thể nhận một GỢI Ý từ người qua chat — STR7's thiết kế đọc gợi ý đó làm một tín hiệu đầu vào (không phải field cuối áp thẳng) khi tính field priority thật | FIFO-theo-seq lộ giới hạn thật được ghi nhận; nếu tiêu thụ gợi ý người từ STR38, field priority cuối vẫn do một bước tính/ghi duy nhất quyết, không phải chat ghi thẳng | proposed | — |
| STR8 | Intent-scoring + signal-driven chaining (hướng đã chốt từ consult). **Chốt qua thảo luận RUL42 (runner) (per STR38, 2026-07-17):** khi STR38 tồn tại, intent có thể nhận một GỢI Ý từ người qua chat — STR8's thiết kế đọc gợi ý đó làm một tín hiệu đầu vào (không phải field cuối áp thẳng) khi tính intent-score thật | Phase 2 đóng; có case cần reactive dispatch; nếu tiêu thụ gợi ý người từ STR38, field intent cuối vẫn do một bước tính/ghi duy nhất quyết, không phải chat ghi thẳng | proposed | — |
| STR36 | Metric riêng cho từng fix của vòng evolving (STR13 D4), chỉ mở khi entropy-trend chứng minh không đủ nhạy ở quy mô 1 fix/lần | Có điểm số đo được trước/sau một fix đơn lẻ, độc lập với entropy-trend tổng | proposed | — |
| STR27 | Orchestrator service tầng fleet: registry worker + heartbeat + push assignment + lease/liveness + capacity + giao thức/auth cho worker từ xa — đắp TRÊN cùng event log (stage-decompose D1: pull là móng, không đập lại) | Chỉ mở khi có nhu cầu fleet thật (worker từ xa/luôn-bật); thiết kế đứng trên log + claim sẵn có | proposed | — |
| STR38 | Human-UI listener (web/chat) trên đất Host Adapter (D b2d18cc7): translator nhận transport (HTTP/chat) → dịch sang verb → spawn `fgos <verb>` → đọc envelope + exit code; kèm identity gate "ai được nói verb nào" trước khi dịch xuống CTR001 (trust invariant dispatch.mjs: `verify` chạy như shell — intake nguồn chưa kiểm là injection vector); KHÔNG fgos-serve in-process khi CTR002 một-cửa-ghi còn per-process; chiều ra tạm poll list/catchup + so data_hash, CTR008 attention envelope chờ lực kéo thật. Đứng trên STR37. **Chốt qua thảo luận RUL42 (runner) (per bàn STR42, 2026-07-17):** STR38 bao gồm một verb chat cho người GỢI Ý priority/intent (phục vụ STR7/STR8) — verb này ghi một field GỢI Ý, KHÔNG PHẢI field cuối cùng; bộ não thông minh ở giai đoạn planning/scoring (clarify/decompose hôm nay, STR7/STR8 mai sau) đọc gợi ý này làm một tín hiệu đầu vào rồi TỰ quyết field cuối qua cửa ghi chuẩn — có thể theo đúng gợi ý hoặc đổi khác nếu thấy hợp lý hơn, nhưng gợi ý luôn được cân nhắc, không bao giờ bị bỏ qua và không bao giờ tự nó là giá trị cuối. Giữ đúng RUL42 (runner) (đúng một actor ghi field cuối, picker vẫn cơ học) | Một người dùng qua web/chat submit được vấn đề, trả lời ask, duyệt approve/reject, VÀ gợi ý priority/intent cho một item — tất cả qua verb CTR001, không đường ghi riêng; caller chưa xác danh không gọi được verb mutation; gợi ý priority/intent của người được planning-stage đọc và cân nhắc (không tự động trở thành field cuối) | proposed | — |
| STR75 | **Cô lập cwd của test e2e/runner để fixture không rò rỉ vào working tree repo thật (FIX GỐC của STR65 "worker: output.txt")** (truy ra 2026-07-21 khi test `fgos session`). **Root cause đã xác định:** các test dựng "worker giả" ghi `output.txt`="produced by worker" rồi runner commit nó — `test/e2e/{runner-loop,pr-gate,self-improve-loop,synthetic-domain}.test.mjs` + `test/runner/{loop,worktree}.test.mjs` (`writeCommittingExecutor`/`commitOnWorktree`, đường dẫn tương đối theo cwd). Khi cwd không cô lập chặt, artifact rơi vào `repo/` thật; lịch sử `git log -- output.txt` chứng minh tái diễn: `30a9277 worker: output.txt` → `87c8a39 chore: remove ... accidental e2e-fixture artifact` → `595db87 worker: output.txt` (thêm lại). Đây là nguồn "worker: output.txt" auto-commit mà STR65 từng ghi "source chưa xác định". **Đề xuất (bee-exploring/small shape):** mọi test tạo artifact/commit trong temp/throwaway checkout riêng (mkdtemp) và tự dọn; hoặc runner-trong-test bị chặn commit ra ngoài worktree được cấp. | Chạy full suite (`npm test`) KHÔNG để lại file lạ nào trong working tree `repo/` (git status sạch trước và sau); mọi fixture worker ghi/commit trong sandbox riêng; không còn commit "worker: output.txt" tái sinh | closed (verified non-bug) | **ĐÓNG 2026-07-21 — giả thuyết bác bỏ (verified):** test ĐÃ cô lập (mkdtemp + cwd tường minh ở mọi call, đọc code 6 file); `npm test` để `repo/` sạch 5 lần liên tiếp; 3 SHA "worker: output.txt" (17-18/7) là commit message của CHÍNH fixture test (`test/runner/loop.test.mjs:131` v.v.), rơi vào `.git` thật chỉ khi runner dispatch fixture executor trong lúc dogfood/concurrent chạy vào checkout DÙNG CHUNG `.git` — tức lõi STR65, KHÔNG phải bug test-isolation. Artifact đã bị STR74 (gitignore) trung hoà. Fix gốc thật = STR65 enforcement (cô lập concurrent/dogfood khỏi shared `.git`). Không refactor test. |
| STR70b | **Raw backstop cho cuộc bàn ở gate — CHỜ Q1 (chưa có nhà hợp lệ)** — nửa bị-chặn của STR70. Giữ lượt thô để khi người phủ định distillate thì đưa lại nguyên văn, drop khi chốt. Đào 2026-07-21 (`CONTEXT.md` Q1) LOẠI O1 (raw thành event core): log fgOS append-only bất khả xâm phạm (L3 `platform-foundations.md:73-74`; RUL11 `work-state.md`; KHÔNG cơ chế redact/prune/TTL nào trong `repo/src`) → "drop khi chốt" bất khả thi. Còn lại: O2 (kho phụ trong core, không phải log) vướng L1 (`platform-foundations.md:17-20` — mọi dữ liệu bền phải là log HOẶC state; raw không thuộc ô nào) ⇒ phải mở L1; O3 (daemon-consumer) đúng nhà theo decision `0014` nhưng lớp đó CHƯA TỒN TẠI (không file daemon/consumer trong `repo/src`\|`repo/bin`; STR46/STR48 còn proposed). HOÃN có chủ đích: quyết bằng số đo thật (người phủ định distillate bao nhiêu lần) chứ không suy luận; luồng phủ định không gãy khi thiếu raw vì distillate vốn không-authoritative. | Q1 chốt được nhà cho raw (O2 mở L1 / O3 chờ consumer) dựa trên tần suất phủ định thực tế; tới lúc đó STR70b nằm chờ, không chặn STR70a | proposed | (hậu STR70a; blocked-on Q1 — `docs/history/gate-dialogue-continuity/CONTEXT.md`) |
| STR76 | **[MVP-BLOCKER · P1, DONE] Sau khi cài bản shipped, KHÔNG có đường discoverable-qua-PATH để khởi động vòng tự hành — người mới submit 1 request KHÔNG BAO GIỜ nhận lại code merge được (autonomous loop never starts)** (tìm ra 2026-07-21 qua đo gap MVP 0016; cả desk-audit lẫn live dry-run npm-install-thật đâm CÙNG bức tường). Cụ thể: `package.json` `bin` chỉ map `fgos` (KHÔNG có entry `fgos-runner`); `fgos init` KHÔNG sinh `.fgos-runner.json`; bước doc chỉ dẫn `fgos discover <id>` chết `ENOENT .fgos-runner.json`, không có hướng khắc phục nào được doc (`bin/fgos.mjs:530-542,633-634`; `docs/history/mvp-gap-measure/reports/dry-run-findings.md:129-156`). KHÔNG phải "loop kẹt cần người gỡ" — tệ hơn: loop KHÔNG TỚI ĐƯỢC chỗ con người có thể can thiệp. | Sau `npm install -g` bản shipped + `fgos init` + `fgos submit "<text>"`: có đường DISCOVERABLE (in PATH / doc rõ) chạy `fgos-runner --once`; `.fgos-runner.json` được sinh (bởi `init` hoặc runner tự tạo default) chứ không đòi người tạo tay; `fgos discover`/`fgos-runner --once` không còn chết ENOENT; item submit đi qua intake → dispatch. Dogfood: một người lạ chạy trọn install→submit→(runner)→proposal, evidence quote được. **Delivered (mọi vế CoS có bằng chứng):** `fgos-runner` là npm bin entry thật, discoverable qua PATH (evidence: `test/install-packaging.test.mjs` e2e cài thật); `.fgos-runner.json` tự sinh tại đường mặc định qua `ensureRunnerConfig` (evidence: `test/runner/dispatch.test.mjs`); `discover`/`fgos-runner` không còn chết ENOENT (evidence: `test/cli/fgos.test.mjs`); **dogfood thật đầy đủ** — `npm install -g` từ gói pack thật → `fgos init` → `fgos submit` → `fgos-runner --once` với PATH KHÔNG bị khử, gọi được `claude` CLI thật, item đi hết `intake → dispatch` và `proposed` trên nhánh `fgw/tsk-ol5` (exit code 0) — evidence: `docs/history/str76-runner-bootstrap/reports/str76-runner-bootstrap-e5.md`. | done | str76-runner-bootstrap — xem `docs/history/str76-runner-bootstrap/` |
| STR77 | **[MVP doc gap · P2] `fgos submit --help` mâu thuẫn hành vi shipped** (đo gap MVP 2026-07-21) — help ghi `required: --text` (một flag) nhưng arg thật là POSITIONAL (`fgos submit "<description>"`) — `src/cli/command-registry.mjs:69,74`, `bin/fgos.mjs:610,1897`. Người mới tự sửa qua error string, nhưng doc-shipped vẫn sai. | `fgos submit --help` mô tả đúng arg positional; help khớp hành vi thật | done | str77-79-doc-gap-fixes (docs/history/str77-79-doc-gap-fixes/) |
| STR78 | **[MVP doc gap · P2] Link "Documentation" trong README CHẾT trong gói npm shipped** (đo gap MVP 2026-07-21, live dry-run bắt — desk-audit không thấy vì đọc in-repo chứ không phải tarball) — `package.json` `files` allowlist (`bin`,`src`,`README.md`,`LICENSE`) loại `docs/` + `.agents/`, nên mọi doc README trỏ tới KHÔNG ship (`docs/history/mvp-gap-measure/reports/dry-run-findings.md:166-170`). Người lạ mở link → không có file. | Doc người-dùng-cuối cần cho install→submit→merge được ship trong gói npm (thêm vào `files`, hoặc README trỏ URL sống); không còn link chết trong package cài thật | done | str77-79-doc-gap-fixes (docs/history/str77-79-doc-gap-fixes/) |
| STR79 | **[MVP doc gap · P2] `--help` không nhất quán theo subcommand** (đo gap MVP 2026-07-21) — `submit`/`discover`/`take --help` exit 4 + báo lỗi một dòng thay vì help thật; `init --help` lặng lẽ bỏ qua flag và rerun init (exit 0) (`dry-run-findings.md:160-165`). `--help` không phải đường khám phá đáng tin cho người mới. | Mọi subcommand `--help` in help thật (exit 0), không exit-4-báo-lỗi, không rerun-tác-dụng-phụ | done | str77-79-doc-gap-fixes (docs/history/str77-79-doc-gap-fixes/) |
| STR74 | **Lưới an toàn chặn test-leak artifact bị commit (belt cho STR75, cắt cơ chế clobber STR65 nhanh)** (2026-07-21). Bổ sung `repo/.gitignore`: `output.txt`, `*-output.txt` (bọc họ fixture `worker-output.txt`/`synth-output.txt`/`coding-output.txt`), `.fgos/sessions.json` (ephemeral runtime, cùng lớp `.fgos/state.json` đã ignore); và `git rm --cached output.txt` (rác đang tracked từ `595db87`, không xoá logic). Chỉ là lưới — KHÔNG thay STR75 (fix gốc cô lập cwd); và không chặn được nếu code `git add` đúng tên file, chỉ chặn kiểu quét rộng `git add -A` (đúng cơ chế clobber STR65). | `output.txt`/`*-output.txt`/`.fgos/sessions.json` được gitignore; `output.txt` không còn tracked; `git add -A` không quét lại được các file này; `npm test` xanh | done | [gitignore-test-leak-artifacts](history/gitignore-test-leak-artifacts/) — commit `7581bb2`; verify xanh (3 pattern ignored, output.txt untracked, state nguồn nguyên). STR75 (fix gốc) vẫn proposed |
| STR59 | **Sample project nhỏ để test khả năng vận hành thật của fgOS** (nêu ra 2026-07-18, trước khi tiếp tục STR50) — STR25 đã dogfood một lần bằng chính backlog/repo forgent làm test subject (2 item thật đi trọn vòng, `docs/history/dogfood-p25-run1/`); STR50 Phase 3 cũng cần một case-study thật nhưng đang deferred tới khi Phase 1+2 của STR50 xong (`docs/history/p50-workflow-induct/plan.md` Gate 2 #3). Việc: chốt qua bee-exploring xem sample project này target cái gì (fgOS hiện tại đã ship, hay layer skill-induction STR50 tương lai, hay cả hai), sống ở đâu (fixture trong `repo/`, repo riêng disposable, hay tái dùng chính repo forgent kiểu STR25), và có phải regression fixture bền hay chỉ chứng minh một lần. **Chốt 2026-07-18 (bee-exploring, `docs/history/fgos-sample-testbed/CONTEXT.md`):** cả hai target tuần tự (fgOS hôm nay trước, layer skill STR50 sau khi ship); fixture riêng trong `repo/`, không tái dùng backlog thật; regression fixture bền, giữ lại; Pass 2 của fixture này CHÍNH LÀ case-study STR50 Phase 3 đang deferred (một fixture, hai CoS) | Một decision record (CONTEXT.md) chốt rõ target/vị trí/độ bền của sample project và quan hệ với case-study đang deferred của STR50 Phase 3; nếu quyết xây mới, sample project chạy được ít nhất một vòng qua fgOS thật, evidence quote được (không phải "should work") | done | [fgos-sample-testbed](history/fgos-sample-testbed/) — Pass 1 (multiply/divide) + Pass 2 (power(), chạy trên lane p50-workflow-induct) cả hai đi trọn vòng qua fgOS thật, evidence quote được; không còn việc gì dở trên lane này |
| STR64 | **[fgOS work — per decision `df90b761`, KHÔNG phải bee-tooling; mọi tham chiếu `bee-*`/`scribing`/`compounding`/`docs-manager` dưới đây là NGUỒN DISTILL, không phải đích sửa — xem D7 của compound-learn-enduser-docs]** **Reconcile map:** "scribing+compounding" → khâu compound-learn của fgOS; "skill export/docs-manager/documentation-management.md" → skill export/tích luỹ đọc-theo-tag của fgOS (CHƯA xây). **Đã giao (feature compound-learn-enduser-docs, ~slice 1-2, reviewed):** bước (1) taxonomy Diataxis + bước (2) tag `docType` capture-time + 1 doc how-to thật. **CÒN LẠI (lý do reopen từ `done`, per STR73 premature-done + decision `f5f7f6d3`):** bước (3) skill export/tích luỹ đọc-theo-tag (soát `docs-manager`/`documentation-management.md` trước khi viết mới, YAGNI) + bước (4) backfill; mới 1/4 quadrant (how-to). **[bước-3 shape locked 2026-07-21, decision `1d336d8a` / CONTEXT D10-D14]:** bước-3 giao **INDEX đọc-theo-tag** (machine-readable, key theo quadrant + purpose/audience + source-capture linkage), KHÔNG gộp/viết lại doc; docs-manager/ck:docs/documentation-management.md đã soát — không tái dùng được, build skill fgOS mới (D11). CoS bậc-3 diễn giải lại + **user-accepted**: index đóng bước-3, phần gộp tài liệu ("gộp sống") là slice sau. **Row KHÔNG flip `done` chỉ vì xong bước-3** (per STR73). — nguyên văn ticket gốc (design reference): **Compound-learn embodiment (p50-workflow-induct slice 2) + tài liệu hướng end-user** — D6 của p50-workflow-induct đã defer "compound-learn" sang slice 2 từ đầu. Đào sâu `bee-compounding`/`bee-scribing`/`feedback.mjs` (2026-07-20) cho thấy bee có input cơ học ổn (CONTEXT/plan/report/cell-trace) và output "spec hiện trạng" ổn (`docs/specs/`, việc của scribing), nhưng **thiếu hẳn trục tài liệu HƯỚNG END-USER** (installation guide/runbook/usage/domain-knowledge/workflow-guide/Q&A) — bee chỉ phân loại theo trục kỹ sư (`pattern/decision/failure`). Đề xuất, người dùng đã đồng ý toàn bộ 2026-07-20, xếp theo impact: (1) chốt taxonomy trước — map vào khung **Diataxis** (Tutorial/How-to/Reference/Explanation) thay vì bịa danh sách riêng; (2) gắn tag phân loại NGAY tại khâu `scribing`+`compounding` lúc ghi nhận (field nhỏ thêm vào, không xây cơ chế mới) — tránh dồn nợ raw-chưa-phân-loại; (3) SAU ĐÓ mới xây skill export/tích luỹ đọc theo tag — soát lại `docs-manager` agent + skill `docs`/`documentation-management.md` đã có trước khi viết mới (YAGNI); (4) backfill nội dung cũ (critical-patterns.md, docs/decisions/) ưu tiên thấp nhất | Taxonomy Diataxis-mapped chốt qua bee-exploring (evidence: docs/history/compound-learn-enduser-docs/CONTEXT.md D5); tag capture-time hoạt động trên scribing+compounding (evidence: docType field wired, cùng CONTEXT D6); ít nhất một tài liệu end-user thật được tích luỹ qua đúng skill export (evidence: `docs/how-to/check-rollup-progress.md`, liên kết capture `doc-fgos-rollup-howto`, slice gộp-sống 2026-07-21); bước-4 backfill (evidence: str64-backfill — `docs/decisions/` [17 ADR] nối vào chỉ mục đọc-theo-tag qua alias ngăn `explanation` + đầu-mô-tả OKF-style; 6 tài liệu `docs/explanation/*.md` chưng cất thật từ 54 mục critical-patterns.md theo đúng bộ lọc "thuộc tiến trình hình thành fgOS" (CONTEXT D1/D4); cả 3 cell goal-check độc lập PASS, `npm test` 1152/1152 + distill check xanh, xem docs/history/str64-backfill/reports/). Cả 4 mệnh đề đều có bằng chứng — flip `done`. | done | [compound-learn-enduser-docs](history/compound-learn-enduser-docs/) (slice 1-2) + [str64-backfill](history/str64-backfill/) (bước 3-4: ADR alias, OKF frontmatter/R1-amend, distill critical-patterns.md) — cả hai đã đóng+goal-checked |
| STR68 | **`discovery.mjs`'s judge call can return prose instead of JSON when nested inside an already-running dispatched worker session, causing a spurious `awaiting-human` fail-safe park** (phát hiện 2026-07-20, trong p50-workflow-induct Phase 3 case-study, item `tsk-5gc` trong `dogfood-fixture`) — `judgeDiscovery` (`src/intake/discovery.mjs`) spawn `claude -p {prompt} --model {model} ...` (theo `.fgos-runner.json`) và bắt buộc stdout `JSON.parse` được thành `{clear: boolean, ...}`; bất kỳ lỗi nào (exit khác 0, timeout, stdout không parse được) đều gộp vào cùng fail-safe `{clear:false, question: DEFAULT_UNCLEAR_QUESTION}` — đúng thiết kế "không bao giờ coi phán đoán không chắc là pass" (module's own D4 comment). Nhưng khi lệnh `claude -p` này được gọi TỪ BÊN TRONG một phiên Claude subagent đã đang chạy (dispatched worker), model con đôi khi trả lời bằng văn xuôi thay vì JSON thuần (tái hiện thủ công, read-only: `claude -p "Reply with exactly this JSON..." --model sonnet ...` trả về văn xuôi từ chối, không phải JSON) — item hoàn toàn rõ ràng (`power(base, exponent)`, linear, không mơ hồ) vẫn bị park `awaiting-human` oan, không phải vì item thật sự mơ hồ mà vì lớp gọi lồng-nhau không ổn định. Full evidence: `docs/history/p50-workflow-induct/reports/p50-workflow-induct-6.md` §6 (Root-cause diagnostic) | Một cách phán robust hơn cho lời gọi lồng-nhau (vd: instruct rõ ràng hơn để chống model con từ chối yêu cầu JSON trần, retry-with-stricter-prompt trước khi fail-safe, hoặc phát hiện "model từ chối" khác với "model không chắc" và xử khác nhau) — item rõ ràng không còn bị park oan khi `discover` chạy từ trong một phiên dispatched worker đã đang mở. **MỞ LẠI 2026-07-22 rồi ĐÓNG LẠI cùng ngày (per STR73 — CoS thực-giao):** bản fix retry-1-lần KHÔNG chặn được ca lồng-trong-Claude-Code (tái hiện 2/2 lần thật). Điều tra + sửa ngay trong ngày: (1) thử thêm "preamble hợp thức hoá" (mào đầu nói model đang bị gọi như classifier tự động) + tăng retry lên 3 lần — **preamble PHẢN TÁC DỤNG**, chứng minh bằng `claude -p` thật: CÙNG một prompt discovery thực tế đang chạy tốt KHÔNG preamble lại BỊ TỪ CHỐI khi thêm preamble (model đọc chính khung "tôi là classifier tự động" như dấu hiệu prompt-injection). (2) Gỡ preamble, CHỈ giữ tăng retry 1→2 lần (3 lượt thử tổng) — xác nhận lại bằng `claude -p` thật với prompt MỚI (không lặp câu hỏi cũ, tránh thiên vị): JSON hợp lệ, không từ chối. **Bài học quan trọng: test suite dùng executor giả KHÔNG bắt được loại hồi quy này — chỉ gọi model thật mới lộ ra**, ghi vào critical-patterns nếu tái diễn dạng tương tự. | done | [str68-discovery-judge-robustness](../../docs/history/str68-discovery-judge-robustness/) — shared `judge-executor.mjs` helper wired into both `judgeDiscovery`/`judgeDecompose`: parse-shaped failure retries once with a stricter JSON-only prompt (same per-attempt timeout budget), a non-parse failure never retries, retry-fails-too falls through unchanged to the existing fail-safe verdict; 1090/1090 tests green. **Tái hiện 2026-07-22, sửa lại cùng ngày trong lane str68-nested-judge-fix** (commits `968dafb`+correction, merge vào main) — retry 1→2 (3 lượt thử tổng), preamble thử rồi gỡ vì phản tác dụng (bằng chứng `claude -p` thật, 2 lần độc lập); 1141/1141 tests xanh trên main |
| STR50 | **Extract/induct bộ skill-workflow kiểu bee vào fgOS — bee gốc GIỮ NGUYÊN, không modify** (thảo luận 2026-07-18; là STR5 mở lại đúng hẹn — D1 bee-fgos-decision: "mở lại khi STR35 đóng", STR35 đã đóng): fgOS có xương sống cơ học mạnh (event-sourced, single-door, CAS, goal-check không-tin-lời, anti-loop, session worktree) nhưng trí tuệ workflow mỏng (3 điểm biên headless one-shot); bee có workflow trí-tuệ-giàu (exploring/planning/validating/reviewing nhiều vòng, gate) trên nền JSON + kỷ luật. Việc: **trích/chưng cất bộ skill workflow vào fgOS như một layer sản phẩm** (induct vào repo fgOS, KHÔNG sửa bee đang chạy ở xưởng), bản inducted đi qua cửa chính danh: phiên skill-based `take`/`return` (STR26 — RUL42 (runner) ghi rõ cửa pull là ngoại lệ chính danh cho trí tuệ vào hệ) làm bề mặt nhận-trả việc; gate-người map vào `awaiting-human` + ask/answer (STR19); cell ≈ work-item con mang `parent` (STR16); goal-check vẫn tự verify. **Một cửa ghi duy nhất:** bản inducted KHÔNG mang sổ state song song kiểu `.bee/` — work-item state chỉ qua verb CTR001/CTR002. **Kèm vòng học liên tục:** đăng ký bee làm **reference source của distillery** (delta scan theo cursor) — theo dõi bee tiến hóa ở xưởng để bản inducted học tiếp từ nó, không phải chép-một-lần-rồi-lệch | Bộ skill inducted nằm trong repo fgOS, versioned cùng sản phẩm; bee tại xưởng không đổi một dòng; bee có mặt trong distillery sources với delta scan chạy được; case study: MỘT feature thật đi trọn vòng bằng phiên skill-based mà mọi state qua verb fgOS (take/return/submit/answer/approve), goal-check xanh; decision record chốt quan hệ với STR5/D1 (case study này là bằng chứng D1 chờ) | done | [p50-workflow-induct](history/p50-workflow-induct/) — case-study thật 2026-07-20 (`power()` qua tsk-5gc): submit→clarify(routing→exploring)→cổng chờ-người THẬT (không kịch bản)→escalate→người trả lời→decompose(planning→validating)→executing→implement→return→review→approve→done, mọi state qua verb fgOS, `.bee/*` không tham gia định tuyến; `docs/distillery/sources/bee.md` xác nhận bee đã là reference source |
| STR60 | **Bug thật: `fgos return`'s working-tree-cleanliness check (`isFgosOnlyStatusLine`, `bin/fgos.mjs`/`src/runner/merge.mjs`) chỉ loại trừ status line có tiền tố `.fgos/` literal** — tìm ra qua dogfood STR59 (cell `fgos-sample-testbed-2`, 2026-07-18): khi cwd nằm trong một thư mục con của repo git thật (vd `repo/dogfood-fixture/`, không phải root), `git status` báo path theo dạng `<subdir>/.fgos/...` (tương đối với git top-level thật), không phải `.fgos/...` — nên exclusion không bao giờ khớp, và `return` liên tục từ chối "working tree not clean" dù state store của chính nó vừa tự sinh ra. Workaround đã dùng (không sửa source fgOS): commit kèm luôn các file `.fgos/*` tự sinh của fixture trong cùng commit với code change. **Mở rộng phát hiện (cell `fgos-sample-testbed-5`, item `divide`, cùng ngày):** gate này KHÔNG chỉ vướng bởi leftover `.fgos/*` — `isWorkingTreeClean` scan `git status` của TOÀN BỘ repo thật (`repo/`), không giới hạn trong cwd/subdirectory đang chạy, nên BẤT KỲ file nào khác trong `repo/` đang uncommitted (ví dụ `docs/backlog.md` do orchestrator sửa song song) cũng chặn `return` y hệt. Phạm vi bug rộng hơn ban đầu: không phải riêng `.fgos/` path-prefix, mà là toàn bộ cleanliness-check chưa cwd-scope đúng cho một fixture nằm trong subdirectory | `isFgosOnlyStatusLine` (hoặc tương đương) nhận diện đúng path `.fgos/` dù cwd là thư mục con của git root thật; RỘNG HƠN: cleanliness-check cho `return` khi cwd là subdirectory nên tự giới hạn vào phạm vi con đó (hoặc allowlist rõ ràng), không đòi hỏi TOÀN BỘ repo sạch — `fgos return` không còn báo working-tree-not-clean giả vì file KHÔNG liên quan tới item đang return | done | fgos-sample-testbed (tìm ra, 2 cell xác nhận); fix qua cell `str60-fgos-return-cleanliness-fix-1` (commit `e9db8a6`): `isFgosOnlyStatusLine(line, prefix)` nhận top-level-relative prefix (`git rev-parse --show-prefix`) nên nhận diện `.fgos/` đúng bất kể cwd sâu bao nhiêu; `bin/fgos.mjs`'s `isWorkingTreeClean(cwd)` (return's per-item gate) thêm scope `-- .` để chỉ scan subtree của cwd; `merge.mjs`'s `isWorkingTreeClean(repoRoot)` (approve's whole-repo gate) giữ nguyên scan toàn repo theo thiết kế, chỉ nhận thêm prefix fix |
| STR61 | **Context continuity cho chat-in-the-middle & ngắt quãng dài khi item bị chia async** (nêu ra 2026-07-19, kèm 2 câu hỏi mở của người dùng khi cung cấp thêm input cho STR38/STR46): (1) work item bị chia nhỏ thành task con độc lập (async) — bối cảnh trao đổi người-hệ thống có bị giới hạn theo từng task con, không gộp được bối cảnh parent/sibling khi cần hỏi-đáp người; (2) trao đổi ngắt quãng dài (người trả lời rồi vắng nhiều giờ mới quay lại) — giữ session/bối cảnh trọn vẹn thế nào để chất lượng trao đổi không giảm. Người dùng tự nhận KHÔNG vướng ở cổng giao tiếp/transport (STR46/STR38 — cho là không phức tạp), CHỈ vướng ở lớp NỘI DUNG bối cảnh chảy QUA cổng đó — STR61 tách riêng đúng ranh giới này. Liên quan STR19 (async-human-gate: item đậu `awaiting-human`, câu hỏi/trả lời là một cặp ghi log, không timeout, không có khái niệm session/transcript) và STR48 (attention channel sống ở daemon-consumer, core vẫn passive). Chưa có khái niệm "sibling" trong data model hiện tại (`work-state.md:864` chỉ có `parent`/`deps`/`blocks`) — cần chốt qua exploring. **Chốt qua bee-exploring 2026-07-19 (`docs/history/str61-chat-context-continuity/CONTEXT.md`):** D1 bối cảnh dựng lại từ state/log mỗi lần đọc, không lưu transcript; D2 tự động kèm đúng 1 cấp cha (không sibling); D3 chỉ báo "đã đổi" khi thay đổi ảnh hưởng câu hỏi/trả lời gốc, im lặng nếu không | `ask` chụp ảnh gốc lúc hỏi (`gates[id].parentSnapshotAtAsk`, RUL45 G3); `list` cộng thêm khóa `awaitingContext[id]` cho item `awaiting-human` có `parent` — luôn có `parent` hiện tại để neo, cộng `changedSinceAsk` khi title/status khác lúc hỏi; vắng mặt hoàn toàn khi không áp dụng, hành vi `list` cũ giữ nguyên byte-for-byte — full suite 1079/1079 xanh | done | [str61-chat-context-continuity](../../docs/history/str61-chat-context-continuity/) — commits `bf47599`/`5f6ec29`/`7d1cf0f`; spec `work-state.md` synced (RUL45) |
| STR58 | **Script `next-doc-id` cho họ "next free integer thủ công"** (Story/Rule/ADR — chốt tại `docs/id-systems-audit.md`, mục gen-id; KHÔNG chặn STR53-STR57, làm sau hoặc song song): tránh trùng khi 2 phiên cùng tự tay chọn số kế tiếp. Đặt trong `repo/scripts/` (fgOS tự sở hữu, không đặt trong bee — theo khung "fgOS tự vận hành" trong `docs/id-systems-audit.md` §Bối cảnh, decision `ADR0004` + STR50), 1 script tham số hoá đọc 1 file + pattern, trả số free kế tiếp; KHÔNG áp dụng cho CTR001-CTR009 (chỉ 9 slot cố định, không cần) | `node scripts/next-doc-id.mjs --file docs/backlog.md --pattern 'STR(\d+)'` trả đúng số kế tiếp; dùng lại được cho RUL (`--file docs/specs/runner.md`) và ADR (`--file docs/decisions/0000-index.md`); có test | done | [str58-next-doc-id](../../docs/history/str58-next-doc-id/) — `repo/scripts/next-doc-id.mjs` (pure `nextFreeId(text, pattern)` + CLI wrapper, mirrors `herdr-cockpit-notify.mjs`'s shape) + `repo/test/scripts/next-doc-id.test.mjs` (5 cases incl. one real end-to-end CLI spawn); verified live against all three real target files (STR→60, RUL→46, ADR→14); `npm test` 1049/1049 green; last item in the P53-P58 ID-naming migration queue — sequence now fully closed |
| STR57 | **Story (đổi tên khỏi "PBI") `P<n>` → `STR<n>`** (chốt tại `docs/id-systems-audit.md`, làm SAU CÙNG trong nhóm docs-rename — rủi ro/chi phí cao nhất, đụng ID đang SỐNG): đổi ID mọi dòng `docs/backlog.md` hiện có (P1..P52) sang `STR<n>` giữ nguyên số, cập nhật quy ước sinh id tại `.claude/skills/bee-scribing/references/scribing-reference.md:297,303`, quét mọi trích dẫn "P<n>" trong `docs/specs/*.md`, `docs/architecture-map.md`, `docs/history/*/CONTEXT.md` đổi theo. Giải quyết dứt điểm va chạm ký tự với severity `P1/P2/P3` (không cần đổi severity, xem `docs/id-systems-audit.md` #11) | `docs/backlog.md` toàn bộ dùng `STR<n>`; field `pbi` ở cell bee (`.bee/bin/lib/cells.mjs:123-128`) vẫn nhận giá trị mới (không đổi tên field, chỉ giá trị đổi dạng); mọi trích dẫn cross-doc cập nhật; `scribing-reference.md` ghi rule mới | done | [story-str-rename](../../docs/history/story-str-rename/) — 60 backlog rows P1-P59 → STR1-STR59 (incl. this row's own STR57 + STR58) + ~609 citations across 44 files (specs, decisions, architecture-map, coexistence, platform-foundations, 29 docs/history CONTEXT.md, scribing-reference.md convention prose); highest-risk item in the P53-P58 sequence, largest live-ID blast radius. Validation caught 2 BLOCKERs (backlog.md line-7 severity/self-reference deadlock — resolved by excluding the row's own prose from bulk rename except the ID cell, matching the P53-P56 precedent; cell1/cell6 undeclared file race on backlog.md — resolved with a deps ordering edge) + 3 CRITICALs (runner.md:834 and decisions/0013:82 severity/review-fix false positives the citation inventory missed) across 2 independent review rounds each — plan-checker and cell-reviewer found non-overlapping defects, both needed. Closing sweep confirms zero unexplained residuals across all 44 files |
| STR56 | **`R#` business rule → `RUL<n>`** (chốt tại `docs/id-systems-audit.md`, làm SAU STR54/STR55 để mẫu tiền tố đã chạy trơn): `runner.md` (R1-R43) + 4 spec khác đang có `R#` riêng (`platform-foundations.md`, `distribution.md`, `work-state.md`, và spec còn lại) đều đổi tiền tố. Bước đầu BẮT BUỘC: liệt kê ĐỦ mọi chỗ `R[0-9]+` xuất hiện (định nghĩa lẫn trích dẫn) trước khi đổi, không chỉ sửa định nghĩa gốc — đây là migration có quy mô, không phải rename 1 chỗ. `R#` vẫn KHÔNG unique toàn cục sau đổi tên (mỗi spec tự đếm riêng) — trích dẫn NGOÀI spec gốc phải kèm tên area (vd `RUL42 (runner)`) | Mọi spec + `CONTEXT.md` liên quan dùng `RUL<n>`; không còn `R[0-9]+` bare nào chỉ-nghĩa-rule (trừ trích dẫn lịch sử bất biến); tài liệu ghi rõ `RUL#` vẫn per-spec, không global | done | [rul-rename](../../docs/history/rul-rename/) — 94 định nghĩa + ~163 trích dẫn xuyên-file đổi sang `RUL<n>` (area) qua 56 file (4 spec + 7 doc chủ động + 52 docs/history, theo phạm vi đầy đủ do người chốt); validating tìm 2 lỗi thật trước thi công + 1 rescue mid-swarm + 1 orchestrator tự sửa sai (worker Kevin2 từ chối một sửa sai do chính orchestrator chẩn đoán nhầm — xác nhận thêm 1 va chạm số R29 runner/work-state); closing sweep xác nhận không còn sót |
| STR55 | **Chuẩn hoá trích dẫn rút gọn ADR → `ADR<n>`** (chốt tại `docs/id-systems-audit.md`, làm cùng đợt rẻ với STR54): tên file đầy đủ `NNNN-slug.md` giữ nguyên, không đổi; mọi trích dẫn RÚT GỌN trong văn xuôi (hiện có chỗ viết bare số, ví dụ "0013") đổi thành `ADR0013`. `docs/decisions/0000-index.md` ghi rõ quy ước trích dẫn mới | `0000-index.md` ghi quy ước `ADR<n>` cho trích dẫn rút gọn; quét `repo/docs/` thay hết trích dẫn rút gọn bare-số thành `ADR<n>` (tên file đầy đủ không đổi) | done | [adr-short-cite-convention](../../docs/history/adr-short-cite-convention/) — 26 trích dẫn trần sửa thành `ADR<n>` qua 7 file (link markdown + tên file đầy đủ giữ nguyên); validating tìm 1 trích dẫn bị bỏ sót (`work-state.md:873`) trước khi thi công; quy ước ghi vào `0000-index.md`; closing sweep xác nhận không còn sót |
| STR54 | **`C1`-`C9` contract → `CTR<n>`** (chốt tại `docs/id-systems-audit.md`, làm SỚM trong nhóm docs-rename — rẻ nhất, dùng để validate mẫu tiền tố trước khi làm STR56/STR57 tốn hơn): đổi bảng registry `architecture-map.md` §7 (`:330-340`) + mermaid diagram liên quan + mọi chỗ trích (`reading-map.md`, các spec) từ `C1..C9` sang `CTR1..CTR9`, qua decision record + version bump theo đúng luật riêng của map (`architecture-map.md:436-437`) | Bảng §7 + mermaid dùng `CTR<n>`; không còn `C[1-9]` bare nào chỉ-nghĩa-contract trong `repo/docs/` (trích dẫn lịch sử trong decision record cũ giữ nguyên, bất biến); decision record mới ghi rõ đổi tên + supersede version | done | [contract-ids-ctr-rename](../../docs/history/contract-ids-ctr-rename/) — `architecture-map.md` C1-C9 → `CTR001`-`CTR009` (v0.4→v0.5, decision `ADR0015`) + rename mirrored across `reading-map.md`/`runner.md`/`work-state.md`/`platform-foundations.md`/`backlog.md`/`0000-index.md`; validating caught a padding-format BLOCKER + a broken verify command before execution; mid-swarm rescue caught a verify-exclusion gap; closing sweep confirms zero unexplained residuals |
| STR53 | **`work.id` bỏ slug-title, đổi sang `TSK<hash>`** (chốt tại `docs/id-systems-audit.md` §1/§2, thảo luận 2026-07-18, làm ĐẦU TIÊN trong nhóm STR53-STR58 — độc lập kỹ thuật, không phụ thuộc STR54-STR58): `generateId()` (`classify.mjs:124`) hiện sinh `slugify(title,≤40 ký tự)+hash` — dài, gần như chép nguyên title (ví dụ thật 44 ký tự trong `.fgos/events.jsonl`). Đổi thành hash-only ngắn (base36 6-8 ký tự), giữ nguyên cơ chế chống trùng đã có. Con (sinh qua `decompose.mjs:245`) dùng `<parentId>-<n>` (n = thứ tự trong lứa con, đệ quy theo `parent` — an toàn tuyệt đối vì `parent` KHÔNG nằm trong `EDITABLE_FIELDS`, `store.mjs:184,232`, không bao giờ đổi sau khi tạo). `TSK` dùng CHUNG cho mọi `kind` — KHÔNG mã hoá kind vào tiền tố, vì `kind` CÓ trong `EDITABLE_FIELDS` (sửa được qua `fgos edit`), mã hoá vào id bất biến sẽ khiến id nói dối sau khi edit; cũng khớp decision `ADR0002` (mô hình phẳng — loại việc là field, không phải shape id riêng). id CŨ trong log giữ nguyên, không backfill (bất biến). Muốn hiển thị dễ hiểu (đọc nhanh là việc gì) thì ghép slug(title) lúc RENDER (CLI list/trích dẫn), không lưu vào id. | `fgos submit` sinh id mới đúng dạng `TSK<hash>`, không còn slug; item có `parent` sinh id con `TSK<hash>-<n>`; test `classify.mjs`/`work.mjs`/`decompose.mjs` cập nhật + xanh; id cũ trong `.fgos/events.jsonl` không bị chạm; vẫn hợp lệ với `ID_PATTERN` hiện có | done | [work-id-tsk-hash](../../docs/history/work-id-tsk-hash/) — `generateId()` → `tsk-<hash>` (classify.mjs), con → `${work.id}-<n>` (decompose.mjs); validating tìm 2 lỗi thật trước khi thi công (BLOCKER: tiền tố TSK bị bỏ sót; CRITICAL: hash trần vỡ ID_PATTERN ~89% trường hợp), sửa xong mới cap; 19+25 test mới xanh, `npm test` 1043/1043 xanh; spec `work-state.md` đã sync |
| STR49 | **Tách prompt worker thành file template riêng + registry agent-type** (thảo luận 2026-07-18): `buildPrompt` (dispatch.mjs) hiện hard-code chuỗi trong code — tune câu chữ phải sửa code; tách ra file template versioned trong repo để review/diff/tune prompt như tài liệu. Chọn template qua **bảng tra cơ học** kind/tier/domain → file (cùng họ bảng tra tier→model, hợp RUL42 (runner) — không LLM trong vòng chọn; cùng hướng STR41 per-tier executor + STR18 domain registry). Prompt mỏng thành: định danh vai (agent type) + trỏ skill cần dùng + data item — worker chạy trong worktree của repo nên **tự nạp được skill** từ thư mục skill của repo. **Hai điều kiện giữ từ thiết kế hiện tại:** (1) substitution vẫn `{placeholder}` string-replace thuần, KHÔNG template engine có logic — giữ guarantee `shell:false` + prompt đoán-trước-được; (2) template là artifact có golden-file test (item mẫu → prompt kỳ vọng). Log dispatch (STR32/STR39) ghi kèm tên + hash template đã dùng để trace "worker sai vì prompt version nào" | Đổi nội dung prompt của một agent-type bằng sửa MỘT file template, không đụng code; chọn template là tra bảng có test; golden-file test đỏ khi template đổi hành vi; log dispatch ghi template id + hash; prompt không-feedback giữ byte-identical với test hiện có hoặc test cập nhật có chủ đích | done | [p49-prompt-template-externalization](../../docs/history/p49-prompt-template-externalization/) — `prompt-templates.mjs` (`selectTemplate`/`renderTemplate`/`hashTemplate`) + `prompt-templates/worker-prompt-default.txt`; `buildPrompt` byte-identical (0 existing test edits); dispatch log records template name+hash (RUL44 (runner)). "Agent-type + tự nạp skill" trong Story vẫn là hướng tương lai, chưa nằm trong CoS đã chốt vòng này. |
| STR44 | Approve session-nesting guard (STR35 Epic 2) chỉ nhận diện worktree đăng ký qua `fgos session start` — một `git worktree add` thủ công, không qua registry, vẫn lọt đúng nguy cơ pull/legacy false-verification (goal-check chạy trên code cũ, báo "verified on main") một cách âm thầm | `approve` xác nhận `repoRoot` LÀ worktree chính (vd `git rev-parse --show-toplevel` so `--git-common-dir`'s parent) thay vì chỉ tra registry — bắt được cả worktree không đăng ký | done | [approve-worktree-guard](../../docs/history/approve-worktree-guard/) — `isMainWorktree` (`src/runner/merge.mjs`) chặn cấu trúc ngay sau chặn sổ đăng ký trong `approve`; red-before-green test (`test/cli/fgos.test.mjs`) tái tạo false-verification rồi xác nhận chặn, 995/995 npm test xanh |
| STR45 | `.fgos/events.lock` (STR35 Epic 3) chỉ đóng đua trùng-seq tại chính `appendEvent` — không làm các thao tác đọc-sửa-ghi cấp cao ở `store.mjs` (kiểm id-đã-tồn-tại của `addWork`, CAS `expectedStatus` của `moveWork`) nguyên tử liên-tiến-trình; hai lệnh `fgos` tương tác song song trên CÙNG một id vẫn có thể mỗi bên qua tiền-kiểm cũ rồi ghi hai sự kiện hợp-lệ-nhưng-mâu-thuẫn | Một khóa bọc-cả-đọc-sửa-ghi (hoặc vòng CAS-thử-lại) ở tầng `store.mjs` cho các thao tác ghi tương tác cạnh nhau trên cùng id — vòng runner tự hành đã có bảo vệ riêng (runner.lock + write-queue), phần thiếu là hai verb tương tác đua nhau | done | [store-atomic-rmw](../../docs/history/store-atomic-rmw/) — `withEventsLock`/`appendEventLocked` (events.mjs) bọc TRỌN đọc-tiền-kiểm-ghi của `addWork`/`editWork`/`moveWork`/`moveStage` (store.mjs) trong MỘT phiên giữ `.fgos/events.lock` sẵn có; 2 test fork-đa-tiến-trình chứng minh đua đóng (red-before-green qua git stash), 1009/1009 npm test xanh |
| STR39 | Xem live output worker (D 644916a4): (a) tee stdout/stderr của worker TỪNG CHUNK vào `.fgos/logs/<id>.log` ngay khi đến — qua đúng cửa worker-log (một-cửa D3 của worker-dispatch-log), giữ nguyên khối kết-cục cuối (timeout/hỏng-spawn vẫn bắt được, resolve trên exit không đổi) → `tail -f` thành cửa sổ xem live per-item, N worker song song không giẫm dòng nhau; (b) tùy chọn stream-json qua executor args trong config runner — thuần Host Adapter, zero code. File log live là chất nền cho chiều-ra của STR38 (UI tail + đẩy) | Trong lúc worker đang chạy, `tail -f` file log của item cho thấy output đến theo thời gian thực; khối kết-cục cuối vẫn ghi đủ như hiện tại; không path committed nào nhận output worker | done | [live-worker-log](history/live-worker-log/) — (a) làm xong: `appendWorkerLogChunk` (worker-log.mjs) + `opts.onChunk` (dispatch.mjs) + wire (loop.mjs), 995/995 test xanh, spec-synced. (b) không cần code riêng — đã sẵn qua `.fgos-runner.json` executor.args (Host Adapter) |
| STR40 | Chế độ vận hành qua herdr (D d3dbe7f5, supersedes ef6ed305 — chốt 2026-07-18, user xác nhận đổi tmux→herdr vì operator thật đã dùng sẵn herdr): phiên herdr chuẩn 4 pane — (1) runner loop `while … fgos-runner --once … sleep` (đứng trên runner.lock + idempotent, không cần flag mới), (2) `tail -F` log live per-item (đứng trên STR39), (3) cửa người submit/answer/review/approve/reject, (4) dashboard poll `fgos list` + chuông chờ-người gộp một pane (`herdr notification show` native, đúng một lần mỗi lượt việc mới vào `awaiting-human`). **LUẬT CỨNG:** herdr chỉ làm chrome — không bao giờ gọi `agent start`/đọc `agent_status` làm tín hiệu quyết định. Đa pane chung checkout đòi STR35 đóng (đã đóng) | Người vận hành dựng được phiên herdr chuẩn từ runbook trong <5 phút: worker đang chạy xem được live, item chờ-người có đánh dấu cửa sổ (native notification, dedup đã chứng minh), duyệt PR không báo động giả khi runner đang merge song song | done | [p40-herdr-operator-cockpit](../../docs/history/p40-herdr-operator-cockpit/) — `scripts/herdr-cockpit.sh` (4 pane, live-proven against real herdr session) + `scripts/herdr-cockpit-notify.mjs` (`detectNewAwaitingHuman`/`formatStatusLine`, dedup live-proven against a sandbox fgOS checkout); `docs/operator-runbook-herdr-cockpit.md`; RUL40 (runner) updated. |
| STR41 | Per-tier executor + executor port hexagonal-lite (D a4fe4c2b): (a) config runner cho MỖI TIER khai executor riêng `{command, args}` — light/standard/heavy cắm CLI khác nhau trong CÙNG một lượt drain; tier không khai riêng rơi về executor chung (config cũ chạy nguyên, backward-compat); (b) dispatch nâng CTR009 từ "mẫu spawn" thành interface executor CÓ TÊN (CTR009 v2) với adapter — hôm nay đúng một adapter `cli-spawn` (argv, shell:false, hành xử giữ nguyên); adapter `rpc/app-server` (vd codex app-server giao tiếp RPC, không phải CLI) chỉ xây khi có nhu cầu cắm thật — mua cái TÊN của bậc trừu tượng, chưa mua bậc | Hai tier trỏ hai executor khác nhau chạy đúng trong cùng một lượt `--once`; config cũ (executor chung) chạy không đổi hành xử; CTR009 v2 có tên interface + adapter trong spec, RPC adapter ghi là deferred | done | [p41-per-tier-executor](../../docs/history/p41-per-tier-executor/) — `EXECUTOR_ADAPTERS` registry (CTR009 v2, sole adapter `cli-spawn`) + `cfg.executors.<tier>` override in `dispatch.mjs`; mixed-tier dispatch proven in one config, 0 pre-existing test edits (RUL41 (runner)). |
| STR43 | Work-graph metrics surface (D f176c18a, dưới stance RUL42 (runner)): một verb đọc-thuần trả metrics graph CƠ HỌC fold từ event log — unblock-set transitive, critical path/depth, connected-components (mấy mũi song song độc lập), chuỗi kẹt/stale-blocked — qua envelope; consumer KÉP: khóa sort tương lai của picker (STR7, vẫn tra bảng — không phạm RUL42 (runner)) + digest cho planner-brain (STR8, LLM hết phải đếm cạnh). Chất liệu distillery ĐÃ ĐÀO, tham khảo bắt buộc khi planning: bvr transparent-triage 8 thành phần giải trình được + greedy-submodular top-k-unblock + what-if + connected-components=parallel-tracks (`docs/distillery/sources/beads-viewer-rust.md` xưởng); beads 10-loại dependency + unified cycle detection (`docs/distillery/sources/beads.md`); graph-drift 6 trục làm health gate; five-tier eligibility stack (`docs/distillery/deep-dives/work-item-management.md` §4 — fgOS đang tier 1-2, STR43+STR7+STR8 leo tier 3-4) | Một verb đọc trả qua envelope ít nhất: unblock-set transitive, critical path, connected-components, chuỗi kẹt; số liệu khớp kiểm tay trên fixture; STR7/STR8 trích dẫn được metric của nó làm input thiết kế | done | [work-graph-intelligence](../../../docs/history/work-graph-intelligence/) — `fgos graph` verb: connected-components + critical-path + stale-blocked + greedy top-k-unblock, deterministic qua CTR001 envelope (S1–S6). Epic S7–S9 CŨNG XONG: `fgos graph --what-if <id>` + architecture frame (S7), `fgos stale` evidence-classifier advisory (S8), `fgos conflicts` + `footprint` field footprint-intersection planner (S9). Docs-track XONG: platform-foundations L9 (thang hoàn tất việc run≠merge≠durable, DT1) + L10 (add-through-not-alongside doctrine, DT2). Epic đóng hoàn toàn. |
| STR37 | Chuẩn hóa entry cho multi-surface (D b2d18cc7): (a) envelope fgos.v1 trên MỌI verb — hoàn tất vế envelope của CTR001 (hiện chỉ submit/discover có; các verb còn lại in chữ thường, UI nào bám vào cũng là bám regex); (b) verb manifest máy-đọc `fgos --help --json` trả `{verb, params, mô tả, read/mutation}` để listener/UI sinh form-lệnh thay vì hard-code (tiền lệ: bee.mjs --help --json) | Mọi verb trả envelope `{contract, generated_at, data_hash, data}` qua stdout; một manifest máy-đọc liệt kê đủ verb surface; agent/UI render mọi verb bằng một renderer, không regex | done | [entry-standardization](../../../docs/history/entry-standardization/) |
| STR4 | Distillery lên state layer làm consumer thứ hai (porting lifecycle thành FSM) | Trạng thái porting đọc/ghi qua cùng một cửa state layer, dựng lại được từ event log | done | [distillery-state-consumer](history/distillery-state-consumer/) |
| STR28 | GitHub adapter cho PR lifecycle: transport remote (tạo PR thật, duyệt trên web, auto-merge qua API) đứng trên CÙNG cổng duyệt nội bộ của STR17 (pr-lifecycle D1 — local-first, adapter chỉ đổi transport) | Một item proposed tạo được PR GitHub thật, duyệt web → hệ merge remote + item done; cổng duyệt nội bộ không fork luật | done | [github-adapter](history/github-adapter/) |
| STR35 | fgos multi-session trên CÙNG checkout (dogfood run-6, 2026-07-17, phiên khác đang chạy STR32 song song lần đầu tiên): (a) `isWorkingTreeClean` của `approve`/`return` không phân biệt "bẩn của phiên nào" — một phiên khác merge song song có thể chặn approve của phiên này 2 lần liền; (b) soát diff nhánh runner trước khi hỏi người approve mà diff thẳng với ref `main` đang di chuyển (thay vì `git merge-base`) tạo báo động giả khi có phiên khác merge song song — run-6 tự gây 1 reject oan vì lỗi này | Quy ước rõ multi-session trên chung checkout: (a) mỗi phiên có worktree/checkout riêng (đứng trên hướng fleet STR27), hoặc (b) quy tắc ai chịu trách nhiệm commit khi tree dirty do phiên khác gây ra; quy trình soát-diff-trước-approve LUÔN dùng `git merge-base`, không diff thẳng một ref có thể di chuyển | done | [fgos-multi-session-checkout](history/fgos-multi-session-checkout/) — `fgos session start/end/list` (worktree-per-session, `.fgos` symlink dùng chung), `approve` từ chối khi lồng trong session, `events.lock` liên-tiến-trình cho append. (b) diff-trước-approve vốn đã đúng (`git diff trunk...branch` = merge-base). Dư lượng đã ghi (Deferred Ideas): ad-hoc worktree ngoài registry, CAS đua verb-tương-tác |
| STR42 | Kiểm định "sự thông minh của giao việc nằm ở đâu": audit ranh giới planner-vs-orchestrator và chốt bằng decision record — hiện loop là cỗ máy CƠ HỌC (FIFO theo seq, tier→model tra bảng, ngân sách visit, ma trận recovery — không gọi LLM nào trong vòng chọn-giao), trí tuệ nằm ở BIÊN stage (clarify/discovery + decompose gọi model, classify intake cơ học); quyết giữ đạo lý "orchestrator ngu — trí tuệ ở biên" hay cho phép thông minh vào vòng chọn việc, và nếu vào thì qua cửa nào | Một decision record trả lời rõ orchestrator được phép thông minh đến đâu, kèm căn cứ đọc code; spec runner ghi stance này; STR8 (intent-scoring) và STR7 (priority) đọc decision này TRƯỚC khi thiết kế | done | chốt qua decision f69951df (thảo luận + căn cứ đọc code 2026-07-17); stance ghi ở spec runner **RUL42 (runner)**: picker cơ học vĩnh viễn, trí tuệ vào qua field-trên-item (dòng chính) + cửa pull (ngoại lệ) |
| STR13 | Evolving loop human-gated (F5): tự-sửa 2-gate (Gate A chọn / Gate B duyệt diff), explicit-accept-per-item, Iron Law cho skill sửa hệ, push never automatic | Vòng evolving chạy human-gated với outcome đo được (tiêu chí F5, luật L6); diff được người duyệt trước khi push, không bao giờ tự động | done | [self-improve-loop](history/self-improve-loop/) — **Iron Law's ranh giới thật:** phân loại rủi ro (keyword/module) + chặn cứng đòi `--acknowledge-iron-law`, KHÔNG mechanically verify "test đỏ chạy trước" (không có hạ tầng theo dõi lịch sử đỏ/xanh — xem CONTEXT.md D17) |
| STR18 | Mô hình base-workflow + domain-extension: một chu trình chung nền tảng (init→làm rõ→chia việc→thực thi→compound-learn) mà coding/marketing/HR/finance kế thừa và thêm stage riêng (work-item-lifecycle-vision.md §2) | Một domain thứ hai (vd marketing) chạy được trên cùng base FSM, chỉ thêm stage riêng, không fork chu trình | done | [base-workflow-model](history/base-workflow-model/) |
| STR21 | Backlog-triage (cửa 2 của triage, phân biệt với STR14 intake-triage = cửa 1): forgent cần một cửa quyết "việc nào nổi lên chú ý" (impact/value ranking) tách bạch khỏi "việc này rủi ro/lane gì" — hiện chưa có (per deep-dive work-item-management.md:106, đúc kết từ beads-viewer-rust ranking) | Có một truy vấn/lệnh xếp hạng việc theo tác động, độc lập với phân loại rủi ro của intake | done | [backlog-triage-ranking](history/backlog-triage-ranking/) — `fgos triage` (blocking-fan-out proxy over `deps`) |
| STR24 | Rollup view theo "bộ" (stage-clarify D11): human hỏi "việc tôi nộp tới đâu rồi" — xem item gốc + tiến độ hậu duệ (n/m xong), dựa trên field parent (STR16 dựng) | Một lệnh đọc cho thấy mọi item gốc kèm tiến độ con; item nội bộ không làm nhiễu tầm nhìn cấp gốc | done | `fgos rollup <id>` (bin/fgos.mjs), dogfooded qua [dogfood-p25-run1](history/dogfood-p25-run1/) run-6 |
| STR5 | Xem xét thay bee bằng state layer của forgent — chỉ mở khi forgent đạt F3 (ngưỡng tên, per D2 phase-1-state-layer) | Quyết định có/không kèm case study vận hành song song | done | [bee-fgos-decision](history/bee-fgos-decision/) — D1: CHƯA thay, mở lại khi STR35 đóng |
| STR22 | Số phận verb `add`: đã hạ cấp khỏi câu chuyện public (stage-clarify D9 — submit là cửa công khai duy nhất; id tự đặt phá nhất quán id tự sinh); quyết xóa hẳn hay giữ làm bề mặt nội bộ cho test/tooling | Spec/docs không kể `add` là cửa vào; quyết định xóa/giữ có decision record; test suite không phụ thuộc đường public nào đã bỏ | done | [work-item-verb-surface](history/work-item-verb-surface/) — D1: giữ nội bộ, không xóa |
| STR23 | Verb update/edit field công khai cho work-item (ghi đè kind/risk do classify gợi ý, sửa verify tay...) — "luôn ghi đè được" (stage-intake D1) hiện chưa có bề mặt CLI nào thực hiện được | Người/agent đổi được field của item đã tồn tại qua một verb, đi qua cửa ghi CTR002, event additive | done | [work-item-verb-surface](history/work-item-verb-surface/) |
| STR9 | Merge tự động đề xuất của worker khi review sạch | Vòng đề-xuất-duyệt chạy tay đủ tin cậy, có số liệu | done | gộp vào [pr-lifecycle](history/pr-lifecycle/) (STR17 D3) |
| STR29 | Packaging/install thật của fgOS (npm/global/copy-bin): câu chuyện phân phối đứng trên nền coexistence STR10 (install-coexistence D1 — đợt STR10 chỉ chạy-tại-chỗ) | Cài fgos vào một project ngoài bằng một lệnh, init + doctrine + marker detection chạy đúng như canary STR10 | done | [distribution-packaging](history/distribution-packaging/) |
| STR3 | Compound-learning trên routing (Phase 3): vòng predicted→actual, capture 2 kênh, entropy-trend, evolving human-gated | Vòng predicted→actual chạy thật với bằng chứng output (tiêu chí F4, luật L6) | done | [phase-3-compound-learning](history/phase-3-compound-learning/) |
| STR6 | Fan-out song song N worker trên frontier | Anti-loop đã chứng minh trong vòng chạy thật (ngưỡng A1 phase-2-routing); N worker không phá single-writer | done | [fan-out-parallel](history/fan-out-parallel/) |
| STR10 | Install/coexistence story của fgOS: cài vào project/global không giao thoa tiến trình với harness khác (doctrine scope lãnh địa, hook gate theo path, một-nhạc-trưởng-mỗi-phiên, phát hiện marker harness khác lúc cài) | Canary chạy trong project cài CẢ bee lẫn fgos: hai bên không chặn nhầm write của nhau, agent không nhận mệnh lệnh điều phối mâu thuẫn. **ĐÓNG PHẦN FGOS per D7:** canary pin 2 deny bee làm known-gap (friction đã file), CoS trọn chờ bee-side fix. | done | [install-coexistence](history/install-coexistence/) |
| STR11 | Ly khai bee (xưởng, gốc) ↔ forgent (sản phẩm, ./repo): artifacts hai bên tách tuyệt đối, release sạch không rác | Baseline verify xanh nguyên trạng trong ./repo; một cell bee smoke chạy trọn vòng ghi vào ./repo; fgos ready + runner --dry-run chạy đúng chỗ mới; doctrine hai bên tách đôi | done | [repo-divorce](history/repo-divorce/) |
| STR12 | Export chưng cất sử ký sản phẩm: sinh decision-records/changelog vào docs/decisions/ của forgent từ hồ sơ xưởng (per D3 repo-divorce) | Người ngoài đọc docs/decisions/ hiểu các quyết định lớn của sản phẩm mà không cần vào xưởng | done | [decision-records-export](history/decision-records-export/) |
| STR14 | Stage Intake: người submit vấn đề, 2 chế độ (a) un-attended rồi bỏ đi / (b) collaborate-now; hệ ghi nhận + auto-classify work-item (per work-item-lifecycle-vision.md §3-4 bước 1-2) | Một vấn đề submit vào tạo đúng một work-item đã phân loại tự động (tier/kind/risk cơ học, id tự sinh); chế độ submit (sync/async) được GHI LẠI trên item — đậu-chờ-người-khi-cần là việc của STR15 (chốt tại exploring D6, không phải submit tự đậu vô điều kiện) | done | [stage-intake](history/stage-intake/) |
| STR15 | Stage Làm-rõ: runner sơ khởi chạy phân loại + context-discovery; task đơn giản → `ready`, task phức tạp → `need-exploring` rồi kích hoạt exploring trong-sản-phẩm (lifecycle §4 bước 3,5,6) | Item đủ rõ tự chuyển sang stage executing (đủ để `ready` nhặt khi deps xong); item thiếu rõ đậu `awaiting-human` mang câu hỏi cụ thể, lặp tới khi đủ — giải qua chiều `stage` (clarify/executing) + context-discovery gọi model, không phải một status FSM mới (chốt sau khi bàn sâu về mô hình 2 tầng + tham khảo 3 nguồn distill) | done | [stage-clarify](history/stage-clarify/) |
| STR16 | Stage Chia-việc: auto làm giàu context + planning, decompose thành n item nhỏ độc lập (dependency rõ) đẩy vào queue (lifecycle §4 bước 7; câu hỏi mở: có cổng-người ở planning không). Ràng buộc đã khóa từ stage-clarify D11: item con mang field `parent` (lineage, không field `origin` riêng); item gốc là mỏ neo chú ý người, không dispatch khi con dang dở; deps giữ nguyên nghĩa chặn, tách khỏi parent; id con dùng chung generateId | Từ một item đã qua stage `executing` (không còn dùng chữ "ready" mơ hồ — xem stage-clarify) sinh ra n item nhỏ có dependency đúng + field parent trỏ đúng gốc, vào queue, runner pick được | done | [stage-decompose](history/stage-decompose/) |
| STR17 | Stage PR lifecycle: tạo PR từ nhánh đề xuất, cổng review người, auto-merge khi duyệt sạch (lifecycle §4 bước 9-11; gồm STR9). Câu hỏi mở: worktree-mỗi-item vs chung, PR-mỗi-item vs gom-theo-human-submit | PR sinh tự động, người duyệt ở cổng, hệ merge tự động sau duyệt; chiến lược worktree + độ hạt PR đã chốt | done | [pr-lifecycle](history/pr-lifecycle/) |
| STR19 | Async human-gate primitive: work-item đậu chờ người quyết (state `awaiting-human`), ghi 'chờ gì', resume khi người trả lời — bất đồng bộ, không chặn; spine dùng lại cho mọi cổng-người của lifecycle (work-item-lifecycle-vision.md). Không làm intake/exploring stage — chỉ primitive | Một work-item vào trạng thái chờ-người, ghi rõ đang chờ gì; người trả lời qua CLI → item rời trạng thái chờ, câu trả lời ghi thành event, item lại actionable | done | [async-human-gate](history/async-human-gate/) |
| P3-S2 | Phase 3 Slice 2 — kênh friction của capture 2 kênh: `work.friction` event tại choke-point park/halt, runner tự quy tội 5 lớp (task-spec/context/environment/verification/state), fold append-theo-id, `fgos check` thêm mục friction (cap 5) + nhắc item thiếu outcome. Settlement dạng đầy đủ dời sau (vision §8, decision 719cbe3a) | Một park/halt thật ghi đúng 1 friction record với layer đúng; `check` in mục friction + nhắc; log không có friction event thì output không đổi | done | [phase-3-compound-learning](history/phase-3-compound-learning/) |
| STR25 | Mốc dogfood tự-phát-triển: dùng fgos tại xưởng điều phối phát triển chính fgos sản phẩm (thay vai bee hiện tại — xưởng điều phối, `./repo` là đối tượng). Điều kiện tối thiểu: STR16 + STR17 khép kín vòng giao việc→nghiệm thu; STR10 coexistence trước khi chạy thật trong xưởng đang có bee. Bee chủ động báo người dùng khi đủ điều kiện (standing request 2026-07-16, kiểm tại mỗi feature close) | Một work-item thật của chính fgos đi trọn vòng submit→clarify→chia việc→execute→PR→merge do fgos-runner điều phối trong xưởng, không cần bee đỡ — run-1 2026-07-16: FAIL tại clarify-loop (→STR30), bằng chứng docs/history/dogfood-p25-run1/ xưởng; run-2 2026-07-17 SAU STR30: clarify HỘI TỤ với answer sẵn có (STR30 CoS đạt), đi tiếp decompose→dispatch→goal-check→park verify-miss — biên giới mới: worker execution rỗng (→STR31) — **ĐẠT 2026-07-17**: 2 item thật trọn vòng (bo-hardcode 763497d, loai-tru-data-dir 6bb3e01) — submit→clarify→(decompose pass-through)→worker code thật→proposed→reject/park→người hoàn tất qua cửa D2→approve→merge→done; settlement/learning/friction/entropy đủ; bee không đỡ bước nào của vòng fgos | done | [human-rounds](history/human-rounds/) + [dogfood-p25-run1](history/dogfood-p25-run1/) |
| STR26 | Cửa pull giao–nhận việc (take/return): tác nhân ngoài runner (người/phiên/runner khác) nhận đúng một item từ queue qua claim CAS, trả kết quả qua cửa event; runner vẫn tự dispatch khi không ai cầm — mô hình hóa attend/unattend (stage-decompose D1) | Một người/phiên take một item, runner không giẫm; return trả kết quả thành event; goal-check giữ luật không-tin-lời-worker | done | [stage-decompose](history/stage-decompose/) |
| STR30 | Discovery prompt đủ ngữ cảnh: buildDiscoveryPrompt (src/intake/discovery.mjs) hiện chỉ đưa title-cắt/kind/risk/refs/deps — THIẾU description đầy đủ + toàn bộ ask/answer history (gates) + discovery history → vòng làm-rõ không hội tụ, hỏi lặp rồi fail-safe park (dogfood STR25 run-1 phát hiện, 2026-07-16) | Item có answer của người không bị hỏi lại cùng chủ đề; item dogfood bo-hardcode-ten-trunk pass clarify sau 1 answer; description đầy đủ nằm trong prompt phán | done | [discovery-context](history/discovery-context/) |
| STR31 | Worker execution thật cho dogfood: run-2 (2026-07-17) worker được dispatch nhưng nhánh fgw rỗng (aheadCount 0, verify-miss ×2 → parked đúng luật) — điều tra executor config/worker prompt của dispatch cho task implementation thật (model nào, prompt đủ chưa, timeout, làm sao thấy log worker) | Item dogfood bo-hardcode-ten-trunk được worker thật implement: nhánh fgw có commit, goal-check xanh, item proposed | done | [worker-execution](history/worker-execution/) |
| STR32 | Persist log worker dispatch (observability): hiện output worker chỉ console-tail per luật security-panel (loop.mjs output-discipline — KHÔNG persist vào path committed); muốn log file .fgos/logs cần SUPERSEDE quyết định đó tường minh + gitignore + một cửa ghi (single-door .fgos) + đính stdout vào DispatchError cho timeout/spawn-fail (plan-checker STR31 B1/W1/W2 đã map đủ ràng buộc) | Quyết supersede-hay-không có decision record; nếu làm: log đọc được cho MỌI kết cục kể cả timeout, không phá single-door, không vào git | done | [worker-dispatch-log](history/worker-dispatch-log/) |
| STR33 | Feedback người tới worker: prompt worker (buildPrompt) thiếu answer từ gates + lý do reject mới nhất → vòng reject không hội tụ (run-3: reject kèm yêu cầu test, worker round-2 no-op vì không thấy lý do); kiểm fold `reason` trên work.move (đường destructure — critical pattern fold-allowlist) | Reject kèm lý do → worker round sau NHÌN THẤY lý do trong prompt và làm đúng; item dogfood có test master-trunk sau một vòng reject — GHI CHÚ: code + unit-proof đã ship (worker-execution-2); live-proof CoS bị anti-loop chặn trước dispatch → gộp vào CoS STR34 | done | [worker-execution](history/worker-execution/) |
| STR34 | Anti-loop vs vòng người: (a) reject/answer của NGƯỜI hiện ăn cùng ngân sách visit với vòng lỗi → item bị park 3/3 TRƯỚC khi worker kịp thấy lý do reject (run-4); (b) item nhánh-runner bị anti-loop park xong thì KHÔNG còn cửa công khai nào cho người hoàn tất đề xuất (pull-door đo main, không hợp việc trên nhánh; classifier cũng chặn đẩy tay — đúng tinh thần). Thiết kế: can thiệp người (answer/reject) làm mới/mở rộng ngân sách visit? verb người-tiếp-quản-nhánh? | Một vòng reject do người chủ động không bị anti-loop nuốt; item park 3/3 có đường người-hoàn-tất chính danh; live-proof STR33 (worker thấy lý do và làm đúng) chạy được | done | [human-rounds](history/human-rounds/) |
| STR20 | Registry-manifest máy-đọc cho architecture-map + script 2 phép kiểm trong verify: (a) chiều import một-chiều-xuống theo nhà đã khai, (b) đủ sổ — mọi file src/** bin/** có row (architecture-map §9.3, record ADR0010; đứng trước S2-friction) | Script đỏ khi có import ngược tầng hoặc file thiếu row; verify chain chạy nó; sổ §6 không drift được nữa | done | [0010](decisions/0010-ban-do-kien-truc-la-ban-chuan.md) + map §9.3 |
| STR2 | Routing trên state (Phase 2): frontier sẵn-sàng thành lệnh, chain handoff, recovery matrix + anti-loop, runner tuần tự | Agent lạ tự tìm việc kế tiếp bằng truy vấn derive, không danh sách tay (tiêu chí F3, luật L6) | done | [phase-2-routing](history/phase-2-routing/) |
| STR1 | forgent tự quản work-state của chính nó qua tầng state/FSM (Phase 1: store zero-dep, transition có precondition, single-door, CAS, decisions event-sourced) | Một agent lạ, không chat history, trả lời "đang ở đâu / việc gì đang mở" từ state chứ không từ trí nhớ (tiêu chí F2, luật L6) | done | [phase-1-state-layer](history/phase-1-state-layer/) |
