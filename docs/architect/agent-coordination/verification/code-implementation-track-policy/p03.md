# P03 — Proof Tiers, Plan-Loop Proof-Contract Fixes, And Baseline Correction — Cell Trace

- **Cell:** P03
- **Cell branch:** `code-implementation-track-policy--p03` (merged, deleted)
- **Coordination id:** `code-implementation-track-policy--p03`
- **testedSha:** `2f169f19` (final cell worktree tip, after the quorum-closing fixer round added the Phases table row — 167/167 targeted tests re-confirmed at this exact commit before merge)
- **integratedSha:** `945e60262ad352876ffcdfcdf2cfce5a81147a86` (`--no-ff` merge into track branch)
- **treeIdentical:** `true` — `git diff 2f169f19 945e6026 -- .` is empty; environment fingerprint unchanged throughout this cell (node v24.18.0; `package-lock.json` sha256:b097ecd850ca73e265a2dcbf94c63aa48c118069db53d42b00996fecd83fe2fc — same track worktree, no toolchain/lockfile edits between the pre-merge test run and the merge), so the pre-merge proof certifies `integratedSha` directly, per the non-inference rule's tree-identity exception (as amended in this same cell). Note: an earlier `testedSha` (`ba29344c`) recorded before this trace was finalized is NOT tree-identical to `integratedSha` — the two follow-up commits after `ba29344c` (this trace file, then the Phases table row) are real content; `2f169f19` is the correct `testedSha` to compare against.
- **Proof:** `full-suite-gate` (run given the scope: this cell touches plan-loop's proof-contract logic, which the whole suite's proof tiers depend on) — declared tier was `targeted`, escalated per the Lead's own judgment, not a mechanical trigger.

## First pass

- Doer (`claude`) — done. Commit `4f1010a5` ("feat(code-panel): focused/affected/full proof tiers; fix plan-loop proof-contract bugs").
- Reviewer — done. 7 MEDIUM + 6 LOW (no HIGH) across 4 claimed-bug areas.
- Red-team — done. 5 attack attempts: 2 HIGH + 2 MEDIUM succeeded (env-fingerprint gap in plan-loop's tree-identity rule; environmental-precondition mislabeling risk; a FULL_TRIGGERS category mismatch; an unenforced close rationale), 1 MEDIUM (evidence fabrication) failed per the red-team's own report.

## Fix round 1

Disposition: accepted — all 7 reviewer MEDIUM + 6 reviewer LOW, and all 4 succeeded red-team findings (2 HIGH + 2 MEDIUM). No action on the 1 failed red-team MEDIUM (evidence fabrication) — the attack did not succeed per the red-team's own report, so there is nothing to fix.

Fixer (`claude`) applied all findings, commit `ba29344c` ("fix: address P03 review/red-team findings + correct baseline double-count").

### Baseline double-count bug (discovered during fix round, not from review/red-team)

While re-verifying against the full suite, the fixer found that the "103 known failures" baseline recorded at P02 was wrong: it was actually **51 unique failing tests**. Node's test runner prints each failing test twice — once inline as it fails, and again in the final "failing tests:" recap — and P02's `grep -c "^✖"` counted both occurrences. The substantive P02 conclusion (0 new failures, byte-identical failure set) was unaffected; only the count was wrong.

Fixed in this cell:
- Corrected `docs/architect/agent-coordination/verification/code-implementation-track-policy/baseline-f60cae1b.txt` to the 51-line unique-failure list (confirmed present, 51 `✖` lines, each annotated with the same environmental-precondition note P02 recorded: all require a compiled `target/release/{fgctl,fgos}` binary this worktree shape has never built).
- Added a correction note at the top of `p02.md` pointing here, rather than silently editing P02's own historical record (P02's "103" language is left as-is below the note, as the record of what was actually known at P02 time).

### Full-suite gate (`npm test`) on `ba29344c`

Re-ran the identical full suite, clean env (`CLAUDE_CODE_*`/`CLAUDECODE` unset, per the known session-leak contamination P02 already identified), against `ba29344c`. Result: **51/51 unique failures match the corrected baseline exactly, 0 new.** `Proof: full-suite-gate` stands for this cell despite not being the declared minimum tier.

### Targeted tests

> **Evidence retrofit (recorded at the next cell, corrected once more at
> P04):** this section originally just said "167/167 pass," which does not
> satisfy the exact-command/duration/fingerprint evidence contract
> `fgos-code-panel`'s own "Record it" section requires (a real review
> finding). Re-verification below ran in the P04 cell worktree, whose tree
> (`a6e6b854`) is **not** identical to `2f169f19`'s (`7cdd1606`) — a P04
> reviewer finding caught an earlier, wrong "tree-identical" claim here,
> since the P04 worktree already carries P04's own edits to the very
> `fgos-code-panel/SKILL.md` this suite's mirror test reads. What the
> re-run does prove: the same command still returns 167/167 with no
> regression — a fresh confirmation, not a tree-identical rerun of the
> original `2f169f19` proof:

```text
command: node --test test/skills/*.test.mjs test/setup/*instruction*.test.mjs test/setup/skill-wrappers.test.mjs test/architecture.test.mjs
result: 167/167 pass
duration: 1987.07ms (re-verification run; original P03 run was not captured with this precision)
environment fingerprint: node v24.18.0; package-lock.json sha256:b097ecd850ca73e265a2dcbf94c63aa48c118069db53d42b00996fecd83fe2fc
```

## Deferred findings (not blocking this cell)

- Red-team MEDIUM (evidence fabrication) — attack failed per the red-team's own report; no action needed.

## Checkpoint identity

| Field | Value |
|---|---|
| phase/cell id | P03 |
| command | targeted: `node --test test/skills/*.test.mjs test/setup/*instruction*.test.mjs test/setup/skill-wrappers.test.mjs test/architecture.test.mjs` (167/167); full: `npm test` clean env (51/51 vs baseline) |
| baseline | 51 unique known failures @ `f60cae1b` (corrected this cell from P02's mistaken 103-double-count; see `baseline-f60cae1b.txt`) |
| testedSha | `2f169f19` |
| integratedSha | `945e60262ad352876ffcdfcdf2cfce5a81147a86` |
| environment fingerprint | node v24.18.0; `package-lock.json` sha256:b097ecd850ca73e265a2dcbf94c63aa48c118069db53d42b00996fecd83fe2fc |
| treeIdentical | `true` (empty diff `2f169f19`→`945e6026`, fingerprint above unchanged) |
| outcome | `checkpoint-verified` — 51/51 full-suite failures match corrected baseline exactly at `testedSha` (0 new), certified for `integratedSha` via the tree-identity exception; 167/167 targeted pass |
