# Cell P02.1 — Chain Verb And Pack Registration

Phase: `plans/260904-2329-group-thinking-plan-loop/phase-02-chain-verb-and-pack-registration.md`
Status: in-progress
Worktree/branch: (filled in by Coordinator once created)

## Requirements

R1-R8 as written in phase-02-chain-verb-and-pack-registration.md (copied
by reference — the phase file is canonical for exact wording).

## Proof Matrix

| Req | Code/Doc Path | Positive Test | Negative Test | Evidence | Status |
|---|---|---|---|---|---|
| R1 | core/protocol-packs/group-thinking.json | Tests First #5 | n/a | pending | missing |
| R2 | src/verbs/coordination/chain.mjs (new) | Tests First #1,2,3 | Tests First #4 (zero-match) | pending | missing |
| R3 | src/verbs/coordination/chain.mjs (--json) | Tests First #1 (json variant) | n/a | pending | missing |
| R4 | src/verbs/coordination/chain.mjs (read-only proof) | Tests First #7 | Tests First #7 (broken PoC trips check) | pending | missing |
| R5 | src/cli/command-registry.mjs, bin/fgos.mjs | Tests First #9 (grep) | n/a | pending | missing |
| R6 | (no new persistence — negative requirement) | code review (no cache file written) | n/a | pending | missing |
| R7 | bin/fgos.mjs (--cwd flag) | Tests First #8 | n/a | pending | missing |
| R8 | src/verbs/coordination/launch-master-loop.mjs (comment-only) | manual diff review | n/a | pending | missing |

## Commands

(Doer fills in real command output as work proceeds.)

## Grounding notes from Coordinator's cell-prep audit (verified before dispatch)

- `core/protocol-packs/group-thinking.json` and
  `core/coordination-protocols/standalone-master-coordination-loop.yaml`
  both exist — confirmed by direct file check. Read the fixture's real
  `metadata.id`/`metadata.version` before writing the pack entry (R1) —
  do not assume from the phase file's quoted id string without
  re-confirming.
- `src/cli/command-registry.mjs` and `bin/fgos.mjs` both exist. Mirror
  the exact registration pattern `show`/`launch-master-loop` already use;
  do not invent a new mechanism.
- Zero file overlap with P01.1 confirmed: P01.1 touches
  `src/runner/coordination/session-engine.mjs`,
  `src/runner/dispatch/execution-contract.mjs`,
  `src/runner/dispatch/assignment-normalizer.mjs`,
  `src/runner/dispatch/operation-choice.mjs` (only if needed),
  `src/verbs/coordination/schema.mjs`, `src/runner/coordination/store.mjs`,
  `docs/how-to/run-a-coordination-session.md`, `CHANGELOG.md` (own line),
  `test/runner/coordination-*.test.mjs`, `test/runner/dispatch-*.test.mjs`,
  `test/runner/assignment-dispatch.test.mjs`, `test/architecture.test.mjs`.
  None of these appear in P02.1's own Files list. `CHANGELOG.md`'s
  `## [Unreleased]` section is the one intentional shared file (each cell
  adds its own line; Coordinator resolves the merge, not a lease
  violation per plan.md).

## Review

(pending — filled in by independent Reviewer)

## Red-Team

(pending — filled in by independent Red-Team)

## Gaps

(none recorded yet)
