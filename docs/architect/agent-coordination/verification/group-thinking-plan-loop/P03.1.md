# Cell P03.1 — Plan-Loop Skill And Live Proof

Phase: `plans/260904-2329-group-thinking-plan-loop/phase-03-plan-loop-skill-and-live-proof.md`
Status: review-needed (R1-R4 done, Coordinator-verified; R5's live proof paused — see "Coordinator note on R5" below)
Worktree/branch: `.claude/worktrees/agent-a3890b158d4a0d9c2` / `worktree-agent-a3890b158d4a0d9c2` @ `362be7e9`

## Coordinator independent verification (Doer, R1-R4)

- `git diff --stat dfbe6314..HEAD`: 7 files, all within the justified
  scope (SKILL.md + its two mirrors, master-coordinator.md's one line,
  the report, this trace) — zero touches to `fgos-test-drive`,
  `docs/specs/runner.md`'s stop-gate, or any kernel/dispatch/verbs file.
- `master-coordinator.md` diff confirmed exactly: 1 file, 3 insertions
  (a blank line + the one pointer line), nothing else changed.
- Mirror byte-identical, confirmed directly (`diff core/skills/...
  .agents/skills/...` and `... plugins/fgOS/skills/...` both empty).
- Re-ran the relevant tests myself: 47/47 pass
  (`test/skills/fgos-mirror.test.mjs` 13, `test/setup/skill-wrappers.test.mjs`
  26, `test/architecture.test.mjs` 8).
- Spot-checked SKILL.md content: accurately states the FINAL mutation
  rule (including the fail-closed `isReadOnlyMode: false` requirement
  from P01.1's Fix Round 3, not an earlier draft), and all three
  templates show genuine 3-executor diversity (`codex-cli`/`claude`/
  `agy-cli`) as a first-class, visible property.
- The `core/skills/fgos-plan-loop/` deviation from the literal Files
  list is accepted as genuinely necessary (verified the build pipeline's
  own prune behavior would silently delete an orphaned `.agents/skills`
  entry lacking a `core/skills` source — matches the already-merged
  sibling skill's own pattern) — not scope creep for convenience.

## Requirements

R1-R7 as written in phase-03-plan-loop-skill-and-live-proof.md (copied
by reference — the phase file is canonical for exact wording).

## Coordinator note on R5 (live proof) — paused, not blocked-forever

R5 requires a live proof on `/home/vantt/projects/fgos-test-drive`, a
REAL, separate host project, involving: killing a real OS process,
dispatching through real distinct executor CLIs (`codex-cli`/`agy-cli`
equivalents), and mutating that project's own git history.

Checked before dispatching anything: `fgos-test-drive` exists
(`/home/vantt/projects/fgos-test-drive`, real git repo, `first commit`
521b44f) and has REAL, current, in-progress fgOS work items in its own
`.fgos/state.json` (e.g. `tsk-2rl`, a blocked feature task) — this is
not an empty sandbox. Its own `.fgos-runner.json` configures exactly ONE
executor (`claude`) — no `codex-cli`/`agy-cli`-equivalent second/third
provider is configured, so R5's own cross-provider dispatch requirement
is NOT currently satisfiable without a real setup step first (installing
or configuring additional executor CLIs, possibly requiring credentials
this session cannot verify are appropriate to provision autonomously).

This matches master-coordinator.md's own named STOP GATE verbatim:
"test/runtime environment cannot produce trustworthy evidence (for
example, a live proof needs a configured executor that is absent)."
Also weighed under this repo's own "Executing actions with care"
guidance: R5 mutates a SEPARATE real project (not a forgentX worktree),
kills real processes, and runs for a real, extended duration — the kind
of hard-to-reverse, cross-system action that warrants a person's
go-ahead rather than an unattended overnight run, especially given
`fgos-test-drive` has real, in-progress, unrelated user work already on
it that a mis-run proof could disrupt.

**Decision**: R1-R4 (SKILL.md authoring, templates, generated mirror,
master-coordinator.md's one Retirement line) proceed now — these are
additive, forgentX-scoped, fully reversible docs/skill work with zero
external risk, same as every other cell in this track. R5 (the live
proof) and R6/R7 (which depend on R5's own real evidence) are prepared
but NOT executed autonomously — left as the explicit next step for the
user, with the exact blocker (executor setup decision + go-ahead to run
against `fgos-test-drive` specifically) named plainly.

## Files

May touch (R1-R4 only, this pass):
- `.agents/skills/fgos-plan-loop/SKILL.md` (new)
- `plugins/fgOS/skills/fgos-plan-loop/**` (generated mirror, via
  `npm run build:skills` — confirm the real command name first)
- `docs/architect/agent-coordination/playbooks/prompts/master-coordinator.md`
  — Retirement section only, one line
- This cell's own report file

Do NOT touch (this pass, R5 deferred):
- `/home/vantt/projects/fgos-test-drive` — no live proof executed yet
- `docs/specs/runner.md`'s stop-gate paragraph (R7 — depends on R5's
  own real evidence, not honest to write yet)
- Any file under `src/runner/coordination/**`, `src/runner/dispatch/**`,
  `src/verbs/coordination/**`

## Proof Matrix

| Req | Status |
|---|---|
| R1 | done — `core/skills/fgos-plan-loop/SKILL.md` (canonical source, 514 lines) covers resume (`chain`), open (`open.json`), read/disposition, fix round (`fix-N.json`), close (`close.json` + Lead's own merge/worktree-removal), the four-condition Mutation Rule, and the Non-Goals (no Work involvement, no git authority inside the session, Phase 01 commit-policy). Every field cited against real code (see report). |
| R2 | done — `plugins/fgOS/skills/fgos-plan-loop/SKILL.md` generated via the real `npm run build:skills` (`scripts/build-skill-wrappers.mjs`), never hand-edited; byte-identical to `.agents/skills/fgos-plan-loop/SKILL.md`, confirmed by `test/skills/fgos-mirror.test.mjs` (13/13 pass) and `test/setup/skill-wrappers.test.mjs`'s own drift guard (26/26 pass). |
| R3 | done — three request templates (`open.json`, `fix-N.json`, `close.json` shapes) inlined in SKILL.md, every field cited against the current `src/verbs/coordination/schema.mjs` (not the retired proposal sketch — that doc no longer exists on this branch, see Gaps). Each template declares a 3-actor `actors[]` roster spanning 3 distinct executors (`codex-cli`/`claude`/`agy-cli`) with per-actor `tier`, and every dispatching step sets `targetActorId` explicitly (required for the policy to actually apply — `run.mjs:432`). |
| R4 | done — `master-coordinator.md`'s own "Retirement" section gained exactly one new pointer line naming `fgos-plan-loop`; nothing else in the file changed (`git diff --stat` shows `1 file changed, 3 insertions(+)`: one blank separator line + the one pointer line). |
| R5 | paused — needs user decision (executor setup + go-ahead on fgos-test-drive) |
| R6 | blocked on R5 |
| R7 | blocked on R5 |

## Commands

```
node -e "assembleSkills/generateAllSkillWrappers/mirrorDevSkillsIntoPlugin" (via) npm run build:skills
node --test test/skills/fgos-mirror.test.mjs        # 13 pass
node --test test/setup/skill-wrappers.test.mjs       # 26 pass
node --test test/architecture.test.mjs               # 8 pass (isReadOnlyMode posture invariant unaffected)
git diff --stat docs/.../master-coordinator.md        # 1 file changed, 3 insertions(+)
diff core/skills/fgos-plan-loop/SKILL.md .agents/skills/fgos-plan-loop/SKILL.md   # identical
diff .agents/skills/fgos-plan-loop/SKILL.md plugins/fgOS/skills/fgos-plan-loop/SKILL.md  # identical
```

## Review

(pending)

## Red-Team

(pending)

## Gaps

- **Design proposal doc missing on this branch.** The dispatch instructions
  named `docs/architect/proposals/group-thinking-plan-loop.md` as a Must Read
  ("has the templates' own original sketch, R3 says do NOT copy verbatim").
  That file does not exist anywhere in `group-thinking-plan-loop`'s own git
  history on this branch (confirmed: `find docs/architect/proposals -iname
  "*plan-loop*"` returns nothing after the `--ff-only` merge that brought in
  the track's real committed content). R3's own instruction to re-verify every
  field against real code rather than copy the sketch made this a non-blocker
  — every template field in `core/skills/fgos-plan-loop/SKILL.md` was derived
  directly from `src/verbs/coordination/schema.mjs`,
  `src/verbs/coordination/launch-master-loop.mjs`, and
  `core/coordination-protocols/standalone-master-coordination-loop.yaml`
  instead. Naming it here so the Coordinator knows the proposal doc reference
  in the dispatch prompt is stale/uncommitted, not that this Doer skipped it.
- **`core/skills/fgos-plan-loop/` was required beyond the cell's literal "May
  touch" list.** The cell's Files section named only
  `.agents/skills/fgos-plan-loop/SKILL.md` (new) and the generated
  `plugins/fgOS/skills/fgos-plan-loop/**` mirror. The real, current build
  pipeline (`src/setup/skill-wrappers.mjs`'s `assembleSkills`, `prune: true`
  by default, called with no override by `scripts/build-skill-wrappers.mjs`)
  deletes any `.agents/skills/<name>` entry that has no matching
  `core/skills/<name>` or `domains/*/skills/<name>` source as an orphan on
  every `npm run build:skills` run — confirmed both by reading
  `skill-wrappers.mjs:215-319` and by the already-merged sibling skill in this
  same track (`core/skills/fgos-group-thinking/SKILL.md`, byte-identical to
  its own `.agents/skills` copy). Authoring `.agents/skills/fgos-plan-loop/
  SKILL.md` directly, without a `core/skills/fgos-plan-loop/` source, would
  have made the very next `npm run build:skills` run silently delete it.
  `core/skills/fgos-plan-loop/SKILL.md` was added as the necessary canonical
  source for this reason — same category of file as the sibling skill already
  on this branch, not a new pattern. Flagging for Coordinator/Reviewer
  awareness since it is a real deviation from the literal file list, even
  though the alternative (skip `core/skills/`) would have broken R1/R2 on the
  very next build.
- **Session-id charset vs. this track's own cell-trace-file naming.**
  `coordinationId` must satisfy the safe filesystem charset (letters/digits/
  underscore/hyphen only, `schema.mjs:29,64-69`) — this track's own
  cell-trace files use a period (`P01.1.md`, `P03.1.md`). A future Lead using
  `fgos-plan-loop` on THIS track must pick a charset-safe cellId (e.g.
  `p03-1`) for the real `coordinationId`, never the period-containing
  trace-file name verbatim. Documented directly in SKILL.md section 0 so a
  fresh Lead session hits this note before hitting the schema rejection.
