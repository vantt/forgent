# Cell P01.1 — Mutation Unlock

Phase: `plans/260904-2329-group-thinking-plan-loop/phase-01-mutation-unlock.md`
Status: recheck-needed (Fix Round 2 — confusion-of-authority binding — Coordinator-verified 665/666; dispatching third full Reviewer+Red-Team recheck)
Worktree/branch: `.claude/worktrees/agent-ab7ff2ac5eda7a106` / `worktree-agent-ab7ff2ac5eda7a106`, Doer `8b24c8a2`, Fixer R1 `8883e0c9`, follow-up `452f26fa`, Fixer R2 `305338f0`

## Coordinator independent verification (Fixer Round 2)

- `git diff --stat 452f26fa..305338f0`: `assignment-runner.mjs`, the test
  file, this trace — zero forbidden-path touches, confirmed.
- Read the actual fix: `assignmentIsMemberOfARealSession` requires
  `asgn.assignmentId` to already appear in some real, on-disk coordination
  session's own `assignmentRefs` — the one array only
  `createSessionAssignment`'s `completeAssignmentRegistration`
  (`coordination/store.mjs`) ever appends to, gated behind that session's
  own active/authorization checks. Read via plain `fs`, never importing
  `coordination/store.mjs` (stays dispatch/infra layer, confirmed against
  `test/architecture.test.mjs`'s own layering check). Added as an
  ADDITIONAL check after R2/R3 pass, not a replacement — defense in
  depth confirmed intact.
- Verified `createAssignmentId` is deterministic, not
  cryptographically random (`asgn_<workId-or-writerId>_<op>_<seq>`) — the
  fix correctly does NOT rely on id secrecy, only on real on-disk
  registration membership, which is the right invariant regardless of id
  guessability.
- Confirmed the previously-vulnerable Round-1 test assertion was
  genuinely FLIPPED to assert refusal (not deleted/weakened) — grepped
  the test file directly, both the flipped test and Red-Team's own new
  reproduction now assert the new refusal message.
- Re-ran the focused command myself: 665/666 pass — matches the Fixer's
  claim. R9 smoke command re-confirmed passing.
- One residual angle worth Red-Team's own third pass: is a real,
  ALREADY-LEGITIMATELY-issued `assignmentId` single-use / bound to the
  specific payload it was originally registered for, or could it be
  "replayed" — reused for a DIFFERENT objective/payload after (or even
  before) its own legitimate execution? This is a natural evolution of
  the ticket-reuse framing the fix already closes one layer of.

## Requirements

R1-R9 as written in phase-01-mutation-unlock.md (copied by reference, not
duplicated here — the phase file is canonical for exact wording).

## Proof Matrix

| Req | Code/Doc Path | Positive Test | Negative Test | Evidence | Status |
|---|---|---|---|---|---|
| R1 | src/verbs/coordination/schema.mjs (assertMutationReadOnly -> assertMutationAllowed) | test/runner/coordination-mutation-unlock.test.mjs "R1: a request-file operation step may declare..." | same test, authorize/disposition/fan-out branch sub-assertions | real, run, passing | done |
| R2 | src/runner/coordination/session-engine.mjs (assertMutatingDispatchAllowed, result.kind check) | Tests First #1(a)/(b) | Tests First #3 (advisory kind refused) | real, run, passing | done |
| R3 | src/runner/coordination/session-engine.mjs (assertMutatingDispatchAllowed) + src/runner/paths.mjs (resolveMainCheckoutRoot/resolveRepoRoot) | Tests First #1(a)/(b) (real linked worktree via `git worktree add`) | Tests First #2(a) (main checkout refused) + #2(b) (outside-git refused, fail-closed-on-null) | real, run, passing | done |
| R4 | src/runner/coordination/session-engine.mjs (buildSessionContract, renamed) | Tests First #1,4 | Tests First #5(b) (proposeConsult/dispatchPrimaryTask unaffected, structural param-absence proof in coordination-r7-work-isolation.test.mjs) | real, run, passing | done |
| R5 | src/runner/coordination/session-engine.mjs (runExecutorAttempt: `isReadOnlyMode: assignment.mutation !== 'mutating'`) | existing static call-site test (coordination-declared-consult.test.mjs, still exactly 1 site) + test/architecture.test.mjs enumeration | n/a | real, run, passing | done |
| R6a | src/runner/dispatch/execution-contract.mjs, assignment-normalizer.mjs (carriesProtocolOperationStamp gate) | Tests First #1(a)/(b) (real mutating inline contract accepted) | pre-existing inline-mutating-rejected tests (assignment-dispatch.test.mjs) still pass unchanged | real, run, passing | done |
| R6b | test/architecture.test.mjs (executeAssignment(...) enumeration, 4 real call sites codebase-wide) | "executeAssignment(...) isReadOnlyMode posture..." test | "R6b posture check actually catches a violation (deliberately broken synthetic input...)" | real, run, passing | done |
| R6c | src/runner/dispatch/operation-choice.mjs -- investigated, proven a verified non-issue, NOT touched | coordination-mutation-unlock.test.mjs "R6c: operation-choice.mjs single buildAssignment(...) call..." (real source-grounded structural test) | n/a | real, run, passing; see report for full reasoning | done (non-issue, no fix needed) |
| R7 | src/runner/dispatch/assignment-runner.mjs / plan.mjs -- investigated, proven a verified non-issue, NOT touched | Tests First #1(a) real persisted dispatch-plan.json assertion (`'cwd' in compiledPlan.invocation === false`) | n/a | real, run, passing; commit-policy decided (see report) | done (non-issue, no fix needed) |
| R8 | src/runner/coordination/store.mjs (resolveCoordinationPaths: fgosDirFromRoot(cwd) -> fgosDirFromRoot(root); PLUS a second sibling bug found by real test execution -- the fallback `resolveMainCheckoutRoot(process.cwd())` chain, which resolved `root` against the CALLING PROCESS's own cwd for a non-git `cwd`, now anchored to `cwd` via `resolveRepoRoot(cwd, {strict:true})`) | Tests First #7 (`R8: resolveSessionPaths keys .fgos on the resolved MAIN CHECKOUT root...`) + Tests First #1(a)'s real `.fgos` location assertions | full regression sweep (659/660 focused tests; the one non-pass is a pre-existing, environment-caused false positive, see Gaps) | real, run, passing | done |
| R9 | process requirement: dispatch smoke command | Commands section below, re-run after every src/runner/dispatch/** edit | n/a | real, run, passing throughout | done |
| Fix Round 1 (Red-Team HIGH) | src/runner/dispatch/execution-contract.mjs (`extractProtocolOperationStamp`/`operationDeclaresWorkProduct`/`resolveMutatingCwdPosture`, shared predicates), src/runner/dispatch/assignment-runner.mjs (`assertInlineMutatingAssignmentAuthorized`, the real dispatch-layer gate, called from `validateAssignmentLegality`), src/runner/coordination/session-engine.mjs (`assertMutatingDispatchAllowed` refactored to call the SAME shared predicates, byte-identical error messages/types) | 5 new tests, `test/runner/coordination-mutation-unlock.test.mjs` "Fix round: ..." / "Fix round R2: ..." / "Fix round R3: ..." (see Commands) | same 5 tests, each IS its own negative case except the 2 positive controls | real, run, passing; reproduces the Red-Team's own exact attack shape and confirms it is now refused | done |
| Fix Round 2 (Red-Team HIGH recheck -- confusion-of-authority) | src/runner/dispatch/assignment-runner.mjs (`assignmentIsMemberOfARealSession`, a new, additive check inside `assertInlineMutatingAssignmentAuthorized`: requires `asgn.assignmentId` to already be a member of some real, on-disk coordination session's own `assignmentRefs`, read directly via `fs` under the resolved main-checkout root -- never weakens or replaces the Fix Round 1 R2/R3 checks, which still run first) | `test/runner/coordination-mutation-unlock.test.mjs` "Fix round: the LEGITIMATE path..." (dispatchDeclaredOperation, real session, still passes) | "Fix round 2: a direct buildAssignment + executeAssignment call whose stamp resolves to a REAL work-product operation... is STILL refused when no real coordination session ever registered this Assignment" (the Round-1 test that previously asserted this as an intentional pass, now flipped) + "Fix round 2: Red-Team's own exact reproduction..." (borrowed the REAL shipped `core.coordination-protocol.standalone-master-coordination-loop@1.0.0#produce-candidate` operation id, zero coordination fixture written, unrelated objective) | real, run, passing; reproduces Red-Team's own recheck attack (`recheck1-borrowed-real-ticket.mjs`) and confirms it is now refused | done |

## Commands

Focused regression sweep (real output, re-run after the Fix Round 1 fix, below):

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'
...
ℹ tests 665
ℹ suites 0
ℹ pass 664
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```
(665 = the original 660 + 5 new Fix Round 1 regression tests in
`test/runner/coordination-mutation-unlock.test.mjs`, all passing. The 1
failure is the SAME pre-existing false positive as before the fix:
`src/runner/coordination/** imports no Work lifecycle, merge, worktree,
transport-spawn, or mission-lite module` in
`test/runner/coordination-static.test.mjs` -- this agent's own worktree
checkout path contains the literal substring "worktrees"
(`.claude/worktrees/agent-<id>/...`), which the test's absolute-path
substring match flags regardless of any real import. Confirmed via `git
stash` against the untouched baseline before the original Doer pass:
identical failure mode, present before this cell's diff existed. Not a
real regression; see report.)

**Fix Round 1 (Red-Team HIGH finding, stamp-forgery bypass) -- isolated
run of the new regression tests:**

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test test/runner/coordination-mutation-unlock.test.mjs
...
ℹ tests 17
ℹ pass 17
ℹ fail 0
```
All 5 new "Fix round" tests pass: (1) the Red-Team's own exact
reproduction (forged stamp naming a nonexistent definition, direct
`buildAssignment`+`executeAssignment` call, never touching
`dispatchDeclaredOperation`) is now refused with no canary file written;
(2) an R3-isolated case (forged stamp naming a REAL definition + a REAL
`work-product` operation, but `cwd` = the main checkout) is refused
naming "main checkout"; (3) an R2-isolated case (forged stamp naming a
REAL definition but an operation declaring `result.kind: 'advisory'`,
against a REAL linked worktree `cwd`) is refused naming
`does not declare result.kind "work-product"`; (4) a positive control
(a genuinely well-formed stamp + real worktree, called directly via
`buildAssignment`+`executeAssignment`, bypassing `dispatchDeclaredOperation`)
still dispatches and writes its real output file, proving the new gate is
not over-broad; (5) the LEGITIMATE path
(`dispatchDeclaredOperation`, real `definitionRef`, real linked worktree)
is unaffected, still grading `verified` with the real output file present.

R9 smoke command, re-run after the fix (touches `src/runner/dispatch/**`),
still passing:

```
$ node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
fgos: executor "gitnexus" declares no "providerModel"/"provider" and its command (none resolvable from its config) is not a recognized Claude CLI command -- its resolved provider family may be unreliable/inconsistent across dispatch code paths. Declare "providerModel" explicitly.
{"mechanism":"in-process","configured":false}
```

**Fix Round 1 follow-up (Stable Code Artifacts correction)**: dropped the
`"(Phase 01 mutation-unlock RN re-verification)"` suffix from the 3 new
runtime error strings in `assertInlineMutatingAssignmentAuthorized`
(`assignment-runner.mjs`) per the Coordinator's disposition above — the
message text before each suffix already states the real reason plainly,
so no meaning is lost. Grepped `test/` first: no test asserted the exact
suffix text (every existing/new regex match targets the substring before
it), so no test changes were needed. Re-ran both commands after the edit,
real output:

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'
ℹ tests 665
ℹ pass 664
ℹ fail 1
```
(identical counts to before this follow-up; same pre-existing
`coordination-static.test.mjs` worktree-path false positive.)

```
$ node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
{"mechanism":"in-process","configured":false}
```

**Fix Round 2 (Red-Team recheck HIGH, confusion-of-authority)**: the
Round 1 fix's own R2/R3 re-verification proved the CLAIMED
`definitionId@version#operationId` resolves to a real, work-product
declaring operation -- but never bound the Assignment itself to any real
session/dispatch record, so any real, already-shipped operation id
(including a core-tier one, discoverable by id alone) worked as a
universal ticket for an unrelated payload. Added
`assignmentIsMemberOfARealSession` (`assignment-runner.mjs`): after the
existing R2/R3 checks pass unchanged, additionally requires
`asgn.assignmentId` to already be a member of some real, on-disk
coordination session's own `assignmentRefs` array -- the one thing only
`createSessionAssignment`'s `completeAssignmentRegistration`
(`coordination/store.mjs`) ever appends to, always after that session's
own active-status/authorization checks already passed. Read directly via
`fs` (no import of `coordination/store.mjs`, staying in the dispatch/infra
layer per `test/architecture.test.mjs`'s layering check). The one Round 1
test that asserted the now-closed bypass shape as an intentional pass
("a direct buildAssignment + executeAssignment call whose stamp genuinely
matches a real work-product operation... is still allowed through") is
flipped to assert refusal; a new test reproduces Red-Team's own exact
recheck attack (unrelated objective, the REAL shipped
`core.coordination-protocol.standalone-master-coordination-loop@1.0.0#produce-candidate`
operation id, zero coordination fixture written anywhere, direct
`buildAssignment` + `executeAssignment`). Real output:

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test test/runner/coordination-mutation-unlock.test.mjs
ℹ tests 18
ℹ pass 18
ℹ fail 0
```

```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'
ℹ tests 666
ℹ pass 665
ℹ fail 1
```
(666 = the 665 after Fix Round 1 + 1 net-new test, one Round-1 test
flipped in place rather than added; same pre-existing
`coordination-static.test.mjs` worktree-path false positive, confirmed
via the same failure signature as every prior run.)

```
$ node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
{"mechanism":"in-process","configured":false}
```

R9 smoke command, re-run after every edit to `src/runner/dispatch/**`, still passing at close:

```
$ node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
fgos: executor "gitnexus" declares no "providerModel"/"provider" and its command (none resolvable from its config) is not a recognized Claude CLI command -- its resolved provider family may be unreliable/inconsistent across dispatch code paths. Declare "providerModel" explicitly.
{"mechanism":"in-process","configured":false}
```

R8 grep evidence (every `fgosDirFromRoot(` call site under `src/runner/coordination/**` and `src/runner/dispatch/**`, re-run at close time):

```
$ grep -n "fgosDirFromRoot(" -r src/runner/coordination/ src/runner/dispatch/
src/runner/coordination/store.mjs:71:  const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:361:  const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:620:    const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:629:    const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:680:  const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:917:        const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:999:        const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/cli.mjs:1242:        const fgosDir = fgosDirFromRoot(root);
src/runner/dispatch/assignment-runner.mjs:573:  const fgosDir = fgosDirFromRoot(root);
```
Every site now passes `root`, including the one this cell fixed (store.mjs). The 7 cli.mjs sites and the 1 assignment-runner.mjs site were already correct (confirmed, matches grounding notes) and needed no change.


## Grounding notes from Coordinator's cell-prep audit (verified before dispatch)

- `src/cli.mjs` named in the phase file's R6b/R6c does not exist. The real
  file is `src/runner/dispatch/cli.mjs` — confirmed `executeAssignment(`
  call sites at lines 957 and 1129 there.
- `src/runner/dispatch/operation-choice.mjs:2198` confirmed:
  `const runResult = await executeAssignment(assignment, opts);` — no
  explicit `isReadOnlyMode` key passed at all. R6c's gap is real.
- `buildReadOnlyContract` is at `session-engine.mjs:189`.
  `PROTOCOL_OPERATION_STAMP_PREFIX` at line 156.
  `runExecutorAttempt` at line 280; its `executeAssignment` call at
  line 281 (re-verify exact line before editing — off by one vs the
  phase file's 280-282 estimate).
- R8 bug confirmed exactly at `store.mjs`, function
  `resolveCoordinationPaths()` (lines 48-59): `root` is computed
  correctly (lines 50-55) but `fgosDir` is keyed on raw `cwd` at line 56.
  Grep of every other `fgosDirFromRoot(` call site under
  `src/runner/coordination/**` and `src/runner/dispatch/**` (7 sites in
  `cli.mjs`, 1 in `assignment-runner.mjs:573`) shows all others already
  pass `root` correctly — store.mjs:56 is the only real instance. Re-grep
  to confirm before closing (repo may have changed since audit).

## Review

Independent Reviewer verdict: **APPROVE**. Verified against real code and a
real, independent test run — not the Doer's own narration. Diff base
`86d0106c..8b24c8a2`.

Method: read every touched file in full (`session-engine.mjs`, `store.mjs`,
`execution-contract.mjs`, `assignment-normalizer.mjs`, `schema.mjs`,
`paths.mjs`, `test/architecture.test.mjs`, both test files), traced
call chains by hand (not by trusting the report's line-number claims), and
independently re-ran the focused regression sweep and R9 smoke command.

**R1** — Verified exhaustively. `grep -n "assertMutationAllowed("
src/verbs/coordination/schema.mjs` finds exactly 7 call sites: only
`validateOperationStep` (operation step) passes `{ allowMutating: true }`;
`task.mutation`, `authorize`/`disposition` steps, fan-out branch, fan-out
step, and contribution step all call it with the 2-arg form, defaulting
`allowMutating: false`. Scoping is real and exhaustive, not just claimed.

**R2/R3** — Read `assertMutatingDispatchAllowed` (session-engine.mjs
~2150-2194) in full. The `operation` parameter traces back to
`resolveDeclaredOperationActor(definition, operationId, ...)`, where
`definition` is the FlowDefinition loaded via `loadCoordinationProtocol` —
never a caller-supplied claim, confirming R2. R3's comparison is exactly
`resolveMainCheckoutRoot(cwd) === resolveRepoRoot(cwd)` (never against raw
`cwd`), and the `null` case (outside git) refuses before the comparison
even runs. Read `src/runner/paths.mjs` directly: `resolveMainCheckoutRoot`
never throws (returns `null` on git failure) and `resolveRepoRoot(cwd)`
(non-strict) throws with `category: 'validation'` when `cwd` isn't inside a
git repo or has no resolvable HEAD — both paths are caught and converted to
a `CoordinationError`, so the fail-closed claim holds. Minor/informational:
the `try { resolveMainCheckoutRoot(cwd) } catch { null }` wrapper is dead
code (the function never throws per its own contract) — harmless, not a
finding worth blocking on.

**R4/R5** — `buildSessionContract` (renamed, confirmed old name gone via
`grep -c buildReadOnlyContract` → 0 matches) defaults `mutation =
'read-only'`; `dispatchPrimaryTask`/`proposeConsult`'s own calls are
unchanged aside from the rename (diff shows no parameter changes at those
2 call sites). `runExecutorAttempt` is confirmed the sole
`isReadOnlyMode: assignment.mutation !== 'mutating'` expression anywhere in
the repo (see R6b below).

**R6a/R6b** — `PROTOCOL_OPERATION_STAMP_PREFIX`/`carriesProtocolOperationStamp`
correctly live in `execution-contract.mjs` (dispatch/infra layer);
`session-engine.mjs` imports rather than redeclaring, and the old
module-local `const PROTOCOL_OPERATION_STAMP_PREFIX` line is gone from the
diff. `assignment-normalizer.mjs`'s `stampInlineAssignment` re-checks the
identical stamp condition as a second gate. Independently enumerated every
`executeAssignment(` call site codebase-wide via
`grep -rn "executeAssignment(" src/ bin/`: exactly 4 real call sites
(`session-engine.mjs:313`, `cli.mjs:957`, `cli.mjs:1129`,
`operation-choice.mjs:2198`) — matches the test's own `expectedFiles`
enumeration and its asserted per-file count (1/2/1). Read
`test/architecture.test.mjs`'s `checkExecuteAssignmentCallSitePostures` in
full: it is a real, non-vacuous check (the "deliberately broken synthetic
input" test proves it actually flags violations for all 4 known files plus
an unknown-file case, and a separate assertion proves the real, current
shapes produce zero violations — not a rubber-stamp).

**R6c** — Independently traced, not trusted from the report. Read
`operation-choice.mjs:2189-2198`: the one `buildAssignment({work, stage,
operation, contextRefs, expectedFiles, options: opts})` call never sets
`provenance`. Read `assignment.mjs`'s declared-assignment builder
(~line 420-462): when `provenance` isn't supplied, it unconditionally
builds `provenance: { kind: 'declared', ... }` and stamps `mutation` via
`stampDeclaredAssignment({role, operation})` — a wholly separate,
pre-existing, role/operation-based classifier (`classifyDeclaredMutation`)
that predates this cell and is out of its scope. This confirms R6c's claim
structurally: `operation-choice.mjs`'s call site can never produce an
inline-provenance Assignment, so the R6a stamp-forgery risk is genuinely
unreachable from there. Also confirms the test's own comment is accurate
that this call site's DECLARED-mutation posture is governed by a different,
older mechanism, correctly out of this cell's R1-R9 scope.

**R7** — Independently read `src/runner/dispatch/plan.mjs`'s
`compileDispatchPlan`: every return path sets `invocation: null` or
`invocation: { via: 'cli', adapter, protocol: 'prompt-stdout-v1' }` (no
`cwd` field on any branch, confirmed by reading the literal object at
~line 261-263). This makes
`effectiveCwd = compiledPlan?.invocation?.cwd ?? compiledPlan?.cwd ?? cwd`
in `assignment-runner.mjs` always collapse to plain `cwd`, matching
`computeChangedFiles(cwd, ...)`'s own input — no mismatch possible on this
path. Verified non-issue, correctly not touched.

**R8** — Read `store.mjs`'s `resolveCoordinationPaths` in full: fix is
correct (`fgosDirFromRoot(root)`, not raw `cwd`), and the sibling fallback
fix (`resolveRepoRoot(cwd, {strict:true})` instead of a second
`resolveMainCheckoutRoot(process.cwd())` probe) is real and matches
`resolveRepoRoot`'s own documented `strict` contract in `paths.mjs`
(`if (strict) return cwd` — never shells out, exactly as claimed). Traced
`resolveSessionPaths` → `resolveCoordinationPaths` to confirm this is the
ONE choke point every session/assignment/claim path in `store.mjs` funnels
through — no other cwd-keyed sibling bug found in the file (checked
`assignmentsDir`, `taskClaimPath`, and every other path derivation; all
derive from `resolveSessionPaths`'s already-fixed output).

**R9** — Re-ran independently:
`node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access`
→ prints a real `{"mechanism":"in-process","configured":false}`. Passes.

**Test realism** — Read `test/runner/coordination-mutation-unlock.test.mjs`
in full (578 lines). Tests First #1(a)/(b) through #7 are all real: a real
temp git repo + real `git worktree add` linked worktree, a real subprocess
fake-executor that writes/commits files, real `assert.equal(runResult.confidence,
'verified')` with real `changedFiles` assertions, real negative tests
(`assert.rejects` with message-content checks for "main checkout",
"does not resolve inside any git checkout", `result.kind "advisory"`). Not
phantom tests. `test/runner/coordination-r7-work-isolation.test.mjs`'s
2 updated tests were checked for weakening: they were narrowed
(from "no `dispatchDeclaredOperation`-family function ever accepts mutation"
to "only `dispatchDeclaredOperation` does, with a `'read-only'` default")
honestly, not gutted — the new assertions still do real static source
scanning and the file's own header comment plainly names the new,
still-open multi-actor-same-worktree question rather than silently
absorbing it.

**Independent regression run** (own execution, not trusted from the
report):
```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'
ℹ tests 660
ℹ pass 659
ℹ fail 1
```
The 1 failure is `test/runner/coordination-static.test.mjs`'s worktree-import
check, confirmed pre-existing and not caused by this diff: that test file is
NOT in this cell's changed-file list (confirmed via `git diff --stat
86d0106c..HEAD`), and the failure mechanism (substring-matching "worktree"
against each import's resolved absolute path) is a pure function of the
sandbox's own checkout path (`.claude/worktrees/agent-.../`) plus an
unmodified test file — it cannot have been introduced by this cell's diff
by construction, independent of the Doer's own `git stash` account.

**Scope/lease** — `git diff --stat 86d0106c..HEAD` shows exactly 13 changed
files, all within the cell's declared "Files: may touch" list. Zero
`bin/fgos.mjs`/`src/cli/command-registry.mjs`/`test/cli/**`/
`core/protocol-packs/group-thinking.json`/`chain.mjs`/
`launch-master-loop.mjs` touched. `run.mjs` confirmed genuinely untouched
(`grep -n "mutation" src/verbs/coordination/run.mjs` → 0 matches) — the
wiring-gap Gap #3 is honestly named, not silently left broken.

**Findings**: 0 HIGH, 0 MEDIUM, 1 LOW (informational, non-blocking — the
dead `try/catch` around `resolveMainCheckoutRoot` in
`assertMutatingDispatchAllowed`, harmless since that function never
throws).

Verdict: **APPROVE**.

### Recheck (post-HIGH-fix)

Rechecked the combined diff `8b24c8a2..HEAD` (commits `8883e0c9`,
`452f26fa`) after Red-Team's HIGH finding and its fix rounds. Did not trust
the Fixer's or Coordinator's own narration — read every changed line, traced
the exact bypass Red-Team demonstrated, and independently re-ran the test
suite and R9 smoke command myself.

**The bypass and the fix, read directly, not narrated.** Confirmed the
original hole: `PROTOCOL_OPERATION_STAMP_PREFIX` is a publicly exported
constant and `carriesProtocolOperationStamp` is a bare
`string.startsWith()` check — a caller that imports both, calls
`buildAssignment({ provenance: { kind: 'inline', contract: {
constraints: [selfForgedStamp], mutation: 'mutating', ... } } })`, then
`executeAssignment(assignment, opts)` directly never touches
`session-engine.mjs`'s `dispatchDeclaredOperation`/
`assertMutatingDispatchAllowed` at all, so R1/R2/R3 never run. Read the fix
in full: `assignment-runner.mjs`'s new `assertInlineMutatingAssignmentAuthorized`
now runs unconditionally inside `validateAssignmentLegality` (i.e. on
EVERY `executeAssignment` call, regardless of `opts.isReadOnlyMode` or which
caller reached it) — confirmed by reading the call site
(`assertInlineMutatingAssignmentAuthorized(asgn, opts);` unconditional, not
gated behind any flag). It early-returns for anything that isn't
`asgn.mutation === 'mutating' && asgn.provenance?.kind === 'inline'`, so
read-only and declared-shape Assignments are untouched (correct scoping —
declared-shape mutation is a separate, pre-existing, unrelated mechanism,
per R6c). For the in-scope case, it re-extracts the stamp from
`asgn.provenance.inline.contract.constraints` (verified this is the real
field `buildInlineAssignment` in `assignment.mjs` actually populates —
traced it myself, not assumed) and re-resolves R2/R3 against a REAL,
on-disk `CoordinationProtocol` via `loadCoordinationProtocol` — not the
caller's claim. A forged/nonexistent `definitionId`, a version mismatch, an
`operationId` that doesn't resolve or doesn't declare `result.kind:
'work-product'`, or a `cwd` that resolves to the main checkout/outside
git/unresolvable are all independently refused. This closes the exact hole:
a forged stamp can only succeed by naming a combination of {real
definition, real work-product operation, real linked-worktree cwd} that a
legitimate `dispatchDeclaredOperation` call would have authorized anyway —
the stamp is now a pointer into real facts, not itself the authorization.

**Shared-helper drift check.** Read `execution-contract.mjs`'s new
`operationDeclaresWorkProduct`/`resolveMutatingCwdPosture` and confirmed
both `session-engine.mjs`'s pre-check (`assertMutatingDispatchAllowed`) and
`assignment-runner.mjs`'s dispatch-layer re-verification
(`assertInlineMutatingAssignmentAuthorized`) call the exact same functions
— not parallel reimplementations. Diffed `session-engine.mjs`'s R2/R3 error
message template strings line-by-line against the pre-fix version: byte-
identical (same text, same `CoordinationError('validation', ...)` type),
only the resolution mechanism was swapped from inline logic to the shared
helper — confirms the Fixer's own "byte-identical error messages" claim
rather than trusting it.

**No other reachable gap found.** Checked whether scoping the new gate to
`provenance.kind === 'inline'` leaves a declared-shape bypass: no —
declared-shape Assignments get their `mutation` field from
`stampDeclaredAssignment`'s pre-existing, unrelated role/operation
classifier (confirmed in the original review), which this cell never
touched and which was never the attack surface (inline contracts didn't
exist as a mutating-capable shape before this cell at all). Checked whether
`cli.mjs`'s or `operation-choice.mjs`'s existing call sites are affected:
no — `cli.mjs`'s `isReadOnlyMode` is always `true` or provenance-conditional
there (per the original R6b enumeration), so the pre-existing
`requireReadOnly` check already rejected any inline-mutating Assignment
reaching it; `operation-choice.mjs` never builds inline Assignments at all
(R6c) — so the new gate's early-return is a genuine no-op for both existing
legitimate call sites and only has teeth against a direct bypass caller,
exactly as intended.

**LOW finding disposition** — confirmed genuinely gone, not moved.
`grep -n "resolveMainCheckoutRoot" src/runner/coordination/session-engine.mjs`
now only matches doc-comment prose, zero actual calls — the function no
longer calls `resolveMainCheckoutRoot` directly at all (delegates entirely
to `resolveMutatingCwdPosture`). Read the new `resolveMutatingCwdPosture`
in `execution-contract.mjs`: no defensive `try/catch` wraps its
`resolveMainCheckoutRoot` call either, and its own doc comment explicitly
cites this Reviewer's original LOW finding as the reason. Not reintroduced
elsewhere.

**Stable Code Artifacts follow-up (`452f26fa`)** — confirmed a small,
correctly-scoped diff: only the 3 new runtime error strings in
`assertInlineMutatingAssignmentAuthorized` had their
`"(Phase 01 mutation-unlock RN re-verification)"` suffix dropped; grepped
`test/` myself and confirmed no test asserts that exact suffix text (all
existing/new regex matches target the substring before it), so the claim
of "no test changes needed" checks out.

**Independent regression run** (own execution):
```
$ FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'
ℹ tests 665
ℹ pass 664
ℹ fail 1
```
The 1 failure is again `test/runner/coordination-static.test.mjs`'s
worktree-path false positive — same test, same mechanism as the original
review, still not in this diff's changed-file set.
```
$ node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
{"mechanism":"in-process","configured":false}
```
R9 passes.

**New test coverage** — read the 121 new lines in
`test/runner/coordination-mutation-unlock.test.mjs` in full: a real,
hand-forged inline mutating Assignment built via `buildAssignment` +
`executeAssignment` directly (never touching `session-engine.mjs`, exactly
the Red-Team's own reproduction shape), with real canary-file negative
proof (`CANARY-attack.txt`/`direct-output.txt` existence assertions, not
mocked), covering: nonexistent definition, real definition + main-checkout
cwd, real definition + non-work-product operation, and a genuinely
well-formed case proving the gate does not over-refuse. Plus a full
end-to-end regression proving the legitimate `dispatchDeclaredOperation`
path is unaffected. Not phantom tests.

**Scope/lease** — `git diff --stat 8b24c8a2..HEAD` (excluding this trace
file) touches only `CHANGELOG.md`, `session-engine.mjs`,
`assignment-runner.mjs`, `execution-contract.mjs`, and the mutation-unlock
test file — all pre-declared in the cell's "Files: may touch" list. Zero
shared-lease files touched.

**Findings**: 0 HIGH, 0 MEDIUM, 0 LOW (the prior LOW is confirmed resolved,
not reintroduced).

Verdict: **APPROVE**.

## Red-Team

Independent Red-Team verdict: **REJECT**. Ran independently, in parallel,
with no access to the Reviewer's own findings above. Diff base
`86d0106c..8b24c8a2`. Method: real throwaway attack scripts executed against
this worktree's own source (never a mocked/stubbed engine), attempting to
falsify the safety claim rather than re-reading the diff. Scripts and full
stdout preserved at
`/tmp/claude-1000/-home-vantt-projects-forgentX/a2984232-9072-4663-9d8a-440469a8964c/scratchpad/redteam-p01-1/` for reproduction.

### HIGH — Forged `protocol-operation:` stamp bypasses R1/R2/R3 entirely; real write to a simulated main checkout

**Attack performed.** `execution-contract.mjs` now `export`s
`PROTOCOL_OPERATION_STAMP_PREFIX` (`'protocol-operation:'`) so
`session-engine.mjs` can import it (R6a's own design). Nothing distinguishes
an engine-minted stamp from a self-minted one — `carriesProtocolOperationStamp`
is a bare `entry.startsWith(PROTOCOL_OPERATION_STAMP_PREFIX)` string check,
never a signature/capability. Wrote a throwaway script
(`attack5-stamp-forgery-direct-call.mjs`) that:
1. Imports `PROTOCOL_OPERATION_STAMP_PREFIX` and hand-builds a constraint
   string `protocol-operation:forged.definition@0.0.0#not-a-real-operation`
   — no real `FlowDefinition`, no `openDeclaredProtocolSession`, no
   `dispatchDeclaredOperation` call anywhere in the script.
2. Calls `buildAssignment({ provenance: { kind: 'inline', contract: {...,
   mutation: 'mutating', constraints: [forgedStamp] }, caller }, options: {} })`
   directly (`src/runner/dispatch/assignment.mjs`) — this is the SAME
   `buildInlineAssignment` path every real caller uses (confirmed it
   unconditionally calls `validateExecutionContract` internally, so this is
   not a validation bypass, it is a demonstration that the validation
   itself now accepts a forged claim).
3. Calls `executeAssignment(assignment, { cwd: repoRoot, runnerConfig })`
   DIRECTLY from `src/runner/dispatch/assignment-runner.mjs` — bypassing
   `session-engine.mjs`/`dispatchDeclaredOperation` entirely, so R1
   (schema step-type scoping), R2 (`result.kind` check), and R3
   (worktree-vs-main-checkout check) are never even reached. `opts.cwd`
   was set to a real git repo with **no linked worktree at all** — i.e.
   the exact "main checkout" shape R3 exists to refuse.
4. `opts.isReadOnlyMode` was never passed (omitted, the natural default for
   any caller unaware of this convention).

**Actual result/evidence** (real run, `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node attack5-stamp-forgery-direct-call.mjs`):
```
--- Step A: does execution-contract.mjs's validateExecutionContract accept the forged stamp? ---
[LEAK] validateExecutionContract ACCEPTED forged contract (return value): undefined
--- Step B: does assignment-normalizer.mjs's stampInlineAssignment accept it? ---
[LEAK] stampInlineAssignment ACCEPTED: {"mutation":"mutating","evidence":{"required":"reported"}}
--- Step C: build the inline Assignment (bypassing session-engine.mjs entirely) ---
assignment.mutation: mutating  provenance.kind: inline
--- Step D: call executeAssignment DIRECTLY against the MAIN CHECKOUT ---
[LEAK] executeAssignment SUCCEEDED against MAIN CHECKOUT with a forged stamp:
  status: done  confidence: inferred
  changedFiles: ["CANARY-attack5.txt"]
  canary file exists at MAIN CHECKOUT root (/tmp/fgos-attack5-repo-AV8rkJ/CANARY-attack5.txt)?  true
  git status in main checkout after attack:
?? .fgos/
?? CANARY-attack5.txt
```
A real worker process ran, wrote a real file, and `git status` confirms a
real, untracked mutation landed at the main-checkout root — precisely the
scenario R3 exists to prevent, achieved with zero R1/R2/R3 enforcement.

**Why R6b's static enumeration does not close this.** Traced
`validateAssignmentLegality` (`assignment-runner.mjs:483-534`) by hand:
`opts.isReadOnlyMode` is a **require-read-only** flag
(`requireReadOnly = Boolean(opts.isReadOnlyMode)`), not a
**permit-mutation** flag — when omitted (`undefined` → `false`), mutation
proceeds based solely on `assignment.mutation`, with no refusal. R5/R6b's
own framing ("the real invariant: `runExecutorAttempt` must remain the ONLY
code path allowed to pass `isReadOnlyMode: false`") is therefore not the
actual authorization boundary: this attack never passed `isReadOnlyMode` at
all, and mutation happened anyway. R6b's static test
(`test/architecture.test.mjs`) enumerates the 4 `executeAssignment(` call
sites **that exist in the repo at test-run time** — by construction it
cannot see a call site in a script/module that does not yet exist in the
repo (this attack script, or any future module that imports these two
long-exported primitives, `buildAssignment` + `executeAssignment`, both
pre-existing exports untouched by this diff). The plan's own R6(b) text
names exactly this risk in the abstract ("A reserved-stamp check alone is
forgeable by any direct `buildAssignment` caller with `provenance.kind:
'inline'`") but the shipped fix addresses it only with a static,
manually-maintained enumeration — not a runtime capability check — so the
risk the plan itself names is not actually closed.

**Confirms this is new attack surface from this cell, not inherited.**
Before this diff, `execution-contract.mjs` unconditionally refused
`contract.mutation === 'mutating'` for every inline contract, stamp or not
(`fail('contract.mutation "mutating" is rejected -- first slice is
read-only only (ADR-006 §6)')`, confirmed in the diff's own `-` lines). This
cell is what first made an inline mutating Assignment constructible at all;
the forgeability of the sole gate guarding that new capability is this
cell's own new exposure.

**Violated requirement/invariant.** R3 ("A `mutating` step is refused
unless the dispatch `cwd` resolves to a linked git worktree — NEVER the
main checkout... fail closed, never fail open") and R6(b)'s own stated
invariant ("the ONLY code path anywhere in this codebase allowed to pass
`isReadOnlyMode: false`") — both bypassed with zero code changes to the
kernel, using only already-exported primitives plus the newly-exported
stamp-prefix constant.

**Smallest fix direction.** Re-verify at the enforcement point, not only at
the request layer: when `effectiveAssignment.mutation === 'mutating'`,
`assignment-runner.mjs`'s `validateAssignmentLegality`/`executeAssignment`
should independently re-check R2 (bound operation's own `result.kind`) and
R3 (worktree-vs-main-checkout) itself — or, at minimum, flip
`requireReadOnly`'s default so a mutating-stamped Assignment is refused
unless the caller passes an explicit, narrowly-scoped authorization value
that only `runExecutorAttempt` can produce (e.g. an unexported `Symbol`
capability token, never a plain boolean or a string-prefix convention any
importer can replicate). A string-prefix "reserved namespace" is not a
capability; do not rely on one as the sole gate for a real filesystem
mutation.

### Confirmed (attacks attempted, claim held)

- **Attack 1 (non-`operation` step smuggling)** — `authorize`, `disposition`,
  fan-out step-level, fan-out branch, and `contribution` steps all refused
  `mutation: 'mutating'` via `validateCoordinationRequest`, including with a
  correctly-shaped `contribution` step (the Doer's own test only covers
  authorize/disposition/fan-out-branch). Case (`"Mutating"`) and
  whitespace (`"mutating "`) variants on an `operation` step were also
  refused — only the literal string is accepted. No leak found at the
  schema layer.
- **Attack 2 (main-checkout mutation, before any fs side effect)** — real
  dispatch against `ctx.cwd` = a real git repo root with no worktree;
  refused with an error naming "main checkout" specifically; canary file
  confirmed absent (checked via `fs.existsSync` immediately after the
  thrown error, not merely inferred).
- **Attack 3 (outside-git cwd, fail-closed-on-null)** — real dispatch
  against a genuine non-`.git` temp dir (its own session opened there
  successfully first, per R8's fallback, isolating this from an unrelated
  "no session" failure); refused with "does not resolve inside any git
  checkout... fail closed on an unresolvable root"; canary confirmed absent.
- **Attack 4 (`result.kind` forgery)** — `produce-advisory` (declared
  `result.kind: 'advisory'`) refused for `mutation: 'mutating'`, both with
  no extra claim and with a forged `result: { kind: 'work-product' }` field
  spliced into the request (ignored — the engine reads only the bound
  operation's own resolved definition, never a request-supplied claim, as
  designed); canary confirmed absent both times.
- **Attack 6 (`operation-choice.mjs` reachability)** — independently traced
  (not re-trusting the Doer's or Reviewer's claim): its one `buildAssignment(
  {work, stage, operation, contextRefs, expectedFiles, options: opts})` call
  never sets `provenance`, so `buildAssignment` (`assignment.mjs`) always
  takes the `buildDeclaredAssignment` branch there, stamping `mutation` via
  the wholly separate, pre-existing `classifyDeclaredMutation`/
  `stampDeclaredAssignment` role-operation table (`assignment-normalizer.mjs`)
  — never session-engine.mjs's inline-contract path. Also confirmed no
  OTHER code path passes a pre-built (session-engine-originated) Assignment
  object into `executeDriverOperationChoice`/`buildAssignment` as a
  parameter — the Assignment is always freshly constructed inside that one
  function. R6c's "verified non-issue" claim holds under independent
  tracing.
- **Attack 7 (R7 committed-mutation grading)** — independently re-ran
  `Tests First #1(b)` in isolation
  (`node --test --test-name-pattern="Tests First #1\(b\)"
  test/runner/coordination-mutation-unlock.test.mjs`): passes, confirming a
  Doer that `git commit`s its own change on the worktree branch grades
  `verified` with the committed file correctly attributed.
- **Attack 8 (R8 sibling-caller regression risk)** — `grep -rn
  "resolveCoordinationPaths\|resolveSessionPaths" src/` (excluding tests):
  every real call site lives in `session-engine.mjs`, `store.mjs` itself,
  `replay.mjs`, and `show.mjs`, and every one of them funnels through
  `resolveSessionPaths` → `resolveCoordinationPaths` — the single choke
  point this cell fixed. No sibling caller found relying on the old,
  buggy cwd-keyed behavior.
- **Attack 9 (rollback claim)** — cloned the repo to scratch, fetched this
  worktree's `HEAD` (`8b24c8a2`), and ran `git revert --no-commit 8b24c8a2`:
  applied cleanly, zero conflicts (12 files staged M/D, no `CONFLICT`
  markers). The "additive/narrowing, clean `git revert`" rollback claim
  holds.
- **Regression sweep** — independently re-ran the full focused command:
  `tests 660 / pass 659 / fail 1`, matching the claimed count exactly; the 1
  failure's full assertion diff was inspected and does show every listed
  import path containing the literal substring `worktrees` (this agent's
  own `.claude/worktrees/agent-<id>/...` checkout location), consistent
  with the claimed environment-path false positive, not a real regression.

### Gaps not independently re-verified

R9's smoke command was not independently re-run (low security relevance,
already independently confirmed by the Reviewer above).

### Recheck (post-HIGH-fix)

Recheck verdict: **REJECT** (1 new HIGH remains). Diff rechecked:
`8b24c8a2..452f26fa` (fix commits `8883e0c9` + `452f26fa`), on the SAME
worktree. Ran independently, real attack scripts against the fixed code —
not a re-read of the diff.

**Original HIGH — CLOSED, confirmed.** Re-ran the EXACT original attack
script (`attack5-stamp-forgery-direct-call.mjs`, self-forged stamp naming
`forged.definition@0.0.0#not-a-real-operation`, direct `buildAssignment` +
`executeAssignment`, `cwd` = a real main checkout, `isReadOnlyMode` never
passed): now refused —
`executeAssignment: mutating inline assignment "..." claims protocol-operation
stamp for definition "forged.definition", which does not resolve to a real
CoordinationProtocol -- refused`. Canary file confirmed absent (only the
attack script's own throwaway executor file was written, no worker ever ran).

**NEW HIGH — confusion-of-authority: a real, already-shipped operation
"ticket" authorizes an unrelated payload.** The fix's own re-verification
(`assertInlineMutatingAssignmentAuthorized`, `assignment-runner.mjs`) checks
only that the CLAIMED `definitionId@version#operationId` resolves to a real
`CoordinationProtocol` operation declaring `result.kind: 'work-product'` —
it never binds the assignment's own actual content (`objective`,
`constraints` beyond the one stamp string, `role`, executor) to that
operation, and never checks any session/coordinationId relationship at all.
Since `loadCoordinationProtocol`'s `packageRoot` defaults to the REAL fgOS
installation root regardless of `cwd`, EVERY core-tier
`CoordinationProtocol` shipped in this repo (e.g.
`core/coordination-protocols/standalone-master-coordination-loop.yaml`,
`metadata.id: core.coordination-protocol.standalone-master-coordination-loop`,
`version: 1.0.0`, operation `produce-candidate` declaring
`result.kind: work-product`) is a universally valid, zero-privilege
"ticket" — grep-able by anyone with read access to this repo, requiring no
`.fgos/coordination-protocols/` fixture write at all.

Attack (`recheck1-borrowed-real-ticket.mjs`): a fresh, unrelated temp repo
+ real linked worktree (no coordination fixture written anywhere in it), a
hand-built inline contract with `objective: 'UNRELATED malicious payload'`
and `constraints: ['protocol-operation:core.coordination-protocol.standalone-master-coordination-loop@1.0.0#produce-candidate']`
(the borrowed real ticket, otherwise wholly disconnected from this attack),
`mutation: 'mutating'`, dispatched via direct `buildAssignment` +
`executeAssignment` (session-engine.mjs/`dispatchDeclaredOperation` never
called; no session ever opened for this or any protocol). **Result: SUCCEEDED.**
```
assignment.mutation: mutating  provenance.kind: inline
[LEAK] executeAssignment SUCCEEDED using a borrowed, real, unrelated shipped ticket:
  status: done  confidence: inferred
  changedFiles: ["CANARY-recheck1.txt"]
  canary file exists (.../CANARY-recheck1.txt)? true
  canary contents: CANARY: borrowed a REAL, unrelated, shipped work-product ticket -- unrelated malicious write
```
A real worker ran and wrote a real file, with an assignment whose only
relationship to the cited operation is a string that names it.

This is narrower than the original finding (a wholly fictitious operation
is now refused; malformed/ambiguous/version-mismatched stamps are refused;
main-checkout cwd is still refused even with this same real ticket — see
below) but achieves the SAME stated goal the fix was built to close: "a
caller that bypasses `dispatchDeclaredOperation` entirely... reaches a real
mutating write with none of R1/R2/R3 ever having run" — R1 (step-type
scoping) is structurally inapplicable outside the request/schema layer, and
R2/R3 as literally worded ("the bound operation declares work-product",
"cwd resolves to a worktree") are satisfied in letter while the two are
completely disconnected: the "bound" operation is never actually what is
executed. Notably, the Fixer's OWN 5th regression test in this same diff
(`test/runner/coordination-mutation-unlock.test.mjs`, "a direct
`buildAssignment` + `executeAssignment` call whose stamp genuinely matches
a real work-product operation... is still allowed through") encodes this
exact shape as INTENDED, not caught as a gap — so without this recheck it
would ship as designed-in behavior.

**Malformed/edge-case stamp fuzzing** (`recheck2-malformed-stamps-and-main-checkout.mjs`,
7 variants) — all correctly refused, no leak beyond the one above:
- Borrowed real ticket + `cwd` = MAIN CHECKOUT: refused, names "main checkout" — R3's re-check genuinely holds even for a real-ticket attack.
- Two stamps present (one real, one fake) — ambiguous, refused ("no single, well-formed... stamp").
- Real definition/operation, WRONG version in the stamp — refused, names the mismatch.
- Real definition, operation id that doesn't exist in it — refused.
- Real definition, a real operation that exists but is `advisory` (`review-candidate`), not `work-product` — refused.
- Malformed shape (no `#` separator) — refused, treated as absent.
- Trailing `#extra` inside the operation-id capture — parsed as part of the operation id (`produce-candidate#extra`), which then fails to match any real operation id — refused, no smuggling.

**Legitimate path re-verified, no false-positive regression.**
Independently ran `node --test test/runner/coordination-mutation-unlock.test.mjs`
(17/17 pass, not just trusted): includes "the LEGITIMATE path
(`dispatchDeclaredOperation`, real definitionRef, real linked worktree) is
unaffected by the new dispatch-layer gate" and a positive direct-call test
with a genuinely-matching stamp, both passing.

**Regression sweep, independently re-run:**
`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/runner/dispatch-*.test.mjs' 'test/runner/assignment-dispatch.test.mjs' 'test/architecture.test.mjs'`
→ `tests 665 / pass 664 / fail 1` (5 new tests vs. the pre-fix 660; the 1
failure is the SAME pre-existing `test/runner/coordination-static.test.mjs`
worktree-path substring false positive, confirmed identical failure
signature to before this fix).

**R9 smoke, independently re-run:**
`node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access`
→ real `{"mechanism":"in-process","configured":false}`. Passes.

**Smallest fix direction.** Bind the re-verification to something the
caller cannot fabricate independent of a real session/dispatch decision —
e.g. require the stamp (or a parallel, engine-minted value) to also carry a
`coordinationId`/assignment-scoped nonce that `assertInlineMutatingAssignmentAuthorized`
cross-checks against a real, on-disk session record for THIS assignment
(not merely "some operation exists somewhere"), or narrow which
`CoordinationProtocol` definitions are eligible tickets at all (e.g. only
ones actually opened as a session for this dispatch, never any core-tier
definition reachable by id alone). A citable, day-one-shipped operation id
should not be sufficient authorization for an unrelated payload.

## Gaps

- **Fix Round 2 (Red-Team recheck HIGH, confusion-of-authority) —
  closed.** Red-Team's recheck of Fix Round 1 confirmed the original
  stamp-forgery bypass was genuinely closed (7 fuzz variants held), but
  found a narrower, still-real HIGH: `assertInlineMutatingAssignmentAuthorized`
  only ever confirmed the CLAIMED operation exists and declares
  `result.kind: 'work-product'` — it never bound the Assignment itself to
  any real session/dispatch record, so any real, already-shipped
  operation id (including a core-tier one shipped with this codebase,
  discoverable by id alone with no fixture write required) worked as a
  universal, zero-privilege ticket for a completely unrelated mutating
  payload. Demonstrated for real: a fresh temp repo with zero coordination
  fixture, an unrelated objective, and the real
  `core.coordination-protocol.standalone-master-coordination-loop@1.0.0#produce-candidate`
  operation id — a real file write succeeded. Notably this exact shape was
  one of Fix Round 1's own 5 regression tests, asserted as an intentional
  "still allowed through" pass. Fixed by adding
  `assignmentIsMemberOfARealSession` (`assignment-runner.mjs`): after the
  existing R2/R3 checks pass unchanged (defense in depth, not a
  replacement), additionally requires `asgn.assignmentId` to already be a
  member of some real, on-disk coordination session's own
  `assignmentRefs` array — the one thing only `createSessionAssignment`'s
  `completeAssignmentRegistration` (`coordination/store.mjs`) ever
  appends to, always after that session's own active-status/authorization
  checks already passed. `assignmentId` generation was verified, not
  assumed, to be guessable/predictable (`createAssignmentId`,
  `assignment.mjs`: a deterministic `asgn_<workId-or-writerId>_<op>_<seq>`
  shape, never a random token) — which is exactly why this binding checks
  a real REGISTRATION record (only reachable through the mediated
  `createSessionAssignment` door) rather than mere file-path existence,
  which an attacker with the same filesystem access could otherwise
  fabricate just as easily as the assignmentId itself. Read directly via
  `fs`, never by importing `coordination/store.mjs` (this module stays in
  the dispatch/infra layer, one level below coordination's use-case layer;
  `test/architecture.test.mjs`'s layering check still passes). The
  vulnerable Fix Round 1 test was flipped to assert refusal, and a new
  test reproduces Red-Team's own exact recheck attack. See Proof Matrix
  and Commands above for the real, run evidence.
- **Fix Round 1 (Red-Team HIGH, stamp-forgery bypass) — closed.** The
  Red-Team's own reproduction (see "## Red-Team" above) showed that a
  caller importing the exported `PROTOCOL_OPERATION_STAMP_PREFIX` and
  calling `buildAssignment` + `executeAssignment` directly — bypassing
  `session-engine.mjs`'s `dispatchDeclaredOperation`/
  `assertMutatingDispatchAllowed` entirely — could self-forge the
  "engine-reserved" stamp and reach a real mutating write against a
  simulated main checkout, with R1/R2/R3 never having run at all. Fixed
  by re-verifying R2 (`result.kind === 'work-product'`) and R3
  (cwd resolves to a linked worktree, never the main checkout) AT THE
  ACTUAL POINT mutation is honored: `assignment-runner.mjs`'s
  `validateAssignmentLegality` now calls a new
  `assertInlineMutatingAssignmentAuthorized`, which re-derives the
  claimed operation from the stamp itself (parses
  `protocol-operation:<definitionId>@<version>#<operationId>`, loads
  the REAL on-disk CoordinationProtocol via `loadCoordinationProtocol`,
  and checks its own `spec.operations` entry) rather than trusting any
  caller-supplied claim — a missing/malformed/ambiguous stamp, an
  unresolvable definition, a version mismatch, or an operation not
  declaring `result.kind: 'work-product'` are all refused, same as a
  main-checkout `cwd`. Scoped to `provenance.kind === 'inline'` only
  (the one shape this cell first made capable of `mutation: 'mutating'`
  at all; a declared-shape Assignment's mutation posture is the
  pre-existing, unrelated `classifyDeclaredMutation` mechanism, untouched
  and out of scope). The R2/R3 predicates themselves
  (`operationDeclaresWorkProduct`/`resolveMutatingCwdPosture`,
  `execution-contract.mjs`) are shared, pure(ish) functions called from
  BOTH this new dispatch-layer gate AND session-engine.mjs's own
  pre-check (`assertMutatingDispatchAllowed`, refactored to call them,
  byte-identical `CoordinationError` messages/types preserved — verified
  by the untouched pre-existing R2/R3 tests still passing unchanged) —
  the Reviewer's LOW finding (a dead `try/catch` around
  `resolveMainCheckoutRoot`, which never throws) was also closed as a
  natural side effect of this refactor, since the shared predicate no
  longer wraps that call in one. 5 new regression tests reproduce the
  Red-Team's own attack shape directly (never touching
  `dispatchDeclaredOperation`) and confirm it is now refused on both the
  R2 and R3 axes independently, while a well-formed direct call and the
  legitimate `dispatchDeclaredOperation` path both still work — see
  Proof Matrix and Commands above.
- **A SECOND, sibling R8-class bug, found only by running the real test
  suite (not by grep alone).** `store.mjs`'s `resolveCoordinationPaths` had
  a fallback chain `root = resolveMainCheckoutRoot(cwd) ??
  resolveMainCheckoutRoot(process.cwd()) ?? process.cwd()`. Before this
  cell's R8 fix, `root` was computed but never actually consumed by
  `fgosDir` (dead for that purpose), so this bug was fully latent. Once R8's
  fix made `fgosDir` depend on `root`, this fallback became live and broke
  ~10 pre-existing tests that dispatch against a plain, non-git temp
  directory (every one of them omits `git init`) -- `resolveMainCheckoutRoot(cwd)`
  correctly returns null for a non-git cwd, and the fallback then resolved
  `root` against the TEST RUNNER PROCESS's own cwd (this repo's real git
  checkout) instead of the caller's own workspace. Fixed by anchoring the
  fallback to `cwd` itself (`resolveRepoRoot(cwd, {strict:true})`, which
  never shells out and just returns `cwd`), matching
  `dispatch/assignment-runner.mjs`'s own already-correct fallback shape
  exactly. Full reasoning and diff in the Doer's report.
- **Two pre-existing tests asserted the exact OLD, now-intentionally-
  superseded invariant** ("dispatchPrimaryTask/dispatchDeclaredOperation/
  recordConsultDisposition accept NO mutation parameter at all" /
  "buildReadOnlyContract hardcodes mutation: 'read-only' as a literal") in
  `test/runner/coordination-r7-work-isolation.test.mjs` (that file's OWN
  "R7" is Phase 06's work-isolation R7, unrelated to this Phase's R7).
  Updated both tests (and the file's own header comment) to assert the NEW,
  correct, narrower invariant: `dispatchPrimaryTask`/`recordConsultDisposition`
  still accept no mutation parameter (unchanged); `dispatchDeclaredOperation`
  now destructures `mutation = 'read-only'` (a default, not a hardcoded
  literal); `buildSessionContract` (renamed) defaults the SAME way. The
  file's header comment also now names, honestly, a genuinely NEW and
  currently-OPEN question this cell's own R1-R9 scope never asked and does
  not answer: could two DIFFERENT `dispatchDeclaredOperation` calls (two
  actors) target the SAME linked-worktree `cwd` concurrently, both
  mutating? R3 only ever refuses the MAIN CHECKOUT; it says nothing about
  two mutating dispatches sharing one worktree. Flagged for whichever
  cell/ADR next addresses multi-actor workspace allocation -- not decided
  here, and no speculative fix attempted (Non-Goals: "no speculative fix...
  unless investigation proves it's genuinely reachable" -- this is a
  genuinely open design question, not a proven-reachable bug in this
  cell's own R1-R9 surface).
- **`src/verbs/coordination/run.mjs` was NOT touched.** R1's schema.mjs
  change (`assertMutationAllowed`, `validateOperationStep` now returns
  `mutation` on its normalized shape) and `dispatchDeclaredOperation`'s new
  `mutation` parameter are both real and tested directly at the engine
  level, but nothing in `run.mjs` currently reads `step.mutation` and
  forwards it into `dispatchDeclaredOperation`'s call -- so `fgos
  coordination run`'s own CLI-driven request path cannot YET actually
  dispatch a mutating step end-to-end. `run.mjs` is not listed in this
  cell's own "Files: may touch," and this cell's Non-Goals explicitly
  disclaim "No CLI-layer change of any kind (that is P02.1's lease)." This
  wiring gap is real, is the natural next step, and is Phase 02/03's own
  scope to close, not silently assumed done here.
- **One test failure in the full-suite run is NOT one of the 4 named
  baseline failures, but IS confirmed pre-existing** (via `git stash`
  against the untouched worktree): `test/runner/coordination-static.test.mjs`'s
  "imports no Work lifecycle, merge, worktree, transport-spawn, or
  mission-lite module" check, which substring-matches "worktree" against
  each import's RESOLVED ABSOLUTE PATH -- this agent's own worktree
  checkout lives at a path containing the literal substring "worktrees"
  (`.claude/worktrees/agent-<id>/...`), so the check fires on essentially
  every real import in `src/runner/coordination/**`, regardless of content.
  Not a code regression; an artifact of running this test from inside a
  `.claude/worktrees/` directory. See report for the exact `git stash`
  comparison.

## Coordinator Disposition (P01.1)

Reviewer (APPROVE, 0 HIGH/0 MEDIUM/1 LOW) and Red-Team (REJECT, 1 HIGH)
both completed independently. Per protocol, a HIGH from either role
blocks close regardless of the other's clean pass.

- **F1 (HIGH) — accepted, fix required before close.** Real, empirically
  demonstrated: a caller that imports the now-exported
  `PROTOCOL_OPERATION_STAMP_PREFIX` and calls `buildAssignment` +
  `executeAssignment` directly — bypassing `session-engine.mjs`'s
  `dispatchDeclaredOperation`/`assertMutatingDispatchAllowed` entirely —
  can self-forge the "engine-reserved" stamp (a plain `string.startsWith()`
  check, not a real capability) and produce a REAL mutating file write
  against a simulated main checkout, with none of R1/R2/R3's own checks
  ever running. This is genuinely NEW attack surface from this cell:
  before this diff, ANY inline `mutation: 'mutating'` was unconditionally
  refused by `execution-contract.mjs`/`assignment-normalizer.mjs`
  regardless of any stamp. This is exactly the risk class the phase file's
  own Risk section already named as real ("R6's schema/normalizer-level
  gate (R6a) alone is insufficient — an actual mistake in an earlier
  draft, caught by adversarial review") — R6b's static enumeration test
  is a closed-world check over call sites that exist TODAY in this repo;
  it cannot and does not claim to catch a new external module built
  directly on `buildAssignment`/`executeAssignment`.
  - Fix direction (accepted, per Red-Team's own first suggestion —
    defense in depth over a forgeable convention): re-verify R2
    (`result.kind === 'work-product'`) and R3 (cwd is a linked worktree,
    never the main checkout) AT THE ACTUAL POINT mutation is honored
    (`execution-contract.mjs`/`assignment-normalizer.mjs`/
    `executeAssignment`'s own path), not only inside
    `session-engine.mjs`'s pre-check. The stamp may stay as a defense-in-
    depth signal, but must never be the SOLE gate standing between a
    caller and a real mutating file write — the checks that matter must
    be re-derived at the point of consequence, independent of which
    caller reached it. Investigate whether a shared, pure, exported
    helper (e.g. verifying `result.kind`/worktree-vs-main-checkout
    against the ACTUAL cwd/operation at hand) can be called from both
    `session-engine.mjs` and the dispatch-layer execution path without
    duplicating logic that drifts.
  - Not accepted as the fix: merely un-exporting
    `PROTOCOL_OPERATION_STAMP_PREFIX` — the string itself is not a secret
    (visible in source/history regardless), so hiding the export does not
    close the actual hole, only makes the exact reproduction marginally
    less convenient. Named explicitly so the Fixer does not reach for it
    as if it solved the problem.
- **Reviewer's LOW (dead try/catch around a function that never throws)**
  — deferred, optional. Fixer may clean it up if trivial while already in
  `session-engine.mjs`, but it is not required and must not become a
  distraction from the HIGH fix.

Fix Batch Plan: single Fixer, sequential, same worktree/branch
(`worktree-agent-ab7ff2ac5eda7a106`) — this is the plan's one
kernel-touching cell, no isolated-workspace parallel-fixer scenario
applies. After the fix, BOTH Reviewer and Red-Team recheck the combined
post-fix diff before this cell can close (same rigor as the first pass —
no shortcut because this is "just a fix").

## Coordinator Disposition (P01.1 Fixer follow-up — Stable Code Artifacts)

Independently verified the Fixer's fix (commit `8883e0c9`): 664/665 focused
pass (matches claim), R9 smoke passes, zero forbidden-lease touches. The
fix genuinely closes the HIGH finding — re-derives the operation from a
REAL on-disk CoordinationProtocol (`loadCoordinationProtocol`) rather than
trusting the stamp's own claim, re-checks R2/R3 independent of caller,
scoped correctly to `provenance.kind === 'inline'`, shares logic with
session-engine.mjs via extracted pure helpers (no drift risk). This is the
accepted fix direction, not the rejected un-export approach.

One repo-rule violation caught before dispatching recheck (this repo's own
`.claude/rules/review-audit-self-decision.md` "Stable Code Artifacts"
section, binding on every role per master-coordinator.md's "REPOSITORY
RULES STILL APPLY": "Do not put plan IDs, phase numbers, audit labels, or
finding codes in code comments... Explain the invariant or behavior
directly"): three new runtime error messages in the Fixer's own
`assertInlineMutatingAssignmentAuthorized` (assignment-runner.mjs) embed
`"(Phase 01 mutation-unlock R2/R3 re-verification)"`-style suffixes —
these are real, operator-visible strings, not just internal comments.
Requiring a small follow-up: drop the plan/finding-label suffix from
those 3 error strings (the message text before the suffix already states
the real reason plainly; the suffix is a locator, not part of the actual
explanation). NOT requiring a full sweep of the many pre-existing
"Phase 01 (RN)" comment labels already present in the ORIGINAL Doer's
session-engine.mjs diff (reviewed and approved without objection by both
independent Reviewer and Red-Team) — that is a real but lower-priority,
repo-wide documentation-hygiene pattern, recorded here as a named,
deferred follow-up rather than reopening the whole kernel diff this late
in the fix cycle. Scope of this follow-up: the 3 new error strings only.

## Coordinator Disposition (P01.1 Fix Round 2 — confusion-of-authority)

Red-Team's recheck confirms the original stamp-forgery bypass is genuinely
closed (7 fuzz variants held, exact original attack now refused). But it
found a real, narrower, still-serious HIGH: `assertInlineMutatingAssignmentAuthorized`
confirms the claimed `definitionId@version#operationId` resolves to a REAL,
already-shipped work-product operation (any core-tier protocol in this
repo's own install, discoverable by id alone) — but never binds the
assignment's own actual objective/executor/constraints to that operation,
and never checks any session/coordinationId relationship. Any real
operation id becomes a universal zero-privilege "ticket" for an entirely
unrelated mutating payload, with no session ever opened. Demonstrated for
real (fresh temp repo, zero coordination fixture, borrowed a real shipped
operation id, unrelated malicious objective, real file write succeeded).
Notably this exact shape was one of the FIXER'S OWN 5 regression tests
from Round 1, asserted as an intentional "still allowed through" pass —
without this recheck it would have shipped as designed-in behavior.

- **F1 (HIGH) — accepted, fix required before close.** Bind mutation
  authorization to something the caller cannot fabricate independent of a
  REAL session/assignment record, not merely "does an operation with this
  id exist somewhere in the installed protocol library." Investigate
  `store.mjs`'s existing session/assignment persistence (session
  manifests under `.fgos/coordination/sessions/<id>`, Assignment claim
  records under `.fgos/assignments/<id>`) for the right binding: does
  `asgn.assignmentId` correspond to a REAL Assignment that a REAL,
  currently-open session actually created for THIS operation/definitionRef,
  under the resolved `cwd`? If `assignmentId` generation is already
  unguessable (verify this assumption, do not just trust it), that is a
  legitimate SECOND, independent binding to combine with the
  already-working R2/R3 checks (defense in depth, not a replacement) —
  do not weaken the existing R2/R3 re-verification to make room for the
  new check. The Fixer's own Round-1 test asserting the vulnerable
  "borrowed-ticket" case as an intentional pass MUST be flipped to assert
  refusal, with a real regression test reproducing Red-Team's exact
  reproduction (unrelated objective, borrowed real shipped operation id,
  zero coordination fixture, direct buildAssignment+executeAssignment).
- Do not scope-creep beyond closing this specific bypass. Do not touch
  `bin/fgos.mjs`/CLI-layer files (still P02.1's lease, already merged and
  closed — do not reopen it).

Fix Batch Plan: single Fixer, sequential, same worktree/branch. After the
fix, BOTH Reviewer and Red-Team recheck the combined post-fix diff again
before this cell can close — third pass, but the plan itself calls for
exactly this rigor on the one kernel-touching cell, no shortcut.
