# P05 — Live proof và rollout

## Scope

P05 was the convergence cell for `code-panel-multicell-facade`: all earlier
cell branches were merged to the track branch, latest main hotfixes were synced
into that track, and the final integrated proof ran on the resulting tree.

Because Claude quota was exhausted and the dispatch/panel surface was the
incident source, this cell was executed directly by the lead session rather than
through code-panel/plan-loop dispatch.

## Tested state

- branch: `code-panel-multicell-facade`
- tested SHA: `069e93cffd6da26f9ce1702c9fec220216b5dddd`
- tested tree: `094be49654b757d62dee26ffad0c94acd047dbb3`
- main merged SHA: `4386a835e684d32837eff412c8e44457acc12dc6`
- main merged tree: `c254e988199978b354b03386bf9e93a979c32297`
- environment: Node `v24.18.0`, npm `11.16.0`, Linux x86_64
- `package-lock.json` sha256: `b097ecd850ca73e265a2dcbf94c63aa48c118069db53d42b00996fecd83fe2fc`

## Proof sequence

```sh
cargo build --release --workspace
node --test test/rust-host/*.test.mjs
node --test test/cli/fgos-intake-4.test.mjs
env -u CLAUDE_CODE_ENTRYPOINT -u CLAUDECODE -u CLAUDE_CODE_SSE_PORT npm test
```

Results:

- Rust release build completed successfully and produced `target/release/fgos`
  plus `target/release/fgctl`.
- `node --test test/rust-host/*.test.mjs` passed 102/102.
- `node --test test/cli/fgos-intake-4.test.mjs` passed 15/15.
- Track final full suite passed: 6593 tests, 6584 pass, 0 fail, 9 skipped, duration
  `395982.212118ms`.
- Main publication full suite on merge commit `4386a835` passed: 6593 tests,
  6584 pass, 0 fail, 9 skipped, duration `390376.330836ms`.

## Baseline comparison

P00's full baseline was red because the worktree lacked Rust release binaries
and carried one deterministic intake failure. The final P05 run shows both were
environment/precondition issues for this track state:

- missing Rust binaries were resolved by `cargo build --release --workspace`;
- the formerly failing intake test passed in isolation and in the final full
  suite;
- final failure count is zero.

## Publication status

The track branch was published to `main` at merge commit
`4386a835e684d32837eff412c8e44457acc12dc6`. User-owned dirty/untracked main
files were stashed before the merge and restored afterward. The older untracked
`plans/260915-code-panel-multicell-facade/` copy from that stash was deliberately
not restored, so it could not overwrite the published/proven plan; it remains
recoverable from the named stash.
