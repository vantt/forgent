# P04 — Pure Read Evaluators — Cell Trace

**Track:** runtime-recovery
**Coordination session:** `runtime-recovery--p04`
**Merge commit:** `6bb50205` (merges `runtime-recovery--p04`, tip `82b23f25`) into the `runtime-recovery` integration branch (not `main` directly — see plan.md's branch policy)

## What landed

`src/runner/coordination/read-evaluators.mjs` (new): four pure functions extracted from real decision logic in `session-engine.mjs`/`schema.mjs` —
`legalNext(snapshot, facts)`, `authorize(action, facts)`, `visibility(snapshot, caller)`, `completion(snapshot, evidence)` — plus
`test/runner/coordination-read-evaluator.test.mjs` (25 tests, all green, includes direct parity assertions against the real write doors). No existing write door touched.

## Fix rounds

1. **Fix-1** (reviewer HIGH+MEDIUM, red-team HIGH+MEDIUM+LOW): comment-matching false-positive in the module's own "never mutates" test guard; `visibility()` treating blank/whitespace refs as valid when the real doors refuse them; `legalNext()` fail-open on an unrecognized action type where every real door fails closed.
2. **Fix-2** (reviewer HIGH+MEDIUM, red-team HIGH+MEDIUM): fix-1's comment fix was a whack-a-mole reword, not a structural fix — the test file's regex scanned raw source text including comments, so ANY comment containing a mutation-sounding literal (this time `child_process`) re-triggered the same false positive. Fixed by stripping comments before the regex scan (the general fix, verified by red-team injecting 6 new never-tried literals across three comment forms). Also closed: `visibility()` failing open on a non-array `requestedRefs` (iterated string characters as individual refs), and `authorize()` accepting `authorizedBy` shapes the real door's `validateAuthorizedBy` refuses.

Final red-team recheck: clean PASS after adversarial re-probing (new literals, non-array shape sweep, malformed-authorizedBy sweep). Only cosmetic/edge-case residuals remain (documented, not fixed — see below).

## Deferred / accepted as non-blocking

- Comment-stripper is a heuristic (line-comment regex can be fooled by `//` inside a string literal on the same line as a real match) — already alias-evadable either way, not a regression this cell introduced.
- `completion()`'s reason text says "still active" for an out-of-enum status (cosmetic wording).
- A few Unicode/edge-case parity notes (U+200B-only ref, `snapshot.assignmentRefs` as a string) — upstream callers already guarantee the shapes that would trigger these, per `validateManifest`.

## Verified independently by the Lead

Re-ran `test/runner/coordination-read-evaluator.test.mjs` directly (not just trusting agent-reported counts): 25/25 pass.

## Next owner

P05S integrates this module into the real `CoordinationSession` write doors. This cell does not wire anything in.
