# Cell P01.2 — Clear-Input Manual Proof (mdview)

Phase: `plans/260905-architecture-advisory-panel/phase-01-advisory-soul-and-manual-proof.md`
Status: **CLOSED.** Two real Phase 9 dialogue turns with the actual
person, both independently interpreted (never coordinator-authored),
reached a real outcome. Final recommendation: fix 4 named first-run
defects (bounded scope), defer Windows packaging to a separate future
decision, drop telemetry-first sequencing in favor of a scripted
first-run walkthrough (the original sequencing would have manufactured a
false "not valued" signal from an audience that doesn't exist yet).
Attack 1 (reversibility dispute) never touched by either turn, stays live
and unresolved in the permanent record. The panel's own bounds mean this
is advice, not a landed change — implementing the four fixes in `mdview`
is a separate decision outside this session.
Worktree/branch: `architecture-advisory-panel--p01-2`

## Requirements

Exactly phase-01-advisory-soul-and-manual-proof.md's P01.2 section, Manual
Proofs section (clear case), and Evaluation Rubric (copied by reference).

## Case

`/home/vantt/projects/mdview` — thin-client vs local-ownership decision for
the experimental native desktop shell. Person confirmed 2026-09-06 this is
still genuinely undecided; authorized real panel dispatch against it.

## Phase Log

- **Phase 1 (Intake and Framing)** — done. `intake.md`, `session.md`.
- **Phase 3 (Understand the Problem)** — done. `scout-report.md`
  (context investigator, `codex-readonly`). Falsifies the initial
  hypothesis: shell has zero domain logic; "single-daemon authority"
  framing partly false (CLI/MCP already bypass it); real strain is 4
  launcher-coordination bugs, not a capability gap.
- **Phase 2 (Understand the Person)** — done. `interpretation.md` (lead
  advisor, `claude-bwrap`), dispatched blind to the scout report by design.
- **Phase 4 (Ask Reluctantly)** — done, no question sent. `decision-request.md`
  records why: every gap was scout-answerable or carried as an explicit
  default into Phase 5.
- **Phase 5 (Diverge Honestly)** — done. 3 isolated proposals
  (`proposals/{system-shaper,alternative-shaper,constraint-advocate}.md`).
  Genuine convergence on the 4 real launcher bugs; genuine divergence on
  ownership (2 of 3 stay-thin, 1 primary-recommends deletion).
- **Phase 6 (Debate Claims)** — done. `critiques/architecture-critic.md`.
  5 attacks, 1 conceded failed, 2 decision-changing (reversibility-illusion;
  "punishing success" flips the deletion premise).
- **Phase 7 (Converge Without Flattening)** — done. `synthesis.md`. ONE
  recommendation ("Thin Shell, Measured First"), both live disagreements
  preserved unresolved and attributed, 14 calibrated claims.
- **Phase 8 (Explain For Ownership)** — done. `explanation.md`, the
  human-facing deliverable, presented to the person.
- **Phase 9 (Stay In Dialogue)** — **DONE, real outcome reached.**
  - Turn 1: person revealed the shell's silence was neglect (busy), not a
    verdict — and explicitly valued the feature. Lead advisor's impact
    read found a genuine third state neither shaper considered
    ("valued-but-unwatched"). Authorized a narrow, person-independent
    Phase 3 reopen.
  - That reopen (`scout-report-followup-1.md`) found a real, concrete,
    currently-live defect: a fresh daemon's one-time auth token is printed
    to stdout, which the desktop shell discards — an unpassable login wall
    for a first-time user.
  - Turn 2: person named the real audience (low-tech Windows users needing
    a standalone viewer — not their own workflow, which already works
    fine) and delegated timing to the panel ("làm luôn cũng được" read
    precisely as permission, not instruction).
  - Final outcome (`dialogue/2-response.md`): fix the 4 first-run defects,
    bounded; defer Windows packaging; drop telemetry-first (would
    manufacture a false negative with no user in the room yet); Attack 1
    stays unresolved, untouched by either turn.

## Real Gaps Found

1. `dispatch.mjs decide/execute` doesn't validate or invoke
   `claude-bwrap`/`agy-bwrap` (never registered as real executors) — see
   `intake.md`. Worked around by direct `bwrap` invocation. Carry to Phase
   02's capability-fit audit: either register these, or correct the
   playbook's ROLE ROUTING text.
2. `partialPolicy` behavior (documented for P00.1/P01.1) not relevant here
   — this cell used no `fgos coordination` session at all, purely manual
   dispatch per phase-01.md's own "Commands And Evidence" section.
3. Both bwrap-sandboxed CLIs, when given a scout-style prompt referencing
   a repo-relative doctrine path, could not read it (correctly reported
   the gap rather than fabricating content) — the coordinator embedded
   full doctrine text verbatim in every prompt as the actual fix, which
   worked cleanly across all 6 bwrap dispatches. Worth carrying to Phase
   04's production skill: prompts dispatched into this confinement can
   never assume access to sibling files outside PROJECT_ROOT/EVIDENCE_DIR.

## Tests

Not applicable — this is a manual advisory proof, not a code change.
Relative-link checks and `git diff --check` run before this cell closes.

## Process Note

A worktree/branch (`architecture-advisory-panel--p01-2`) was created at
cell open per the track's usual pattern, but every real dispatch and
commit for this cell was actually issued directly on `group-thinking-plan-loop`
in the main checkout (the manual-dispatch mechanism this cell uses has no
`--cwd` step that would have caught the mismatch the way `fgos coordination
run` did for P00.1/P01.1). No harm done — nothing else was concurrently
committing to this branch — but the worktree/branch were unused and have
been removed as cleanup (`git branch -d`, confirmed fully merged before
deletion) rather than left as an orphan.

## Changed Paths

- `docs/architect/agent-coordination/verification/architecture-advisory-panel/proofs/P01.2/**` (new)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/P01.2.md` (this file)

## Commit

(pending)
