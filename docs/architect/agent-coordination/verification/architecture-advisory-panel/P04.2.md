# Cell P04.2 — Surface, Examples, And Dialogue

Phase: `plans/260905-architecture-advisory-panel/phase-04-production-skill-and-dialogue.md`
Status: **CLOSED.** Real how-to guide + 8 example families, independently
reviewed AND red-teamed across 2 fix rounds. One finding was genuinely
BLOCKING (the documented flow could not complete at all) — both rounds,
and the blocker's fix specifically, independently re-verified live by
the Coordinator and both review roles, not accepted on report text.

## Requirements

Exactly phase-04's P04.2 section: first prove whether `fgos-group-thinking`
plus `fgos coordination run/show` already provide the required
entry/resume path (add nothing new unless an evidenced gap exists); then
`docs/how-to/use-fgos-architecture-panel.md` and 7 required example
families plus a heterogeneous/homogeneous roster pair.

## Implementation (Doer, `fullstack-developer`)

**Step 1 (required investigation, done first):** drove a real throwaway
session through plain `fgos coordination run --file` / `fgos coordination
show --json` (fake registered executor, no real LLM calls) — raw intent
translated into a request, genuinely resumed across a second, separate
CLI invocation, `show --json` alone giving full orientation with zero
chat history. Confirmed the existing doors are sufficient; no new
use-case or CLI verb was added.

Shipped `docs/how-to/use-fgos-architecture-panel.md` and 8 files under
`docs/how-to/coordination-examples/architecture-advisory-panel-v1-*.md`,
each disclosing whether it's grounded in a REAL P01.2/P01.3 session
moment or is constructed-but-doctrinally-grounded. One honest disclosed
divergence: `material-context-reopen.md`'s real precedent (P01.2 Turn 1,
a Phase-3 reopen) doesn't correspond to a mechanism the registered
protocol's graph actually has (only `revise-synthesis`/`revise-explanation`/
`close-dialogue` exist) — made the divergence itself the teaching point
rather than hiding it. 3 initial commits.

**Two pre-existing regressions surfaced by this cell's own full-suite
run, fixed directly by the Coordinator** (outside this cell's own
authorship, since they were introduced by earlier cells): P03.2 never
added its new protocol to `test/runner/flow-definition-protocol-loader.test.mjs`'s
expected-fixture list; P04.1's SKILL.md cited a file-local disposition
id ("P01.3 D1") from outside its own CONTEXT.md, violating this repo's
decision-citation-drift convention. Both fixed, full suite back to the
track's own recorded 4-item baseline. Commit `4164b7ab`.

## Review (independent Reviewer, `code-reviewer`)

**10 findings: 3 HIGH, 3 MEDIUM, 4 LOW.** The Step 1 investigation
claim reproduced independently; grounding in P01.2/P01.3 was accurate
everywhere checked (6 citations spot-checked, not 3); the disclosed
manual-playbook-vs-graph divergence was handled honestly.

- **R1 (HIGH) — independently re-verified by the Coordinator:** every
  published reopen/close `authorize` fragment passed `grantedContextRefs: []`,
  dispatching with NO context — no proposals, no critique, not even its
  own prior synthesis — contradicting the same files' own prose. Proven
  by copying the passing conformance test and changing only that one
  field.
- **R3 (HIGH, real mutation risk) — independently re-verified live by
  the Coordinator:** the homogeneous-fallback example bound all 8
  advisory roles to bare `"executor": "claude"`. Confirmed directly
  against the live `.fgos/config.json`: this name IS registered and
  really does resolve to a git-write-capable, unconfined executor
  (`--permission-mode acceptEdits --allowedTools Bash(git add:*),Bash(git commit:*)`)
  — worse than the documented `tsk-1o4` gap, because this one actually
  runs and can mutate the person's real project, violating SKILL.md's
  own BOUNDS #2.
- **R2 (HIGH) — requirement violation + false public claim:** phase-04
  requires every example to pin the protocol id AND show explicit
  per-actor routing. Grep-counted: 0 of 8 satisfied both;
  `CHANGELOG.md` claimed all 8 did.
- MEDIUMs/LOWs: citation-drift ("reproduced in full" was actually
  paraphrased); a constructed challenge accusing the panel of something
  it explicitly declined to do (loss-of-soul); a ceremonial constructed
  Decision Request with literal `[named default]` placeholders; no
  `.json` fixture shipped (so the doctor check covered none of these
  examples); a silently-corrected typo in a claimed-verbatim quote; a
  reopen-cap table implying a shared pool instead of per-binding caps;
  no committed artifact for the live investigation proof.

## Red-Team (independent, `code-reviewer`, live adversarial testing)

**10 attacks: 6 HOLD, 3 WEAK, 1 FAILS — the failure was BLOCKING.**

- **RT-P04.2-08 (CRITICAL, BLOCKING) — independently re-verified by the
  Coordinator before dispatching the fix:** `DEFAULT_AGGREGATE_BOUNDS.maxRounds = 10`
  (`src/runner/coordination/schema.mjs:74-80`); the panel's mandatory
  pre-dialogue path is exactly 10 operations; no example anywhere
  declared `aggregateBounds`. Built and drove a real session from the
  documented shapes: `explain-recommendation` landed as round 10, then
  every Phase-9 operation — including `close-dialogue`, the ONLY route
  to lead-advisor quorum — was refused at the dispatch door. **The
  documented flow could never complete, not even for a pure `decide`
  turn with zero reopens.** The conformance test already knew this and
  commented the exact reason at its own line 202; the docs were the one
  artifact that had to carry that fact and didn't. Confirmed
  `aggregateBounds` is a request-time-only field (not expressible in the
  FlowDefinition itself), so the fix correctly belonged in the docs.
- Loss-of-soul attacks mostly HELD: the constructed challenge targets
  P01.2's real strongest unrebutted objection, not a softball; the
  material-context-reopen divergence promises nothing the graph can't
  deliver; the homogeneous fallback (before R3's fix) preserved 8
  discrete actor bindings in the JSON, not just the prose.

## Fix Round 1

All 11 distinct findings (10 Reviewer + Red-Team's 1 blocking) fixed in
one round, each independently re-verified LIVE by the Doer before/after
— not patched on report text. Headline verification: built one real,
continuous 12-round session through 6 separate CLI invocations
(opening → 3 shapers → critique/assess → synthesize → red-team → explain →
Phase-9 revise-synthesis → close-dialogue) with `aggregateBounds:
{maxRounds:20, maxAssignments:30}` declared once at open — result:
`status: "completed"`. Confirmed live: the corrected `grantedContextRefs`
produces a real non-empty grant; the corrected homogeneous roster
(`claude-bwrap` for all 8 roles, tier-differentiated) resolves genuinely
different models per tier on the identical, confined executor.

**A new gap found and fixed mid-verification, beyond the assigned 11:**
`actors[]` is read fresh per request and does not persist on the session
manifest — a resumed call that omits it silently falls back to the
global default (unconfined) executor. The Doer caught this from their
own early rerun showing the wrong provider, fixed it by repeating
`actors[]` on every dispatching fragment across all 8 files, and
documented it as its own how-to guide section.

**Coordinator independently verified before accepting**: re-ran the
focused suite (757/757, exact match), read the actual `grantedContextRefs`
and `claude-bwrap` roster fixes directly in the source files, confirmed
the how-to guide's own explanation of `aggregateBounds`-once-vs-`actors[]`-every-call
is correct and consistent with why individual reopen-example files don't
redeclare bounds.

## Recheck 1 (independent Reviewer + Red-Team)

- **Red-Team: blocker FIXED, independently reproduced from scratch** —
  rebuilt a fresh sandbox with a rewritten `coordinationId` so nothing
  could resolve against the Doer's own session state, ran all 6
  invocations, confirmed `status: completed` and — checking the event
  log, not just the final status — that `revise-synthesis` and
  `close-dialogue` genuinely dispatched. Also independently confirmed
  the new `actors[]` finding is real from source (`run.mjs:437`) and
  live (a second call omitting `actors[]` silently resolved to the
  unconfined default).
- **Reviewer: 8/10 fixed, 2 partial (both openly disclosed, accepted)** —
  R1/R2/R3 all confirmed fixed by independent re-derivation, not
  re-reading the fix. Found 2 NEW small issues: one opening request in
  `material-context-reopen.md` still lacked `aggregateBounds` (the exact
  blocking condition, missed in one spot); a prose/JSON mismatch in the
  same file's placeholder ref descriptions.

## Fix Round 2 (small, final)

Fixed both new items: added the missing `aggregateBounds` declaration
(with a note that the discipline applies to every opening request
regardless of that example's own individual risk), and corrected the
prose to match the real JSON's granted refs. 1 commit (`fffe6aaf`).

**Coordinator independently verified before closing**: re-ran the
focused suite (757/757) and full `npm test` (5695 tests, 5686 pass, 3
fail — the exact 3-item subset of the track's own 4-item baseline that
fired this run, zero new failures).

## Real Gaps Found (filed, out of this cell's scope)

- `tsk-3bf` — a declared-protocol session's `actors[]` roster is read
  fresh per request with no manifest persistence; a resumed call
  omitting it silently loses confinement rather than being refused.
  Independently confirmed by both the Doer and Red-Team from source
  (`run.mjs:436-437`, `schema.mjs:144`) and live reproduction. Same risk
  class as `tsk-1o4` but for a different mechanism — a correctly
  confined roster silently losing its own confinement, not an
  unregistered name falling back.
- Noted, not filed separately (low priority, pre-existing, unrelated to
  this cell): the `coordination-example-requests-valid` doctor check is
  already `passed: false` from two stale `group-thinking` resume
  fixtures (commit `7914e807`) carrying placeholder ref tokens — the
  check validates this cell's new fixture correctly but the aggregate
  signal is already red, so it can't flag a future regression here.

## Tests

`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/cli/coordination.test.mjs' 'test/architecture.test.mjs'`:
757/757 throughout (this cell touches no kernel/protocol code). Full
`npm test`: 5695 tests, 5686 pass, 3 fail at close — the track's own
recorded baseline (item 4, a known non-deterministic live-executor
flake, didn't fire this run), zero new failures, confirmed independently
by the Coordinator at close.

## Changed Paths

- `docs/how-to/use-fgos-architecture-panel.md` (new)
- `docs/how-to/coordination-examples/architecture-advisory-panel-v1-*.md`
  (8 new example files)
- `docs/how-to/coordination-examples/architecture-advisory-panel-v1-request.json` (new fixture)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/proofs/P04.2/**` (new)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/P04.2.md` (this file)
- `test/runner/flow-definition-protocol-loader.test.mjs`,
  `core/skills/fgos-architecture-panel/SKILL.md` + projections (P03.2/P04.1
  regression fixes, not this cell's own scope but fixed here)
- `CHANGELOG.md`, `docs/enduser-docs-index.json` (regenerated)

## Commit

7 commits total on `group-thinking-plan-loop`: `78000931`, `c0d86521`,
`f6008777` (initial implementation), `4164b7ab` (P03.2/P04.1 regression
fixes), `34b8a941` (reviewer + red-team reports), `a5112f76`, `32ef2dd6`
(fix round 1), `fffe6aaf` (fix round 2) — plus this closing report's own
commit (pending).
