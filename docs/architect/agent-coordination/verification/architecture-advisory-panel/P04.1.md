# Cell P04.1 — Production Skill And Projections (fgos-architecture-panel)

Phase: `plans/260905-architecture-advisory-panel/phase-04-production-skill-and-dialogue.md`
Status: **CLOSED.** Real skill authoring, independently reviewed AND
red-teamed (per phase-04's own explicit requirement to assess both hard
correctness and "loss of soul") across 2 fix rounds, both rounds
independently re-verified by the Coordinator. All findings resolved.

## Requirements

Exactly phase-04's P04.1 section: author `core/skills/fgos-architecture-panel/SKILL.md`,
projecting Phase 01's proven advisory doctrine (9-role panel, ~4700 lines
across 4 playbook docs) into a production skill. Must contain enough
doctrine for a fresh agent to operate well — "a link-only shell fails" is
the phase's own named failure mode.

## Implementation (Doer, `fullstack-developer`)

Authored `core/skills/fgos-architecture-panel/SKILL.md` (627 lines
initial), condensing the four Phase 01 playbook docs into per-role task
packets (notice/reason/evidence/change-position/uncertainty/avoid for all
9 roles), an executor roster with per-binding cognitive rationale, and
Decision Dialogue task prose — linking out to the full playbooks for deep
reference rather than restating them. Built and byte-verified all 3
projections (`.agents/`, `.claude/`, `plugins/fgOS/`). 2 commits:
`11df9802` (skill + projections + CHANGELOG), `f08fb4f2` (doer report).

**A real judgment call found while cross-referencing the manual playbook
against the shipped P03.2 protocol:** `architecture-advisory-panel-v1.yaml`'s
dialogue-reopen graph has no backward edge into re-shaping/re-critiquing
— only `revise-synthesis`/`revise-explanation`/`close-dialogue` exist,
narrower than the manual playbook's own Phase 9. Documented explicitly as
a disclosed V1 boundary ("Bounded Reopen Scope") rather than
overpromising or silently ignoring it.

Focused suite: 757/757 (unchanged — this cell touches no kernel/protocol
code). Skill-projection tests: 39/39.

## Review (independent Reviewer, `code-reviewer`)

**16 findings: 1 HIGH, 10 MEDIUM, 5 LOW.**

- **HIGH (R1) — independently verified by the Coordinator before
  accepting, real commands run:** the skill's roster names
  `claude-bwrap`/`agy-bwrap`/`codex-readonly`, none of which are
  registered in the live `.fgos/config.json`
  (`node src/runner/dispatch.mjs decide <name>` returns
  `{"configured":false}` for all three, confirmed live). `resolve.mjs`'s
  fallback (`const executor = byExecutor ?? (cfg && cfg.executor)`)
  silently substitutes the GLOBAL DEFAULT rather than refusing — and that
  default, confirmed against the live config, really is
  `claude -p ... --permission-mode acceptEdits --allowedTools
  Bash(git add:*),Bash(git commit:*),...`: the exact class of unconfined,
  mutating executor P00.1 spent its whole cell falsifying and excluding.
  Following the roster literally through a real `fgos coordination run`
  request would silently run all 9 advisory roles on one unconfined,
  git-write-capable provider, with no error.
- Verified TRUE: the disclosed "no backward edge" finding — the phase→
  node table matches the real graph exactly.
- Not a link-only shell: all 9 role packets carry all 6 required
  dimensions.
- Notable MEDIUMs: Scout-Before-Ask's stated rule contradicted its own
  cited real exemplar (tested against P01.3's actual `decision-request.md`,
  3 of 7 gaps were genuinely material, yet the section implied zero);
  wrong citation for the `actors[].model` prohibition; a P01.2/P01.3 turn
  misattribution plus a mischaracterized dialogue-turn citation (the
  skill cited "làm luôn cũng được" as "a complete, actionable dialogue
  turn" when the real artifact explicitly read it as permission, not
  instruction — the exact reading the artifact itself refused); a
  critic-vs-coordinator misattribution; stale "bwrap runnability not yet
  proven" framing (inherited verbatim from the role doctrine itself); a
  dead relative link from `.agents/skills/`; missing `persona` field;
  dropped diversity-hedge audit; two agy result-reporting gaps (P02.1
  B12/B13) unnamed despite the roster routing 2 roles through `agy-bwrap`.

## Red-Team (independent, `code-reviewer`, adversarial reading + live checks)

**14 attacks: 6 HOLDS, 4 WEAK, 4 FAILS** (3 HIGH, 1 MEDIUM). Mechanical
claims about the graph/windows/caps all verified true against the real
YAML; all 3 Known-Gaps refusals reproduced live through
`validateCoordinationRequest`.

- **FAILS (soul, not mechanics) — the condensation kept each role's
  inbound discipline and repeatedly dropped its outbound obligation:**
  the explanation standard was absent (`consequence` appeared exactly
  once in 627 lines, independently confirmed by the Coordinator; "Explain
  For Ownership" never said what ownership requires); three lead-advisor
  artifacts (`dialogue/<n>-impact.md`, `decision-request.md`, clarify-turn
  responses) had no graph operation to carry them, while the file's own
  BOUNDS #7 forbade the only way to author them inline; the driver
  (this file's own reader) was never told the disposition rules, while
  the file armed the red-team to catch exactly that violation; reframing
  was invited without being told it still owes a candidate.
- Calibration note: scout-before-ask and the designated-loser guard were
  genuinely operationalized, not concept-naming — recorded as positive
  evidence, not just failures.

## Fix Round 1

All 17 distinct findings (Reviewer's 16 + Red-Team's 9, with overlaps
merged) addressed in 2 commits (`ba1c68d5` content + doctrine correction,
`d398dacf` report). Highlights: a prominent WARNING added above the
Executor Roster stating the exact fallback mechanism, the manual-dispatch
workaround this track's own P01.2/P01.3 actually used, and `tsk-1o4`
(the real registration fix, filed, out of this cell's own scope); the
explanation standard added; the 3 ungraphed artifacts reconciled as
coordinator-authored bookkeeping (not panel output); a new "Driver
Disposition" section; multiple citation corrections; one surgical,
Coordinator-authorized correction to the role doctrine's own stale
sentence (verified: 9 insertions/7 deletions, one hunk, nothing else in
the 1601-line file touched).

**Coordinator independently verified before accepting**: re-ran both
test suites (exact match), re-ran the real `dispatch.mjs decide` commands
personally before authorizing the fix, read the actual doctrine diff to
confirm surgical scope.

## Recheck 1 (independent Reviewer + Red-Team)

- **Reviewer: 11/16 fixed, 5/16 NOT fixed** — R12-R16 (all LOW) were
  silently absent from the fix-round report despite it claiming "all 17
  were real" with no rejections or deferrals. Also found 2 new small
  defects introduced by the fix itself: an off-by-one line citation in
  the new WARNING (`resolve.mjs:398` should be `:399`, copied from the
  Reviewer's own original citation without re-deriving it), and an
  invented "16/16 links" count in the doer's report (the file has 13
  links; both fully resolve, so the underlying fix was right — only the
  reported number was wrong).
- **Red-Team: 7/8 fixed, 1 still weak** (A6b) — the reconciliation's own
  reasoning was sound (independently re-verified against the real YAML),
  but the phase table still listed "you + lead-advisor" as Actor for
  `decision-request.md`, contradicting the new note assigning it to the
  coordinator alone; and the preamble's "the deep doctrine wins"
  precedence rule was left unreconciled against a note that deliberately
  diverges from the doctrine.

## Fix Round 2 (small, final)

Fixed all 5 previously-dropped findings (R12-R16), both self-introduced
citation defects, the A6b residual (both prose edits), and an optional
forward-reference — folded into one round rather than a third round-trip,
since Red-Team's recheck landed while this round was already in
progress. 1 commit: `34b0eea3`. This time, the fix-round report lists
every item from both original review rounds explicitly in a disposition
table (16 Reviewer + 9 Red-Team, 3 duplicate-merges, 22 distinct items) —
no summary claim. Also corrected round 1's own report in place rather
than leaving its inaccurate "16/16 links"/"all 17 were real" lines stale.

**Coordinator independently re-verified before closing**: re-ran both
test suites (757/757, 39/39, exact match), confirmed the phase table's
`decision-request.md` row now lists "you" not "you + lead-advisor",
confirmed `resolve.mjs:399` is correctly cited, confirmed the conformance
test count (13, matching `grep -c "^test("` on the real file), confirmed
final file length (910 lines).

## Real Gaps Found (not fixed here, cited as Known Gaps)

- `tsk-1o4` (P02.1 BL2) — the executor registration gap this cell's own
  HIGH finding depends on; the skill documents the manual-dispatch
  workaround and warns against literal `fgos coordination run` use of
  the roster names until this lands.
- `tsk-44p`, `tsk-3xk` (P03.2's own filed gaps) — cited with concrete
  workarounds consistent with what the kernel currently allows.
- P02.1 B12/B13 (agy result-reporting gaps) — named given the roster's
  use of `agy-bwrap` for 2 roles.

## Tests

`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/cli/coordination.test.mjs' 'test/architecture.test.mjs'`:
757/757 throughout (this cell touches no kernel/protocol code, confirmed
unchanged at every stage). `test/setup/skill-wrappers.test.mjs
test/skills/fgos-mirror.test.mjs`: 39/39 throughout. Three projections
(`core/`, `.agents/`, `plugins/fgOS/`) byte-identical at every rebuild.

## Changed Paths

- `core/skills/fgos-architecture-panel/SKILL.md` (new, 910 final lines)
- `.agents/skills/fgos-architecture-panel/SKILL.md`,
  `.claude/skills/fgos-architecture-panel/SKILL.md`,
  `plugins/fgOS/skills/fgos-architecture-panel/SKILL.md` (generated
  projections)
- `docs/architect/agent-coordination/playbooks/architecture-advisory-role-doctrine.md`
  (one surgical, authorized paragraph correction)
- `CHANGELOG.md`
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/proofs/P04.1/**` (new)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/P04.1.md` (this file)

## Commit

6 commits total on `group-thinking-plan-loop`: `11df9802`, `f08fb4f2`
(initial), `ba1c68d5`, `d398dacf` (fix round 1), `34b0eea3` (fix round 2)
— plus this closing report's own commit (pending).
