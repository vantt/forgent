# I03 — RunResult v2 And Attribution

Cell: git branch `dispatch-operability-implementation--i03`
Role: Doer + 3 fix rounds
Date: 2026-09-15

## Coordination sessions

Four, because a fix round that only dispatches `revise-candidate`/`*-recheck`
(no new first pass) never satisfies `closeSessionByQuorum`'s required-role
check (same structural gap already documented in I01's own trace), and
because one round hit a real `codex-cli` provider quota exhaustion mid-cell.

| Session | Round | Outcome |
|---|---|---|
| `--i03` | Produce (op_016) + first review/red-team (op_017/018) | Doer `done`/`verified`, commit `1904655f`. Reviewer found 2/200 focused tests failing (a reviewer-findings claim settling `confidence:failed` instead of `reported`). Red-team found the same root cause plus a HIGH: `reconcileHerdrSpawnRun` (`herdr-round.mjs`) still read `result.json` raw, bypassing `interpretRunResult`, so a corrupt v2 result was accepted there instead of failing closed. **Lead found the doer's own worktree was NOT clean when review started**: uncommitted changes on top of `1904655f` (a real fix for the reviewer's 2 failing tests, plus the required `docs/architecture-manifest.json` registration for I01/I02/I03's new modules — `test/architecture.test.mjs` would have failed on the track branch without it). Verified the diff line-by-line, reverted only the incidental GitNexus-regenerated `AGENTS.md`/`CLAUDE.md` reindex noise (found and cleared a stale, process-less `index.lock` first), and committed the rest as `8100c46e`. |
| `--i03` fix round 1 (same session) | Fixer (op_019) `done`/`verified`, commit `efee11f2`: `reconcileHerdrSpawnRun` now calls `interpretRunResult`. Reviewer-recheck (op_020) and red-team-recheck (op_021) both clean — 213/213 required tests. |
| `--i03b` (fresh session; `--i03` never re-closes once first-pass quorum is spent) | Fix round 2, attempt 1 | **codex-cli quota exhausted** ("You've hit your usage limit") — fixer (op_022), reviewer-recheck (op_023), and red-team-recheck (op_024) all failed with no claim artifact (genuine infra failure, not a content rejection). This surfaced from the Lead's own decision to run the FULL `npm test` beyond I03's required list (per plan.md's Required Final Evidence) after committing `8100c46e`: `test/runner/loop.test.mjs`'s `P4a composed` test failed — a hand-tampered (contract-corrupt) `result.json` was now triggering a fresh re-dispatch (`run '02'`) instead of leaving Work untouched. Confirmed as a REAL regression (not pre-existing) by running the identical test on the pre-I03 track branch, where it passes clean. |
| `--i03c` (retried with `executor: claude` since codex-cli was down) | Fix round 2, attempt 2 | Fixer (op_025) reported `status:"blocked"`: correctly root-caused and fixed the regression (`findLatestAssignmentRunResult`'s early `\|\| runResult.corrupt` skip treated any contract-corrupt result as absent evidence, falling through to a fresh dispatch instead of the pre-existing runId/claim-bytes/settleReports re-derivation logic) and committed `4dfbb740`, but its own dispatch environment refused every `node --test`/shell verification command session-wide — could not self-verify. **Lead independently ran the full required 6-file test set: 306/306 pass**, confirming the fix. Reviewer-recheck (op_026) and red-team-recheck (op_027) both ran successfully against `4dfbb740` and found the underlying fix correct, but surfaced two new issues: reviewer LOW (a pre-existing `coordination-recheck-disposition.test.mjs` assertion does a raw substring search for `"disposition"` that now collides with the legitimate `classification.policy.disposition` field name, and will fail on the track branch post-merge) and red-team HIGH (`contract.version` demoted to anything other than `2`, or `contract.id` changed, routes `interpretRunResult` to the legacy-v1 branch, which trusts the attacker-writable top-level `status`/`confidence` directly — a full bypass of the just-fixed contract-corrupt protection) plus a second HIGH (the round's own regression coverage for the fix is tautological: `operation-choice.test.mjs`'s corrupt-result fixture lacks `claimSha256` and is skipped by an unrelated, pre-existing binding check, so it passes identically whether or not the actual fix is present). |
| `--i03d` (fresh session; codex-cli account refreshed mid-cell) | Fix round 3 (cap reached) | Fixer (op_028) `done`/`verified`, commit `d204e380`: a present-but-wrong `contract` object (bad version or id) is now itself treated as `contract-corrupt`/unsupported — only a fully absent `contract` field takes the legacy-v1 path; added `interpretRunResult treats a present unsupported contract as corrupt, never as legacy v1` (genuinely non-tautological, confirmed independently below); tightened the `coordination-recheck-disposition.test.mjs` assertion to check for a real driver-disposition-vocabulary leak instead of a blanket substring match. Reviewer-recheck (op_029) reported `status:"failed"`: the production H1 fix is correct (independently confirmed via a scratch-worktree revert), but the NEW `operation-choice.test.mjs` demotion fixture is *also* tautological (still passes with the fail-closed branch disabled) — the same class of gap as the first HIGH, not yet closed by a properly-bound fixture. Red-team-recheck (op_030) independently reached the identical conclusion: the production fix holds under direct attack (contract demotion, missing id, non-object contract all fail closed), but the accompanying regression test does not prove it. |

**Structural findings for a future Lead (new):**
1. Same as I01: a fix-round-only session (no new first pass) never auto-closes via quorum — expected, not a bug.
2. A `codex-cli` provider quota exhaustion produces a claim-less, report-less "failed" assignment (no `agent-result.json`, no `agent-report.md`, exit code non-zero) with the quota message only visible in `stderr.log` — visually indistinguishable from a crash until read. Switching `actors[].executor` to `"claude"` for one retry round worked around it cleanly; the account was refreshed mid-cell and later rounds reverted to `codex-cli` per the track's own documented actor policy.
3. A dispatched worker can itself report `status:"blocked"` when its OWN tool environment refuses shell/test commands session-wide (not a content problem) while still leaving a correct, verifiable code change committed — the Lead's own independent test run is what actually closed the loop, not the worker's self-report.

## Requirements proof (phase-03-runresult-v2-and-attribution.md)

| Acceptance criterion | Evidence | Status |
|---|---|---|
| `RunResult` v2 is the only terminal truth for new runs | All 3 production `result.json` writers in `assignment-runner.mjs` route through `normalizeRunResultV2` (confirmed by grep, re-confirmed by reviewer/red-team across every round) | satisfied |
| A reviewer finding is `execution.completed` + `assessment.findings`, not a provider crash | Fixed in `8100c46e` (`classifyRunEvidence` now recognizes a reviewer/red-team-role or findings-verdict claim with a companion report as `reported`); live-proven by red-team across rounds 1-3 | satisfied |
| Timeout/crash/nonzero exit map to typed failure families | `normalizeRunResultV2` unit tests (`provider crash → failure.family:provider`, `timeout → failure.family:resource`); untouched by any fix round | satisfied |
| Invalid/contradictory worker claims are preserved and refused, artifacts never erased | `evidence-attribution.mjs`'s policy-refusal-preserves-evidence test; confirmed by red-team's M1/L1 residuals below, neither of which involves evidence erasure | satisfied |
| Legacy files are read deterministically and never rewritten | `interpretRunResult`'s byte-preservation test, SHA-256-verified live by red-team in rounds 1 and 2 | satisfied |
| Existing `status`/`confidence` consumers keep behaving conservatively | Reviewer explicitly re-verified `operation-choice.mjs`/`session-engine.mjs` still read only the top-level compatibility projection across every round | satisfied |
| A v2 result whose stored status/confidence mismatches its own classification fails closed as contract-corrupt (and, per round 3, so does a present-but-unsupported `contract`) | `validateRunResultV2`/`interpretRunResult` unit tests; `reconcileHerdrSpawnRun` (round 1) and `findLatestAssignmentRunResult`'s admission path (round 2) both route through it; contract-version/id demotion closed in round 3, live-verified by two independent revert-based checks (reviewer + red-team) | satisfied |
| A contract-corrupt result must never be silently consumed AND must never trigger a spurious re-dispatch | Round 2's regression (a corrupt result triggered `run '02'`) fixed by removing the early `runResult.corrupt` short-circuit so it falls through to the pre-existing, untouched binding/re-derivation checks instead — proven live by red-team's own P4a-plus-ten-variant attack matrix (round 2 recheck) and re-confirmed in round 3 | satisfied |

## Tests

`node --test test/runner/loop.test.mjs test/runner/run-result-v2.test.mjs test/runner/evidence-attribution.test.mjs test/runner/assignment-runresult.test.mjs test/runner/operation-choice.test.mjs test/runner/herdr-round-reconcile.test.mjs test/runner/coordination-recheck-disposition.test.mjs` — **328/328 pass**, re-run independently by the Lead at HEAD `d204e380`. Full `npm test` was run three times across this cell; failures varied by run (2-3 failures each time, in `test/cli/fgos-intake-4.test.mjs`, `test/runner/coordination-research-fan-out.test.mjs`, `test/runner/dispatch.test.mjs`'s concurrency-window assertions) and never touched a file in I03's lease — consistent with known environment/timing flakiness on this shared dev machine (see `project_npm_test_not_hermetic_inside_agent_session` in the Lead's own memory), not a regression from this cell.

## Deferred / residual (fix-round cap reached — not blocking, all confirmed "safe direction")

- **Tautological regression test for the contract-version/id demotion fix (H2, carried from round 2 into round 3):** `operation-choice.test.mjs`'s corrupt/demoted-contract fixtures still pass with the actual fail-closed branch disabled, so they do not yet lock the fix in mechanically. The underlying PRODUCTION behavior is independently verified correct by both reviewer (round 3 recheck, scratch-worktree revert) and red-team (round 3 recheck, direct multi-variant attack) — this is a test-coverage gap, not a live defect. A future item should replace these fixtures with ones bound the same way `reconcileHerdrSpawnRun`'s own regression test (round 1) is bound (real `claimSha256`, `settleReports`, matching `runId`).
- **M1 (red-team, round 2 recheck):** the "never re-dispatch" guarantee this cell delivers covers the contract-corrupt subset specifically; a run missing `settleReports`/`claimSha256`/`planContentHash` entirely (never reaching a bound claim at all) still re-dispatches — described by red-team itself as "the code's documented one-conservative-re-dispatch stance." Not changed by this cell; worth a follow-up item if that stance itself needs revisiting, but it is a pre-existing, intentional distinction (ambiguous/incomplete evidence retries; provably-tampered evidence does not), not a regression.
- **L1 (red-team, round 2 recheck):** a corrupt-result stop returns the Work item to `todo` (poll churn) and the settle reason drops `corruptionReasons`. Minor, cosmetic, does not affect correctness of the stop-vs-retry decision itself.
- GitNexus's index still does not reflect this track's branch (see I01/I02 traces) — impact analysis for this cell relied on direct source reading, cross-checked against `git show --stat` for every commit against the declared file lease (only `herdr-round.mjs` in round 1 and `run-result.mjs`/`operation-choice.mjs`/`coordination-recheck-disposition.test.mjs` in rounds 2-3 fell outside the phase file's own primary lease list — all justified in the fix objectives above as the minimal wiring needed to close a specific accepted finding, none of them touching the phase's explicit do-not-edit list).

## Merge

`git merge --no-ff dispatch-operability-implementation--i03` into
`implementation-track--dispatch-operability-evidence-attribution` — see
`plan.md`'s cell-status table for the merge commit.
