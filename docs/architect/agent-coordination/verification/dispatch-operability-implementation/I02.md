# I02 — Effective Execution Contract

Cell: git branch `dispatch-operability-implementation--i02`
Role: Doer + 1 fix round
Date: 2026-09-15

## Coordination session

One session (`--i02`), auto-closed by quorum once the fix round supplied the
`fixer` role.

| Step | Outcome |
|---|---|
| Produce (op_010) | Doer `done`/`verified`, commit `d65fcc4d`. New `src/runner/dispatch/effective-execution-contract.mjs` pure projection; persisted `effective-execution-contract.json` in `assignment-runner.mjs` before spawn; contract summary rendered into the prompt/brief. |
| Review (op_011) | HIGH: `buildEffectiveExecutionContract()` derived `enforcementPosture`/`permissions.filesystem.enforced` from the REQUESTED confinement config, before `prepareConfinementForLaunch()` resolved a real backend — a `{mode:"required", policyId:"policy-request-only"}` request was reported `enforced:true` regardless of what actually got applied. |
| Red-team (op_012) | Same HIGH independently confirmed live: a `cli-spawn` dispatch whose confinement mode/policyId never resolved to an installed backend (`protected/prepared-invocation/*.json` showed `requirement.mode:"unconfined"`, `backend:{id:"none"}`) still persisted `enforcementPosture:"enforced"`. Plus MEDIUM: `validateEffectiveExecutionContract` accepted a contract missing `workspace.mainCheckout`, `permissions`, `enforcementPosture`, or `adapterFamily`, and accepted `limits.timeoutMs` as a silent fallback for a missing `limits.executorTimeoutMs`. RT-I02-SECRET-LEAK and RT-I02-PRELAUNCH-RACE both passed (no action). |
| Disposition | Both HIGH (accepted, same root cause) and MEDIUM (accepted) findings routed to fix-1. |
| Fix (op_013) | Fixer `done`/`verified`, commit `b6e0c2e0`. Contract is now built from the Authority-resolved backend/requirement pair (post `prepareConfinementForLaunch`), not the raw requested config, while still landing strictly before the worker process spawns. `validateEffectiveExecutionContract` now rejects a contract missing any of the four fields, and drops the ambiguous `limits.timeoutMs` fallback. |
| Reviewer-recheck (op_014) | Approve. Live-verified from a real `executeAssignment`/`cli-spawn` run: `prepared-invocation` showed `unconfined`/`backend:none`, and the sibling `effective-execution-contract.json` correctly recorded `enforcementPosture:"instructed"`, `permissions.filesystem.enforced:false`. Confirmed the four-field validation tightening. |
| Red-team-recheck (op_015) | No findings. Re-ran all four named attacks (enforcement-overclaim, required-fields-optional, secret-leak, prelaunch-race) live — all pass. |

## Requirements proof (phase-02-effective-execution-contract.md)

| Acceptance criterion | Evidence | Status |
|---|---|---|
| Contract file exists before the executor is spawned | RT-I02-PRELAUNCH-RACE (op_012, op_015): contract present at worker's first instruction, both before and after the fix | satisfied |
| No secrets/raw env token values in the contract | RT-I02-SECRET-LEAK (op_012, op_015): live sentinel env var absent from persisted contract and prompt across the fix | satisfied |
| Records assignment id, run id, mutation, cwd/write scope, main checkout, executor id, adapter family, limits, result claim path, enforcement posture | `validateEffectiveExecutionContract` now requires all of these; RT-I02-REQUIRED-FIELDS-OPTIONAL closed (op_015) | satisfied |
| Prompt/brief and persisted contract agree on claim path, mutation, limits | Reviewer (op_011) directly compared a live dispatch's prompt against the persisted contract: mutation, claim path, timeout all matched | satisfied |
| Honest about requested vs enforced permissions | Fixed in fix round 1: enforcement fields now derive from the Authority-resolved backend, not the request. Live-proven both by reviewer-recheck and red-team-recheck | satisfied |
| A test fails if a production path drops a required field between DispatchPlan and adapter | `validateEffectiveExecutionContract`'s tightened required-field rejection, exercised by a production-door fixture (RT-I02-REQUIRED-FIELDS-OPTIONAL) | satisfied |

## Tests

`node --test test/runner/effective-execution-contract.test.mjs test/runner/assignment-runresult.test.mjs test/runner/execution-contract.test.mjs` — **73/73 pass**, re-run independently by the Lead at HEAD `b6e0c2e0`.

## Deferred / residual (not blocking)

- Red-team noted the supervisor's own protected launch envelope (`protected/launch-envelope*.json`) retains the inherited process environment, including a sensitive sentinel set for the test — a separate secret-handling surface from the I02 contract/prompt boundary (which is clean), outside `effective-execution-contract.mjs`'s file lease. Not actioned here; worth a future item if the launch envelope itself is ever exposed to a less-trusted reader.
- GitNexus's index still does not reflect this track's branch (see I01's trace) — impact analysis for I02 relied on direct source reading of the primary-lease files' existing exports, cross-checked by the doer's own file lease compliance (`git show --stat` confirms no file outside the declared lease was touched).

## Merge

`git merge --no-ff dispatch-operability-implementation--i02` into
`implementation-track--dispatch-operability-evidence-attribution` — see
`plan.md`'s cell-status table for the merge commit.
