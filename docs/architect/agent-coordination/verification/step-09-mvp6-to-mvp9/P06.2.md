# P06.2 — Visibility Runtime, Grant Enforcement, And Replay

Status: DONE | Cell owner: P06.2 Doer | Last updated: 2026-09-04

Scope: `src/runner/coordination/session-engine.mjs` (runtime enforcement
only) + `test/runner/coordination-driver-authorization.test.mjs` (new
"Phase 06 R2 (P06.2): visibility windows" test section). No changes to
`src/runner/definitions/*` (P06.1's already-closed schema scope, read-only
reference only), `src/runner/team-cognition/*`, `src/verbs/coordination/*`,
or `core/coordination-protocols/group-cognition-framework.yaml`.

## Requirements (phase-06-mvp6-visibility-windows.md, P06.2 cell)

1. Derive milestone state (window open/closed) from listed `result-linked`
   events already in the session's event log — no new stored "window
   state" field.
2. Keep the window closed for missing, late, or failed source obligations.
3. Allow an accepted `actor-replaced` lineage to satisfy the original
   source obligation without rewriting history (the original event stays
   in the log, untouched).
4. At both `authorizeDeclaredOperation` and `dispatchDeclaredOperation`,
   require every granted context ref to satisfy BOTH existing same-session
   ownership (`assertRefsOwnedBySession`, unchanged) AND active-window
   legality (new).
5. Persist no duplicate "visibility-window-applied" truth — pure
   derivation from existing events, recomputed identically by replay.

## Proof Matrix

| Req | Code Path | Positive Test | Negative Test | Evidence | Status |
|---|---|---|---|---|---|
| 1 | `deriveVisibilityWindowState`/`resolveOperationOutcome` (`session-engine.mjs`) — derives purely from `assignment-created`/`result-linked` events + on-disk RunResult, no stored field | `visibility window opens once ALL opensAfter.operationRefs[] have a qualifying result-linked...` | `visibility window stays closed with zero source result-linked events...` | test run below | PASS |
| 2 (missing) | `resolveOperationOutcome` `reason: 'missing'` branch | (above) | `visibility window stays closed with zero source result-linked events...`, `...with a partial subset of required source events...` | test run below | PASS |
| 2 (late) | `resolveOperationOutcome` `reason: 'late'` branch | — | `a "late" source (created, never result-linked) does NOT open the window...` | test run below | PASS |
| 2 (failed) | `resolveOperationOutcome` `reason: 'failed'` branch, same `status:'failed'`/`confidence` vocabulary `classifySessionQuorum`/`synthesizeResearchFanIn` already use | — | `a failed source result does NOT open the window...` | test run below | PASS |
| 3 | `buildActorReplacementMap` + lineage-following loop in `resolveOperationOutcome` | `an accepted actor-replaced lineage satisfies the original source obligation without the original failed event disappearing from the log` (also asserts the original `assignment-created`/`result-linked` events remain on the log) | — | test run below | PASS |
| 4 (authorize) | `authorizeDeclaredOperation`, new block right after the existing `grantedContextRefs`/`targetArtifactRef` `assertRefsOwnedBySession` calls | `visibility window opens once ALL...` (authorize succeeds once open) | `authorization is refused when a granted ref's window is not yet open, even though the ref itself passes same-session ownership` | test run below | PASS |
| 4 (dispatch, defense in depth) | `dispatchDeclaredOperation`, new block inside the `isDriverAuthorized` branch, before `legalContextRefs` is built | (above) | `dispatch independently refuses a window-gated operation even when authorization was forged straight through the raw store door (defense in depth)` | test run below | PASS |
| 4 (ownership unweakened) | `assertRefsOwnedBySession` call sites unchanged | — | `the existing same-session ownership gate stays enforced even when the visibility window is open (additive, never a replacement)` | test run below | PASS |
| 5 | No new event kind, no new manifest/event field; `deriveVisibilityWindowState` is a pure function of `replaySession()` output + `fgosDir`, recomputed on every call (no cache) | `replay independently reconstructs the same window-open/closed decision as the live path (open scenario)`, `...(closed scenario, failed source)` | — | test run below | PASS |

Every requirement has at least one named positive and/or negative test; the
Tests-First list's own 8 named scenarios (zero sources, partial subset, all
satisfied, failed/late, actor-replaced, authorize-refused-despite-ownership,
dispatch-refused-independently, replay-parity) are all covered 1:1 by the
11 new tests added to `coordination-driver-authorization.test.mjs`.

## Design Notes (why this shape)

**One derivation function, two call sites, never cached.**
`deriveVisibilityWindowState(definition, windowId, replayed, fgosDir)` is
the ONE place window open/closed state is computed. `authorizeDeclaredOperation`
and `dispatchDeclaredOperation` both call it fresh (a new `replaySession()`
at authorize time; the SAME already-memoized `replayOnce()` reconstruction
dispatch already takes for its edge/authorization checks) — never a
cached/stored verdict from one call reused at the other, per the Bug
Taxonomy's "a window that silently stays permanently open once any single
source links (should re-check per-request, not cache/latch)". Both gates
are additive, placed alongside (never replacing) the existing
`assertRefsOwnedBySession` calls at their respective sites
(`session-engine.mjs:~1087` authorize, `~1609`/`~1639` dispatch, per
`current-cell.md`'s own citations).

**Source resolution reuses the actor-replacement pattern already proven by
`classifySessionQuorum`, not a new one.** `opensAfter.operationRefs[]` and
(structurally, though not currently enforced — see Gaps)
`permits.sourceOperationRefs[]` name `spec.operations[]` template ids, not
Assignment ids. Resolving "the Assignment that fulfils operation X" cannot
rely on `assignment-created.operationId`/`nodeId`, because those fields are
populated ONLY for a `driver-authorized` binding
(`DRIVER_PROVENANCE_COMPANION_FIELDS` in `coordination/schema.mjs` — they
"travel together" with `authorizationId` or not at all) — and the paradigm
visibility-window use case (P00.1: "private-first-pass vs.
post-independent-pass") is exactly REQUIRED source operations feeding a
later driver-authorized one. `assignment-created.actorId`, by contrast, is
always present (required field, every dispatch shape). So
`resolveOperationOutcome` resolves the operation's graph-bound actor via
`resolveDeclaredOperationActor` (the SAME resolver the function being
authorized/dispatched already uses), then follows any `actor-replaced`
lineage (`buildActorReplacementMap`, independently built here — same
semantics as `classifySessionQuorum`'s own `resolveEffectiveActor`, not a
shared helper, so this module stays a single self-contained read of the
event log per call) to the CURRENT effective actor, then classifies exactly
the way `classifySessionQuorum`/`synthesizeResearchFanIn` already classify
a settled Assignment (`missing`/`late`/`failed`/satisfied, the SAME
`status === 'failed' || confidence === 'failed' || confidence ===
'no-evidence'` vocabulary, never a second one — per the cell's own "do not
invent a second vocabulary" instruction).

**Actor-replaced test fixture uses the raw store door for the
replacement's own attempt, not `dispatchDeclaredOperation`.** A declared
operation's graph binding names a STATIC actor id
(`graph.nodes[].operations[].actor`); `replaceSessionActor` swaps who
fulfils a role SESSION-side (an `actor-replaced` event + a new
`actor-bound`) but never rewrites the FlowDefinition's own static graph, so
a subsequent `dispatchDeclaredOperation` call for the SAME `operationId`
would still resolve to the ORIGINAL actor id, not the dynamically-allocated
replacement. This mirrors reality: a replacement actor is typically a
cohort-planner allocation the static protocol never predeclared. The test
(and, by the same reasoning, any real caller materializing a replacement's
work) uses `createSessionAssignment`/`linkResult` directly — the exact
house style `coordination-recovery-and-quorum.test.mjs`'s own R1/R2
fixtures already use for this same reason — to produce the replacement's
`assignment-created`/`result-linked` events under the NEW actor id, which
`resolveOperationOutcome`'s lineage-following then picks up correctly. This
is a read-side (replay/derivation) proof, not a claim about how
`dispatchDeclaredOperation` itself routes a replacement's redispatch — that
remains out of this cell's scope.

**`permits.sourceOperationRefs[]`/`permits.delivery` are not enforced as a
per-ref filter (scope decision, see Gaps).** The cell's Goal text says
"every granted context ref must satisfy BOTH... ownership... AND
active-window legality" — implemented here as: a binding whose
`contextAccess.visibilityWindowRef` is set may not be authorized/dispatched
AT ALL unless its window is open (a binding-level gate, applying uniformly
to every ref granted to that binding, empty grant included). It does NOT
additionally filter individual `grantedContextRefs` entries against
`permits.sourceOperationRefs[]`. Reasons, stated plainly: (a) this
codebase's real `grantedContextRefs` values are frequently opaque,
non-`asgn_`-prefixed artifact strings (see
`coordination-driver-authorization.test.mjs`'s own existing
`'artifact:candidate@2'` fixture ref) that `assertRefsOwnedBySession`
itself already treats as unstructured and leaves alone — there is no
existing primitive in this codebase that maps such a ref back to "which
operation produced it", so building one would be new, untested surface
area outside this cell's Tests-First list; (b) the Tests-First list and Bug
Taxonomy for this cell test/name window open/closed gating exclusively,
never per-ref `permits` filtering. `permits.sourceOperationRefs[]`/
`delivery` stay exactly as P06.1 shipped them: schema-validated, structural
metadata describing intent, not yet a runtime filter. This is named
explicitly as a gap below for P06.3 (proof/promotion) to decide before any
contract text is promoted.

**Fan-out source operations are not disambiguated.** `resolveOperationOutcome`
calls `resolveDeclaredOperationActor(definition, operationId)` with no
`targetActorId` (first graph match only), matching that resolver's own
documented default behavior for every other omitted-`targetActorId` caller
in this file. A window whose `opensAfter.operationRefs[]`/
`permits.sourceOperationRefs[]` names an operation wired to MULTIPLE actors
(independent fan-out sharing one operation template) is not resolved
per-branch by this cell — named as a gap below.

## Commands

Run from the working root
(`/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9-p06.2`):

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-session-engine.test.mjs' 'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-driver-authorization.test.mjs' 'test/runner/flow-definition*.test.mjs'
```

Result: `tests 155 / pass 155 / fail 0 / cancelled 0` (144 pre-existing +
11 new visibility-window tests). Zero regressions across every
pre-existing test in this glob.

Broader regression sweep (all `test/runner/coordination-*.test.mjs` except
`coordination-static.test.mjs`, which `current-cell.md` itself names as a
known worktree-path false-fail out of scope for this cell):

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(ls test/runner/coordination-*.test.mjs | grep -v coordination-static.test.mjs)
```

Result: `tests 323 / pass 323 / fail 0 / cancelled 0`.

`coordination-static.test.mjs` itself: confirmed to fail IDENTICALLY
(same forbidden-import list, same assertion) with this cell's changes
`git stash`-ed out — i.e. the failure is a pre-existing worktree-path
artifact of this repo's static-import-boundary check running from inside
a `.claude/worktrees/` path, not a regression this cell introduced.

Module load sanity check:
`node -e "import('./src/runner/coordination/session-engine.mjs').then(m => console.log(typeof m.deriveVisibilityWindowState))"`
→ `function` (confirms the new export loads with no syntax errors).

## GitNexus Impact-Analysis Gate (per project CLAUDE.md)

Not run: this worktree
(`/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9-p06.2`)
is a fresh, isolated worktree with no registered GitNexus index (mirrors
P06.1's own recorded gap for its sibling `step-09-mvp6-to-mvp9` worktree —
same class of gap, different worktree). Cross-checked instead by direct
code review of every call site of the two touched functions
(`authorizeDeclaredOperation`, `dispatchDeclaredOperation`) within
`session-engine.mjs` itself (both are the file's own exported public
surface, consumed by `test/runner/coordination-driver-authorization.test.mjs`
and, transitively, by every other test file in the "Commands" section
above) — all pass unchanged in the broader regression sweep, which is
direct behavioral proof of no external regression, not merely a
static-analysis inference. Both new gates are strictly additive
(`if (visibilityWindowRef !== undefined) { ... }`), so a binding that
declares no `contextAccess.visibilityWindowRef` — every pre-existing
fixture in this codebase — takes zero new code paths at all.

## `git diff --stat` (this cell's own files only)

```
src/runner/coordination/session-engine.mjs                        | 156 +++++++++
test/runner/coordination-driver-authorization.test.mjs             | 382 +++++++++++++++++++++
2 files changed, 538 insertions(+)
```

## Gaps

- `permits.sourceOperationRefs[]`/`permits.delivery` are validated
  (P06.1) but not enforced as a per-ref delivery filter at runtime — see
  Design Notes above for the reasoning. P06.3 should decide, before any
  contract promotion, whether this cell's binding-level "window open"
  gate is the intended final shape or whether per-ref filtering against
  `permits.sourceOperationRefs[]` needs its own follow-up cell (which
  would first need a real ref→producing-operation mapping primitive this
  codebase does not currently have for non-`asgn_`-shaped refs).
- Fan-out source operations (one `operationId` wired to more than one
  actor) are not disambiguated in `opensAfter.operationRefs[]`/
  `permits.sourceOperationRefs[]` resolution — `resolveOperationOutcome`
  always resolves the FIRST graph match, matching this file's own
  existing default-`targetActorId` behavior elsewhere, but not proven
  against a fan-out fixture by this cell's own tests.
- No live GitNexus impact-analysis confirmation for this exact worktree
  (no index registered); see gate section above for the fallback
  verification used instead.
- No changes to `docs/architect/agent-coordination/contracts/flow-definition.md`
  or `coordination-session.md` — contract promotion is explicitly P06.3's
  job, after proof (Non-Goals, `current-cell.md`).

## Fix Round

Round owner: P06.2 Fixer | Date: 2026-09-04 | Working root:
`.claude/worktrees/step-09-mvp6-to-mvp9`

Closes both accepted HIGH findings from the Wave 2 Reviewer and Red-Team
(`P06.2-P07.2-review.md` findings 1+2, `P06.2-P07.2-red-team.md` finding
P06.2-1, both attack repros). One root cause, named plainly: the original
`resolveOperationOutcome` answered "did source operation X settle?" with
`events.find(e => e.type === 'assignment-created' && e.payload.actorId ===
effectiveActorId)` — a match on actor id ALONE, against only the FIRST
graph binding of X. Neither half of the question the window actually asks
(which operation, which of its bindings) was ever checked.

### What changed (file + line)

`src/runner/coordination/session-engine.mjs` (the only source file
touched; no schema, no store, no contract, no fixture):

| Symbol | Line | What it does |
|---|---|---|
| `declaredOperationBindingActors` | `:1047` | Every DISTINCT actor the graph binds an operation to, each re-resolved through `resolveDeclaredOperationActor` with its own `targetActorId` so each binding gets identical declared/role validation. Zero actor-bound matches falls through to the single-binding resolver, so undeclared/unwired/role-only operations raise the exact same named `CoordinationError` as before. |
| `readTaskClaimsByAssignment` | `:1067` | `assignmentId -> raw taskKey`, read from `createSessionAssignment`'s own `tasks/<hashTaskKey>.json` claim records. |
| `assignmentServesOperation` | `:1112` | The new gate: POSITIVE proof an Assignment was materialized for this operation. |
| `classifyOperationAssignment` | `:1137` | The unchanged `missing`/`late`/`failed`/satisfied classification, extracted verbatim. |
| `resolveBindingOutcome` | `:1168` | One binding's outcome: lineage-follow to the effective actor, then classify only that actor's operation-verified Assignments. |
| `resolveOperationOutcome` | `:1209` | Now `branches.every(satisfied)` across ALL bindings, not one binding's verdict. |
| `deriveVisibilityWindowState` | `:1250` | Builds the claim map once per call from `replayed.sessionDir` and threads it through. Public signature UNCHANGED (`replaySession()` already returns `sessionDir`) — no test or caller had to adapt. |

`assignmentServesOperation` accepts three independent doors, any one of
which is proof; nothing else counts, so an unrelated Assignment fails
CLOSED:

1. `assignment-created.payload.operationId === operationId` — exact, but
   present only with driver `authorizationProvenance`, so it can never be
   the only door (the paradigm window source is a `required` operation).
2. The claiming taskKey is in the operation's own default-derivation
   namespace: `declared:<operationId>` exactly, or a
   `declared:<operationId>:` prefix (the `:round-N` / `:auth:<id>`
   discriminators `dispatchDeclaredOperation` itself appends, `:1759`/
   `:1783`/`:1720`). This is the door a replacement's re-attempt
   materialized through the raw store door reaches.
3. The Assignment's own inline contract carries the
   `protocol-operation:<definitionId>@<version>#<operationId>` constraint
   `dispatchDeclaredOperation` stamps onto EVERY declared dispatch it
   builds (`:1992`), regardless of activation mode or taskKey.

Door 3 is what makes Finding 1 a real fix rather than a fail-safe refusal
— see the investigation below. Doors 1+2 use exactly the mechanisms the
Coordinator's own grounding research named; door 3 is a third
already-committed, operation-keyed marker found while investigating how
the real fan-out fixture is dispatched.

### Investigation: how the real fan-out fixture is actually dispatched today

The open question routed to this round was whether true one-operation-
many-actors fan-out is already disambiguated at the dispatch layer by
callers supplying distinct explicit `taskKey`s.

**Answer: yes — and NOT via the `declared:<operationId>` convention.**
`core/coordination-protocols/independent-research-fan-out-fan-in.yaml`
binds `independent-research` to both `researcher-a` and `researcher-b` at
node `phase-research`. Its real dispatch caller is
`dispatchResearchFanOut` (`session-engine.mjs:2433-2454`), which passes
`taskKey: branch.taskKey ?? \`research-branch:${branch.actorId}\``
(`:2449`) and `targetActorId: branch.actorId` (`:2440`) per branch. The
test that drives it (`test/runner/coordination-research-fan-out.test.mjs`,
`twoBranches()` at `:153`) supplies no `taskKey` at all, so every real
fan-out branch in this repo lands on `research-branch:<actorId>`.

Two consequences, both load-bearing for the fix:

- Fan-out IS already disambiguated per actor at the dispatch layer — by a
  caller-private naming convention (`research-branch:<actorId>`), never by
  anything derivable from the FlowDefinition. So a taskKey-only lookup
  (doors 1+2) could not resolve a real fan-out branch at all; it would
  have fallen to `missing` and left the paradigm use case permanently
  unopenable.
- The contract constraint `protocol-operation:...#<operationId>` (door 3)
  IS stamped on those same branch Assignments, because they go through
  `dispatchDeclaredOperation` like every other declared dispatch. That is
  the operation-identity evidence that survives an arbitrary caller-chosen
  taskKey.

Confirmed independently (not taken from the routing summary):
`createSessionAssignment`'s `taskClaimPath` write (`store.mjs:665`,
written at `:835-839`) is UNCONDITIONAL — inside the same locked critical
section, for every dispatch shape, required and driver-authorized alike.
There is no `if (driverAuthorized)` around it.

### Scope decision for Finding 1

**Fully fixed, not merely fail-safed.** `resolveOperationOutcome` now
resolves EVERY graph binding of a source operation and requires all of
them satisfied — the same all-of rule already applied across
`opensAfter.operationRefs[]`, one level deeper. Because door 3 resolves a
branch dispatched under any caller-chosen taskKey, this works against the
real fan-out shape rather than refusing it, and the new fan-out regression
test proves it end to end (partial cohort → closed; full cohort → open).

The residual, named honestly: an Assignment materialized through the raw
`createSessionAssignment` store door with BOTH a non-`declared:<op>`
taskKey AND no contract constraint carries no operation identity anywhere
on disk, and is therefore treated as `missing` — the window stays closed.
That is the correct fail direction (a refusal, never a silent open), and
it is the only shape this cell cannot resolve. Door 2 is trust-equivalent
to the raw store door itself: a caller that can choose taskKeys can assert
operation identity. That is not a new hole — the raw store door is already
fully unmediated in this codebase (it is how the "late" and
actor-replaced fixtures work) — but it is why door 2 is scoped to the
exact default-derivation namespace and not to any looser substring match.

No dispatch-layer taskKey convention was changed, no schema/contract text
was touched, and P06.3 territory (contract promotion) was not entered.

### Finding 2 and the `actor-replaced` lineage

Finding 2 (cross-operation actor reuse) is closed outright: actor identity
is now necessary but never sufficient. The `actor-replaced` lineage-
following the original cell added still works and is unchanged in
mechanism — `resolveBindingOutcome` still walks `buildActorReplacementMap`
to the current effective actor — but the lineage now transfers the
OBLIGATION, not a licence for any completed work to answer it. The
regression test asserts both halves: the replacement's unrelated task
leaves the source `missing` (and `actorId` still resolves to
`researcher-a-v2`, proving the lineage is followed and it is the WORK that
fails to qualify), and the same replacement's later `declared:research-a:replacement`
attempt then satisfies it.

### New regression tests

All three in `test/runner/coordination-driver-authorization.test.mjs`,
appended to the existing "Phase 06 R2 (P06.2): visibility windows"
section, in that section's own fixture/helper style:

1. `a source actor ALSO bound to an unrelated operation does not satisfy the window by completing that unrelated operation` — Red-Team Attack 1.
2. `an actor-replaced replacement completing UNRELATED work does not satisfy the obligation its lineage inherited` — Red-Team Attack 2, plus the positive control that real work toward the operation still opens the window.
3. `a fan-out source operation (ONE operation bound to MANY actors) needs every bound actor settled, not just the first` — Reviewer Finding 1, dispatched exactly the way `dispatchResearchFanOut` dispatches (explicit `research-branch:<actorId>` taskKey per branch).

`writeVisibilityWindowFixture` gained defaulted `windowOperationRefs`/
`operations`/`entry`/`sourceNodes` overrides so these shapes reuse one
fixture writer; every default equals the original literal, so every
pre-existing caller writes a byte-identical definition (confirmed: the
existing 53 tests passed unchanged after the refactor, before any new test
was added).

**Pre-fix failure confirmed, not assumed.** With `git show
HEAD:src/runner/coordination/session-engine.mjs` swapped back in (test
file untouched), the three new tests fail and the 53 pre-existing ones
pass — `tests 56 / pass 53 / fail 3`. The fix file was then restored.
The two original `/tmp` repro scripts also now pass unmodified against
the fixed engine (`tests 2 / pass 2 / fail 0`).

### Fix Round verification

Run from `/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9`:

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-session-engine.test.mjs' 'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-driver-authorization.test.mjs' 'test/runner/flow-definition*.test.mjs' 'test/runner/coordination-schema.test.mjs' 'test/runner/team-cognition*.test.mjs'
```
→ `tests 259 / pass 259 / fail 0 / cancelled 0` (256 pre-fix + 3 new; zero regressions, all 11 original P06.2 tests unchanged and passing).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(ls test/runner/coordination-*.test.mjs | grep -v coordination-static.test.mjs)
```
→ `tests 326 / pass 326 / fail 0 / cancelled 0` (323 pre-fix + 3 new).

`coordination-static.test.mjs` excluded for the same pre-existing,
independently-confirmed worktree-path reason `index.md` and the original
round already record — unchanged by this round.

### Impact-analysis gate

GitNexus MCP has no index for this worktree path (same recorded gap as the
original round). Cross-checked by direct call-graph read instead:
`resolveOperationOutcome` has exactly ONE caller
(`deriveVisibilityWindowState`), which has exactly two production call
sites (`session-engine.mjs:1378` authorize gate, `:1925` dispatch gate) —
both introduced by P06.2 itself — plus the tests above
(`grep -rn 'resolveOperationOutcome\|deriveVisibilityWindowState\|buildActorReplacementMap' src/ test/ bin/`). Every new
helper is module-private and referenced only from this chain. Blast radius
is contained to P06.2's own surface, which the 326-test sweep exercises
directly.

### `git diff --stat` (Fix Round, cumulative with the original round)

```
src/runner/coordination/session-engine.mjs             | 213 ++++++++++++----
test/runner/coordination-driver-authorization.test.mjs | 217 +++++++++++++++--
2 files changed, 378 insertions(+), 52 deletions(-)
```

### Gaps still open after this round

- The `permits.sourceOperationRefs[]`/`permits.delivery` per-ref filter gap
  (original Gaps #1) is untouched and unchanged — both independent Wave 2
  rounds agreed it is a correctly-scoped decision, and it was explicitly
  not routed to this round.
- An Assignment created through the raw store door with neither an
  operation-namespaced taskKey nor a declared-dispatch contract constraint
  cannot be attributed to any operation, and is treated as `missing`. Named
  above; fails closed.
- Two bindings of the same operation to the SAME actor at two different
  nodes collapse to one branch (dedupe is by actor, and no on-disk marker
  distinguishes them by node). Fails closed in the sense that one satisfied
  Assignment answers both node positions; a future cell that needs
  per-node fan-out must add a node-qualified marker at dispatch time first.

---

## Fix Round 2

Round owner: P06.2 Fixer | Date: 2026-09-04 | Working root:
`.claude/worktrees/step-09-mvp6-to-mvp9`

Both independent rechecks confirmed Fix Round 1 closed the two original
HIGH findings (both repros now fail, fan-out all-of and actor-replaced
lineage correct in both directions, no false negatives). Both then found
the same new defect: Fix Round 1's own proof mechanism was bypassable
through the SAME public `dispatchDeclaredOperation` API the original attack
used. This round closes that, plus the MEDIUM namespace-collision finding
and the two bundled items.

### Item 1 (HIGH) — the engine's proof now has a channel the caller cannot write

The root cause, stated plainly: Round 1 called the contract constraint an
engine stamp, but spliced it into the SAME `constraints` array
`params.constraints` populates, and read it back with `.includes()`. An
`Array.includes` over a caller-contributed list is not provenance.

Two changes, both in `src/runner/coordination/session-engine.mjs`:

**(a) The stamp namespace is reserved and engine-written only.**

| Symbol | Line | What it does |
|---|---|---|
| `PROTOCOL_OPERATION_STAMP_PREFIX` | `:143` | The reserved namespace, one constant shared by writer and reader (never two literals that can drift). |
| `protocolOperationStamp` | `:145` | The one stamp constructor. |
| `assertNoReservedOperationStamp` | `:149` | Refuses ANY caller-supplied constraint in the reserved namespace. |
| `buildReadOnlyContract` | `:160` | Now calls that guard, and takes a `protocolOperationRef` parameter it appends itself. |
| `dispatchDeclaredOperation` | `:2051` | Passes `protocolOperationRef:` instead of splicing the stamp into `constraints`. |

`buildReadOnlyContract` is the single contract constructor every mediated
door in this file goes through (four call sites: `dispatchPrimaryTask`,
`proposeConsult`, `dispatchDeclaredOperation`, `recordConsultDisposition`),
so putting the guard there covers all of them rather than hardening one
door and leaving three. `grep -rn "protocol-operation:" src/ test/ bin/
core/` confirmed no legitimate caller anywhere supplies that prefix, so the
lockdown breaks nothing. Red-Team's RT-A is now refused AT the dispatch
door with a named `CoordinationError`, not merely disbelieved later.

The guard was extracted into `assertNoReservedOperationStamp` rather than
inlined: `coordination-r7-work-isolation.test.mjs:171` asserts
`mutation: 'read-only'` appears within the first 800 source characters of
`buildReadOnlyContract`, and an inline loop pushed it past that window.
Extracting keeps that pre-existing invariant's shape intact (confirmed: it
fails inlined, passes extracted).

**(b) Engine evidence is now EXCLUSIVE, not merely one of three doors.**

`assignmentServesOperation` (`:1166`) no longer ORs three doors. It ranks
them: an engine stamp, when present, settles the question in BOTH
directions — an Assignment stamped for a different operation is refused
there, instead of falling through to a claim key its own dispatch caller
chose. The taskKey namespace is consulted only for an Assignment carrying
no engine evidence at all (the unmediated raw store door — how a
replacement's re-attempt is materialized today).

This is what closes Red-Team's RT-B, and it is deliberately NOT a new
dispatch-time rejection of `params.taskKey`. Rejecting a caller taskKey in
another operation's `declared:` namespace was the other candidate fix, and
it is wrong here: `coordination-recheck-disposition.test.mjs:352` depends
on exactly that shape reaching the recheck claim-collision gate and
raising ITS named error ("is not the one that Assignment consumed"). A new
guard upstream would have pre-empted a real, tested safety check with a
different message. Ranking the evidence instead makes the forged taskKey
inert without touching `taskKey`'s documented public contract at all —
RT-B's `noise-op` dispatch still succeeds (it is a legal dispatch), it
simply carries the engine's `#noise-op` stamp, so `research-a` stays
`missing`.

### Item 2 (MEDIUM) — the claim-key namespace is now exact, never an open prefix

`DECLARED_TASK_KEY_DISCRIMINATOR` (`:1159`) /
`taskKeyDeclaresOperation` (`:1161`) replace
`startsWith('declared:' + operationId + ':')` with an exact match against
the CLOSED set of discriminators `dispatchDeclaredOperation` itself ever
appends: `declared:<operationId>` plus at most `:round-<n>` and/or
`:auth:<id>` (`:1815`/`:1839`/`:1776`). Operation ids carry no character
restriction (`definitions/schema.mjs` validates non-empty string only), so
the open prefix let a legitimately-named sibling `research:deep` claim
window source `research`'s namespace with no attacker involved.

**Fixture consequence, named rather than hidden.** Two raw-store-door
fixtures used `taskKey: 'declared:research-a:replacement'`, which is not in
the closed set and no longer qualifies. Both were changed to
`declared:research-a:round-2` — a discriminator the engine really does
derive (`coordination-driver-authorization.test.mjs:1802`, `:2045`). The
tests' assertions are unchanged; only the fixture's claim key moved onto
the convention the check now enforces. Real callers are unaffected: a
replacement re-dispatched through `dispatchDeclaredOperation` carries the
engine stamp and never reaches this door at all. Only a raw-store-door
caller must now use the engine's own discriminators, which is the point.

### Item 3 (docs-only) — `declaredOperationBindingActors` comment corrected

`:1080`'s doc comment no longer claims the zero-match fallback preserves
"the exact message a caller has always seen". It now states the real,
deliberate narrowing: an operation with a MIX of role-only and actor-bound
bindings resolves through its actor-bound ones instead of raising the
single-binding resolver's role-only error (which fired only when the FIRST
graph match happened to be role-only). No behavior change — role-only
bindings contribute no branch either way, and positive operation proof is
still required for every branch that does.

### Item 4 (documented, deliberately not fixed) — see "Gaps after Fix Round 2" below

### New regression tests

All three in `test/runner/coordination-driver-authorization.test.mjs`,
under a new "Fix Round 2" heading, reusing the same fixture writer and
helpers:

1. `a caller cannot forge the engine's protocol-operation stamp through dispatchDeclaredOperation's own constraints parameter` — Red-Team RT-A. Asserts the dispatch is REFUSED at the door and the window stays closed.
2. `a caller-chosen taskKey in another operation's declared namespace does not satisfy that operation's window source` — Red-Team RT-B. Asserts the dispatch legitimately succeeds, and the window still stays closed with `research-a` reason `missing`.
3. `a sibling operation whose id begins with the source id does not cross-satisfy it through the claim-key namespace` — Red-Team RT-G, covering BOTH paths: the mediated dispatch (closed by engine-evidence exclusivity) and a raw-store-door Assignment claimed under `declared:research:deep:round-2` (closed by the exact discriminator match). Without the second half the new regex would be untestable and could be loosened silently later.

**Pre-fix failure confirmed, not assumed.** With Fix Round 1's
`session-engine.mjs` swapped back in (test file untouched), exactly these
three fail: `tests 59 / pass 56 / fail 3`. Restored afterward. The two
original Round 1 repro scripts still pass unmodified (`tests 2 / pass 2 /
fail 0`).

### Fix Round 2 verification

Run from `/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9`:

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-session-engine.test.mjs' 'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-driver-authorization.test.mjs' 'test/runner/flow-definition*.test.mjs' 'test/runner/coordination-schema.test.mjs' 'test/runner/team-cognition*.test.mjs'
```
→ `tests 262 / pass 262 / fail 0 / cancelled 0` (259 after Round 1 + 3 new).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(ls test/runner/coordination-*.test.mjs | grep -v coordination-static.test.mjs)
```
→ `tests 329 / pass 329 / fail 0 / cancelled 0` (326 + 3).

Extra check, because this round changed the shared contract constructor
rather than only the window derivation: `node --test
test/cli/coordination.test.mjs` → `tests 32 / pass 32 / fail 0` (the CLI
suite drives the real `independent-research-fan-out-fan-in` protocol).

### `git diff --stat` (cumulative: original round + both fix rounds)

```
src/runner/coordination/session-engine.mjs             | 283 +++++++++++---
test/runner/coordination-driver-authorization.test.mjs | 361 ++++++++++++++---
2 files changed, 585 insertions(+), 59 deletions(-)
```

### Gaps after Fix Round 2

Supersedes Fix Round 1's Gaps list where they overlap.

- **Mutually-exclusive graph paths are permanently unopenable (liveness, not
  bypass) — documented for P06.3, deliberately NOT fixed here.** An
  operation bound at two DIFFERENT nodes to two DIFFERENT actors along
  mutually-exclusive graph paths is not a fan-out cohort, but the all-of
  rule treats it as one: only one path can ever run, so its window can
  never open. This fails CLOSED (a refusal, never a silent open) and no
  protocol committed to this repo has the shape — both rechecks swept all
  four `core/coordination-protocols/*.yaml` and found only genuine cohorts
  (`independent-research-fan-out-fan-in.yaml`'s two researchers at one
  node, `group-cognition-framework.yaml`'s three explorers at one node).
  Fixing it needs a way to tell "cohort" from "either/or", which P06.1's
  schema has no discriminator for — a topology-modeling decision beyond
  this cell's declared scope. P06.3 must decide before promoting any
  `visibilityWindows` contract text.
- The same operation bound to the SAME actor at two different nodes still
  collapses to one branch (dedupe is by actor; no on-disk marker
  distinguishes the node). One satisfied Assignment answers both node
  positions.
- The unmediated `createSessionAssignment` store door can still write both
  a `protocol-operation:` constraint and any claim key, so an Assignment
  created there can assert its own operation identity. That door is
  unmediated in this codebase by design (it is how the "late" and
  actor-replaced fixtures work at all), and Red-Team's own recheck
  explicitly does not count it as a finding. Every MEDIATED path — every
  door in `session-engine.mjs` that builds a contract — is now closed.
- `permits.sourceOperationRefs[]`/`permits.delivery` remain validated but
  unenforced as a per-ref filter, unchanged and still correctly scoped out
  by both Wave 2 rounds.

---

## Fix Round 3

Round owner: P06.2 Fixer | Date: 2026-09-04 | Working root:
`.claude/worktrees/step-09-mvp6-to-mvp9`

Structural round, per the Coordinator's change of direction. Rounds 1 and 2
each closed a real door and each left the NEXT one open (actor-only →
RT-A/RT-B → RT-H2 via `dispatchPrimaryTask`), because the resolution logic
kept answering "which operation?" from channels more than one door can
write. This round stops enumerating doors and reduces the question to a
single channel only one door can write into.

### Item 1 — the taskKey door is gone, and so is the driver-provenance door

`assignmentServesOperation` (`session-engine.mjs:1136`) is now one
predicate over one channel:

```js
Array.isArray(constraints) && constraints.includes(protocolOperationStamp(definition, operationId))
```

Deleted outright, not narrowed: `readTaskClaimsByAssignment`,
`DECLARED_TASK_KEY_DISCRIMINATOR`, `taskKeyDeclaresOperation`, and the
whole `taskKeys` parameter threaded through `resolveBindingOutcome`
(`:1188`), `resolveOperationOutcome`, and `deriveVisibilityWindowState`
(whose `replayed` no longer needs `sessionDir`). Dead machinery in a gate
is a liability, so none of it was left behind "just in case".

**Door 1 (`assignment-created.payload.operationId`) was removed too, and
the disposition asked for that to be checked rather than assumed.** I
checked: `authorizationProvenance` has exactly ONE producer in this file —
`dispatchDeclaredOperation`'s driver-authorized branch (`:2009`), reaching
the store through the generic `createAndExecuteSessionTask` (`:309`/
`:313`). `grep -n authorizationProvenance src/runner/coordination/session-engine.mjs`
shows no other writer. That is the same function, on the same Assignment,
that writes the stamp — so door 1 could never satisfy a source the stamp
did not already satisfy. It was redundant on every mediated path and a
second forgeable surface on the unmediated one. One channel, not two.

To answer the disposition's other question plainly: **a driver-authorized
operation IS reachable as a window source** (nothing in the schema or in
`deriveVisibilityWindowState` restricts `opensAfter.operationRefs[]` to
`required` bindings), and it is now stamp-anchored exactly like a required
one, because `dispatchDeclaredOperation` stamps before it branches on
activation mode. No test covers that shape today — the paradigm case is
required sources feeding a driver-authorized consumer — but the resolution
path is uniform, so there is no second rule to get wrong.

### Item 4 — why no fifth door can exist (by construction)

Confirming Round 2's own list rather than re-discovering it — the four
`buildReadOnlyContract` call sites are unchanged:

| Call site | Function | Passes `protocolOperationRef`? |
|---|---|---|
| `:459` | `dispatchPrimaryTask` (`:432`) | no |
| `:686` | `proposeConsult` (`:639`) | no |
| `:2001` | `dispatchDeclaredOperation` (`:1663`) | **yes** (`:2009`) |
| `:2246` | `recordConsultDisposition` (`:2160`) | no |

The argument is now structural, not an enumeration:

1. A window source is satisfied ONLY by the reserved stamp for that exact
   operation (`:1136`).
2. `PROTOCOL_OPERATION_STAMP_PREFIX` (`:143`) is a reserved namespace;
   `assertNoReservedOperationStamp` (`:149`) refuses any caller-supplied
   entry in it, and every mediated contract in this file is built by
   `buildReadOnlyContract` (`:164`), which appends the stamp itself from
   `protocolOperationRef` — a parameter no caller-facing signature exposes.
3. Therefore a door that does not pass `protocolOperationRef` produces
   Assignments that satisfy **no** window source, whatever their taskKey,
   actor, constraints, or provenance.

A fifth door added tomorrow inherits (3) automatically. The previous
rounds' invariant ("absence of engine evidence means the unmediated store
door") required every door to remember something; this one requires the
opposite — a door has to opt IN, and opting in is only possible for a door
that actually materializes a declared operation.

Red-Team's own recheck already proved the writer/reader predicates stay
symmetric under 6 forgery shapes; the reader is now exact equality
(`.includes(stamp)`), which is at least as tight as the previous
`startsWith` filter: a boxed `String`, a leading space, or different
casing all dodge the writer's `typeof === 'string'` guard AND fail exact
equality.

### Item 3 — the RT-H2 regression test

`a mediated door that does not stamp (dispatchPrimaryTask) cannot satisfy a
window source, even under the source operation's own claim key and actor`
reproduces RT-H2 exactly on the STOCK fixture: dispatch `research-b`
normally, never dispatch `research-a`, `replaceSessionActor('researcher-a'
→ PRIMARY_ACTOR_ID)`, then one ordinary `dispatchPrimaryTask` with
`taskKey: 'declared:research-a'`. It asserts the primary task really
succeeded, that its stored contract carries no `protocol-operation:`
constraint, that the window is CLOSED with `research-a` `missing`, that the
lineage still resolves `actorId` to `primary` (so it is the WORK that fails
to qualify, not the lineage), that `authorizeDeclaredOperation('synthesize')`
throws, and that zero `operation-authorized` events reach the log.

### Item 2 — every existing test re-verified, and the three adjustments justified

All 3 rounds' P06.2 tests pass under stamp-only. The paradigm cases — fan-out
cohort, ordinary required sources, replay parity, authorize/dispatch defense
in depth, RT-A, RT-B — needed **zero** changes, confirming
`dispatchDeclaredOperation` really does stamp unconditionally.

Three tests changed. None was weakened; two were strengthened to exactly
the shapes Round 2 could not close, and all three are proven below to fail
against Round 2's engine.

1. **`an accepted actor-replaced lineage satisfies the original source obligation…`** and the positive half of **`an actor-replaced replacement completing UNRELATED work…`** — these genuinely relied on taskKey-only proof, so they had to change. Both materialize the replacement's re-attempt through the raw `createSessionAssignment` store door (the graph binds `researcher-a`, never the dynamically-allocated `researcher-a-v2`, so no mediated door will dispatch to it — the original round's own design note). Under stamp-only, such an Assignment must declare its operation through the reserved stamp instead of through a `declared:research-a:round-2` claim key. `completeThroughRawStoreDoor` gained a `servesOperation` parameter that writes the stamp; both replacement fixtures now use it, with taskKeys (`replacement:research-a`) that are deliberately in NO `declared:` namespace, so the test proves the stamp decides and the claim key never does. **What this does and does not change:** the raw store door was already able to write any claim key AND the stamp (Red-Team RT-J), and it remains the documented unmediated boundary. What moved is which channel it must use — from one that mediated doors also honour, to one no other door can write. That is the whole point of the round.
2. **Negative half of the same test** — its unrelated task now claims `declared:research-a` (research-a's own default key) rather than `declared:totally-unrelated-task`. Strictly harder: under Round 2 this key would have satisfied the source.
3. **`a sibling operation whose id begins with the source id…`** (renamed from `…does not cross-satisfy it through the claim-key namespace` to `…never cross-satisfies it, through any door`) — its raw-door half now uses `declared:research:round-2`, which is Red-Team RT-G2's surviving collision class (a sibling id that literally contains a real discriminator). Round 2's exact-discriminator regex accepted it; stamp-only refuses it. P06.2-3 is now closed on the raw door too, not only the mediated one.

### Also fixed: the LOW from Round 2's recheck (not routed, one line, named here)

`assertNoReservedOperationStamp` now returns early on a non-array
(`:149-152`), so `constraints: null` again raises the contract validator's
typed `RunnerConfigError` instead of the untyped `TypeError` Round 2's
guard introduced for `dispatchPrimaryTask`/`proposeConsult`. This repairs a
regression this cell caused rather than adding scope; flagged explicitly so
it is easy to reject.

### Fix Round 3 verification

Run from `/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9`:

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-session-engine.test.mjs' 'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-driver-authorization.test.mjs' 'test/runner/flow-definition*.test.mjs' 'test/runner/coordination-schema.test.mjs' 'test/runner/team-cognition*.test.mjs'
```
→ `tests 263 / pass 263 / fail 0 / cancelled 0` (262 after Round 2 + 1 new).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(ls test/runner/coordination-*.test.mjs | grep -v coordination-static.test.mjs)
```
→ `tests 330 / pass 330 / fail 0 / cancelled 0` (329 + 1).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test test/cli/coordination.test.mjs
```
→ `tests 32 / pass 32 / fail 0`.

**Pre-fix failure confirmed.** With Round 2's `session-engine.mjs` swapped
back in (test file untouched), exactly three fail — the new RT-H2 test plus
the two adjustments described above, each landing on a shape Round 2 left
open: `tests 60 / pass 57 / fail 3`. Restored afterward. Round 1's two
original repro scripts still pass unmodified (`tests 2 / pass 2 / fail 0`).

### `git diff --stat` (cumulative: original round + all three fix rounds)

```
src/runner/coordination/session-engine.mjs             | 233 ++++++++---
test/runner/coordination-driver-authorization.test.mjs | 465 +++++++++++++++--
2 files changed, 631 insertions(+), 67 deletions(-)
```

### Gaps after Fix Round 3

Supersedes the Round 2 list.

- **The unmediated `createSessionAssignment` store door can still write the
  reserved stamp** (Red-Team RT-J), and this round makes that the intended
  way a trusted engine-internal caller declares an Assignment's operation —
  it is how the actor-replaced fixtures work. Unchanged in trust level,
  narrower in surface: the previous claim-key channel was shared with
  mediated doors, this one is not. The stamp lives in
  `contract.constraints`, not first-class Assignment state; promoting it to
  a first-class field is a schema change (P06.1 scope) worth considering at
  promotion time.
- **An actor-replaced replacement cannot discharge its obligation through
  any MEDIATED door**, because `dispatchDeclaredOperation` resolves against
  the static graph, which never names the replacement actor. That is
  pre-existing (the original round's own design note) and unchanged here,
  but stamp-only makes it explicit: the raw door is the only route today.
  Worth a P06.3 decision on whether replacement re-dispatch deserves a
  mediated door of its own.
- **Mutually-exclusive graph paths remain permanently unopenable** (liveness,
  not bypass) — an operation bound at two different nodes to two different
  actors on alternative paths is treated as a cohort by the all-of rule. No
  committed protocol has this shape; distinguishing "cohort" from
  "either/or" needs a schema discriminator P06.1 does not have. P06.3 must
  decide before promoting any `visibilityWindows` contract text.
- The same operation bound to the SAME actor at two different nodes still
  collapses to one branch (dedupe is by actor).
- `permits.sourceOperationRefs[]`/`permits.delivery` remain validated but
  unenforced as a per-ref filter — unchanged, still correctly scoped out.

---

## Fix Round 4 (final)

Round owner: P06.2 Fixer | Date: 2026-09-04 | Working root:
`.claude/worktrees/step-09-mvp6-to-mvp9`

Both rechecks confirmed Round 3's structural direction closes the whole
accumulated attack history (RT-A/RT-B/RT-G/RT-H2 all fail to reproduce, no
fifth door, driver-authorized sources correct). Round 4 is bounded to the two
findings that remained, plus the Reviewer's two LOWs. No new door-hunting.

### Item 1 (HIGH, P06.2-4) — the guard now inspects the value that persists

Round 3's proof rested on "the guard refuses any caller-supplied entry in the
reserved namespace". It refused any entry the caller chose to *show* it. The
guard iterated the caller's own container, then `buildReadOnlyContract`
handed that same object onward to be read again by `isStringArray` and by
persistence — so a container answering differently across reads (an accessor
property on a plain array, an `Array` subclass with a lying `Symbol.iterator`,
a `Proxy`) passed the check with a benign value and stored the reserved stamp.

Fix, exactly the shape Red-Team verified — `buildReadOnlyContract`
(`session-engine.mjs:176-179`):

```js
const declared = Array.isArray(constraints) ? [...constraints] : constraints;
assertNoReservedOperationStamp(declared);
const stamped = protocolOperationRef !== undefined && Array.isArray(declared) ? [...declared, protocolOperationRef] : declared;
```

The container is read ONCE. Whatever that single read yields is both what the
guard inspects and what is persisted, so check and use are provably the same
data. Two properties worth stating because they are load-bearing rather than
incidental:

- A non-array is still passed through untouched, never spread — it would
  otherwise raise an untyped `TypeError`, undoing Round 3's own LOW repair.
  The contract validator downstream rejects it with its own typed error.
- The stamping branch now appends to the SNAPSHOT (`[...declared, ref]`), not
  to the caller's container, so the engine's own stamp cannot be displaced by
  a later read either.

Both fixes are in the one shared constructor, so all four mediated doors
inherit them; no door was patched individually.

The explanatory comment sits ABOVE the function rather than inside it:
`coordination-r7-work-isolation.test.mjs:171` asserts `mutation: 'read-only'`
appears within the first 800 source characters of `buildReadOnlyContract`, and
an in-body comment pushes it past that window. Same constraint Round 2 hit;
noting it so the next editor does not rediscover it.

### Item 2 (MEDIUM, P06.2-5) — claim squatting now fails loudly, not silently

Fixed rather than deferred: the check was cheap because the predicate already
existed. `dispatchDeclaredOperation` (`:2010`) now peeks the claim record for
the taskKey it is about to use, and refuses when that claim already resolves
to a REGISTERED Assignment that does not carry this operation's stamp:

```
dispatchDeclaredOperation: taskKey "declared:research-a" already resolves to
Assignment "asgn_...", which carries no "protocol-operation:...#research-a"
provenance -- it was not materialized for operation "research-a" by this
declared-dispatch door, so resuming it would report success while leaving the
operation permanently unperformed; pass an explicit, distinct taskKey to
dispatch this operation
```

Design notes, all following this file's own existing precedent rather than
inventing anything:

- It reuses `assignmentServesOperation` — the same single predicate the window
  derivation uses — so "the stamp this dispatch would have produced" can never
  drift from "the stamp a window source accepts".
- `manifest.assignmentRefs.includes(...)` is the load-bearing guard, exactly as
  in the driver-authorized collision guard directly above it: a crash-recovery
  self-heal target's claim is not yet registered, so a genuine resume of an
  interrupted write still passes through untouched.
- It is placed AFTER the driver-authorized block, so that block's more
  specific errors (`is not the one that Assignment consumed`, relied on by
  `coordination-recheck-disposition.test.mjs:352`) still fire first and keep
  their exact messages.
- It invents no new claim-key behavior — that stays Phase 03's — it only stops
  the shape from looking like success, which is the posture this file already
  takes one guard up.

### Reviewer LOWs (both folded in)

- **R3-L1** — the stamp-reader comment (`:1160`) claimed a boxed `String`
  "dodges this too" at the reader. It does not; it would JSON-round-trip into
  a plain string and match. The comment now names the real layer that stops it
  — `validateExecutionContract`'s `isStringArray`, running inside
  `buildAssignment` BEFORE persistence — and says plainly that this reader
  alone would accept such a forgery, so a future maintainer cannot mistake the
  reader for self-sufficient.
- **R3-L2** — the driver-authorized-window-source shape now has a permanent
  test (below), not just a reasoned claim in this trace.

### New regression tests (6, all in `coordination-driver-authorization.test.mjs`)

1-3. `a constraints container that answers differently on each read (<shape>) cannot smuggle the reserved stamp past the guard` — RT-N1/RT-N2/RT-N3, one test per container shape via a table, each on the stock fixture through `dispatchPrimaryTask` (the RT-H2 reachability). Each asserts the forged stamp never reaches disk, the window stays closed with `research-a` `missing`, authorization throws, and zero `operation-authorized` events are written. The assertion deliberately accepts EITHER outcome — refused outright, or persisted without the stamp — because which one occurs depends only on what the container returns on its single read; what must never happen is the stamp landing.
4. `a two-faced constraints container cannot smuggle a foreign operation stamp through dispatchDeclaredOperation either` — RT-N1c. Asserts the forged `#research-a` stamp is absent AND the engine's own `#noise-op` stamp is still present, i.e. the fix did not close the hole by breaking legitimate stamping.
5. `a declared dispatch refuses to resume an Assignment squatting its claim key without this operation's stamp...` — RT-O.
6. `a driver-authorized operation used AS a window source is stamp-anchored exactly like a required one` — Reviewer PROBE-4, both halves: authorized-but-not-dispatched stays closed, genuinely dispatched opens, and the stored contract carries the stamp (proving it is written on the common path, before the activation-mode branch).

**Pre-fix failure confirmed.** With Round 3's `session-engine.mjs` swapped
back in and the test file untouched, exactly five fail — the three container
shapes, the declared-door variant, and claim squatting: `tests 66 / pass 61 /
fail 5`. Test 6 correctly PASSES against Round 3, since it is coverage for
already-correct behavior, not a fix. Restored afterward.

### Fix Round 4 verification

Run from `/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9`:

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-session-engine.test.mjs' 'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-driver-authorization.test.mjs' 'test/runner/flow-definition*.test.mjs' 'test/runner/coordination-schema.test.mjs' 'test/runner/team-cognition*.test.mjs'
```
→ `tests 269 / pass 269 / fail 0 / cancelled 0` (263 after Round 3 + 6 new).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(ls test/runner/coordination-*.test.mjs | grep -v coordination-static.test.mjs)
```
→ `tests 336 / pass 336 / fail 0 / cancelled 0` (330 + 6).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test test/cli/coordination.test.mjs
```
→ `tests 32 / pass 32 / fail 0`.

Round 1's two original repro scripts still pass unmodified (`tests 2 / pass 2
/ fail 0`).

### `git diff --stat` (cumulative: original round + all four fix rounds)

```
src/runner/coordination/session-engine.mjs             | 279 ++++++---
test/runner/coordination-driver-authorization.test.mjs | 695 +++++++++++++++++-
2 files changed, 907 insertions(+), 67 deletions(-)
```

### Gaps after Fix Round 4

Supersedes the Round 3 list.

- **`contextRefs`/`expectedOutputs` have the same double-read container shape**
  and were deliberately NOT normalized this round (bounded scope). Neither
  carries a security predicate today — nothing reads them to decide an
  authorization — so the TOCTOU is inert for them. If a future cell ever adds
  a reserved namespace or any guard over either field, it must snapshot first,
  the same way `constraints` now does.
- **The unmediated `createSessionAssignment` store door can still write the
  reserved stamp**, and that remains the intended way a trusted
  engine-internal caller declares an Assignment's operation (it is how the
  actor-replaced fixtures work). Unchanged trust level; the stamp lives in
  `contract.constraints`, not first-class Assignment state, and promoting it
  to a first-class field is a P06.1-scope schema change worth weighing at
  promotion time.
- **`assignments/` is repo-wide, not session-scoped** (`resolveSessionPaths`,
  `store.mjs:101`), so a stamp written in session A could in principle satisfy
  session B's window for the same definition@version — but only by writing an
  `assignment-created` event into session B's log, which is strictly inside the
  raw-store-door gap above. Pre-existing, untouched by this cell, and narrowed
  by it (the taskKey channel it replaced had the same property AND was
  reachable from mediated doors). Recorded for the P06.3 promotion decision.
- **An actor-replaced replacement cannot discharge its obligation through any
  MEDIATED door** — `dispatchDeclaredOperation` resolves against the static
  graph, which never names the replacement actor. Pre-existing; stamp-only
  makes it explicit. P06.3 should decide whether replacement re-dispatch
  deserves a mediated door.
- **Mutually-exclusive graph paths remain permanently unopenable** (liveness,
  not bypass): an operation bound at two different nodes to two different
  actors on alternative paths is treated as a cohort by the all-of rule. No
  committed protocol has this shape; telling "cohort" from "either/or" needs a
  schema discriminator P06.1 does not have.
- The same operation bound to the SAME actor at two different nodes still
  collapses to one branch (dedupe is by actor).
- `permits.sourceOperationRefs[]`/`permits.delivery` remain validated but
  unenforced as a per-ref filter — unchanged, still correctly scoped out.

---

Status: DONE
Summary: Added `deriveVisibilityWindowState`/`resolveOperationOutcome` (pure,
event-log-only derivation, no stored window state) to `session-engine.mjs`,
wired as an additive gate — alongside, never replacing, the existing
`assertRefsOwnedBySession` ownership check — into both
`authorizeDeclaredOperation` and `dispatchDeclaredOperation`; 11 new tests
cover every Tests-First scenario (missing/late/failed sources, actor-replaced
lineage, authorize-vs-dispatch defense in depth, ownership-unweakened,
replay parity) and all 155 focused-glob tests plus the full 323-test
coordination sweep (excluding the pre-existing, independently-confirmed
`coordination-static.test.mjs` worktree false-fail) pass green.
