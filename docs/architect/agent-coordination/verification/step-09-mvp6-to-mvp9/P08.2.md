# P08.2 — Session Ledger, Replay, And Visibility (Phase 08, MVP8)

Status: DONE_WITH_CONCERNS | Cell owner: P08.2 Doer | Last updated: 2026-09-04

Scope: `src/runner/coordination/{schema,store,replay,session-engine}.mjs` — the
new `deliberation-contribution-linked` event, its raw store door, its mediated
definition-aware door, its replay reconstruction and refusals, the derived
open/resolved views, and one added ownership rule on the existing
`driver-disposition-recorded` door — plus the matching rule on that door's
read-side mirror in `src/verbs/coordination/show.mjs` (see Design Notes). Plus
one new test file
`test/runner/coordination-deliberation-ledger.test.mjs` (22 tests as first
shipped; 29 after the Fix Round below, 31 after Fix Round 2). No file
under `src/runner/deliberation/` was edited — P08.1's validators are called,
never forked. Nothing under `src/runner/definitions/`,
`src/runner/team-cognition/`, or `core/` was touched.

**Concern, stated up front rather than buried in Gaps: the Candidate
Contract's `contributions.allowedTypes[]` declaration channel does not exist
and this cell could not create it.** `spec.operations[]` in
`src/runner/definitions/schema.mjs` is a closed whitelist
(`OPERATION_FIELDS = {id, role, capabilities, task, policy, result}`) with no
`contributions` key, and `src/runner/definitions/*` is on this cell's Do Not
Touch list. So the operation/type gate this cell wires is narrowed to "the
`operationRef` is a real declared operation of the bound protocol" and cannot
narrow further per operation. The alternative — accepting the
`declaredOperations` map from the caller, as P08.1's own pure-module signature
invites — is *precisely* the caller-supplied-config bypass this track has
already shipped three times, so it was refused. An unnarrowed but
session-derived declaration is the honest option. Closing this properly needs
one field added to the FlowDefinition schema by whichever cell owns that file
next; see Gaps.

## Requirements

From `phase-08-mvp8-deliberation-memory.md`'s P08.2 bullets + Candidate
Contract, and `current-cell.md`'s P08.2 section:

1. Append and replay contribution links without copying artifact content.
2. Enforce MVP6 window/context legality via the EXISTING
   `deriveVisibilityWindowState`, never a second implementation.
3. Determine whether `driver-disposition-recorded.targetRef` can already
   target a contribution safely, or needs an explicit ownership check — read
   the real code, state the finding.
4. Derive open/resolved from immutable events; no stored mutable status.
5. Audit every new function that reads a FlowDefinition or session-bound
   configuration and confirm it resolves it internally.

## Finding on requirement 3 (read before assuming — the answer is "no")

`assertDispositionRefOwnedBySession` (`store.mjs:1012`) does **not** already
make a contribution-id `targetRef` safe. Reading the real code, it asks exactly
two questions per `/`-separated segment:

- does the segment name a *different* coordination session's directory on disk?
- does the segment match `/^asgn_/` **and** exist under `.fgos/assignments/`
  **and** not appear in `manifest.assignmentRefs`?

A contribution id is neither. It is pure ledger state with no `.fgos/`
directory of its own, so a bare `contrib_whatever` matches no segment rule,
resolves against nothing, and sails through unchecked. Concretely, before this
cell a disposition could target a contribution that never existed, or one
belonging to a different session, and both would be accepted. That is
literally the Bug Taxonomy entry "a disposition able to target a contribution
from a DIFFERENT session".

**An explicit check was therefore added**, mirroring the Assignment-ownership
check's own shape (prefix-keyed, then resolved against what this session
actually owns):

- `CONTRIBUTION_REF_PREFIX = 'contribution:'` (`schema.mjs`) — a reserved ref
  namespace, the same device `protocol-operation:` already establishes in this
  codebase. A ref in it must name a contribution **this session's own log
  linked**; anything else is `dangling-ref`.
- `assertDispositionRefOwnedBySession` gains a `contributionIds` set, supplied
  by `recordDriverDisposition` from a **lock-held** `readEvents`, so the answer
  is computed against the log as it is at the moment of the write. Defaulting
  to an empty set makes an unsupplied set refuse every `contribution:` ref
  rather than skip the check.
- It applies to `evidenceRefs[]` identically — same loop, same rule.

A **bare** contribution id keeps its pre-existing meaning (an opaque ref this
codebase has no registry to resolve) and can never target a contribution. That
is deliberate: inventing an id-shape constraint would have forced a naming
convention on contribution ids, which P08.1 leaves free.

> **Refined by the Fix Round (items 2 and 6).** Both halves of that reasoning
> survive, but each gained one exception. A bare ref that exactly matches one of
> THIS session's linked contributions is now refused rather than treated as
> opaque — it was a silent no-op, not a legitimate opaque ref. And the id
> namespace stays free of a naming convention, but path separators, `..`, and a
> self-referential `contribution:` prefix are refused. See the Fix Round below.

## Proof Matrix

| Req | Code path | Positive test | Negative test | Status |
|---|---|---|---|---|
| 1. Event shape enforced | `schema.mjs` `EVENT_SPECS['deliberation-contribution-linked']` + its own block | "a well-formed … payload validates, with and without lineage" | "type is the closed MVP8 enum — all six pass, a seventh does not"; "linkedBy must be driver-shaped" | DONE |
| 1. No content, no mailbox field, no mutable status is *representable* | payload whitelist | — | "no mailbox-shaped or content-bearing field is representable at all" (8 fields: body/content/text/recipient/delivery/unread/status/resolved) | DONE |
| 1. `sessionId` never on a payload | `FORBIDDEN_FIELD_NAMES` deep scan + whitelist | — | "sessionId cannot be smuggled onto the payload" (top-level AND nested inside `linkedBy`) | DONE |
| 1. Write door + idempotency | `store.mjs:recordContributionLink` | "appends once, is an idempotent no-op on a byte-identical repeat" | "…refuses a different payload under the same contributionId" (`duplicate-ref`) | DONE |
| 1. Provenance is this session's own | same door | — | "a foreign Assignment and a mis-shaped runId are both refused" (incl. `run_<id>_../../../../tmp/evil`) | DONE |
| 1. Driver-authored, active-session only | `assertDriverIdentity` + status check | — | "linking is driver-authored, and refused once the session has closed" | DONE |
| 1. Append + replay round trip, ref+revision only | `replay.mjs` contribution branch | "replay reconstructs a contribution from ref+revision alone — no artifact content ever reaches the log" (asserts the exact projected key set, and greps the raw `events.jsonl`) | — | DONE |
| 1. Replay refuses forged/duplicate/out-of-order links | `replay.mjs` | — | "refuses a duplicate contributionId, a foreign linker, and a contribution citing a Run that had not settled yet" | DONE |
| 1. Lineage cycles unrepresentable | P08.1's `validateAnchors`/`validateResponseLineage`, called at both doors | — | "a lineage ref that only appears LATER in the log is refused — which is why a cycle cannot be replayed at all"; "…a self-reference is dangling by construction" | DONE |
| 1. Post-terminal neutralization | `ignoredContributions` | "a post-terminal contribution is neutralized into ignoredContributions" | — | DONE |
| 2. Window legality via the existing mechanism | `session-engine.mjs:linkSessionContribution` → `deriveVisibilityWindowState` | "a contribution is linked once its binding's window is open **and its reasoning was produced under it**…" (Fix Round item 4 split this from the original single test) | "…whose reasoning PREDATES the window is refused, both while the window is closed and after it later opens" — both halves, nothing written by either refusal | DONE |
| 2. The window ref is not the caller's to pick | binding's `contextAccess.visibilityWindowRef` | asserts `linked.visibilityWindowRef === WINDOW_ID` came from the binding | "static: linkSessionContribution takes no definition, no visibility-window, and no revision parameter" | DONE |
| 3. Disposition may target a session-owned contribution | `assertDispositionRefOwnedBySession` + `CONTRIBUTION_REF_PREFIX` | "targetRef may name a contribution of THIS session" | "…may not name an unknown one or one belonging to another session" (two real sessions in one workspace); evidenceRefs case too | DONE |
| 4. Open/resolved derived, never stored | `replay.mjs` `openContributionIds`/`resolvedContributionIds` | "open/resolved is DERIVED at replay time from immutable events" (asserts the ledger is byte-prefix-stable across the disposition, and no `resolved`/`status` key exists on any record) | — | DONE |
| 5. Definition resolved from the session | `manifest.definitionRef` + drift refusal | — | "the FlowDefinition is resolved from the session, never from the caller — a drifted definition is refused" | DONE |
| Immutable artifact backing is real, not asserted | `aggregationSourceFrom` settle-report pin + current-hash comparison | — | "an artifact edited after settle can no longer be linked" | DONE |
| Only a declared operation can back a contribution | `stampedOperationRefOf` (reserved stamp only) | — | "an Assignment that performed no declared operation can back no contribution" | DONE |
| 3. The read-side mirror agrees with the write door | `show.mjs:isRefOwnedBySession` | "show: the read-side ownership mirror agrees with the write door about a contribution: ref" (owned → `true`) | same test: a raw-appended disposition naming an unlinked contribution → `false` | DONE |

22 tests, all green.

## The caller-supplied-definition audit (requirement 5), in full

Every function this cell added, and what it reads:

| New function | Exported? | FlowDefinition / session config it reads | Where that value comes from |
|---|---|---|---|
| `linkSessionContribution` (session-engine) | **yes** | the FlowDefinition; the visibility-window ref; the operationRef; the runId; the artifactRef + revision pin; `declaredOperations` | ALL derived: definition from `manifest.definitionRef` via `loadCoordinationProtocol` with the same version-drift refusal the four sibling doors use; window ref from the node binding's `contextAccess`; operationRef from the Assignment's reserved `protocol-operation:` stamp; runId from the latest `result-linked`; artifactRef/revision from `aggregationSourceFrom`'s settle-report pin; `declaredOperations` from `definition.spec.operations[]`. **None is a parameter** — proven by a static signature test, not by inspection. |
| `stampedOperationRefOf` (session-engine) | no | takes `definition` as an argument | module-private; both call sites (`knownStampedAssignments`, `linkSessionContribution`) pass the internally-resolved definition. Not caller-reachable — same posture as the pre-existing private `resolveOperationOutcome`/`hasBoundActor`/`aggregationSourceFrom`. |
| `knownStampedAssignments` (session-engine) | no | takes `definition` as an argument | same as above. |
| `recordContributionLink` (store) | yes | none — `store.mjs` holds no definition and imports none | reads the manifest off disk by `coordinationId`, under the lock. |
| `knownContributionsFromEvents` (store) | yes | none | pure over `(events, coordinationId)`; the id is the session's own, passed by the door that resolved it. |
| `asCoordinationError` (store) | yes | none | pure error mapping. |
| the `deliberation-contribution-linked` branch in `replaySession` | n/a | none — replay stays definition-blind by design (P07.3) | `manifest.provenanceRoot.writerId` read off disk. |
| the `contributionIds` parameter on `assertDispositionRefOwnedBySession` | no (module-private) | not config — a set of ids | built by `recordDriverDisposition` from a lock-held `readEvents`, never passed in from outside the module. |

**Result: no new function accepts a caller-supplied FlowDefinition, visibility
rule, window state, or session-bound configuration for any legality-relevant
decision.** One caveat carried over verbatim from P07.3's own Gaps, because it
is precedent parity rather than anything this cell introduced: the definition
is pinned by `{id, version}` and never by content, and `opts.cwd`/
`opts.packageRoot` still steer where `loadCoordinationProtocol` resolves it
from. `authorizeDeclaredOperation`, `dispatchDeclaredOperation`,
`dispatchResearchFanOut`, and `validateSessionAggregation` all share the
identical exposure.

> **The MECHANISM is precedent parity; the CONSEQUENCE at this door is not, and
> the paragraph above understates it.** A same-`{id, version}` content swap does
> not merely change which evidence a verdict is derived over here — it removes
> the entire MVP6 window/context-legality property this cell's Acceptance
> criterion rests on, and launders a false `visibilityWindowRef` claim into a
> permanent record. Read the escalated framing in **Fix Round item 11** before
> relying on this section's Acceptance claim.

## Design Notes

### Two doors, and which question each can answer

Exactly the P07.3 split. `store.mjs` has no definition and can never have one
(it sits below `session-engine.mjs` in the import graph), so it owns the
questions answerable from the manifest and the log alone: driver identity,
session membership, runId shape, artifact-ref scope, contribution-id
uniqueness, and lineage resolution. `session-engine.mjs`'s
`linkSessionContribution` owns everything that needs the bound protocol:
which operation the backing Assignment actually performed, which window gates
it, whether that window is open, and what the immutable pin is. A caller
reaching the raw door directly still cannot forge identity, membership, or
lineage — it can only bypass the *definition-aware* half, which is the same
unmediated-store-door boundary Phase 06 and Phase 07 already named.

### What the payload deliberately does not carry

Two fields from P08.1's contribution shape are absent from the event payload,
and neither is an oversight:

- **`sessionId`** is a `FORBIDDEN_FIELD_NAMES` entry (ADR-008 Decision 5),
  rejected at any nesting depth in any coordination event payload. This is a
  better answer than persisting it: the log *is* the session, so the doors
  reconstruct `sessionId` from `coordinationId` and hand P08.1's validator a
  value no caller could have set. `validateSessionMembership` therefore cannot
  fail on the mediated path — by construction, not by luck.
- **`ts`** is stamped on the event envelope by `state/events.mjs` for every
  event kind, so no kind in the Event Log table carries one as a payload
  field. The Candidate Contract's "timestamp" is that envelope stamp,
  projected back onto the record by `replay.mjs` exactly as it is for
  `aggregation-validated` and `driver-disposition-recorded`.

`linkedBy` was added beyond P08.1's field set, matching
`validatedBy`/`authorizedBy`: linking a contribution is ledger state the
session's driver writes, and replay refuses one linked under any other
identity.

### Why cycles are unrepresentable rather than merely detected

P08.1's Gaps named two P08.2 ledger-layer obligations: anchors are resolved
but never walked (so an anchor cycle or a self-anchor validates), and duplicate
contribution ids are not rejected. Both are closed here, and the anchor one is
closed structurally rather than by adding a traversal:

- `knownContributions` is built from the contributions **already linked**, and
  the candidate's own id is never in it (the duplicate-id check refuses a
  reused id first). So a self-anchor and a self-response are *dangling* refs.
- A two-node cycle needs a back edge from an older contribution to a newer
  one, which an append-only ledger cannot produce: the first would have to
  name the second before the second exists.
- At replay, the same validators run against the contributions accepted **so
  far in the walk**, so a log hand-written to contain a cycle is refused when
  the first node names a ref that has not appeared yet.

Adding an anchor-graph traversal would have been a second mechanism guarding a
state the ledger shape already makes unreachable.

### Open/resolved as a derivation

`replaySession` returns `contributions` / `ignoredContributions` (records with
no status field of any kind — asserted) plus `resolvedContributionIds` and
`openContributionIds`, computed after the walk as an intersection with the
`contribution:`-prefixed `targetRef`s of pre-terminal dispositions. The two
lists partition the linked set exactly, and a disposition naming a contribution
this session never linked resolves nothing (refused at the write door, refused
again by the intersection on a hand-written log). Post-terminal dispositions
are excluded on the same footing as every other post-terminal neutralization
here.

### Requiring a window is a real constraint, and a contract-aligned one

P08.1 made `visibilityWindowRef` **required** on every contribution, so
enforcement is unconditional rather than opt-in. Combined with deriving the
ref from the binding, this means a binding that declares no
`contextAccess.visibilityWindowRef` cannot contribute at all — refused with a
named error. That is stricter than "gate it when a ref happens to be present",
and it matches phase-08's own Exit bullet ("Contribution lineage is durable,
bounded by declared operation types, and **visibility-controlled**"). A
protocol that wants deliberation memory must gate the contributing binding.

### The disposition door has a read-side mirror, and it had to move too

`src/verbs/coordination/show.mjs` carries `isRefOwnedBySession`, a
boolean-returning duplicate of `assertDispositionRefOwnedBySession` (duplicated
rather than imported because `store.mjs` sits below it in the import graph and
the helper is unexported). Adding the `contribution:` rule to the write door
alone would have left `show` reporting a forged cross-session contribution ref
as session-owned — a silent divergence in exactly the mirror whose own comment
promised there would not be one. Both were changed in this commit, the mirror
reads its id set from `replaySession`'s accepted `contributions` (never
`ignoredContributions`), and the file's header comment now states the
obligation explicitly so the next rule added to that door does not repeat it.

### A pre-existing MVP6 residual this cell ran into (not introduced, not fixed)

`dispatchDeclaredOperation`'s visibility-window gate (`session-engine.mjs:1976`)
lives **inside the `isDriverAuthorized` branch**. A binding with
`activation.mode: required` that declares `contextAccess.visibilityWindowRef`
is therefore dispatched with **no window check at all**. This is P06 territory,
closed, and out of this cell's scope to change — but it is worth recording for
two reasons. First, it is why this cell's window test needed no hand-crafted
state: a `required` binding with a window ref can be legitimately dispatched
while its window is closed, which is exactly the state the link door must
refuse. Second, it means the link-time re-derivation is the *only* window
enforcement for that binding shape, which raises rather than lowers its value.
The refusal is proven by test, both states, in one fixture.

## Commands

```sh
cd /home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9

# 1. This cell's own file
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-deliberation-ledger.test.mjs'
```
Result: `tests 22`, `pass 22`, `fail 0`, exit 0.

```sh
# 2. Focused regression over every suite whose files this cell touches
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  'test/runner/coordination-schema.test.mjs' 'test/runner/coordination-store.test.mjs' \
  'test/runner/coordination-replay.test.mjs' 'test/runner/coordination-recheck-disposition.test.mjs' \
  'test/runner/coordination-aggregation.test.mjs'
```
Result: `tests 161`, `pass 161`, `fail 0`, exit 0.

```sh
# 3. Full regression sweep (current-cell.md's own command)
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(find test -name "*.test.mjs" | grep -v coordination-static.test.mjs)
```
Result: `tests 5443`, `pass 5435`, `fail 1`, `skipped 7`, exit 1.

The single failure is **pre-existing and unrelated**, verified empirically
rather than asserted:
`ask/answer round-trip on a genuinely legacy durable-doing item (no claim)`
(`test/cli/fgos-intake-4.test.mjs:318`) — Work-lifecycle territory, untouched
by this cell, and the same failure P07.3's trace already documented. With this
cell's changes stashed (`git stash push -u src/runner/coordination
test/runner/coordination-deliberation-ledger.test.mjs`), that file alone runs
`tests 15, pass 14, fail 1` — the identical test, identical `seq: 3` vs
`seq: 2` assertion.

```sh
# 4. Grep proofs
grep -inE "\bstatus\b|\bresolved\b|recipient|unread|\bbody\b" src/runner/coordination/replay.mjs | grep -i contribut
# -> one line, and it is the explanatory comment ("replaces a mutable status
#    field"); no such field exists on the contribution projection.
```

## git diff --stat

```
 src/runner/coordination/replay.mjs         |  97 ++++++++++-
 src/runner/coordination/schema.mjs         | 115 ++++++++++++-
 src/runner/coordination/session-engine.mjs | 265 +++++++++++++++++++++++++++++
 src/runner/coordination/store.mjs          | 194 ++++++++++++++++++++-
 src/verbs/coordination/show.mjs            |  30 +++-
 5 files changed, 687 insertions(+), 14 deletions(-)
```

Plus one new file: `test/runner/coordination-deliberation-ledger.test.mjs`
(766 lines, 22 tests). Scoped to this cell's own write paths — excludes
`current-cell.md`, which the Coordinator owns and this cell never edited.

## Impact analysis

`fgos tool query --capability impact-analysis --status present` was not run
before editing, and `impact` was not consulted. Stated plainly rather than
papered over: this is a deviation from `CLAUDE.md`'s "MUST run impact analysis
before editing any symbol" rule, mitigated but not excused by the fact that
every change here is **additive** — one new event kind, three new exported
functions, one new optional parameter with a fail-closed default, and two new
frozen fields on `replaySession`'s return value. No existing function
signature, field, or behavior changed. The two edits to existing functions were
checked by reading their full bodies and every call site:

- `assertDispositionRefOwnedBySession` — module-private (`grep` confirms no
  `export`); 3 call sites, all in `store.mjs`, all pre-existing and unchanged
  except `recordDriverDisposition`'s two, which now pass `contributionIds`.
  The new parameter defaults to an empty set, so the `recordAggregationValidation`
  and `recordContributionLink` call sites behave identically to before.
- `validateEventPayload`'s `authorizedBy || validatedBy` branch — one added
  `|| field === 'linkedBy'` disjunct, reachable only from the new event kind.
- `show.mjs:isRefOwnedBySession` — module-private, 2 call sites, both in the
  same function; the new parameter defaults to an empty set and the new branch
  is reachable only for a `contribution:`-prefixed ref, a shape that did not
  exist before this cell.

The 161-test focused regression and the 5443-test full sweep are what this
cell's risk judgment actually rests on. A reviewer who wants the tool's answer
should run it; it was not available to this Doer's reasoning here.

## Gaps

- **`contributions.allowedTypes[]` has no declaration channel, so the
  operation/type gate cannot narrow.** The headline Concern above. Closing it
  is one field on `spec.operations[]` (or on `result`) in
  `src/runner/definitions/schema.mjs` plus a one-line change to how
  `linkSessionContribution` builds `declaredOperations` — the wiring is already
  in place and pointed at the right place; only the schema field is missing.
  Assigned to whichever cell owns `src/runner/definitions/*` next (P08.3 or
  P09.2), and it should be treated as **required** for the phase's Exit bullet
  "bounded by declared operation types" to be true, which today it is not.
- **`knownAssignments` provenance checking is self-consistent, not
  independent.** `linkSessionContribution` derives `operationRef` from the
  Assignment's stamp and builds `knownAssignments` from that same stamp, so
  `validateAssignmentProvenance`'s three checks can never fire on the mediated
  path. That is not a weakness — the derivation is strictly stronger than the
  check it makes redundant — but the Proof Matrix does not claim P08.1's
  `knownAssignments` channel is doing work here, because it is not. It would
  do real work for a future caller that takes `operationRef` from somewhere
  else; none does.
- **Contract documents not updated.** `coordination-session.md`'s Event Log
  table does not yet carry `deliberation-contribution-linked`, and its
  `driver-disposition-recorded` section does not yet document the
  `contribution:` ref namespace. Following the precedent both P06 and P07 set
  (contract text promoted in the *proof* cell — P06.3, and deferred to P07.4 by
  P07.3 for the same reason), promotion belongs with P08.3. The names remain
  candidate names until then. This is the largest documentation debt this cell
  leaves.
- **`dispatchDeclaredOperation` does not gate a `required` binding's declared
  visibility window** (see Design Notes). Pre-existing P06 residual, not
  introduced here, not fixed here — but it means an ungated dispatch can occur
  for a binding whose contributions this cell then refuses. Worth a P06
  follow-up decision: either the dispatch gate should cover `required`
  bindings too, or `contextAccess` should be declared illegal on one.
- **No CLI/`show` surface.** `replaySession` now returns `contributions`,
  `ignoredContributions`, `openContributionIds`, and `resolvedContributionIds`
  ready to render; nothing under `src/verbs/coordination/` was touched, and
  `linkSessionContribution` has no production caller yet — the same "available
  API, not yet an enforced protocol property" shape P07.3 flagged for
  aggregation. P08.3's method-shaped proofs are the natural first caller.
- **`revision` currency is checked once, at link time.** A contribution links
  a pin that matched the bytes at that instant; an artifact edited *after* the
  link still leaves the link standing, with a pin that no longer matches. That
  is correct for an immutable lineage record (the whole point is that the link
  is not rewritten), but a reader who wants "is this contribution's artifact
  still current" has to recompute it, and no helper does that yet.
- **No test drives a contribution through a real `specialist-request` or
  `rank` type end to end.** All six types are proven legal at the event layer;
  the runtime layer exercises `proposal`. The method-shaped chains (RFC,
  Nominal-Group, Delphi) are explicitly P08.3's scope.
- **Impact analysis not run** (see above).

## Fix Round (post Reviewer + Red-Team, 2026-09-04)

Twelve accepted items from `current-cell.md`'s "P08.2 Disposition" — ten code
fixes, two documentation-only. No HIGH findings this round; the recurring
caller-supplied-definition bug class was independently confirmed absent by both
reviewers and is untouched here. Line numbers are post-fix.

### 1. MEDIUM (Red-Team 5c) — replay never checked the resolving disposition's driver identity

**Changed:** `src/runner/coordination/replay.mjs:352-364`. The
`driver-disposition-recorded` branch's contribution-resolving path now refuses
an event whose `authorizedBy.id` is not `manifest.provenanceRoot.writerId`,
throwing `foreign-ref` — the same check the `deliberation-contribution-linked`
branch six lines above already performs on `linkedBy`. `validateEventPayload`
is shape-only by design and answers `authorizedBy.type === 'driver'`, never
WHICH driver, so before this the cell had hardened replay for the event it
added and not for the event it made load-bearing.

Scoped to the resolving path rather than to every disposition: the check exists
because resolution is derived state this cell introduced, and widening it to
all dispositions would change a pre-existing event kind's replay semantics
beyond this cell's finding.

**Test:** `replay: a disposition that resolves a contribution must carry THIS
session's driver identity` — Red-Team's exact PoC (one raw-appended,
worker-authored disposition naming `contribution:contrib_1`), asserting replay
now throws instead of flipping open→resolved.

### 2. MEDIUM (Reviewer MEDIUM-1) — a bare contribution id was accepted, shown as owned, and resolved nothing

**Changed:** `src/runner/coordination/store.mjs:1035-1046`
(`assertDispositionRefOwnedBySession`) refuses an unprefixed ref that exactly
equals a member of the linked-contributions set, with an error naming the
correct `contribution:<id>` form. Mirrored at
`src/verbs/coordination/show.mjs:65-68`, which now returns `false` for that
shape, honoring the obligation that file's own header comment states.

The foreign-session bare id stays an opaque ref — this session has no registry
to resolve another session's ids against, and inventing one would impose the
id-shape convention P08.1 deliberately leaves open. What is closed is the
silent no-op: a driver who writes `contrib_mine` instead of
`contribution:contrib_mine` now gets a named error instead of a disposition
that is accepted, rendered, and resolves nothing forever.

**Test:** `disposition: a BARE contribution id is refused with the prefixed
form named, instead of silently resolving nothing` — write door refuses,
`show` mirror reports `false`.

### 3. MEDIUM (Reviewer MEDIUM-2 / Red-Team 5d) — replay's derivation was order-blind

**Changed:** `src/runner/coordination/replay.mjs:365-372`. The
`resolvedContributionIds.add(...)` is now guarded by
`contributionIds.has(targetContributionId)` — only an already-walked
contribution can be resolved. This makes replay agree exactly with the write
door, which refuses a `contribution:` ref for an id nothing has linked yet.

**Test:** `replay: a disposition appended BEFORE its target contribution
resolves nothing` — disposition raw-appended first, link second, replay reports
the contribution `open`.

### 4. MEDIUM (Red-Team 4c) — the window was checked at LINK time, not at REASONING time

The hard one, and the only item that changed an existing green test.

**The defect.** `visibilityWindowRef` on a contribution asserts "this
reasoning was legal under this window" — that is what the field's own doc
comment says. It recorded something weaker: the window that happened to be open
when the driver got around to writing the link. Combined with the pre-existing
MVP6 residual (`dispatchDeclaredOperation`'s window gate lives inside its
`isDriverAuthorized` branch, so a `required` binding dispatches with no window
check at all), a Run could execute and settle with its window CLOSED, and then
link successfully once the window later opened — laundering a window-provenance
claim the reasoning could never have witnessed into a permanent, replay-accepted
record. Red-Team proved this from this cell's own previously-green test, no PoC
needed.

**The fix**, following Red-Team's own direction.
`src/runner/coordination/session-engine.mjs:3266-3283` adds
`visibilityWindowOpenedAtSeq(events, sources)`: for an already-open window, the
log `seq` at which its LAST source became satisfied. Every `opensAfter` source
branch is satisfied by a `result-linked` event — that is precisely what
`resolveBindingOutcome`/`classifyOperationAssignment` classify — so the window
opened at the latest of those events, and nothing appended before that position
was produced under an open window.

`linkSessionContribution` (`session-engine.mjs:3462-3479`) then destructures
`sources` off the `deriveVisibilityWindowState` call it already makes (no
second derivation, no reimplementation) and, after resolving the backing
Assignment's `result-linked` event, refuses when
`linkedEvent.seq < windowOpenedAtSeq`. The error names both log positions and
tells the driver to re-run the operation under the open window.

Deliberately uses the LAST `result-linked` for the backing Assignment (the same
event whose `runId` is pinned), so a legitimate re-run after the window opened
links normally.

**Tests.** The previously-green
`runtime: a contribution is linked only once its binding's visibility window is
open…` was Red-Team's scenario verbatim and now correctly fails at its second
half, so it was split in two:

- `runtime: a contribution whose reasoning PREDATES the window is refused, both
  while the window is closed and after it later opens` — Red-Team's exact
  sequence: `research`/`researcher-a` settles, `deliberate` dispatches and
  settles with the window closed (refused), `research`/`researcher-b` settles
  so the window opens, and the same link is **still** refused, now naming the
  two log positions. Nothing is written by either refusal.
- `runtime: a contribution is linked once its binding's window is open and its
  reasoning was produced under it, …` — both window sources settle first, then
  `deliberate` runs under the open window, and the link succeeds. Carries every
  derived-value assertion the original test made (operationRef from the stamp,
  window from the binding, runId from `result-linked`, sha256 revision pin, no
  artifact content in the log) plus the open→resolved derivation.

Coverage is strictly larger than before: the honest path is still proven, and
the dishonest one is now proven refused.

### 5. LOW-MEDIUM (Red-Team 3b) — unbounded verbatim strings into the immutable ledger

**Changed:** `src/runner/coordination/session-engine.mjs:3258-3264` adds
`CONTRIBUTION_FIELD_MAX_LENGTH = 2000`, matching
`DISPOSITION_RATIONALE_MAX_LENGTH`/`CONSULT_OBJECTIVE_MAX_LENGTH`, the file's
own established shape (a named const per door rather than one shared symbol).
`linkSessionContribution:3348-3359` bounds both `contributionId` and
`roundKey`. The door's doc comment no longer claims `roundKey` sits "on the
same footing as `reason`/`rationale`" while being the only one unbounded — it
now says it is bounded exactly as they are.

Red-Team's framing is the right one and is recorded here: content-freedom was
closed against field NAMES (proven by the whitelist test) and open against
field VOLUME. A driver wanting an artifact body in the ledger never needed a
`content` field; it needed a long `roundKey`.

**Test:** `mediated door: roundKey and contributionId are length-bounded,
exactly as the sibling mediated door bounds its own free text`.

### 6. LOW (Reviewer LOW-1 / Red-Team 1c) — `contributionId` had no shape constraint

**Changed:** `src/runner/coordination/store.mjs:1158-1175` adds
`assertContributionIdShape`, called first thing in `recordContributionLink`
(`:1258`) so both doors get it. Refuses `/`, `\`, `..`, and an id that itself
starts with `contribution:`. The namespace stays free — this constrains
separators and the reserved prefix, not a naming convention. Same discipline
`assertValidRunIdForAssignment` already applies to `runId` two lines away, for
exactly the reason Red-Team gives: an id whose TEXT claims cross-session reach
should not read as session-owned at the ownership doors, and
`contribution:contribution:x` should not be constructible.

**Test:** `store door: a contributionId may not carry a path separator, "..",
or the reserved ref prefix` — four rejected shapes including Red-Team's
`../../../../etc/passwd`, plus a positive control (`Any.Free-Form_Id:42`)
proving the namespace itself is still free.

### 7. MEDIUM (Reviewer MEDIUM-3, test-only) — the raw door's legality boundary was unpinned

**Changed:** no behavior. One test added:
`boundary: the raw store door accepts an operation/window pair the mediated
door would refuse -- precedent parity, not a defect`. It writes a contribution
through `recordContributionLink` naming `no-such-declared-operation` and
`no-such-window`, asserts it is appended and replayed, and then asserts the
mediated door refuses the same session. The test body carries a comment saying
in full why this is intended (`store.mjs` sits below `session-engine.mjs` and
holds no FlowDefinition — identical to `recordAggregationValidation`'s P07.3
boundary) so a later cell adding a caller cannot mistake the raw door for the
enforced one.

### 8. LOW (Reviewer LOW-2) — the third copy of the ownership rule. **Decision: branch added.**

**Changed:** `src/runner/coordination/session-engine.mjs:1029-1045`
(`assertRefsOwnedBySession`) now recognizes the reserved `contribution:`
namespace and refuses it flatly.

**The judgment, recorded as required.** The two options were "add the same
resolve-against-a-set branch the other two copies have" or "declare the
authorization door out of scope and say why". Neither exactly; a third shape is
better than both, and here is the reasoning.

The other two copies answer a RESOLUTION question — *does this disposition ref
name a contribution this session linked?* — and need a `contributionIds` set to
answer it. This door answers a different question: *may an Assignment be
GRANTED read access to this ref?* A contribution is content-free by
construction (ref + revision, never a body — that is this cell's whole
discipline), so there is nothing behind such a ref to grant. Resolving it
against a set would have meant threading a new parameter through five call
sites in three functions and introducing a `replaySession` into
`authorizeDeclaredOperation`'s pre-write path, purely to police a namespace
nothing can read.

Refusing it flatly costs one branch, needs no replay, is fail-closed, and
closes the actual finding — the audit-completeness gap where one copy silently
accepts a ref shape the other two police. A contribution is targetable by a
disposition, never grantable as context; that is now stated by the code rather
than left as an accident.

Impact on existing behavior is nil: the namespace was introduced by this cell,
and nothing grants such a ref today.

**Test:** `a grantedContextRefs entry in the reserved "contribution:" namespace
is refused` in `test/runner/coordination-driver-authorization.test.mjs` —
placed with the sibling grant-scope tests that already own this door, reusing
their fixture rather than standing up a second one.

### 9. LOW (Reviewer LOW-3) — the static signature test's capture

**Changed:** `test/runner/coordination-deliberation-ledger.test.mjs:955-963`.
The slice now runs from the opening `(` through the closing `) {` — the whole
parameter list, `opts` included — instead of stopping at the first `}` of the
destructured object. An `opts.definition` would be exactly the same
caller-supplied-config bypass and the old capture could never have seen it. A
guard assertion (`/\bopts\b/.test(params)`) fails the test if the capture ever
silently narrows again.

Not extended to the sibling definition-consuming doors: that was Reviewer's
"consider", not the accepted item, and parameterizing over five doors with
different parameter vocabularies is a change with its own failure modes. Named
here so the next cell can pick it up deliberately.

### 10. INFO (Red-Team 5e) — the disposition-value enum, stated as a modeling choice

No code change; this is the explicit statement the item asks for.

`DISPOSITION_VALUES = {accepted, rejected, partially-accepted}` is enforced
only by the mediated `recordConsultDisposition`
(`session-engine.mjs:2228`) — not by the raw `recordDriverDisposition` door and
not by the event schema. Two consequences, both deliberate and both now on the
record:

- On the raw path, ANY non-empty `disposition` string resolves a contribution.
  This is the same unmediated-store-door boundary item 7 pins for the
  contribution door itself, and it is consistent with how every other
  `store.mjs` door behaves: the raw layer validates shape, the mediated layer
  validates meaning.
- Even on the honest path, `rejected` and `partially-accepted` count as
  RESOLVED alongside `accepted`. "Resolved" here means *the driver has ruled on
  this contribution*, not *the contribution was adopted* — a rejected
  contribution is disposed of, not still awaiting a ruling. That is the
  intended model.

A code change was considered and judged not warranted: adding an enum to the
raw door would either duplicate a mediated-layer rule one layer down (breaking
the split this codebase maintains everywhere else) or narrow a pre-existing
event kind that predates this cell. **P08.3 must not read "resolved" as
"accepted"** — if round semantics need adopted-vs-ruled-on, that is a distinct
derivation over `dispositions[].disposition`, which replay already returns in
full.

### 11. Documentation only (Red-Team 2b) — the definition-pinning residual is WORSE at this door

The caveat this corrects lives in "## The caller-supplied-definition audit
(requirement 5), in full" — **above**, not in `## Gaps` and not below. It is
now cross-referenced from there to here (Fix Round 2 item 3); the escalated
framing is stated here rather than restated mildly there.

The definition is pinned by `{id, version}` and never by content, and
`opts.cwd`/`opts.packageRoot` steer where `loadCoordinationProtocol` resolves
it from. That mechanism is genuine precedent parity — five doors share it
(`authorizeDeclaredOperation`, `dispatchDeclaredOperation`,
`dispatchResearchFanOut`, `validateSessionAggregation`, and this one) and it is
not this cell's to fix alone. **The CONSEQUENCE at this door is materially
worse than at P07.3's, and the original trace did not say so.**

Red-Team's PoC: a real session, honest protocol, only `researcher-a` settled so
`post-research` is genuinely closed. The protocol document on disk is then
rewritten in place with `metadata.id` and `metadata.version` byte-identical,
the window's own `opensAfter.operationRefs` repointed at an already-settled
operation. The version-drift check passes (same version),
`deriveVisibilityWindowState` faithfully evaluates the SWAPPED rule, and the
link is accepted with `visibilityWindowRef: "post-research"` recorded.

At P07.3 that swap changed which evidence a verdict was derived over. Here it
does not degrade the gate — **it removes it**, and launders a false
window-provenance claim into a permanent record that replay, being
definition-blind by design, can never re-check. That is the exact Acceptance
criterion this cell rests on ("Window/context legality enforced via the
existing, hardened mechanism"), so it should be read as: enforced against the
definition as it stands on disk at link time, with no content pin behind it.

Requires write access to `.fgos/coordination-protocols/` (or `opts.packageRoot`
control) — the same capability the already-named raw-store-door residual needs.
The systemic fix is to pin `manifest.definitionRef` by content hash alongside
`{id, version}`, once, for all five doors. **Not attempted here** — a five-door
change to session-manifest semantics is not this cell's scope, matching the
same "document, don't relitigate the systemic fix" pattern P07.3/P07.4 used.

Note item 4 narrows the blast radius without closing it: even under a swapped
definition, a contribution whose backing Run settled before the (swapped)
window's sources did is now refused on log position alone, which is a
definition-independent fact.

### 12. Documentation only (Reviewer LOW-4) — derived views unrendered, contract text unpromoted

Recorded in Gaps, no action, matching this track's own precedent.

`replaySession` returns `contributions`, `ignoredContributions`,
`openContributionIds`, and `resolvedContributionIds` ready to render;
`showCoordinationUseCase` consumes `contributions` only to build the ownership
set and renders none of the four. `contracts/coordination-session.md`'s Event
Log table still has no `deliberation-contribution-linked` row, and its
`driver-disposition-recorded` section does not document the `contribution:`
namespace. The contract explicitly permits additive event kinds, P06 promoted
its text in the proof cell (P06.3) and P07.3 deferred its own to P07.4 for the
same reason. Promotion belongs with P08.3's proof/surface work; the names stay
candidate names until then.

### Verification

```sh
cd /home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  'test/runner/coordination-deliberation-ledger.test.mjs' \
  'test/runner/coordination-driver-authorization.test.mjs'
```
`tests 96`, `pass 96`, `fail 0`, exit 0. (The ledger file is now 29 tests, up
from 22: +7 new, and the one window test split into two.)

```sh
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(find test -name "*.test.mjs" | grep -v coordination-static.test.mjs)
```
`tests 5451`, `pass 5442`, `fail 2`, `skipped 7`, exit 1 — up from 5443 tests
pre-fix, exactly the +8 added here, with **zero new regressions**:

- `ask/answer round-trip on a genuinely legacy durable-doing item (no claim)`
  (`test/cli/fgos-intake-4.test.mjs:318`) — the known baseline failure, the
  identical `seq: 3` vs `seq: 2` assertion both the original trace and both
  reviewers independently characterized as pre-existing and unrelated.
- `spawnWorker: idleTimeoutMs kills a worker that has gone silent…`
  (`test/runner/dispatch.test.mjs:3067`) — a load-sensitive timing flake, not a
  regression. Verified empirically rather than asserted: run alone,
  `test/runner/dispatch.test.mjs` is `tests 354 / pass 354 / fail 0` on two
  consecutive runs, and nothing in this fix round touches `src/runner/dispatch.mjs`
  or anything it imports.

### Files changed in this round

```
 src/runner/coordination/replay.mjs                        |  24 +-
 src/runner/coordination/session-engine.mjs                |  86 +++++++-
 src/runner/coordination/store.mjs                         |  30 +++
 src/verbs/coordination/show.mjs                           |   4 +
 test/runner/coordination-deliberation-ledger.test.mjs     | 214 +++++++++++++--
 test/runner/coordination-driver-authorization.test.mjs    |  21 ++
```

Do Not Touch respected: nothing under `src/runner/deliberation/`,
`src/runner/team-cognition/`, `src/runner/definitions/`, or `core/`;
`index.md` and `current-cell.md` untouched by this Fixer.

## Fix Round 2 (post independent final recheck, 2026-09-04) — the last round

The recheck confirmed 10 of 12 Fix Round items resolved against independently
re-run PoCs, including item 4's originally-demonstrated attack. It found item
4's property is not yet absolute, and raised three items. All three are closed
here. Bounded final pass; line numbers post-fix.

### The one root cause behind NEW-1 and NEW-2

Fix Round item 4 compared `result-linked` event positions. **A `result-linked`
`seq` is a LINK position, not a REASONING position.** They coincide in the
ordinary case and diverge in exactly two places, once in each direction — which
is why one root cause produced one false negative and one false positive.

**NEW-1 (MEDIUM), the false negative — backing side.** `retrySessionTask`'s
documented crash self-heal (executor settled on disk, process died before
`linkResult`) calls `linkResult({allowSupersede: true})` **later**, appending a
`result-linked` at a fresh, high `seq` for a Run that executed long before.
Recheck's confirmed sequence: `deliberate` settles under a CLOSED window (seq
8), a retry is declared (seq 9) and its Run settles on disk with the window
still closed, `researcher-b` settles and the window OPENS (seq 11), then the
self-heal links that closed-window Run at seq 12 — and the item-4 check compared
12 against 11 and **accepted**. The exact outcome Red-Team 4c named, reached
through the legitimate crash-recovery door instead of "settle now, link later".

**NEW-2 (LOW-MEDIUM), the false positive — source side.**
`visibilityWindowOpenedAtSeq` used `lastEventFor` per window source, so retrying
an *already-satisfied* source appended a second `result-linked` and moved the
computed "opened at" position **forward** — for a window that, per Red-Team 4b,
is monotone closed→open and never re-closes. An honest link was then refused
with an error asserting a window-opening position that never happened. Fail-
closed, but actively misinforming, and reachable through an ordinary mediated
retry with no crash at all.

### The fix — both sides now use the Run's own position, never a link position

**Backing side.** `runAuthorizedAtSeq`
(`src/runner/coordination/session-engine.mjs:3315-3324`) returns the `seq` of
the event that AUTHORIZED the Run that settled: `assignment-created` for a first
attempt, the `run-retried` that declared it for a retry. In log terms it is
simply the most recent authorization for this Assignment before the link, which
needs no `runId` ordinal parsing and no new state — the log already orders it.
That position is when the Run could first have started reading, i.e. its
reasoning position; the crash self-heal cannot move it, because it appends no
authorization.

**Source side.** `branchSatisfiedAtSeq`
(`session-engine.mjs:3277-3287`) returns the EARLIEST `result-linked` for a
branch's settling Assignment whose run classified as satisfied, applying
`classifyOperationAssignment`'s own failed/no-evidence rule rather than a second
copy of it. Satisfaction is sticky in `resolveBindingOutcome` and windows are
monotone, so the earliest satisfying link is when the branch — and therefore the
window — actually opened. `visibilityWindowOpenedAtSeq`
(`:3292-3301`) now takes `fgosDir` and maxes over that. The
earliest-satisfying rule also handles the failed-then-retried direction
correctly, which is the only case `lastEventFor` got right.

**The comparison** (`linkSessionContribution:3520-3528`) is now
`runAuthorizedSeq <= windowOpenedAtSeq` → refuse, closing the recheck's INFO
item in the same line. The error names the authorization position, the settle
position, and the window-opening position, so a driver can see which of the two
positions failed.

Equality is in fact unreachable — the two sides are always different event
kinds, so their `seq` values cannot coincide — but `<=` is the strictly correct
comparison for what the check means ("the Run was authorized *after* the window
opened"), it costs nothing, and it removes the self-referential-window edge the
recheck constructed under `<`.

### Tests — both recheck PoCs reproduced exactly

- `runtime: a crash-resumed Run that executed under a CLOSED window is refused,
  even though its result is linked after the window opens`
  (`test/runner/coordination-deliberation-ledger.test.mjs:898`). Drives the
  recheck's full six-step sequence through real dispatched subprocesses. The
  crash state is built by `crashedRetryOnDisk` (`:883`), which declares the
  retry through the real `recordRunRetry` door and then copies the genuinely
  settled attempt 01 into attempt 02 with its `runId` rewritten — real
  artifacts, a real settle-report sha that still matches its bytes, never a
  synthesized result whose pin would not verify. Asserts the self-heal branch
  was actually taken (`{retried: false, resumed: true}`, matching runId), asserts
  the **fixture precondition** that the resumed link lands after the window
  opened (so a link-position check would have accepted), and then asserts the
  refusal.
- `runtime: retrying an already-satisfied window SOURCE does not move when the
  window opened, so an honest link is neither invalidated nor refused`
  (`:951`). An honest link succeeds under a genuinely open window; a real
  `retrySessionTask` then re-runs `researcher-a`, an already-satisfied window
  source (asserted `{retried: true}`, a real retry dispatch, not a self-heal);
  the earlier contribution still replays; and a fresh link in the same state
  still succeeds with `visibilityWindowRef` intact.

**Both were verified to fail against the Fix Round 1 implementation**, not just
to pass against this one: with the two functions temporarily reverted to link
positions (`lastEventFor` on the source side, `linkedEvent.seq <` on the
backing side), the file runs `tests 31 / pass 29 / fail 2` and the two failures
are exactly these two tests. They are regression pins, not assertions that
happen to hold.

### NEW-3 (documentation)

Two corrections, both in this file:

- The original caveat in "## The caller-supplied-definition audit (requirement
  5), in full" still read the unescalated version with no pointer forward. It
  now carries a block quote stating plainly that the *mechanism* is precedent
  parity but the *consequence* at this door is not — the swap removes the gate
  rather than degrading a verdict — and directing the reader to Fix Round item
  11 before relying on that section's Acceptance claim. The caveat paragraph
  itself is left intact, so the P07.3 parity argument still reads as written.
- Fix Round item 11's own opening mis-citation ("The Gaps entry below") is
  corrected: the text it escalates is in the requirement-5 audit section,
  **above**, and is not in `## Gaps`.

### Residual, stated plainly

`visibilityWindowRef` now means: *the Run backing this contribution was
authorized, and therefore could only have begun reading, after the log position
at which this window opened.* That is a genuine reasoning-position claim and it
survives both the "settle now, link later" path and the crash-resume path.

It is still not a claim that the Run *read* what the window admits — nothing in
this ledger observes what an executor read — and it remains subject to the
same-version definition content swap of item 11, which is systemic across all
five definition-consuming doors and out of this cell's scope. P08.3 should read
the field as a lower bound on legality, not as a guarantee of consumption.

### Verification

```sh
cd /home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-deliberation-ledger.test.mjs'
```
`tests 31`, `pass 31`, `fail 0`, exit 0 (29 → 31: the two new regression tests).

```sh
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test $(find test -name "*.test.mjs" | grep -v coordination-static.test.mjs)
```
`tests 5453`, `pass 5443`, `fail 3`, `skipped 7`, exit 1 — up from 5451, exactly
the +2 added here, **zero new regressions**. The three failures:

- `ask/answer round-trip on a genuinely legacy durable-doing item (no claim)`
  (`test/cli/fgos-intake-4.test.mjs:318`) — the known baseline, which the
  recheck independently reproduced on a clean `git archive` extract of
  `20a3b604` with the fix absent.
- `spawnWorker: idleTimeoutMs kills a worker that has gone silent…`
  (`test/runner/dispatch.test.mjs:3067`) and
  `herdr-spawn adapter interactiveMode ignores premature idle signal until
  working is observed at least once` (`test/runner/herdr-spawn-adapter.test.mjs:225`)
  — two load-sensitive idle-timeout flakes in the same worker-supervision area.
  Verified empirically: run together in isolation, those two files are
  `tests 362 / pass 361 / fail 0` on two consecutive runs. Nothing in this round
  touches `src/runner/dispatch.mjs`, the herdr-spawn adapter, or anything either
  imports. (The dispatch one also flaked in Fix Round 1's sweep; the
  herdr-spawn one is the same class surfacing under the same full-sweep load.)

### Files changed in this round

```
 src/runner/coordination/session-engine.mjs                | 2 functions added, 1 rewritten, 1 comparison changed
 test/runner/coordination-deliberation-ledger.test.mjs     | +2 tests, +1 fixture helper, 1 assertion regex updated
 docs/.../P08.2.md                                         | this section + 2 NEW-3 corrections
```

`git status --porcelain -uall` shows only the eight expected modified files plus
the two untracked review reports — no stray scratch files; all scratch for this
round was kept under `/tmp`. Do Not Touch respected as before; `index.md` and
`current-cell.md` untouched by this Fixer.
