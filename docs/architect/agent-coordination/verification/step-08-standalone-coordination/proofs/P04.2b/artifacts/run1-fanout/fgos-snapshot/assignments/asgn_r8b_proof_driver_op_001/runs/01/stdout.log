Wrote the result artifact. Since Work and context refs were both `(none)`, there was nothing to fan out to, so no sub-agents were spawned and no Work lifecycle verbs were called.

`agent-result.json`:
```json
{
  "status": "completed",
  "summary": "No work items or context refs were supplied to fan out (Work: none, Context refs: none), so no sub-tasks were dispatched. No Work lifecycle verbs were called, per instructions, since this assignment was not marked as the lifecycle driver."
}
```
