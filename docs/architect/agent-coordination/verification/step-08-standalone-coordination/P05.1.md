# Cell P05.1 — Group Cognition framework declaration (Phase 05 R1-R4)

## Requirements

Phase 05 R1-R4 (`plans/260901-1542-step08-standalone-coordination/phase-05-group-cognition-framework.md`):
R1 framework definition, R2 cognitive policy, R3 independence/bounded
exchange, R4 synthesis contract. R5-R8 (external case lock, single-agent
baseline, live heterogeneous proof, quality report) are P05.2 -- R5
specifically requires a business-judgment call (selecting a real
external fgOS-using project's decision) explicitly reserved for the
Coordinator/user, not attempted here.

## Files

New: `core/coordination-protocols/group-cognition-framework.yaml` (319
lines total, including header comments), `test/runner/group-cognition-framework.test.mjs`
(21 tests).
Modified: `test/runner/flow-definition-protocol-loader.test.mjs`
(3-line diff, adds the new fixture's id to an exhaustive-discovery-list
assertion -- necessary, minimal, same class of change made when the
second core fixture originally landed).

## Proof Matrix (from the Doer's report, cross-checked against source)

| Req | Test |
|---|---|
| R1 validates through real schema, golden determinism, exact 6-phase order, correct actor/role wiring per phase, every result advisory | 5 tests |
| R2 tier floors across all 3 named tiers, capabilities/persona requirements, monotonicity, no portable executor/model pin | 4 tests |
| R3 no sibling edge among divergent branches, no incoming edge on fan-in phases, exactly one critique-intent edge capped at maxRounds:1, every edge capped at maxRounds:1, isolated-until-fan-in cohort, vote-as-truth/recursive-graph/peer-chat structurally inexpressible | 8 tests |
| R4 terminal phase advisory+reported, evidenceRequired restricted to honest 2-value enum, 9 synthesis-contract fields documented, completion.mode reachability genuinely exercised | 4 tests |

## Commands

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test \
  'test/runner/group-cognition*.test.mjs' 'test/runner/coordination-*.test.mjs' \
  'test/runner/cohort-planner*.test.mjs'
```
→ **185 pass, 0 fail** (Doer's report; independently re-run by the
Coordinator, same result).

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/**/*.test.mjs'
```
→ (Doer's report) 4895/4908 pass, 8 fail, all exact baseline matches (3
baseline rows even shrank/passed this run). Coordinator has not yet
independently re-run the full suite -- pending, will do at cell close.

## Coordinator's independent source review (before dispatching Reviewer)

Read the full fixture (320 lines including header comments) and
confirmed:
- Divergent-exploration fan-out correctly mirrors
  `independent-research-fan-out-fan-in.yaml`'s already-proven shape:
  every `explore`-intent edge sources from `facilitator-actor` only,
  never a sibling explorer -- structurally makes cross-branch context
  leakage unreachable (R3), matching `dispatchResearchFanOut`'s already-
  proven isolation invariant from P04.2.
- `cluster-deduplicate`/`convergent-synthesis`/`recommend-with-dissent`
  correctly have no incoming topology edge (fan-in shape, matches
  `synthesize-findings`'s sibling precedent) -- reuses
  `synthesizeResearchFanIn`'s already-proven evidence discipline by
  reference, doesn't re-implement or bypass it.
- Exactly one edge (`clusterer-actor -> critic-actor`, intent
  `critique`) carries the critique intent; the other 1:1 edge
  (`critic-actor -> evidence-reviewer-actor`) carries a DIFFERENT intent
  (`evidence-handoff`) -- structurally cannot be dispatched as a second
  critique round (different declared intent), independent of the
  `maxRounds: 1` cap both edges already carry.
- `divergent-exploration` operation correctly wired to 3 DIFFERENT
  actors (explorer-a/b/c) via 3 separate `graph.nodes[].operations[]`
  entries referencing the SAME operation id -- confirmed this is legal
  per `validateFlowDefinition`'s schema (no uniqueness constraint on
  `node.operations[].ref` within one node), and confirmed
  `dispatchDeclaredOperation`'s `targetActorId` disambiguator (built
  specifically in an earlier cell, P04.2, for this exact "one operation
  template, multiple actors" shape) is the intended later consumer --
  the Doer correctly anticipated and reused this existing mechanism
  rather than inventing a new one.
- No operation declares `capabilities:` -- confirmed this is a genuine,
  previously-discovered config gap (zero real `kind:"agent"` executors
  declare a matching `for:` array), not an unexplained omission; `persona`
  + `policy.minTier` carry R2's requirement instead, correctly avoiding
  the landmine `cohort-planner.mjs`'s `matchCandidateToRequirement` would
  otherwise hard-trigger.
- `evidenceRequired: verified` on `evidence-review` is explicitly
  documented as an ASPIRATIONAL target this activity is FOR, not a claim
  that today's read-only execution already reaches it -- honest framing,
  matches this track's own established "never fabricate evidence"
  discipline.
- R4's 9-field synthesis contract is documented as a `contractTemplate`
  convention (the only declarative expression available, since V1's
  FlowDefinition schema has no structured "required output field"
  vocabulary of its own) -- correctly scoped as a documentation
  commitment for whichever later cell renders real output through it,
  not a schema-level enforcement this cell could fabricate.

No item flagged as ambiguous for the Reviewer this cell -- the fixture
is thorough, well-cross-referenced against prior cells' proven
mechanisms, and honest about its own scope limits. Dispatching a
standard Reviewer round matching this track's proportional-rigor
precedent (pure declarative/schema work, no fs/concurrency/execution
surface -- same bar as P02.1/P04.1).

## Review

Independent Reviewer dispatched with a full verification checklist,
specifically directed to scrutinize the fixture's most structurally
novel choice (wiring one operation id to 3 different actors). Result:
**clean, no blocking findings.** Every reuse claim (fan-out isolation
mirroring `independent-research-fan-out-fan-in.yaml`, fan-in-no-edge
mirroring the same fixture's `synthesize-findings`, 1:1 handoff
mirroring `declared-consult.yaml`), every schema-compatibility claim
(the multi-actor wiring is genuinely legal per `validateGraph`'s lack of
a `ref` uniqueness constraint, AND genuinely resolved by
`dispatchDeclaredOperation`'s `targetActorId` parameter -- confirmed
that parameter was built in P04.2 specifically for this exact shape, not
retrofitted), and every negative-test claim (R3's vote-as-truth/
recursive-graph/peer-chat rejections are real injection-and-reject
proofs against `validateFlowDefinition`, tied to specific field-whitelist
rejections, not assertion-by-absence) were independently re-checked
against actual source -- not re-trusted from the fixture's own comments.
`capabilities:` landmine claim independently re-verified against the
real `.fgos/config.json` (all 10 `kind:"agent"` executors confirmed to
declare no `for` field). Focused command independently re-run: 185/185.

Two LOW, non-blocking nits, both accepted as documented gaps, neither
warranting a Fixer round:
- The R4 "9 fields documented" test proves less than its name implies
  (whole-file substring match, not anchored to the specific operation's
  own comment block) -- correct today, structurally weaker than named.
- This trace file's own "247 lines" fixture-size figure was ambiguous
  (matches non-comment line count, not the true 319-line total) --
  corrected below.

**Coordinator disposition: cell CLOSEABLE, no further Red-Team round.**
Pure declarative/schema work with zero fs/concurrency/execution surface
-- matches P02.1/P04.1's proportional-rigor precedent exactly. Both LOW
nits are test-precision/documentation issues, not correctness defects;
fixing them would not change any behavior or close any real gap.

## Gaps

- **LOW, documented not fixed**: `group-cognition-framework.test.mjs`'s
  R4 "9 required output fields... documented directly alongside the
  recommend-with-dissent operation declaration" test asserts via
  whole-file substring match, not anchored to that specific operation's
  own comment block -- correct today, would not catch the phrases moving
  elsewhere in the file in a future edit. Worth tightening if this
  fixture's comments are ever restructured, not blocking now.

## Full-suite verification (pre-close)

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/**/*.test.mjs'
```
→ **4908 tests, 4895 pass, 8 fail, 5 skipped.** All 8 failures match the
documented baseline by exact name. **No new failure beyond the
documented baseline.**

```
node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
```
→ `{"mechanism":"in-process","configured":false}` -- `mechanism` field
intact.
