# Cell P00 — Inventory And Inputs Freeze

Track: `rust-host-r1-kernel`. Phase file:
`plans/260910-1700-rust-host-r1-kernel/phase-00-inventory-and-inputs-freeze.md`.
Capability: `code:review`. No code change (report only).

## Sessions

Two coordination sessions were needed for this cell because of a real
coordination-engine gap, filed as `tsk-40j`: `openDeclaredProtocolSession`
always derives `manifest.actors` (and therefore `closeSessionByQuorum`'s
`requiredActorIds`) from the **protocol definition's own full actor roster**
(`doer`/`reviewer`/`red-team`/`fixer` for
`standalone-master-coordination-loop`), never from the request's own
`actors[]` — so a review-only cell with no `produce-candidate` step can never
close unless `partialPolicy.allowedOmissions` is declared in the session's
**first** open request (partialPolicy cannot be added retroactively; a later
request on the same `coordinationId` resumes the existing manifest via
`findExistingManifest` and its own `partialPolicy` field is silently
ignored).

1. `rust-host-r1-kernel--cell-01` (opened without `partialPolicy`) — ran
   review + red-team successfully but could never close
   (`closeSessionByQuorum` refused: missing `doer`/`fixer`). **Abandoned,
   left unclosed.** Its two findings sets are still valid (real repo state
   was reviewed) and are folded into Round 1 below. Git branch/worktree
   renamed `rust-host-r1-kernel--cell-01` → `rust-host-r1-kernel--p00` before
   any further work (no content lost).
2. `rust-host-r1-kernel--p00` (opened without `partialPolicy`, second
   attempt at the same mistake) — same refusal, same reason. **Abandoned,
   left unclosed.**
3. `rust-host-r1-kernel--p00-review` (opened WITH
   `partialPolicy: {"allowedOmissions": ["doer","fixer"]}` from its first
   request) — closed `partially-complete` cleanly after its recheck pass.
   **This is the session of record for this cell's disposition.**

## Round 1 (session `rust-host-r1-kernel--cell-01`)

- Reviewer (`claude-reviewer-herdr`, opus): 9 findings — 1 HIGH (F1: R3
  missing `scripts/herdr-cockpit-notify.mjs:45`), 4 MEDIUM (F2 second
  path-composed caller unclassified, F3 ownership-header wording asserts a
  state the track won't reach, F4 four unaccounted `rg` hits, F5 unmet
  `rust-cli-and-proof-components-plan.md` §3 instruction), 4 LOW (F6-F9,
  citation/scope precision).
- Red-team (`codex-herdr`, gpt-5.6-terra): 2 real findings — 1 MEDIUM
  (uninventoried caller `plugins/fgOS/skills/terminal/rename.sh:51`), 1 LOW
  (R3 scope wording excludes `package.json:29`); 3 "not found" (selector
  count, lease overlap, Decisions-table contradiction — report held).
- **Disposition: all real findings accepted.** Verified independently by the
  Lead (grep/read against the real files) before accepting each. Fixed by
  editing `plans/260910-1700-rust-host-r1-kernel/reports/p00-inventory.md`
  directly (commit `c2e86dc8`) — added R3 items #10-12, corrected R5's
  wording, added R1's docs-location gap note + 15-row scope caveat,
  corrected R2's citation, listed R3's four previously-unaccounted `rg` hits,
  added an R6 lease-map note.

## Round 2 (session `rust-host-r1-kernel--p00-review`, recheck of commit `c2e86dc8`)

- Reviewer recheck: 3 MEDIUM (M1 wrong `execSync` line numbers for item #11,
  M2 off-by-one file:line for item #12, M3 broader doc-prose caller class
  outside R3's scope), 3 LOW (L1 R5 header over-commit nuance, L2 R6
  lease-map internally inconsistent, L3 missing file:line for item #7's
  resolver site). `verifiedClean` list confirms every other Round-1 fix held
  verbatim.
- Red-team recheck: 1 HIGH (same underlying gap as M3 — active skill-doc
  prose under `domains/coding/skills/**` invokes `bin/fgos.mjs` directly,
  outside R3 and outside `plan.md`'s resolver lease), rest held.
- **Disposition:**
  - M1, M2, L1, L2, L3 — **accepted, fixed** by editing the report again
    (commit `6fb224d1`): corrected `knowledge-canary.mjs` `execSync` line
    numbers (6 real calls at `:26,:30,:43,:61,:64,:67`, not 4 at
    `:27,:31,:42`), corrected `rename.sh` range to `:50-51`, added
    `src/setup/bin-discovery.mjs:24` to item #7, added the R5 npm-channel
    nuance, reconciled R6's lease-map note with items #11/#12.
  - M3 / HIGH (red-team) — **accepted as a real finding, deferred to P10,
    not fixed in this cell.** Rationale: phase-00's own R3 requirement names
    a closed, nine-site list (`legacy-cli-transition.md` §2's own
    enumeration); this report already widened that once to items #10-12
    (concrete source-code/script callers). The newly-found class is
    categorically different — active skill-doc prose instructing an agent to
    run `node "$root/bin/fgos.mjs" ...` directly, spanning at least 13 sites
    across `domains/coding/skills/**`, `plugins/fgOS/skills/retro-next/`, and
    `dogfood-fixture/test/`. Enumerating it exhaustively is P10's own job
    (`plan.md` Goal 6, tier-0 resolver work) — P00 records the gap and the
    concrete example sites P10 must not rediscover from scratch, rather than
    performing a repo-wide prose sweep outside its named scope. Full
    rationale and the example site list are in the report's own "Deferred,
    not resolved in this phase" note (R6 section) and "Revision note".

Fix-round count: 2 (cap 3, per `plan.md`'s Group-Thinking Execution Contract
point 6). No unresolved fixable finding remains; one class is knowingly
deferred with rationale, which is within policy ("deferred only when it is
outside the cell's scope and the rationale says so").

## Verification

- `git diff --stat rust-host-r1-kernel rust-host-r1-kernel--p00` — exactly
  one file changed: `plans/260910-1700-rust-host-r1-kernel/reports/p00-inventory.md`
  (430 insertions, 0 deletions from the track branch's base). No `src/**`,
  `test/**`, `apps/**`, `packages/**`, `Cargo.*` touched (R8 held).
- `grep -c "^    name:" src/cli/command-registry.mjs` → 73, matches R2's
  table; independently confirmed twice (Round 1 reviewer, Round 2 reviewer).
- The nine-file `rg -n "bin/fgos\.mjs" ...` command's every hit is accounted
  for (call site or explicit comment) — confirmed in both rounds.

## Merge

Merge commit: recorded in `plan.md`'s Cell status table after this trace is
written (see that table for the exact commit hash).

## Bug filed during this cell

`tsk-40j` — the `partialPolicy`-must-be-declared-at-open coordination-engine
gap described above. Filed via `fgos submit`, `todo`, not part of this
track's own scope (infra fix, not a track deliverable).
