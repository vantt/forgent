# P06 — Group-Thinking Coding Dogfood Proof

Cell: git branch `confinement-authority-implementation--p06`
Role: Doer + Reviewer + Red-Team + 1 fix round
Date: 2026-09-10

## Purpose (different from P00-P05)

This cell is not primarily a code-change cell. Phase-06's own objective is to
**prove** — with real live dispatch, not mocks — that this track's coordination
mechanism genuinely works end-to-end: cross-provider dispatch, real confinement
enforcement (not just observation), a real fix round, replayability, and zero
Work-lifecycle coupling. This directly and explicitly satisfies the operator's
own standing instruction: verify that the group-thinking/panel executor
configuration (`agy-herdr`, `claude-reviewer-herdr`, `codex-herdr`) is
confinement-compliant **via real live dispatch tests, not unit/mocked tests.**

## Coordination sessions

| Session | Round | Outcome |
|---|---|---|
| `--p06` | Produce | Doer (op_077, `agy-herdr`) executed the full R1-R7 dogfood proof: added a real regression test as the coding payload (closing P05's own deferred LOW — a direct-call test for `evaluateBypassPairing()`'s bypass-pairing gap, plus a table test), recorded the live `.fgos/config.json` + `fgos doctor` output before any change (R6), temporarily configured one capability with an explicit **REQUIRED workspace-write** policy and one with **REQUIRED host-write-denied**, both backed by the real bwrap backend (P03's registration), captured the resulting attestations, then reverted the config to its exact original state and verified the revert with a sha256 hash comparison. Committed as `a93539a9`. The orchestrating `fgos coordination run` CLI process was killed by real system memory pressure again (the third such incident this session, after two during P05) immediately after the Doer settled — the SAME recovery pattern applied: the Doer's herdr pane finished on its own, its result was read directly from `outbox/result-1.json`. |
| `--p06` (continuation, review) | Review | A follow-up request against the SAME `coordinationId`, covering just `review-candidate` with `contextRefs` at the Doer's assignment, since the CLI died before review could dispatch from the original request. Reviewer (op_078, `claude-reviewer-herdr`) did NOT take the Doer's strong claims at face value — it independently re-verified every one: recovered both R3 attestation records directly from the machine's own attestation store and confirmed both show outcome `enforced` (not merely claimed); reproduced one of the required-policy dispatches itself and independently confirmed real bwrap denies both out-of-workspace and `$HOME` writes; independently computed the `.fgos/config.json` sha256 and confirmed it matches the Doer's own recorded baseline exactly; confirmed the new regression tests genuinely fail against a reverted fix (15/16, "Missing expected rejection") and pass at HEAD (16/16); confirmed zero Work-lifecycle state changed via its own mtime/git-status check (not trusting the Doer's own admittedly-vacuous `bash_history` grep, itself flagged as L3). Found 0 HIGH, 2 MEDIUM (M1: the Doer's own report cited evidence-artifact file paths that were never actually written to disk, even though the underlying claims were independently confirmed true by other means; M2: a pre-existing gap, `enforced` attestations still carry observe-mode `unverified`/`unknown` detail in their `channels[]` array — not a P06 regression, carried to P07), 4 LOW (minor evidence-scoping/documentation notes). |
| `--p06` (continuation, red-team) | Red-team | Another follow-up against the SAME `coordinationId` after the CLI died a second time before red-team could dispatch. Red-team (op_079, `codex-herdr`) independently re-confirmed the config restoration, reproduced a live required-policy bwrap dispatch and got `outcome: enforced` again, and confirmed no Work-lifecycle state changed. Found 1 new MEDIUM: the new regression tests cover a direct bypass call using LEGACY boolean flags and pure-boolean table inputs, but not the case where the same incomplete pairing is expressed using `confinement-policy.v1`-shaped input instead — the code already correctly refuses this shape (confirmed, not a live security gap), but no test proved it, so a future regression in that specific input shape would go undetected. |
| Fix round 1 (single round; 0 HIGH throughout this cell) | Fixer `claude-reviewer-herdr` | Fixed reviewer's M1 by actually capturing and committing real evidence: `plans/260910-1243-confinement-authority-implementation/reports/phase-06-group-thinking-coding-dogfood-proof-report.md` plus a `phase-06-evidence/` directory with the real coordination chain/show JSON snapshots and both attestation JSONs. Fixed red-team's M1 by adding a `confinement-policy.v1`-shaped incomplete-bypass test case alongside the existing legacy-flag one. Committed as `277bf650`. Reviewer-recheck (op_081) verified the fix with real rigor: confirmed 5 of the new evidence files are byte-identical (sha256) to what it had itself captured in op_078, 2 of them also byte-identical to the live machine attestation store; independently re-ran `fgos coordination chain`/`show` and got fresh matching captures. Confirmed via **mutation testing** that the new v1-shaped test is genuinely sensitive: 16/17 with a deliberately broken `herdr-round.mjs:225`, 17/17 restored. Found only 2 trivial LOW (a narrative-prose mismatch in the report vs. its own committed evidence file, and a ~12-second timestamp-source discrepancy) plus the already-known M2 carried forward. Red-team-recheck (op_082) independently confirmed the v1-shaped test now exists and passes, 17/17, 0 findings. |

## How this satisfies the operator's standing instruction

The operator required real, live-dispatch proof (not mocked/unit tests) that the
group-thinking/panel executor configuration is confinement-compliant. This cell
delivers exactly that, independently corroborated by 3 separate real dispatches
(Doer, Reviewer, Red-Team, across 2 distinct providers — Claude and OpenAI Codex,
plus Gemini via `agy-herdr` for the Doer):

- **Live enforcement, not observation**: a `code:implement`-class capability with
  an explicit `required`+`workspace-write` policy, and a `code:review`-class
  capability with `required`+`host-write-denied`, both backed by the REAL bwrap
  0.6.1 backend (not a fake/mocked adapter), produced genuine `outcome: enforced`
  attestations — recovered directly from the machine's own attestation store by
  the reviewer, not merely relayed from the Doer's own claim, and reproduced live
  by the reviewer AND independently again by red-team.
- **No permanent live-config mutation**: the temporary required-policy
  configuration used to prove enforcement was reverted to `.fgos/config.json`'s
  exact original state, verified via sha256 hash comparison by TWO independent
  parties (reviewer and red-team), not just the Doer's own claim.
- **Zero Work-lifecycle coupling**: independently confirmed via git-status/mtime
  checks by both the reviewer and red-team (not relying on the Doer's own
  admittedly weak headless `bash_history` grep).
- **The dogfood proof used all three group-thinking/panel executors for real**:
  `agy-herdr` (Doer), `claude-reviewer-herdr` (Reviewer and, after the executor
  swap, Fixer), `codex-herdr` (Red-Team) — exactly the roster this track has used
  throughout, all confirmed working under confinement-aware dispatch in this
  cell's own live run.

Full evidence is committed at
`plans/260910-1243-confinement-authority-implementation/reports/phase-06-group-thinking-coding-dogfood-proof-report.md`
and `plans/260910-1243-confinement-authority-implementation/reports/phase-06-evidence/`.

## Requirements proof (phase-06-group-thinking-coding-dogfood-proof.md)

| Req | Evidence | Status |
|---|---|---|
| R1 | Real plan-loop cell from a linked worktree (`confinement-authority-implementation-p06`), not the main checkout | satisfied |
| R2 | 4 actor bindings (doer/reviewer/red-team/fixer) across 2 distinct real providers (Claude, OpenAI Codex) plus Gemini via `agy-herdr`'s doer role — 3 distinct providers total | satisfied |
| R3 | A `code:implement`-class route used `required`+`workspace-write`; a `code:review`-class route used `required`+`host-write-denied`; both produced genuine `enforced` attestations, live-verified by 3 independent parties (Doer, reviewer, red-team) | satisfied |
| R4 | A real fix round occurred (authorize, revise, reviewer-recheck, red-team-recheck, disposition, final close) — not forced artificially, arose naturally from genuine findings | satisfied |
| R5 | `fgos coordination chain`/`show --json`, attestation refs/digests, changed paths, tests, and no-Work-touch proof all captured and committed as real evidence files (fix round 1's own deliverable, closing the initial evidence-packaging gap) | satisfied |
| R6 | Exact config + doctor output recorded before the temporary required-policy change, and again after reverting — the revert independently sha256-verified by 2 parties | satisfied |
| R7 | Not needed — all 3 live providers were available and worked throughout; no fake-provider fallback was required | satisfied (moot) |

## Tests

`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/dispatch*.test.mjs' 'test/runner/assignment-*.test.mjs' 'test/runner/coordination*.test.mjs' 'test/verbs/coordination*.test.mjs' 'test/setup/*.test.mjs' 'test/architecture.test.mjs' 'test/e2e/runner-loop.test.mjs'` at final HEAD `277bf650`, independently reproduced by both reviewer and red-team: **1888 tests, 1885 pass, 2 fail, 1 skip** — the 2 stable failures are `dispatch-production-call-sites.test.mjs:427,526` (P04's own already-deferred M-2, unrelated). New tests (`test/runner/dispatch-confinement-p05.test.mjs`, extended in this cell): 17/17 pass, mutation-verified sensitive.

## Deferred (not blocking — 0 HIGH throughout this cell)

- 2 trivial LOW from the final recheck: a narrative-prose mismatch between the phase-06 report and its own committed evidence file (`late`/`missing` actor lists), and a ~12-second capture-vs-generation timestamp discrepancy in the report. Cosmetic, no action needed beyond noting them.
- **Carried from P05/reviewer op_078, unaffected by this cell**: M2 — `enforced` attestations still carry observe-mode `unverified`/`unknown` detail strings in their `channels[]` array (`authority.mjs:270-330`) even when the overall `outcome` is correctly `enforced`. This is a pre-existing attestation-richness gap (the same shape as P03's own NEW-1b/NEW-1c: the label is correct, the record's descriptive body underneath it is still thin), not something this cell introduced. Left for P07 or a future hardening pass.
- All P01-P05 structural/residual findings not specifically touched by this cell remain as previously documented; not re-litigated here.

## Merge

`git merge --no-ff confinement-authority-implementation--p06` into
`confinement-authority-implementation` — see `plan.md`'s cell-status table
for the merge commit.
