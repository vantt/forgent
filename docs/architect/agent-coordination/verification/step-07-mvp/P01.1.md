# Cell P01.1 — executeAssignment Layering And Crash-Snapshot Timing

Status: done (2026-08-31)
Phase: 01 (executeAssignment hardening + plan-verdict derivation)
Requirements: R1 (G1), R2 (G6)

## Requirements

- R1 (G1): `src/runner/dispatch/assignment-runner.mjs` no longer imports
  `src/intake/plan.mjs`.
- R2 (G6): `gitBefore` captured before launch unconditionally;
  `evidence.gitBeforeSource` marks `pre-launch` vs `post-crash-fallback`.

## Proof Matrix

| Req | Code/Doc Path | Positive Test | Negative Test | Evidence | Status |
|---|---|---|---|---|---|
| R1 | `src/runner/dispatch/assignment-runner.mjs`, `src/runner/paths.mjs`, `src/intake/plan.mjs` | `test/architecture.test.mjs` "import một chiều xuống" (line ~134) green | `test/intake/plan.test.mjs` (87/91 pass, 4 pre-existing G2 failures byte-identical to baseline) | commands below | closed |
| R2 | `src/runner/dispatch/assignment-runner.mjs` (~700–970) | `test/runner/assignment-runresult.test.mjs::"executeAssignment captures gitBefore pre-launch when the worker commits then crashes (Cell 6.7 G6)"` and `test/runner/assignment-runresult.test.mjs::"executeAssignment captures gitBefore pre-launch when the worker commits then the run rejects via worker-timeout"` both green | same two tests assert `status:'failed'`/`confidence:'failed'` on their respective crash/reject path alongside the gitBefore/gitBeforeSource checks | commands below; see also "## Fix Evidence" | closed |

`resolveContentRoot`/`readLockedContext` moved from `src/intake/plan.mjs`
(use-case layer) to `src/runner/paths.mjs` (infra layer, alongside
`resolveMainCheckoutRoot`); `plan.mjs` now `import`s + re-exports both
unchanged so every other caller (`discovery.mjs`, `bin/fgos.mjs`,
`operation-choice.mjs`, `loop.mjs`) is untouched. `assignment-runner.mjs`
imports `resolveContentRoot` from `../paths.mjs` (merged into its existing
paths.mjs import) and no longer imports `../../intake/plan.mjs` at all.

`gitBefore` is now captured via `safeGitHead(effectiveCwd)` unconditionally
BEFORE `executeExecutorCli` is awaited (was previously
`rawResult?.headBefore ?? safeGitHead(effectiveCwd)` computed AFTER
settle/crash, same instant as `gitAfter`). `evidence.gitBeforeSource` is a
new field, `'pre-launch'` today (the only code path reaching the capture —
`safeGitHead` never throws, so there is currently no runtime branch that
falls back); `'post-crash-fallback'` is kept as the documented sibling
value for future-proofing per phase-01 R2's explicit ask, not currently
produced by any path.

## Commands

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/architecture.test.mjs'
# -> 6/6 pass, including "import một chiều xuống" (G1 closed)

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/**/*.test.mjs' 'test/architecture.test.mjs'
# -> 1215/1215 pass, 0 fail (includes the new G6 crash-simulation test)

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/intake/plan.test.mjs'
# -> 87/91 pass, 4 fail (G2 baseline, confirmed byte-identical via `git stash`
#    diff-test against the pre-change tree: same 4 test names/messages)

FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/intake/**/*.test.mjs' 'test/cli/**/*.test.mjs' 'test/e2e/**/*.test.mjs'
# -> 1122 tests, 1110 pass, 7 fail: 4x G2 (test/intake/plan.test.mjs),
#    1x G7 (test/cli/fgos-intake-4.test.mjs ask/answer round-trip flake),
#    2x G4 (test/e2e/pr-gate.test.mjs, test/e2e/self-improve-loop.test.mjs)
#    -- all 7 confirmed byte-identical to the pre-change baseline via
#    `git stash` diff-test.
```

## Coordinator Verification

Independently re-ran (not trusting Doer's self-report):
- `test/architecture.test.mjs`: 6/6 pass, layering test green (G1 closed).
- `test/runner/**/*.test.mjs` + `test/architecture.test.mjs`: 1215/1215 pass.
- Full `test/**/*.test.mjs`: 4483/4491 pass, 8 fail — exactly the remaining
  baseline set (G2×4, G4×2, G7×1, herdr-live×1); G1 dropped out (9->8).
- `gitnexus detect_changes(scope:'all', repo:forgentX)`: 2 changed symbols
  (`executeAssignment`, `executorId` in `assignment-runner.mjs`), 4 affected
  cross-community processes (all `ExecuteAssignment -> ...`), risk `medium`,
  no HIGH/CRITICAL. Confirms blast radius stays inside the executeAssignment
  path per the cell's scope; addresses the Doer's flagged GitNexus-tool gap.

## Review

Independently re-verified R1 and R2 against the diff (`git diff
c425fe6e7dce8db683cbc92fd0bb61d6245fca6b -- src/runner/dispatch/assignment-runner.mjs
src/runner/paths.mjs src/intake/plan.mjs test/runner/assignment-runresult.test.mjs`),
the callers the Doer claimed were untouched, and the two required commands.
No HIGH or MEDIUM findings. Two LOW/informational notes below; neither blocks
close.

**R1 (G1) — verified correct.**
- `src/runner/dispatch/assignment-runner.mjs` no longer imports
  `../../intake/plan.mjs` (grep-confirmed); it imports `resolveContentRoot`
  from `../paths.mjs` (infra, same layer). `src/runner/paths.mjs` gained
  `readLockedContext`/`resolveContentRoot`, unchanged bodies, plus a new
  `import { branchNameFor, findCheckoutPath } from './worktree.mjs'` — same
  infra layer (`docs/architecture-manifest.json` ranks both `infra`), no new
  violation introduced.
- `src/intake/plan.mjs` re-exports both symbols from `../runner/paths.mjs`
  and keeps using them internally (lines 582-583, 907-908) via that same
  import — `execFileSync`/`branchNameFor`/`findCheckoutPath` are fully
  removed from `plan.mjs` (grep-confirmed zero remaining references), so
  nothing is dead-imported or silently broken.
- Verified every claimed caller still imports from `plan.mjs` unchanged and
  passes its own tests: `discovery.mjs` (`test/intake/discovery.test.mjs`,
  37/37 pass), `bin/fgos.mjs` and `operation-choice.mjs`
  (`test/runner/operation-choice.test.mjs`, 130/130 pass), `loop.mjs`
  (covered by the `test/runner/**` run below). None of these files were
  touched by the diff.
- `test/intake/plan.test.mjs`: 87/91 pass, same 4 failing test names
  before and after the diff — confirmed byte-identical via `git stash` /
  `git stash pop` diff-test run independently (not trusting the Doer's or
  Coordinator's report), so the G2 baseline genuinely carries zero new
  regressions from this move.

**R2 (G6) — verified correct.**
- `gitBefore = safeGitHead(effectiveCwd)` is captured unconditionally
  before `executeExecutorCli` is awaited (line ~707), strictly earlier than
  the pre-change `rawResult?.headBefore ?? safeGitHead(effectiveCwd)` line
  that used to run after settle. `gitAfter` capture (line ~757, unchanged)
  still runs after settle/crash. The two are always captured at genuinely
  different instants on every path (happy, crash, timeout) — matches the
  phase file's Bug Taxonomy prohibition ("gitBefore/gitAfter captured at the
  same instant on the happy path... silently masks G6") and does not
  reintroduce it.
- `evidence.gitBeforeSource` is written from one single `const` in both
  `evidenceData` (evidence.json) and `runResult.evidence` — same value both
  places, no risk of the two disagreeing.
- The new crash-simulation test (`test/runner/assignment-runresult.test.mjs`)
  genuinely simulates a crash: a real git repo, a real subprocess that
  performs a real `git commit` then `process.exit(1)`, asserted
  `status:'failed'`/`confidence:'failed'`, `gitBefore === <sha before run>`,
  `gitBefore !== gitAfter`, `gitBeforeSource === 'pre-launch'`, checked both
  in the returned RunResult and in `evidence.json` on disk. Ran it 3x in
  isolation — stable, no flake.
- No consumer of `evidence.json` enforces `additionalProperties: false` or a
  fixed key set (checked `operation-choice.mjs`'s evidence.json reader and
  `src/state/`); the new `gitBeforeSource` field is additive and does not
  break any existing schema/consumer.

**LOW — `evidence.gitBeforeSource` currently has one unreachable literal.**
`'post-crash-fallback'` is never assigned by any code path: `gitBefore` is
now always sourced from the single pre-launch `safeGitHead(effectiveCwd)`
call, which fails closed to `null` internally and never throws, so there is
no branch left that would need a distinct "we had to fall back after the
crash" label. The Doer's own Gaps section discloses this. Reading the phase
file's R2 wording literally ("the RunResult marks... 'pre-launch' vs
'post-crash-fallback'") could be read as requiring a reachable second
branch, but the more defensible reading is that R2's actual goal — stop
gitBefore/gitAfter being captured at the same instant, make the source
explicit — is fully met, and `'post-crash-fallback'` is correctly scoped as
a documented sibling value for a future capture path (Phase 02/03), not a
presently-required branch. Not blocking. Smallest-fix direction if this is
later judged insufficient: none needed now: any code that later reintroduces
an alternate gitBefore-capture path (e.g. a session-relative worktree not
yet materialized pre-launch) must explicitly set `gitBeforeSource:
'post-crash-fallback'` at that call site — no test currently exercises
that branch, so nothing catches an omission until then.

**LOW — latent (currently unreachable) asymmetry between gitBefore and
gitAfter sourcing.** `gitBefore` now always reads `safeGitHead(effectiveCwd)`
directly; `gitAfter` still prefers `rawResult?.headAfter` (captured inside
`cli.mjs`'s `executeExecutorCli`, at its own `cwd` parameter, for the
out-of-process/agent-CLI adapter path) before falling back to
`safeGitHead(effectiveCwd)`. Today `effectiveCwd` (`compiledPlan?.invocation?.cwd
?? compiledPlan?.cwd ?? cwd`) always resolves to plain `cwd` — grep-confirmed
`invocation.cwd`/`compiledPlan.cwd` are never set anywhere in `src/`, so
`cli.mjs`'s internal `cwd` and `effectiveCwd` are always the same directory
and `git rev-parse HEAD` is repo-level, not directory-level, so this is a
non-issue in practice. If a future change starts populating
`compiledPlan.invocation.cwd`/`compiledPlan.cwd` with a directory in a
*different* git worktree than `cwd`, `gitBefore` and `gitAfter` would then be
read from two different repositories and the comparison becomes meaningless
— worth a one-line comment near `effectiveCwd`'s definition (or in R2's own
comment block) naming this constraint so a later Phase 02/03 change does not
reintroduce a silent mismatch. Not blocking for this cell; no test currently
covers it because no code path can trigger it yet.

**Test commands independently re-run (not trusting reported numbers):**
- `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/architecture.test.mjs'`
  — 6/6 pass, including the layering test (G1 closed). Matches claim.
- `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/**/*.test.mjs' 'test/architecture.test.mjs'`
  — first run: 1213/1215 pass, 2 fail (`herdr-spawn-adapter.test.mjs`'s
  interactiveMode polling test timed out at 5000ms; `session.test.mjs`'s
  concurrent-createSession test hit a 10s lock-acquire timeout). Re-ran both
  files in isolation: `session.test.mjs` went fully green (23/23); the SAME
  `herdr-spawn-adapter.test.mjs` file instead failed a *different* test (the
  explicitly-named `(LIVE)` test, on a 60s timeout hitting real binaries).
  This is exactly the known-flaky, load/timing-sensitive `herdr-live` gap
  the cell's own acceptance criteria names as staying open ("G2/G4/G7/herdr-live
  stay open"), not a regression from this diff — neither failing file is
  touched by the diff, and `assignment-runresult.test.mjs` (the file this
  cell actually changed) passed 25/25 across 3 repeated isolated runs with
  zero flake. The Coordinator's reported "1215/1215, 0 fail" appears to have
  been a favorable/lucky run rather than a deterministic result on this
  machine; future coordinators re-running this exact command should expect
  occasional herdr-spawn/session timeouts unrelated to G1/G6 and should not
  treat a single green run as proof the baseline moved.

## Red-Team

Independently re-ran the required baseline before trusting any prior claim:
`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/architecture.test.mjs'
'test/runner/**/*.test.mjs'` — 1215/1215 pass, 0 fail (own run, this session).

**HIGH — the new G6 crash-simulation test is vacuous: it passes even with the
R2 fix fully reverted, so it does not actually prove the fix works.**

Attack: mentally reverting R2 means restoring the pre-diff lines
`const gitBefore = rawResult?.headBefore ?? safeGitHead(effectiveCwd);` /
`const gitAfter = rawResult?.headAfter ?? safeGitHead(effectiveCwd);`, both
computed AFTER `executeExecutorCli` settles (the exact "captured at the same
instant" bug G6 exists to close). Per the cell's own attack checklist item
("does it fail if you mentally revert the R2 fix?"), I did this for real
instead of reasoning about it abstractly: edited
`src/runner/dispatch/assignment-runner.mjs` in place to restore the two
old lines (keeping `gitBeforeSource` as a literal so only the value-computation
changed), ran `node --test --test-name-pattern="Cell 6.7 G6"
test/runner/assignment-runresult.test.mjs`, then fully restored the file from
a byte-for-byte backup (`diff` confirmed identical afterward; `git diff --stat`
on the file matches the original 28-line diff again — no residual change).

Result: the shipped test **still passes** against the reverted code —
`gitBefore` still equals `shaBeforeRun`, `gitBefore` still differs from
`gitAfter` — only the `gitBeforeSource` field assertion would have failed
(and only because that field is new, not because it proves any timing
behavior).

Root cause: the test's "crash" is `process.exit(1)` inside the worker after a
real `git commit`. In `transport.mjs`'s `cliSpawnAdapter`, a nonzero exit still
fires the child's `'close'` event and **resolves** the adapter promise
normally (`resolve({status: code, signal, stdout, stderr, ...})` at
`transport.mjs:467-469`) — it never rejects. So `executeExecutorCli` in
`cli.mjs` runs to completion and returns `buildDispatchResult({..., headBefore,
headAfter})`, where `headBefore` was captured by `cli.mjs`'s OWN
`captureHeadSha(cwd)` at `cli.mjs:501`, itself already pre-adapter-call and
UNCHANGED by this diff. So `rawResult.headBefore` is already correct
(pre-commit SHA) regardless of whether `assignment-runner.mjs`'s new
pre-launch capture exists — the old `rawResult?.headBefore ?? safeGitHead(...)`
fallback never engages its `safeGitHead` branch at all for this specific crash
shape (nonzero exit, `'close'` event, no thrown exception). The test never
reaches the actual buggy branch (`rawResult` built manually in
`assignment-runner.mjs`'s own `catch (err)` block at line ~743-751, which has
no `headBefore`/`headAfter` field at all) — that branch is only reached when
`executeExecutorCli` itself **rejects** (worker-timeout, idle-timeout,
maxBuffer, spawn-fail/ENOENT, or `dispatch-in-flight` lock contention), not on
a plain nonzero exit code.

I confirmed the fix IS real and DOES matter, just not provably from the
shipped test: wrote a second repro
(`/tmp/.../scratchpad/g6-timeout-repro.test.mjs`, not committed, scratch-only)
where the worker commits then hangs past `timeoutMs` (a genuine
`worker-timeout` rejection from `executeExecutorCli`, the real "crash after
commit" shape). Against the current fixed code this repro passes
(`gitBefore` = pre-run SHA, differs from `gitAfter`). Against the same
mentally-reverted code, this repro **fails**: `gitBefore === gitAfter`
(both equal the post-crash HEAD, exactly the bug R2 describes), proving the
fix's actual behavioral effect and that a correctly-shaped test can and does
catch its absence.

Evidence (commands run, this session):
```
# shipped test, reverted code -> still green (vacuous)
node --test --test-name-pattern="Cell 6.7 G6" test/runner/assignment-runresult.test.mjs
# -> 1 pass

# scratch timeout-repro, current (fixed) code -> green
node --test /tmp/.../scratchpad/g6-timeout-repro.test.mjs
# -> 1 pass, gitBefore = shaBeforeRun, gitBefore != gitAfter

# scratch timeout-repro, reverted code -> red
node --test /tmp/.../scratchpad/g6-timeout-repro.test.mjs
# -> 1 fail: gitBefore === gitAfter (both post-crash HEAD) -- the exact G6 bug
```

What breaks: the Proof Matrix's R2 negative-test claim ("same test asserts
status:'failed'/confidence:'failed' on the crash path") and the Review's
"genuinely simulates a crash... stable, no flake" both overstate what this
test proves. It proves crash-path status/confidence classification correctly
(that part is real and does fail on revert — did not re-check status/confidence
specifically against reverted code but that logic is untouched by this diff
regardless). It does NOT prove the G6 gitBefore-timing fix, because the chosen
crash shape (synchronous nonzero exit) never exercises the code path
(`assignment-runner.mjs`'s own `catch` block / `rawResult` without
`headBefore`) that the fix changes. Today this happens to still be safe in
practice ONLY because `cli.mjs`'s independent, unrelated, pre-existing
`headBefore` capture (line 501) already provides correct provenance for this
one crash shape — a coincidence of two independently-correct pre-launch
captures, not something this test demonstrates or depends on. If a future
change altered `cli.mjs`'s own headBefore timing (Phase 02/03 touches
`cli.mjs`/`transport.mjs` per the plan's broader scope) while this test stayed
green, a real regression in `assignment-runner.mjs`'s G6 fix could go
undetected because this test would keep passing for the wrong reason.

Severity: HIGH — not because the underlying G6 code is wrong (it is
correct, confirmed above), but because the stated proof for it is false:
the negative test does not test what the Proof Matrix and Review both claim
it tests, so a future regression in this exact fix has no test coverage
that would catch it via a nonzero-exit-only crash shape.

Smallest fix direction: change the crash-simulation test's worker script to
actually reject `executeExecutorCli` after a commit — e.g. commit then hang
past a short `timeoutMs` (proven above to correctly fail on revert), or
commit then have the adapter fail to spawn a expected artifact causing a
`worker-timeout`/`worker-spawn-fail` DispatchError. Keep the existing
nonzero-exit test too (it is still a valid, separate proof that
status/confidence classify a real crash correctly) but add the
timeout/reject-shaped case as the actual G6 proof, and note in a comment why
both shapes are needed (one proves classification, the other proves
gitBefore-timing).

**Verified (not broken) — `safeGitHead` never-throws claim.** Read
`safeGitHead` (`assignment-runner.mjs:123-135`) directly: `!dir` returns
`null` early; the `execFileSync` call is wrapped in a bare `try/catch` that
returns `null` on ANY exception (missing repo, detached HEAD, corrupted
`.git`, permission error, non-git directory — all collapse to the same
`catch {}` → `null`). Detached HEAD does not even hit the catch branch:
`git rev-parse HEAD` resolves fine in detached HEAD. Confirmed no runtime
path exists today that would need a `'post-crash-fallback'` branch — the
Doer/Reviewer's claim holds on direct code read, not just on trusting the
prose. Attack failed to find a bug: `gitBeforeSource` staying `'pre-launch'`
even when `gitBefore` itself is `null` (failed capture) is consistent, not
mislabeled — the field describes WHEN the capture was attempted, not
whether it succeeded, and no code path currently claims otherwise.

**Verified (not broken) — cwd-sourcing asymmetry is genuinely unreachable
today, confirmed by reading the actual object construction, not just
grepping for field names.** Traced `effectiveCwd`
(`compiledPlan?.invocation?.cwd ?? compiledPlan?.cwd ?? cwd`) against
`compileDispatchPlan`'s real return shape in `src/runner/dispatch/plan.mjs:261-274`:
the `invocation` object it constructs is always exactly
`{ via, adapter, protocol }` (or `null`) — no `cwd` key exists in that object
literal anywhere in the function, and the top-level returned plan object never
sets a `cwd` key either. So `effectiveCwd` always resolves to the plain `cwd`
local (`opts.cwd ?? process.cwd()`), matching the Reviewer's grep-based claim.
Additionally traced `rawResult.headAfter`'s actual source: `cli.mjs`'s
`executeExecutorCli` is called with that SAME `cwd` value (not a
separately-resolved one), and internally captures its own `headBefore`/
`headAfter` via `captureHeadSha(cwd)` using that identical value
(`cli.mjs:501,504`) — so today `gitAfter`'s two possible sources
(`rawResult.headAfter` or the `safeGitHead(effectiveCwd)` fallback) are
always reading the same directory as `gitBefore`. Attack failed to construct
a live divergence: no code path in the current tree ever populates
`compiledPlan.invocation.cwd` or `compiledPlan.cwd`, so the asymmetry stays
exactly as latent as the Review already stated. Agrees with the Review's LOW
note; no new evidence to escalate it.

**LOW — pre-existing (not introduced by this diff) concurrent-dispatch race
in run-attempt directory allocation; touches G6's "provenance is explicit"
claim under concurrency but is outside R1/R2's changed lines.** The
attempt-number computation (`assignment-runner.mjs:618-634`, unchanged by
this diff) reads existing `runs/NN` dirs via `readdirSync`, picks the next
free number via a `fs.existsSync` loop, then `fs.mkdirSync(runDir, {recursive:
true})`. `mkdirSync` with `recursive: true` does not throw if the directory
already exists (documented Node.js behavior) — so two concurrent
`executeAssignment()` calls for the same assignment (e.g. a caller retries
without awaiting the first attempt, or two dispatch paths race) could both
observe the same "next free" attempt number in the `existsSync`/`mkdirSync`
window and both write into the same `runs/01` directory: `dispatch-plan.json`,
`run.json`, `evidence.json` (including `gitBefore`/`gitAfter`/
`gitBeforeSource`) all last-writer-wins, silently mixing two runs' evidence
into one directory. The `acquireMainCheckoutLock` cwd-level dispatch lock
(`cli.mjs:466`) does exist but is acquired well AFTER `assignment-runner.mjs`
already created `runDir` and captured `gitBefore` (lines 634, 715 vs.
`cli.mjs:466`), so it does not protect the run-dir-allocation window at all —
it only prevents two concurrent WORKER SPAWNS in the same cwd, not two
concurrent run-directory claims. Not introduced, changed, or fixed by this
diff (the attempt-number logic is untouched in the reviewed range); flagged
because it directly undermines R2's "provenance is explicit" goal under
concurrency, which is otherwise correctly closed for the single-run case.
Smallest fix direction (out of this cell's scope): make attempt-number
allocation atomic, e.g. `fs.mkdirSync(runDir)` WITHOUT `recursive: true`
inside a loop that retries the next number on `EEXIST`, so two racers can
never both "win" the same `runDir`.

**Attempted, did not break — dirty-before/gitBefore capture-order gap.**
`dirtyBefore` (`safeGitStatusFiles`) is captured at line 704, `gitBefore`
(`safeGitHead`) at line 715 — two sequential synchronous `execFileSync` calls
with no `await` between them. In principle an external mutation landing in
that microscopic window could make the two snapshots inconsistent with each
other, but this gap predates the diff (both calls already existed in this
relative order before R2; R2 only changed WHEN `gitBefore` is captured
relative to worker launch, not relative to `dirtyBefore`) and is not
practically exploitable (sub-millisecond synchronous window, no I/O yield
point). Not a new regression from R1/R2.

**Attempted, did not break — layering circularity via the new
`paths.mjs -> worktree.mjs` import.** Checked `worktree.mjs`'s own imports
(`src/runner/worktree.mjs:56-60`): only `node:fs`, `node:os`, `node:path`,
`node:child_process`, and `../state/store.mjs` — no import back into
`paths.mjs` or `intake/`. No circular-import risk introduced by R1's move.

## Disposition

- RT-1 (HIGH, vacuous G6 crash-simulation test): ACCEPTED. Underlying R2 fix
  is correct (Red-Team proved this with an independent timeout-repro that
  fails on revert); the shipped test just doesn't exercise the branch it
  claims to. Routed to Fixer: reshape the crash simulation to trigger a real
  `executeExecutorCli` rejection (worker-timeout, per Red-Team's proven
  repro) so it fails on revert of the R2 fix. Keep the existing nonzero-exit
  case too (valid, separate proof of status/confidence classification) —
  add the timeout-shaped case alongside it, not instead of it.
- RT-2 (LOW, run-attempt-dir concurrency race): DEFERRED, out of this cell's
  scope. Pre-existing (unchanged by R1/R2's diff lines), touches
  `assignment-runner.mjs:618-634` (attempt-number allocation), not part of
  Phase 01 R1/R2. Logged in index.md as a follow-up for whichever future
  phase next touches that function; does not block P01.1 close.
- Both "attempted, did not break" attacks (dirty-before capture-order gap;
  paths.mjs->worktree.mjs circularity) and both "verified, not broken"
  re-checks (safeGitHead never-throws; cwd-sourcing asymmetry) require no
  action — no code change, no disposition needed.

## Fix Evidence

RT-1 fix: added a second test to `test/runner/assignment-runresult.test.mjs`
alongside (not replacing) the existing nonzero-exit crash test —
`"executeAssignment captures gitBefore pre-launch when the worker commits
then the run rejects via worker-timeout"`. Its worker performs a real `git
commit` then hangs (`execFileSync('sleep', ['5'])`) past a short configured
`timeoutMs: 300`, so `cliSpawnAdapter` genuinely rejects with a
`DispatchError('worker-timeout', ...)` and `executeExecutorCli` itself
rejects — landing in `assignment-runner.mjs`'s own `catch (err)` block
(`rawResult` built with no `headBefore`/`headAfter` field), the exact branch
Red-Team proved the shipped test never reached. Asserts `gitBefore ===
shaBeforeRun`, `gitBefore !== gitAfter`, `evidence.gitBeforeSource ===
'pre-launch'`, checked both in the returned RunResult and in `evidence.json`
on disk — same shape as the existing test. A one-line comment above the new
test states why both tests exist (nonzero-exit resolves and proves
status/confidence classification; timeout rejects and proves the
gitBefore-timing fix — genuinely different code paths). The existing
nonzero-exit test is unchanged.

Revert-then-fail / restore-then-pass proof (mirrors Red-Team's own repro
shape, run for real this session, not reasoned about abstractly):
- Backed up `src/runner/dispatch/assignment-runner.mjs`
  (`md5sum` before: `ba8558239dd2de36387492969d7e03f1`).
- Reverted the R2 fix in place: moved `const gitBefore = safeGitHead(effectiveCwd);`
  out of its pre-launch position and restored the pre-diff line
  `const gitBefore = rawResult?.headBefore ?? safeGitHead(effectiveCwd);`
  computed after settle, alongside `gitAfter` (byte-for-byte the same revert
  Red-Team described) — `gitBeforeSource` left as the literal `'pre-launch'`
  so only the value-computation changed.
- Ran both tests against the reverted code
  (`node --test --test-name-pattern="Cell 6.7 G6|worker-timeout"
  test/runner/assignment-runresult.test.mjs`): the existing nonzero-exit test
  **still passed** (confirms Red-Team's vacuousness finding independently);
  the new timeout test **failed** —
  `AssertionError: gitBefore expected shaBeforeRun ('154c638f...'), got the
  post-commit HEAD ('58fbc3fb...')` — the exact G6 bug (gitBefore reading the
  post-crash HEAD instead of the pre-run one).
- Restored `assignment-runner.mjs` from the backup (`cp`), confirmed
  byte-identical via `diff` (no output) and `md5sum` (`ba8558239dd2de36387492969d7e03f1`,
  matches pre-revert). `git diff` against the pre-existing (uncommitted) R2
  changes on this branch shows exactly the original 25 insertions/3
  deletions, unchanged by this fix session.
- Re-ran both tests against the restored (fixed) code: both passed
  (`node --test --test-name-pattern="Cell 6.7 G6|worker-timeout"
  test/runner/assignment-runresult.test.mjs` — 2/2 pass).

Final required command:
```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/**/*.test.mjs' 'test/architecture.test.mjs'
# -> 1216 tests, 1215 pass, 1 fail: test/runner/herdr-spawn-adapter.test.mjs's
#    "(LIVE): dispatch a real agy-herdr interactiveMode executor against real
#    binaries" hit its own 60000ms worker-timeout against real binaries --
#    this is the pre-existing, known-flaky "herdr-live" gap the Review
#    section already names as staying open, unrelated to and untouched by
#    this fix (test/runner/assignment-runresult.test.mjs itself: 26/26 pass,
#    up from 25/25 -- the one new test).
```

## Coordinator Close Verification

Independently re-ran the revert/restore proof and final suite myself, not
trusting the Fixer's self-report:
- Backed up `assignment-runner.mjs`, reverted R2 in place (moved
  `gitBefore`'s capture back to post-settle, matching the pre-diff line),
  ran `--test-name-pattern="gitBefore pre-launch"` on
  `test/runner/assignment-runresult.test.mjs`: nonzero-exit test still green
  (vacuous, confirms RT-1 independently), timeout test red with
  `AssertionError [gitBefore: expected 6e1581c..., got 8be5795...]` — the
  post-commit HEAD, exactly the G6 bug.
- Restored the file, `git diff --stat` shows the original 25/-3 line diff,
  unchanged; re-ran the same pattern: 2/2 pass.
- Final: `FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/**/*.test.mjs'
  'test/architecture.test.mjs'` -> 1216 tests, 1215 pass, 1 fail
  (`herdr-spawn-adapter.test.mjs` LIVE test, the known open herdr-live gap,
  unrelated to this cell). No new failures beyond the recorded baseline;
  RT-1 closed with proof; RT-2 deferred and logged in index.md.

Close conditions (state machine H.3) all satisfied: every requirement (R1,
R2) maps to implementation and evidence; required positive/negative tests
pass, including the reshaped G6 negative test; no new failures beyond
baseline; no unresolved HIGH (RT-1 fixed and reverified); MEDIUM none; LOW
RT-2 explicitly deferred with rationale; independent Review and Red-Team
both occurred; no false-success/lifecycle/governance gap; no unexplained
out-of-scope changes (diff is exactly `assignment-runner.mjs`, `paths.mjs`,
`plan.mjs` re-export, and the two test files).

## Gaps

- `gitnexus` MCP tools were not present in this Doer's tool manifest (only
  the CLI `fgos tool query --capability impact-analysis --status present`
  was reachable, confirming gitnexus registered/present but its `impact()`/
  `context()` MCP calls were unavailable in this session). Substituted a
  manual grep-based blast-radius check for every symbol touched
  (`resolveContentRoot`, `readLockedContext`, `executeAssignment`,
  `evidence`/`evidenceData` shape) across `src/`, `bin/`, `domains/`, and
  `test/` before editing, plus a `git stash` diff-test of every baseline
  failure list to prove no regression. Not a scope gap in R1/R2 themselves,
  but the repo's own GitNexus "Always Do" gate could not be satisfied via
  its intended tool in this session — flagged for the coordinator/Review
  step rather than silently skipped.
- `evidence.gitBeforeSource` is always `'pre-launch'` under current code
  (no path exercises `'post-crash-fallback'`) — per the phase file's own
  instruction, the field/literal exists for future-proofing rather than a
  presently-reachable second branch. If Phase 02/03 later introduce a
  capture path that can genuinely fail before launch, that path must set
  `'post-crash-fallback'` explicitly rather than assuming the existing
  code covers it.
