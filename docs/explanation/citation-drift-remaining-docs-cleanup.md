---
authoritative_for: citation-format cleanup applied to the remaining docs/*.md files (tsk-2sp-5)
---

# Citation-format cleanup: the remaining `docs/*.md` files

`tsk-2sp-5` closed out `tsk-2sp`'s narrowed scope (306 findings across
the remaining non-skill baseline files, after `tsk-2sp-1`'s `work-state.md`
and `tsk-2sp-2`'s `runner.md`), applying the same fix contract: a
one-line gloss for `bare-citation` findings, delete-the-id (inlining
content where needed) for `d-local-outside-home` findings. See
`docs/how-to/fix-bare-citation-findings.md`,
`docs/how-to/fix-d-local-outside-home-findings.md`, and
`docs/explanation/citation-baseline-cleanup-calibration-slice.md` for the
technique and the reasoning behind the file/kind split. With `tsk-2sp-1`,
`tsk-2sp-2`, and this item, `tsk-2sp`'s entire narrowed scope (1019
findings, the 12 non-skill baseline files) is closed — the remaining
open scope (skill-file citations) belongs to `tsk-56w`, per
`docs/explanation/citation-drift-backlog-scope-split-with-tsk-56w.md`.
