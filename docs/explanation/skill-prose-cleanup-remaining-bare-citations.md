---
authoritative_for: remaining bare citations cleaned in fgos-routing/approve/pick skills (tsk-56w-9)
---

# The remaining bare-citation cleanup: `fgos-routing`, `approve`, `pick`

`tsk-56w-9` closed out `tsk-56w`'s third defect (see
`docs/explanation/skill-prose-cleanup-design.md`) for the skills not
already covered by the 7 skills split into `SKILL.md`/`references`
(`tsk-56w-1` through `tsk-56w-7`): `fgos-routing`, `approve`, and `pick`.
Every remaining bare governance-id citation in these skills was removed
per D1's product/shippable rule — the reason written directly into plain
prose instead. With this item, `tsk-56w`'s entire three-defect scope
(oversized skills split, CLI-wrapper boilerplate deduplicated, bare
citations removed) is closed across every fgOS skill.
