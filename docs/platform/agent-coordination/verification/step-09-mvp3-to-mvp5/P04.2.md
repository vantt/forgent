# P04.2 — Phase 04 (MVP5): R8 Dogfood Handoff (closes Phase 04 and the plan)

Cell: P04.2. Track: step-09-mvp3-to-mvp5. Base: 52a1db76. Docs-only cell —
no `src/`/`core/`/`test/` file touched. Closes Phase 04's R8, the plan's
last requirement.

## Requirements (verbatim)

- **R8 Dogfood handoff.** Produce a concrete handoff showing how MVP6+ can be
  started through the new runtime/surface path: input shape, command/surface,
  expected roles, expected artifacts, resume command, and what still remains
  outside coordination authority for source mutation.

## The handoff document

**Path:** `docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md`

**Why this path, not `verification/`.** The Must-Read/Files list treats the
handoff's exact permanent home as this cell's own call. Chose `playbooks/`
over `verification/step-09-mvp3-to-mvp5/` because the document's own nature
is operational — "how a maintainer starts MVP6+ dogfooding through the real
runtime" — not a proof trace of a closed cell. It is the direct sibling of
`playbooks/coordination-operating-harness.md` (the manual/human-driven
harness this document's own §0 states it replaces for the narrow slice that
fits the shipped fixture) and follows the same header convention used by
`architecture/coordination-foundation-baseline.md`/
`playbooks/coordination-operating-harness.md` ("Document type", "Design
status", "Implementation", "Last reviewed", "Canonical for", "Related").
Indexed in `playbooks/README.md` alongside the other two playbooks, with the
same one-paragraph description pattern already used there.

**Content summary (§ headers as written):**

1. **Input shape** — fixture id/version (`core.coordination-protocol.standalone-master-coordination-loop`, `1.0.0`), the `declared-protocol` request's `steps[]` types and $ref resolution (including the "no `$ref:` label survives a call boundary" rule), `actors[]` trusted per-actor policy, and the required top-level `writerId`.
2. **Command/surface** — `fgos coordination launch-master-loop` for starting a round (required first pass only, by design), `fgos coordination run --file` for every subsequent authorize/revise/recheck/disposition round against the same `coordinationId`.
3. **Expected roles** — Doer/Reviewer/Red-Team/Fixer table with fixture-declared `minTier` floors; explicit statement that Recheck is Reviewer's/Red-Team's own `phase-recheck` operations, not a fifth actor; the structural read-only guarantee for every role.
4. **Expected artifacts** — Assignment/Run/RunResult/evidence-ref shape reproduced from P04.1's own live counts; `show`'s rendered `authorizations`/`ignoredAuthorizations`/`dispositions`/`pendingDriverAuthorizations`; the explicit "recheck lineage is not a guaranteed edge" constraint.
5. **Resume command** — the exact `findExistingManifest` mechanism, the writerId-identity constraint (hard refusal on mismatch, citing the real HIGH-1 fix and its two independent recheck approvals), "no driver handoff exists" stated as a still-open limitation, and the terminal-absorbing behavior.
6. **What remains outside coordination authority** — the plan's own Non-Negotiable Boundaries text verbatim, R6's live measurement (no `.git`, no `.fgos/state.json` touched), the Coordination Foundation Baseline's Accepted Invariants, and a plain statement that applying/committing any resulting change is not this substrate's job.
7. **Future Expansion** — MVP6 (visibility windows) / MVP7 (aggregation rules) / MVP8 (deliberation memory) / MVP9 (dynamic specialist pull-in), sourced from the original proposal's own framing language (`docs/architect/proposals/step-09-group-thinking-substrate.md` lines 355-358), explicitly stated as not blocking MVP6+ dogfooding from starting.
8. **Still-open limitations** — driver-handoff/multi-writer resume, corrupted-session coverage scope, concurrent-resume-same-writer proof scope, `aggregateBounds` field-by-field test coverage, and the pre-existing session-wide-cap self-heal gap — carried forward, not silently dropped or presented as resolved.

## Proof Matrix (handoff claim → citation)

| Handoff claim | Citation |
|---|---|
| Fixture id/version, actors, six operations, graph shape | `core/coordination-protocols/standalone-master-coordination-loop.yaml:14-17,35-37,49-57`; frozen and cross-checked in `P00.1.md` §3 "Fixture id/version" |
| Request `steps[]` types, `$ref` resolution, no-cross-call-survival | `thin-launcher-surface-readiness.md` "Request shape a launcher would emit"; `P04.1.md` §"R4's live proof — two real CLI invocations" ("No `$ref:` label survives a call boundary") |
| `actors[]` trusted per-actor policy, escalation mechanism | `docs/how-to/run-a-coordination-session.md` "Trusted per-actor policy"; `P03.1.md` "Final Intended Mapping", "R4/R6" |
| `writerId` required, non-optional | `P04.1.md` "Red-Team Recheck" point 3 (schema.mjs `isNonEmptyString` gate, upstream of the resume identity check) |
| `launch-master-loop` composes required first pass only | `P02.1.md` §3 "Design Decisions Worth Recording" |
| `run --file` is the door for authorize/revise/recheck/disposition rounds | `P04.1.md` §"R1/R2 — the live scenario and how it's started" |
| Role → operation → minTier mapping; Recheck is not a 5th actor; structural read-only | `P03.1.md` "Final Intended Mapping" table, R1 finding #3; `standalone-master-coordination-loop.yaml:150-159` (phase-recheck node) |
| Assignment/Run/RunResult/evidence shape, `show` rendered fields | `P04.1.md` §"R3 — evidence path", §"`fgos coordination show`, after both calls (R7)"; `P02.2.md` §2 "Design Decisions" |
| Recheck lineage not a guaranteed edge | `P00.1.md` §3 "Artifact ref behavior"; `docs/how-to/run-a-coordination-session.md` "`fgos coordination show`" |
| Resume mechanism (`findExistingManifest`), zero new dispatch call sites | `src/verbs/coordination/run.mjs:130-146`; `P04.1.md` §"R4 — the resume gap, confirmed, and the fix" |
| writerId-identity constraint, HIGH-1 fix, both rechecks APPROVE | `src/verbs/coordination/run.mjs:139-142`; `P04.1.md` §"Red-Team Recheck", §"Reviewer Recheck" (both "Final verdict: APPROVE") |
| No driver handoff | `thin-launcher-surface-readiness.md` gap #4 |
| Terminal-absorbing across resume door | `P04.1.md` §"Terminal-absorbing across the resume door too (R5, resume-specific)" |
| No Work/git/worktree mutation (measured) | `P04.1.md` §"No Work / no git / no repo mutation, measured (R6)" |
| Non-Negotiable Boundaries text | `plans/260903-1049-step09-mvp3-to-mvp5/plan.md:94-96` |
| Accepted Invariants (Work is sole delivery authority) | `docs/architect/agent-coordination/architecture/coordination-foundation-baseline.md` "Accepted Invariants" |
| MVP6-9 framing language | `docs/architect/proposals/step-09-group-thinking-substrate.md:355-358` |
| Still-open limitations (driver handoff, corrupted-session coverage, concurrent-resume scope, aggregateBounds field coverage, self-heal-branch gap) | `P04.1.md` "Gaps" section (all four bullets); `P00.1.md` Carried-Forward Gap #4 |

Every claim in the handoff traces to a closed cell's own live-proved
evidence, never to this cell's own assertion. No aspirational capability is
described as already working.

## Docs staleness fixed in passing (docs-only, in scope)

While writing the handoff, found `docs/how-to/run-a-coordination-session.md`'s
`fgos coordination run --file` section described session opening only
("opens a session"), without stating the real, now-proven resume behavior
P04.1 added (`run --file` against an EXISTING `coordinationId` resumes
rather than refusing, subject to the writerId-identity gate). This is a
genuine documentation gap, not a source gap — the mechanism itself is
correct and already proven by P04.1; only this how-to doc's own prose was
stale. Fixed with one clarifying paragraph, cross-referencing the
`launch-master-loop` section below it (which already correctly described
"continuing... means hand-authoring a follow-up `run --file` request... see
'Request file shape' below" — that sentence was accurate; only the top
section lacked the explicit resume statement). No STOP was warranted — this
is a docs correction, not a source-code gap, and stays inside this cell's
docs-only scope.

## Commands

```bash
git diff --check -- \
  plans/260903-1049-step09-mvp3-to-mvp5 \
  docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5
# -> exit 0, no output
```

```bash
git status --porcelain -- docs/architect/agent-coordination/ docs/how-to/ plans/260903-1049-step09-mvp3-to-mvp5/
```
```
 M docs/architect/agent-coordination/playbooks/README.md
 M docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/current-cell.md
 M docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/index.md
 M docs/how-to/run-a-coordination-session.md
?? docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md
```

`current-cell.md`/`index.md` diffs are pre-existing Coordinator-owned state
from this cell's own dispatch — confirmed by `git diff --stat` on both
before this cell made any edit; this cell wrote zero bytes to either. This
cell's own diff is exactly: the new handoff file, the playbooks README
index entry, and the one-paragraph how-to correction. No `src/`, `core/`,
or `test/` file touched — confirmed by the same `git status --porcelain`
output above (no such path appears).

## Plan-Level Acceptance — final check across the WHOLE plan

Per this cell's own dispatch, confirming the plan's own closing criteria
(`plan.md` "Plan-Level Acceptance") are satisfied plan-wide now that P04.1
is closed, not merely re-asserting P04.2's own scope:

| Plan-Level Acceptance bullet | Satisfied by | Evidence |
|---|---|---|
| Full test suite passes relative to the baseline recorded before the first implementation cell | Every phase close (P01.1 through P04.1) reports the full suite matching the recorded baseline (`test/cli/fgos-intake-4.test.mjs:318`, the only known-unrelated failure) with no new failure, occasional non-reproducing subprocess-timing flakes explicitly re-run in isolation and confirmed clean | `index.md` Phase 00-04 Status sections, each cell's own "Full suite (final)" line |
| MVP3 preserves old RunResults/verdicts and records recheck as new Assignment lineage, not retry supersession | P01.1 (Phase 01, one cell, no split) | `index.md` "Phase 01 Status": "CLOSED... recheck as a genuinely new Assignment via a ROOT-CAUSE fix to the default taskKey derivation" (already closed by predecessor MVP1/MVP2 per handoff-first review; P01.1 accepted unchanged where already proven) |
| MVP3 disposition is a driver event with provenance and evidence refs; a worker cannot self-disposition final truth | P01.1 | `index.md` "Phase 01 Status": `recordDriverDisposition` + new `assertDispositionRefOwnedBySession` gate, 4 mutation-verified tests |
| MVP4 launcher assembles a request for a declared fixture and calls the coordination runtime; no group-thinking logic, hidden actors, hidden prompts, or bypasses | P02.1 + P02.2 | `index.md` "Phase 02 Status": "zero fork of engine logic — the launcher calls the existing `runCoordinationUseCase` door and nothing else"; `P02.1.md` Review + Red-Team both independently confirmed no forked schema/dispatch/evidence logic |
| Role execution policy explicit enough to avoid accidental all-lightweight execution: Doer/Fixer cheap by default, Reviewer/Red-Team/Recheck analytical/critical escalation | P03.1 | `index.md` "Phase 03 Status": `policy.minTier` on all 6 operations, Doer/Fixer `standard`, Reviewer/Red-Team/Recheck `analytical`, escalation to `critical` proven live by R8 dispatch tests |
| MVP5 live proof runs from a normal user-facing command path, not manual Master Prompt orchestration alone | P04.1 | `P04.1.md` §"R1/R2": started through `fgos coordination run --file`, the public request-file door, per R2's own escape clause |
| MVP5 proof can resume from persisted session state without duplicating assignments, reconsuming authorization keys, or losing disposition lineage | P04.1 | `P04.1.md` §"R4's live proof": 6 Assignments exactly once, 3 distinct invocationKeys, 2 dispositions in order, both independently re-verified by Reviewer and Red-Team recheck rounds |
| MVP5 leaves a documented dogfood handoff showing how MVP6+ can be started through the new runtime/surface path instead of the manual Master Prompt | **P04.2 (this cell)** | `docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md` |
| No Work item, Work status/stage, claim, return, approval, merge, branch management, tracked source mutation, or worktree delivery mutation created by this standalone proof | P04.1 (measured directly), consistent throughout P00.1-P04.1 | `P04.1.md` §"No Work / no git / no repo mutation, measured (R6)": `.git` never created, `.fgos/state.json`/event log identical before/after both calls |
| Existing Step 08 and MVP1/MVP2 behavior remains compatible | Every cell's own scope check | Zero diff to `session-engine.mjs`/`replay.mjs`/`schema.mjs`/`group-cognition-framework.yaml` across all cells; `store.mjs` carries P01.1's own additive `assertDispositionRefOwnedBySession` change (+53/-1, `P01.1.md` "## 6. Commands" `git diff --stat -- src test`), Reviewer-confirmed backward-compatible in that cell's own Review section — zero diff to `store.mjs` in every OTHER cell (P02.1, P02.2, P03.1, P04.1) |

**All ten Plan-Level Acceptance bullets are satisfied**, the last one by
this cell's own deliverable. No bullet found unsatisfied or silently
assumed.

**Non-Negotiable Boundaries — plan-wide, re-checked, not re-litigated:**
`group-cognition-framework.yaml` unchanged throughout (single commit
`833888ba` since Step 08, confirmed in `P00.1.md` §5 Entry Condition 4, and
zero diff reconfirmed in every later cell's own scope check); no autonomous
coordinator actor added to the fixture (still four worker-only actors,
`P00.1.md` §3, unchanged by P03.1's policy-only fixture edit); no Work/git/
coding-domain authority added (R6, measured); no skill/slash command became
a second coordination engine (`P02.1.md` Gaps: "No skill/slash command
file created ahead of schedule"); no playbook prose used as runtime logic
(this cell's own handoff doc explicitly inherits `playbooks/README.md`'s
"Runtime Boundary" — nothing under `playbooks/` is a runtime dependency);
this cell's own handoff explicitly disclaims code-mutation ownership (§6);
`addSessionEdge`/global mailbox/intent registry/Delphi/NGT/RFC/dynamic
specialist pull-in were never introduced anywhere in this plan — named only
as MVP8/MVP9 future expansion in this cell's own handoff, never built.

## Gaps

- **No genuine gap in P04.1's own proof was surfaced by writing this
  handoff.** The one thing that looked worth flagging on first read — the
  `run --file` how-to doc not describing resume — was a documentation
  staleness issue, not a defect in P04.1's shipped mechanism, and was fixed
  directly (see "Docs staleness fixed in passing" above) rather than
  triggering a STOP.
- **Driver handoff / multi-writer resume remains genuinely unbuilt** —
  named explicitly in the handoff's own "Still-open limitations" section,
  not hidden. MVP6+ dogfooding is scoped to single-operator-identity
  sessions until a driver-handoff design is accepted.
- **The handoff's Future Expansion section relies on the ORIGINAL
  proposal's own MVP6-9 framing language** (`step-09-group-thinking-substrate.md`),
  per this cell's own May-Inspect instruction ("for MVP6/7/8/9 framing
  language only, not as something already built") — confirmed none of the
  four (`addSessionEdge`, visibility-window schema, aggregation-rule
  schema, deliberation-memory schema) exist in this plan's shipped code;
  the handoff states this explicitly rather than implying partial
  implementation.

## Status

This closes P04.2, Phase 04 (MVP5), and the entire `step-09-mvp3-to-mvp5`
plan's scope. No further cell is dispatched by this Doer; Coordinator owns
the close verdict and `current-cell.md`/`index.md` updates.

## Red-Team (Independent)

Independent adversarial pass, run in parallel with (not coordinated with) the
Reviewer. Attack surface for this cell is the document's own honesty and
completeness, not code invariants — every claim in `mvp6-dogfood-handoff.md`
was independently traced back to its cited source before being accepted, and
the "Still-open limitations"/"What remains outside coordination authority"
sections were checked against the full gap inventory across P00.1-P04.1, not
just this cell's own restatement of it.

### Attack 1 — Overclaim hunt (§5 Resume command, §4 Expected artifacts):
attempted and held

Independently re-derived every load-bearing claim in §5 ("Resume command")
and §4 ("Expected artifacts") against `P04.1.md` directly (the `part1.json`/
`part2.json` two-call proof, the 6-Assignment/3-authorization/2-disposition
counts, the `writerId`-identity gate's exact refusal text, both independent
recheck "Final verdict: APPROVE" lines) and against live source
(`src/verbs/coordination/run.mjs`, `findExistingManifest`). Every citation
resolves to real evidence, none stronger than what was actually proven. The
handoff does not claim resume works for a different writer, does not claim
`show`'s recheck-lineage join is a hard edge, and does not claim the
`launch-master-loop` composer authorizes/dispositions anything — all
correctly scoped. **No overclaim found in §4/§5.**

### Attack 2 — Underclaim/gap-hiding hunt: HIGH finding

**The handoff's "Still-open limitations" section omits the
crash-bricked-`coordinationId` window — a gap this cell's own contract names
by name as a required inclusion.**

`current-cell.md`'s own Bug Taxonomy states: *"Treating a still-open
limitation (no driver handoff/multi-writer resume, the session-wide-caps
self-heal gap, **the crash-bricked-coordinationId window**) as solved
because MVP5 touched the same area — name what P04.1 actually closed vs.
what remains open."* This is one of exactly three named examples the cell
contract requires this handoff to get right. The first two (driver
handoff/multi-writer resume, session-wide-caps self-heal gap) are both
present in the handoff's "Still-open limitations" section. **The third is
absent — verified by direct search:** `grep -ni "crash\|bricked\|SIGKILL\|permanently unrunnable" docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md` returns zero matches anywhere in the document.

**The gap is real and still open, not stale.** `P00.1.md`'s Carried-Forward
Gap #15 (citing the MVP1/MVP2 predecessor, 12/40 real SIGKILLs reproduced):
"Crash between `mkdirSync` and `writeManifestRaw` permanently bricks a
`coordinationId`... directly relevant if MVP5's live proof uses a fixed
`coordinationId`... 'one badly-timed crash makes that exact file permanently
unrunnable in that workspace.'" `P04.1.md`'s own Red-Team section
("Broken/corrupted-session resume: attempted and held (with one cosmetic
note)") independently reproduced this EXACT shape against the new resume
door specifically: a session directory that exists with no `session.json`
(the crash shape) causes `findExistingManifest` to correctly report
`not-found`, which falls through to `openStandaloneSession`, which hits
`EEXIST` on the already-existing directory and throws `"coordination session
... already exists"` — uncaught, exit 4, no `session.json` ever written, no
recovery path. This is not a hypothetical: P04.1's own live proof used a
FIXED `coordinationId` (`coord_master_loop_live_standalone`) for its two-call
resume scenario, exactly the shape gap #15 warns is vulnerable.

**Why this matters for the handoff's actual audience.** A maintainer
dogfooding MVP6+ per this handoff's own §5 recipe opens a session with a
fixed `coordinationId` and expects to resume it across multiple CLI
invocations over time — exactly the workflow this document exists to enable.
If their process is killed (Ctrl-C, OOM, machine sleep, CI timeout) at the
unlucky moment between `mkdirSync` and `writeManifestRaw`, that specific
`coordinationId` is permanently unusable — not just for that round, forever,
in that workspace — and the only diagnostic they get is a generic "already
exists" error that gives no hint they should abandon the id and start over
under a new one. This is exactly the kind of operationally load-bearing
limitation a dogfooding maintainer needs named up front, and it is the
literal example this cell's own Bug Taxonomy was written to catch.

**Smallest fix direction.** Add one bullet to "Still-open limitations",
alongside the existing four, citing `P00.1.md` Carried-Forward Gap #15 and
`P04.1.md`'s Red-Team "Broken/corrupted-session resume" section: a crash
between session-directory creation and manifest write permanently bricks
that exact `coordinationId` (fails closed with a generic "already exists"
error, not a corruption, but also not recoverable) — a dogfooding maintainer
who hits this must pick a new `coordinationId` and cannot resume the bricked
one. Does not require reopening §5's core resume-mechanism claims, which
hold independently of this gap.

Everything else in the Still-open limitations / Future Expansion sections
was cross-checked against the full carried-forward gap inventory
(`P00.1.md` §4's 18-item table, `P04.1.md`'s own "Genuinely open after this
cell" list) and found present or correctly out-of-scope (items #1/#2/#5/#12
are pure forward notes not relevant to this fixture's actual wiring; #3b is
a narrower unlocked-reader replay edge not specific to the resume door this
handoff documents; #9/#14 were resolved by P01.1/P02.1 respectively). Only
#15 is a genuine, contract-named omission.

### Attack 3 — Authority-boundary violation hunt: attempted and held

Re-read §6 ("What remains outside coordination authority") against
`plan.md`'s Non-Negotiable Boundaries (verbatim quote matches exactly,
including the "existing external tooling or later Coding/Work authority"
clause) and against `coordination-foundation-baseline.md`'s "Accepted
Invariants" (direct read: "Work remains the sole delivery lifecycle
authority... Coordination may reference Work as read-only context, but
coordination verbs may not move Work status/stage, approve, merge, or
mutate Work lifecycle" — quoted accurately). No sentence anywhere in the
handoff implies coordination could or should eventually own source
mutation; §6's closing paragraph explicitly states a human or a separate
Coding/Work workflow still has to apply/commit/merge any resulting change,
"before or after MVP5." **No boundary violation, implicit or explicit.**

### Attack 4 — Plan-Level Acceptance re-check, re-attacked: MEDIUM finding

**The Plan-Level Acceptance table's "Existing Step 08 and MVP1/MVP2 behavior
remains compatible" row cites a "zero diff" claim that is directly
contradicted by P01.1's own recorded diff.**

The table's evidence column reads: *"Zero diff to
`session-engine.mjs`/`store.mjs`/`replay.mjs`/`schema.mjs`/
`group-cognition-framework.yaml` confirmed independently by Reviewer and/or
Red-Team in P01.1, P02.1, P02.2, P03.1, and P04.1."* Re-read `P01.1.md`'s own
"## 6. Commands" section directly:

```
git diff --stat -- src test
#  src/runner/coordination/store.mjs                          | 53 ++++++++-
#  test/runner/coordination-recheck-disposition.test.mjs       | 61 ++++++++
#  test/verbs/coordination-run-driver-steps.test.mjs            | 47 ++++++
#  3 files changed, 160 insertions(+), 1 deletion(-)
```

`store.mjs` carries a real, substantial +53/-1 diff in P01.1 — this is
`assertDispositionRefOwnedBySession`, P01.1's own R4 deliverable (adding the
disposition ref session-ownership check), not a zero-diff cell. The blanket
"zero diff to... store.mjs... confirmed... in P01.1" claim is false on its
face against the cell it cites as its own evidence — the same
self-certification pattern P00.1's own Red-Team round previously caught and
flagged (a confident claim stronger than the cited source supports).

**Does this change the bottom-line verdict?** No — re-checked independently:
`session-engine.mjs` genuinely has zero diff across P01.1-P04.1 (confirmed:
P01.1's own `git diff --stat -- src test` lists only `store.mjs`; P02.1/
P02.2/P03.1's own docs show no `session-engine.mjs`/`store.mjs` diff-stat
lines; P04.1's own scope table confirms zero diff to `session-engine.mjs`/
`store.mjs`/`replay.mjs`/`schema.mjs` for ITS OWN cell specifically). The
`store.mjs` change P01.1 made is additive (a new ownership-check helper, not
a modification of existing dispatch/disposition semantics) and was
independently Reviewer-confirmed in P01.1's own Review section as
backward-compatible. So the underlying compatibility claim likely still
holds — this is a citation-accuracy finding (the evidence text overclaims
"zero diff" when one of the five named files was NOT zero-diff in the one
cell that actually changed it), not a finding that Step 08/MVP1/MVP2
compatibility was actually broken.

**Smallest fix direction.** Reword the evidence cell to: "Zero diff to
`session-engine.mjs`/`replay.mjs`/`schema.mjs`/
`group-cognition-framework.yaml` across all cells; `store.mjs` carries
P01.1's own additive `assertDispositionRefOwnedBySession` change (+53/-1),
Reviewer-confirmed backward-compatible in that cell's own Review section —
zero diff to `store.mjs` in every OTHER cell (P02.1, P02.2, P03.1, P04.1)."

Every other Plan-Level Acceptance bullet spot-checked (MVP5 resume proof,
R6 no-Work/no-git measurement, MVP4 launcher "zero fork of engine logic")
was independently re-verified against the cited cell's own trace and held
without qualification.

### Attack 5 — Command accuracy, live-checked: attempted and held

Ran the actual CLI (not the handoff's own prose) against this repo's live
`src/cli/command-registry.mjs`:

```
$ node bin/fgos.mjs coordination launch-master-loop --help
... "launch-master-loop" ... given --plan/--objective/--writer-id it builds
a declared-protocol request covering ONLY the fixture's required first pass
(produce/review/red-team) ...
```

Cross-checked the actual parameter schema directly
(`src/cli/command-registry.mjs:750-757`): `plan`, `objective`, `writer-id`,
`coordination-id`, `fixture-version`, `executor`, `model`, `tier` are all
real, exactly-named parameters for `launch-master-loop`; `file` is the real
parameter for `run`. Every flag the handoff's §2 documents (`--plan`,
`--objective`, `--writer-id`, `--file`) matches the live registry exactly,
no renaming, no invented flag. **Held — no discrepancy.**

### Attack 6 — Scope escape: attempted and held

```
$ git status --porcelain -- src/ core/ test/
(empty)
```

No `src/`, `core/`, or `test/` file touched by this cell. Docs-only scope
held.

### Verdict: APPROVE WITH CONCERNS

The document's core mechanism claims (§1-§5, §6) are all independently
verified accurate and properly cited — no aspirational capability, no
authority-boundary blur, no command-shape drift. One HIGH finding (a
contract-named gap category — the crash-bricked-`coordinationId` window —
silently absent from "Still-open limitations," the exact trap this cell's
own Bug Taxonomy names by example) and one MEDIUM finding (a "zero diff"
overclaim in the whole-plan Plan-Level Acceptance re-check table, citation-
only, does not change the underlying compatibility verdict) should be fixed
before this cell — and with it, the entire `step-09-mvp3-to-mvp5` plan — is
treated as closed. Both fixes are small, additive, single-paragraph/bullet
edits to already-written prose; neither requires reopening any cited cell's
own substantive proof.

**Findings: 1 HIGH, 1 MEDIUM, 0 LOW.**

## Review (Reviewer)

Independent re-verification against real source and real cited traces, not
the Doer's prose. Scope confirmed docs-only first (`git status --porcelain
-- src/ core/ test/` empty except the untracked handoff/trace/report files
and pre-existing unrelated-session noise, matching the environment note).

### What was independently re-derived (not just cited)

- **§1 "`$ref` does not survive a call boundary."** Read
  `src/verbs/coordination/run.mjs` in full: `const labels =
  Object.create(null);` is declared fresh inside `runCoordinationUseCase`
  for both the `agent-led` branch (line 257) and the `declared-protocol`
  branch (line 314) — a new object per function call, never persisted
  across separate CLI invocations. The claim is true, not merely plausible.
- **§5 writerId-identity error message.** The handoff quotes the refusal
  message verbatim. Diffed byte-for-byte against the real throw in
  `run.mjs:139-143` (`findExistingManifest`) — exact match, including the
  parenthetical and the trailing clause. Citation `run.mjs:130-146` for the
  function and `run.mjs:139-142` for the constraint both land inside the
  real function body (130-146) and its throw (139-143) — accurate, off by
  at most one line on the closing brace, immaterial.
- **§4 Assignment/authorization/disposition counts.** Cross-checked against
  P04.1.md's own live-proof section ("R4's live proof — two real CLI
  invocations, same coordinationId"): 6 Assignments (`op_001`-`op_006`,
  `manifest.assignmentRefs.length === 6`), 3 `operation-authorized` events
  with 3 distinct `invocationKey`s, 2 `driver-disposition-recorded` events
  in order `['rejected', 'accepted']`. All three numbers in the handoff
  match P04.1's actual reported counts exactly, not rounded or restated
  loosely.
- **§5 "no driver handoff exists," §6 boundaries, Future Expansion sourcing.**
  Read `thin-launcher-surface-readiness.md`'s gap #4 directly — the
  handoff's quoted sentence ("That is a product decision to make before it
  becomes load-bearing, not a bug to route around") is verbatim. Read
  `plan.md`'s actual "Non-Negotiable Boundaries" section — the handoff's §6
  block-quote of "Do not claim Step 09 standalone coordination owns code
  mutation after MVP5..." matches the real plan text verbatim. Read
  `step-09-group-thinking-substrate.md` around lines 350-360 — rows 6-9 of
  that table are exactly Visibility windows / Aggregation rules /
  Deliberation memory / Dynamic specialist pull-in, i.e. MVP6-9, matching
  the handoff's Future Expansion section; `addSessionEdge` greps to zero
  hits in `src/` and `core/`, confirming the "framing language only, not
  built" claim. §6 does not overclaim or underclaim relative to the
  boundaries text — it restates the same non-code-mutation scope, no more,
  no less.
- **§3 role/tier mapping and structural read-only claim.** Cross-checked
  against P03.1.md's "Final Intended Mapping" table (Doer/Fixer `standard`,
  Reviewer/Red-Team `analytical` with critical escalation) and its R1
  finding #3 (`isReadOnlyMode: true`/`mutation: 'read-only'` unconditional
  for every operation via the one shared `runExecutorAttempt` call site) —
  both match the handoff's table and its "structural read-only" claim
  exactly.
- **Fixture id/version, actors, graph shape (§1, §3).** Read the actual
  `standalone-master-coordination-loop.yaml` header and `spec.actors`/
  `phase-recheck` node directly: `metadata.id`/`version` match, four
  worker-only actors match, the `phase-recheck` node's two
  `driver-authorized` bindings (`reviewer-recheck`, `red-team-recheck`)
  match the handoff's "Recheck is not a fifth actor" framing exactly.
- **Both "Final verdict: APPROVE" citations (§5).** Confirmed both actually
  exist in P04.1.md — "Red-Team Recheck" section ends "Final verdict:
  APPROVE" (line 1106) and "Reviewer Recheck" independently confirms the
  same fix (both call sites gated, missing/null/empty `writerId` fails
  closed one layer upstream via `schema.mjs:474-475`'s required
  `isNonEmptyString` check) — the handoff's "point 3" citation for
  `schema.mjs` lands on Red-Team Recheck's own numbered point 3, exactly.

### Findings

**MEDIUM-1 — Plan-Level Acceptance closing count is wrong (a miscount in
the exact check this cell was supposed to get right).** `plan.md`'s "Plan-Level
Acceptance" section (`plan.md:65-92`) has **10** bullets, not nine: (1) full
test suite, (2) MVP3 preserves RunResults, (3) MVP3 disposition, (4) MVP4
launcher, (5) role execution policy, (6) MVP5 live proof, (7) MVP5 resume,
(8) MVP5 dogfood handoff, (9) no Work/git mutation, (10) Step 08/MVP1/MVP2
compatibility — confirmed by direct count of `plan.md:65-92`. This cell's
own "Plan-Level Acceptance" table in this file correctly lists all **10**
rows with accurate mappings and citations (spot-checked 3 independently
above and 3 more by reading the table against `plan.md`'s real bullet text
— all accurate). But the summary sentence immediately below the table reads
"**All nine** Plan-Level Acceptance bullets are satisfied, the last one by
this cell's own deliverable" — undercounting by one. This is exactly the
failure mode this cell's own Bug Taxonomy and this review's brief both name
("treating a check as done because the table looks complete, without the
arithmetic actually being right"). Not a coverage gap — every real bullet
does have a row and a real citation — but the closing claim is factually
wrong on its own terms, in the plan's final closing document.
**Smallest fix:** change "nine" to "10" (or "ten") in the summary sentence;
no other change needed.

No other finding. No claim in the handoff document itself was found
aspirational, hedged-as-proven, or citing something that doesn't say what
it's claimed to say. §6's authority-boundary language does not blur or
overclaim source-mutation ownership. The writerId-identity constraint, the
`$ref` call-boundary limitation, the artifact/evidence counts, the role/tier
table, and the "no driver handoff" limitation are all independently
re-verified against the real, current source and the real cited traces, not
merely re-cited from the Doer's own prose.

### Verdict: APPROVE WITH CONCERNS

The handoff document itself (the real deliverable) is accurate, citable, and
free of aspirational claims — every spot-checked claim across all six
required sections plus Future Expansion and Still-open limitations holds
against the real source or the real cited trace. The one finding (MEDIUM-1)
is a one-word arithmetic error in this cell's own closing self-check, not a
defect in the handoff document that MVP6+ dogfooding will actually read and
rely on. Recommend the Coordinator either have this corrected before final
close or accept it explicitly as a scoped, trivial fix — either is
reasonable; it does not block treating Phase 04 and the plan as
substantively complete.

### Reviewer Recheck

Independent recheck pass against the actual current file content (not the
Fixer's own description), covering all three accepted findings (Red-Team's
HIGH crash-bricked-`coordinationId` omission, Red-Team's MEDIUM `store.mjs`
"zero diff" overclaim, and this Reviewer's own MEDIUM "nine"→"ten" miscount).

1. **Crash-bricked-`coordinationId` bullet (HIGH, Red-Team Attack 2) —
   CONFIRMED-RESOLVED.** Read the new 6th bullet in
   `mvp6-dogfood-handoff.md`'s "Still-open limitations" section directly
   (line 311 onward: "A crash between session-directory creation and
   manifest write permanently bricks that exact `coordinationId`..."). It
   correctly names `openSession`/`store.mjs` creating the session directory
   before `session.json`, the `EEXIST`/"already exists" fail-closed-not-
   recoverable behavior, and states plainly it is "not fixed or owned by
   any cell in this plan." Independently re-read `P00.1.md` line 203 (Gap
   #15) directly: "Crash between `mkdirSync` and `writeManifestRaw`
   permanently bricks a `coordinationId` (12/40 real SIGKILLs reproduced)
   ... directly relevant if MVP5's live proof uses a fixed
   `coordinationId`" — the bullet's description matches this cited source
   exactly, no overclaim or understatement. `grep -c "^- \*\*"` on the
   section confirms exactly 6 bullets now present (5 original + this one).
2. **`store.mjs` "zero diff" citation (MEDIUM, Red-Team Attack 4) —
   CONFIRMED-RESOLVED.** Read the corrected Plan-Level Acceptance row in
   this file directly: it now reads "Zero diff to
   `session-engine.mjs`/`replay.mjs`/`schema.mjs`/
   `group-cognition-framework.yaml` across all cells; `store.mjs` carries
   P01.1's own additive `assertDispositionRefOwnedBySession` change
   (+53/-1...) — zero diff to `store.mjs` in every OTHER cell." Independently
   re-read `P01.1.md`'s own "## 6. Commands" section directly (line 151):
   `git diff --stat -- src test` shows `src/runner/coordination/store.mjs |
   53 ++++++++-` — the +53/-1 figure in the corrected row matches the real
   cited diff-stat exactly. The row's underlying compatibility conclusion
   (Step 08/MVP1/MVP2 remain compatible) still holds given the corrected
   fact: the change is additive (a new ownership-check helper), independently
   Reviewer-confirmed backward-compatible in P01.1's own Review section, and
   `session-engine.mjs`/`replay.mjs`/`schema.mjs`/
   `group-cognition-framework.yaml` genuinely have zero diff across every
   cell — no reversal of the bottom-line verdict, only the citation was
   wrong and is now accurate.
3. **"All ten" count — CONFIRMED CORRECT.** Independently counted
   `plan.md`'s own "Plan-Level Acceptance" section directly (`plan.md:65-92`):
   10 bullets (full test suite; MVP3 preserves RunResults; MVP3 disposition;
   MVP4 launcher; role execution policy; MVP5 live proof; MVP5 resume; MVP5
   dogfood handoff; no Work/git mutation; Step 08/MVP1/MVP2 compatibility).
   This file's summary sentence now reads "**All ten** Plan-Level Acceptance
   bullets are satisfied" — matches the real count exactly.
4. **No new problem introduced.** Re-read the full `mvp6-dogfood-handoff.md`
   end to end after the edit: the new "Still-open limitations" bullet is
   consistent with §5's resume recipe (which assumes a fixed
   `coordinationId`, exactly the assumption the new bullet warns against),
   does not contradict any other section, and does not reintroduce a claim
   already flagged elsewhere. The corrected Plan-Level Acceptance row and
   the "All ten" sentence in `P04.2.md` read consistently with the rest of
   the Proof Matrix and Gaps section — no new inconsistency found.
5. **Scope — CONFIRMED.** `git status --porcelain -- src/ core/ test/`
   returns empty (re-run directly, not re-cited). `git diff --check --
   docs/architect/agent-coordination/playbooks/mvp6-dogfood-handoff.md
   docs/architect/agent-coordination/verification/step-09-mvp3-to-mvp5/P04.2.md`
   exits 0 with no output. Only the two docs files the Fixer named were
   touched.

**All three findings are CONFIRMED-RESOLVED** against the real, current file
content — not the Fixer's description of them. No new problem was
introduced by the fix pass.

**Final verdict: APPROVE.** With all three findings (1 HIGH, 2 MEDIUM)
independently confirmed fixed and no new issue surfaced, this cell — and
with it, Phase 04 and the entire `step-09-mvp3-to-mvp5` plan — is clear to
close.

### Red-Team Recheck

Independent recheck pass, re-attacking the Fixer's three docs-only edits
against the real current files (not the Fixer's own report prose), plus one
fresh full read of the whole handoff document.

**1. Crash-bricked-`coordinationId` bullet — re-verified against source.**
Read the new 6th "Still-open limitations" bullet in
`mvp6-dogfood-handoff.md` directly. Read `store.mjs` directly (not cited
secondhand): `openSession` (line 178) calls `fs.mkdirSync(sessionDir)`
(line 197) inside a `try`, catching `EEXIST` to throw `"coordination
session ... already exists"` (line 199) — strictly before `writeManifestRaw`
(line 249) ever runs. This is exactly the window the new bullet names:
directory created first, manifest written second, so a crash between them
leaves an unrecoverable, manifest-less directory. `P00.1.md` line 203 (Gap
#15) reads "Crash between `mkdirSync` and `writeManifestRaw` permanently
bricks a `coordinationId` (12/40 real SIGKILLs reproduced)" — matches the
new bullet's citation in substance. **HIGH-1: CONFIRMED-RESOLVED** — the
bullet accurately conveys the gap, correctly ties it to §5's
fixed-`coordinationId` resume recipe, and correctly states it is unfixed/
unowned by any cell. No other gap from this Red-Team's own original
cross-check (`P00.1.md` §4's 18-item table, `P04.1.md`'s "Genuinely open
after this cell" list) is missing from "Still-open limitations" — re-walked
the same inventory used in Attack 2 above and found no second omission.

**2. `store.mjs`/P01.1 citation — independently re-verified, accurate.**
Read `P01.1.md`'s own `## 6. Commands` `git diff --stat -- src test`
directly: `store.mjs | 53 ++++++++-`, matching the fixed wording's `+53/-1`
exactly. Independently confirmed `store.mjs` is zero-diff in every other
named cell by reading each cell's own text directly, not the Fixer's
summary: P03.1.md line 434 states `git diff HEAD -- store.mjs` is empty;
P04.1.md line 609 lists `store.mjs` among files confirmed zero-diff for
that cell; P02.1.md/P02.2.md both treat `store.mjs` as a read-only
Do-Not-Touch file they never edit (P02.2.md lines 65-67: "`store.mjs` is on
this cell's Do Not Touch list"). **MEDIUM-1 (Attack 4): CONFIRMED-RESOLVED**
— the new wording is accurate against every cell it names.

**3. "Ten" Plan-Level Acceptance count — independently recounted, correct.**
Read `plan.md:65-92`'s "Plan-Level Acceptance" section directly and counted
the bullets myself, independent of the Doer's table and both the Reviewer's
and Reviewer Recheck's counts: (1) full test suite, (2) MVP3 preserves
RunResults/verdicts, (3) MVP3 disposition is a driver event, (4) MVP4
launcher assembles a request, (5) role execution policy explicit, (6) MVP5
live proof from a normal command path, (7) MVP5 resume without
duplication, (8) MVP5 dogfood handoff, (9) no Work/git/mutation created,
(10) Step 08/MVP1/MVP2 compatible. **Ten, confirmed independently.**
**MEDIUM-2 (Reviewer): CONFIRMED-RESOLVED.**

**4. Fresh whole-document read — no new finding.** Read
`mvp6-dogfood-handoff.md` end to end as a stranger maintainer, past the
three fixed spots. Spot-checked two claims neither this Red-Team's original
pass nor the Reviewer had explicitly quote-verified: the terminal-absorption
error text in §5 (`P04.1.md:297` — `fgos: session "..." is not active
(status: "completed") -- cannot create an Assignment` — the handoff's
generic-`<id>` paraphrase matches exactly) and the `ignoredAuthorizations`
semantics in §4 ("an authorization written after the session already
closed — reported, never hidden") against `P02.2.md`'s own description
("`replay.mjs` already neutralizes-and-separately-reports a post-terminal
`operation-authorized` event") — accurate, no overclaim. Header metadata
(`Design status`, `Implementation`, `Last reviewed: 2026-09-03`) is
consistent with the doc's own content and today's date. No new overclaim,
underclaim, or authority-boundary issue found beyond the three already
fixed.

**5. Scope re-confirmed.** `git status --porcelain -- src/ core/ test/` —
empty. `git diff --check` on both touched files — exit 0, no output.

**Final verdict: APPROVE.** All three findings (HIGH-1, MEDIUM-1, MEDIUM-2)
are confirmed-resolved against live source, not merely against the Fixer's
own report. No new finding surfaced by a fresh full-document read or by
independently re-deriving the three fixed claims from scratch. This
Red-Team instance has no outstanding objection to closing P04.2, Phase 04,
and the `step-09-mvp3-to-mvp5` plan.
