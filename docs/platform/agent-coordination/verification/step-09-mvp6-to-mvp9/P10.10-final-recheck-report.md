# P10.10 Fix Round 1 — Final Independent Recheck

Reviewer: independent recheck agent (third round; follows the first-pass
Reviewer and Red-Team rounds, and the Fixer's Fix Round 1).
Date: 2026-09-04
Worktree: `/home/vantt/projects/forgentX/.claude/worktrees/step-09-mvp6-to-mvp9`
Scope: verify Fix Round 1 (§9 of `P10.10.md`) against the Coordinator's
disposition (§8), independently — probes and reads, not §9's narration.

**Verdict: APPROVE WITH CONCERNS.** Close-worthy. The central classification
decision and all nine dispositioned fixes are genuinely implemented and
correct. Three items to note; one (Finding A) I would close before the final
commit, but none blocks the classification decision or the track.

---

## 1. Item-by-item verification

Every row below was verified by reading the current file and/or running my own
probe. Nothing here is taken from §9's description of itself.

| # | Item | Result |
|---|------|--------|
| 1 | MEDIUM-1 / M1 — error preservation in both `catch` blocks | **Verified live.** See §2.1. |
| 2 | L1 — empty `anchors` normalization | **Verified live.** See §2.2. |
| 3 | L2 — the kernel-file touch (`src/runner/coordination/store.mjs`) | **Verified safe.** See §2.3. |
| 4 | MEDIUM-2 — corrected "Five bypasses" text | **Partially correct.** Real defect fixed; a new, milder inaccuracy introduced. See Finding B. |
| 5 | M3 — strike the "worsened" claim from §1c row #5 | **Verified.** See §2.4. |
| 6 | M2 — new §1c row #22 + decoy-test coupling comment | **Verified.** See §2.5. |
| 7 | §1d ruling applied to rows #10 / #14 / #15 / #20 | **Verified in the actual table.** See §2.6. |
| 8 | MEDIUM-3 — new positive Nominal-Group-Lite `rank` test | **Genuine, not tautological.** See §2.7. |
| 9 | MEDIUM-4 / LOW-5 / LOW-7 | **All three verified present and accurate.** See §2.8. |

---

## 2. Verification detail

### 2.1 MEDIUM-1 / M1 — original error genuinely preserved at both sites

Read the current `catch` blocks directly in `src/verbs/coordination/run.mjs`:

- `aggregationCloseParams`'s own load (~`:246`) — `catch (err)`, message appends
  `: ${err.message}`, and `wrapped.cause = err` before `throw wrapped`.
- The request-boundary `request.protocolRef.id` resolution (~`:378`) — same
  shape, thrown as `StoreError('validation', ...)`.

Both use post-construction property assignment rather than extending the
shared `CoordinationError`/`StoreError` constructors. `Error#cause` is a normal
writable property, so this is correct and keeps the blast radius at two sites
instead of two widely-shared error classes. Sound call.

**My own probe** (`probe-m1-error-cause.mjs`): a project-tier registry holding
one healthy protocol plus an unrelated malformed sibling `zz-broken.json`,
with `runCoordinationUseCase` asked for the *healthy* protocol id.

```
constructor      : StoreError | category: validation
message          : coordination request: protocol "probe.coordination-protocol.m1"
                   could not be resolved -- refusing the request rather than crashing
                   with an unresolvable-definition error: flow-definition: cannot parse
                   ".../zz-broken.json": Expected property name or '}' in JSON at position 2
cause defined    : true
cause constructor: FlowDefinitionError
names the REAL broken file in message: true
```

The failure is now correctly attributed to the actual broken file, not only to
the healthy protocol id the caller asked for. **Fix confirmed.**

Site 2 (`aggregationCloseParams`) is the identical pattern by read, and the
repo's own test drives it through `call2.closeRefusalReason`. I did not build a
separate live probe for site 2 because fix #1 runs first and would intercept a
corrupted-sibling scenario; the existing decoy-protocol test is the correct
isolation technique and it passes.

Note on the absolute path now appearing in the refusal message: not a data
exposure concern. This is a local CLI operating on a workspace the driver
already fully controls.

### 2.2 L1 — empty `anchors` normalization

Read `validateContributionStep` in `src/verbs/coordination/schema.mjs`:

```js
let anchors = step.anchors !== undefined ? validateStringArray(step.anchors, ...) : undefined;
if (anchors !== undefined) anchors.forEach((ref, j) => assertSafeId(ref, ...));
if (anchors !== undefined && anchors.length === 0) anchors = undefined;
```

Validation still runs on a supplied array before it is collapsed, so a
malformed non-empty `anchors` is still refused. Correct ordering.

**My own probe** (`probe-l1-anchors.mjs`), the exact idempotency scenario,
driven through the real pack gate (`runGroupThinkingRequest`, Nominal-Group-Lite,
three-call chain, real `ranking-open` window):

```
link1 (anchors: []) appended = true  | anchors = []
link2 (anchors omitted) appended = false | anchors = []
contributions on ledger: [ [ 'probe_c1', 'rank', undefined ] ]
L1 VERDICT: FIXED -- identical claim is a true idempotent no-op
```

A genuine no-op, not a `duplicate-ref` refusal. **Fix confirmed.**

**Additional probe the Fixer did not run** (`probe-l1b-anchors-immutability.mjs`)
— the obvious risk with a normalization like this is that it over-collapses and
weakens the immutability guarantee. It does not:

```
base link appended = true
anchored link appended = true
repeat with DIFFERENT (dropped) anchors -> duplicate-ref: recordContributionLink:
  contributionId "probe_c2" in session "probe-l1b" was already linked with
  different content -- a contribution link is immutable
ledger: [ [ 'anchor_base', undefined ], [ 'probe_c2', '["anchor_base"]' ] ]
L1b VERDICT: immutability check INTACT -- normalization did not weaken it
```

Linking `probe_c2` with `anchors: ['anchor_base']` and then repeating it with
`anchors` dropped is still correctly refused as `duplicate-ref`. The
normalization collapses only the two spellings of "no anchors"; it does not
make genuinely different content look identical. **The fix is correctly narrow.**

### 2.3 L2 — the kernel-file touch, given the most scrutiny

This is the one Fix Round 1 change under `src/runner/**`, so I re-derived its
safety rather than accepting §9's caller audit.

Signature now reads:

```js
function assertDriverIdentity(manifest, authorizedBy,
  { coordinationId, label, subject, fieldName = 'authorizedBy' }) {
```

I grepped every real call site in the repo myself (`grep -rn "assertDriverIdentity"
src/ core/ test/ bin/`). Exactly **five** call sites, all in `store.mjs`:

| Line | Function | `fieldName` | Behavior |
|------|----------|-------------|----------|
| 948 | `authorizeOperation` | *(not passed)* | default `'authorizedBy'` — unchanged |
| 1109 | `recordSpecialistAuthorization` | *(not passed)* | default `'authorizedBy'` — unchanged |
| 1244 | `recordDriverDisposition` | *(not passed)* | default `'authorizedBy'` — unchanged |
| 1425 | `recordContributionLink` | `'linkedBy'` | NEW override |
| 1558 | `recordAggregationValidation` | `'validatedBy'` | NEW override |

Every remaining `assertDriverIdentity` hit in the grep is a prose comment
reference, not a call.

**Tests asserting on the refusal message, checked individually:**

- `test/runner/coordination-driver-authorization.test.mjs:1545` — the only test
  pinned to the literal field-name prefix
  (`/authorizedBy\.id "coordinator-1" is not the driver identity/`). It drives
  `authorizeDeclaredOperation` → `authorizeOperation`, which keeps the default.
  **Unaffected**, and confirmed green (144/144 in the driver-identity suite).
- `test/runner/coordination-deliberation-ledger.test.mjs:226` — generic
  `/not the driver identity/`. Unaffected.
- `test/runner/coordination-aggregation.test.mjs:424` — generic
  `/is not the driver identity of session/`. Unaffected.
- `test/runner/coordination-recheck-disposition.test.mjs:461` — generic; drives
  `recordDriverDisposition`, default path. Unaffected.
- `test/runner/coordination-specialist-binding.test.mjs:240` — generic; default
  path. Unaffected.
- `test/verbs/coordination-run-driver-steps.test.mjs:814` — asserts on
  `run.mjs:143`'s own separate resume-writerId message, not on
  `assertDriverIdentity` at all. Unaffected.

**Docs quoting the message:** `P04.1.md:807` quotes
`fgos: authorizeOperation: authorizedBy.id "attacker" is not the...` — the
default path, still accurate. `P10.10-redteam-report.md:58/290` quote the
pre-fix `recordContributionLink: authorizedBy.id ...` message, but that is a
frozen evidence record of what was observed at probe time, not a live claim.

**Conclusion:** the kernel touch is genuinely mechanical, default-preserving,
and backward-compatible for every pre-existing caller. Fixing it rather than
deferring it was within §8's own "fix if isolated/cheap" authorization.

### 2.4 M3 — the "worsened" claim

`grep -n "worsened" P10.10.md` returns only lines 624, 630, 809 — all in the
§8/§9 narrative *describing* the correction. The word no longer appears in
§1c row #5 or §1d.

Row #5 now reads (in part): *"Its EXISTENCE, severity, and non-blocking
classification are unchanged by Phase 10 — but its REACHABILITY is not:
Red-Team's Probe D (P10.10 Fix Round 1) proved a project-tier shadow protocol
at the same `id@version` ... lets a forbidden contribution type land on the
immutable deliberation ledger THROUGH the new `contribution` step — a path only
reachable via a direct engine test-file call before this cell."*

I read Red-Team's Probe D (`P10.10-redteam-report.md:224-254`) and the
replacement is faithful to it: reachability increased, gap existence and
severity unchanged, classification unchanged. **Correction accurate.**

### 2.5 M2 — row #22 and the coupling comment

Row #22 exists in §1c (`P10.10.md:152`), classified "Implementation bug,
deferred — no live exploit today", correctly describing the base
`runCoordinationUseCase` door's missing resumed-session cross-check, noting
that only `group-thinking-pack.mjs` has it, and naming the test-isolation
coupling.

The coupling comment is present at
`test/verbs/coordination-aggregation-surface.test.mjs:706-713`:

> `// Coupling note: this isolation technique works ONLY because`
> `// runCoordinationUseCase's base door does not cross-check a resumed`
> `// session's CLAIMED protocolRef.id (here, the decoy) against its REAL`
> `// bound manifest.definitionRef.id -- only the group-thinking pack gate`
> `// has that cross-check. If a future change hardens run.mjs with the`
> `// pack gate's own cross-check, this test's own call 2 will be refused`
> `// before it ever reaches aggregationCloseParams, and this specific`
> `// isolation will need to be redesigned, not just re-asserted.`

Exactly what §8 asked for. **Both verified.**

### 2.6 §1d ruling actually applied to the table

Read the current §1c rows, not the disposition's description of them:

- **#10** (minTier provenance-source granularity) — "**Implementation bug,
  deferred** (re-classified from 'shared missing primitive' — §1d ruling,
  P10.10 Fix Round 1) — not blocking", with the display-vs-correctness
  reasoning inline. ✓
- **#14** ((operationId, nodeId) gating dedup collision) — "**Implementation
  bug, deferred**", same re-classification note, citing the bounded
  `actorGatingOperationIds` fix and zero live exposure across 11 shipped
  protocols. ✓
- **#15** (structurally-unopenable window) — "**Contract ambiguity,
  deferred**", noting the fix needs a product decision. ✓
- **#20** (R7 static-scan forbidden-name methodology) — "**Fixture
  convenience**", correctly tying it to the same substring-matching mechanism
  behind this track's own `coordination-static.test.mjs` false-fail. ✓

#5 is stated as the only remaining "shared missing primitive" in the sweep,
consistent with the ruling. **All four applied as ruled.**

### 2.7 MEDIUM-3 — the new Nominal-Group-Lite positive test

Read in full at
`test/verbs/coordination-group-thinking-nominal-group-lite-pack.test.mjs:288-407`.
It is genuinely real, not shallow:

- Three **separate** `runGroupThinkingRequest` calls, not one batched request.
- Real window gating — call 1 stops before `clarify`, call 2 opens
  `ranking-open` via a real `authorize` + `clarify` dispatch, then links
  participant-a's `rank`; call 3 finishes participants b and c.
- Asserts the session's `closed` state transitions `false → false → true`, so
  premature closure would fail the test rather than pass it silently.
- Cross-call `grantedContextRefs` correctly uses call 1's **literal**
  `share` assignmentId, not a `$ref:` (labels do not survive across separate
  pack-gate calls) — a real constraint, correctly handled.
- Reconstructs from `replaySession` alone: asserts three contributions, each
  `type: 'rank'`, and each `operationRef === 'private-rank'` — the last is the
  key non-tautological assertion, since `operationRef` is derived by the engine
  from the backing Assignment's stamp and is never accepted from the request.
- Re-asserts no-tally semantics (`replayed.aggregations` stays empty).

It matches the RFC-Review-Lite positive proof's shape as §8 required. **Ran it;
it passes** (part of the 87/87 below).

### 2.8 MEDIUM-4, LOW-5, LOW-7

- **MEDIUM-4** — §7 Gaps carries the new bullet naming the unrelated
  `coordination-store.test.mjs` concurrency flake (~50% under full parallel
  load, 5/5 clean in isolation, confirmed unrelated because that file imports
  nothing from `src/verbs/coordination/`), explicitly distinguished from the
  track's standing `fgos-intake-4.test.mjs` baseline. ✓
- **LOW-5** — §4 now reads "6 new unit-level request-boundary tests ... 1
  existing test updated". I reconciled this against the diff myself: 7 added
  `test(` blocks, 1 removed `test(` line (the renamed "four supported types" →
  "five supported types" test). 7 − 1 = 6 genuinely new + 1 updated.
  **Count is accurate.** ✓
- **LOW-7** — `coordination-session.md:733-734` discloses that the request
  boundary's `contributionId`/`anchors`/`respondsTo` charset (`assertSafeId`)
  is stricter than the engine's own `assertContributionIdShape` (which permits
  dots and colons), naming it as narrowing-only and a safe direction. ✓

---

## 3. Regression evidence — real numbers I personally observed

Re-ran every suite §9 claims, from this worktree, with
`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1`:

| Suite | Observed | §9 claimed | Match |
|---|---|---|---|
| Touched-file focused (6 files) | **tests 87 / pass 87 / fail 0** | 87/87/0 | ✓ |
| Driver-identity regression (L2's own callers) | **tests 144 / pass 144 / fail 0** | 144/144/0 | ✓ |
| Combined focused regression | **tests 742 / pass 741 / fail 1** | 742/741/1 | ✓ |
| Adjacent regression (deliberation/team-cognition/setup) | **tests 127 / pass 127 / fail 0** | 127/127/0 | ✓ |
| Full-repo sweep (excl. `coordination-static`) | **tests 5570 / pass 5562 / fail 1 / skipped 7** | 5570/5562/1/7 | ✓ |

**Every number reproduces exactly.** Both failures independently confirmed
pre-existing and unrelated:

1. Combined focused — `test/runner/coordination-static.test.mjs:61`. Actual
   assertion output: `cohort-planner.mjs imports "../dispatch/config.mjs"
   (resolved: /home/vantt/projects/forgentX/.claude/worktrees/step-09-.../src/runner/dispatch/config.mjs,
   matches forbidden "worktree")`. The resolved absolute path contains
   `worktrees`, which the naive substring scan matches against the forbidden
   token `worktree`. This is the documented path-dependent false-fail — and a
   live self-demonstration of §1c item #20's own methodology limitation.
2. Full-repo sweep — `test/cli/fgos-intake-4.test.mjs:318`, `seq: 3` vs
   expected `seq: 2` on an ask/answer round-trip. The track's standing
   baseline; unrelated to coordination by name, location, and content.

**Zero new regressions.**

---

## 4. Findings

### Finding A — all three Fix Round 1 code fixes shipped with zero regression coverage

*Severity: real, non-blocking for correctness. The item I would close first.*

Each of the three fixes is **correct** — I verified all three live. But none is
guarded by a test, so all three are silently revertible:

- **M1** — `grep '\.cause' test/verbs/coordination-*.test.mjs
  test/runner/coordination-*.test.mjs` returns **zero hits**. No test asserts
  `.cause` is set, and no test asserts the wrapped message names the real
  broken file. The two existing tests match only `/could not be resolved/`,
  which the Doer's original `catch {` already produced. **Reverting
  `catch (err)` → `catch {` and dropping `wrapped.cause = err` keeps the entire
  5570-test suite green.** The exact diagnosability regression this finding was
  raised about is now unguarded.
- **L1** — `grep -rn "anchors: \[\]" test/` returns **zero hits**. The existing
  normalization test at `coordination-run-driver-steps.test.mjs:367` uses a
  non-empty `anchors`. Reverting the normalization line keeps the suite green.
- **L2** — no test asserts `linkedBy.id` or `validatedBy.id` in the refusal
  message; the two relevant tests match only the generic
  `/not the driver identity/`. Reverting `fieldName` keeps the suite green.

This is one coherent gap against this repo's own definition of done
(`AGENTS.md` L5 question 5: *"new or changed behavior gets a matching test"*) —
a bar this track has otherwise held rigorously; the Doer's own round shipped 6
unit tests plus 2 end-to-end tests for its two fixes. §9's "No existing test
needed to change" is literally true, but it elides that no *new* assertion was
added either, which reads as though coverage were fine.

**Why non-blocking:** the fixes are correct today (independently probed); the
risk is future silent regression, not present incorrectness.

**Why worth closing anyway:** all four remedies are one-line edits to
assertions that already exist and already construct the exact scenarios:

| Fix | File | Edit |
|---|---|---|
| M1 | `test/verbs/coordination-aggregation-surface.test.mjs:633` | add `err.cause instanceof Error && /zz-broken-sibling\.json/.test(err.message)` to the existing predicate |
| L1 | `test/verbs/coordination-run-driver-steps.test.mjs:367` | assert `anchors: []` normalizes to `undefined` |
| L2 | `test/runner/coordination-deliberation-ledger.test.mjs:226` | tighten `/not the driver identity/` → `/linkedBy\.id .* is not the driver identity/` |
| L2 | `test/runner/coordination-aggregation.test.mjs:424` | tighten → `/validatedBy\.id .* is not the driver identity/` |

No new test files, no new fixtures.

### Finding B — the MEDIUM-2 correction fixes the real error but mislabels bypass #1

*Severity: documentation accuracy in a canonical contract doc; non-blocking.*

`docs/architect/agent-coordination/contracts/coordination-session.md:756-758`
now reads:

> *(`P10.1.md`'s own accurate framing: three bypasses refused because the
> vocabulary never exposes them, two — switch-protocols and bypass-grants —
> refused because upstream mediation already handles them.)*

The **actual MEDIUM-2 defect is genuinely fixed**: the doc no longer claims
bypass #2 (bypass grants) is refused because "the vocabulary never exposes it,"
and lines 750-756 now correctly describe `authorize` as exposed but mediated
through `authorizeDeclaredOperation`. That was the finding, and it is closed.

Two problems remain in the trailing parenthetical:

1. **`P10.1.md` never says "upstream mediation."** Its own wording
   (`P10.1.md:717`) is: *"two (switch-protocols, bypass-grants) because **this
   gate's own explicit-selection checks** run before, and add nothing to,
   `run.mjs`'s existing enforcement."* §9's claim that the correction quotes
   *"`P10.1.md`'s exact wording rather than paraphrasing"* is not accurate —
   this is a paraphrase that substitutes a different mechanism.
2. **For switch-protocols the substituted mechanism is wrong.** Per
   `P10.1.md` §5 #1, the refusals are `resolvePackProtocol`'s own check and
   `runGroupThinkingRequest`'s `protocolRef.id` agreement check — both live in
   `group-thinking-pack.mjs`, the pack gate itself, not upstream in `run.mjs`.
   More pointedly: the doc now asserts upstream mediation handles something
   **this same fix round's own new §1c row #22 says the upstream door
   explicitly does not do** — the base `runCoordinationUseCase` has no
   resumed-session protocol cross-check. The two artifacts contradict each
   other.

Note the paragraph's own body at lines 741-744 describes #1 **correctly** ("the
pack's own explicit-selection check plus, on resume, a cross-check against the
session's REAL bound protocol ... fixed by P10.1's own Fix Round 1"). Only the
summary parenthetical mislabels it.

**Remedy:** restore `P10.1.md`'s actual two-bucket wording — three refused
because `run.mjs`'s vocabulary never exposes them; two because the pack gate's
own explicit-selection checks run before, and add nothing to, `run.mjs`'s
existing enforcement.

### Finding C — worktree carries unrelated dirty state that would contaminate the closing commit

*Severity: housekeeping / closure mechanics. Not a P10.10 defect.*

- `docs/history/worker-prompt-iron-law-evidence-timing/plan.md` is **unmerged
  (`AA`) with live conflict markers on disk** — `<<<<<<< Updated upstream`
  (line 5), `=======` (line 18), `>>>>>>> Stashed changes` (line 19). Appears
  to be an unresolved stash-pop conflict from unrelated worker-prompt work.
- Two unrelated files are **already staged in the index**:
  `.claude/skills/fgos-coding-implement/references/implement-and-collaboration.md`
  and `.claude/skills/fgos-coding-implement/references/return-mechanics.md`.

Neither belongs to this cell. But a `git add -A` at closure would commit
literal conflict markers plus unrelated skill-doc edits into the track's
closing commit. **Resolve or set aside before committing.**

---

## 5. Fresh whole-diff look

`git diff --stat` across everything this cell has touched (Doer's original diff
plus Fix Round 1): 14 files, +1203 / −93.

- `src/runner/coordination/store.mjs` — 6 lines. The only `src/runner/**` file.
  Verified backward-compatible in §2.3. Zero files under `core/`. §9's
  kernel-boundary note is honest about this rather than leaving §3b/§6's
  "untouched" claim as a silent contradiction — good practice.
- `src/verbs/coordination/{run,schema}.mjs` — the two application-layer files
  the Doer originally scoped. Read in full; the new `contribution` branch adds
  no new trust (it forwards `driverIdentity`, never a caller-supplied
  `linkedBy`, enforced by `assertNoLinkedBy` at the schema layer) and reaches
  an already-proven mediated door.
- **No weakened assertions anywhere in the diff.** I checked every changed test
  predicate. The `coordination-launch-master-loop.test.mjs` update actually
  *tightened* its assertion, adding `err instanceof StoreError &&
  err.category === 'validation'` where the prior version matched a bare regex.
  This independently confirms both first-pass rounds' finding on that point.
- The new `coordination-run-driver-steps.test.mjs` tests are a real
  negative-space battery: `linkedBy` rejection, unknown-field rejection,
  per-field missing battery, closed-enum `contributionType` rejection,
  path-escape rejection, plus well-formed normalization.
- `flow-definition.md` (+13) and `step-09-group-thinking-substrate.md` (+61/−.)
  promotions read accurately and are appropriately hedged — the proposal's
  status line correctly distinguishes the implemented slice from the still-
  deferred items, and keeps "Canonical for: nothing until explicitly accepted;
  the implemented slice is canonical only through the contracts it was promoted
  into." That is the right posture.
- No plan-ID drift beyond the `P10.10 (...)` code comments already
  dispositioned under Reviewer LOW-6 as established precedent in these exact
  files. Not re-litigated.

No inconsistency found from the accumulated changes beyond Findings A and B.

---

## 6. Recommendation

Close **Finding A** (four one-line assertion edits, listed in the table above)
and **Finding B** (one sentence in `coordination-session.md`), clear **Finding
C**'s dirty worktree state, then this cell — and by extension the whole
`step-09-mvp6-to-mvp9` track — is ready for final closure.

Neither A nor B disturbs the central classification decision. I independently
confirm what both prior rounds concluded: the contribution-lineage finding is a
scoped implementation bug, not a shared missing primitive, and the "leave Step
09 open" consequence does not trigger. The §1d ruling is sound and was applied
faithfully to the table.

**Probe scripts** (re-runnable):
`/tmp/claude-1000/-home-vantt-projects-forgentX/c0828946-6a3e-4606-bc76-a0900fea1753/scratchpad/probe-l1-anchors.mjs`,
`probe-l1b-anchors-immutability.mjs`, `probe-m1-error-cause.mjs`.

## 7. Unresolved questions

None. Every §8 disposition item was located and verified; no finding required a
judgment call I could not resolve from the repo itself.

---

Status: DONE
Summary: APPROVE WITH CONCERNS — all nine Fix Round 1 items are genuinely
implemented and independently verified (three live probes, including one the
Fixer did not run confirming the anchors normalization did not weaken the
immutability check; all five regression numbers reproduce exactly at
87/144/742/127/5570), and the L2 kernel touch is confirmed backward-compatible
across all five `assertDriverIdentity` call sites. Two real but non-blocking
items: all three code fixes shipped with zero regression coverage (each
revertible without turning any test red), and the corrected "Five bypasses"
text mislabels switch-protocols as upstream-mediated, contradicting the cell's
own new §1c row #22.
Concerns/Blockers: Not blocking. Recommend four one-line test assertions and
one sentence correction before the closing commit. Separately, the worktree
carries an unrelated unmerged file with live conflict markers
(`docs/history/worker-prompt-iron-law-evidence-timing/plan.md`) plus two staged
unrelated skill-doc edits — clear these before `git add`.
