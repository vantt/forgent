# Cell P05.1 — Hard Conformance And Recovery Proof

Phase: `plans/260905-architecture-advisory-panel/phase-05-comparative-proof-and-promotion.md`
Status: **CLOSED.** Real live-proof cell, independently reviewed AND
red-teamed. The original claim ("all 6 hard-conformance areas hold") was
FALSE under adversarial testing — corrected honestly, per this track's
own standing discipline, rather than closed on the initial version. This
is the value this cell exists to produce, not a failure of it.

## Requirements

Exactly phase-05's P05.1 section: exercise the installed product path
(real CLI, not internal engine calls) against every authority,
visibility, aggregation, routing, bound, and replay claim; kill the
driver after meaningful durable progress and resume from a fresh process
with only the installed skill + session id.

## Implementation (Doer, `fullstack-developer`)

Built a real, isolated `WORK_DIR` (fgOS is cwd-strict — this never
touched the shared repo's own `.fgos/`), 4 real registered fake
executors, one coherent 14-op main session plus 3 dedicated attack
sessions, all driven through the real `fgos coordination run|show` CLI.
Centerpiece: a genuine `kill -9` of a real, separately-spawned dispatch
process mid-flight, confirmed dead via independent OS-level PID checks,
then a fresh process reading `fgos coordination show` alone and
correctly diagnosing the interrupted state — which itself surfaced a
real, disclosed gap (`tsk-47l`, initial version) in the recovery path.
Commit `9416298d`.

## Review (independent Reviewer, `code-reviewer`)

Re-verified all 6 areas independently in throwaway sandboxes rather than
reading the Doer's conclusions first. 5 of 6 reproduced exactly
(authority, visibility, routing provenance for the specific run tested,
bounds cap, aggregation). **Found the real minimum `maxRounds` for the
full flow including one reopen is 12, not 20** (informational). On Area
6, found the crash IS real (byte-equivalent on-disk state reproduced
independently) but the disclosure stopped one step short: **a second,
undisclosed per-cwd dispatch lock** (`src/runner/dispatch/cli.mjs`, real
repo `timeoutMs: 2100000` = 35 minutes) also survives SIGKILL, and the
documented "remove `dispatch.claim` and re-run" workaround, attempted
inside that window, **permanently poisons the assignment** rather than
recovering it. Also found 3 evidence gaps: a referenced-but-missing
crash-driver script, no committed `result.json` artifacts despite
phase-05 requiring them, and a "verbatim" transcript quote that was
actually synthesized (a `ps` line no real `ps` invocation produces).

## Red-Team (independent, `code-reviewer`, 18 live attacks)

**8 blocked as expected, 7 real bypasses (5 at HIGH severity), 3
unexpected.** This is where the cell's original claim broke down
substantively:

- **F1 (HIGH):** `findLatestRunResult` (`session-engine.mjs:262`) reads
  `runs/<n>/result.json` off disk with NO shape or authenticity
  validation and links it as genuine RunResult provenance. A
  hand-planted file claiming a different executor/provider/tier than
  was actually requested was accepted, exit 0, **zero executors
  spawned**.
- **F8:** every executor subprocess runs with `cwd = workspace` and can
  read/write EVERY OTHER assignment's directory, across roles and
  sessions — the same mechanism that enables F1's cross-boundary spoof.
  Together: the LEAST trusted actor in the system (a dispatched
  executor) can fabricate another assignment's provenance.
- **F5 (HIGH):** `revise-synthesis`'s documented precondition ("spendable
  once a human turn has been recorded") is prose only — dispatched with
  zero human turns and zero prior synthesis, skipping the entire
  intended graph sequencing. Only the `activation.maxInvocations` CAP is
  kernel-enforced; the precondition gating whether it may fire at all is
  not.
- **F6 (HIGH):** `aggregateBounds` declared on a RESUMED request is
  silently discarded in both directions — an under-budgeted session
  becomes permanently wedged with no CLI recovery door.
- **F2 (HIGH), F3 (HIGH), RT-14:** on Area 6, `dispatch.claim` is a
  zero-byte file recording no owner identity anywhere, so `tsk-47l`'s
  own original fix premise ("verify the owner is dead") isn't
  implementable as stated; a second lock surviving SIGKILL means the
  documented recovery workaround can permanently poison the assignment
  inside a real, deterministic 35-minute window (Red-Team's own
  self-correction: not incidental, unconditional per this repo's real
  config); and a SIGKILL during event-log append can make BOTH `show`
  and `run` exit 5, undiagnosable rather than merely unrecoverable.
- What genuinely held: all human-decision-slot bypass variants; window
  re-derivation defeating a forged authorization; the reopen cap under
  genuine 2-process concurrency (10/10, no race); no fencepost error in
  the round cap.

## Correction Round (honest rewrite, not a fix round)

Per this track's own standing discipline (a proof cell's job is to state
what actually held, not what was hoped to hold), the Doer rewrote
`doer-report.md`'s conclusion — not softened, not summarized away —
stating per area, with exact finding IDs, whether it held, held
narrowly, or broke: **3 of 6 areas (Routing, Bounds, Replay/crash-resume)
have a real, independently reproduced bypass of the claim as originally
stated.** Authority and Aggregation hold for their literal claims but
have adjacent gaps that don't (ledger-tampering via filesystem write
access; resumed-request bounds silently discarded). Visibility holds at
the prompt/context-grant layer but not the process layer. Fixed the 3
evidence gaps Reviewer found (committed the real crash-driver script and
real `result.json` artifacts; corrected the fabricated-verbatim
transcript). Commit `d34ced2e`.

**Real gaps filed as separate work items** (proof-cell scope — file,
don't fix, matching this track's own established pattern):
- `tsk-63z` (HIGH) — unauthenticated `result.json` trust, cross-boundary
  provenance spoofing by any executor subprocess.
- `tsk-3ru` (HIGH) — `revise-synthesis`/`revise-explanation`'s
  precondition is prose-only, not kernel-enforced.
- `tsk-1zk` (HIGH) — `aggregateBounds` silently discarded on resume.
- `tsk-47l` (HIGH, amended twice) — crash-recovery: no owner identity on
  `dispatch.claim`; a second lock with a deterministic 35-minute
  SIGKILL-survival window whose documented workaround permanently
  poisons the assignment; event-log corruption making a session
  undiagnosable.

**Carried forward to P05.2** (Reviewer's own note, not filed as a
separate item since it's explicitly out of P05.1's own scope):
`grantedContextRefs` accepts dangling/unvalidated refs — should be
checked when P05.2 exercises this same mechanism.

## Recheck (independent Reviewer + Red-Team, against the honest rewrite)

Both confirmed the rewrite accurately represents their own findings at
full severity — no softening, no downgrade, nothing dropped. **Red-Team's
own recheck produced an upward self-correction**: re-verifying the
rewrite's citation of the real repo config against their own report,
they found their original F2 characterization (window lasts "while the
orphan executor happens to still be running") was wrong for this repo —
the real window is a **deterministic 35 minutes**, unconditional, because
the underlying lock validity check has no PID-liveness probe at all,
only TTL freshness. Appended to `redteam-report.md` and folded into
`tsk-47l`'s final description.

**Coordinator independently verified throughout**: re-ran the focused
suite at every stage (757/757, exact match), personally confirmed the
`findLatestRunResult` code path lacks validation before accepting F1 as
real, personally confirmed the real `timeoutMs: 2100000` config value
before accepting Red-Team's correction, filed and cross-referenced all 4
work items, upgraded their risk/tier to `heavy` to match their confirmed
severity (originally auto-classified `light`).

## Tests

`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/cli/coordination.test.mjs' 'test/architecture.test.mjs'`:
757/757 throughout (no kernel/protocol source file was touched — this
cell is pure evidence-gathering, per its own scope; every found gap was
filed, not fixed, matching this track's established pattern). Full
`npm test`: at the track's own recorded baseline throughout.

## Changed Paths

- `docs/architect/agent-coordination/verification/architecture-advisory-panel/proofs/P05.1/**` (new: requests, responses, session-events, executors, human, run-results, doer-report.md, crash-proof-log.txt, crash-state-snapshot.md, reviewer/red-team findings+reports)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/P05.1.md` (this file)
- No `src/` file touched.

## Commit

2 commits total on `group-thinking-plan-loop`: `9416298d` (initial
proof), `d34ced2e` (honest rewrite after Review + Red-Team) — plus this
closing report's own commit (pending).
