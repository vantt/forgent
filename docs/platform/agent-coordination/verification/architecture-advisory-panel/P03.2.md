# Cell P03.2 — Protocol And Artifact Envelope (architecture-advisory-panel-v1)

Phase: `plans/260905-architecture-advisory-panel/phase-03-minimal-hard-shell-and-protocol.md`
Status: **CLOSED.** Real protocol registration, independently reviewed
across 1 fix round, both rounds independently re-verified by the
Coordinator. All findings resolved (0 HIGH, 3 MEDIUM fixed, 3 LOW + 2
informational accepted as-is per Coordinator scope decision).

## Requirements

Exactly phase-03's P03.2 section: define and register
`core.coordination-protocol.architecture-advisory-panel-v1` with the
minimum graph for independent work, controlled reveal, typed
deliberation, synthesis, recheck, specialists, park/resume, and bounded
dialogue reopen — built on P03.1's human-turn door. The protocol declares
legality and collaboration posture only; it must not duplicate the role
doctrine and task prose Phase 04 projects.

## Implementation (Doer, `fullstack-developer`)

Built `core/coordination-protocols/architecture-advisory-panel-v1.yaml`
(the minimum graph for the 9-role panel: independent framing/shaping,
window-gated critique→synthesis→red-team→explanation via visibility
windows on driver-authorized bindings, matching
`independent-research-fan-out-fan-in-gated.yaml`'s own pattern; a
specialist slot reusing the existing specialist-authorization mechanism;
a bounded driver-authorized dialogue-reopen with `activation.maxInvocations: 2`
caps), registered it as the group-thinking pack's 5th member, and wrote
`test/verbs/coordination-architecture-advisory-panel-conformance.test.mjs`
(12 tests covering all of phase-03's named scenarios: premature reveal,
foreign refs, hidden dissent, missing actors, unauthorized specialist,
over-cap reopen, wrong recheck revision, terminal mutation, Work
isolation, crash/replay, CLI/headless parity, human-authority
impersonation, heterogeneous actor/tier provenance with a real
distinct-model assertion). 3 commits: `faff2fc1`, `2a3ba803`, `df563359`.

**A real design bug found and fixed during construction, not assumed up
front:** per the kernel's own multi-operation quorum rule, the
deliberately unwindowed `revise-synthesis`/`revise-explanation` bindings
don't gate quorum — without a fix, the session would auto-close the
instant `explain-recommendation` settles, before a genuinely later human
turn could ever arrive. Fixed with `close-dialogue`, a third gating
binding for the lead-advisor actor satisfiable only by the driver's own
explicit authorization — the graph's answer to "who decides the dialogue
is over."

**Two pre-existing request-schema gaps found and disclosed, not fixed**
(per phase-03's own "no speculative kernel work" instruction for this
cell — P03.1 already used this phase's kernel-change budget): (1)
`assertSafeRefOrId`'s charset check refuses ANY `contribution:`/
`human-turn:`-prefixed ref in `grantedContextRefs`, disposition
`targetRef`, or disposition `evidenceRefs`, for every protocol, even
though the store layer fully supports both namespaces — forced the
dialogue-reopen design onto free-text `reason` citation instead of a real
ref. (2) No `specialist-authorize` request-step type exists for
`fgos coordination run`. Both filed as separate work items: `tsk-44p`,
`tsk-3xk`.

Focused suite: 728 (P03.1's own end state) → 756. Full RFC/NGT/Delphi/
master-loop conformance tests re-run and confirmed unregressed, per
phase-03's own explicit requirement for this cell.

## Review (independent Reviewer, `code-reviewer`)

Read the spec, code, and tests cold. **8 findings: 0 HIGH, 3 MEDIUM, 3
LOW, 2 informational.**

- Verified, from source, BEFORE reading the Doer's report: the
  `close-dialogue` bug claim (re-derived independently from
  `session-engine.mjs`'s actual quorum-gating logic — confirmed real,
  confirmed correctly bounded, confirmed test-proven in the negative
  direction), and both disclosed gaps (found the charset restriction is
  actually broader than disclosed — it blocks both namespaces across
  all three ref fields, not just the one the Doer's report emphasized).
- Minimum-graph discipline: PASS — no advisory heuristic encoded as an
  enum or transition anywhere.
- Portability: PASS — `policy.minTier` is the only literal lever in 527
  lines; no executor/provider/model pin found.
- No status-enum widening: PASS structurally — the diff touches zero
  files under `src/`.
- **MEDIUM (R2, the one that mattered most):** `revise-explanation` had
  ZERO test coverage — `grep -rn "revise-explanation" test/` returned
  nothing. Provably deletable with no test failure: as an ungated
  driver-authorized binding it gated no quorum, appeared in no window,
  and was inert to every other derivation. Half of "bounded dialogue
  reopen," including its own `maxInvocations: 2` cap, was unproven.
- **MEDIUM (R1):** the shipped YAML's own header still told a driver a
  `disposition` step "may cite `human-turn:<turnId>` today" — refused at
  the charset check the Doer's own test file and report already
  correctly described. Stale prose in the artifact Phase 04 will
  actually read.
- **MEDIUM (R3):** two rejection predicates asserted only
  `err instanceof CoordinationError` where every other test in the file
  pinned a specific message; the specialist one's negative arm supplied
  neither precondition, unable to isolate its own named guarantee.
- Reviewer explicitly looked for a phantom test matching the rigor P03.1
  set and found exactly one (R2) — the same discipline held here as in
  the kernel cell.

## Fix Round 1

All 3 MEDIUM findings addressed in 2 commits (`556e5bf9` fix, `38bea45e`
docs). R2: added a full mirror of `revise-synthesis`'s own over-cap test
for `revise-explanation` — proven non-phantom by temporarily deleting its
`maxInvocations: 2` cap, confirming the new test alone failed (12/13),
then restoring byte-identical. R1: corrected the YAML header to
accurately describe the charset refusal's real scope (both namespaces,
all three ref fields, every protocol) and cite `tsk-44p`/`tsk-3xk` as
filed gaps rather than framing the refused path as an intended step. R3:
one disclosed, verified deviation from the literal recommendation — the
specialist negative arm's "authorize the operation but not the slot"
isn't achievable, since `authorizeDeclaredOperation` resolves the actor
through the same function `dispatch` uses and throws the identical
unbound-slot error at authorize time itself; pinned the specific message
instead, which achieves the same isolation goal through the mechanism the
code actually supports. Two Coordinator-answered open questions from
Reviewer: keep `revise-explanation` in V1 (real need, evidenced by
P01.2/P01.3's own actual dialogue turns needing to revise both synthesis
and explanation); no separate blocker-list entry needed for the disclosed
gaps beyond the already-filed `tsk-44p`/`tsk-3xk`.

Focused suite: 756 → 757 (+1, the new `revise-explanation` test).

## Recheck (independent Reviewer, against the fix)

Re-derived every verdict from source/re-execution, not the fix-round
report. **3/3 fixed, 0 not fixed, 0 partial.** Independently reproduced
the Doer's own falsification check for R2 (deleted the cap, confirmed
exactly 1/13 tests failed for the right reason, restored byte-identical —
md5-verified). Confirmed R1's corrected header matches the code exactly.
Independently verified R3's disclosed deviation by reading
`resolveDeclaredOperationActor`'s real call sites — confirmed the Doer's
technical reason was correct, accepted the substitute approach.

**Coordinator independently verified before accepting**: re-ran the
focused suite at every stage (756 → 757 → 757, exact match each time).

## Real Gaps Found (filed, out of this cell's scope)

Two request-schema gaps found while building the conformance suite,
filed as separate work items rather than fixed here (per phase-03's
explicit "no speculative kernel work" for this cell):

- `tsk-44p` — `assertSafeRefOrId`'s charset check refuses
  `contribution:`/`human-turn:`-prefixed refs in every ref field, for
  every protocol, even though the store layer already supports both.
- `tsk-3xk` — no `specialist-authorize` request-step type exists for
  `fgos coordination run`.

## Tests

`FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/runner/coordination-*.test.mjs' 'test/verbs/coordination-*.test.mjs' 'test/cli/coordination.test.mjs' 'test/architecture.test.mjs'`:
728 (P03.1's end state) → 756 (Doer) → **757 (final, all passing)**.
Existing RFC/NGT/Delphi/master-loop conformance tests re-run individually
and confirmed unregressed at every stage, per phase-03's explicit
requirement.

## Changed Paths

- `core/coordination-protocols/architecture-advisory-panel-v1.yaml` (new)
- `core/protocol-packs/group-thinking.json` (5th member added)
- `test/verbs/coordination-architecture-advisory-panel-conformance.test.mjs` (new)
- `test/verbs/coordination-group-thinking-pack-registration.test.mjs`
  (membership assertions tightened, not loosened)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/proofs/P03.2/**` (new)
- `docs/architect/agent-coordination/verification/architecture-advisory-panel/P03.2.md` (this file)

## Commit

5 commits total on `group-thinking-plan-loop`: `faff2fc1`, `2a3ba803`,
`df563359` (initial implementation), `556e5bf9`, `38bea45e` (fix round 1)
— plus this closing report's own commit (pending).
