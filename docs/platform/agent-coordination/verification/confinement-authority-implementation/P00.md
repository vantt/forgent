# P00 — Contract Freeze And Repo Map

Cell: `confinement-authority-implementation--p00` (git branch)
Role: Lead-authored artifact (review-only, `code:review`; no Doer per
phase-00-contract-freeze-and-repo-map.md R6)
Date: 2026-09-10

## Coordination sessions

Three, in sequence — the reason is a plumbing bug and a design gap this
cell's own execution discovered, both recorded here so a later review-only
cell does not rediscover them from scratch:

| Session | Outcome | Why it ended |
|---|---|---|
| `confinement-authority-implementation--p00` | Dead, never closed | Opened without `partialPolicy`. `doer`/`fixer` stay required `SessionActor`s per the FlowDefinition regardless of a request's own `actors[]`; a review-only cell with no Doer/Fixer dispatch can never satisfy default quorum, and `partialPolicy` is immutable once a session opens without it. Its two real dispatches (`op_003` reviewer, `op_004` red-team, both `status: done`) are real, substantive review passes — their findings were folded into the report directly rather than replayed. |
| `confinement-authority-implementation--p00b` | `partially-complete` / `closed: true` | Reopened the same cell with `partialPolicy: {allowedOmissions: ["doer","fixer"]}`. Reviewer (`op_005`) found 1 HIGH: the report over-corrected a prior LOW finding into a false claim that the 5 `herdr-spawn` executors get real confinement enforcement today. Red-team (`op_006`) found a second HIGH: `session-engine.mjs` misclassified comment-only when it is a real door-#2 caller via `executeAssignment`. Both applied directly (commit `be546194`). |
| `confinement-authority-implementation--p00c` | `partially-complete` / `closed: true` | Closing recheck. Reviewer (`op_007`) confirmed both `p00b` HIGH fixes correct, found 1 MEDIUM (a self-contradicting parenthetical) + 2 LOW (four line-ref off-by-ones; a missing `codex-readonly` clause). Red-team (`op_008`) found nothing. All three applied directly (commit `0f6b0303`), no HIGH remained, no further dispatch needed. |

**Structural note for the Lead at a future review-only cell:** before opening
a `code:review` cell's coordination session, declare `partialPolicy:
{allowedOmissions: ["doer","fixer"]}` in the very first `open.json` —
`fgos-plan-loop` SKILL.md section 5 step 2 does not currently say this, and
the omission cost one dead session here.

**Blocking bug found and fixed mid-cell (unrelated to P00's own scope,
necessary to unblock this track's herdr-backed roster entirely):**
`src/runner/dispatch/brief.mjs` instructed every herdr-spawn worker to
claim `"status": "settled"`, a value `validateAgentResultClaim`
(`assignment.mjs`) never accepts (`done`/`blocked`/`failed`/`no-evidence`
only) — every dispatch under this track's roster (100% `*-herdr`
executors) would have failed schema validation regardless of actual work
quality. Fixed on `main` at commit `250a3edf`, rebased into the track and
this cell's branch before the first real dispatch. Focused suite green
(1728/1728) before use.

## Requirements proof (phase-00-contract-freeze-and-repo-map.md)

| Req | Evidence | Status |
|---|---|---|
| R1 | Report §R1, sourced from `docs/specs/confinement-authority.md` §9/§9.1; verified by 3 independent review passes | satisfied |
| R2 | Report §R2 call-site inventory table, 495 matches / 42 files (`src`+`test`); verified/corrected across 3 rounds (adapter completeness, `resolve.mjs`, `session-engine.mjs`, exit-point line refs) | satisfied |
| R3 | Report §R3 classification rationale | satisfied |
| R4 | Report §R4 doc-index impact table, `reading-map.md` gap verified live (`rg -n "confinement" docs/specs/reading-map.md` → 0 matches) | satisfied |
| R5 | Report present under `plans/260910-1243-confinement-authority-implementation/reports/`; risk classification, lease cross-check, focused test list all present | satisfied |
| R6 | `git diff --stat confinement-authority-implementation...HEAD` → 1 file (the report), 103 insertions, 0 deletions; no `src/**`, `test/**`, or `.fgos/config.json` change across any of the 4 commits on this branch | satisfied |

## Deferred / open (not blocking, named for the Lead at each named phase's own open)

- `http`/`httpAdapter` scope decision — resolve at P02 open.
- `dispatch.mjs`'s `EXECUTOR_ADAPTERS`/`DEFAULT_ADAPTER`/`decideExecutorCli`/`fanoutBatchExecutorCli` re-export — P02 either closes it or explicitly scopes its "no direct adapter call" gate around it.
- `src/runner/loop.mjs` lease gap (door #1's production caller, no named bucket) — resolve at P02 open.
- Two lease-glob overlaps (`test/runner/dispatch-executor-profile.test.mjs`, `test/runner/dispatch-confinement*.test.mjs`) — inert under `MAX_PARALLEL_CELLS: 1`, matters only if the Lead splits a cell.
- `docs/specs/distribution.md` appears in two lease buckets (`config-registry`, `docs-closeout`) — same, inert today.
- Phase file's own Verification grep (`phase-00-contract-freeze-and-repo-map.md:50`) is 5 terms, misses `cliSpawnAdapter`/`herdrSpawnAdapter`/`herdrSpawnInteractiveAdapter` — outside P00's own May-touch list, named for whoever next edits that phase file.
- Spec §2's stale `0/17` denominator (`docs/specs/confinement-authority.md:59`, should read `0/18`) — substance correct, only the count is stale; outside P00's lease, a P07 (docs-closeout) item.
- `codex-readonly`'s retired-but-registered `-s read-only` sandbox as a non-bwrap, non-declared enforcement surface — P04 should account for or explicitly exclude it.

## Tests

Doc-only cell (R6); no `src/**`/`test/**` change, so `FOCUSED_DISPATCH_TEST`
was not re-run for this cell's own diff. It was run once, mid-cell, to
verify the `brief.mjs` dispatch fix (1728/1728 pass, see above) — that
fix landed on `main` before this cell's first real dispatch, not as part
of this cell's own commits.

## Merge

`git merge --no-ff confinement-authority-implementation--p00` into
`confinement-authority-implementation` — see `plan.md`'s cell-status table
for the merge commit.
