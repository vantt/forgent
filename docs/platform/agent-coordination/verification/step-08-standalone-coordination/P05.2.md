# Cell P05.2 — Group Cognition framework live proof (Phase 05 R5-R8)

## Requirements

Phase 05 R5-R8 (`plans/260901-1542-step08-standalone-coordination/phase-05-group-cognition-framework.md`):
R5 external case lock, R6 single-agent baseline, R7 live heterogeneous
framework proof, R8 independent quality report. R1-R4 (framework
definition/policy/independence/synthesis contract) closed in P05.1.

## R5 — External case lock

Frozen and committed separately, before any output was observed, per
this track's own bias-avoidance discipline: `proofs/P05.2/case-lock.md`
(commit `7910fc22`). Case: mdview's "new, separate editor screen (never
inline editing)" question, frozen at mdview commit `84a6710a`. Two
candidates scouted and rejected first (forgentX itself — excluded by
D-ADR0035 and R5's own "outside this repository" text; herdr-gateway's
terminal-scrolling question — excluded as already extensively
pre-analyzed, defeating R5's "genuinely unobserved" purpose). Selected
and confirmed directly with the maintainer, per this track's "Scout
First" doctrine (business-judgment call reserved to the Coordinator/user).

Config gap fixed before dispatch, also committed separately (`c852814d`):
`.fgos/config.json` lacked `standard`/`creative`/`analytical`/`critical`
tier entries for `gemini`/`openai-codex`, which R7's own tier floors
require. Fixed with real, CLI-verified model identifiers (`agy models`
output), never guessed.

## R6/R7 — Doer's live execution

Dispatched to a fresh Doer (fullstack-developer) with the full brief in
`current-cell.md`. Full real subprocess dispatch throughout — no mocks,
no stubs, no fabricated output.

### R6 (single-agent baseline)

`openStandaloneSession` + `dispatchPrimaryTask`, `primaryRole: 'advisor'`,
`cliOverride: {minTier:'critical', preferExecutor:'claude'}` (critical
tier chosen to match R7's own synthesis ceiling, so the baseline isn't
reasoning-starved relative to the framework). Real executor resolved to
`claude-reviewer` (pre-existing read-only redirect in
`assignment-runner.mjs`, not a bug). Settled `status:failed` /
`confidence:failed` — cause independently confirmed on disk
(`r6-single-agent-baseline/run.log`): `agent-result.json was present but
failed schema validation` (model wrote a richer/differently-shaped JSON
than the schema expects), a FORMAT failure, not a content failure. The
real `agent-report-attempt-01.md` is a complete architectural answer:
simplicity judgment, one ranked-with-runner-up top risk, 7 ordered
decision criteria, 6 distinct alternatives with accept/reject reasoning,
9 explicitly self-flagged unsupported assumptions. Wall time 224,213 ms,
0 retries (`budget.maxRuns:1`), cost `"unknown"` (not surfaced by
RunResult, not guessed).

### R7 (live 6-phase framework proof)

Real allocation via `planCohort`/`verifyPlannedAllocationAgainstCurrentConfig`
(the R4 handoff pattern from P04.1/P04.2) against an **in-memory-only**
`opts.runnerConfig` override — a real, pre-existing, documented extension
point of `session-engine.mjs`/`cohort-planner.mjs` (used for the R4
re-verification since P04.x), never persisted to `.fgos/config.json`.
This was necessary because Cohort Planner V1's "never reuse one executor
across two actors in one plan" rule cannot allocate this fixture's 8
distinct actors against the real, unmodified config (only 7 real
`kind:"agent"` candidates carry any policy tier at all) — a genuine,
newly-discovered sizing gap, independently confirmed (see Gaps). The
override added `claude-clone-1/2` and `gemini-clone-1/2/3` as exact
clones of the real `claude`/`agy-cli` invocation blocks under new ids —
every dispatch on a clone still spawns the real CLI. `distinctProviderFamilies: 2`
(claude + gemini) satisfied cleanly, no fallback rule needed. All 8
`verifyPlannedAllocationAgainstCurrentConfig` calls: `ok:true, abort:false`
before dispatch.

All 9 real dispatches across the 6 phases settled (driver exit 0,
unattended, 0 retries, `budget.maxRuns:1` per op). Independently confirmed
on disk:

- **4 of 5 `gemini`-routed dispatches failed for the SAME real cause**:
  `facilitator-actor` (op_001), `clusterer-actor` (op_005), and BOTH
  synthesis ops (`op_008` convergent-synthesis, `op_009`
  recommend-with-dissent, both routed to `gemini-clone-3`) — every one's
  raw `stderr.log` reads `"Error: Individual quota reached. Please
  upgrade your subscription... Resets in ~3h"` (verified directly from
  `.fgos/assignments/*/runs/01/stderr.log` in the run's own ephemeral
  cwd, not from the Doer's or R8's narrative). This is a single external
  account-quota exhaustion event blocking the `gemini` family for the
  whole run window, not four independent causes.
- **2 explorer dispatches (`explorer-b`, `explorer-c`) failed on a real,
  pre-existing per-cwd concurrency lock** (`"dispatch for cwd ... is
  already in flight (held for 0s)"`), independently confirmed in
  `op_003`/`op_004`'s raw `stderr.log` — the same class of dispatch-layer
  concurrency cap already documented in P04.2b's precedent, not a bug
  introduced here.
- **`explorer-a`, `critic-actor`, and the supplementary `codex-pi`
  dispatch** hit the same `agent-result.json` schema-validation mismatch
  as R6, across THREE different providers (claude, claude, openai-codex)
  — a real, recurring, cross-provider friction point (see Gaps).
- **`evidence-reviewer-actor` (op_007)** is the only fully clean settle
  (`status:done, confidence:reported`, claude sonnet, analytical tier) —
  independently verified via `run-result-evidence-reviewer-actor-evidence-review.json`.

**Real chain content** (both edges independently verified from the raw
agent-report files): the critic correctly refused to fabricate a critique
of content it never received (clusterer failed before producing
clusters) and explicitly marked the objective's no-inline-editing
constraint check `"NOT-EVALUABLE, not clean"` rather than silently
passing it. The evidence-reviewer independently re-read the 3 explorer
`assignment.json` files the critic admitted skipping and **caught a real
factual error** in the critic's own claim ("enforced only at the critic
stage" — false; all 3 explorer contracts embed the identical constraint
text). This is a genuine multi-agent audit mechanism working as intended.

Provider families used: primary run targeted claude+gemini (fixture's
own diversity requirement, satisfied at plan time); gemini turned out
externally quota-blocked for the whole window (discovered live, not
assumed). One bounded, explicitly-labeled supplementary dispatch (not a
retry) reused `codex-pi` (openai-codex family) after confirming gemini's
unavailability, giving 2 real distinct families completing across
primary+supplement — a deliberate, recorded judgment call matching this
cell's own "your choice which family, e.g. availability/reliability
found during the attempt" allowance.

Wall time: facilitator 13,131 ms; explorer batch 122,231 ms (3-way
concurrent); clusterer 10,031 ms; critic 145,507 ms; evidence-review
206,239 ms; convergent-synthesis 10,714 ms; recommend-with-dissent
8,778 ms — sum ≈ 516,631 ms. Supplement 90,495 ms. 0 retries throughout.

## R8 — Independent quality report

Dispatched to a fresh evaluator (code-reviewer role) scoped to read ONLY
`case-lock.md` plus the raw R6/R7 artifact files — never the Doer's own
narrative, never the Coordinator's commentary — per case-lock's
evaluator-independence protocol. Full report:
`proofs/P05.2/r8-independent-quality-report.md`.

**Objective-constraint check**: HONORED by every piece of real content in
both modes (R6 explicitly names and rejects inline editing in its own
"Alternatives" section; R7's explorer-a and supplement-clusterer reports
explicitly re-check it). The framework's own designated verification gate
for this exact check (critical-challenge) never ran against real content
(its sole input, the clusterer, failed before producing clusters) — R8
correctly records this as "not evaluable," never as "passed."

**Per-dimension scores** (Strong/Adequate/Weak/None/N/A, case-lock
defines no numeric scale): R6 scores Strong on 5 of 9 scoreable
dimensions (evidence coverage, alternatives/risks, decision-criteria
coverage, actionability) plus Adequate on unsupported-claims (self-
audited, never independently checked in this proof). R7 scores Weak on
5 of 10 dimensions and None on decision-criteria coverage — its one
Strong-equivalent result is a genuine, verifiable win on dimension 2
(evidence-reviewer caught a real critic error), and its dissent-
preservation (dimension 5) behaved honestly (labeled missing siblings
"operational outliers," never fabricated a minority view) but was never
exercised end-to-end since the framework never reached a completed
synthesis.

**Overall verdict** (R8's own words): *"No demonstrated quality gain from
the framework run over the single-agent baseline in this proof... the
framework's own distinguishing cognitive mechanisms cannot be judged on
their merits because most of the pipeline never executed successfully."*
Because 8 of 9 R7 dispatches failed for infrastructure/dispatch-layer
reasons (gemini quota, cwd lock contention, schema validation) rather
than for reasons related to the framework's cognitive design, this proof
cannot distinguish "the mechanism underperforms a single agent" from "the
dispatch layer couldn't keep the pipeline alive long enough to test it."

**Disposition (Coordinator, independently re-verified — not re-trusted
from either the Doer's or R8's own narrative alone):** every headline
claim above was cross-checked directly against the raw artifact files
(`run-result-*.json` status/executorId fields, raw `stderr.log` quota and
schema-mismatch text, `case-lock.md`/`group-cognition-framework.yaml`
diffed as untouched, mdview's before/after git-status snapshots
byte-identical). Per case-lock's own explicit, frozen instruction — *"a
null or negative quality result... still closes this proof if both
contracts held... it triggers a documented product reassessment, never a
fabricated benefit"* — **this is exactly that: an honest null result.**
Both contracts held (real dispatch throughout, no fabrication, no
redefinition of the objective, no edit to the frozen lock or fixture).
Phase 05's own exit criterion — *"Real external-project comparison
records quality gain or honest null result with dissent preserved"* — is
satisfied by the null-result branch. The recommended product
reassessment (dispatch reliability: per-cwd concurrency locking under
real parallel fan-out, provider-quota headroom for `creative`/
`analytical`/`critical` tiers, the cross-provider `agent-result.json`
schema-validation friction) is recorded below and belongs to Phase 06/a
future cell, not to a re-run of this proof.

## Test-suite verification (before close)

Full suite regressed by exactly one test versus the documented baseline,
traced to this cell's own `.fgos/config.json` fix (`c852814d`, committed
in the prior cell): `test/runner/cohort-planner.test.mjs`'s
`buildCandidateInventory against the real committed .fgos/config.json`
test hardcoded `agy-cli`/`codex-pi`'s `supportedTiers` as `['lightweight']`
only — stale now that `gemini`/`openai-codex` genuinely carry all 5
tiers. Fixed by updating the two hardcoded arrays to the same 5-tier list
`claude`'s own assertion already used (`test/runner/cohort-planner.test.mjs:488,490`)
— the test's intent (read tiers from the real config, never guess) is
unchanged; only the expected data was stale.

```
FGOS_DISABLE_OPPORTUNISTIC_CHECKS=1 node --test 'test/**/*.test.mjs'
```
→ **4908 tests, 4890 pass, 13 fail (incl. the "failing tests:" banner
line), 5 skipped.** 10 of the 11 documented baseline rows present by
exact name (row 11, `withLockRetry: numeric holderPid self qualifier`,
did not fail this run — list may only shrink). 2 additional names not in
the documented 11-row table (`concurrent createSession from real
separate OS processes never loses a registry entry`,
`spawnWorker throws worker-spawn-fail with stdout captured up to a
maxBuffer kill`, `withLockRetry: renders remaining-TTL phrase and holder
qualifier when remainingTtlMs > 0`) — independently re-run standalone,
isolated from the full-suite load: **3/3 pass cleanly**, confirming
session-load flakiness (this session had ~50 background
teammates/subagents live during the run) matching the exact precedent
already documented for the `spawnWorker` maxBuffer-kill test in P04.2b's
own close note. **No new logic regression beyond the one test-data fix
above, which is itself now fixed and re-verified green.**

## Gaps

- **MEDIUM, documented not fixed**: Cohort Planner V1's "never reuse one
  executor across two actors in a plan" allocation rule cannot allocate
  the Group Cognition fixture's 8 actors against the real, unmodified
  `.fgos/config.json` today — only 7 real `kind:"agent"` candidates carry
  any policy tier at all. R7 worked around this with an in-memory-only
  `runnerConfig` override (a real, sanctioned extension point, never
  persisted) rather than adding real executor entries to the committed
  config, which was out of this cell's scope (config changes need their
  own review, same bar as the tier-floor fix). A real fix (either
  allowing controlled executor reuse in Cohort Planner V1, or registering
  enough real distinct executor ids per family) is a Phase 06/07 or
  later-track candidate, not blocking Phase 05's own close.
- **LOW, documented not fixed**: `codex-cli`/`codex-herdr` genuinely
  derive `providerFamily: "codex"` (no `providerModel` field set), NOT
  `"openai-codex"` as an earlier cell's own briefing assumed — confirmed
  directly against `deriveProviderFamily` (`src/runner/dispatch/resolve.mjs:135`)
  and the real config. The `c852814d` tier-floor fix genuinely benefits
  `codex-pi`/`pi-herdr` (real `providerModel: "openai-codex"`) and
  `agy-cli`/`agy-herdr` (real `providerModel: "gemini"`), but NOT
  `codex-cli`/`codex-herdr`, which remain configured only at `lightweight`
  under a `"codex"` family that has zero further tier entries. Worth
  correcting the assumption in any future cell's briefing; not a blocker
  since R7 used `codex-pi`, not `codex-cli`.
- **MEDIUM, documented not fixed**: a real, recurring `agent-result.json`
  schema-validation mismatch (model writes a richer/uppercase-status JSON
  shape than the schema accepts) hit dispatches across THREE different
  providers in this cell alone (R6's claude-reviewer, R7's explorer-a and
  critic-actor on claude, the supplement dispatch on codex-pi/openai-codex)
  — a cross-provider friction point in the agent-result contract or its
  validator, worth a dedicated investigation outside this cell's scope
  (this cell's Do-Not-Touch list forbids editing `session-engine.mjs`'s
  core logic or any dispatch/definitions file).
- **LOW, documented not fixed**: the supplementary `codex-pi` dispatch
  (one extra real dispatch beyond the fixture's 9 declared operations,
  added after discovering gemini's live quota block) stayed well within
  every numeric bound in case-lock's frozen Budgets section
  (`maxAssignments:20`, `wallTimeMs:3600000`) but was not pre-declared in
  case-lock's own Amendment log before the run, as that section's own
  text requires for "any deviation." Case-lock.md was correctly never
  edited post-freeze (editing it was forbidden for this cell) — recording
  the gap here instead, per the Amendment-log section's own instruction
  to name a weakened-evidence caveat explicitly rather than hide it. The
  supplement dispatch's own content is genuine, real, and clearly labeled
  as a distinct, later, separate dispatch throughout every artifact — the
  gap is procedural (no advance amendment entry), not evidentiary.
- **LOW, informational**: whether `gemini` genuinely "supports" the
  `creative`/`analytical`/`critical` tiers required by this fixture under
  real sustained load, versus only nominally routing to them before
  hitting an unrelated account-quota ceiling, is not resolved either way
  by this run — 4 of 5 gemini-routed dispatches failed for quota reasons
  before reaching a state that would prove or disprove tier-level
  reachability. A second live attempt, ideally scheduled outside the
  account's quota window, would be needed to close this specific
  question; not blocking Phase 05's own close, which only requires an
  honest result, not a positive one.
- **Recommended product reassessment** (per case-lock's own null-result
  clause, for Phase 06 or a later cell to pick up): (1) the per-cwd
  dispatch concurrency lock silently drops 2 of 3 parallel explorer
  branches under real fan-out load — worth surfacing louder than a
  generic "already in flight" message, or relaxing for genuinely
  independent cwds; (2) provider-quota headroom should be checked (e.g.
  via a lightweight pre-flight probe) before a multi-tier live framework
  run commits to a provider family, rather than discovering exhaustion
  mid-run; (3) the cross-provider `agent-result.json` schema mismatch
  above.

## Full-suite verification (pre-close)

See "Test-suite verification" above — 4890/4908 pass, all remaining 12
distinct failures match the documented baseline by exact name or were
independently confirmed as session-load flakiness via isolated re-run.
**No new failure beyond the documented baseline.**

```
node src/runner/dispatch.mjs decide --for smoke --needs-soul --has-live-task-access
```
→ `{"mechanism":"in-process","configured":false}` — unaffected.

## Phase 05 disposition

**R1-R8 all closed.** Exit criterion (*"Real external-project comparison
records quality gain or honest null result with dissent preserved"*) is
satisfied via the honest-null-result branch: real, live, independently
verified evidence; no fabrication; dissent-preservation behaved honestly
even though never exercised end-to-end. **Phase 05: done.**
