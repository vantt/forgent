# Cell P00.1 — Canonical decisions and contracts (Phase 00 R1-R4)

## Requirements

- R1 Canonical decisions (new ADR(s), rejected alternatives, compatibility posture)
- R2 Contracts and architecture (CoordinationSession + FlowDefinition/profile schemas; update runtime/protocol/dispatch-control-plane/work-integration/system-context/indexes/portal)
- R3 Vocabulary and area spec (Phase, SessionActor, Persona, Stance; Role=seat=responsibility; `docs/specs/runner.md` additions; reading-map pointers)
- R4 Decision reconciliation (Step 07 stale Phase 02 row; proposal annotation with extraction links; AC-I001..009 preserved; initial intent proof matrix)

## Proof Matrix

| Req | Doc Path | Positive Check | Negative Check | Evidence | Status |
|---|---|---|---|---|---|
| R1 | `decisions/ADR-008-coordination-session-and-mission-deferral.md`, `ADR-009-flow-definition-shared-ir-and-typed-profiles.md`, `ADR-010-interactive-headless-parity-and-work-isolation.md` | Required metadata block present on all three (Document type: ADR; Design status: Accepted; Implementation: Not started; Last reviewed: 2026-09-01; Canonical for: ...); each has Context/Decision/Consequences/Rejected Alternatives; SessionActor/Persona/Stance terminology (H1), one-way ledger (M7), `purpose` dropped (M8), Mission deferred-preserved (AC-I007) all recorded | ADR-006/007 body unedited | `git diff -- docs/architect/agent-coordination/decisions/ADR-006-*.md docs/architect/agent-coordination/decisions/ADR-007-*.md` produced empty output (see Commands) | done |
| R2 | `contracts/coordination-session.md`, `contracts/flow-definition.md`; updated `architecture/{runtime-model,protocol-model,dispatch-control-plane,work-integration,system-context}.md`, `contracts/README.md`, `architecture/README.md`, `decisions/README.md`, `README.md` | Both new contracts include versions/ids/refs/bounds (`aggregateBounds`), status vocabulary (`active\|completed\|partial\|failed`), topology, `spec.profile` discriminator, PolicyPatch provenance shape `{field:{value,source:{scope,id}}}`, and forbidden-field tables (no `missionId`; Assignment `FORBIDDEN_SESSION_FIELDS` reused from ADR-006 §6, not redefined); indexes list the two new contracts and three new ADRs; portal README summarizes the three new ADRs | No accepted page still calls the CoordinationSession/FlowDefinition boundary "unresolved"/"remains under design" — protocol-model.md's Stage-vs-Phase question and system-context.md's "remain under discussion" line for CoordinationSession identity are both resolved with ADR-008/009 pointers; a repo-wide anchor-link validation pass (Python script over every changed file) found zero broken cross-references | verified: python anchor-link check (`ALL LINKS OK`, see Commands) + manual re-read of protocol-model.md/system-context.md diffs | done |
| R3 | `vocabulary/canonical-concepts.md` (Phase, SessionActor, Persona, Stance, FlowDefinition entries; Role entry now states "seat/responsibility position" explicitly), `vocabulary/deprecated-and-reserved.md` (Participant reservation), `docs/specs/runner.md` (new "CoordinationSession — điều phối agent Work-độc-lập" section), `docs/specs/reading-map.md` (new pointer bullet) | Phase/SessionActor/Persona/Stance defined with design status; `Participant` collision resolved (H1) via explicit reservation entry + forbidden-equivalence line; Role=seat=responsibility stated in the Role entry itself | `Participant` term not reused for the new actor-instance concept anywhere in the changed files (only the reservation note references it, pointing to the platform-level meaning) | verified: read-back of both vocabulary files + grep for `Participant` in changed files shows only the reservation/forbidden-equivalence lines | done |
| R4 | `plans/260831-1637-step07-inline-assignment-mvp/plan.md` (Phase 2 row), `proposals/step-08-standalone-coordination-protocols.md` (extraction-pointer blockquotes at 6 sections), `intent-preservation-ledger.md` (AC-I003 source already correct on inspection, AC-I006 annotated with H2/P00.2 pointer + M8 trigger phrase, new "Step 08 Phase 00 Intent Proof Matrix (Cell P00.1)" section, decision-trace rows updated to Accepted) | Phase 2 row now reads "done (`c7c0ed39` ...)"; proposal has 6 extraction-pointer blockquotes, Design status still "Discussion"; AC-I001-009 headings all present (verified by script, 9/9) with no entry deleted or reinterpreted by omission | Proposal Design status line unchanged (`Discussion`) and metadata line clarifies pointers don't make it normative; ledger diff only adds annotations, never removes/relabels an existing AC-I entry | verified: python count of `### AC-I00[1-9]` headings = 9/9; manual re-read of plan.md row and proposal blockquotes | done |

## Commands

Doc-only cell; no test command required by phase-00 for R1-R4. Doer ran:

```bash
git diff --check -- docs/architect/agent-coordination docs/specs \
  plans/260831-1637-step07-inline-assignment-mvp/plan.md
# exit 0, no output (no whitespace/conflict-marker issues)

git diff -- docs/architect/agent-coordination/decisions/ADR-006-*.md \
  docs/architect/agent-coordination/decisions/ADR-007-*.md
# exit 0, no output (ADR-006/007 bodies unchanged)

git status --porcelain -- src test domains
# no output (no code touched)
```

A Python script (inline, not committed — no repository precedent for a
standalone link-checker script) parsed every `](*.md#anchor)` reference in
every file this cell created or modified under `docs/architect/agent-coordination/`
and `docs/specs/`, resolved the target file, and checked the anchor against a
GitHub-style slug of every heading in that file. Result: `ALL LINKS OK` (zero
missing files, zero missing anchors). A second script counted
`### AC-I00[1-9]` headings in `intent-preservation-ledger.md`: 9/9 present.

## Review

Reviewer: independent (this session), 2026-09-01, branch `step-08-standalone-coordination`.
Scope verified: `git status --porcelain -- src test domains` is empty (no
runtime code touched); `git diff -- decisions/ADR-006-*.md decisions/ADR-007-*.md`
is empty (bodies unedited); all three new ADRs carry the full Required
Metadata block plus Context/Decision/Consequences/Rejected Alternatives; the
proposal's `Design status` line is unchanged (`Discussion`) and carries 6
extraction-pointer blockquotes; `SessionActor` is used consistently and
`Participant` is explicitly reserved in `vocabulary/deprecated-and-reserved.md`;
`purpose` is dropped from FlowDefinition V1 with the revisit trigger recorded
in the ledger (AC-I006), not in ADR-009's body; `distinctModelFamilies` (no
source, per pre-plan finding H2) does not appear in the accepted
`flow-definition.md` contract — only the sourced `distinctProviderFamilies`
does; the Step 07 plan.md fix is scoped to exactly the stale Phase 2 row; all
9 `AC-I00[1-9]` ledger headings are present and none is weakened, deleted, or
silently reinterpreted; a spot-check of ~15 cross-references (vision.md V-001/
V-005/V-006/V-008/V-009/V-012 anchors, canonical-concepts.md's SessionActor/
Persona/Stance/Phase/FlowDefinition/Role anchors, deprecated-and-reserved.md's
Participant anchor, both contracts' PolicyPatch(-provenance) anchors,
ADR-001/workflow-stage-operation.md/assignment-run-runresult.md paths) all
resolved. `protocol-model.md`'s Stage-vs-Phase question and
`system-context.md`'s CoordinationSession/FlowDefinition boundary are both
resolved with ADR-008/ADR-009 pointers; the residual "remain under discussion"
text in `system-context.md` is correctly re-scoped to AdhocTask/full-runtime/
broader-protocol-catalog content that is genuinely still open, not to the
promoted identity/schema boundary.

**M1 (MEDIUM) — Misplaced extraction-pointer blockquote splits a coherent
bullet list and misattributes what was extracted**

File: `docs/architect/agent-coordination/proposals/step-08-standalone-coordination-protocols.md:1424-1448`
(the "Coordination storage and Work isolation" checkpoint block; extraction
pointer at line 1441).

Failing scenario: the checkpoint's "For a Work-attached session:" bullet list
has 5 items. The Doer inserted the `> Extracted into [ADR-010](...) and the
[CoordinationSession Contract](...) storage layout / Work Boundary sections.`
blockquote after only the first 3 bullets, leaving the last 2 bullets ("the
coordination ledger records workspace/isolation references but cannot merge
branches, advance Work, or invent a coding isolation policy" and "the first
standalone proofs stay read-only; Work-attached mutation remains a stop gate
until a coding-domain live proof...") trailing after the blockquote as an
apparently separate, unpointed-to list. Both trailing bullets are in fact also
covered by ADR-010 (Decision 4 "no coordination code path... gains merge
authority..."; Decision 5 "Work-attached mutation stays a stop gate") and by
the CoordinationSession Contract's Work Boundary section. Splitting the list
this way (a) breaks the list's Markdown continuity (a blockquote interrupts a
loose list, which most renderers show as two separate lists with a quote
between them) and (b) makes the extraction pointer read as if it only covers
the first 3 bullets, understating what was actually promoted to ADR-010.

Violated requirement: documentation-governance.md's "Cross-links must be
checked after every move or rename" / general accuracy-of-attribution
expectation for an accepted extraction pointer; adjacent to the cell's own Bug
Taxonomy category "stale cross-links after doc moves" (current-cell.md:63-64).

Evidence: `git diff -- docs/architect/agent-coordination/proposals/step-08-standalone-coordination-protocols.md`
hunk around the "Coordination storage and Work isolation" heading (see the
`> Extracted into [ADR-010]...` line inserted between 3rd and 4th bullet of a
5-bullet list).

Smallest fix: move the blockquote to after the full 5-bullet list (right
before `##### Interactive and headless operating modes`), or duplicate it
once for the storage/isolation half and once for the parity half if the two
concerns should be pointed to separately. Non-blocking for merge; a one-line
Markdown move.

**L1 (LOW) — ADR-010's Context section cites pre-plan finding M5c as if it
already substantiated "human-confirmed intention," when M5c's own text
records the opposite as of the review**

File: `docs/architect/agent-coordination/decisions/ADR-010-interactive-headless-parity-and-work-isolation.md:17-23`.

ADR-010's Context reads: "The 2026-09-01 Step 08 checkpoint separately records
target capability *parity* as an original human-confirmed intention: ...
(pre-plan review finding M5c; ledger entry AC-I008)." Read in isolation, the
`(pre-plan review finding M5c...)` citation looks like it is naming M5c as a
source that already confirms parity as human-confirmed intent. But
`plans/reports/reviewer-260901-1403-GH-07-step08-pre-plan-architecture-review.md`
M5(c) says the opposite at review time: "Missing intention AC-I008 'mechanism
parity' — withdrawn after rebuttal... Not human-confirmed → not a ledger
entry," and the same report's "Unresolved questions for the human" section
lists "(b) is 'interactive and headless doors have identical coordination
capability' a product intention to record in the ledger... " as still open.
The actual confirmation is a separate, later source: the proposal's own
"Discussion Checkpoint" text (`proposals/step-08-standalone-coordination-protocols.md:1461`,
"Target capability parity is an original intention...") and the ledger's own
AC-I008 entry, which correctly cites "explicit human clarification recorded on
2026-09-01 in the Step 08 checkpoint" (`intent-preservation-ledger.md:189-190`)
— not the pre-plan review. This is plausibly just a citation-ordering
shorthand (M5c raised the open question; the checkpoint then resolved it, same
day), and the ledger entry itself is sourced correctly, so this is not a
misrepresentation of substance — AC-I008 is legitimately `deferred-preserved`
and Decision 3 correctly does not over-claim a "shipped V1 guarantee." But a
reader who checks only the cited M5c text against ADR-010's claim will find
what looks like a direct contradiction.

Violated requirement: none blocking; documentation-governance.md's general
expectation that citations support the sentence they are attached to.

Smallest fix: reword ADR-010's Context citation to something like "(pre-plan
review finding M5c raised this as an open question; the 2026-09-01 checkpoint
records the maintainer's resolution; ledger entry AC-I008)" so the citation
doesn't read as if M5c itself confirmed the intention.

**L2 (LOW, non-blocking, informational) — `AGENTS.md`/`CLAUDE.md` show a diff
outside `docs/`**

`current-cell.md`'s Do Not Touch lists "any file outside `docs/`." `git diff --stat`
shows 2-line diffs in both `AGENTS.md` and `CLAUDE.md`, both outside `docs/`.
Inspecting the diff: both changes are solely the auto-regenerated GitNexus
`<!-- gitnexus:start -->...<!-- gitnexus:end -->` symbol/relationship count
line (26672→27846 symbols, 36902→38260 relationships), which that same block's
own comment states "regenerates from GitNexus's own template on `gitnexus
analyze`" — not hand-authored content, and not evidence the Doer edited these
files. Recorded for completeness; not a real scope violation and does not
change the CLEAN/FINDINGS calculus.

Verdict: FINDINGS (1 MEDIUM, 2 LOW; 0 HIGH)

**Coordinator disposition (2026-09-01):**
- M1: ACCEPTED — fix. Dispatched to Fixer.
- L1: ACCEPTED — fix (bundled with M1, same Fixer pass, trivial reword).
- L2: NO ACTION — confirmed non-issue (auto-regenerated GitNexus index-count
  block, not authored content, not a scope violation). No fix needed.

## Red-Team

Red-Team: independent (this session), 2026-09-01, branch `step-08-standalone-coordination`.
Scope: attempted every applicable attack from the documentation-authority
playbook (silent contract reopening, proposal laundering, forbidden-field
escape hatch, AC-I traceability weakening, dangling cross-links on a sample
disjoint from the Reviewer's, metadata/self-certification drift,
out-of-scope creep, report/reality mismatch) against the Doer's work, the
Reviewer's findings (M1/L1/L2), and the Fixer's M1/L1 patch. The `missionId`
forbidden-field check, the AC-I001/AC-I006/AC-I007 wording comparison against
`plan.md`'s Intent Traceability table, a 20-link cross-reference sample
disjoint from the Reviewer's ~15, and the Fixer's M1/L1 patch verification
against the live files all passed without finding a gap — no write-up below
because those attacks did not succeed.

**H1 (HIGH) — ADR-008 Decision 4's "detect-and-report" posture for legacy
`.fgos/missions/` contradicts `plan.md`'s own Locked Product Decision text,
and is self-certified `Design status: Accepted` without ever naming the
conflict**

Files: `docs/architect/agent-coordination/decisions/ADR-008-coordination-session-and-mission-deferral.md:63-74,128-133`
(Decision 4 and the corresponding Rejected Alternatives entry); propagated
into `docs/architect/agent-coordination/intent-preservation-ledger.md:238`
(Current Step 08 Decision Trace row) and `docs/specs/runner.md:~1197-1205`
(new CoordinationSession persistence paragraph, confirmed by the Doer's own
report line 76: "mission-lite direct cutover with detect-and-report
posture").

Failing scenario (attack 6, self-certification, and attack 1, silent
contract reopening): `current-cell.md`'s Must Read list treats
`plans/260901-1542-step08-standalone-coordination/plan.md`'s "Locked Product
Decisions" as accepted, maintainer-approved authority entering this cell
(plan.md header: "Status: approved for implementation | Owner: maintainer").
That text states, verbatim, at `plan.md:68-71`:

> Mission-lite is replaced directly. There is no migration reader, detector,
> reporter, compatibility writer, or stored-data migration.

ADR-008 Decision 4 instead asserts (`ADR-008…md:63-68`):

> Direct mission-lite cutover, with a detect-and-report posture for legacy
> data... The one exception: if a legacy `.fgos/missions/` directory is
> found at runtime, the system reports its presence plainly (for example
> through `doctor` or session startup) rather than silently ignoring it.
> This is a report-only check, not an automatic migration reader...

A runtime check that finds a legacy directory and reports its presence is
precisely a detector plus a reporter — the two component types `plan.md`
names and rejects by name. Grepping the actual maintainer-approved checkpoint
text (`proposals/step-08-standalone-coordination-protocols.md:171-173`,
"Locked in the 2026-09-01 checkpoint") confirms it says only "The unreleased
mission-lite prototype is replaced directly; no legacy record reader or
migration is built" — no detect-and-report requirement appears anywhere in
the checkpoint's "Human decision recorded" text. The only source for
"detect-and-report" in the entire diff is the pre-plan architecture review's
finding M4 and its Rebuttal Reconciliation row 6
(`plans/reports/reviewer-260901-1403-GH-07-step08-pre-plan-architecture-review.md:159,267`),
which is a reviewer's recommendation for the plan-writing step to weigh, not
itself an "explicit human decision" — the ledger's own rule
(`intent-preservation-ledger.md:43-45`) says only human-confirmed product
intentions belong in canonical docs. `plan.md`, written after that review,
appears to have landed on the stricter no-detector/no-reporter position
instead (it also separately bars "migration compatibility" in its Out Of
Scope list). ADR-008 cites only the pre-plan review and reachability
evidence in its Context and Rejected Alternatives sections and never
acknowledges or reconciles the conflict with `plan.md`'s own locked text,
yet carries `Design status: Accepted` and `Last reviewed: 2026-09-01`. The
same unreconciled claim was then copied into the ledger's Decision Trace row
and into the new `docs/specs/runner.md` section, so three separate
documents now assert a mechanism `plan.md`'s Locked Product Decisions
explicitly rules out by name — and the Doer, Reviewer, and Fixer all passed
over it.

Violated requirement: `plan.md`'s own Autonomous Execution Contract stop-gate
1 — "an accepted source above contradicts a requirement and no additive
interpretation exists" — since `plan.md` is itself one of the cell's
required-read accepted sources and textually contradicts ADR-008 Decision 4;
`current-cell.md`'s Bug Taxonomy category "Silently promoting proposal prose
to normative without extraction" also applies, since a reviewer's finding
(not a locked decision) was promoted straight to `Accepted`.

Evidence: the three greps above (`plan.md:68-71`, `ADR-008…md:63-74`,
`proposals/step-08-standalone-coordination-protocols.md:171-173`) plus the
Doer's report line 76 confirming intent, not accident.

Smallest fix direction: either (a) strike the "detect-and-report" exception
from ADR-008 Decision 4/Rejected Alternatives, the ledger's Decision Trace
row, and the `runner.md` persistence paragraph, aligning with `plan.md`'s
literal "no detector, reporter" text; or (b) if detect-and-report is in fact
the intended final maintainer position, get that confirmed explicitly and
amend `plan.md`'s Locked Product Decisions bullet to say so before re-citing
it as ADR-008's source. This is a genuine open product decision that the
plan's own stop-gate 1 requires halting for, not one an autonomous cell may
resolve unilaterally by reverting to an earlier, superseded review
disposition.

**L1 (LOW) — CoordinationSession contract's storage layout names an
`AdhocTask` persistence path that `plan.md`'s Locked Product Decisions
excludes from V1**

File: `docs/architect/agent-coordination/contracts/coordination-session.md:25`
(Storage Layout).

Failing scenario (attack 7, out-of-scope creep): the contract's Storage
Layout block includes `tasks/<task-id>.json # optional session-local
AdhocTask records`. `plan.md`'s Locked Product Decisions states: "`purpose`
routing, executor fitness scoring, autonomous provider routing, organization
overlays, first-class AgentMessage, **general AdhocTask storage**,
unrestricted peer chat, and a framework marketplace are out of V1." Phase
00's R2 (the requirement this contract exists to satisfy) lists only
CoordinationSession persistence/recovery field categories — no AdhocTask.
The pre-plan review explicitly recommended keeping AdhocTask deferred until
Phase 8.4 fan-in needs roll-up state. Naming and reserving a canonical
storage path for "AdhocTask records" inside an `Accepted` contract — even
undefined/optional — pre-authorizes a persistence hook for a concept
`plan.md` names as explicitly out of V1, ahead of any phase actually
designing it.

Violated requirement: `plan.md` Out Of Scope / Locked Product Decisions
("general AdhocTask storage... are out of V1").

Smallest fix: remove the `tasks/<task-id>.json` line from Storage Layout (or
mark it reserved-and-undefined, explicitly out of scope until a future ADR
designs AdhocTask), matching the care the rest of the contract already takes
with `missionId` (named forbidden rather than silently reserved).

Verdict: FINDINGS (1 HIGH, 0 MEDIUM, 1 LOW)

**Coordinator disposition (2026-09-01):**
- H1: ACCEPTED — option (a), fix. Independently re-verified against the
  primary checkpoint text directly (not just the pre-plan review's citation
  of it): `proposals/step-08-standalone-coordination-protocols.md:1495-1496`
  ("Locked in the 2026-09-01 checkpoint" section) reads "direct-cut over
  mission-lite code and tests; do not add a legacy reader, detector,
  reporter, or stored-data migration path" — this matches `plan.md:68-71`
  verbatim in substance. Both the primary human-approved checkpoint (higher
  authority than a reviewer's recommendation) and the plan itself reject any
  detect-and-report mechanism. The pre-plan review's M4/Rebuttal
  Reconciliation row 6 was a recommendation for the plan-writing step to
  weigh; `plan.md`, written after, did not adopt it. This is not a genuinely
  unresolved decision — it is resolvable directly from the two
  higher-authority sources already read into this cell, per
  review-audit-self-decision.md's "Scout First" and "Verified Decisions"
  rules. No stop gate. Strike the detect-and-report exception from ADR-008
  Decision 4 + Rejected Alternatives, the ledger's Decision Trace row
  (`intent-preservation-ledger.md:238`), and the `runner.md` persistence
  paragraph; align with `plan.md`/checkpoint's literal "no detector, no
  reporter" text.
- L1: ACCEPTED — fix. Remove the `tasks/<task-id>.json` AdhocTask line from
  `coordination-session.md`'s Storage Layout (AdhocTask storage is out of V1
  per `plan.md`'s Locked Product Decisions).
- Both dispatched to a Fixer; a focused Red-Team re-check (attacks 1 and 6
  only, plus a fresh read of the corrected files) follows the fix before this
  cell can close.

## Gaps

(none recorded yet — cell just opened)

## Fix Evidence

Fixer pass (2026-09-01) addressing the two ACCEPTED findings from the
Coordinator disposition above (M1, L1). Scope: exactly these two files, no
other content touched.

**M1 fix** — `proposals/step-08-standalone-coordination-protocols.md`
(around lines 1424-1450). Moved the `> Extracted into [ADR-010](...) and the
[CoordinationSession Contract](...) storage layout / Work Boundary
sections.` blockquote from between the 3rd and 4th bullets of the
"For a Work-attached session:" 5-item list to after the full 5-bullet list,
immediately before the `##### Interactive and headless operating modes`
heading. Diff shape:

```diff
 - mutating activity is either serialized in one authorized workspace or placed
   in distinct domain-provisioned worktrees/branches with resource claims;
-
-> Extracted into [ADR-010](../decisions/ADR-010-interactive-headless-parity-and-work-isolation.md)
-> and the [CoordinationSession Contract](../contracts/coordination-session.md)
-> storage layout / Work Boundary sections.
 - the coordination ledger records workspace/isolation references but cannot
   merge branches, advance Work, or invent a coding isolation policy;
 - the first standalone proofs stay read-only; Work-attached mutation remains a
   stop gate until a coding-domain live proof demonstrates conflict detection,
   isolation, merge ownership, and recovery.
+
+> Extracted into [ADR-010](../decisions/ADR-010-interactive-headless-parity-and-work-isolation.md)
+> and the [CoordinationSession Contract](../contracts/coordination-session.md)
+> storage layout / Work Boundary sections.

 ##### Interactive and headless operating modes
```

**L1 fix** — `decisions/ADR-010-interactive-headless-parity-and-work-isolation.md`
(Context section, around lines 16-22). Reworded the M5c citation so it no
longer reads as if M5c itself already confirmed capability parity. Diff
shape:

```diff
-before it runs unattended (pre-plan review finding M5c; ledger entry
-AC-I008).
+before it runs unattended (pre-plan review finding M5c raised this as an open
+question; the 2026-09-01 checkpoint records the maintainer's resolution;
+ledger entry AC-I008).
```

No other lines in either file were changed. ADR-010's Decision/Consequences
sections and the rest of the proposal file are untouched.

---

Fixer round 2 (2026-09-01) addressing the two ACCEPTED Red-Team findings
(H1, L1) per the Coordinator disposition above. Scope: exactly the 4 files
named in the disposition, only the passages identified.

**H1 fix** — `decisions/ADR-008-coordination-session-and-mission-deferral.md`:
- Decision 4 (was lines 63-74): replaced the "detect-and-report posture for
  legacy data" text (which described a runtime check reporting a legacy
  `.fgos/missions/` directory via `doctor`/session startup as "the one
  exception") with plain text: "Direct mission-lite cutover, no
  compatibility mechanism... No migration reader, detector, reporter,
  compatibility writer, or stored-data migration is built for
  `.fgos/missions/` — matching `plan.md`'s Locked Product Decisions and the
  2026-09-01 maintainer checkpoint verbatim."
- Rejected Alternatives (was the "A migration reader/compatibility
  writer..." entry, lines ~128-133): inverted the entry to record that a
  detect-and-report posture (pre-plan review finding M4) was considered but
  rejected because `plan.md:68-71` and the 2026-09-01 checkpoint
  (`proposals/step-08-standalone-coordination-protocols.md:1495-1496`) both
  explicitly rule out any detector/reporter mechanism, superseding M4's
  recommendation.

Propagation:
- `intent-preservation-ledger.md:238` (Decision Trace table, "Mission-lite
  direct cutover" row): changed the effect-description cell from "...with a
  detect-and-report posture for legacy `.fgos/missions/` data" to "...with
  no legacy reader, detector, reporter, or stored-data migration for
  `.fgos/missions/` data."
- `docs/specs/runner.md` (CoordinationSession Persistence paragraph,
  ~line 1201-1204): changed "bị thay thế TRỰC TIẾP — không có
  reader/migration tương thích ngược, TRỪ một điều: nếu tìm thấy dữ liệu
  `.fgos/missions/` cũ trên đĩa thì phải BÁO RÕ (không im lặng bỏ qua),
  không tự động migrate nó" to "bị thay thế TRỰC TIẾP — không có
  reader/detector/reporter/migration tương thích ngược nào cho
  `.fgos/missions/`" (removes the detect-and-report carve-out, states
  plain direct cutover with no detector/reporter).

Verified no remaining "detect-and-report" text describes it as the adopted
posture in any of the three files (`grep -n "detect-and-report"` on all
three: only match left is the ADR-008 Rejected Alternatives entry naming
what was considered and rejected).

**L1 fix** — `contracts/coordination-session.md` Storage Layout block (was
line 25): removed the line `tasks/<task-id>.json # optional session-local
AdhocTask records` entirely (not commented out, not marked reserved) since
`plan.md`'s Locked Product Decisions states general AdhocTask storage is
out of V1. Diff shape:

```diff
     session.json                     # manifest (Decision below)
     events.jsonl                     # append-only session event log
-    tasks/<task-id>.json             # optional session-local AdhocTask records
     synthesis.md                     # optional synthesis artifact
```

No other files or sections touched. `src/`, `test/`, `domains/` untouched;
no commit made.

### Red-Team re-check (H1/L1 fix)

Independent re-check (this session), 2026-09-01, branch
`step-08-standalone-coordination`. Read the four fixed files' live current
content directly (not the Fix Evidence prose) and cross-checked against
`plan.md:68-71` and the checkpoint (`proposals/step-08-standalone-coordination-protocols.md:1493-1499`).

- `ADR-008…md` Decision 4 (lines ~63-68) now reads plainly: "No migration
  reader, detector, reporter, compatibility writer, or stored-data migration
  is built for `.fgos/missions/` — matching `plan.md`'s Locked Product
  Decisions and the 2026-09-01 maintainer checkpoint verbatim." No exception
  carve-out remains. The Rejected Alternatives entry (~line 121) correctly
  frames the detect-and-report idea as considered-and-**rejected**, citing
  both `plan.md:68-71` and the checkpoint's `1495-1496` line as the reason —
  not adopted.
- Cross-check against primary sources: `plan.md:68-71` reads "There is no
  migration reader, detector, reporter, compatibility writer, or stored-data
  migration"; the checkpoint's 8.1 bullet reads "direct-cut over mission-lite
  code and tests; do not add a legacy reader, detector, reporter, or
  stored-data migration path." ADR-008's corrected Decision 4 text matches
  both verbatim in substance, no remaining contradiction.
- `intent-preservation-ledger.md:238` (Decision Trace row, "Mission-lite
  direct cutover"): "Removes prototype code/tests directly, with no legacy
  reader, detector, reporter, or stored-data migration for
  `.fgos/missions/` data." No detect-and-report survivor.
- `docs/specs/runner.md:1201-1203`: "bị thay thế TRỰC TIẾP — không có
  reader/detector/reporter/migration tương thích ngược nào cho
  `.fgos/missions/`." Matches the corrected posture; no lingering
  report/detect claim (old "BÁO RÕ" carve-out text confirmed gone via grep).
- `contracts/coordination-session.md` Storage Layout (lines ~19-24): the
  `tasks/<task-id>.json` AdhocTask line is genuinely gone — only
  `session.json`, `events.jsonl`, `synthesis.md` remain. Repo-wide grep for
  `AdhocTask` shows no other file defining an accepted storage path for it;
  all other hits are legitimate forward-looking discussion in
  proposals/vision/vocabulary docs, unaffected by this fix.
- Scope: `git status --porcelain -- src test domains` empty. `git status
  --porcelain` shows modifications confined to `AGENTS.md`/`CLAUDE.md`
  (pre-existing, auto-regenerated GitNexus block, L2 no-action) plus files
  under `docs/architect/agent-coordination/**`, `docs/specs/{reading-map.md,
  runner.md}`, and `plans/260831-1637-step07-inline-assignment-mvp/plan.md`
  — matches the cell's declared footprint across both Fixer passes; nothing
  unexpected.
- New-contradiction sweep: grepped ADR-009, ADR-010, and
  `contracts/flow-definition.md` for `detect-and-report`/`detector`/
  `reporter`/`AdhocTask`/`tasks/<task-id>` — zero matches in any of the
  three. Repo-wide grep for `detect-and-report` shows the only remaining hit
  inside canonical/living docs is ADR-008's own Rejected Alternatives entry
  (correctly naming the rejected option); the other hits are in stateful
  report/verification files (this file, plan reports) or an unrelated topic
  in `docs/history/events-jsonl-merge-abort-truncation-gap/RESEARCH.md`
  (CI-wiring detect-and-report, a different subject, pre-existing, untouched
  by this cell).
- `git diff --check` clean; `git diff -- decisions/ADR-006-*.md
  decisions/ADR-007-*.md` still empty.

Verdict: FIX CONFIRMED
