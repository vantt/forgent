# tsk-3wq: .fgos/events.jsonl is a shared, append-only log that multiple
# concurrent sessions/branches write to. Git's default line-based 3-way
# merge treats two-sided end-of-file appends as a conflict; a hand-resolved
# conflict on this file has already silently dropped real events at least
# once (docs/history/live-events-seq-corruption/CONTEXT.md, tsk-n4i D1).
# `union` is git's own built-in merge driver (no local config needed): it
# keeps lines from both sides instead of leaving conflict markers, which is
# exactly the right primitive for a provably append-only log. It does not
# guarantee order or dedupe/reseq the result — see
# scripts/events-jsonl-contiguity.mjs and the `events-jsonl-contiguous`
# fgos doctor check for that half.
.fgos/events.jsonl merge=union
