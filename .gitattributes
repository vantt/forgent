# tsk-2xg originally added per-file `merge=union` rules here for 5
# diagnostic logs (approve-post-success-faults.jsonl, invocation-faults.jsonl,
# main-checkout-guard-warnings.jsonl, changelog-nag-history.jsonl,
# entropy-history.jsonl) to stop fgos-write-rejected deadlocks on worker
# branches. Removed (phase-01, plans/260825-0842-fgos-logs-dir-bucketing):
# all 5 moved under the gitignored `.fgos/logs/` bucket, so git merge never
# sees them at all now — no union rule needed, and no new one needed for
# whatever else lands in that bucket later.

# tsk-1wk: tsk-3ve's per-writer sharded event log (.fgos/events/<writer-id>-
# <openTs>.jsonl) is append-only exactly like the old single-file
# .fgos/events.jsonl above, but wasn't covered by that same union-merge
# rule — every session's own live shard hit the identical append-conflict
# problem tsk-3wq/tsk-2xg already solved for the old layout.
.fgos/events/*.jsonl merge=union

