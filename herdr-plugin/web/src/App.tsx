// Framework placeholder (tsk-yo0) -- no screens yet. Taskboard (tsk-5jr) and
// task detail (tsk-4id) build the real views on top of the API client in
// src/api/.
export default function App() {
  return (
    <div className="flex min-h-svh items-center justify-center text-slate-500">
      herdr web dashboard
    </div>
  )
}
